# Embedded Services (اردو)

🌐 **Languages:** 🇺🇸 [English](../../../../frameworks/EMBEDDED-SERVICES.md) · 🇪🇹 [am](../../../am/docs/frameworks/EMBEDDED-SERVICES.md) · 🇸🇦 [ar](../../../ar/docs/frameworks/EMBEDDED-SERVICES.md) · 🇦🇿 [az](../../../az/docs/frameworks/EMBEDDED-SERVICES.md) · 🇧🇬 [bg](../../../bg/docs/frameworks/EMBEDDED-SERVICES.md) · 🇧🇩 [bn](../../../bn/docs/frameworks/EMBEDDED-SERVICES.md) · 🇨🇿 [cs](../../../cs/docs/frameworks/EMBEDDED-SERVICES.md) · 🇩🇰 [da](../../../da/docs/frameworks/EMBEDDED-SERVICES.md) · 🇩🇪 [de](../../../de/docs/frameworks/EMBEDDED-SERVICES.md) · 🇬🇷 [el](../../../el/docs/frameworks/EMBEDDED-SERVICES.md) · 🇪🇸 [es](../../../es/docs/frameworks/EMBEDDED-SERVICES.md) · 🇪🇪 [et](../../../et/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇷 [fa](../../../fa/docs/frameworks/EMBEDDED-SERVICES.md) · 🇫🇮 [fi](../../../fi/docs/frameworks/EMBEDDED-SERVICES.md) · 🇫🇷 [fr](../../../fr/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇪 [ga](../../../ga/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇳 [gu](../../../gu/docs/frameworks/EMBEDDED-SERVICES.md) · 🇳🇬 [ha](../../../ha/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇱 [he](../../../he/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇳 [hi](../../../hi/docs/frameworks/EMBEDDED-SERVICES.md) · 🇭🇷 [hr](../../../hr/docs/frameworks/EMBEDDED-SERVICES.md) · 🇭🇺 [hu](../../../hu/docs/frameworks/EMBEDDED-SERVICES.md) · 🇦🇲 [hy](../../../hy/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇩 [id](../../../id/docs/frameworks/EMBEDDED-SERVICES.md) · 🇳🇬 [ig](../../../ig/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇹 [it](../../../it/docs/frameworks/EMBEDDED-SERVICES.md) · 🇯🇵 [ja](../../../ja/docs/frameworks/EMBEDDED-SERVICES.md) · 🇬🇪 [ka](../../../ka/docs/frameworks/EMBEDDED-SERVICES.md) · 🇰🇭 [km](../../../km/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇳 [kn](../../../kn/docs/frameworks/EMBEDDED-SERVICES.md) · 🇰🇷 [ko](../../../ko/docs/frameworks/EMBEDDED-SERVICES.md) · 🇱🇹 [lt](../../../lt/docs/frameworks/EMBEDDED-SERVICES.md) · 🇱🇻 [lv](../../../lv/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇳 [ml](../../../ml/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇳 [mr](../../../mr/docs/frameworks/EMBEDDED-SERVICES.md) · 🇲🇾 [ms](../../../ms/docs/frameworks/EMBEDDED-SERVICES.md) · 🇲🇹 [mt](../../../mt/docs/frameworks/EMBEDDED-SERVICES.md) · 🇲🇲 [my](../../../my/docs/frameworks/EMBEDDED-SERVICES.md) · 🇳🇵 [ne](../../../ne/docs/frameworks/EMBEDDED-SERVICES.md) · 🇳🇱 [nl](../../../nl/docs/frameworks/EMBEDDED-SERVICES.md) · 🇳🇴 [no](../../../no/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇳 [or](../../../or/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇳 [pa](../../../pa/docs/frameworks/EMBEDDED-SERVICES.md) · 🇵🇭 [phi](../../../phi/docs/frameworks/EMBEDDED-SERVICES.md) · 🇵🇱 [pl](../../../pl/docs/frameworks/EMBEDDED-SERVICES.md) · 🇵🇹 [pt](../../../pt/docs/frameworks/EMBEDDED-SERVICES.md) · 🇧🇷 [pt-BR](../../../pt-BR/docs/frameworks/EMBEDDED-SERVICES.md) · 🇷🇴 [ro](../../../ro/docs/frameworks/EMBEDDED-SERVICES.md) · 🇷🇺 [ru](../../../ru/docs/frameworks/EMBEDDED-SERVICES.md) · 🇱🇰 [si](../../../si/docs/frameworks/EMBEDDED-SERVICES.md) · 🇸🇰 [sk](../../../sk/docs/frameworks/EMBEDDED-SERVICES.md) · 🇸🇮 [sl](../../../sl/docs/frameworks/EMBEDDED-SERVICES.md) · 🇷🇸 [sr](../../../sr/docs/frameworks/EMBEDDED-SERVICES.md) · 🇸🇪 [sv](../../../sv/docs/frameworks/EMBEDDED-SERVICES.md) · 🇰🇪 [sw](../../../sw/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇳 [ta](../../../ta/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇳 [te](../../../te/docs/frameworks/EMBEDDED-SERVICES.md) · 🇹🇭 [th](../../../th/docs/frameworks/EMBEDDED-SERVICES.md) · 🇹🇷 [tr](../../../tr/docs/frameworks/EMBEDDED-SERVICES.md) · 🇺🇦 [uk-UA](../../../uk-UA/docs/frameworks/EMBEDDED-SERVICES.md) · 🇺🇿 [uz](../../../uz/docs/frameworks/EMBEDDED-SERVICES.md) · 🇻🇳 [vi](../../../vi/docs/frameworks/EMBEDDED-SERVICES.md) · 🇳🇬 [yo](../../../yo/docs/frameworks/EMBEDDED-SERVICES.md) · 🇨🇳 [zh-CN](../../../zh-CN/docs/frameworks/EMBEDDED-SERVICES.md) · 🇹🇼 [zh-TW](../../../zh-TW/docs/frameworks/EMBEDDED-SERVICES.md)

---

> **ورژن:** v3.8.44
> **آخری تازہ کاری:** 2026-09-09
> **قارئین:** ایمبیڈڈ سروسز (9Router، CLIProxyAPI، Mux، Bifrost، open-wa) شامل کرنے، برقرار رکھنے، یا ان میں خرابیوں کی تشخیص کرنے والے انجینئرز۔

ایمبیڈڈ سروسز مقامی طور پر انسٹال کردہ پروسیس سائڈکار ٹولز ہیں جنہیں OmniRoute انسٹال کرتا، زیرِ نگرانی رکھتا، اور
اوّل درجے کے روٹنگ اہداف کے طور پر دستیاب کرتا ہے۔ بیرونی فراہم کنندگان (جن تک انٹرنیٹ کے ذریعے
API کلیدوں سے رسائی حاصل کی جاتی ہے) کے برعکس، ایمبیڈڈ سروسز OmniRoute والی ہی مشین پر چلتی ہیں اور لوپ بیک کے ذریعے مواصلت کرتی ہیں۔

---

## فہرستِ مضامین

1. [جائزہ](#1-overview)
2. [آرکیٹیکچر — 4 پرتیں](#2-architecture--4-layers)
3. [لائف سائیکل اسٹیٹ مشین](#3-lifecycle-state-machine)
4. [API حوالہ](#4-api-reference)
5. [سیکیورٹی](#5-security)
6. [ایک نئی ایمبیڈڈ سروس شامل کرنا](#6-adding-a-new-embedded-service)
7. [خرابیوں کا ازالہ](#7-troubleshooting)
8. [اکثر پوچھے گئے سوالات](#8-faq)

---

## 1. جائزہ

### ایمبیڈڈ سروسز کیوں؟

چھ سروسز ایمبیڈ کی گئی ہیں:

| سروس            | npm پیکیج                        | ڈیفالٹ پورٹ | مقصد                                                                                                                                                                                                      |
| --------------- | -------------------------------- | :---------: | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **9Router**     | `9router`                        |    20130    | AI روٹر جسے OmniRoute ذیلی فراہم کنندہ کے طور پر استعمال کر سکتا ہے۔ ماڈلز `9router/{sub}/{model}` کے طور پر دستیاب کیے جاتے ہیں                                                                          |
| **CLIProxyAPI** | GitHub ریلیز بائنری (`cliproxy`) |    8317     | Anthropic CLI تصدیقی بہاؤ کے لیے مقامی پراکسی اڈاپٹر۔ OAuth ٹوکنز کی میعاد ختم ہونے پر فال بیک روٹنگ فراہم کرتا ہے                                                                                        |
| **Mux**         | `mux` (ہیڈ لیس `mux server`)     |    8322     | مقامی ایجنٹ آرکیسٹریشن ڈیمَن (coder/mux)۔ صرف لائف سائیکل کا انتظام کیا جاتا ہے — یہ روٹنگ ہدف نہیں ہے (کوئی LLM پراکسینگ نہیں)۔                                                                          |
| **Bifrost**     | `@maximhq/bifrost`               |    8080     | Go AI گیٹ وے ریلے بیک اینڈ۔ چلنے کی صورت میں، ریلے روٹ (`/v1/relay/`) اسے خودکار طور پر منتخب کرتا ہے                                                                                                     |
| **Dario**       | `@askalf/dario`                  |    3456     | Claude سبسکرپشن پراکسی — Claude-Code طرز کی ٹریفک کے لیے CLIProxyAPI کا متبادل/فیل اوور؛ انجیکٹ کردہ کلید `DARIO_ADMIN_TOKEN` بن جاتی ہے، جو اس کے `/admin/*` OAuth کنٹرول پلین تک رسائی کو محدود کرتی ہے |
| **open-wa**     | `@open-wa/wa-automate`           |    8323     | WhatsApp Web آٹومیشن (Puppeteer کے ذریعے ہیڈ لیس Chromium)۔ صرف لائف سائیکل کا انتظام کیا جاتا ہے — یہ روٹنگ ہدف نہیں ہے۔                                                                                 |

تمام چھ ایک ہی نگرانی کے ماڈل کی پیروی کرتی ہیں:

- OmniRoute انہیں `DATA_DIR/services/{name}/` کے تحت انسٹال کرتا ہے (OmniRoute کی اپنی `package.json` سے الگ تھلگ)
- OmniRoute انہیں چائلڈ پراسیسز کے طور پر چلاتا اور مانیٹر کرتا ہے
- OmniRoute چائلڈ کے ماحول میں ایک عارضی API کلید انجیکٹ کرتا ہے اور اسے بغیر کسی ڈاؤن ٹائم کے تبدیل کرتا ہے (جہاں قابل اطلاق ہو)
- تمام انتظامی روٹس (`/api/services/*`) **LOCAL_ONLY** ہیں — صرف لوپ بیک سے قابل رسائی (قطعی اصول #17)

### اہم فیصلے (ڈیزائن پلان سے)

| فیصلہ                                    | قدر                                                                                               |
| ---------------------------------------- | ------------------------------------------------------------------------------------------------- |
| 9Router کے مقامی UI تک ڈیش بورڈ کی رسائی | `/dashboard/providers/services/9router/embed/*` پر ریورس پراکسی                                   |
| تنصیب کا طریقۂ کار                       | `execFile` کے ذریعے `npm install {package}` (بغیر شیل انٹرپولیشن)                                 |
| استعمال کا موڈ                           | روٹنگ انجن میں `9router/{sub}/{model}` کے طور پر رجسٹر شدہ فراہم کنندہ                            |
| API کلید کا انتظام                       | OmniRoute اسے بناتا، غیر فعال حالت میں انکرپٹ کرتا (AES-256-GCM)، اور env کے ذریعے انجیکٹ کرتا ہے |
| ڈیش بورڈ کا مقام                         | `/dashboard/providers/services` (تین ٹیبز)                                                        |
| خودکار آغاز                              | ہر سروس کے لیے الگ ٹوگل، ڈیفالٹ طور پر بند                                                        |

---

## 2. آرکیٹیکچر — 4 تہیں

```
┌────────────────────────────────────────────────────────────────────┐
│  تہہ 1 — UI                                                        │
│  /dashboard/providers/services  (ٹیبز: CLIProxyAPI | 9Router | Mux)│
│  براہِ راست لاگز (SSE)، شروع/بند/دوبارہ شروع/اپ ڈیٹ، ترتیبات، تنصیب│
│                                                                    │
│  src/app/(dashboard)/dashboard/providers/services/                 │
│    ├── page.tsx               شیل + ?tab= کے ذریعے ٹیب روٹنگ       │
│    ├── tabs/                  CliproxyServiceTab, NinerouterServiceTab,│
│    │                          MuxServiceTab                        │
│    └── components/            ServiceStatusCard, ServiceLifecycleButtons,│
│                               ServiceLogsPanel, ApiKeyCard, ...    │
└──────────────────────┬─────────────────────────────────────────────┘
                       │ HTTP (Next.js fetch)
┌──────────────────────▼─────────────────────────────────────────────┐
│  تہہ 2 — API (LOCAL_ONLY — صرف loopback)                           │
│                                                                    │
│  /api/services/9router/{install|start|stop|restart|update|         │
│                          rotate-key|status|auto-start|logs}        │
│  /api/services/cliproxy/{install|start|stop|restart|update|        │
│                           status|auto-start|logs}                  │
│  /api/services/mux/{install|start|stop|restart|update|             │
│                      status|auto-start|logs}                       │
│  /dashboard/providers/services/9router/embed/[...path]             │
│    (ریورس HTTP + WebSocket پراکسی → 9Router upstream)              │
│                                                                    │
│  گیٹ: LOCAL_ONLY_API_PREFIXES میں "/api/services/" اور             │
│        "/dashboard/providers/services/*/embed/" شامل ہیں           │
└──────────────────────┬─────────────────────────────────────────────┘
                       │ in-process کالز
┌──────────────────────▼─────────────────────────────────────────────┐
│  تہہ 3 — ServiceSupervisor (src/lib/services/)                     │
│                                                                    │
│  ServiceSupervisor.ts   عمومی سپروائزر (child_process.spawn)       │
│    ├── تنصیب:      execFile('npm', ['install', pkg, '--prefix'])   │
│    ├── آغاز:       spawn(node, [entrypoint], {env, cwd})           │
│    ├── api_key:    crypto.randomBytes(32) → env NINEROUTER_API_KEY  │
│    ├── پورٹ:       9Router کے لیے 20130 (قابلِ ترتیب)              │
│    ├── لاگز:       stdio رِنگ بفر 5 MB → SSE ایونٹس                │
│    ├── صحت:        ہر 2–5 s بعد HTTP GET /health، سست بازیابی      │
│    └── لائف سائیکل: SIGTERM 15 s → SIGKILL                         │
│                                                                    │
│  registry.ts        getSupervisor(name) / registerSupervisor()     │
│  bootstrap.ts       پراسیس آغاز پر تمام SERVICES[] کو بوٹ اسٹریپ کرتا ہے│
│  apiKey.ts          getOrCreateApiKey(), generateServiceApiKey()   │
│  modelSync.ts       متواتر GET /v1/models → service_models ٹیبل    │
│  ringBuffer.ts      دائروی لاگ بفر (فی سروس 5 MB)                  │
│  healthCheck.ts     پولنگ پر مبنی HTTP صحت کی جانچ                 │
│  installers/        ninerouter.ts, cliproxy.ts, mux.ts, openwa.ts  │
│                      (انسٹالر اڈاپٹرز)                             │
└──────────────────────┬─────────────────────────────────────────────┘
                       │ OpenAI سے ہم آہنگ HTTP (loopback)
┌──────────────────────▼─────────────────────────────────────────────┐
│  تہہ 4 — فراہم کنندہ / روٹنگ                                      │
│                                                                    │
│  open-sse/executors/ninerouter.ts                                  │
│    ہر درخواست پر پورٹ اور API کلید دوبارہ تلاش کرتا ہے (کوئی کیشنگ نہیں)۔│
│    پراکسی کرنے سے پہلے model id سے "9router/" سابقہ ہٹاتا ہے۔      │
│    اگر سپروائزر "running" میں نہ ہو تو 503 service_not_running لوٹاتا ہے۔│
│                                                                    │
│  src/shared/constants/providers.ts                                 │
│    "9router" کے لیے اندراج: isEmbeddedService: true                │
│                                                                    │
│  open-sse/config/providerRegistry.ts                               │
│    ماڈلز "9router/{sub}/{model}" کے طور پر محفوظ ہوتے ہیں (سابقے کے ساتھ)۔│
│    modelSync.ts کے ذریعے ہر 5 min بعد ہم وقت کیے جاتے ہیں۔         │
│                                                                    │
│  Mux کا صرف لائف سائیکل منظم کیا جاتا ہے (تہیں 1-3) — یہ ایک agent-│
│  orchestration daemon ہے، LLM پراکسی نہیں، اس لیے اس کا کوئی تہہ 4 │
│  executor/provider اندراج نہیں اور یہ کبھی روٹنگ کا ہدف نہیں بنتا۔│
└────────────────────────────────────────────────────────────────────┘
```

### اہم سورس فائلیں

| فائل                                        | کردار                                             |
| ------------------------------------------- | ------------------------------------------------- |
| `src/lib/services/ServiceSupervisor.ts`     | بنیادی کلاس: لائف سائیکل، لاک، صحت، رِنگ بفر      |
| `src/lib/services/bootstrap.ts`             | پراسیس کی سطح پر رجسٹریشن اور خودکار آغاز         |
| `src/lib/services/registry.ts`              | سنگلٹن میپ `tool → supervisor`                    |
| `src/lib/services/apiKey.ts`                | کلید کی تخلیق، محفوظ حالت میں AES-256-GCM انکرپشن |
| `src/lib/services/modelSync.ts`             | وقفے وقفے سے ماڈل سنک (5 منٹ) + حسبِ طلب          |
| `src/lib/services/ringBuffer.ts`            | SSE سبسکرائب کے ساتھ 5 MB کا گردشی لاگ بفر        |
| `src/lib/services/healthCheck.ts`           | HTTP صحت کی جانچ (قابلِ ترتیب وقفہ)               |
| `src/lib/services/installers/ninerouter.ts` | 9Router کے لیے npm انسٹال/اپ ڈیٹ/اَن انسٹال       |
| `src/lib/services/installers/cliproxy.ts`   | CLIProxyAPI کے لیے npm انسٹال/اپ ڈیٹ/اَن انسٹال   |
| `src/lib/services/installers/mux.ts`        | Mux کے لیے npm انسٹال/اپ ڈیٹ/اَن انسٹال           |
| `src/lib/services/installers/openwa.ts`     | open-wa کے لیے npm انسٹال/اپ ڈیٹ/اَن انسٹال       |
| `src/app/api/services/9router/_lib.ts`      | `getOrInitSupervisor()` معاون                     |
| `src/app/api/services/[name]/logs/route.ts` | مشترکہ SSE لاگز اینڈ پوائنٹ                       |
| `open-sse/executors/ninerouter.ts`          | فراہم کنندہ ایگزیکیوٹر (پرت 4)                    |

---

## 3. لائف سائیکل اسٹیٹ مشین

```
                    install()
  ┌─────────────┐ ──────────► ┌─────────────┐
  │ not_installed│             │   stopped   │◄──────────────────┐
  └─────────────┘             └──────┬──────┘                   │
                                     │ start()                   │
                                     ▼                           │ stop()
                               ┌──────────┐                      │
                               │ starting │                      │
                               └────┬─────┘                     │
           ہیلتھ پروب کامیاب   │         کریش / SIGTERM         │
                               ┌────▼─────┐  (5s میں اخراج)      │
                               │ running  │──── کریش ───────────►┤
                               └────┬─────┘                   ┌─▼────┐
                             stop() │                          │error │
                                    ▼                          └──────┘
                               ┌──────────┐
                               │ stopping │
                               └──────────┘
```

اسٹیٹس `version_manager` DB ٹیبل (`status` کالم) میں محفوظ کیے جاتے ہیں اور
`ServiceSupervisor` کی اِن میموری حالت میں ان کی عکاسی کی جاتی ہے۔ چلتے ہوئے
پروسیس کے لیے اِن میموری حالت حتمی اتھارٹی ہے؛ بوٹ کے وقت DB حالت پائیدار متبادل ہے۔

### حالت کی منتقلیاں

| از              | ایونٹ                                  | تک                     |
| --------------- | -------------------------------------- | ---------------------- |
| `not_installed` | `install()` کامیاب ہو                  | `stopped`              |
| `stopped`       | `start()` کال کیا جائے                 | `starting`             |
| `starting`      | ہیلتھ پروب 200 واپس کرے                | `running`              |
| `starting`      | صحت مند ہونے سے پہلے پروسیس ختم ہو     | `error`                |
| `running`       | `stop()` کال کیا جائے                  | `stopping` → `stopped` |
| `running`       | پروسیس غیر متوقع طور پر ختم ہو (< 5 s) | `error` (فوری کریش)    |
| `running`       | پروسیس غیر متوقع طور پر ختم ہو (> 5 s) | `error`                |
| `error`         | `start()` کال کیا جائے                 | `starting`             |
| کوئی بھی        | `stopping` کے دوران `stop()`           | کوئی کارروائی نہیں     |

### آپریشن لاک

`ServiceSupervisor` ایک async آپریشن لاک (`withLock()`) کے ذریعے لائف سائیکل
آپریشنز کو سلسلہ وار بناتا ہے۔ ایک ہی سپروائزر پر بیک وقت `start()` کالز کے نتیجے
میں بالکل ایک پروسیس spawn ہوتا ہے؛ دوسرا کالر انتظار کرتا ہے اور موجودہ اسٹیٹس
واپس حاصل کرتا ہے۔ یہ race conditions کو روکتا ہے، مثلاً جب auto-start اور UI
بٹن بیک وقت متحرک ہوں۔

---

## 4. API حوالہ

`/api/services/` کے تحت تمام روٹس **LOCAL_ONLY** ہیں (صرف loopback، قطعی اصول #17)۔
غیر loopback درخواستوں کو auth token سے قطع نظر `403 LOCAL_ONLY` موصول ہوتا ہے۔

### 4.1 9Router endpoints (11 روٹس)

#### `POST /api/services/9router/install`

npm سے 9Router انسٹال کرتا ہے۔ اس کے اپنے `package.json` اور `node_modules/` کے ساتھ
`DATA_DIR/services/9router/` بناتا ہے۔ OmniRoute کی اپنی dependencies سے متصادم نہیں ہوتا۔

**درخواست کی body** (تمام اختیاری):

```json
{ "version": "latest" }
```

| فیلڈ      | قسم      | ڈیفالٹ     | تفصیل                                        |
| --------- | -------- | ---------- | -------------------------------------------- |
| `version` | `string` | `"latest"` | انسٹال کرنے کے لیے npm version tag یا semver |

**جوابات:**

| اسٹیٹس | تفصیل                                                       |
| ------ | ----------------------------------------------------------- |
| `200`  | `{ ok: true, installedVersion: "x.y.z", path: "..." }`      |
| `400`  | غلط request body (Zod validation کی ناکامی)                 |
| `409`  | انسٹالیشن پہلے ہی جاری ہے (lock زیرِ استعمال ہے)            |
| `500`  | npm install ناکام — قابلِ فہم خرابی کے لیے `message` دیکھیں |

**نوٹس:** `execFile('npm', [...])` استعمال کرتا ہے — کوئی shell یا interpolation نہیں (قطعی اصول #13)۔
EACCES کی خرابیاں قابلِ فہم پیغامات کی صورت میں ظاہر کی جاتی ہیں۔

---

#### `POST /api/services/9router/start`

9Router شروع کرتا ہے۔ اگر supervisor پہلے سے رجسٹرڈ نہ ہو تو اسے رجسٹر کرتا ہے، پھر
`supervisor.start()` کو کال کرتا ہے۔ پہلے سے چلنے کی صورت میں idempotent ہے۔

**درخواست کی body:** کوئی نہیں

**جوابات:**

| اسٹیٹس | تفصیل                                                  |
| ------ | ------------------------------------------------------ |
| `200`  | `ServiceStatus` object (ذیل میں schema دیکھیں)         |
| `409`  | 9Router انسٹال نہیں ہے (`status: "not_installed"`)     |
| `503`  | آغاز ناکام ہوا (process کی خرابی — `lastError` دیکھیں) |

**ServiceStatus schema:**

```json
{
  "tool": "9router",
  "state": "running",
  "pid": 12345,
  "port": 20130,
  "health": "healthy",
  "startedAt": "2026-05-25T10:00:00.000Z",
  "lastError": null
}
```

---

#### `POST /api/services/9router/stop`

9Router کو مناسب طریقے سے روکتا ہے۔ SIGTERM بھیجتا ہے، 15 s انتظار کرتا ہے، پھر اگر process اب بھی فعال ہو تو SIGKILL بھیجتا ہے۔
پہلے سے رکے ہونے کی صورت میں idempotent ہے۔

**درخواست کی body:** کوئی نہیں

**جوابات:**

| اسٹیٹس | تفصیل                                   |
| ------ | --------------------------------------- |
| `200`  | `ServiceStatus` (state: "stopped")      |
| `503`  | روکنے کا عمل غیر متوقع طور پر ناکام ہوا |

---

#### `POST /api/services/9router/restart`

operation lock کے تحت `stop()` اور پھر `start()` کے مساوی ہے۔

**درخواست کی body:** کوئی نہیں

**جوابات:** `start` جیسے ہی ہیں (حتمی `ServiceStatus` واپس کرتا ہے)۔

---

#### `POST /api/services/9router/update`

9Router کو npm کے نئے version پر اپ ڈیٹ کرتا ہے۔ اگر service چل رہی ہو تو پہلے اسے
روکا جاتا ہے، npm install چلایا جاتا ہے (نیا version اسی جگہ انسٹال کرتے ہوئے)، اور پھر
service دوبارہ شروع کی جاتی ہے۔

**درخواست کی body** (تمام اختیاری):

```json
{ "version": "latest" }
```

**جوابات:**

| اسٹیٹس | تفصیل                                                           |
| ------ | --------------------------------------------------------------- |
| `200`  | `{ ok: true, previousVersion: "...", installedVersion: "..." }` |
| `400`  | غلط body                                                        |
| `500`  | npm update ناکام ہوا                                            |

---

#### `POST /api/services/9router/rotate-key`

9Router کے لیے نئی API key بناتا ہے، اسے at-rest حالت میں encrypt کرتا ہے، اور service کو
(اگر چل رہی ہو) دوبارہ شروع کرتا ہے تاکہ وہ اپنے environment سے نئی key حاصل کرے۔ پرانی key
فوری طور پر غیر مؤثر کر دی جاتی ہے۔

**درخواست کی body:** کوئی نہیں

**جوابات:**

| اسٹیٹس | تفصیل                                      |
| ------ | ------------------------------------------ |
| `200`  | `{ keyRotated: true, restarted: boolean }` |
| `500`  | key rotation ناکام ہوئی                    |

**سیکیورٹی:** نئی key کبھی response میں واپس نہیں کی جاتی (credential کا اخراج نہیں ہوتا)۔
اسے `version_manager` table میں encrypted حالت (AES-256-GCM) میں محفوظ کیا جاتا ہے۔

---

#### `GET /api/services/9router/status`

version metadata اور API key preview سمیت مشترکہ live + DB status واپس کرتا ہے۔

**جوابات:**

| اسٹیٹس | تفصیل                   |
| ------ | ----------------------- |
| `200`  | ذیل میں schema دیکھیں   |
| `500`  | status پڑھنے میں ناکامی |

**Response schema:**

```json
{
  "tool": "9router",
  "state": "running",
  "pid": 12345,
  "port": 20130,
  "health": "healthy",
  "startedAt": "2026-05-25T10:00:00.000Z",
  "lastError": null,
  "installedVersion": "1.2.3",
  "latestVersion": "1.2.4",
  "updateAvailable": true,
  "apiKeyMasked": "nr_****abcd",
  "autoStart": false,
  "providerExpose": false
}
```

---

#### `POST /api/services/9router/auto-start`

auto-start flag کو toggle کرتا ہے۔ جب `enabled: true` ہو تو اگلی مرتبہ OmniRoute کے boot ہونے پر
service خودکار طور پر شروع ہو جاتی ہے (بشرطیکہ service انسٹال ہو)۔

**درخواست کی body:**

```json
{ "enabled": true }
```

**جوابات:**

| اسٹیٹس | تفصیل                 |
| ------ | --------------------- |
| `200`  | `{ autoStart: true }` |
| `400`  | غلط body              |

---

#### `GET /api/services/9router/logs`

9Router کے stdout/stderr ring buffer سے live logs کا SSE stream۔

**Query parameters:**

| پیرامیٹر | قسم       | ڈیفالٹ    | تفصیل                                                                                    |
| -------- | --------- | --------- | ---------------------------------------------------------------------------------------- |
| `tail`   | `integer` | 200       | پہلے بھیجی جانے والی تاریخی لائنوں کی تعداد (زیادہ سے زیادہ 1000)                        |
| `filter` | `string`  | کوئی نہیں | حروف کی بڑی یا چھوٹی شکل سے غیر حساس substring filter (کوئی regex نہیں — ReDoS سے محفوظ) |

**SSE events:**

| Event       | Data        | تفصیل                  |
| ----------- | ----------- | ---------------------- |
| `snapshot`  | `LogLine[]` | ابتدائی تاریخی tail    |
| `log`       | `LogLine`   | live log لائن          |
| `heartbeat` | `{}`        | ہر 15 s بعد keep-alive |

**LogLine schema:**

```json
{
  "ts": 1716633600000,
  "stream": "stdout",
  "line": "[9router] Listening on :20130"
}
```

**جوابات:**

| حیثیت | تفصیل                                      |
| ----- | ------------------------------------------ |
| `200` | `text/event-stream`                        |
| `400` | `filter` پیرامیٹر بہت طویل ہے (> 200 حروف) |
| `404` | سروس نہیں ملی (سپروائزر رجسٹرڈ نہیں ہے)    |

---

### 4.2 CLIProxyAPI اینڈ پوائنٹس (10 روٹس)

CLIProxyAPI کے اینڈ پوائنٹس کی ساخت 9Router جیسی ہے، سوائے `rotate-key` کے، نیز اس میں
`accounts`، `provider-expose` اور `auto-restart-adopted` شامل ہیں۔ اب اسے شروع کیے جانے کے
وقت ایک مخصوص ڈیٹا پلین API کلید فراہم کی جاتی ہے (`bootstrap.ts` میں `needsApiKey: true`،
جو ماڈل کی ہم وقت سازی کے لیے استعمال ہوتی ہے)؛ `status` میں نسبتاً کم فیلڈز شامل ہیں۔

| طریقہ  | راستہ                               | تفصیل                                     |
| ------ | ----------------------------------- | ----------------------------------------- |
| `POST` | `/api/services/cliproxy/install`    | npm سے CLIProxyAPI انسٹال کریں            |
| `POST` | `/api/services/cliproxy/start`      | CLIProxyAPI شروع کریں                     |
| `POST` | `/api/services/cliproxy/stop`       | CLIProxyAPI روکیں                         |
| `POST` | `/api/services/cliproxy/restart`    | CLIProxyAPI دوبارہ شروع کریں              |
| `POST` | `/api/services/cliproxy/update`     | نئے ورژن پر اپ ڈیٹ کریں                   |
| `GET`  | `/api/services/cliproxy/status`     | لائیو + DB حیثیت (`apiKeyMasked` کے بغیر) |
| `POST` | `/api/services/cliproxy/auto-start` | خودکار آغاز کو فعال یا غیر فعال کریں      |

مشترکہ `GET /api/services/{name}/logs` اینڈ پوائنٹ (§4.1 دیکھیے) `[name]`
ڈائنامک سیگمنٹ استعمال کرتے ہوئے چاروں سروسز کے لیے کام کرتا ہے۔

---

### 4.3 Mux اینڈ پوائنٹس (8 روٹس)

Mux کے اینڈ پوائنٹس کی ساخت CLIProxyAPI جیسی ہے — API سطح پر کوئی `rotate-key` روٹ
نہیں ہے (بیئرر ٹوکن 9Router کی طرح `getOrCreateApiKey("mux")` کے ذریعے تیار اور
`MUX_SERVER_AUTH_TOKEN` ماحولیاتی متغیر کے ذریعے فراہم کیا جاتا ہے، لیکن ابھی تک
کلید کی گردش کے لیے کوئی مخصوص اینڈ پوائنٹ موجود نہیں ہے)۔ Mux کا نظم صرف لائف سائیکل
کی حد تک کیا جاتا ہے: 9Router کے برعکس، اس میں کوئی لیئر 4 ایگزیکیوٹر نہیں ہے اور اسے
کبھی بھی روٹنگ فراہم کنندہ کے طور پر رجسٹر نہیں کیا جاتا۔

| طریقہ  | راستہ                          | تفصیل                                |
| ------ | ------------------------------ | ------------------------------------ |
| `POST` | `/api/services/mux/install`    | npm سے Mux انسٹال کریں (`npm i mux`) |
| `POST` | `/api/services/mux/start`      | Mux شروع کریں (`mux server`)         |
| `POST` | `/api/services/mux/stop`       | Mux روکیں                            |
| `POST` | `/api/services/mux/restart`    | Mux دوبارہ شروع کریں                 |
| `POST` | `/api/services/mux/update`     | نئے npm ورژن پر اپ ڈیٹ کریں          |
| `GET`  | `/api/services/mux/status`     | لائیو + DB حیثیت                     |
| `POST` | `/api/services/mux/auto-start` | خودکار آغاز کو فعال یا غیر فعال کریں |

---

### 4.4 Bifrost اینڈ پوائنٹس (8 روٹس)

Bifrost ایک Go پر مبنی AI-گیٹ وے ریلے بیک اینڈ (`@maximhq/bifrost`) ہے۔ یہ
CLIProxyAPI جیسی اینڈ پوائنٹ ساخت استعمال کرتا ہے (`rotate-key` کے بغیر — Bifrost
اپنی فراہم کنندہ کلیدوں کا نظم اپنے `-app-dir` کے تحت `config.json` میں کرتا ہے)۔

| طریقہ  | راستہ                              | تفصیل                                                   |
| ------ | ---------------------------------- | ------------------------------------------------------- |
| `POST` | `/api/services/bifrost/install`    | npm سے Bifrost انسٹال کریں (`@maximhq/bifrost`)         |
| `POST` | `/api/services/bifrost/start`      | Bifrost کو پورٹ 8080 پر شروع کریں (ڈیفالٹ)              |
| `POST` | `/api/services/bifrost/stop`       | Bifrost روکیں                                           |
| `POST` | `/api/services/bifrost/restart`    | Bifrost دوبارہ شروع کریں                                |
| `POST` | `/api/services/bifrost/update`     | نئے ورژن پر اپ ڈیٹ کریں                                 |
| `GET`  | `/api/services/bifrost/status`     | لائیو + DB حیثیت                                        |
| `POST` | `/api/services/bifrost/auto-start` | خودکار آغاز کو فعال یا غیر فعال کریں                    |
| `GET`  | `/api/services/bifrost/logs`       | SSE لاگ ٹیل (مشترکہ `[name]/logs` ڈائنامک روٹ کے ذریعے) |

**روٹنگ وائرنگ:** جب `BIFROST_BASE_URL` متعین نہ ہو اور زیر نگرانی Bifrost
انسٹینس چل رہا ہو، تو `getBifrostRoutingConfig()` (`routingBackend.ts` میں) خودکار طور پر
`http://127.0.0.1:{port}` کو ریلے بیس URL کے طور پر استعمال کرتا ہے۔ واضح طور پر متعین
`BIFROST_BASE_URL` ماحولیاتی متغیر کو ہمیشہ ترجیح حاصل ہوتی ہے۔

---

### 4.5 Dario اینڈ پوائنٹس (12 روٹس)

دیگر سروسز جیسی ہی لائف سائیکل ساخت (`install`، `start`، `stop`، `restart`،
`update`، `status`، `auto-start`، `auto-restart-adopted`) کے ساتھ `admin/` کے تحت
ٹوکن سے محفوظ OAuth کنٹرول پلین بھی شامل ہے: `admin/accounts`، `admin/import-from-omniroute`،
`admin/login-start`، `admin/login-complete` (یہ سب `DARIO_ADMIN_TOKEN` سے محفوظ ہیں)۔

### 4.6 open-wa اینڈ پوائنٹس (7 روٹس)

open-wa (`@open-wa/wa-automate`) WhatsApp Web کو خودکار بنانے کے لیے ایک ہیڈلیس
Chromium انسٹینس (Puppeteer کے ذریعے) چلاتا ہے۔ یہ Mux جیسی اینڈ پوائنٹ ساخت استعمال
کرتا ہے (ابھی تک کوئی `rotate-key` روٹ نہیں ہے)۔ اس کا نظم صرف لائف سائیکل کی حد تک
کیا جاتا ہے — یہ روٹنگ ہدف نہیں ہے، اور اس کے لیے کوئی لیئر 4 ایگزیکیوٹر/فراہم کنندہ
اندراج موجود نہیں ہے۔

| طریقہ  | راستہ                             | وضاحت                                                   |
| ------ | --------------------------------- | ------------------------------------------------------- |
| `POST` | `/api/services/openwa/install`    | npm سے open-wa (`@open-wa/wa-automate`) انسٹال کریں     |
| `POST` | `/api/services/openwa/start`      | open-wa کو پورٹ 8323 (ڈیفالٹ) پر شروع کریں              |
| `POST` | `/api/services/openwa/stop`       | open-wa کو روکیں                                        |
| `POST` | `/api/services/openwa/restart`    | open-wa کو دوبارہ شروع کریں                             |
| `POST` | `/api/services/openwa/update`     | نئے ورژن پر اپ ڈیٹ کریں                                 |
| `GET`  | `/api/services/openwa/status`     | لائیو + DB اسٹیٹس                                       |
| `POST` | `/api/services/openwa/auto-start` | خودکار آغاز کو ٹوگل کریں                                |
| `GET`  | `/api/services/openwa/logs`       | SSE لاگ ٹیل (مشترکہ `[name]/logs` ڈائنامک روٹ کے ذریعے) |

**API کلید:** `WA_KEY` کے طور پر داخل کی جاتی ہے — open-wa کا عمومی `WA_*` سابقے والا env
اوور رائیڈ اسے `--key`/`-k` CLI آپشن پر میپ کرتا ہے
(`dist/cli/setup.js::envArgs()`، انسٹال شدہ 4.76.0
پیکیج کے مقابل تصدیق شدہ)۔ `generateServiceApiKey()` کے ذریعے بنائے جانے پر `ow_` سابقہ لگایا جاتا ہے۔ open-wa
کلید کو `key`/`api_key` HTTP ہیڈر سے واپس پڑھتا ہے (`Authorization:
Bearer` سے نہیں)؛ `/api-docs*` کو واضح طور پر جانچ سے مستثنیٰ رکھا گیا ہے
(`dist/cli/server.js` میں `setupAuthenticationLayer`)، اس لیے ہیلتھ پروب کو
کسی تصدیقی ہیڈر کی ضرورت نہیں۔

**پیئرنگ:** open-wa غیر سرکاری ہے اور WhatsApp سے وابستہ نہیں — منسلک
نمبر کو WhatsApp کی اپنی آٹومیشن شناخت کے باعث پابندی کا خطرہ لاحق ہوتا ہے۔
پہلی بار آغاز پر، پیئرنگ QR کوڈ stdout پر پرنٹ ہوتا ہے اور
موجودہ لاگز پینل/SSE اسٹریم کے ذریعے دکھایا جاتا ہے — اس انٹیگریشن میں ابھی
کوئی مخصوص QR امیج اینڈ پوائنٹ موجود نہیں۔

---

### 4.7 ریورس پراکسی (9Router ڈیش بورڈ ایمبیڈ)

ڈیش بورڈ اندرونی ریورس
پراکسی کے ذریعے 9Router ویب UI کو ایک iframe میں ایمبیڈ کرتا ہے:

```
GET|POST|... /dashboard/providers/services/9router/embed/[...path]
```

یہ پراکسی:

- درخواست کو `http://127.0.0.1:{port}/{path}` پر فارورڈ کرتی ہے (صرف لوپ بیک)
- آنے والے `cookie` اور `authorization` ہیڈرز ہٹاتی ہے (OmniRoute سیشن کا کوئی اخراج نہیں)
- 9Router تصدیق کے لیے `Authorization: Bearer {apiKey}` داخل کرتی ہے
- رسپانس سے `set-cookie`، `content-security-policy`، `x-frame-options`، `cross-origin-*` ہٹاتی ہے
- `<base href>` داخل کرنے اور مطلق راستوں کو معمول پر لانے کے لیے HTML رسپانسز کو دوبارہ لکھتی ہے (`/foo` → `/dashboard/.../embed/foo`)

ایمبیڈڈ ڈیش بورڈ کے لیے WebSocket اپ گریڈز ایک
مختص پورٹ پر موجود معاون سرور سنبھالتا ہے (`src/lib/services/embedWsProxy.ts` دیکھیں)۔

**سیکیورٹی:** ایمبیڈ پراکسی روٹس کو `LOCAL_ONLY_API_PREFIXES`
کے تحت درجہ بند کیا گیا ہے اور ان تک صرف لوپ بیک سے رسائی حاصل کی جا سکتی ہے۔ ایسا حملہ آور جو
Cloudflare/Ngrok ٹنل کے ذریعے JWT حاصل کر لے، ایمبیڈڈ سروسز تک پراکسی نہیں کر سکتا۔

---

## 5. سیکیورٹی

### LOCAL_ONLY کا نفاذ (سخت اصول #17)

`/api/services/` اور `/dashboard/providers/services/*/embed/` کے تحت تمام روٹس کو
`src/server/authz/routeGuard.ts` میں LOCAL_ONLY کے طور پر درجہ بند کیا گیا ہے۔ لوپ بیک کی جانچ
کسی بھی تصدیقی شاخ سے پہلے غیر مشروط طور پر چلتی ہے:

```
درخواست موصول ہوتی ہے
  → isLocalOnlyPath(path)?
      → غیر لوپ بیک → 403 LOCAL_ONLY (ہمیشہ، تصدیقی جانچ سے پہلے)
      → لوپ بیک     → معمول کی تصدیق پر جاری رہیں
```

یہ کسی افشا شدہ JWT (مثلاً، ٹنل کے ذریعے) کو `npm install` متحرک کرنے یا
پراسیس شروع کرنے سے روکتا ہے۔ مکمل درجہ جاتی میٹرکس کے لیے
`docs/security/ROUTE_GUARD_TIERS.md` دیکھیں۔

### API کلید کا اندراج

9Router اور Mux کو اپنے HTTP اینڈ پوائنٹس کے لیے API کلید/بیئرر ٹوکن درکار ہوتا ہے۔
OmniRoute:

1. `crypto.randomBytes(32).toString("base64url")` کے ذریعے سروس کے لیے مخصوص
   سابقے (`nr_` برائے 9Router، `mx_` برائے Mux) کے ساتھ کلید بناتا ہے۔
2. اسے AES-256-GCM کے ذریعے محفوظ حالت میں انکرپٹ کرتا ہے (وہی سائفر جو فراہم کنندہ کی اسناد کے لیے استعمال ہوتا ہے)۔
3. پراسیس شروع کرتے وقت اسے ڈکرپٹ کرکے ایک ماحولیاتی متغیر کے طور پر داخل کرتا ہے —
   9Router کے لیے `NINEROUTER_API_KEY`، Mux کے لیے `MUX_SERVER_AUTH_TOKEN` (کبھی بھی CLI
   فلیگ کے طور پر نہیں، تاکہ ٹوکن کبھی `ps`/پراسیس فہرستوں میں ظاہر نہ ہو)۔
4. کسی بھی HTTP جواب میں سادہ متن والی کلید کبھی واپس نہیں کرتا۔

CLIProxyAPI کو پراسیس شروع کرتے وقت ایک مخصوص ڈیٹا پلین کلید دی جاتی ہے
(`needsApiKey: true` — اڈاپٹر کے خلاف ماڈل سنک کے لیے استعمال ہوتی ہے)۔

### SSRF سے دفاع

ریورس HTTP پراکسی (`/dashboard/.../embed/[...path]`) کو صرف
`http://127.0.0.1:{port}` کی طرف فارورڈ کرنے کے لیے ہارڈ کوڈ کیا گیا ہے۔ یہ کبھی بھی غیر لوپ بیک
منزلوں کی طرف ری ڈائریکٹس کی پیروی نہیں کرتی۔ `ssrf-req-filter` لائبریری ہر ایسے upstream URL کو
مسترد کرنے کے لیے استعمال ہوتی ہے جو لوپ بیک رینج سے باہر resolve ہو۔

### شیل کی حفاظت (سخت اصول #13)

`npm install` کو `execFile('npm', ['install', pkg, '--prefix', dir])` کے ذریعے چلایا جاتا ہے —
کوئی ٹیمپلیٹ لٹرلز، کوئی شیل، اور کمانڈ اسٹرنگ میں بیرونی پاتھز کی کوئی انٹرپولیشن نہیں۔
رن ٹائم اقدار (پورٹس، API کلیدیں) چائلڈ کے `env` آبجیکٹ کے ذریعے فراہم کی جاتی ہیں۔

### خرابیوں کی تطہیر (سخت اصول #12)

`/api/services/*` سے آنے والے تمام خرابی کے جوابات `buildErrorBody()` یا
`sanitizeErrorMessage()` سے گزرتے ہیں۔ خام `err.stack` اور `err.message` کبھی بھی
کال کرنے والے کو من و عن واپس نہیں کیے جاتے۔

---

## 6. نئی ایمبیڈڈ سروس شامل کرنا

ان 8 مراحل پر عمل کریں۔ مستند حوالہ کے طور پر `src/lib/services/installers/`
اور `src/app/api/services/` میں موجود نفاذات پڑھیں۔

### مرحلہ 1 — انسٹالر بنائیں

`ninerouter.ts` کے نمونے پر `src/lib/services/installers/{name}.ts` بنائیں:

```typescript
export const NAME_PACKAGE = "your-npm-package";
export const NAME_DEFAULT_PORT = 20132; // کوئی خالی پورٹ منتخب کریں

export async function install(version = "latest"): Promise<InstallResult> { ... }
export async function update(version = "latest"): Promise<InstallResult> { ... }
export async function uninstall(): Promise<void> { ... }
export function resolveSpawnArgs(apiKey: string, port: number): SpawnArgs { ... }
export async function getInstalledVersion(): Promise<string | null> { ... }
export async function getLatestVersion(): Promise<string | null> { ... }
```

`installers/utils.ts` سے `runNpm(['install', NAME_PACKAGE, '--prefix', dir])` استعمال کریں
— `execSync` یا شیل انٹرپولیشن کبھی استعمال نہ کریں۔

### مرحلہ 2 — بوٹسٹریپ میں رجسٹر کریں

`src/lib/services/bootstrap.ts` میں `SERVICES` ارے میں ایک `ServiceEntry` شامل کریں:

```typescript
{
  tool: "myservice",
  port: NAME_DEFAULT_PORT,
  healthPath: "/health",
  healthIntervalMs: 5_000,
  stopTimeoutMs: 15_000,
  logsBufferBytes: 5_242_880,
  needsApiKey: true, // اگر API کلید درکار نہ ہو تو false
}
```

`cfg.tool === "myservice"` کو سنبھالنے کے لیے `buildSpawnArgsFactory()` کو توسیع دیں۔

#### قابلِ توسیع فراہم کنندہ پلگ اِن معاہدہ (مرحلہ 1، #7333)

`src/lib/services/providerPlugins/` ایک `ServiceProviderPlugin` معاہدہ متعارف کراتا ہے جو
کسی بیک اینڈ کے `bootstrap.ts` کے `ServiceEntry` فیلڈز اور `serviceBackends.ts` کے
مینی فیسٹ ٹیمپلیٹ فیلڈز کو ایک آبجیکٹ میں پیکج کرتا ہے، بجائے اس کے کہ ایک ہی بیک اینڈ کی ساخت
دو غیر متعلقہ فائلوں میں الگ الگ ظاہر کی جائے۔ اس تحریر کے وقت **صرف `9router` کو
منتقل کیا گیا ہے** — `bootstrap.ts` اپنی `SERVICES[]` انٹری
`getServiceProviderPlugin("9router")` (`src/lib/services/providerPlugins/registry.ts`) سے اخذ کرتا ہے،
اور اگر پلگ اِن کبھی موجود نہ ہو تو آغاز کی خرابی پھینکتا ہے۔ `cliproxy`، `mux`، اور `bifrost`
بغیر کسی تبدیلی کے پہلے سے موجود اِن لائن `SERVICES[]` لٹرلز پر برقرار ہیں۔

`open-sse/config/providerPluginManifest.ts` میں ایک اضافی
`createServiceBackendManifestEntry(pluginId, template)` مددگار بھی شامل کیا گیا ہے جو
`SERVICE_BACKEND_MANIFEST_TEMPLATE` انٹری سے ایک درست ساخت والا
`ProviderPluginManifestEntry` بناتا ہے — اسے ابھی تک کسی فعال درخواستی پاتھ سے
**منسلک نہیں** کیا گیا (نہ `generateProviderPluginManifestFromRegistry()`
اور نہ `/v1/providers/[provider]/models`)؛ معاہدہ دوسرے بیک اینڈ کے لیے ثابت ہونے کے بعد
یہ کام اگلے مرحلے میں کیا جائے گا۔

اگلی PRs تک مؤخر کیے گئے کام، جو مسئلہ #7333 کے تحت ٹریک کیے جا رہے ہیں: `cliproxyapi` کو اسی
رجسٹری کے ذریعے منتقل کرنا، `mux`/`bifrost` کو `ServiceBackendPluginId` یونین میں عمومی بنانا،
ایگزیکیوٹر روٹنگ کی خصوصی صورتوں (`open-sse/executors/index.ts`،
`open-sse/handlers/chatCore/executorProxy.ts`) کو پلگ اِن معاہدے میں شامل کرنا، اور
`createServiceBackendManifestEntry()` کو کسی فعال مینی فیسٹ/ماڈلز کوڈ پاتھ سے منسلک کرنا۔

### مرحلہ 3 — مائیگریشن اور DB سیڈ شامل کریں

یقینی بنائیں کہ `src/lib/db/migrations/` میں ایک مائیگریشن کے ذریعے سروس کے لیے
`version_manager` میں ایک قطار موجود ہو۔ قطار میں درج ذیل ہونا چاہیے:

```sql
INSERT OR IGNORE INTO version_manager (tool, status, auto_start, provider_expose)
VALUES ('myservice', 'not_installed', 0, 0);
```

### مرحلہ 4 — 7 API اینڈ پوائنٹس بنائیں

`src/app/api/services/{name}/` کے تحت:

```
_lib.ts            getOrInitSupervisor() مددگار
install/route.ts   POST — installer.install() کو کال کرتا ہے
start/route.ts     POST — supervisor.start() کو کال کرتا ہے
stop/route.ts      POST — supervisor.stop() کو کال کرتا ہے
restart/route.ts   POST — supervisor.restart() کو کال کرتا ہے
update/route.ts    POST — installer.update() کو کال کرتا ہے
status/route.ts    GET  — فعال + DB اسٹیٹس کو یکجا کرتا ہے
auto-start/route.ts POST — auto_start فلیگ کو ٹوگل کرتا ہے
```

مشترکہ `GET /api/services/[name]/logs` روٹ پہلے ہی منسلک ہے — وہاں کسی تبدیلی کی
ضرورت نہیں۔

تمام خرابی کے جوابات `createErrorResponse()` / `buildErrorBody()` کے ذریعے دیں۔

### مرحلہ 5 — LOCAL_ONLY_API_PREFIXES میں شامل کریں

`src/server/authz/routeGuard.ts` میں تصدیق کریں کہ `/api/services/` پہلے سے درج ہے۔
اگر آپ کوئی نیا prefix متعارف کراتے ہیں (مثلاً `/api/tools/`)، تو اسے
`LOCAL_ONLY_API_PREFIXES` اور، اگر وہ processes شروع کرتا ہے، تو `SPAWN_CAPABLE_PREFIXES`
دونوں میں شامل کریں۔
`tests/unit/authz/routeGuard.test.ts` میں ایک test شامل کریں۔

### مرحلہ 6 — UI tab شامل کریں

`src/app/(dashboard)/dashboard/providers/services/tabs/{Name}ServiceTab.tsx` بنائیں۔
مشترکہ components دوبارہ استعمال کریں:

- `ServiceStatusCard` — براہِ راست حالت + health badge
- `ServiceLifecycleButtons` — شروع کریں / روکیں / دوبارہ شروع کریں / اپ ڈیٹ کریں
- `ServiceLogsPanel` — SSE log tail (`/api/services/{name}/logs` سے منسلک ہوتا ہے)
- `ApiKeyCard` — key ظاہر کرنا + rotate کرنا (اگر `needsApiKey: true` ہو)

tab کو `ServicesPageShell.tsx` میں register کریں۔

### مرحلہ 7 — provider entry شامل کریں (اگر service ایک routing target ہے)

اگر embedded service ایک OpenAI-compatible `/v1/chat/completions` endpoint فراہم کرتی ہے:

1. `src/shared/constants/providers.ts` میں `isEmbeddedService: true` کے ساتھ ایک provider entry شامل کریں۔
2. `BaseExecutor` کو extend کرتے ہوئے `open-sse/executors/{name}.ts` بنائیں۔ ہر request پر port اور
   API key دوبارہ تلاش کریں (constructor میں کبھی cache نہ کریں)۔ جب supervisor کی حالت `"running"` نہ ہو تو
   `503 service_not_running` response واپس کریں۔
3. service prefix کے ساتھ `open-sse/config/providerRegistry.ts` میں models register کریں
   (مثلاً `myservice/sub/model`)۔ `modelSync.ts` انہیں اپ ڈیٹ رکھے گا۔

### مرحلہ 8 — دستاویز بنائیں اور test کریں

1. `docs/frameworks/EMBEDDED-SERVICES.md` (یہ file) اپ ڈیٹ کریں — service کو
   §1 کے table میں اور کسی بھی نئے endpoints کو §4 میں شامل کریں۔
2. `tests/unit/services/` میں unit tests شامل کریں (lifecycle، installer، API shape)۔
3. `tests/integration/services/` میں integration test شامل کریں (`RUN_SERVICES_INT=1` کے پیچھے)۔
4. نئے endpoints کے ساتھ `docs/openapi.yaml` اپ ڈیٹ کریں۔

---

## 7. مسائل کا حل

### سروس شروع نہیں ہوتی

**علامات:** Start بٹن 503 واپس کرتا ہے، حالت `"error"` یا `"starting"` پر برقرار رہتی ہے۔

**چیک لسٹ:**

1. `GET /api/services/{name}/logs` چیک کریں (یا dashboard میں Logs پینل)۔ ایسی سطور تلاش کریں جیسے `Error: ENOENT`، `address already in use`، یا `Cannot find module`۔
2. تصدیق کریں کہ `npm`، PATH میں موجود ہے: اسی صارف اکاؤنٹ سے `which npm` چلائیں جو OmniRoute چلاتا ہے۔
3. تصدیق کریں کہ سروس انسٹال ہے: `installedVersion` کے لیے `GET /api/services/{name}/status` چیک کریں۔ اگر یہ `null` ہو، تو پہلے انسٹالیشن چلائیں۔
4. چیک کریں کہ `DATA_DIR/services/{name}/node_modules/` موجود ہے اور خالی نہیں ہے۔
5. صاف شدہ اختتامی وجہ کے لیے status response میں `lastError` فیلڈ چیک کریں۔

---

### کولڈ اسٹارٹ سست ہے (`running` تک پہنچنے میں > 10 s)

**علامات:** حالت `"running"` یا `"error"` میں جانے سے پہلے کافی دیر تک `"starting"` پر برقرار رہتی ہے۔

**وضاحت:** 9Router کے کولڈ اسٹارٹ میں بڑے dependency trees (DNS، tunnel، MITM modules) کو import کرنا شامل ہے۔ پہلے سے طے شدہ health interval 2 s ہے اور supervisor کی جانب سے timeout قرار دیے جانے سے پہلے 3 کوششیں کی جاتی ہیں (تاہم polling جاری رہتی ہے)۔

**حل:** `healthIntervalMs` اور `waitForHealthy` timeout
(`healthIntervalMs * 3`) کو `bootstrap.ts` میں ترتیب دیا جا سکتا ہے۔ زیادہ startup وقت لینے والی سروسز کے لیے `healthIntervalMs` کو 5000 اور `stopTimeoutMs` کو 30 000 تک بڑھائیں۔

---

### پورٹ کا ٹکراؤ (`EADDRINUSE`)

**علامات:** Logs میں `address already in use :::20130` دکھائی دیتا ہے۔

**وجوہات:**

- کوئی دوسرا process پہلے ہی port 20130 استعمال کر رہا ہے۔
- پچھلا 9Router process مکمل طور پر بند نہیں ہوا تھا (zombie PID)۔

**حل:**

1. `.env` میں `NINEROUTER_PORT` environment variable کے ذریعے پہلے سے طے شدہ port تبدیل کریں۔
2. متصادم process تلاش کرکے ختم کریں: `lsof -ti :20130 | xargs kill -9`۔
3. `bootstrap.ts` میں `port` فیلڈ کے ذریعے ہر سروس کے لیے port الگ سے ترتیب دیا جا سکتا ہے۔

**نوٹ:** 9Router بطور ڈیفالٹ port 20130 استعمال کرتا ہے تاکہ خاص طور پر OmniRoute کے پہلے سے طے شدہ port 20128 سے ٹکراؤ سے بچا جا سکے۔

---

### انسٹالیشن کے دوران اجازت مسترد (`EACCES`)

**علامات:** انسٹالیشن 500 واپس کرتی ہے، logs میں `EACCES` یا `permission denied` دکھائی دیتا ہے۔

**وجوہات:**

- OmniRoute process کو `DATA_DIR` یا اس کی parent directory میں لکھنے کی اجازت نہیں ہے۔
- mapped volume تک تحریری رسائی کے بغیر rootless Docker کے اندر چلایا جا رہا ہے۔

**حل:**

1. `DATA_DIR` چیک کریں (ڈیفالٹ: `~/.omniroute/`): `ls -la ~/.omniroute/`
2. یقینی بنائیں کہ OmniRoute process کا صارف directory کا مالک ہے: `chown -R $USER ~/.omniroute/`
3. Docker میں یقینی بنائیں کہ volume mount پر container user کے لیے درست permissions موجود ہیں۔

---

### اپ ڈیٹ ناکام ہو جاتا ہے (`npm install` timeout یا network error)

**علامات:** اپ ڈیٹ `InstallError` کے ساتھ 500 واپس کرتا ہے، logs میں network timeout دکھائی دیتا ہے۔

**چیک لسٹ:**

1. تصدیق کریں کہ npm registry قابلِ رسائی ہے: `npm ping`۔
2. corporate proxy چیک کریں: `npm config get proxy`، `npm config get https-proxy`۔
3. انسٹالیشن دستی طور پر آزمائیں: `npm install {package}@latest --prefix ~/.omniroute/services/{name}/`۔
4. اگر air-gap کے پیچھے ہوں تو tarball پہلے سے download کریں اور `npm install /path/to/tarball.tgz` استعمال کریں۔

---

### سروس شروع ہونے کے فوراً بعد `"error"` حالت دکھاتی ہے (فوری کریش)

**علامات:** حالت 5 سیکنڈ سے کم وقت میں `"starting"` سے `"error"` میں تبدیل ہو جاتی ہے۔
`lastError` میں `"Fast crash (exited with code 1)"` دکھائی دیتا ہے۔

**چیک لسٹ:**

1. log کا مکمل آخری حصہ پڑھیں: `GET /api/services/{name}/logs?tail=500`۔
2. عام وجہ: سروس کو درکار environment variables موجود نہیں ہیں۔
3. 9Router کے لیے: تصدیق کریں کہ `NINEROUTER_DISABLE_MITM=true` اور
   `NINEROUTER_DISABLE_TUNNEL=true`، spawn کے وقت پاس کیے گئے env میں موجود ہیں (`installers/ninerouter.ts` میں `resolveSpawnArgs` دیکھیں)۔

---

## 8. اکثر پوچھے گئے سوالات

**سوال: کیا میں ایمبیڈڈ سروسز کے endpoints کو non-loopback clients کے لیے قابلِ رسائی بنا سکتا ہوں؟**

نہیں۔ LOCAL_ONLY tier دانستہ طور پر مقرر کیا گیا ہے (سخت اصول #17)۔ وہ routes جو
`npm install` چلا سکتے ہیں یا `node` processes شروع کر سکتے ہیں، non-loopback
traffic سے قابلِ رسائی نہیں ہونے چاہییں، کیونکہ بصورتِ دیگر کسی tunnel (Cloudflare، Ngrok، Tailscale) کے ذریعے
لیک ہونے والا JWT صوابدیدی processes شروع کرنے کی اجازت دے دے گا۔ اس پابندی سے دست برداری کی کوئی استثنائی گنجائش
`/api/services/` کے لیے موجود نہیں ہے — `/api/mcp/` کے برعکس، اسے manage-scope bypass
فہرست سے خارج رکھا گیا ہے۔ `docs/security/ROUTE_GUARD_TIERS.md` دیکھیں۔

---

**سوال: کیا 9Router اور CLIProxyAPI production/cloud deployments میں دستیاب ہوں گے؟**

ہاں۔ دونوں سروسز OmniRoute کی طرح اسی local-first ماڈل کی پیروی کرتی ہیں۔ وہ اسی
مشین پر چلتی ہیں اور loopback کے ذریعے رابطہ کرتی ہیں۔ یہاں "Production" سے مراد وہ VPS
یا local server ہے جہاں OmniRoute deploy کیا گیا ہے، نہ کہ کوئی remote cloud provider۔

---

**سوال: میں supervisor کو debug کیسے کروں؟**

1. SSE log stream کو tail کریں: `curl -N http://localhost:20128/api/services/9router/logs`۔
2. OmniRoute کے pino output میں structured logs کو
   `service:supervisor` namespace کے مطابق filter کر کے دیکھیں۔
3. DB row کا معائنہ کریں: `sqlite3 ~/.omniroute/omniroute.db "SELECT * FROM version_manager WHERE tool='9router'"`۔
4. موجودہ live state، PID، health،
   اور `lastError` کو ایک ہی call میں دیکھنے کے لیے `GET /api/services/9router/status` استعمال کریں۔

---

**سوال: supervisor میں `health: "degraded"` یا `health: "unknown"` دکھائی دیتا ہے، لیکن state `"running"` ہے۔ کیا یہ مسئلہ ہے؟**

`"degraded"` کا مطلب ہے کہ health probe نے non-200 response واپس کیا۔ `"unknown"` کا مطلب ہے کہ ابھی تک کوئی
probe مکمل نہیں ہوا (پہلے poll کے ساتھ race)۔ startup کے دوران دونوں حالتیں عارضی ہوتی ہیں۔
اگر `"running"` کے بعد `healthIntervalMs * 3` ms سے زیادہ وقت تک health
`"degraded"` رہے، تو ایمبیڈڈ سروس چل رہی ہے لیکن اس کا HTTP API جواب نہیں دے رہا۔ چیک کریں
کہ status response میں port درست ہے اور آیا سروس واقعی
اس port پر سن رہی ہے۔

---

**سوال: کیا میں مکمل restart کے بغیر 9Router API key تبدیل کر سکتا ہوں؟**

نہیں۔ API key کو spawn کے وقت environment variable کے ذریعے 9Router کو دیا جاتا ہے۔
چلتے ہوئے process میں environment variables تبدیل نہیں کیے جا سکتے۔ `POST .../rotate-key`
نئی key لاگو کرنے کے لیے سروس کو خودکار طور پر روکتا اور دوبارہ شروع کرتا ہے۔ key rotation
سروس کے `stopTimeoutMs` (default 15 s) اور اس کے startup
وقت کے اندر مؤثر ہو جاتی ہے۔

---

**سوال: ring buffer کی حد کیا ہے اور بھر جانے پر کیا ہوتا ہے؟**

ہر سروس کے لیے مختص 5 MB کا ring buffer ہوتا ہے۔ buffer بھر جانے پر نئی log lines کے لیے جگہ بنانے کی خاطر سب سے پرانی
log lines نکال دی جاتی ہیں۔ SSE `snapshot` event،
`tail` حد کے اندر تازہ ترین lines واپس کرتا ہے۔ logs کو disk پر برقرار نہیں رکھا جاتا، سوائے اس صورت کے جب
DB row میں `logsBufferPath` مقرر ہو۔

---

## مزید دیکھیں

- `docs/security/ROUTE_GUARD_TIERS.md` — LOCAL_ONLY tier کی تفصیلات
- `docs/architecture/CODEBASE_DOCUMENTATION.md` — §3.2 ایمبیڈڈ سروسز module mapping
- `docs/architecture/ARCHITECTURE.md` — system-level سیاق و سباق
- `docs/openapi.yaml` — machine-readable endpoint definitions
- `CLAUDE.md` §"Adding a New Embedded Service" — فوری حوالہ checklist
