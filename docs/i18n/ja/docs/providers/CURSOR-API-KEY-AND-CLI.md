# Cursor API provider and the Cursor CLI passthrough (日本語)

🌐 **Languages:** 🇺🇸 [English](../../../../providers/CURSOR-API-KEY-AND-CLI.md) · 🇪🇹 [am](../../../am/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇸🇦 [ar](../../../ar/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇦🇿 [az](../../../az/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇧🇬 [bg](../../../bg/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇧🇩 [bn](../../../bn/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇨🇿 [cs](../../../cs/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇩🇰 [da](../../../da/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇩🇪 [de](../../../de/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇬🇷 [el](../../../el/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇪🇸 [es](../../../es/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇪🇪 [et](../../../et/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇮🇷 [fa](../../../fa/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇫🇮 [fi](../../../fi/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇫🇷 [fr](../../../fr/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇮🇪 [ga](../../../ga/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇮🇳 [gu](../../../gu/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇳🇬 [ha](../../../ha/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇮🇱 [he](../../../he/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇮🇳 [hi](../../../hi/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇭🇷 [hr](../../../hr/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇭🇺 [hu](../../../hu/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇦🇲 [hy](../../../hy/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇮🇩 [id](../../../id/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇳🇬 [ig](../../../ig/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇮🇹 [it](../../../it/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇬🇪 [ka](../../../ka/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇰🇭 [km](../../../km/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇮🇳 [kn](../../../kn/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇰🇷 [ko](../../../ko/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇱🇹 [lt](../../../lt/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇱🇻 [lv](../../../lv/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇮🇳 [ml](../../../ml/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇮🇳 [mr](../../../mr/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇲🇾 [ms](../../../ms/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇲🇹 [mt](../../../mt/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇲🇲 [my](../../../my/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇳🇵 [ne](../../../ne/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇳🇱 [nl](../../../nl/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇳🇴 [no](../../../no/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇮🇳 [or](../../../or/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇮🇳 [pa](../../../pa/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇵🇭 [phi](../../../phi/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇵🇱 [pl](../../../pl/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇵🇹 [pt](../../../pt/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇧🇷 [pt-BR](../../../pt-BR/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇷🇴 [ro](../../../ro/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇷🇺 [ru](../../../ru/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇱🇰 [si](../../../si/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇸🇰 [sk](../../../sk/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇸🇮 [sl](../../../sl/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇷🇸 [sr](../../../sr/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇸🇪 [sv](../../../sv/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇰🇪 [sw](../../../sw/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇮🇳 [ta](../../../ta/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇮🇳 [te](../../../te/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇹🇭 [th](../../../th/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇹🇷 [tr](../../../tr/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇺🇦 [uk-UA](../../../uk-UA/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇵🇰 [ur](../../../ur/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇺🇿 [uz](../../../uz/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇻🇳 [vi](../../../vi/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇳🇬 [yo](../../../yo/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇨🇳 [zh-CN](../../../zh-CN/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇹🇼 [zh-TW](../../../zh-TW/docs/providers/CURSOR-API-KEY-AND-CLI.md)

---

IDE セッションなしで Cursor を OmniRoute の背後に配置するには、次の 2 つの方法があります。

1. **`cursor-api` プロバイダー**（カード「Cursor API」、エイリアス `cua`）：Cursor ユーザー API キー（`crsr_…`、`https://cursor.com/dashboard/api` で生成）を保持する API キー方式の
   プロバイダーです。任意の OmniRoute クライアントから、
   通常のクォータ、フォールバック、ロギングの各レイヤーを介して、`/v1/chat/completions` 経由で
   `cursor-api/<model>` または `cua/<model>` として Cursor モデルにアクセスできます。IDE
   プロバイダー（`cursor`、OAuth/IDE セッション）に変更はありません。
2. **Cursor CLI パススルー**：Cursor CLI（`agent`）を OmniRoute に向けることで、
   CLI が実行するすべての RPC が OmniRoute API キーで認証され、
   `cursor-api` 接続の認証情報を使用して Cursor に転送され、Logs
   ページに記録されます。

## キーが交換される理由

`api2.cursor.sh` は、生の `crsr_…` キーを Bearer トークンとして使用すると拒否します（401）。Cursor
CLI は最初にキーを `/auth/exchange_user_api_key` へ POST し、1 時間後に期限切れとなるセッション
JWT を受け取ります。返される `refreshToken` も同じ
`exp` を持つため、更新するにはキーを再交換する必要があります。
`open-sse/services/cursorApiKeyAuth.ts` はこの交換を行い、キーごとに 1 つのセッション
トークンをキャッシュし、有効期限の 5 分前に再交換し、Cursor から 401 が返された場合はキャッシュ済み
トークンを破棄します。`CursorExecutor` は、`cursor-api` 接続で
アップストリームのストリームを開く直前にこれを呼び出します。

## `cursor-api` プロバイダー

レジストリ：`open-sse/config/providers/registry/cursor/index.ts`
（`cursor_apiProvider`、`authType: "apikey"`、`cursor` と同じ `format`、`baseUrl`、
`models`）。カタログカード：
`src/shared/constants/providers/apikey/specialty-media.ts`。Executor マップ：
`open-sse/executors/index.ts`（`"cursor-api"` / `cua` →
`new CursorExecutor("cursor-api")`）。

ダッシュボード：Providers → Cursor API → Add API key。

REST：

```bash
curl -sS -X POST http://localhost:20128/api/providers \
  -H "Content-Type: application/json" \
  -d '{"provider":"cursor-api","name":"cursor-api-key","apiKey":"crsr_…","priority":1}'
```

続いて：

```bash
curl -sS http://localhost:20128/v1/chat/completions \
  -H "Authorization: Bearer <omniroute-api-key>" \
  -H "Content-Type: application/json" \
  -d '{"model":"cursor-api/auto","messages":[{"role":"user","content":"say PONG"}]}'
```

注：

- `cursor-api` のモデル一覧は静的な Cursor レジストリ（IDE
  プロバイダーがフォールバックに使用するものと同じ一覧）から取得されます。OmniRoute ホストに
  `cursor-agent` をインストールする必要はありません。
- `POST /api/providers/{id}/refresh-cursor` は `cursor` IDE プロバイダー
  専用です。`cursor-api` 接続には更新対象となる IDE セッションがありません。

## ネイティブモデル ID と effort

`cursor` / `cu` および `cursor-api` / `cua` では、共有 Claude-effort ノーマライザーは
リクエストされたモデル ID をそのまま維持します。Cursor では、`-low` のようなサフィックスを
OmniRoute の effort エイリアスとしてではなく、実際のモデル ID の一部として公開する場合があります。
Cursor executor はライブカタログとの完全一致を維持します。一致しない場合は、
既存のモデルリゾルバーがサフィックスからパラメーターへのフォールバックを処理します。

これは、Claude への直接ルート、Claude 互換ルート、
または Vertex ルートの effort 正規化を変更するものではありません。利用可否は、選択した Cursor アカウントの
カタログとエンタイトルメントに引き続き依存します。

## Cursor CLI パススルー

ルート: `src/app/api/cursor-cli/[...path]/route.ts` →
`open-sse/handlers/cursorCliProxy.ts`。プレフィックス `/api/cursor-cli/` は、
ハンドラーが独自に認証を実施するため、`src/shared/constants/publicApiRoutes.ts` に
登録されています。

| パス                                                                                                                            | CLI から想定される認証       | OmniRoute の処理                                                                                                                                                                 |
| ------------------------------------------------------------------------------------------------------------------------------- | ---------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `POST /auth/exchange_user_api_key`                                                                                              | `Bearer <OmniRoute APIキー>` | キーを検証し、1時間有効な HS256 JWT（`JWT_SECRET` で署名）を発行して返します                                                                                                     |
| その他すべてのパス（`/aiserver.v1.*`、`/agent.v1.AgentService/RunSSE`、`/aiserver.v1.BidiService/BidiAppend`、`/v1/traces`、…） | `Bearer <そのJWT>`           | 発行者、オーディエンス、有効期限を検証し、アクティブな `cursor-api` 接続を選択して、Authorization ヘッダーを交換済みの Cursor トークンに置き換え、応答をストリーミングで返します |

CLI は受け取ったトークンから `exp` をデコードするため、不透明な
トークンを渡すと、ほぼすべてのリクエストの前に再交換が行われます。発行された JWT により、
これを回避できます。OmniRoute から 401 が返されると、CLI は再度交換します。

### セットアップ

1. OmniRoute API キー（ダッシュボード → API キー）と `cursor-api`
   接続を作成します。
2. エージェントストリームで HTTP/1.1 を使用するよう CLI を設定します。
   `~/.cursor/cli-config.json` で次のように設定します。

   ```json
   { "network": { "useHttp1ForAgent": true } }
   ```

   この設定がない場合、CLI は個別に設定されたエージェントホストへのエージェントターンを
   HTTP/2 で開始し、コントロールプレーン RPC のみがこのエンドポイントを経由します。

3. OmniRoute に対して CLI を実行します。

   ```bash
   export CURSOR_API_ENDPOINT=http://localhost:20128/api/cursor-cli
   export CURSOR_API_KEY=<omniroute-api-key>
   agent -p --trust "Reply with exactly OK"
   ```

各ホップは、プロバイダー `cursor-api`、リクエストタイプ `cursor-cli`、
パス `/api/cursor-cli/<rpc>` としてログに記録され、OmniRoute API キーと、
リクエストを処理した接続に紐付けられます。

### 失敗モード

| 状況                                             | CLI への応答                                          |
| ------------------------------------------------ | ----------------------------------------------------- |
| 不明な OmniRoute キーかつ `REQUIRE_API_KEY=true` | 交換時に 401 `unauthenticated`                        |
| `REQUIRE_API_KEY=false`                          | 匿名セッション（`/v1/*` の動作と同様）                |
| 期限切れ / 外部発行 / 改ざんされたセッション JWT | 401、CLI が再交換                                     |
| 交換後に OmniRoute API キーが失効                | 次の RPC で 401                                       |
| アクティブな `cursor-api` 接続がない             | 503 `unavailable`                                     |
| Cursor が接続のキーを拒否                        | 401 `unauthenticated`、キャッシュ済みセッションを破棄 |
| アップストリームに到達不能                       | 502 `unavailable`（サニタイズされたメッセージ）       |
| `JWT_SECRET` が未設定                            | 交換時に 503                                          |
