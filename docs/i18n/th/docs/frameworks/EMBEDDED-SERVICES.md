# Embedded Services (ไทย)

🌐 **Languages:** 🇺🇸 [English](../../../../frameworks/EMBEDDED-SERVICES.md) · 🇪🇹 [am](../../../am/docs/frameworks/EMBEDDED-SERVICES.md) · 🇸🇦 [ar](../../../ar/docs/frameworks/EMBEDDED-SERVICES.md) · 🇦🇿 [az](../../../az/docs/frameworks/EMBEDDED-SERVICES.md) · 🇧🇬 [bg](../../../bg/docs/frameworks/EMBEDDED-SERVICES.md) · 🇧🇩 [bn](../../../bn/docs/frameworks/EMBEDDED-SERVICES.md) · 🇨🇿 [cs](../../../cs/docs/frameworks/EMBEDDED-SERVICES.md) · 🇩🇰 [da](../../../da/docs/frameworks/EMBEDDED-SERVICES.md) · 🇩🇪 [de](../../../de/docs/frameworks/EMBEDDED-SERVICES.md) · 🇬🇷 [el](../../../el/docs/frameworks/EMBEDDED-SERVICES.md) · 🇪🇸 [es](../../../es/docs/frameworks/EMBEDDED-SERVICES.md) · 🇪🇪 [et](../../../et/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇷 [fa](../../../fa/docs/frameworks/EMBEDDED-SERVICES.md) · 🇫🇮 [fi](../../../fi/docs/frameworks/EMBEDDED-SERVICES.md) · 🇫🇷 [fr](../../../fr/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇪 [ga](../../../ga/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇳 [gu](../../../gu/docs/frameworks/EMBEDDED-SERVICES.md) · 🇳🇬 [ha](../../../ha/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇱 [he](../../../he/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇳 [hi](../../../hi/docs/frameworks/EMBEDDED-SERVICES.md) · 🇭🇷 [hr](../../../hr/docs/frameworks/EMBEDDED-SERVICES.md) · 🇭🇺 [hu](../../../hu/docs/frameworks/EMBEDDED-SERVICES.md) · 🇦🇲 [hy](../../../hy/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇩 [id](../../../id/docs/frameworks/EMBEDDED-SERVICES.md) · 🇳🇬 [ig](../../../ig/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇹 [it](../../../it/docs/frameworks/EMBEDDED-SERVICES.md) · 🇯🇵 [ja](../../../ja/docs/frameworks/EMBEDDED-SERVICES.md) · 🇬🇪 [ka](../../../ka/docs/frameworks/EMBEDDED-SERVICES.md) · 🇰🇭 [km](../../../km/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇳 [kn](../../../kn/docs/frameworks/EMBEDDED-SERVICES.md) · 🇰🇷 [ko](../../../ko/docs/frameworks/EMBEDDED-SERVICES.md) · 🇱🇹 [lt](../../../lt/docs/frameworks/EMBEDDED-SERVICES.md) · 🇱🇻 [lv](../../../lv/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇳 [ml](../../../ml/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇳 [mr](../../../mr/docs/frameworks/EMBEDDED-SERVICES.md) · 🇲🇾 [ms](../../../ms/docs/frameworks/EMBEDDED-SERVICES.md) · 🇲🇹 [mt](../../../mt/docs/frameworks/EMBEDDED-SERVICES.md) · 🇲🇲 [my](../../../my/docs/frameworks/EMBEDDED-SERVICES.md) · 🇳🇵 [ne](../../../ne/docs/frameworks/EMBEDDED-SERVICES.md) · 🇳🇱 [nl](../../../nl/docs/frameworks/EMBEDDED-SERVICES.md) · 🇳🇴 [no](../../../no/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇳 [or](../../../or/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇳 [pa](../../../pa/docs/frameworks/EMBEDDED-SERVICES.md) · 🇵🇭 [phi](../../../phi/docs/frameworks/EMBEDDED-SERVICES.md) · 🇵🇱 [pl](../../../pl/docs/frameworks/EMBEDDED-SERVICES.md) · 🇵🇹 [pt](../../../pt/docs/frameworks/EMBEDDED-SERVICES.md) · 🇧🇷 [pt-BR](../../../pt-BR/docs/frameworks/EMBEDDED-SERVICES.md) · 🇷🇴 [ro](../../../ro/docs/frameworks/EMBEDDED-SERVICES.md) · 🇷🇺 [ru](../../../ru/docs/frameworks/EMBEDDED-SERVICES.md) · 🇱🇰 [si](../../../si/docs/frameworks/EMBEDDED-SERVICES.md) · 🇸🇰 [sk](../../../sk/docs/frameworks/EMBEDDED-SERVICES.md) · 🇸🇮 [sl](../../../sl/docs/frameworks/EMBEDDED-SERVICES.md) · 🇷🇸 [sr](../../../sr/docs/frameworks/EMBEDDED-SERVICES.md) · 🇸🇪 [sv](../../../sv/docs/frameworks/EMBEDDED-SERVICES.md) · 🇰🇪 [sw](../../../sw/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇳 [ta](../../../ta/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇳 [te](../../../te/docs/frameworks/EMBEDDED-SERVICES.md) · 🇹🇷 [tr](../../../tr/docs/frameworks/EMBEDDED-SERVICES.md) · 🇺🇦 [uk-UA](../../../uk-UA/docs/frameworks/EMBEDDED-SERVICES.md) · 🇵🇰 [ur](../../../ur/docs/frameworks/EMBEDDED-SERVICES.md) · 🇺🇿 [uz](../../../uz/docs/frameworks/EMBEDDED-SERVICES.md) · 🇻🇳 [vi](../../../vi/docs/frameworks/EMBEDDED-SERVICES.md) · 🇳🇬 [yo](../../../yo/docs/frameworks/EMBEDDED-SERVICES.md) · 🇨🇳 [zh-CN](../../../zh-CN/docs/frameworks/EMBEDDED-SERVICES.md) · 🇹🇼 [zh-TW](../../../zh-TW/docs/frameworks/EMBEDDED-SERVICES.md)

---

> **เวอร์ชัน:** v3.8.44
> **อัปเดตล่าสุด:** 2026-09-09
> **กลุ่มผู้อ่าน:** วิศวกรที่เพิ่ม ดูแลรักษา หรือแก้ไขข้อบกพร่องของบริการแบบฝังตัว (9Router, CLIProxyAPI, Mux, Bifrost, open-wa)

บริการแบบฝังตัวคือเครื่องมือกระบวนการ sidecar ที่ติดตั้งภายในเครื่อง ซึ่ง OmniRoute จะติดตั้ง กำกับดูแล และ
เปิดให้ใช้งานเป็นเป้าหมายการกำหนดเส้นทางระดับเฟิร์สคลาส บริการแบบฝังตัวจะแตกต่างจากผู้ให้บริการภายนอก (ซึ่งเข้าถึงผ่านอินเทอร์เน็ต
โดยใช้ API keys) ตรงที่ทำงานอยู่บนเครื่องเดียวกับ OmniRoute และสื่อสารผ่าน loopback

---

## สารบัญ

1. [ภาพรวม](#1-overview)
2. [สถาปัตยกรรม — 4 ชั้น](#2-architecture--4-layers)
3. [เครื่องสถานะวงจรชีวิต](#3-lifecycle-state-machine)
4. [เอกสารอ้างอิง API](#4-api-reference)
5. [ความปลอดภัย](#5-security)
6. [การเพิ่มบริการแบบฝังตัวใหม่](#6-adding-a-new-embedded-service)
7. [การแก้ไขปัญหา](#7-troubleshooting)
8. [คำถามที่พบบ่อย](#8-faq)

---

## 1. ภาพรวม

### เหตุใดจึงใช้บริการแบบฝังตัว?

มีบริการแบบฝังตัวทั้งหมด 6 รายการ:

| บริการ          | แพ็กเกจ npm                           | พอร์ตเริ่มต้น | วัตถุประสงค์                                                                                                                                                                                                   |
| --------------- | ------------------------------------- | :-----------: | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **9Router**     | `9router`                             |     20130     | เราเตอร์ AI ที่ OmniRoute สามารถใช้เป็นผู้ให้บริการย่อยได้ โมเดลจะถูกเปิดเผยในรูปแบบ `9router/{sub}/{model}`                                                                                                   |
| **CLIProxyAPI** | ไบนารีจาก GitHub release (`cliproxy`) |     8317      | อะแดปเตอร์พร็อกซีภายในเครื่องสำหรับโฟลว์การยืนยันตัวตนของ Anthropic CLI ทำหน้าที่เป็นเส้นทางสำรองเมื่อโทเค็น OAuth หมดอายุ                                                                                     |
| **Mux**         | `mux` (`mux server` แบบ headless)     |     8322      | ดีมอนสำหรับการประสานงานเอเจนต์ภายในเครื่อง (coder/mux) — จัดการเฉพาะวงจรชีวิตเท่านั้น ไม่ใช่เป้าหมายการกำหนดเส้นทาง (ไม่มีการพร็อกซี LLM)                                                                      |
| **Bifrost**     | `@maximhq/bifrost`                    |     8080      | แบ็กเอนด์รีเลย์เกตเวย์ AI ที่เขียนด้วย Go เมื่อทำงานอยู่ ระบบจะเลือกโดยอัตโนมัติผ่านเส้นทางรีเลย์ (`/v1/relay/`)                                                                                               |
| **Dario**       | `@askalf/dario`                       |     3456      | พร็อกซีการสมัครสมาชิก Claude — ทางเลือก/ระบบสำรองของ CLIProxyAPI สำหรับทราฟฟิกที่มีรูปแบบ Claude Code โดยคีย์ที่ฉีดเข้าไปจะกลายเป็น `DARIO_ADMIN_TOKEN` ซึ่งใช้ควบคุมการเข้าถึงส่วนควบคุม OAuth ที่ `/admin/*` |
| **open-wa**     | `@open-wa/wa-automate`                |     8323      | ระบบอัตโนมัติสำหรับ WhatsApp Web (Chromium แบบ headless ผ่าน Puppeteer) — จัดการเฉพาะวงจรชีวิตเท่านั้น ไม่ใช่เป้าหมายการกำหนดเส้นทาง                                                                           |

บริการทั้ง 6 รายการใช้โมเดลการควบคุมดูแลเดียวกัน:

- OmniRoute ติดตั้งบริการไว้ภายใต้ `DATA_DIR/services/{name}/` (แยกออกจาก `package.json` ของ OmniRoute)
- OmniRoute เริ่มต้นและตรวจสอบบริการในฐานะโปรเซสลูก
- OmniRoute ฉีดคีย์ API แบบชั่วคราวเข้าไปในสภาพแวดล้อมของโปรเซสลูก และหมุนเวียนคีย์โดยไม่หยุดให้บริการ (ในกรณีที่รองรับ)
- เส้นทางการจัดการทั้งหมด (`/api/services/*`) เป็น **LOCAL_ONLY** — เข้าถึงได้จากอินเทอร์เฟซลูปแบ็กเท่านั้น (กฎตายตัวข้อ #17)

### การตัดสินใจหลัก (จากแผนการออกแบบ)

| การตัดสินใจ                                   | ค่า                                                                          |
| --------------------------------------------- | ---------------------------------------------------------------------------- |
| การเข้าถึง UI ดั้งเดิมของ 9Router จากแดชบอร์ด | รีเวิร์สพร็อกซีที่ `/dashboard/providers/services/9router/embed/*`           |
| กลไกการติดตั้ง                                | `npm install {package}` ผ่าน `execFile` (ไม่มีการแทรกคำสั่งเชลล์)            |
| โหมดการใช้งาน                                 | ลงทะเบียนผู้ให้บริการเป็น `9router/{sub}/{model}` ในเอนจินการกำหนดเส้นทาง    |
| การจัดการคีย์ API                             | OmniRoute สร้าง เข้ารหัสขณะจัดเก็บ (AES-256-GCM) และฉีดผ่านตัวแปรสภาพแวดล้อม |
| ตำแหน่งในแดชบอร์ด                             | `/dashboard/providers/services` (สามแท็บ)                                    |
| การเริ่มต้นอัตโนมัติ                          | สวิตช์เปิด/ปิดแยกตามบริการ โดยค่าเริ่มต้นเป็นปิด                             |

---

## 2. สถาปัตยกรรม — 4 เลเยอร์

```
┌────────────────────────────────────────────────────────────────────┐
│  เลเยอร์ 1 — UI                                                    │
│  /dashboard/providers/services  (แท็บ: CLIProxyAPI | 9Router | Mux)│
│  ล็อกแบบสด (SSE), เริ่ม/หยุด/เริ่มใหม่/อัปเดต, การตั้งค่า, ติดตั้ง │
│                                                                    │
│  src/app/(dashboard)/dashboard/providers/services/                 │
│    ├── page.tsx               เชลล์ + การกำหนดเส้นทางแท็บด้วย ?tab=│
│    ├── tabs/                  CliproxyServiceTab, NinerouterServiceTab,│
│    │                          MuxServiceTab                        │
│    └── components/            ServiceStatusCard, ServiceLifecycleButtons,│
│                               ServiceLogsPanel, ApiKeyCard, ...    │
└──────────────────────┬─────────────────────────────────────────────┘
                       │ HTTP (Next.js fetch)
┌──────────────────────▼─────────────────────────────────────────────┐
│  เลเยอร์ 2 — API (LOCAL_ONLY — เฉพาะลูปแบ็กเท่านั้น)              │
│                                                                    │
│  /api/services/9router/{install|start|stop|restart|update|         │
│                          rotate-key|status|auto-start|logs}        │
│  /api/services/cliproxy/{install|start|stop|restart|update|        │
│                           status|auto-start|logs}                  │
│  /api/services/mux/{install|start|stop|restart|update|             │
│                      status|auto-start|logs}                       │
│  /dashboard/providers/services/9router/embed/[...path]             │
│    (พร็อกซี HTTP แบบย้อนกลับ + WebSocket → อัปสตรีมของ 9Router)    │
│                                                                    │
│  เกต: LOCAL_ONLY_API_PREFIXES มี "/api/services/" และ              │
│        "/dashboard/providers/services/*/embed/"                    │
└──────────────────────┬─────────────────────────────────────────────┘
                       │ การเรียกภายในโปรเซส
┌──────────────────────▼─────────────────────────────────────────────┐
│  เลเยอร์ 3 — ServiceSupervisor (src/lib/services/)                 │
│                                                                    │
│  ServiceSupervisor.ts   ซูเปอร์ไวเซอร์ทั่วไป (child_process.spawn) │
│    ├── ติดตั้ง:    execFile('npm', ['install', pkg, '--prefix'])    │
│    ├── เริ่ม:      spawn(node, [entrypoint], {env, cwd})           │
│    ├── คีย์ API:   crypto.randomBytes(32) → env NINEROUTER_API_KEY │
│    ├── พอร์ต:      20130 สำหรับ 9Router (กำหนดค่าได้)              │
│    ├── ล็อก:       บัฟเฟอร์วงแหวน stdio ขนาด 5 MB → อีเวนต์ SSE    │
│    ├── สุขภาพ:     HTTP GET /health ทุก 2–5 วินาที, กู้คืนแบบหน่วง │
│    └── วงจรชีวิต:  SIGTERM 15 วินาที → SIGKILL                    │
│                                                                    │
│  registry.ts        getSupervisor(name) / registerSupervisor()     │
│  bootstrap.ts       บูตสแตรป SERVICES[] ทั้งหมดเมื่อโปรเซสเริ่มต้น │
│  apiKey.ts          getOrCreateApiKey(), generateServiceApiKey()   │
│  modelSync.ts       GET /v1/models เป็นระยะ → ตาราง service_models │
│  ringBuffer.ts      บัฟเฟอร์ล็อกแบบวงกลม (5 MB ต่อบริการ)          │
│  healthCheck.ts     โพรบสุขภาพ HTTP แบบพอลลิง                      │
│  installers/        ninerouter.ts, cliproxy.ts, mux.ts, openwa.ts  │
│                      (อะแดปเตอร์ตัวติดตั้ง)                         │
└──────────────────────┬─────────────────────────────────────────────┘
                       │ HTTP ที่เข้ากันได้กับ OpenAI (ลูปแบ็ก)
┌──────────────────────▼─────────────────────────────────────────────┐
│  เลเยอร์ 4 — ผู้ให้บริการ / การกำหนดเส้นทาง                        │
│                                                                    │
│  open-sse/executors/ninerouter.ts                                  │
│    ค้นหาพอร์ตและคีย์ API ใหม่ต่อคำขอแต่ละครั้ง (ไม่มีการแคช)       │
│    ตัดคำนำหน้า "9router/" ออกจาก ID โมเดลก่อนส่งผ่านพร็อกซี        │
│    ส่งคืน 503 service_not_running หากซูเปอร์ไวเซอร์ไม่ได้อยู่ใน "running"│
│                                                                    │
│  src/shared/constants/providers.ts                                 │
│    รายการสำหรับ "9router": isEmbeddedService: true                 │
│                                                                    │
│  open-sse/config/providerRegistry.ts                               │
│    จัดเก็บโมเดลเป็น "9router/{sub}/{model}" (มีคำนำหน้า)           │
│    ซิงค์ทุก 5 นาทีโดย modelSync.ts                                 │
│                                                                    │
│  Mux ได้รับการจัดการเฉพาะวงจรชีวิตเท่านั้น (เลเยอร์ 1-3) — เป็น   │
│  ดีมอนประสานงานเอเจนต์ ไม่ใช่พร็อกซี LLM จึงไม่มีรายการ executor/ │
│  provider ในเลเยอร์ 4 และไม่เคยเป็นเป้าหมายการกำหนดเส้นทาง        │
└────────────────────────────────────────────────────────────────────┘
```

### ไฟล์ต้นฉบับที่สำคัญ

| ไฟล์                                        | บทบาท                                                           |
| ------------------------------------------- | --------------------------------------------------------------- |
| `src/lib/services/ServiceSupervisor.ts`     | คลาสหลัก: วงจรชีวิต, ล็อก, สถานะการทำงาน, บัฟเฟอร์แบบวงแหวน     |
| `src/lib/services/bootstrap.ts`             | การลงทะเบียนระดับโปรเซสและการเริ่มต้นอัตโนมัติ                  |
| `src/lib/services/registry.ts`              | แมปซิงเกิลตัน `tool → supervisor`                               |
| `src/lib/services/apiKey.ts`                | การสร้างคีย์, การเข้ารหัส AES-256-GCM สำหรับข้อมูลที่จัดเก็บ    |
| `src/lib/services/modelSync.ts`             | การซิงค์โมเดลเป็นระยะ (5 นาที) + ตามคำขอ                        |
| `src/lib/services/ringBuffer.ts`            | บัฟเฟอร์บันทึกแบบวงแหวนขนาด 5 MB พร้อมการสมัครรับข้อมูลผ่าน SSE |
| `src/lib/services/healthCheck.ts`           | การตรวจสอบสถานะผ่าน HTTP (กำหนดช่วงเวลาได้)                     |
| `src/lib/services/installers/ninerouter.ts` | การติดตั้ง/อัปเดต/ถอนการติดตั้ง 9Router ด้วย npm                |
| `src/lib/services/installers/cliproxy.ts`   | การติดตั้ง/อัปเดต/ถอนการติดตั้ง CLIProxyAPI ด้วย npm            |
| `src/lib/services/installers/mux.ts`        | การติดตั้ง/อัปเดต/ถอนการติดตั้ง Mux ด้วย npm                    |
| `src/lib/services/installers/openwa.ts`     | การติดตั้ง/อัปเดต/ถอนการติดตั้ง open-wa ด้วย npm                |
| `src/app/api/services/9router/_lib.ts`      | ฟังก์ชันช่วยเหลือ `getOrInitSupervisor()`                       |
| `src/app/api/services/[name]/logs/route.ts` | เอนด์พอยต์บันทึก SSE ที่ใช้ร่วมกัน                              |
| `open-sse/executors/ninerouter.ts`          | ตัวดำเนินการของผู้ให้บริการ (เลเยอร์ 4)                         |

---

## 3. เครื่องสถานะวงจรชีวิต

```
                    install()
  ┌─────────────┐ ──────────► ┌─────────────┐
  │ not_installed│             │   stopped   │◄──────────────────┐
  └─────────────┘             └──────┬──────┘                   │
                                     │ start()                   │
                                     ▼                           │ stop()
                               ┌──────────┐                      │
                               │ starting │                      │
                               └────┬─────┘                     │
         การตรวจสอบสุขภาพสำเร็จ   │         ขัดข้อง / SIGTERM    │
                               ┌────▼─────┐  (ออกภายใน 5 วินาที) │
                               │ running  │──── ขัดข้อง ─────────►┤
                               └────┬─────┘                   ┌─▼────┐
                             stop() │                          │error │
                                    ▼                          └──────┘
                               ┌──────────┐
                               │ stopping │
                               └──────────┘
```

สถานะจะถูกจัดเก็บในตาราง `version_manager` ของฐานข้อมูล (คอลัมน์ `status`) และทำสำเนา
ไว้ในสถานะในหน่วยความจำของ `ServiceSupervisor` สถานะในหน่วยความจำเป็นแหล่งข้อมูลหลักสำหรับ
กระบวนการที่กำลังทำงาน ส่วนสถานะในฐานข้อมูลเป็นข้อมูลสำรองแบบถาวรเมื่อบูต

### การเปลี่ยนสถานะ

| จาก             | เหตุการณ์                             | ไปยัง                         |
| --------------- | ------------------------------------- | ----------------------------- |
| `not_installed` | `install()` สำเร็จ                    | `stopped`                     |
| `stopped`       | มีการเรียก `start()`                  | `starting`                    |
| `starting`      | การตรวจสอบสุขภาพส่งคืน 200            | `running`                     |
| `starting`      | กระบวนการออกก่อนมีสถานะพร้อมใช้งาน    | `error`                       |
| `running`       | มีการเรียก `stop()`                   | `stopping` → `stopped`        |
| `running`       | กระบวนการออกโดยไม่คาดคิด (< 5 วินาที) | `error` (ขัดข้องอย่างรวดเร็ว) |
| `running`       | กระบวนการออกโดยไม่คาดคิด (> 5 วินาที) | `error`                       |
| `error`         | มีการเรียก `start()`                  | `starting`                    |
| สถานะใดๆ        | `stop()` ขณะอยู่ในสถานะ `stopping`    | ไม่ดำเนินการใดๆ               |

### ล็อกการดำเนินการ

`ServiceSupervisor` จัดลำดับการดำเนินการของวงจรชีวิตผ่านล็อกการดำเนินการแบบ async
(`withLock()`) การเรียก `start()` พร้อมกันบน supervisor เดียวกันจะทำให้เกิดการ spawn
เพียงหนึ่งครั้งเท่านั้น ผู้เรียกรายที่สองจะรอและส่งคืนสถานะที่มีอยู่ ซึ่งช่วยป้องกัน
race condition เช่น เมื่อการเริ่มต้นอัตโนมัติและปุ่มใน UI ถูกเรียกใช้พร้อมกัน

---

## 4. เอกสารอ้างอิง API

ทุกเส้นทางภายใต้ `/api/services/` เป็น **LOCAL_ONLY** (เฉพาะ loopback เท่านั้น, กฎตายตัว #17)
คำขอที่ไม่ได้มาจาก loopback จะได้รับ `403 LOCAL_ONLY` โดยไม่คำนึงถึงโทเค็นการยืนยันตัวตน

### 4.1 เอนด์พอยต์ของ 9Router (11 เส้นทาง)

#### `POST /api/services/9router/install`

ติดตั้ง 9Router จาก npm โดยสร้าง `DATA_DIR/services/9router/` พร้อม
`package.json` และ `node_modules/` ของตัวเอง และไม่ขัดแย้งกับ dependency ของ OmniRoute

**เนื้อหาคำขอ** (ทั้งหมดเป็นตัวเลือก):

```json
{ "version": "latest" }
```

| ฟิลด์     | ชนิด     | ค่าเริ่มต้น | คำอธิบาย                                  |
| --------- | -------- | ----------- | ----------------------------------------- |
| `version` | `string` | `"latest"`  | แท็กเวอร์ชัน npm หรือ semver ที่จะติดตั้ง |

**การตอบกลับ:**

| สถานะ | คำอธิบาย                                                                      |
| ----- | ----------------------------------------------------------------------------- |
| `200` | `{ ok: true, installedVersion: "x.y.z", path: "..." }`                        |
| `400` | เนื้อหาคำขอไม่ถูกต้อง (การตรวจสอบความถูกต้องด้วย Zod ล้มเหลว)                 |
| `409` | กำลังติดตั้งอยู่แล้ว (ล็อกถูกยึดอยู่)                                         |
| `500` | การติดตั้ง npm ล้มเหลว — ดูข้อความแสดงข้อผิดพลาดที่เข้าใจง่ายได้จาก `message` |

**หมายเหตุ:** ใช้ `execFile('npm', [...])` — ไม่มี shell และไม่มี interpolation (กฎตายตัว #13)
ข้อผิดพลาด EACCES จะแสดงเป็นข้อความที่เข้าใจง่าย

---

#### `POST /api/services/9router/start`

เริ่มต้น 9Router โดยลงทะเบียน supervisor หากยังไม่ได้ลงทะเบียน จากนั้นเรียก
`supervisor.start()` การเรียกซ้ำไม่มีผลเพิ่มเติมหากกำลังทำงานอยู่แล้ว

**เนื้อหาคำขอ:** ไม่มี

**การตอบกลับ:**

| สถานะ | คำอธิบาย                                                  |
| ----- | --------------------------------------------------------- |
| `200` | ออบเจ็กต์ `ServiceStatus` (ดูสคีมาด้านล่าง)               |
| `409` | ยังไม่ได้ติดตั้ง 9Router (`status: "not_installed"`)      |
| `503` | การเริ่มต้นล้มเหลว (ข้อผิดพลาดของโพรเซส — ดู `lastError`) |

**สคีมา ServiceStatus:**

```json
{
  "tool": "9router",
  "state": "running",
  "pid": 12345,
  "port": 20130,
  "health": "healthy",
  "startedAt": "2026-05-25T10:00:00.000Z",
  "lastError": null
}
```

---

#### `POST /api/services/9router/stop`

หยุด 9Router อย่างนุ่มนวล โดยส่ง SIGTERM รอ 15 วินาที จากนั้นส่ง SIGKILL หากโพรเซสยังทำงานอยู่
การเรียกซ้ำไม่มีผลเพิ่มเติมหากหยุดทำงานอยู่แล้ว

**เนื้อหาคำขอ:** ไม่มี

**การตอบกลับ:**

| สถานะ | คำอธิบาย                           |
| ----- | ---------------------------------- |
| `200` | `ServiceStatus` (state: "stopped") |
| `503` | การหยุดทำงานล้มเหลวโดยไม่คาดคิด    |

---

#### `POST /api/services/9router/restart`

เทียบเท่ากับการเรียก `stop()` แล้วตามด้วย `start()` ภายใต้ล็อกการดำเนินการ

**เนื้อหาคำขอ:** ไม่มี

**การตอบกลับ:** เหมือนกับ `start` (ส่งคืน `ServiceStatus` สุดท้าย)

---

#### `POST /api/services/9router/update`

อัปเดต 9Router เป็นเวอร์ชัน npm ที่ใหม่กว่า หากบริการกำลังทำงานอยู่ ระบบจะหยุดบริการ
ก่อน จากนั้นเรียกใช้การติดตั้ง npm (ติดตั้งเวอร์ชันใหม่กว่าทับในตำแหน่งเดิม) แล้วจึง
เริ่มบริการใหม่

**เนื้อหาคำขอ** (ทั้งหมดเป็นตัวเลือก):

```json
{ "version": "latest" }
```

**การตอบกลับ:**

| สถานะ | คำอธิบาย                                                        |
| ----- | --------------------------------------------------------------- |
| `200` | `{ ok: true, previousVersion: "...", installedVersion: "..." }` |
| `400` | เนื้อหาคำขอไม่ถูกต้อง                                           |
| `500` | การอัปเดต npm ล้มเหลว                                           |

---

#### `POST /api/services/9router/rotate-key`

สร้างคีย์ API ใหม่สำหรับ 9Router เข้ารหัสคีย์ขณะจัดเก็บ และเริ่มบริการใหม่
(หากกำลังทำงานอยู่) เพื่อให้บริการโหลดคีย์ใหม่จากสภาพแวดล้อม คีย์เก่าจะ
ถูกยกเลิกทันที

**เนื้อหาคำขอ:** ไม่มี

**การตอบกลับ:**

| สถานะ | คำอธิบาย                                   |
| ----- | ------------------------------------------ |
| `200` | `{ keyRotated: true, restarted: boolean }` |
| `500` | การหมุนเวียนคีย์ล้มเหลว                    |

**ความปลอดภัย:** ระบบจะไม่ส่งคืนคีย์ใหม่ในการตอบกลับ (ไม่มีการรั่วไหลของข้อมูลรับรอง)
คีย์จะถูกจัดเก็บแบบเข้ารหัส (AES-256-GCM) ในตาราง `version_manager`

---

#### `GET /api/services/9router/status`

ส่งคืนสถานะแบบเรียลไทม์ร่วมกับสถานะจาก DB ซึ่งรวมข้อมูลเมตาของเวอร์ชันและตัวอย่างคีย์ API

**การตอบกลับ:**

| สถานะ | คำอธิบาย            |
| ----- | ------------------- |
| `200` | ดูสคีมาด้านล่าง     |
| `500` | การอ่านสถานะล้มเหลว |

**สคีมาการตอบกลับ:**

```json
{
  "tool": "9router",
  "state": "running",
  "pid": 12345,
  "port": 20130,
  "health": "healthy",
  "startedAt": "2026-05-25T10:00:00.000Z",
  "lastError": null,
  "installedVersion": "1.2.3",
  "latestVersion": "1.2.4",
  "updateAvailable": true,
  "apiKeyMasked": "nr_****abcd",
  "autoStart": false,
  "providerExpose": false
}
```

---

#### `POST /api/services/9router/auto-start`

สลับแฟล็กการเริ่มต้นอัตโนมัติ เมื่อ `enabled: true` บริการจะเริ่มต้นโดยอัตโนมัติ
ในครั้งถัดไปที่ OmniRoute บูต (หากติดตั้งบริการแล้ว)

**เนื้อหาคำขอ:**

```json
{ "enabled": true }
```

**การตอบกลับ:**

| สถานะ | คำอธิบาย              |
| ----- | --------------------- |
| `200` | `{ autoStart: true }` |
| `400` | เนื้อหาคำขอไม่ถูกต้อง |

---

#### `GET /api/services/9router/logs`

สตรีม SSE ของบันทึกแบบเรียลไทม์จาก ring buffer ของ stdout/stderr ของ 9Router

**พารามิเตอร์คิวรี:**

| พารามิเตอร์ | ชนิด      | ค่าเริ่มต้น | คำอธิบาย                                                                          |
| ----------- | --------- | ----------- | --------------------------------------------------------------------------------- |
| `tail`      | `integer` | 200         | จำนวนบรรทัดย้อนหลังที่จะส่งก่อน (สูงสุด 1000)                                     |
| `filter`    | `string`  | ไม่มี       | ตัวกรองสตริงย่อยแบบไม่คำนึงถึงตัวพิมพ์เล็ก-ใหญ่ (ไม่ใช้ regex — ปลอดภัยจาก ReDoS) |

**อีเวนต์ SSE:**

| อีเวนต์     | ข้อมูล      | คำอธิบาย                          |
| ----------- | ----------- | --------------------------------- |
| `snapshot`  | `LogLine[]` | ส่วนท้ายของประวัติเริ่มต้น        |
| `log`       | `LogLine`   | บรรทัดบันทึกแบบเรียลไทม์          |
| `heartbeat` | `{}`        | สัญญาณคงการเชื่อมต่อทุก 15 วินาที |

**สคีมา LogLine:**

```json
{
  "ts": 1716633600000,
  "stream": "stdout",
  "line": "[9router] Listening on :20130"
}
```

**การตอบกลับ:**

| สถานะ | คำอธิบาย                                      |
| ----- | --------------------------------------------- |
| `200` | `text/event-stream`                           |
| `400` | พารามิเตอร์ `filter` ยาวเกินไป (> 200 อักขระ) |
| `404` | ไม่พบบริการ (ยังไม่ได้ลงทะเบียน supervisor)   |

---

### 4.2 เอ็นด์พอยต์ CLIProxyAPI (10 เส้นทาง)

CLIProxyAPI มีรูปแบบเอ็นด์พอยต์เหมือนกับ 9Router แต่ไม่มี `rotate-key` และเพิ่ม
`accounts`, `provider-expose` และ `auto-restart-adopted` ปัจจุบันได้รับ
คีย์ API สำหรับ data plane โดยเฉพาะ ซึ่งฉีดเข้ามาตอน spawn (`needsApiKey: true` ใน
`bootstrap.ts` และใช้สำหรับซิงค์โมเดล) โดย `status` มีฟิลด์น้อยกว่า

| เมธอด  | พาธ                                 | คำอธิบาย                                      |
| ------ | ----------------------------------- | --------------------------------------------- |
| `POST` | `/api/services/cliproxy/install`    | ติดตั้ง CLIProxyAPI จาก npm                   |
| `POST` | `/api/services/cliproxy/start`      | เริ่ม CLIProxyAPI                             |
| `POST` | `/api/services/cliproxy/stop`       | หยุด CLIProxyAPI                              |
| `POST` | `/api/services/cliproxy/restart`    | รีสตาร์ต CLIProxyAPI                          |
| `POST` | `/api/services/cliproxy/update`     | อัปเดตเป็นเวอร์ชันที่ใหม่กว่า                 |
| `GET`  | `/api/services/cliproxy/status`     | สถานะแบบเรียลไทม์ + DB (ไม่มี `apiKeyMasked`) |
| `POST` | `/api/services/cliproxy/auto-start` | เปิด/ปิดการเริ่มอัตโนมัติ                     |

เอ็นด์พอยต์ร่วม `GET /api/services/{name}/logs` (ดู §4.1) ใช้งานได้กับบริการ
ทั้งสี่รายการผ่านเซกเมนต์แบบไดนามิก `[name]`

---

### 4.3 เอ็นด์พอยต์ Mux (8 เส้นทาง)

Mux มีรูปแบบเอ็นด์พอยต์เหมือนกับ CLIProxyAPI — ไม่มีเส้นทาง `rotate-key` ในพื้นผิว
API (bearer token ถูกสร้างด้วยวิธีเดียวกับของ 9Router ผ่าน
`getOrCreateApiKey("mux")` และฉีดผ่านตัวแปรสภาพแวดล้อม `MUX_SERVER_AUTH_TOKEN` แต่
ยังไม่มีเอ็นด์พอยต์เฉพาะสำหรับหมุนเวียนคีย์) Mux ได้รับการจัดการเฉพาะวงจรชีวิตเท่านั้น:
ต่างจาก 9Router ตรงที่ไม่มีตัวดำเนินการ Layer 4 และไม่เคยถูกลงทะเบียนเป็นผู้ให้บริการกำหนดเส้นทาง

| เมธอด  | พาธ                            | คำอธิบาย                           |
| ------ | ------------------------------ | ---------------------------------- |
| `POST` | `/api/services/mux/install`    | ติดตั้ง Mux จาก npm (`npm i mux`)  |
| `POST` | `/api/services/mux/start`      | เริ่ม Mux (`mux server`)           |
| `POST` | `/api/services/mux/stop`       | หยุด Mux                           |
| `POST` | `/api/services/mux/restart`    | รีสตาร์ต Mux                       |
| `POST` | `/api/services/mux/update`     | อัปเดตเป็นเวอร์ชัน npm ที่ใหม่กว่า |
| `GET`  | `/api/services/mux/status`     | สถานะแบบเรียลไทม์ + DB             |
| `POST` | `/api/services/mux/auto-start` | เปิด/ปิดการเริ่มอัตโนมัติ          |

---

### 4.4 เอ็นด์พอยต์ Bifrost (8 เส้นทาง)

Bifrost เป็นแบ็กเอนด์รีเลย์เกตเวย์ AI ที่เขียนด้วย Go (`@maximhq/bifrost`) โดยใช้
รูปแบบเอ็นด์พอยต์เดียวกับ CLIProxyAPI (ไม่มี `rotate-key` — Bifrost จัดการคีย์ของ
ผู้ให้บริการเองใน `config.json` ภายใต้ `-app-dir`)

| เมธอด  | พาธ                                | คำอธิบาย                                                               |
| ------ | ---------------------------------- | ---------------------------------------------------------------------- |
| `POST` | `/api/services/bifrost/install`    | ติดตั้ง Bifrost จาก npm (`@maximhq/bifrost`)                           |
| `POST` | `/api/services/bifrost/start`      | เริ่ม Bifrost บนพอร์ต 8080 (ค่าเริ่มต้น)                               |
| `POST` | `/api/services/bifrost/stop`       | หยุด Bifrost                                                           |
| `POST` | `/api/services/bifrost/restart`    | รีสตาร์ต Bifrost                                                       |
| `POST` | `/api/services/bifrost/update`     | อัปเดตเป็นเวอร์ชันที่ใหม่กว่า                                          |
| `GET`  | `/api/services/bifrost/status`     | สถานะแบบเรียลไทม์ + DB                                                 |
| `POST` | `/api/services/bifrost/auto-start` | เปิด/ปิดการเริ่มอัตโนมัติ                                              |
| `GET`  | `/api/services/bifrost/logs`       | ติดตามส่วนท้ายของบันทึกผ่าน SSE (ผ่านเส้นทางไดนามิกร่วม `[name]/logs`) |

**การเชื่อมต่อระบบกำหนดเส้นทาง:** เมื่อไม่ได้ตั้งค่า `BIFROST_BASE_URL` และอินสแตนซ์
Bifrost ที่อยู่ภายใต้การควบคุมกำลังทำงาน `getBifrostRoutingConfig()` (ใน
`routingBackend.ts`) จะใช้ `http://127.0.0.1:{port}` เป็น URL ฐานของรีเลย์โดยอัตโนมัติ
ตัวแปรสภาพแวดล้อม `BIFROST_BASE_URL` ที่ตั้งค่าไว้อย่างชัดเจนจะมีลำดับความสำคัญสูงกว่าเสมอ

---

### 4.5 เอ็นด์พอยต์ Dario (12 เส้นทาง)

มีรูปแบบวงจรชีวิตเหมือนกับบริการอื่น (`install`, `start`, `stop`, `restart`,
`update`, `status`, `auto-start`, `auto-restart-adopted`) พร้อม control plane OAuth
ที่มีโทเค็นควบคุมภายใต้ `admin/`: `admin/accounts`, `admin/import-from-omniroute`,
`admin/login-start`, `admin/login-complete` (ทั้งหมดอยู่ภายใต้การป้องกันของ `DARIO_ADMIN_TOKEN`)

### 4.6 เอ็นด์พอยต์ open-wa (7 เส้นทาง)

open-wa (`@open-wa/wa-automate`) ควบคุมอินสแตนซ์ Chromium แบบ headless (ผ่าน
Puppeteer) เพื่อทำให้ WhatsApp Web ทำงานโดยอัตโนมัติ โดยใช้รูปแบบเอ็นด์พอยต์เดียวกับ Mux (ยังไม่มี
เส้นทาง `rotate-key`) และได้รับการจัดการเฉพาะวงจรชีวิตเท่านั้น — ไม่ใช่เป้าหมายการกำหนดเส้นทาง
ไม่มีรายการตัวดำเนินการ/ผู้ให้บริการ Layer 4

| เมธอด  | พาธ                               | คำอธิบาย                                                      |
| ------ | --------------------------------- | ------------------------------------------------------------- |
| `POST` | `/api/services/openwa/install`    | ติดตั้ง open-wa จาก npm (`@open-wa/wa-automate`)              |
| `POST` | `/api/services/openwa/start`      | เริ่มต้น open-wa บนพอร์ต 8323 (ค่าเริ่มต้น)                   |
| `POST` | `/api/services/openwa/stop`       | หยุด open-wa                                                  |
| `POST` | `/api/services/openwa/restart`    | เริ่มต้น open-wa ใหม่                                         |
| `POST` | `/api/services/openwa/update`     | อัปเดตเป็นเวอร์ชันที่ใหม่กว่า                                 |
| `GET`  | `/api/services/openwa/status`     | สถานะแบบเรียลไทม์ + สถานะ DB                                  |
| `POST` | `/api/services/openwa/auto-start` | เปิดหรือปิดการเริ่มต้นอัตโนมัติ                               |
| `GET`  | `/api/services/openwa/logs`       | ส่วนท้ายล็อก SSE (ผ่านไดนามิกรูต `[name]/logs` ที่ใช้ร่วมกัน) |

**คีย์ API:** ถูกแทรกเป็น `WA_KEY` — การแทนที่ผ่านตัวแปรสภาพแวดล้อมแบบทั่วไปของ open-wa
ที่มีคำนำหน้า `WA_*` จะแมปค่านี้ไปยังตัวเลือก CLI `--key`/`-k`
(`dist/cli/setup.js::envArgs()` ซึ่งตรวจสอบแล้วกับแพ็กเกจเวอร์ชัน 4.76.0
ที่ติดตั้งอยู่) โดยจะเติมคำนำหน้า `ow_` เมื่อสร้างด้วย `generateServiceApiKey()` ส่วน open-wa
จะอ่านคีย์กลับจากส่วนหัว HTTP `key`/`api_key` (ไม่ใช่ `Authorization:
Bearer`) ทั้งนี้ `/api-docs*` ได้รับการยกเว้นจากการตรวจสอบอย่างชัดเจน
(`setupAuthenticationLayer` ใน `dist/cli/server.js`) ดังนั้นโพรบตรวจสอบสถานะ
จึงไม่จำเป็นต้องมีส่วนหัวการยืนยันตัวตน

**การจับคู่:** open-wa เป็นเครื่องมือที่ไม่เป็นทางการและไม่มีความเกี่ยวข้องกับ WhatsApp —
หมายเลขที่เชื่อมต่อมีความเสี่ยงที่จะถูกแบนจากระบบตรวจจับการทำงานอัตโนมัติของ WhatsApp
เมื่อเริ่มต้นครั้งแรก โค้ด QR สำหรับการจับคู่จะถูกพิมพ์ไปยัง stdout และแสดงผ่าน
แผง Logs/สตรีม SSE ที่มีอยู่ — ขณะนี้การผสานรวมนี้ยังไม่มี
เอนด์พอยต์สำหรับรูปภาพ QR โดยเฉพาะ

---

### 4.7 รีเวิร์สพร็อกซี (การฝังแดชบอร์ด 9Router)

แดชบอร์ดฝัง UI เว็บของ 9Router ภายใน iframe ผ่านรีเวิร์สพร็อกซีภายใน
ที่:

```
GET|POST|... /dashboard/providers/services/9router/embed/[...path]
```

พร็อกซีนี้:

- ส่งต่อคำขอไปยัง `http://127.0.0.1:{port}/{path}` (เฉพาะลูปแบ็ก)
- ลบส่วนหัว `cookie` และ `authorization` ที่เข้ามา (ไม่มีการรั่วไหลของเซสชัน OmniRoute)
- แทรก `Authorization: Bearer {apiKey}` สำหรับการยืนยันตัวตนกับ 9Router
- ลบ `set-cookie`, `content-security-policy`, `x-frame-options`, `cross-origin-*` ออกจากการตอบกลับ
- เขียนการตอบกลับ HTML ใหม่เพื่อแทรก `<base href>` และปรับพาธแบบสัมบูรณ์ให้เป็นรูปแบบมาตรฐาน (`/foo` → `/dashboard/.../embed/foo`)

การอัปเกรด WebSocket สำหรับแดชบอร์ดที่ฝังไว้จะได้รับการจัดการโดยเซิร์ฟเวอร์คู่ทำงานบน
พอร์ตเฉพาะ (ดู `src/lib/services/embedWsProxy.ts`)

**ความปลอดภัย:** รูตของพร็อกซีสำหรับการฝังถูกจัดอยู่ภายใต้ `LOCAL_ONLY_API_PREFIXES`
และสามารถเข้าถึงได้จากลูปแบ็กเท่านั้น ผู้โจมตีที่ได้รับ JWT ผ่าน
ทันเนล Cloudflare/Ngrok จะไม่สามารถใช้พร็อกซีเข้าถึงบริการที่ฝังไว้ได้

---

## 5. ความปลอดภัย

### การบังคับใช้ LOCAL_ONLY (กฎตายตัว #17)

เส้นทางทั้งหมดภายใต้ `/api/services/` และ `/dashboard/providers/services/*/embed/` ถูก
จัดประเภทเป็น LOCAL_ONLY ใน `src/server/authz/routeGuard.ts` การตรวจสอบ loopback
จะทำงานโดยไม่มีเงื่อนไขก่อนเข้าสู่สาขาการยืนยันตัวตนใดๆ:

```
คำขอมาถึง
  → isLocalOnlyPath(path)?
      → ไม่ใช่ loopback → 403 LOCAL_ONLY (เสมอ ก่อนตรวจสอบการยืนยันตัวตน)
      → เป็น loopback  → ดำเนินการต่อไปยังการยืนยันตัวตนตามปกติ
```

การดำเนินการนี้ป้องกันไม่ให้ JWT ที่รั่วไหล (เช่น ผ่าน tunnel) เรียกใช้ `npm install` หรือ
สร้างโปรเซส โปรดดูเมทริกซ์ระดับทั้งหมดที่ `docs/security/ROUTE_GUARD_TIERS.md`

### การแทรก API key

9Router และ Mux ต้องใช้ API key/bearer token สำหรับ HTTP endpoint ของตนเอง
OmniRoute:

1. สร้างคีย์ผ่าน `crypto.randomBytes(32).toString("base64url")` โดยมี
   คำนำหน้าเฉพาะบริการ (`nr_` สำหรับ 9Router, `mx_` สำหรับ Mux)
2. เข้ารหัสคีย์ขณะจัดเก็บด้วย AES-256-GCM (cipher เดียวกับที่ใช้สำหรับข้อมูลรับรองของ provider)
3. ถอดรหัสและแทรกคีย์เป็น environment variable ขณะสร้างโปรเซส —
   `NINEROUTER_API_KEY` สำหรับ 9Router, `MUX_SERVER_AUTH_TOKEN` สำหรับ Mux (ไม่ใช้เป็น
   flag ของ CLI เพื่อไม่ให้ token ปรากฏใน `ps`/รายการโปรเซส)
4. ไม่ส่งคืนคีย์แบบ plaintext ใน HTTP response ใดๆ

CLIProxyAPI จะได้รับคีย์เฉพาะสำหรับ data plane ซึ่งถูกแทรกขณะสร้างโปรเซส
(`needsApiKey: true` — ใช้สำหรับซิงค์โมเดลกับ adapter)

### การป้องกัน SSRF

reverse HTTP proxy (`/dashboard/.../embed/[...path]`) ถูกกำหนดแบบตายตัวให้ส่งต่อ
ไปยัง `http://127.0.0.1:{port}` เท่านั้น โดยจะไม่ติดตาม redirect ไปยัง
ปลายทางที่ไม่ใช่ loopback ไลบรารี `ssrf-req-filter` ใช้เพื่อปฏิเสธ upstream URL ใดๆ ที่
resolve ไปยังตำแหน่งนอกช่วง loopback

### ความปลอดภัยของ shell (กฎตายตัว #13)

`npm install` ถูกเรียกใช้ผ่าน `execFile('npm', ['install', pkg, '--prefix', dir])` —
ไม่มี template literal, ไม่มี shell และไม่มีการแทรก path ภายนอกลงในสตริงคำสั่ง
ค่าขณะ runtime (port, API key) จะถูกส่งผ่านออบเจ็กต์ `env` ของ child

### การคัดกรองข้อผิดพลาด (กฎตายตัว #12)

error response ทั้งหมดจาก `/api/services/*` จะผ่าน `buildErrorBody()` หรือ
`sanitizeErrorMessage()` โดยจะไม่มีการส่งคืน `err.stack` และ `err.message` แบบดิบ
ไปยังผู้เรียกโดยตรง

---

## 6. การเพิ่มบริการแบบฝังตัวใหม่

ทำตาม 8 ขั้นตอนต่อไปนี้ โปรดอ่าน implementation ที่มีอยู่ใน `src/lib/services/installers/`
และ `src/app/api/services/` เพื่อใช้เป็นแหล่งอ้างอิงมาตรฐาน

### ขั้นตอนที่ 1 — สร้าง installer

สร้าง `src/lib/services/installers/{name}.ts` โดยใช้ `ninerouter.ts` เป็นต้นแบบ:

```typescript
export const NAME_PACKAGE = "your-npm-package";
export const NAME_DEFAULT_PORT = 20132; // เลือก port ที่ว่าง

export async function install(version = "latest"): Promise<InstallResult> { ... }
export async function update(version = "latest"): Promise<InstallResult> { ... }
export async function uninstall(): Promise<void> { ... }
export function resolveSpawnArgs(apiKey: string, port: number): SpawnArgs { ... }
export async function getInstalledVersion(): Promise<string | null> { ... }
export async function getLatestVersion(): Promise<string | null> { ... }
```

ใช้ `runNpm(['install', NAME_PACKAGE, '--prefix', dir])` จาก `installers/utils.ts`
— ห้ามใช้ `execSync` หรือการแทรกค่าผ่าน shell

### ขั้นตอนที่ 2 — ลงทะเบียนใน bootstrap

เพิ่ม `ServiceEntry` ลงในอาร์เรย์ `SERVICES` ใน `src/lib/services/bootstrap.ts`:

```typescript
{
  tool: "myservice",
  port: NAME_DEFAULT_PORT,
  healthPath: "/health",
  healthIntervalMs: 5_000,
  stopTimeoutMs: 15_000,
  logsBufferBytes: 5_242_880,
  needsApiKey: true, // ใช้ false หากไม่จำเป็นต้องมี API key
}
```

ขยาย `buildSpawnArgsFactory()` ให้รองรับ `cfg.tool === "myservice"`

#### สัญญา provider-plugin แบบเสียบเปลี่ยนได้ (ระยะที่ 1, #7333)

`src/lib/services/providerPlugins/` เพิ่มสัญญา `ServiceProviderPlugin` ซึ่ง
รวมฟิลด์ `ServiceEntry` จาก `bootstrap.ts` ของ backend และฟิลด์เทมเพลต manifest จาก
`serviceBackends.ts` ไว้ในออบเจ็กต์เดียว แทนที่จะนิยามโครงสร้างของ backend เดียวกัน
แยกกันอยู่ในสองไฟล์ที่ไม่เกี่ยวข้องกัน ณ ขณะที่เขียนนี้ **มีเพียง `9router` เท่านั้นที่
ย้ายแล้ว** — `bootstrap.ts` สร้างรายการใน `SERVICES[]` จาก
`getServiceProviderPlugin("9router")` (`src/lib/services/providerPlugins/registry.ts`)
และจะ throw startup error หาก plugin หายไป ส่วน `cliproxy`, `mux` และ `bifrost`
ยังคงใช้ literal แบบ inline ใน `SERVICES[]` ที่มีอยู่เดิมโดยไม่มีการเปลี่ยนแปลง

`open-sse/config/providerPluginManifest.ts` ยังได้รับ helper แบบเพิ่มเติม
`createServiceBackendManifestEntry(pluginId, template)` ซึ่งสร้าง
`ProviderPluginManifestEntry` ที่มีรูปแบบถูกต้องจากรายการ `SERVICE_BACKEND_MANIFEST_TEMPLATE` —
ขณะนี้ helper ดังกล่าว **ยังไม่ได้** เชื่อมต่อเข้ากับเส้นทางคำขอที่ใช้งานจริงใดๆ (ทั้ง
`generateProviderPluginManifestFromRegistry()` และ `/v1/providers/[provider]/models`);
ส่วนนี้ยังคงเป็นงานติดตามผลหลังจากพิสูจน์สัญญานี้กับ backend รายที่สองแล้ว

งานที่เลื่อนไปยัง PR ติดตามผลและติดตามภายใต้ issue #7333 ได้แก่ การย้าย `cliproxyapi` ผ่าน
registry เดียวกัน, การทำให้ `mux`/`bifrost` เป็นรูปแบบทั่วไปใน union ของ
`ServiceBackendPluginId`, การรวมกรณีพิเศษของ executor routing
(`open-sse/executors/index.ts`, `open-sse/handlers/chatCore/executorProxy.ts`)
เข้าไว้ในสัญญา plugin และการเชื่อมต่อ `createServiceBackendManifestEntry()` เข้ากับ
เส้นทางโค้ด manifest/models ที่ใช้งานจริง

### ขั้นตอนที่ 3 — เพิ่ม migration และข้อมูลเริ่มต้นใน DB

ตรวจสอบให้แน่ใจว่าบริการมีแถวใน `version_manager` ผ่าน migration ใน
`src/lib/db/migrations/` โดยแถวดังกล่าวควรมีข้อมูลดังนี้:

```sql
INSERT OR IGNORE INTO version_manager (tool, status, auto_start, provider_expose)
VALUES ('myservice', 'not_installed', 0, 0);
```

### ขั้นตอนที่ 4 — สร้าง API endpoint ทั้ง 7 รายการ

ภายใต้ `src/app/api/services/{name}/`:

```
_lib.ts            helper getOrInitSupervisor()
install/route.ts   POST — เรียก installer.install()
start/route.ts     POST — เรียก supervisor.start()
stop/route.ts      POST — เรียก supervisor.stop()
restart/route.ts   POST — เรียก supervisor.restart()
update/route.ts    POST — เรียก installer.update()
status/route.ts    GET  — รวมสถานะปัจจุบันกับสถานะจาก DB
auto-start/route.ts POST — สลับ flag auto_start
```

เส้นทาง `GET /api/services/[name]/logs` ที่ใช้ร่วมกันได้รับการเชื่อมต่อไว้เรียบร้อยแล้ว — ไม่จำเป็นต้องแก้ไข
ในส่วนนี้

ส่งต่อการตอบกลับข้อผิดพลาดทั้งหมดผ่าน `createErrorResponse()` / `buildErrorBody()`

### ขั้นตอนที่ 5 — เพิ่มใน LOCAL_ONLY_API_PREFIXES

ใน `src/server/authz/routeGuard.ts` ให้ตรวจสอบว่า `/api/services/` อยู่ในรายการแล้ว
หากคุณเพิ่มคำนำหน้าใหม่ (เช่น `/api/tools/`) ให้เพิ่มลงในทั้ง
`LOCAL_ONLY_API_PREFIXES` และหากคำนำหน้าดังกล่าวสร้างโปรเซส ให้เพิ่มลงใน `SPAWN_CAPABLE_PREFIXES` ด้วย
เพิ่มการทดสอบใน `tests/unit/authz/routeGuard.test.ts`

### ขั้นตอนที่ 6 — เพิ่มแท็บ UI

สร้าง `src/app/(dashboard)/dashboard/providers/services/tabs/{Name}ServiceTab.tsx`
ใช้คอมโพเนนต์ที่ใช้ร่วมกันต่อไปนี้:

- `ServiceStatusCard` — สถานะแบบเรียลไทม์ + ป้ายสถานะความพร้อมใช้งาน
- `ServiceLifecycleButtons` — เริ่ม / หยุด / เริ่มใหม่ / อัปเดต
- `ServiceLogsPanel` — ส่วนท้ายของล็อกผ่าน SSE (เชื่อมต่อกับ `/api/services/{name}/logs`)
- `ApiKeyCard` — แสดง + หมุนเวียนคีย์ (หาก `needsApiKey: true`)

ลงทะเบียนแท็บใน `ServicesPageShell.tsx`

### ขั้นตอนที่ 7 — เพิ่มรายการผู้ให้บริการ (หากบริการเป็นปลายทางการกำหนดเส้นทาง)

หากบริการแบบฝังตัวเปิดเผยปลายทาง `/v1/chat/completions` ที่เข้ากันได้กับ OpenAI:

1. เพิ่มรายการผู้ให้บริการใน `src/shared/constants/providers.ts` โดยกำหนด `isEmbeddedService: true`
2. สร้าง `open-sse/executors/{name}.ts` ที่สืบทอดจาก `BaseExecutor` ค้นหาพอร์ตและ
   คีย์ API ใหม่สำหรับแต่ละคำขอ (ห้ามแคชในคอนสตรักเตอร์) ส่งคืนการตอบกลับ `503 service_not_running`
   เมื่อสถานะของตัวควบคุมบริการไม่ใช่ `"running"`
3. ลงทะเบียนโมเดลใน `open-sse/config/providerRegistry.ts` โดยใช้คำนำหน้าของบริการ
   (เช่น `myservice/sub/model`) โดย `modelSync.ts` จะคอยอัปเดตโมเดลเหล่านั้นให้เป็นปัจจุบัน

### ขั้นตอนที่ 8 — จัดทำเอกสารและทดสอบ

1. อัปเดต `docs/frameworks/EMBEDDED-SERVICES.md` (ไฟล์นี้) — เพิ่มบริการลงใน
   ตารางใน §1 และเพิ่มปลายทางใหม่ทั้งหมดลงใน §4
2. เพิ่มการทดสอบหน่วยใน `tests/unit/services/` (วงจรชีวิต ตัวติดตั้ง รูปแบบ API)
3. เพิ่มการทดสอบการผสานรวมใน `tests/integration/services/` (ทำงานเมื่อกำหนด `RUN_SERVICES_INT=1`)
4. อัปเดต `docs/openapi.yaml` ด้วยปลายทางใหม่

---

## 7. การแก้ไขปัญหา

### บริการไม่เริ่มทำงาน

**อาการ:** ปุ่มเริ่มทำงานส่งคืน 503 และสถานะยังคงเป็น `"error"` หรือ `"starting"`

**รายการตรวจสอบ:**

1. ตรวจสอบ `GET /api/services/{name}/logs` (หรือแผง Logs ในแดชบอร์ด) มองหา
   บรรทัดอย่างเช่น `Error: ENOENT`, `address already in use` หรือ `Cannot find module`
2. ตรวจสอบว่า `npm` อยู่ใน PATH: เรียกใช้ `which npm` จากบัญชีผู้ใช้เดียวกับที่ใช้เรียกใช้ OmniRoute
3. ตรวจสอบว่าบริการได้รับการติดตั้งแล้ว: ตรวจสอบ `GET /api/services/{name}/status` เพื่อดู
   `installedVersion` หากเป็น `null` ให้เรียกใช้การติดตั้งก่อน
4. ตรวจสอบว่า `DATA_DIR/services/{name}/node_modules/` มีอยู่และไม่ใช่ไดเรกทอรีว่าง
5. ตรวจสอบฟิลด์ `lastError` ในการตอบกลับสถานะเพื่อดูเหตุผลการออกจากโปรแกรมที่ผ่านการกรองข้อมูลสำคัญแล้ว

---

### การเริ่มต้นแบบ Cold Start ใช้เวลานาน (> 10 วินาทีกว่าจะถึงสถานะ `running`)

**อาการ:** สถานะยังคงเป็น `"starting"` เป็นเวลานานก่อนจะเปลี่ยนเป็น `"running"` หรือ `"error"`

**คำอธิบาย:** การเริ่มต้นแบบ Cold Start ของ 9Router รวมถึงการนำเข้าลำดับชั้นการพึ่งพาขนาดใหญ่ (โมดูล DNS,
tunnel และ MITM) ช่วงเวลาตรวจสอบสถานะเริ่มต้นคือ 2 วินาที โดยพยายาม 3 ครั้งก่อนที่
supervisor จะประกาศว่าหมดเวลา (แต่ยังคงตรวจสอบต่อไป)

**วิธีแก้ไข:** `healthIntervalMs` และระยะหมดเวลาของ `waitForHealthy`
(`healthIntervalMs * 3`) สามารถกำหนดค่าได้ใน `bootstrap.ts` สำหรับบริการที่ใช้เวลา
เริ่มต้นนานกว่านั้น ให้เพิ่ม `healthIntervalMs` เป็น 5000 และ `stopTimeoutMs` เป็น 30 000

---

### พอร์ตชนกัน (`EADDRINUSE`)

**อาการ:** บันทึกแสดง `address already in use :::20130`

**สาเหตุ:**

- มีกระบวนการอื่นกำลังใช้พอร์ต 20130 อยู่แล้ว
- กระบวนการ 9Router ก่อนหน้านี้หยุดทำงานไม่สมบูรณ์ (PID ซอมบี้)

**วิธีแก้ไข:**

1. เปลี่ยนพอร์ตเริ่มต้นผ่านตัวแปรสภาพแวดล้อม `NINEROUTER_PORT` ใน `.env`
2. ค้นหาและหยุดกระบวนการที่ขัดแย้งกัน: `lsof -ti :20130 | xargs kill -9`
3. สามารถกำหนดค่าพอร์ตแยกตามบริการได้ใน `bootstrap.ts` ผ่านฟิลด์ `port`

**หมายเหตุ:** 9Router ใช้พอร์ต 20130 เป็นค่าเริ่มต้นโดยเฉพาะเพื่อหลีกเลี่ยงการชนกับ
พอร์ตเริ่มต้น 20128 ของ OmniRoute

---

### สิทธิ์ถูกปฏิเสธ (EACCES) ระหว่างการติดตั้ง

**อาการ:** การติดตั้งส่งคืน 500 และบันทึกแสดง `EACCES` หรือ `permission denied`

**สาเหตุ:**

- กระบวนการ OmniRoute ไม่มีสิทธิ์เขียนไปยัง `DATA_DIR` หรือไดเรกทอรีแม่
- ทำงานภายใน Docker แบบ rootless โดยไม่มีสิทธิ์เขียนไปยังวอลุ่มที่แมปไว้

**วิธีแก้ไข:**

1. ตรวจสอบ `DATA_DIR` (ค่าเริ่มต้น: `~/.omniroute/`): `ls -la ~/.omniroute/`
2. ตรวจสอบให้แน่ใจว่าผู้ใช้ของกระบวนการ OmniRoute เป็นเจ้าของไดเรกทอรี: `chown -R $USER ~/.omniroute/`
3. ใน Docker ให้ตรวจสอบว่าการเมานต์วอลุ่มมีสิทธิ์ที่ถูกต้องสำหรับผู้ใช้ในคอนเทนเนอร์

---

### การอัปเดตล้มเหลว (`npm install` หมดเวลาหรือเกิดข้อผิดพลาดของเครือข่าย)

**อาการ:** การอัปเดตส่งคืน 500 พร้อม `InstallError` และบันทึกแสดงว่าการเชื่อมต่อเครือข่ายหมดเวลา

**รายการตรวจสอบ:**

1. ยืนยันว่าเข้าถึงรีจิสทรี npm ได้: `npm ping`
2. ตรวจสอบพร็อกซีขององค์กร: `npm config get proxy`, `npm config get https-proxy`
3. ลองติดตั้งด้วยตนเอง: `npm install {package}@latest --prefix ~/.omniroute/services/{name}/`
4. หากอยู่หลังเครือข่ายแบบ air-gap ให้ดาวน์โหลด tarball ล่วงหน้าและใช้ `npm install /path/to/tarball.tgz`

---

### บริการแสดงสถานะ `"error"` ทันทีหลังเริ่มทำงาน (หยุดทำงานอย่างรวดเร็ว)

**อาการ:** สถานะเปลี่ยนจาก `"starting"` เป็น `"error"` ภายในเวลาไม่ถึง 5 วินาที
`lastError` แสดง `"Fast crash (exited with code 1)"`

**รายการตรวจสอบ:**

1. อ่านส่วนท้ายของบันทึกฉบับเต็ม: `GET /api/services/{name}/logs?tail=500`
2. สาเหตุทั่วไป: ไม่มีตัวแปรสภาพแวดล้อมที่บริการต้องใช้
3. สำหรับ 9Router: ตรวจสอบว่า `NINEROUTER_DISABLE_MITM=true` และ
   `NINEROUTER_DISABLE_TUNNEL=true` อยู่ในสภาพแวดล้อมที่ส่งผ่านเมื่อสร้างกระบวนการ (ดู
   `installers/ninerouter.ts` `resolveSpawnArgs`)

---

## 8. คำถามที่พบบ่อย

**ถาม: ฉันสามารถเปิดเผย endpoint ของบริการแบบฝังตัวให้ไคลเอนต์ที่ไม่ใช่ loopback เข้าถึงได้หรือไม่**

ไม่ได้ ระดับ LOCAL_ONLY ถูกกำหนดไว้โดยเจตนา (กฎตายตัวข้อ #17) Route ที่สามารถเรียกใช้
`npm install` หรือสร้างโปรเซส `node` ต้องไม่สามารถเข้าถึงได้จากทราฟฟิกที่ไม่ใช่ loopback
เพราะมิฉะนั้น JWT ที่รั่วไหลผ่าน tunnel (Cloudflare, Ngrok, Tailscale) จะทำให้
สามารถสร้างโปรเซสใดๆ ก็ได้ ไม่มีข้อยกเว้นให้ปิดใช้ข้อจำกัดนี้สำหรับ
`/api/services/` ซึ่งต่างจาก `/api/mcp/` ตรงที่ถูกยกเว้นออกจากรายการ bypass
ของ manage-scope ดู `docs/security/ROUTE_GUARD_TIERS.md`

---

**ถาม: 9Router และ CLIProxyAPI จะพร้อมใช้งานในการนำขึ้นใช้งานจริง/ระบบคลาวด์หรือไม่**

พร้อมใช้งาน ทั้งสองบริการใช้โมเดลที่เน้นการทำงานภายในเครื่องเป็นหลักเช่นเดียวกับ OmniRoute โดยทำงาน
บนเครื่องเดียวกันและสื่อสารกันผ่าน loopback ในที่นี้ "ระบบใช้งานจริง" หมายถึง VPS
หรือเซิร์ฟเวอร์ภายในเครื่องที่ติดตั้งใช้งาน OmniRoute ไม่ใช่ผู้ให้บริการคลาวด์ระยะไกล

---

**ถาม: ฉันจะดีบัก supervisor ได้อย่างไร**

1. ติดตามสตรีมล็อก SSE: `curl -N http://localhost:20128/api/services/9router/logs`
2. ตรวจสอบล็อกแบบมีโครงสร้างในเอาต์พุต pino ของ OmniRoute โดยกรองตาม namespace
   `service:supervisor`
3. ตรวจสอบแถวใน DB: `sqlite3 ~/.omniroute/omniroute.db "SELECT * FROM version_manager WHERE tool='9router'"`
4. ใช้ `GET /api/services/9router/status` เพื่อดูสถานะปัจจุบันแบบสด, PID, สถานะสุขภาพ
   และ `lastError` ได้ในคำขอเดียว

---

**ถาม: supervisor แสดง `health: "degraded"` หรือ `health: "unknown"` แต่ state เป็น `"running"` นี่เป็นปัญหาหรือไม่**

`"degraded"` หมายความว่าการตรวจสอบสุขภาพได้รับการตอบกลับที่ไม่ใช่ 200 ส่วน `"unknown"` หมายความว่ายังไม่มี
การตรวจสอบใดเสร็จสิ้น (เกิด race กับการ poll ครั้งแรก) ทั้งสองสถานะเป็นเพียงชั่วคราวระหว่างการเริ่มต้นระบบ
หากสถานะสุขภาพยังคงเป็น `"degraded"` นานกว่า `healthIntervalMs * 3` ms หลังจาก
`"running"` แสดงว่าบริการแบบฝังตัวกำลังทำงานอยู่ แต่ HTTP API ไม่ตอบสนอง ให้ตรวจสอบ
ว่าพอร์ตในผลตอบกลับสถานะถูกต้องหรือไม่ และบริการกำลังรับฟังอยู่บนพอร์ตนั้นจริงหรือไม่

---

**ถาม: ฉันสามารถเปลี่ยน API key ของ 9Router โดยไม่ต้องรีสตาร์ตทั้งหมดได้หรือไม่**

ไม่ได้ API key จะถูกส่งให้ 9Router ผ่าน environment variable ขณะสร้างโปรเซส
ไม่สามารถเปลี่ยน environment variable ในโปรเซสที่กำลังทำงานอยู่ได้ `POST .../rotate-key`
จะหยุดและรีสตาร์ตบริการโดยอัตโนมัติเพื่อนำคีย์ใหม่ไปใช้ การหมุนเวียนคีย์
จะมีผลภายในระยะเวลา `stopTimeoutMs` ของบริการ (ค่าเริ่มต้น 15 s) บวกกับเวลา
เริ่มต้นระบบของบริการ

---

**ถาม: ขีดจำกัดของ ring buffer คือเท่าใด และจะเกิดอะไรขึ้นเมื่อเต็ม**

แต่ละบริการมี ring buffer เฉพาะขนาด 5 MB เมื่อบัฟเฟอร์เต็ม
บรรทัดล็อกที่เก่าที่สุดจะถูกนำออกเพื่อเพิ่มพื้นที่ให้บรรทัดใหม่ Event `snapshot` ของ SSE จะส่งคืน
บรรทัดล่าสุดภายในขีดจำกัด `tail` ล็อกจะไม่ถูกบันทึกลงดิสก์ เว้นแต่
จะกำหนด `logsBufferPath` ไว้ในแถวของ DB

---

## ดูเพิ่มเติม

- `docs/security/ROUTE_GUARD_TIERS.md` — รายละเอียดระดับ LOCAL_ONLY
- `docs/architecture/CODEBASE_DOCUMENTATION.md` — §3.2 การแมปโมดูลบริการแบบฝังตัว
- `docs/architecture/ARCHITECTURE.md` — บริบทระดับระบบ
- `docs/openapi.yaml` — คำจำกัดความ endpoint ที่เครื่องอ่านได้
- `CLAUDE.md` §"การเพิ่มบริการแบบฝังตัวใหม่" — รายการตรวจสอบอ้างอิงฉบับย่อ
