# Embedded Services (Tiếng Việt)

🌐 **Languages:** 🇺🇸 [English](../../../../frameworks/EMBEDDED-SERVICES.md) · 🇪🇹 [am](../../../am/docs/frameworks/EMBEDDED-SERVICES.md) · 🇸🇦 [ar](../../../ar/docs/frameworks/EMBEDDED-SERVICES.md) · 🇦🇿 [az](../../../az/docs/frameworks/EMBEDDED-SERVICES.md) · 🇧🇬 [bg](../../../bg/docs/frameworks/EMBEDDED-SERVICES.md) · 🇧🇩 [bn](../../../bn/docs/frameworks/EMBEDDED-SERVICES.md) · 🇨🇿 [cs](../../../cs/docs/frameworks/EMBEDDED-SERVICES.md) · 🇩🇰 [da](../../../da/docs/frameworks/EMBEDDED-SERVICES.md) · 🇩🇪 [de](../../../de/docs/frameworks/EMBEDDED-SERVICES.md) · 🇬🇷 [el](../../../el/docs/frameworks/EMBEDDED-SERVICES.md) · 🇪🇸 [es](../../../es/docs/frameworks/EMBEDDED-SERVICES.md) · 🇪🇪 [et](../../../et/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇷 [fa](../../../fa/docs/frameworks/EMBEDDED-SERVICES.md) · 🇫🇮 [fi](../../../fi/docs/frameworks/EMBEDDED-SERVICES.md) · 🇫🇷 [fr](../../../fr/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇪 [ga](../../../ga/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇳 [gu](../../../gu/docs/frameworks/EMBEDDED-SERVICES.md) · 🇳🇬 [ha](../../../ha/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇱 [he](../../../he/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇳 [hi](../../../hi/docs/frameworks/EMBEDDED-SERVICES.md) · 🇭🇷 [hr](../../../hr/docs/frameworks/EMBEDDED-SERVICES.md) · 🇭🇺 [hu](../../../hu/docs/frameworks/EMBEDDED-SERVICES.md) · 🇦🇲 [hy](../../../hy/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇩 [id](../../../id/docs/frameworks/EMBEDDED-SERVICES.md) · 🇳🇬 [ig](../../../ig/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇹 [it](../../../it/docs/frameworks/EMBEDDED-SERVICES.md) · 🇯🇵 [ja](../../../ja/docs/frameworks/EMBEDDED-SERVICES.md) · 🇬🇪 [ka](../../../ka/docs/frameworks/EMBEDDED-SERVICES.md) · 🇰🇭 [km](../../../km/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇳 [kn](../../../kn/docs/frameworks/EMBEDDED-SERVICES.md) · 🇰🇷 [ko](../../../ko/docs/frameworks/EMBEDDED-SERVICES.md) · 🇱🇹 [lt](../../../lt/docs/frameworks/EMBEDDED-SERVICES.md) · 🇱🇻 [lv](../../../lv/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇳 [ml](../../../ml/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇳 [mr](../../../mr/docs/frameworks/EMBEDDED-SERVICES.md) · 🇲🇾 [ms](../../../ms/docs/frameworks/EMBEDDED-SERVICES.md) · 🇲🇹 [mt](../../../mt/docs/frameworks/EMBEDDED-SERVICES.md) · 🇲🇲 [my](../../../my/docs/frameworks/EMBEDDED-SERVICES.md) · 🇳🇵 [ne](../../../ne/docs/frameworks/EMBEDDED-SERVICES.md) · 🇳🇱 [nl](../../../nl/docs/frameworks/EMBEDDED-SERVICES.md) · 🇳🇴 [no](../../../no/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇳 [or](../../../or/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇳 [pa](../../../pa/docs/frameworks/EMBEDDED-SERVICES.md) · 🇵🇭 [phi](../../../phi/docs/frameworks/EMBEDDED-SERVICES.md) · 🇵🇱 [pl](../../../pl/docs/frameworks/EMBEDDED-SERVICES.md) · 🇵🇹 [pt](../../../pt/docs/frameworks/EMBEDDED-SERVICES.md) · 🇧🇷 [pt-BR](../../../pt-BR/docs/frameworks/EMBEDDED-SERVICES.md) · 🇷🇴 [ro](../../../ro/docs/frameworks/EMBEDDED-SERVICES.md) · 🇷🇺 [ru](../../../ru/docs/frameworks/EMBEDDED-SERVICES.md) · 🇱🇰 [si](../../../si/docs/frameworks/EMBEDDED-SERVICES.md) · 🇸🇰 [sk](../../../sk/docs/frameworks/EMBEDDED-SERVICES.md) · 🇸🇮 [sl](../../../sl/docs/frameworks/EMBEDDED-SERVICES.md) · 🇷🇸 [sr](../../../sr/docs/frameworks/EMBEDDED-SERVICES.md) · 🇸🇪 [sv](../../../sv/docs/frameworks/EMBEDDED-SERVICES.md) · 🇰🇪 [sw](../../../sw/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇳 [ta](../../../ta/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇳 [te](../../../te/docs/frameworks/EMBEDDED-SERVICES.md) · 🇹🇭 [th](../../../th/docs/frameworks/EMBEDDED-SERVICES.md) · 🇹🇷 [tr](../../../tr/docs/frameworks/EMBEDDED-SERVICES.md) · 🇺🇦 [uk-UA](../../../uk-UA/docs/frameworks/EMBEDDED-SERVICES.md) · 🇵🇰 [ur](../../../ur/docs/frameworks/EMBEDDED-SERVICES.md) · 🇺🇿 [uz](../../../uz/docs/frameworks/EMBEDDED-SERVICES.md) · 🇳🇬 [yo](../../../yo/docs/frameworks/EMBEDDED-SERVICES.md) · 🇨🇳 [zh-CN](../../../zh-CN/docs/frameworks/EMBEDDED-SERVICES.md) · 🇹🇼 [zh-TW](../../../zh-TW/docs/frameworks/EMBEDDED-SERVICES.md)

---

> **Phiên bản:** v3.8.44
> **Cập nhật lần cuối:** 2026-09-09
> **Đối tượng:** Các kỹ sư bổ sung, bảo trì hoặc gỡ lỗi các dịch vụ nhúng (9Router, CLIProxyAPI, Mux, Bifrost, open-wa).

Dịch vụ nhúng là các công cụ tiến trình sidecar được cài đặt cục bộ mà OmniRoute cài đặt, giám sát và
cung cấp như những đích định tuyến hạng nhất. Không giống các nhà cung cấp bên ngoài (được truy cập qua internet
bằng API key), dịch vụ nhúng chạy trên cùng máy với OmniRoute và giao tiếp qua loopback.

---

## Mục lục

1. [Tổng quan](#1-tổng-quan)
2. [Kiến trúc — 4 lớp](#2-kiến-trúc--4-lớp)
3. [Máy trạng thái vòng đời](#3-máy-trạng-thái-vòng-đời)
4. [Tài liệu tham chiếu API](#4-tài-liệu-tham-chiếu-api)
5. [Bảo mật](#5-bảo-mật)
6. [Thêm dịch vụ nhúng mới](#6-thêm-dịch-vụ-nhúng-mới)
7. [Khắc phục sự cố](#7-khắc-phục-sự-cố)
8. [Câu hỏi thường gặp](#8-câu-hỏi-thường-gặp)

---

## 1. Tổng quan

### Tại sao cần các dịch vụ nhúng?

Sáu dịch vụ được nhúng:

| Dịch vụ         | Gói npm                                           | Cổng mặc định | Mục đích                                                                                                                                                                                                                |
| --------------- | ------------------------------------------------- | :-----------: | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **9Router**     | `9router`                                         |     20130     | Bộ định tuyến AI mà OmniRoute có thể sử dụng như một nhà cung cấp phụ. Các mô hình được cung cấp dưới dạng `9router/{sub}/{model}`                                                                                      |
| **CLIProxyAPI** | Tệp nhị phân từ bản phát hành GitHub (`cliproxy`) |     8317      | Bộ điều hợp proxy cục bộ cho các luồng xác thực Anthropic CLI. Cung cấp định tuyến dự phòng khi token OAuth hết hạn                                                                                                     |
| **Mux**         | `mux` (`mux server` không giao diện)              |     8322      | Trình nền điều phối tác tử cục bộ (coder/mux). Chỉ được quản lý vòng đời — không phải là đích định tuyến (không proxy LLM).                                                                                             |
| **Bifrost**     | `@maximhq/bifrost`                                |     8080      | Backend chuyển tiếp cổng AI viết bằng Go. Khi đang chạy, được tuyến chuyển tiếp (`/v1/relay/`) tự động chọn                                                                                                             |
| **Dario**       | `@askalf/dario`                                   |     3456      | Proxy đăng ký Claude — phương án thay thế/dự phòng cho CLIProxyAPI đối với lưu lượng có cấu trúc Claude Code; khóa được chèn sẽ trở thành `DARIO_ADMIN_TOKEN`, bảo vệ mặt phẳng điều khiển OAuth `/admin/*` của dịch vụ |
| **open-wa**     | `@open-wa/wa-automate`                            |     8323      | Tự động hóa WhatsApp Web (Chromium không giao diện thông qua Puppeteer). Chỉ được quản lý vòng đời — không phải là đích định tuyến.                                                                                     |

Cả sáu dịch vụ đều tuân theo cùng một mô hình giám sát:

- OmniRoute cài đặt chúng trong `DATA_DIR/services/{name}/` (tách biệt với `package.json` của chính OmniRoute)
- OmniRoute khởi chạy và giám sát chúng dưới dạng các tiến trình con
- OmniRoute chèn một khóa API tạm thời vào môi trường của tiến trình con và luân phiên khóa mà không gây thời gian ngừng hoạt động (khi có thể áp dụng)
- Tất cả các tuyến quản lý (`/api/services/*`) đều là **LOCAL_ONLY** — chỉ có thể truy cập từ loopback (quy tắc bắt buộc #17)

### Các quyết định chính (từ kế hoạch thiết kế)

| Quyết định                                             | Giá trị                                                                              |
| ------------------------------------------------------ | ------------------------------------------------------------------------------------ |
| Quyền truy cập dashboard vào giao diện gốc của 9Router | Reverse proxy tại `/dashboard/providers/services/9router/embed/*`                    |
| Cơ chế cài đặt                                         | `npm install {package}` thông qua `execFile` (không nội suy shell)                   |
| Chế độ sử dụng                                         | Nhà cung cấp được đăng ký dưới dạng `9router/{sub}/{model}` trong công cụ định tuyến |
| Quản lý khóa API                                       | OmniRoute tạo, mã hóa khi lưu trữ (AES-256-GCM) và chèn thông qua biến môi trường    |
| Vị trí dashboard                                       | `/dashboard/providers/services` (ba tab)                                             |
| Tự động khởi động                                      | Nút bật/tắt riêng cho từng dịch vụ, mặc định TẮT                                     |

---

## 2. Kiến trúc — 4 lớp

```
┌────────────────────────────────────────────────────────────────────┐
│  Lớp 1 — UI                                                       │
│  /dashboard/providers/services  (tab: CLIProxyAPI | 9Router | Mux) │
│  Nhật ký trực tiếp (SSE), Khởi động/Dừng/Khởi động lại/Cập nhật,   │
│  Cài đặt, Thiết lập                                               │
│                                                                    │
│  src/app/(dashboard)/dashboard/providers/services/                 │
│    ├── page.tsx               Khung + định tuyến tab theo ?tab=    │
│    ├── tabs/                  CliproxyServiceTab, NinerouterServiceTab,│
│    │                          MuxServiceTab                        │
│    └── components/            ServiceStatusCard, ServiceLifecycleButtons,│
│                               ServiceLogsPanel, ApiKeyCard, ...    │
└──────────────────────┬─────────────────────────────────────────────┘
                       │ HTTP (Next.js fetch)
┌──────────────────────▼─────────────────────────────────────────────┐
│  Lớp 2 — API (LOCAL_ONLY — chỉ loopback)                           │
│                                                                    │
│  /api/services/9router/{install|start|stop|restart|update|         │
│                          rotate-key|status|auto-start|logs}        │
│  /api/services/cliproxy/{install|start|stop|restart|update|        │
│                           status|auto-start|logs}                  │
│  /api/services/mux/{install|start|stop|restart|update|             │
│                      status|auto-start|logs}                       │
│  /dashboard/providers/services/9router/embed/[...path]             │
│    (proxy ngược HTTP + WebSocket → upstream 9Router)               │
│                                                                    │
│  Cổng kiểm soát: LOCAL_ONLY_API_PREFIXES bao gồm "/api/services/" │
│        và "/dashboard/providers/services/*/embed/"                 │
└──────────────────────┬─────────────────────────────────────────────┘
                       │ các lệnh gọi trong cùng tiến trình
┌──────────────────────▼─────────────────────────────────────────────┐
│  Lớp 3 — ServiceSupervisor (src/lib/services/)                     │
│                                                                    │
│  ServiceSupervisor.ts   Trình giám sát tổng quát (child_process.spawn)│
│    ├── cài đặt:   execFile('npm', ['install', pkg, '--prefix'])    │
│    ├── khởi động: spawn(node, [entrypoint], {env, cwd})            │
│    ├── api_key:    crypto.randomBytes(32) → env NINEROUTER_API_KEY  │
│    ├── cổng:       20130 cho 9Router (có thể cấu hình)             │
│    ├── nhật ký:    bộ đệm vòng stdio 5 MB → sự kiện SSE            │
│    ├── sức khỏe:   HTTP GET /health mỗi 2–5 giây, phục hồi lười    │
│    └── vòng đời:   SIGTERM 15 giây → SIGKILL                       │
│                                                                    │
│  registry.ts        getSupervisor(name) / registerSupervisor()     │
│  bootstrap.ts       Khởi tạo tất cả SERVICES[] khi tiến trình chạy │
│  apiKey.ts          getOrCreateApiKey(), generateServiceApiKey()   │
│  modelSync.ts       GET /v1/models định kỳ → bảng service_models   │
│  ringBuffer.ts      Bộ đệm nhật ký vòng (5 MB cho mỗi dịch vụ)     │
│  healthCheck.ts     Thăm dò sức khỏe HTTP theo chu kỳ              │
│  installers/        ninerouter.ts, cliproxy.ts, mux.ts, openwa.ts  │
│                      (các bộ điều hợp trình cài đặt)                │
└──────────────────────┬─────────────────────────────────────────────┘
                       │ HTTP tương thích OpenAI (loopback)
┌──────────────────────▼─────────────────────────────────────────────┐
│  Lớp 4 — Nhà cung cấp / Định tuyến                                 │
│                                                                    │
│  open-sse/executors/ninerouter.ts                                  │
│    Tra cứu lại cổng và khóa API cho mỗi yêu cầu (không lưu đệm).  │
│    Loại bỏ tiền tố "9router/" khỏi mã model trước khi proxy.       │
│    Trả về 503 service_not_running nếu trình giám sát không ở       │
│    trạng thái "running".                                           │
│                                                                    │
│  src/shared/constants/providers.ts                                 │
│    Mục nhập cho "9router": isEmbeddedService: true                 │
│                                                                    │
│  open-sse/config/providerRegistry.ts                               │
│    Các model được lưu dưới dạng "9router/{sub}/{model}" (có tiền tố).│
│    Được modelSync.ts đồng bộ mỗi 5 phút.                           │
│                                                                    │
│  Mux CHỈ được quản lý vòng đời (Lớp 1-3) — đây là daemon điều phối │
│  tác tử, không phải proxy LLM, vì vậy nó không có executor/mục nhà │
│  cung cấp ở Lớp 4 và không bao giờ là đích định tuyến.             │
└────────────────────────────────────────────────────────────────────┘
```

### Các tệp nguồn chính

| Tệp                                         | Vai trò                                                    |
| ------------------------------------------- | ---------------------------------------------------------- |
| `src/lib/services/ServiceSupervisor.ts`     | Lớp cốt lõi: vòng đời, khóa, tình trạng, bộ đệm vòng       |
| `src/lib/services/bootstrap.ts`             | Đăng ký ở cấp tiến trình và tự động khởi động              |
| `src/lib/services/registry.ts`              | Ánh xạ singleton `tool → supervisor`                       |
| `src/lib/services/apiKey.ts`                | Tạo khóa, mã hóa AES-256-GCM khi lưu trữ                   |
| `src/lib/services/modelSync.ts`             | Đồng bộ mô hình định kỳ (5 phút) + theo yêu cầu            |
| `src/lib/services/ringBuffer.ts`            | Bộ đệm nhật ký vòng 5 MB có hỗ trợ đăng ký SSE             |
| `src/lib/services/healthCheck.ts`           | Thăm dò tình trạng HTTP (khoảng thời gian có thể cấu hình) |
| `src/lib/services/installers/ninerouter.ts` | Cài đặt/cập nhật/gỡ cài đặt 9Router bằng npm               |
| `src/lib/services/installers/cliproxy.ts`   | Cài đặt/cập nhật/gỡ cài đặt CLIProxyAPI bằng npm           |
| `src/lib/services/installers/mux.ts`        | Cài đặt/cập nhật/gỡ cài đặt Mux bằng npm                   |
| `src/lib/services/installers/openwa.ts`     | Cài đặt/cập nhật/gỡ cài đặt open-wa bằng npm               |
| `src/app/api/services/9router/_lib.ts`      | Hàm trợ giúp `getOrInitSupervisor()`                       |
| `src/app/api/services/[name]/logs/route.ts` | Điểm cuối nhật ký SSE dùng chung                           |
| `open-sse/executors/ninerouter.ts`          | Trình thực thi nhà cung cấp (Lớp 4)                        |

---

## 3. Máy trạng thái vòng đời

```
                    install()
  ┌─────────────┐ ──────────► ┌─────────────┐
  │ not_installed│             │   stopped   │◄──────────────────┐
  └─────────────┘             └──────┬──────┘                   │
                                     │ start()                   │
                                     ▼                           │ stop()
                               ┌──────────┐                      │
                               │ starting │                      │
                               └────┬─────┘                     │
            kiểm tra sức khỏe đạt   │         sự cố / SIGTERM   │
                               ┌────▼─────┐  (thoát trong 5 giây)│
                               │ running  │──── sự cố ──────────►┤
                               └────┬─────┘                   ┌─▼────┐
                             stop() │                          │error │
                                    ▼                          └──────┘
                               ┌──────────┐
                               │ stopping │
                               └──────────┘
```

Các trạng thái được lưu trong bảng DB `version_manager` (cột `status`) và được phản chiếu
trong trạng thái trong bộ nhớ của `ServiceSupervisor`. Trạng thái trong bộ nhớ là nguồn có thẩm quyền
đối với tiến trình đang chạy; trạng thái DB là phương án dự phòng bền vững khi khởi động.

### Chuyển đổi trạng thái

| Từ              | Sự kiện                                   | Sang                   |
| --------------- | ----------------------------------------- | ---------------------- |
| `not_installed` | `install()` thành công                    | `stopped`              |
| `stopped`       | `start()` được gọi                        | `starting`             |
| `starting`      | kiểm tra sức khỏe trả về 200              | `running`              |
| `starting`      | tiến trình thoát trước khi khỏe mạnh      | `error`                |
| `running`       | `stop()` được gọi                         | `stopping` → `stopped` |
| `running`       | tiến trình thoát ngoài dự kiến (< 5 giây) | `error` (sự cố nhanh)  |
| `running`       | tiến trình thoát ngoài dự kiến (> 5 giây) | `error`                |
| `error`         | `start()` được gọi                        | `starting`             |
| bất kỳ          | `stop()` khi đang `stopping`              | không thực hiện gì     |

### Khóa thao tác

`ServiceSupervisor` tuần tự hóa các thao tác vòng đời thông qua một khóa thao tác bất đồng bộ
(`withLock()`). Các lệnh gọi `start()` đồng thời trên cùng một supervisor chỉ tạo ra đúng
một tiến trình; bên gọi thứ hai chờ và trả về trạng thái hiện có. Điều này ngăn chặn
các tình trạng tranh chấp, chẳng hạn khi tính năng tự động khởi động và một nút trên giao diện người dùng được kích hoạt cùng lúc.

---

## 4. Tham chiếu API

Tất cả các route trong `/api/services/` đều là **LOCAL_ONLY** (chỉ loopback, quy tắc bắt buộc #17).
Các yêu cầu không phải loopback sẽ nhận `403 LOCAL_ONLY` bất kể token xác thực.

### 4.1 Các endpoint của 9Router (11 route)

#### `POST /api/services/9router/install`

Cài đặt 9Router từ npm. Tạo `DATA_DIR/services/9router/` với
`package.json` và `node_modules/` riêng. Không xung đột với các dependency của OmniRoute.

**Nội dung yêu cầu** (tất cả đều không bắt buộc):

```json
{ "version": "latest" }
```

| Trường    | Kiểu     | Mặc định   | Mô tả                                     |
| --------- | -------- | ---------- | ----------------------------------------- |
| `version` | `string` | `"latest"` | Tag phiên bản npm hoặc semver cần cài đặt |

**Phản hồi:**

| Trạng thái | Mô tả                                                    |
| ---------- | -------------------------------------------------------- |
| `200`      | `{ ok: true, installedVersion: "x.y.z", path: "..." }`   |
| `400`      | Nội dung yêu cầu không hợp lệ (xác thực Zod thất bại)    |
| `409`      | Đang cài đặt (khóa đang được giữ)                        |
| `500`      | Cài đặt npm thất bại — xem `message` để biết lỗi dễ hiểu |

**Ghi chú:** Sử dụng `execFile('npm', [...])` — không dùng shell, không nội suy (quy tắc bắt buộc #13).
Các lỗi EACCES được hiển thị dưới dạng thông báo dễ hiểu.

---

#### `POST /api/services/9router/start`

Khởi động 9Router. Đăng ký supervisor nếu chưa được đăng ký, sau đó gọi
`supervisor.start()`. Có tính lũy đẳng khi dịch vụ đã chạy.

**Nội dung yêu cầu:** không có

**Phản hồi:**

| Trạng thái | Mô tả                                                 |
| ---------- | ----------------------------------------------------- |
| `200`      | Đối tượng `ServiceStatus` (xem schema bên dưới)       |
| `409`      | 9Router chưa được cài đặt (`status: "not_installed"`) |
| `503`      | Khởi động thất bại (lỗi tiến trình — xem `lastError`) |

**Schema ServiceStatus:**

```json
{
  "tool": "9router",
  "state": "running",
  "pid": 12345,
  "port": 20130,
  "health": "healthy",
  "startedAt": "2026-05-25T10:00:00.000Z",
  "lastError": null
}
```

---

#### `POST /api/services/9router/stop`

Dừng 9Router một cách an toàn. Gửi SIGTERM, chờ 15 giây, sau đó gửi SIGKILL nếu tiến trình vẫn còn hoạt động.
Có tính lũy đẳng khi dịch vụ đã dừng.

**Nội dung yêu cầu:** không có

**Phản hồi:**

| Trạng thái | Mô tả                               |
| ---------- | ----------------------------------- |
| `200`      | `ServiceStatus` (state: "stopped")  |
| `503`      | Dừng dịch vụ thất bại ngoài dự kiến |

---

#### `POST /api/services/9router/restart`

Tương đương với `stop()` rồi `start()` trong phạm vi khóa thao tác.

**Nội dung yêu cầu:** không có

**Phản hồi:** giống như `start` (trả về `ServiceStatus` cuối cùng).

---

#### `POST /api/services/9router/update`

Cập nhật 9Router lên phiên bản npm mới hơn. Nếu dịch vụ đang chạy, dịch vụ sẽ được dừng
trước, tiến hành cài đặt npm (cài đặt phiên bản mới hơn tại chỗ), sau đó
dịch vụ được khởi động lại.

**Nội dung yêu cầu** (tất cả đều không bắt buộc):

```json
{ "version": "latest" }
```

**Phản hồi:**

| Trạng thái | Mô tả                                                           |
| ---------- | --------------------------------------------------------------- |
| `200`      | `{ ok: true, previousVersion: "...", installedVersion: "..." }` |
| `400`      | Nội dung không hợp lệ                                           |
| `500`      | Cập nhật npm thất bại                                           |

---

#### `POST /api/services/9router/rotate-key`

Tạo khóa API mới cho 9Router, mã hóa khóa khi lưu trữ và khởi động lại dịch vụ
(nếu đang chạy) để dịch vụ nhận khóa mới từ môi trường. Khóa cũ
bị vô hiệu hóa ngay lập tức.

**Nội dung yêu cầu:** không có

**Phản hồi:**

| Trạng thái | Mô tả                                      |
| ---------- | ------------------------------------------ |
| `200`      | `{ keyRotated: true, restarted: boolean }` |
| `500`      | Xoay vòng khóa thất bại                    |

**Bảo mật:** Khóa mới không bao giờ được trả về trong phản hồi (không làm rò rỉ thông tin xác thực).
Khóa được lưu trữ dưới dạng mã hóa (AES-256-GCM) trong bảng `version_manager`.

---

#### `GET /api/services/9router/status`

Trả về trạng thái trực tiếp + trạng thái DB kết hợp, bao gồm siêu dữ liệu phiên bản và bản xem trước khóa API.

**Phản hồi:**

| Trạng thái | Mô tả                   |
| ---------- | ----------------------- |
| `200`      | Xem schema bên dưới     |
| `500`      | Đọc trạng thái thất bại |

**Schema phản hồi:**

```json
{
  "tool": "9router",
  "state": "running",
  "pid": 12345,
  "port": 20130,
  "health": "healthy",
  "startedAt": "2026-05-25T10:00:00.000Z",
  "lastError": null,
  "installedVersion": "1.2.3",
  "latestVersion": "1.2.4",
  "updateAvailable": true,
  "apiKeyMasked": "nr_****abcd",
  "autoStart": false,
  "providerExpose": false
}
```

---

#### `POST /api/services/9router/auto-start`

Bật/tắt cờ tự động khởi động. Khi `enabled: true`, dịch vụ sẽ tự động khởi động
vào lần tiếp theo OmniRoute khởi chạy (nếu dịch vụ đã được cài đặt).

**Nội dung yêu cầu:**

```json
{ "enabled": true }
```

**Phản hồi:**

| Trạng thái | Mô tả                 |
| ---------- | --------------------- |
| `200`      | `{ autoStart: true }` |
| `400`      | Nội dung không hợp lệ |

---

#### `GET /api/services/9router/logs`

Luồng SSE chứa log trực tiếp từ bộ đệm vòng stdout/stderr của 9Router.

**Tham số truy vấn:**

| Tham số  | Kiểu      | Mặc định | Mô tả                                                                                    |
| -------- | --------- | -------- | ---------------------------------------------------------------------------------------- |
| `tail`   | `integer` | 200      | Số dòng lịch sử được gửi trước tiên (tối đa 1000)                                        |
| `filter` | `string`  | không có | Bộ lọc chuỗi con không phân biệt chữ hoa/thường (không dùng regex — an toàn trước ReDoS) |

**Sự kiện SSE:**

| Sự kiện     | Dữ liệu     | Mô tả                       |
| ----------- | ----------- | --------------------------- |
| `snapshot`  | `LogLine[]` | Phần cuối lịch sử ban đầu   |
| `log`       | `LogLine`   | Dòng log trực tiếp          |
| `heartbeat` | `{}`        | Duy trì kết nối mỗi 15 giây |

**Schema LogLine:**

```json
{
  "ts": 1716633600000,
  "stream": "stdout",
  "line": "[9router] Listening on :20130"
}
```

**Phản hồi:**

| Trạng thái | Mô tả                                            |
| ---------- | ------------------------------------------------ |
| `200`      | `text/event-stream`                              |
| `400`      | Tham số `filter` quá dài (> 200 ký tự)           |
| `404`      | Không tìm thấy dịch vụ (supervisor chưa đăng ký) |

---

### 4.2 Các endpoint CLIProxyAPI (10 route)

CLIProxyAPI có cấu trúc endpoint giống 9Router nhưng không có `rotate-key`, đồng thời có thêm
`accounts`, `provider-expose` và `auto-restart-adopted`. Hiện tại, nó nhận một
khóa API data-plane chuyên dụng được chèn khi khởi tạo (`needsApiKey: true` trong
`bootstrap.ts`, được dùng để đồng bộ mô hình); `status` bao gồm ít trường hơn.

| Phương thức | Đường dẫn                           | Mô tả                                               |
| ----------- | ----------------------------------- | --------------------------------------------------- |
| `POST`      | `/api/services/cliproxy/install`    | Cài đặt CLIProxyAPI từ npm                          |
| `POST`      | `/api/services/cliproxy/start`      | Khởi động CLIProxyAPI                               |
| `POST`      | `/api/services/cliproxy/stop`       | Dừng CLIProxyAPI                                    |
| `POST`      | `/api/services/cliproxy/restart`    | Khởi động lại CLIProxyAPI                           |
| `POST`      | `/api/services/cliproxy/update`     | Cập nhật lên phiên bản mới hơn                      |
| `GET`       | `/api/services/cliproxy/status`     | Trạng thái trực tiếp + DB (không có `apiKeyMasked`) |
| `POST`      | `/api/services/cliproxy/auto-start` | Bật/tắt tự động khởi động                           |

Endpoint dùng chung `GET /api/services/{name}/logs` (xem §4.1) hoạt động cho cả
bốn dịch vụ bằng cách sử dụng phân đoạn động `[name]`.

---

### 4.3 Các endpoint Mux (8 route)

Mux có cấu trúc endpoint giống CLIProxyAPI — không có route `rotate-key` trong bề mặt
API (bearer token được tạo theo cùng cách với 9Router thông qua
`getOrCreateApiKey("mux")` và được chèn qua biến môi trường `MUX_SERVER_AUTH_TOKEN`, nhưng
hiện vẫn chưa có endpoint chuyên dụng để xoay vòng khóa). Mux chỉ được quản lý vòng đời: không giống
9Router, nó không có trình thực thi Layer 4 và không bao giờ được đăng ký làm nhà cung cấp định tuyến.

| Phương thức | Đường dẫn                      | Mô tả                              |
| ----------- | ------------------------------ | ---------------------------------- |
| `POST`      | `/api/services/mux/install`    | Cài đặt Mux từ npm (`npm i mux`)   |
| `POST`      | `/api/services/mux/start`      | Khởi động Mux (`mux server`)       |
| `POST`      | `/api/services/mux/stop`       | Dừng Mux                           |
| `POST`      | `/api/services/mux/restart`    | Khởi động lại Mux                  |
| `POST`      | `/api/services/mux/update`     | Cập nhật lên phiên bản npm mới hơn |
| `GET`       | `/api/services/mux/status`     | Trạng thái trực tiếp + DB          |
| `POST`      | `/api/services/mux/auto-start` | Bật/tắt tự động khởi động          |

---

### 4.4 Các endpoint Bifrost (8 route)

Bifrost là backend chuyển tiếp cổng AI viết bằng Go (`@maximhq/bifrost`). Nó sử dụng cùng
cấu trúc endpoint với CLIProxyAPI (không có `rotate-key` — Bifrost tự quản lý các khóa
nhà cung cấp trong `config.json` thuộc `-app-dir` của nó).

| Phương thức | Đường dẫn                          | Mô tả                                                                 |
| ----------- | ---------------------------------- | --------------------------------------------------------------------- |
| `POST`      | `/api/services/bifrost/install`    | Cài đặt Bifrost từ npm (`@maximhq/bifrost`)                           |
| `POST`      | `/api/services/bifrost/start`      | Khởi động Bifrost trên cổng 8080 (mặc định)                           |
| `POST`      | `/api/services/bifrost/stop`       | Dừng Bifrost                                                          |
| `POST`      | `/api/services/bifrost/restart`    | Khởi động lại Bifrost                                                 |
| `POST`      | `/api/services/bifrost/update`     | Cập nhật lên phiên bản mới hơn                                        |
| `GET`       | `/api/services/bifrost/status`     | Trạng thái trực tiếp + DB                                             |
| `POST`      | `/api/services/bifrost/auto-start` | Bật/tắt tự động khởi động                                             |
| `GET`       | `/api/services/bifrost/logs`       | Phần cuối log qua SSE (thông qua route động `[name]/logs` dùng chung) |

**Kết nối định tuyến:** Khi `BIFROST_BASE_URL` chưa được đặt và phiên bản Bifrost
do supervisor quản lý đang chạy, `getBifrostRoutingConfig()` (trong `routingBackend.ts`) tự động
sử dụng `http://127.0.0.1:{port}` làm URL cơ sở chuyển tiếp. Biến môi trường
`BIFROST_BASE_URL` được đặt rõ ràng luôn được ưu tiên.

---

### 4.5 Các endpoint Dario (12 route)

Có cùng cấu trúc vòng đời như các dịch vụ khác (`install`, `start`, `stop`, `restart`,
`update`, `status`, `auto-start`, `auto-restart-adopted`), cùng với một control plane OAuth
được bảo vệ bằng token trong `admin/`: `admin/accounts`, `admin/import-from-omniroute`,
`admin/login-start`, `admin/login-complete` (tất cả đều được bảo vệ bởi `DARIO_ADMIN_TOKEN`).

### 4.6 Các endpoint open-wa (7 route)

open-wa (`@open-wa/wa-automate`) điều khiển một phiên bản Chromium headless (thông qua
Puppeteer) để tự động hóa WhatsApp Web. Nó sử dụng cùng cấu trúc endpoint như Mux (hiện chưa có
route `rotate-key`). Nó chỉ được quản lý vòng đời — không phải mục tiêu định tuyến,
không có trình thực thi Layer 4/mục nhà cung cấp.

| Phương thức | Đường dẫn                         | Mô tả                                                                |
| ----------- | --------------------------------- | -------------------------------------------------------------------- |
| `POST`      | `/api/services/openwa/install`    | Cài đặt open-wa từ npm (`@open-wa/wa-automate`)                      |
| `POST`      | `/api/services/openwa/start`      | Khởi động open-wa trên cổng 8323 (mặc định)                          |
| `POST`      | `/api/services/openwa/stop`       | Dừng open-wa                                                         |
| `POST`      | `/api/services/openwa/restart`    | Khởi động lại open-wa                                                |
| `POST`      | `/api/services/openwa/update`     | Cập nhật lên phiên bản mới hơn                                       |
| `GET`       | `/api/services/openwa/status`     | Trạng thái trực tiếp + DB                                            |
| `POST`      | `/api/services/openwa/auto-start` | Bật/tắt tự động khởi động                                            |
| `GET`       | `/api/services/openwa/logs`       | Theo dõi log qua SSE (thông qua route động `[name]/logs` dùng chung) |

**Khóa API:** được chèn dưới dạng `WA_KEY` — cơ chế ghi đè biến môi trường chung có tiền tố `WA_*` của open-wa ánh xạ biến này tới tùy chọn CLI `--key`/`-k`
(`dist/cli/setup.js::envArgs()`, đã xác minh với package 4.76.0 được cài đặt).
Khóa được thêm tiền tố `ow_` khi được tạo bởi `generateServiceApiKey()`. open-wa
đọc lại khóa từ header HTTP `key`/`api_key` (không phải `Authorization:
Bearer`); `/api-docs*` được miễn kiểm tra một cách rõ ràng
(`setupAuthenticationLayer` trong `dist/cli/server.js`), vì vậy phép thăm dò tình trạng
không cần header xác thực.

**Ghép nối:** open-wa không chính thức và không liên kết với WhatsApp — số điện thoại
được kết nối có nguy cơ bị cấm do cơ chế phát hiện tự động hóa của chính WhatsApp.
Trong lần khởi động đầu tiên, mã QR ghép nối được in ra stdout và hiển thị thông qua
bảng Logs/luồng SSE hiện có — hiện chưa có endpoint chuyên dụng cho ảnh QR
trong tích hợp này.

---

### 4.7 Proxy ngược (nhúng bảng điều khiển 9Router)

Bảng điều khiển nhúng giao diện web 9Router vào iframe thông qua một proxy ngược nội bộ
tại:

```
GET|POST|... /dashboard/providers/services/9router/embed/[...path]
```

Proxy này:

- Chuyển tiếp yêu cầu tới `http://127.0.0.1:{port}/{path}` (chỉ loopback)
- Loại bỏ các header `cookie` và `authorization` gửi đến (không làm rò rỉ phiên OmniRoute)
- Chèn `Authorization: Bearer {apiKey}` để xác thực 9Router
- Loại bỏ `set-cookie`, `content-security-policy`, `x-frame-options`, `cross-origin-*` khỏi phản hồi
- Ghi lại các phản hồi HTML để chèn `<base href>` và chuẩn hóa các đường dẫn tuyệt đối (`/foo` → `/dashboard/.../embed/foo`)

Các yêu cầu nâng cấp WebSocket cho bảng điều khiển được nhúng được xử lý bởi một máy chủ đồng hành trên
một cổng chuyên dụng (xem `src/lib/services/embedWsProxy.ts`).

**Bảo mật:** Các route proxy nhúng được phân loại trong `LOCAL_ONLY_API_PREFIXES`
và chỉ có thể được truy cập từ loopback. Kẻ tấn công lấy được JWT thông qua
đường hầm Cloudflare/Ngrok không thể dùng proxy để truy cập vào các dịch vụ được nhúng.

---

## 5. Bảo mật

### Thực thi LOCAL_ONLY (quy tắc bắt buộc #17)

Tất cả các route thuộc `/api/services/` và `/dashboard/providers/services/*/embed/` được
phân loại là LOCAL_ONLY trong `src/server/authz/routeGuard.ts`. Việc kiểm tra loopback
luôn được thực hiện trước bất kỳ nhánh xác thực nào:

```
yêu cầu được gửi đến
  → isLocalOnlyPath(path)?
      → không phải loopback → 403 LOCAL_ONLY (luôn luôn, trước bước kiểm tra xác thực)
      → loopback           → tiếp tục quy trình xác thực thông thường
```

Điều này ngăn một JWT bị rò rỉ (ví dụ: qua tunnel) kích hoạt `npm install` hoặc
khởi chạy tiến trình. Xem `docs/security/ROUTE_GUARD_TIERS.md` để biết ma trận phân
tầng đầy đủ.

### Chèn API key

9Router và Mux yêu cầu API key/bearer token cho các endpoint HTTP riêng của chúng.
OmniRoute:

1. Tạo một khóa qua `crypto.randomBytes(32).toString("base64url")` với tiền tố
   dành riêng cho từng dịch vụ (`nr_` cho 9Router, `mx_` cho Mux).
2. Mã hóa khóa khi lưu trữ bằng AES-256-GCM (cùng loại mã hóa được dùng cho thông tin xác thực của provider).
3. Giải mã và chèn khóa dưới dạng biến môi trường khi khởi chạy tiến trình —
   `NINEROUTER_API_KEY` cho 9Router, `MUX_SERVER_AUTH_TOKEN` cho Mux (không bao giờ dùng
   cờ CLI, vì vậy token không bao giờ xuất hiện trong `ps`/danh sách tiến trình).
4. Không bao giờ trả về khóa dạng văn bản thuần trong bất kỳ phản hồi HTTP nào.

CLIProxyAPI nhận một khóa data-plane chuyên dụng được chèn khi khởi chạy tiến trình
(`needsApiKey: true` — được dùng để đồng bộ mô hình với adapter).

### Phòng chống SSRF

Reverse HTTP proxy (`/dashboard/.../embed/[...path]`) được mã hóa cứng để chỉ chuyển tiếp
đến `http://127.0.0.1:{port}`. Nó không bao giờ đi theo các chuyển hướng đến đích
không phải loopback. Thư viện `ssrf-req-filter` được dùng để từ chối mọi URL upstream
được phân giải ra ngoài phạm vi loopback.

### An toàn shell (quy tắc bắt buộc #13)

`npm install` được gọi qua `execFile('npm', ['install', pkg, '--prefix', dir])` —
không có template literal, không có shell và không nội suy đường dẫn bên ngoài vào chuỗi
lệnh. Các giá trị runtime (cổng, API key) được truyền qua đối tượng `env` của tiến trình con.

### Làm sạch lỗi (quy tắc bắt buộc #12)

Tất cả phản hồi lỗi từ `/api/services/*` đều đi qua `buildErrorBody()` hoặc
`sanitizeErrorMessage()`. `err.stack` và `err.message` thô không bao giờ được trả về
nguyên văn cho bên gọi.

---

## 6. Thêm một dịch vụ nhúng mới

Hãy làm theo 8 bước sau. Đọc các phần triển khai hiện có trong `src/lib/services/installers/`
và `src/app/api/services/` để làm tài liệu tham chiếu chuẩn.

### Bước 1 — Tạo trình cài đặt

Tạo `src/lib/services/installers/{name}.ts` theo mẫu `ninerouter.ts`:

```typescript
export const NAME_PACKAGE = "your-npm-package";
export const NAME_DEFAULT_PORT = 20132; // chọn một cổng còn trống

export async function install(version = "latest"): Promise<InstallResult> { ... }
export async function update(version = "latest"): Promise<InstallResult> { ... }
export async function uninstall(): Promise<void> { ... }
export function resolveSpawnArgs(apiKey: string, port: number): SpawnArgs { ... }
export async function getInstalledVersion(): Promise<string | null> { ... }
export async function getLatestVersion(): Promise<string | null> { ... }
```

Sử dụng `runNpm(['install', NAME_PACKAGE, '--prefix', dir])` từ `installers/utils.ts`
— không bao giờ dùng `execSync` hoặc nội suy shell.

### Bước 2 — Đăng ký trong bootstrap

Thêm một `ServiceEntry` vào mảng `SERVICES` trong `src/lib/services/bootstrap.ts`:

```typescript
{
  tool: "myservice",
  port: NAME_DEFAULT_PORT,
  healthPath: "/health",
  healthIntervalMs: 5_000,
  stopTimeoutMs: 15_000,
  logsBufferBytes: 5_242_880,
  needsApiKey: true, // false nếu không cần API key
}
```

Mở rộng `buildSpawnArgsFactory()` để xử lý `cfg.tool === "myservice"`.

#### Hợp đồng provider-plugin có thể cắm thêm (Giai đoạn 1, #7333)

`src/lib/services/providerPlugins/` giới thiệu hợp đồng `ServiceProviderPlugin`, đóng gói
các trường `ServiceEntry` trong `bootstrap.ts` của một backend và các trường mẫu manifest
trong `serviceBackends.ts` vào một đối tượng, thay vì biểu diễn cấu trúc của cùng một
backend riêng biệt trong hai tệp không liên quan. Tại thời điểm viết tài liệu này, **chỉ
`9router` đã được di chuyển** — `bootstrap.ts` tạo mục nhập `SERVICES[]` của nó từ
`getServiceProviderPlugin("9router")` (`src/lib/services/providerPlugins/registry.ts`),
đồng thời ném lỗi khởi động nếu plugin bị thiếu. `cliproxy`, `mux` và `bifrost` vẫn giữ
nguyên các literal `SERVICES[]` nội tuyến đã tồn tại từ trước.

`open-sse/config/providerPluginManifest.ts` cũng được bổ sung helper
`createServiceBackendManifestEntry(pluginId, template)`, dùng để tạo một
`ProviderPluginManifestEntry` hợp lệ từ một mục `SERVICE_BACKEND_MANIFEST_TEMPLATE` —
helper này **chưa** được nối vào bất kỳ luồng yêu cầu đang hoạt động nào (cả
`generateProviderPluginManifestFromRegistry()` lẫn `/v1/providers/[provider]/models`);
đây vẫn là công việc tiếp theo sau khi hợp đồng được chứng minh với backend thứ hai.

Các công việc được hoãn sang những PR tiếp theo và được theo dõi trong issue #7333 gồm:
di chuyển `cliproxyapi` qua cùng registry, tổng quát hóa `mux`/`bifrost` vào union
`ServiceBackendPluginId`, tích hợp logic xử lý đặc biệt cho định tuyến executor
(`open-sse/executors/index.ts`, `open-sse/handlers/chatCore/executorProxy.ts`) vào hợp
đồng plugin, và nối `createServiceBackendManifestEntry()` vào một luồng mã
manifest/models đang hoạt động.

### Bước 3 — Thêm migration và dữ liệu khởi tạo DB

Đảm bảo dịch vụ có một hàng trong `version_manager` thông qua một migration trong
`src/lib/db/migrations/`. Hàng đó phải có nội dung:

```sql
INSERT OR IGNORE INTO version_manager (tool, status, auto_start, provider_expose)
VALUES ('myservice', 'not_installed', 0, 0);
```

### Bước 4 — Tạo 7 endpoint API

Trong `src/app/api/services/{name}/`:

```
_lib.ts            helper getOrInitSupervisor()
install/route.ts   POST — gọi installer.install()
start/route.ts     POST — gọi supervisor.start()
stop/route.ts      POST — gọi supervisor.stop()
restart/route.ts   POST — gọi supervisor.restart()
update/route.ts    POST — gọi installer.update()
status/route.ts    GET  — hợp nhất trạng thái trực tiếp + DB
auto-start/route.ts POST — bật/tắt cờ auto_start
```

Route dùng chung `GET /api/services/[name]/logs` đã được kết nối sẵn — không cần
thay đổi tại đó.

Ủy quyền tất cả phản hồi lỗi thông qua `createErrorResponse()` / `buildErrorBody()`.

### Bước 5 — Thêm vào LOCAL_ONLY_API_PREFIXES

Trong `src/server/authz/routeGuard.ts`, hãy xác minh rằng `/api/services/` đã được liệt kê.
Nếu bạn đưa vào một tiền tố mới (ví dụ: `/api/tools/`), hãy thêm tiền tố đó vào cả
`LOCAL_ONLY_API_PREFIXES` và, nếu nó khởi tạo tiến trình, vào `SPAWN_CAPABLE_PREFIXES`.
Thêm một bài kiểm thử trong `tests/unit/authz/routeGuard.test.ts`.

### Bước 6 — Thêm tab giao diện người dùng

Tạo `src/app/(dashboard)/dashboard/providers/services/tabs/{Name}ServiceTab.tsx`.
Tái sử dụng các thành phần dùng chung:

- `ServiceStatusCard` — trạng thái trực tiếp + huy hiệu tình trạng
- `ServiceLifecycleButtons` — Khởi động / Dừng / Khởi động lại / Cập nhật
- `ServiceLogsPanel` — phần cuối nhật ký SSE (kết nối tới `/api/services/{name}/logs`)
- `ApiKeyCard` — hiển thị + xoay vòng khóa (nếu `needsApiKey: true`)

Đăng ký tab trong `ServicesPageShell.tsx`.

### Bước 7 — Thêm mục nhà cung cấp (nếu dịch vụ là một đích định tuyến)

Nếu dịch vụ nhúng cung cấp endpoint `/v1/chat/completions` tương thích với OpenAI:

1. Thêm một mục nhà cung cấp vào `src/shared/constants/providers.ts` với `isEmbeddedService: true`.
2. Tạo `open-sse/executors/{name}.ts` mở rộng `BaseExecutor`. Tra cứu lại cổng và
   khóa API cho mỗi yêu cầu (không bao giờ lưu vào bộ nhớ đệm trong hàm khởi tạo). Trả về phản hồi
   `503 service_not_running` khi trạng thái của trình giám sát không phải là `"running"`.
3. Đăng ký các mô hình trong `open-sse/config/providerRegistry.ts` với tiền tố dịch vụ
   (ví dụ: `myservice/sub/model`). `modelSync.ts` sẽ duy trì việc cập nhật chúng.

### Bước 8 — Viết tài liệu và kiểm thử

1. Cập nhật `docs/frameworks/EMBEDDED-SERVICES.md` (tệp này) — thêm dịch vụ vào
   bảng trong §1 và mọi endpoint mới vào §4.
2. Thêm các bài kiểm thử đơn vị trong `tests/unit/services/` (vòng đời, trình cài đặt, cấu trúc API).
3. Thêm bài kiểm thử tích hợp trong `tests/integration/services/` (được kiểm soát bởi `RUN_SERVICES_INT=1`).
4. Cập nhật `docs/openapi.yaml` với các endpoint mới.

---

## 7. Khắc phục sự cố

### Dịch vụ không khởi động

**Triệu chứng:** Nút khởi động trả về 503, trạng thái vẫn là `"error"` hoặc `"starting"`.

**Danh sách kiểm tra:**

1. Kiểm tra `GET /api/services/{name}/logs` (hoặc bảng Logs trong dashboard). Tìm
   các dòng như `Error: ENOENT`, `address already in use` hoặc `Cannot find module`.
2. Xác minh `npm` có trong PATH: chạy `which npm` từ cùng tài khoản người dùng đang chạy OmniRoute.
3. Xác minh dịch vụ đã được cài đặt: kiểm tra `GET /api/services/{name}/status` để tìm
   `installedVersion`. Nếu là `null`, hãy chạy cài đặt trước.
4. Kiểm tra `DATA_DIR/services/{name}/node_modules/` có tồn tại và không trống.
5. Kiểm tra trường `lastError` trong phản hồi trạng thái để biết nguyên nhân thoát đã được lược bỏ thông tin nhạy cảm.

---

### Khởi động nguội chậm (> 10 giây để đạt trạng thái `running`)

**Triệu chứng:** Trạng thái duy trì ở `"starting"` trong thời gian dài trước khi chuyển sang `"running"` hoặc `"error"`.

**Giải thích:** Quá trình khởi động nguội của 9Router bao gồm việc nhập các cây phần phụ thuộc lớn (các mô-đun DNS,
đường hầm, MITM). Khoảng thời gian kiểm tra tình trạng mặc định là 2 giây với 3 lần thử trước khi
trình giám sát tuyên bố hết thời gian chờ (nhưng vẫn tiếp tục thăm dò).

**Cách khắc phục:** `healthIntervalMs` và thời gian chờ của `waitForHealthy`
(`healthIntervalMs * 3`) có thể được cấu hình trong `bootstrap.ts`. Đối với các dịch vụ có thời gian
khởi động lâu hơn, hãy tăng `healthIntervalMs` lên 5000 và `stopTimeoutMs` lên 30 000.

---

### Xung đột cổng (`EADDRINUSE`)

**Triệu chứng:** Nhật ký hiển thị `address already in use :::20130`.

**Nguyên nhân:**

- Một tiến trình khác đang sử dụng cổng 20130.
- Một tiến trình 9Router trước đó chưa được dừng hoàn toàn (PID zombie).

**Cách khắc phục:**

1. Thay đổi cổng mặc định thông qua biến môi trường `NINEROUTER_PORT` trong `.env`.
2. Tìm và kết thúc tiến trình gây xung đột: `lsof -ti :20130 | xargs kill -9`.
3. Có thể cấu hình cổng cho từng dịch vụ trong `bootstrap.ts` thông qua trường `port`.

**Lưu ý:** 9Router mặc định sử dụng cổng 20130 nhằm tránh xung đột với
cổng mặc định 20128 của OmniRoute.

---

### Quyền bị từ chối (EACCES) khi cài đặt

**Triệu chứng:** Quá trình cài đặt trả về 500, nhật ký hiển thị `EACCES` hoặc `permission denied`.

**Nguyên nhân:**

- Tiến trình OmniRoute không có quyền ghi vào `DATA_DIR` hoặc thư mục cha của nó.
- Chạy bên trong Docker rootless mà không có quyền ghi vào volume được ánh xạ.

**Cách khắc phục:**

1. Kiểm tra `DATA_DIR` (mặc định: `~/.omniroute/`): `ls -la ~/.omniroute/`
2. Đảm bảo người dùng chạy tiến trình OmniRoute sở hữu thư mục: `chown -R $USER ~/.omniroute/`
3. Trong Docker, đảm bảo volume được gắn có quyền phù hợp cho người dùng trong container.

---

### Cập nhật thất bại (hết thời gian chờ `npm install` hoặc lỗi mạng)

**Triệu chứng:** Quá trình cập nhật trả về 500 với `InstallError`, nhật ký hiển thị lỗi hết thời gian chờ mạng.

**Danh sách kiểm tra:**

1. Xác nhận có thể truy cập npm registry: `npm ping`.
2. Kiểm tra proxy doanh nghiệp: `npm config get proxy`, `npm config get https-proxy`.
3. Thử cài đặt thủ công: `npm install {package}@latest --prefix ~/.omniroute/services/{name}/`.
4. Nếu ở sau mạng cách ly, hãy tải trước tarball và sử dụng `npm install /path/to/tarball.tgz`.

---

### Dịch vụ hiển thị trạng thái `"error"` ngay sau khi khởi động (sập nhanh)

**Triệu chứng:** Trạng thái chuyển từ `"starting"` sang `"error"` trong chưa đầy 5 giây.
`lastError` hiển thị `"Fast crash (exited with code 1)"`.

**Danh sách kiểm tra:**

1. Đọc toàn bộ phần cuối của nhật ký: `GET /api/services/{name}/logs?tail=500`.
2. Nguyên nhân phổ biến: thiếu các biến môi trường mà dịch vụ yêu cầu.
3. Đối với 9Router: xác minh `NINEROUTER_DISABLE_MITM=true` và
   `NINEROUTER_DISABLE_TUNNEL=true` có trong môi trường được truyền khi tạo tiến trình (xem
   `installers/ninerouter.ts` `resolveSpawnArgs`).

---

## 8. Câu hỏi thường gặp

**H: Tôi có thể cho phép các máy khách không thuộc loopback truy cập các endpoint của dịch vụ nhúng không?**

Không. Tầng LOCAL_ONLY là chủ đích thiết kế (quy tắc cứng #17). Các route có thể gọi
`npm install` hoặc sinh tiến trình `node` không được phép tiếp nhận lưu lượng không
thuộc loopback, vì nếu JWT bị rò rỉ qua một tunnel (Cloudflare, Ngrok, Tailscale),
kẻ xấu có thể tùy ý sinh tiến trình. Không có ngoại lệ cho phép bỏ qua quy tắc này đối với
`/api/services/` — không giống `/api/mcp/`, route này không nằm trong danh sách bỏ qua
manage-scope. Xem `docs/security/ROUTE_GUARD_TIERS.md`.

---

**H: 9Router và CLIProxyAPI có khả dụng trong các bản triển khai production/cloud không?**

Có. Cả hai dịch vụ đều tuân theo mô hình ưu tiên cục bộ giống như chính OmniRoute. Chúng chạy
trên cùng một máy và giao tiếp qua loopback. "Production" ở đây có nghĩa là VPS
hoặc máy chủ cục bộ nơi OmniRoute được triển khai, chứ không phải một nhà cung cấp đám mây từ xa.

---

**H: Làm cách nào để gỡ lỗi supervisor?**

1. Theo dõi liên tục luồng nhật ký SSE: `curl -N http://localhost:20128/api/services/9router/logs`.
2. Kiểm tra nhật ký có cấu trúc trong đầu ra pino của OmniRoute, được lọc theo
   namespace `service:supervisor`.
3. Kiểm tra hàng trong DB: `sqlite3 ~/.omniroute/omniroute.db "SELECT * FROM version_manager WHERE tool='9router'"`.
4. Sử dụng `GET /api/services/9router/status` để xem trạng thái trực tiếp hiện tại, PID, tình trạng hoạt động
   và `lastError` trong một lần gọi.

---

**H: Supervisor hiển thị `health: "degraded"` hoặc `health: "unknown"` nhưng trạng thái là `"running"`. Đây có phải là vấn đề không?**

`"degraded"` có nghĩa là phép thăm dò tình trạng hoạt động trả về phản hồi không phải 200. `"unknown"` có nghĩa là chưa có
phép thăm dò nào hoàn tất (xảy ra tranh chấp thời điểm với lần thăm dò đầu tiên). Cả hai đều là trạng thái tạm thời trong quá trình khởi động.
Nếu tình trạng hoạt động vẫn là `"degraded"` sau hơn `healthIntervalMs * 3` ms kể từ khi
chuyển sang `"running"`, dịch vụ nhúng đang chạy nhưng API HTTP của nó không phản hồi. Hãy kiểm tra
xem cổng trong phản hồi trạng thái có chính xác không và dịch vụ có thực sự
lắng nghe trên cổng đó hay không.

---

**H: Tôi có thể thay đổi khóa API của 9Router mà không cần khởi động lại hoàn toàn không?**

Không. Khóa API được truyền cho 9Router qua một biến môi trường tại thời điểm sinh tiến trình.
Không thể thay đổi biến môi trường trong một tiến trình đang chạy. `POST .../rotate-key`
tự động dừng và khởi động lại dịch vụ để áp dụng khóa mới. Việc xoay vòng khóa
có hiệu lực trong khoảng thời gian bằng `stopTimeoutMs` của dịch vụ (mặc định 15 giây) cộng với thời gian
khởi động của dịch vụ.

---

**H: Giới hạn của bộ đệm vòng là bao nhiêu và điều gì xảy ra khi bộ đệm đầy?**

Mỗi dịch vụ có một bộ đệm vòng chuyên biệt dung lượng 5 MB. Khi bộ đệm đầy, các dòng
nhật ký cũ nhất sẽ bị loại bỏ để dành chỗ cho các dòng mới. Sự kiện SSE `snapshot` trả về
các dòng gần đây nhất trong giới hạn `tail`. Nhật ký không được lưu bền vững vào đĩa trừ khi
`logsBufferPath` được thiết lập trong hàng DB.

---

## Xem thêm

- `docs/security/ROUTE_GUARD_TIERS.md` — thông tin chi tiết về tầng LOCAL_ONLY
- `docs/architecture/CODEBASE_DOCUMENTATION.md` — §3.2 Ánh xạ mô-đun Dịch vụ nhúng
- `docs/architecture/ARCHITECTURE.md` — ngữ cảnh cấp hệ thống
- `docs/openapi.yaml` — định nghĩa endpoint có thể đọc bằng máy
- `CLAUDE.md` §"Adding a New Embedded Service" — danh sách kiểm tra tham khảo nhanh
