# Feature Flags (Tiếng Việt)

🌐 **Languages:** 🇺🇸 [English](../../../../reference/FEATURE_FLAGS.md) · 🇪🇹 [am](../../../am/docs/reference/FEATURE_FLAGS.md) · 🇸🇦 [ar](../../../ar/docs/reference/FEATURE_FLAGS.md) · 🇦🇿 [az](../../../az/docs/reference/FEATURE_FLAGS.md) · 🇧🇬 [bg](../../../bg/docs/reference/FEATURE_FLAGS.md) · 🇧🇩 [bn](../../../bn/docs/reference/FEATURE_FLAGS.md) · 🇨🇿 [cs](../../../cs/docs/reference/FEATURE_FLAGS.md) · 🇩🇰 [da](../../../da/docs/reference/FEATURE_FLAGS.md) · 🇩🇪 [de](../../../de/docs/reference/FEATURE_FLAGS.md) · 🇬🇷 [el](../../../el/docs/reference/FEATURE_FLAGS.md) · 🇪🇸 [es](../../../es/docs/reference/FEATURE_FLAGS.md) · 🇪🇪 [et](../../../et/docs/reference/FEATURE_FLAGS.md) · 🇮🇷 [fa](../../../fa/docs/reference/FEATURE_FLAGS.md) · 🇫🇮 [fi](../../../fi/docs/reference/FEATURE_FLAGS.md) · 🇫🇷 [fr](../../../fr/docs/reference/FEATURE_FLAGS.md) · 🇮🇪 [ga](../../../ga/docs/reference/FEATURE_FLAGS.md) · 🇮🇳 [gu](../../../gu/docs/reference/FEATURE_FLAGS.md) · 🇳🇬 [ha](../../../ha/docs/reference/FEATURE_FLAGS.md) · 🇮🇱 [he](../../../he/docs/reference/FEATURE_FLAGS.md) · 🇮🇳 [hi](../../../hi/docs/reference/FEATURE_FLAGS.md) · 🇭🇷 [hr](../../../hr/docs/reference/FEATURE_FLAGS.md) · 🇭🇺 [hu](../../../hu/docs/reference/FEATURE_FLAGS.md) · 🇦🇲 [hy](../../../hy/docs/reference/FEATURE_FLAGS.md) · 🇮🇩 [id](../../../id/docs/reference/FEATURE_FLAGS.md) · 🇳🇬 [ig](../../../ig/docs/reference/FEATURE_FLAGS.md) · 🇮🇹 [it](../../../it/docs/reference/FEATURE_FLAGS.md) · 🇯🇵 [ja](../../../ja/docs/reference/FEATURE_FLAGS.md) · 🇬🇪 [ka](../../../ka/docs/reference/FEATURE_FLAGS.md) · 🇰🇭 [km](../../../km/docs/reference/FEATURE_FLAGS.md) · 🇮🇳 [kn](../../../kn/docs/reference/FEATURE_FLAGS.md) · 🇰🇷 [ko](../../../ko/docs/reference/FEATURE_FLAGS.md) · 🇱🇹 [lt](../../../lt/docs/reference/FEATURE_FLAGS.md) · 🇱🇻 [lv](../../../lv/docs/reference/FEATURE_FLAGS.md) · 🇮🇳 [ml](../../../ml/docs/reference/FEATURE_FLAGS.md) · 🇮🇳 [mr](../../../mr/docs/reference/FEATURE_FLAGS.md) · 🇲🇾 [ms](../../../ms/docs/reference/FEATURE_FLAGS.md) · 🇲🇹 [mt](../../../mt/docs/reference/FEATURE_FLAGS.md) · 🇲🇲 [my](../../../my/docs/reference/FEATURE_FLAGS.md) · 🇳🇵 [ne](../../../ne/docs/reference/FEATURE_FLAGS.md) · 🇳🇱 [nl](../../../nl/docs/reference/FEATURE_FLAGS.md) · 🇳🇴 [no](../../../no/docs/reference/FEATURE_FLAGS.md) · 🇮🇳 [or](../../../or/docs/reference/FEATURE_FLAGS.md) · 🇮🇳 [pa](../../../pa/docs/reference/FEATURE_FLAGS.md) · 🇵🇭 [phi](../../../phi/docs/reference/FEATURE_FLAGS.md) · 🇵🇱 [pl](../../../pl/docs/reference/FEATURE_FLAGS.md) · 🇵🇹 [pt](../../../pt/docs/reference/FEATURE_FLAGS.md) · 🇧🇷 [pt-BR](../../../pt-BR/docs/reference/FEATURE_FLAGS.md) · 🇷🇴 [ro](../../../ro/docs/reference/FEATURE_FLAGS.md) · 🇷🇺 [ru](../../../ru/docs/reference/FEATURE_FLAGS.md) · 🇱🇰 [si](../../../si/docs/reference/FEATURE_FLAGS.md) · 🇸🇰 [sk](../../../sk/docs/reference/FEATURE_FLAGS.md) · 🇸🇮 [sl](../../../sl/docs/reference/FEATURE_FLAGS.md) · 🇷🇸 [sr](../../../sr/docs/reference/FEATURE_FLAGS.md) · 🇸🇪 [sv](../../../sv/docs/reference/FEATURE_FLAGS.md) · 🇰🇪 [sw](../../../sw/docs/reference/FEATURE_FLAGS.md) · 🇮🇳 [ta](../../../ta/docs/reference/FEATURE_FLAGS.md) · 🇮🇳 [te](../../../te/docs/reference/FEATURE_FLAGS.md) · 🇹🇭 [th](../../../th/docs/reference/FEATURE_FLAGS.md) · 🇹🇷 [tr](../../../tr/docs/reference/FEATURE_FLAGS.md) · 🇺🇦 [uk-UA](../../../uk-UA/docs/reference/FEATURE_FLAGS.md) · 🇵🇰 [ur](../../../ur/docs/reference/FEATURE_FLAGS.md) · 🇺🇿 [uz](../../../uz/docs/reference/FEATURE_FLAGS.md) · 🇳🇬 [yo](../../../yo/docs/reference/FEATURE_FLAGS.md) · 🇨🇳 [zh-CN](../../../zh-CN/docs/reference/FEATURE_FLAGS.md) · 🇹🇼 [zh-TW](../../../zh-TW/docs/reference/FEATURE_FLAGS.md)

---

> Các nút chuyển đổi thời gian chạy giúp thay đổi hành vi của OmniRoute **mà không cần triển khai lại**.
> Mọi cờ được liệt kê tại đây đều được định nghĩa trong
> [`src/shared/constants/featureFlagDefinitions.ts`](../../src/shared/constants/featureFlagDefinitions.ts)
> — nguồn thông tin chính xác duy nhất. Cả bảng điều khiển và REST API đều đọc từ
> tệp đó, vì vậy bảng bên dưới được tạo để khớp với tệp theo tỷ lệ 1:1.

---

## Cờ tính năng là gì

Cờ tính năng là một nút chuyển đổi có tên (boolean hoặc enum), có thể thay đổi giá trị
trong thời gian chạy và lưu vào cơ sở dữ liệu mà không cần triển khai lại tiến trình. Mỗi
cờ được mô tả bằng một `FeatureFlagDefinition` gồm `key`, `label`,
`description`, `category`, `defaultValue`, `type` và gợi ý `requiresRestart`.

### Thứ tự phân giải

**Giá trị hiệu lực** của một cờ được phân giải bởi
[`resolveFeatureFlag()`](../../src/shared/utils/featureFlags.ts) theo thứ tự
ưu tiên sau (mức cao nhất được áp dụng):

1. **Giá trị ghi đè trong DB** — một giá trị được lưu trong bảng `key_value` thuộc
   không gian tên `feature_flags` (được thiết lập qua bảng điều khiển hoặc REST API).
2. **Biến môi trường** — `process.env[<KEY>]`, nếu được thiết lập và không rỗng.
3. **Giá trị mặc định của định nghĩa** — `defaultValue` từ `featureFlagDefinitions.ts`.

Một cờ boolean được xem là **đã bật** khi giá trị hiệu lực của nó là `"true"`,
`"1"` hoặc `"yes"` (xem `isFeatureFlagEnabled()`).

> [!NOTE]
> Hầu hết các cờ cũng có một biến môi trường tương ứng **cùng tên**
> được ghi lại trong [`ENVIRONMENT.md`](./ENVIRONMENT.md). Giá trị ghi đè trong DB
> của cờ được ưu tiên hơn biến môi trường đó. Một cờ có
> `requiresRestart: true` sẽ được lưu ngay lập tức nhưng chỉ được đọc lại khi tiến trình
> khởi động — việc chuyển đổi cờ này sẽ hiển thị biểu ngữ **"Khởi động lại máy chủ"** trong bảng điều khiển.

---

## Danh mục cờ

75 cờ thuộc 6 danh mục. **Mặc định** là giá trị mặc định trong định nghĩa — giá trị
được sử dụng khi không có giá trị ghi đè trong DB cũng như biến môi trường.

### Bảo mật (10)

| Khóa                                    | Kiểu    | Mặc định | Mô tả                                                                                                                                                                                                                                                                                                        |
| --------------------------------------- | ------- | -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `REQUIRE_API_KEY`                       | boolean | `false`  | Yêu cầu khóa API cho tất cả yêu cầu đến.                                                                                                                                                                                                                                                                     |
| `INPUT_SANITIZER_ENABLED`               | boolean | `true`   | Bật tính năng làm sạch đầu vào cho tất cả yêu cầu.                                                                                                                                                                                                                                                           |
| `INJECTION_GUARD_MODE`                  | enum    | `off`    | Chế độ bảo vệ chống chèn prompt. Các giá trị: `off`, `warn`, `block`, `redact`.                                                                                                                                                                                                                              |
| `PII_REDACTION_ENABLED`                 | boolean | `false`  | Che thông tin nhận dạng cá nhân (PII) khỏi các yêu cầu (độc lập với `INPUT_SANITIZER_MODE`).                                                                                                                                                                                                                 |
| `PII_RESPONSE_SANITIZATION`             | boolean | `false`  | Làm sạch PII khỏi phản hồi của nhà cung cấp.                                                                                                                                                                                                                                                                 |
| `PII_RESPONSE_SANITIZATION_MODE`        | enum    | `redact` | Chế độ làm sạch PII trong phản hồi. Các giá trị: `redact`, `warn`, `block`, `off`.                                                                                                                                                                                                                           |
| `OUTBOUND_SSRF_GUARD_ENABLED`           | boolean | `true`   | Chặn các yêu cầu gửi đi đến những dải IP riêng tư/nội bộ.                                                                                                                                                                                                                                                    |
| `ALLOW_API_KEY_REVEAL`                  | boolean | `false`  | Cho phép người dùng bảng điều khiển đã xác thực hiển thị các khóa API được lưu trữ thay vì chỉ thấy các giá trị đã được che.                                                                                                                                                                                 |
| `AUTH_LOG_INCLUDE_ACCOUNT_ID`           | boolean | `false`  | Bao gồm tiền tố tài khoản trong các dòng nhật ký AUTH (ví dụ: "Đang sử dụng tài khoản <provider>: abc12345..."). Bị tắt theo mặc định để mã định danh tài khoản được che khỏi nhật ký tiến trình dùng chung/đa đối tượng thuê. Độc lập với Chế độ gỡ lỗi; việc bật Chế độ gỡ lỗi không làm lộ thông tin này. |
| `OMNIROUTE_OIDC_DISABLE_PASSWORD_LOGIN` | boolean | `false`  | Khi OIDC được bật, tắt đăng nhập bằng mật khẩu để người dùng chỉ có thể xác thực qua Đăng nhập một lần OIDC. Khi bị tắt (mặc định), cả đăng nhập bằng mật khẩu và OIDC đều khả dụng.                                                                                                                         |

### Mạng (17)

| Khóa                                            | Kiểu    | Mặc định | Khởi động lại | Mô tả                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| ----------------------------------------------- | ------- | -------- | ------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `ENABLE_TLS_FINGERPRINT`                        | boolean | `false`  | ✓             | Bật chế độ ẩn dấu vân tay TLS.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `AUDIO_REMOTE_PROVIDER_NODES`                   | boolean | `false`  |               | Cho phép các route /v1/audio/* sử dụng các nút nhà cung cấp tương thích với OpenAI được lưu trữ bên ngoài localhost. Mặc định tắt — việc định tuyến âm thanh đến máy chủ từ xa làm thay đổi danh tính đầu ra và phải là quyết định rõ ràng của người vận hành. Các nút loopback luôn được phép và không bị ảnh hưởng.                                                                                                                                                                                                                                   |
| `RERANK_REMOTE_PROVIDER_NODES`                  | boolean | `false`  |               | Cho phép POST /v1/rerank (và bước rerank loopback của công cụ bộ nhớ) sử dụng các nút nhà cung cấp tương thích với OpenAI được lưu trữ bên ngoài localhost. Mặc định tắt — việc định tuyến đến máy chủ từ xa làm thay đổi danh tính đầu ra và phải là quyết định rõ ràng của người vận hành. Các nút loopback luôn được phép; các nút từ xa cũng phải đáp ứng chính sách URL đi ra của nhà cung cấp.                                                                                                                                                    |
| `PROXY_AUTO_SELECT_ENABLED`                     | boolean | `false`  |               | Khi chưa có proxy nào được gán cho một kết nối, tự động chọn proxy hoạt động đầu tiên từ registry. Mặc định tắt (nếu không, bất kỳ proxy nào trong registry cũng trở thành phương án dự phòng toàn cục — #3332).                                                                                                                                                                                                                                                                                                                                        |
| `OMNIROUTE_CONTROL_PLANE_PROXY_DIRECT_FALLBACK` | boolean | `false`  |               | Cho phép các luồng xác thực OAuth và xác thực nhà cung cấp bỏ qua proxy được ghim và kết nối trực tiếp khi bước kiểm tra trước khả năng truy cập proxy thất bại. Mặc định tắt vì điều này có thể làm thay đổi IP đầu ra.                                                                                                                                                                                                                                                                                                                                |
| `NETWORK_ROTATION_SHARED_EGRESS_GUARD`          | boolean | `true`   |               | Khi xảy ra ngoại lệ mạng (hết thời gian chờ, kết nối bị từ chối/đặt lại) đối với bộ thực thi luân phiên nhiều tài khoản, nếu tài khoản gặp lỗi không có proxy riêng, áp dụng thời gian tạm nghỉ ngắn và bỏ qua các tài khoản khác không có proxy trong phần còn lại của yêu cầu thay vì thử lại từng tài khoản. Mặc định bật (an toàn: không thay đổi IP đầu ra, chỉ giảm độ trễ/nguy cơ tạm nghỉ đối với các tài khoản dùng chung đầu ra). Tắt để khôi phục việc lan truyền lỗi ngay lập tức ở lần phát sinh lỗi đầu tiên từ tài khoản không có proxy. |
| `PROXY_SKIP_RECENTLY_FAILED`                    | boolean | `false`  |               | Các nhóm proxy và cơ chế luân phiên theo tài khoản của opencode ngừng phân phối lại một proxy vừa gặp lỗi (kiểm tra TCP bị từ chối hoặc nhận được mã 429 thông qua proxy đó) trong một khoảng thời gian riêng cho mỗi tiến trình, khoảng thời gian này tăng gấp đôi sau mỗi lần lỗi lặp lại cho đến một giới hạn. Không ghi trạng thái proxy; khi mọi ứng viên đều bị tạm gác, lựa chọn không thay đổi. Mặc định tắt.                                                                                                                                   |
| `PROXY_POOL_EGRESS_OBSERVATION`                 | boolean | `false`  |               | Hiển thị bên dưới một nhóm proxy trong bảng điều khiển số lượng IP đầu ra quan sát được đã phục vụ các thành viên của nhóm trong 24 giờ qua và số lượng kết nối đã sử dụng chúng. Chỉ đọc, được tính toán từ nhật ký proxy và không bao giờ được dùng để định tuyến. Mặc định tắt.                                                                                                                                                                                                                                                                      |
| `OPENCODE_RESPONSES_STALL_ROTATION`             | boolean | `false`  |               | Đối với bộ thực thi OpenCode, theo dõi byte đầu tiên của phần thân trong phản hồi Responses được truyền phát (khoảng chờ: `RESPONSES_FIRST_BYTE_TIMEOUT_MS`, mặc định `15000`). Luồng Responses 2xx vẫn im lặng sau khoảng chờ được coi là bị đình trệ: tài khoản được tạm nghỉ và yêu cầu được luân chuyển sang tài khoản tiếp theo một lần; lần đình trệ thứ hai sẽ thất bại ngay. Mặc định tắt: các luồng bị đình trệ tiếp tục chờ như hiện tại cho đến khi hết thời gian chờ sẵn sàng của luồng.                                                    |
| `OPENCODE_USER_BLOCKED_ROTATION`                | boolean | `false`  |               | Trình thực thi OpenCode: khi gặp phản hồi 403/451 chứa từ chối `user_blocked` (không phải do vị trí địa lý, cũng không phải từ chối dấu vân tay Cloudflare), tạm ngưng tài khoản bị từ chối và chuyển sang tài khoản tiếp theo tối đa một lần cho mỗi yêu cầu; lần từ chối thứ hai được trả về nguyên trạng, không đánh dấu thành công. Mặc định tắt: định tuyến vòng qua lệnh chặn người dùng từ thượng nguồn có thể bị xem là hành vi né tránh và làm cờ cảnh báo lan rộng trên toàn bộ nhóm tài khoản.                                               |
| `OPENCODE_TRANSIENT_FAILOVER_BACKOFF`           | boolean | `false`  |               | Luân chuyển OpenCode: sau hai lỗi thượng nguồn tạm thời liên tiếp (5xx hoặc 400 với nội dung trống), tạm dừng trước khi chuyển sang tài khoản tiếp theo — bắt đầu từ 1,5 giây và tăng gấp đôi sau mỗi lỗi tiếp theo, giới hạn tối đa 6 giây cho mỗi lần tạm dừng và 10 giây cho mỗi yêu cầu, bỏ qua khi máy khách ngắt kết nối; phần nội dung phản hồi lỗi được giải phóng trước khi chờ. Mặc định tắt: chuyển đổi dự phòng vẫn diễn ra ngay lập tức.                                                                                                   |
| `OPENCODE_PARK_AND_RESUME`                      | boolean | `false`  |               | Luân chuyển OpenCode: tạm giữ yêu cầu sau nhiều phản hồi 429 tạm thời lặp lại (hoặc khi có dấu hiệu mới cho thấy nhóm tài khoản đang chịu tải cao) bằng tín hiệu duy trì kết nối, sau đó phát lại một lượt có giới hạn qua tối đa 3 tài khoản tuần tự thay vì phân tán trên toàn bộ nhóm tài khoản. Mặc định tắt: mỗi phản hồi 429 sẽ chuyển sang tài khoản tiếp theo đúng như trước đây.                                                                                                                                                               |
| `OPENCODE_RATE_LIMITED_429_EARLY_STOP`          | boolean | `false`  |               | Luân chuyển OpenCode: dừng lượt tài khoản tại phản hồi 429 đầu tiên được phân loại là giới hạn tốc độ thực sự (`Retry-After` có thể phân tích được hoặc nội dung phản hồi đề cập đến giới hạn tốc độ/mức sử dụng) và trả về nguyên trạng phản hồi 429 từ thượng nguồn đó. Các phản hồi 429 chưa được phân loại vẫn tiếp tục luân chuyển. Mặc định tắt: gói miễn phí bị giới hạn theo từng IP đầu ra (#9611), vì vậy mọi phản hồi 429 đều dẫn đến luân chuyển và khi đã thử hết một lượt, phản hồi 429 cuối cùng từ thượng nguồn sẽ được trả về.         |
| `MITM_DISABLE_TLS_VERIFY`                       | boolean | `false`  | ✓             | Tắt xác minh chứng chỉ TLS cho proxy MITM. **Nguy hiểm.**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `OMNIROUTE_ALLOW_PRIVATE_PROVIDER_URLS`         | boolean | `false`  |               | Cho phép URL của nhà cung cấp trỏ đến các mạng riêng/nội bộ.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `OMNIROUTE_ALLOW_LOCAL_PROVIDER_URLS`           | boolean | `true`   |               | Cho phép thêm/xác thực nhà cung cấp trên các địa chỉ cục bộ/riêng tư (127.0.0.1, localhost, LAN). Mặc định bật (ưu tiên cục bộ); tắt để chỉ cho phép địa chỉ công khai một cách nghiêm ngặt. Siêu dữ liệu đám mây vẫn bị chặn.                                                                                                                                                                                                                                                                                                                          |
| `ENABLE_CC_COMPATIBLE_PROVIDER`                 | boolean | `false`  | ✓             | Bật chế độ nhà cung cấp tương thích với Claude Code.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |

### Chính sách (5)

| Khóa                            | Kiểu    | Mặc định   | Mô tả                                                                                                                                                                                                                                               |
| ------------------------------- | ------- | ---------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `TOOL_POLICY_MODE`              | enum    | `disabled` | Chế độ thực thi chính sách sử dụng công cụ. Các giá trị: `disabled`, `warn`, `block`.                                                                                                                                                               |
| `RATE_LIMIT_AUTO_ENABLE`        | boolean | `false`    | Tự động bật giới hạn tốc độ dựa trên các mẫu sử dụng.                                                                                                                                                                                               |
| `DISABLE_CONTEXT_WINDOW_CHECKS` | boolean | `false`    | Bỏ qua bước kiểm tra cục bộ của OmniRoute đối với cửa sổ ngữ cảnh / số lượng token đầu vào tối đa cho các yêu cầu trực tiếp đến một mô hình duy nhất. Các giới hạn từ nhà cung cấp thượng nguồn vẫn được áp dụng.                                   |
| `CAPABILITY_FILTER_ENABLED`     | boolean | `false`    | Từ chối yêu cầu trước khi chuyển tiếp nếu mô hình đích thiếu các khả năng bắt buộc (thị giác, công cụ, đầu ra có cấu trúc, cửa sổ ngữ cảnh). Bảo vệ các yêu cầu trực tiếp đến một nhà cung cấp duy nhất vốn bỏ qua bộ lọc tương thích ở lớp tổ hợp. |
| `RADAR_ENABLED`                 | boolean | `false`    | Bật mô-đun OmniRoute Radar (các màn hình nguồn cấp danh mục và đồng bộ hóa). Mặc định tắt; việc bật chỉ mở khóa giao diện người dùng — đồng bộ hóa dữ liệu vẫn là một tùy chọn tham gia riêng biệt.                                                 |

### Thời gian chạy (33)

| Khóa                                        | Kiểu    | Mặc định | Khởi động lại | Mô tả                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| ------------------------------------------- | ------- | -------- | ------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `UNIVERSAL_CONTEXT_HANDOFF_ENABLED`         | boolean | `true`   |               | Tạo và chèn bản tóm tắt cuộc hội thoại khi định tuyến combo chuyển đổi mô hình. Tắt tùy chọn này để xử lý các lần chuyển đổi mô hình một cách độc lập và ngăn các yêu cầu chuyển giao chạy nền cho tất cả combo hiện có và trong tương lai.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `RESPONSES_PASSTHROUGH_DROP_COMMENTARY`     | boolean | `true`   |               | Loại bỏ các mục đầu ra nội bộ ở giai đoạn bình luận khỏi luồng chuyển tiếp của Responses API trước khi chuyển tiếp đến máy khách. Tắt tùy chọn này để nhận nội dung bình luận thô từ thượng nguồn.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `OMNIROUTE_MCP_ENFORCE_SCOPES`              | boolean | `true`   |               | Thực thi các giới hạn phạm vi đối với quyền truy cập công cụ MCP.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `OMNIROUTE_MCP_COMPRESS_DESCRIPTIONS`       | boolean | `false`  |               | Nén phần mô tả công cụ MCP để giảm mức sử dụng token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `OMNIROUTE_ENABLE_RUNTIME_BACKGROUND_TASKS` | boolean | `false`  |               | Bật xử lý tác vụ nền trong thời gian chạy.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `OMNIROUTE_DISABLE_BACKGROUND_SERVICES`     | boolean | `false`  | ✓             | Tắt tất cả dịch vụ nền (làm mới hạn ngạch, đồng bộ hóa, v.v.).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `OMNIROUTE_RTK_TRUST_PROJECT_FILTERS`       | boolean | `false`  |               | Tin cậy các bộ lọc RTK cấp dự án mà không cần xác thực.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `OMNIROUTE_ENABLE_LIVE_WS`                  | boolean | `true`   | ✓             | Khởi động máy chủ WebSocket của bảng điều khiển thời gian thực khi nhập (cổng mặc định là 20132).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `OMNIROUTE_CODEX_WS_ENABLED`                | boolean | `true`   |               | Cho phép Codex sử dụng phương thức truyền Responses-over-WebSocket. Khi tắt, Codex sẽ chuyển về HTTP Responses.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `OMNIROUTE_CODEX_APP_SERVER_ENABLED`        | boolean | `true`   |               | Cho phép Codex sử dụng phương thức truyền JSON-RPC qua WebSocket của app-server cục bộ (codexTransport=app-server). Khi tắt, các kết nối được cấu hình để sử dụng app-server sẽ chuyển về các phương thức truyền khác của Codex.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `OMNIROUTE_EMERGENCY_FALLBACK`              | boolean | `true`   |               | Định tuyến các yêu cầu đã cạn ngân sách đến nhà cung cấp/mô hình dự phòng miễn phí khẩn cấp. (Xem [Dự phòng ngân sách khẩn cấp](#emergency-budget-fallback) bên dưới.)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `STREAM_RECOVERY_ENABLED`                   | boolean | `false`  |               | Bật tính năng thử lại sớm một cách minh bạch đối với các luồng SSE thượng nguồn bị cắt ngắn trước khi bất kỳ byte phản hồi nào đến được máy khách.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `STREAM_RECOVERY_MIDSTREAM_ENABLED`         | boolean | `false`  |               | Cho phép khôi phục luồng bằng cách yêu cầu lại và nối phản hồi sau khi các byte đã đến máy khách.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `STREAM_RECOVERY_TOOLCALL_ORDER_FIX`        | boolean | `false`  |               | Đảm bảo việc tiếp tục giữa luồng an toàn cho lệnh gọi công cụ: không bao giờ tiếp tục một luồng bị ngắt sau khi lệnh gọi công cụ đã được phát ra (đang thực hiện hoặc đã hoàn tất với finish_reason tool_calls), đồng thời đóng sau một lần tiếp tục trống thay vì sử dụng hết toàn bộ ngân sách. Tắt: hành vi của bản phát hành.                                                                                                                                                                                                                                                                                                                                                                                                         |
| `STREAM_EARLY_EOF_SIBLING_FAILOVER_ENABLED` | boolean | `false`  |               | Chuyển đổi dự phòng một lần sang kết nối cùng cấp khi luồng SSE đóng trước khi phát ra bất kỳ khung hữu ích nào và số lần thử lại có giới hạn trên cùng kết nối đã hết; nếu không có kết nối cùng cấp khả dụng, lỗi 502 `STREAM_EARLY_EOF` ban đầu sẽ được trả về. Mặc định tắt: EOF sớm vẫn là lỗi kết thúc sau khi thử lại trên cùng kết nối.                                                                                                                                                                                                                                                                                                                                                                                           |
| `MODEL_CATALOG_INCLUDE_NAMES`               | boolean | `true`   |               | Bao gồm các trường tên thân thiện với người dùng trong phản hồi `/v1/models`. Tắt đối với các máy khách chỉ chấp nhận ID mô hình.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `MODELS_CATALOG_PREFIX_MODE`                | enum    | `dual`   |               | Kiểm soát cách thêm tiền tố vào ID mô hình trong /v1/models. 'dual' (mặc định) xuất cả tiền tố bí danh và tiền tố ID nhà cung cấp chuẩn để đảm bảo khả năng tương thích ngược. 'alias' chỉ xuất tiền tố bí danh ngắn (ví dụ: ds-web/model, không phải deepseek-web/model). 'canonical' chỉ xuất tiền tố ID nhà cung cấp đầy đủ. Các giá trị: `dual`, `alias`, `canonical`.                                                                                                                                                                                                                                                                                                                                                                |
| `ARENA_ELO_SYNC_ENABLED`                    | boolean | `true`   |               | Bật đồng bộ ELO định kỳ từ bảng xếp hạng Arena AI để xếp hạng trí tuệ mô hình.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `EXPOSE_CC_DISCOVERY_ALIASES`               | boolean | `false`  |               | Công bố các ID bản sao `claude/<provider>/<model>` trên `/v1/models` để tính năng khám phá mô hình của cổng Claude Code liệt kê các mô hình không phải Claude. Cấp toàn cục của cơ chế kiểm soát ba cấp (biến môi trường được ưu tiên hơn cấu hình ghi đè trên bảng điều khiển). Xem [Cấu hình Claude Code](../guides/CLAUDE-CODE-CONFIGURATION.md#discovery-aliases--surface-non-claude-models-in-the-model-picker).                                                                                                                                                                                                                                                                                                                     |
| `NO_THINKING_ALIAS_ENABLED`                 | boolean | `true`   |               | Công tắc chính cho các bí danh cổng no-think/<provider>/<model>. Khi bật (mặc định): /v1/models công bố một biến thể không suy luận cho mọi mô hình Claude đủ điều kiện có khả năng suy luận, và ID no-think/ được gửi trong yêu cầu sẽ được phân giải trở lại mô hình thực với tính năng suy luận bị vô hiệu hóa. Khi tắt: không có biến thể nào được công bố và ID no-think/ được xử lý như mọi ID mô hình không xác định khác. Cơ chế chọn tham gia/rút lui theo từng mô hình ModelSpec.noThinkingAlias vẫn áp dụng khi tùy chọn này được bật.                                                                                                                                                                                         |
| `OMNIROUTE_DISABLE_THINKING_LEVEL_VARIANTS` | boolean | `false`  |               | Vô hiệu hóa việc tạo các biến thể cấp độ suy luận (ví dụ: -low, -medium, -high) trong danh mục /v1/models.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `OMNIROUTE_CHAT_VIRTUAL_LANES`              | boolean | `false`  | ✓             | Bật các làn tiếp nhận ảo thích ứng theo từng đối tượng thuê cho hoạt động điều phối đến nhà cung cấp (#9654): lưu lượng tăng đột biến của một đối tượng thuê sẽ không còn gây lỗi 503 cho đối tượng khác. Biến môi trường OMNIROUTE_CHAT_VIRTUAL_LANES được ưu tiên hơn cấu hình ghi đè trên bảng điều khiển này; các thay đổi có hiệu lực khi máy chủ khởi động lại.                                                                                                                                                                                                                                                                                                                                                                     |
| `EXPOSE_FUNCTIONAL_GATEWAY_MIRRORS`         | boolean | `false`  |               | Quảng bá các id bản sao <gateway-alias>/<model> trên /v1/models cho những model mà chủ sở hữu chính tắc không có thông tin xác thực đang hoạt động nhưng được một gateway chuyển tiếp có thông tin xác thực đang hoạt động định tuyến. Cảnh báo: thêm các mục danh mục cho tất cả client khi được bật trên toàn cục.                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `NEWAPI_AGGREGATOR_BALANCE`                 | boolean | `false`  |               | Bật tính năng phát hiện số dư cho các node tương thích với bộ tổng hợp New-API / One-API / Sub2API. Khi được bật, các node tương thích có cờ bộ tổng hợp được đặt sẽ báo cáo số dư của chúng trong dashboard và quá trình định tuyến kiểm tra trước hạn mức.                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `SERVER_OWNED_TOOL_LOOP_ENABLED`            | boolean | `false`  |               | Tiếp tục các lệnh gọi công cụ do máy chủ sở hữu không theo luồng cho đến khi model trả về phản hồi mà client có thể sử dụng.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `SEARCH_STATS_HIDE_DELETED_CONNECTIONS`     | boolean | `false`  |               | Số liệu thống kê tìm kiếm và các lượt tìm kiếm gần đây chỉ tính những nhà cung cấp vẫn còn kết nối đang hoạt động (các nhà cung cấp không cần khóa như duckduckgo-free luôn được tính). Khi tắt, giữ lại mọi hàng tìm kiếm đã được lưu có id nhà cung cấp.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `FREE_BADGE_REQUIRES_PROVIDER_FREE_TIER`    | boolean | `false`  |               | Các trang nhà cung cấp trên dashboard: chỉ hiển thị huy hiệu Miễn phí dựa trên những tín hiệu mà nhà cung cấp công nhận — loại bỏ phương pháp suy đoán theo tên hiển thị, các trường miễn phí không phải boolean và hậu tố :free trên các nhà cung cấp đã đăng ký nhưng không có gói miễn phí được ghi nhận trong tài liệu. Khi tắt, giữ nguyên quy tắc huy hiệu trước đây.                                                                                                                                                                                                                                                                                                                                                               |
| `RETRY_AFTER_PROVENANCE_ENABLED`            | boolean | `false`  |               | Trên các phản hồi không khả dụng 429/503 đã tổng hợp, bỏ qua `Retry-After` khi không xác định được thời điểm thử lại cụ thể trong tương lai (thay vì giá trị 1 giây giả lập), thêm `error.retry_after_provenance` (`signal` \| `none`) và cho phép các luồng xả combo đọc gợi ý thử lại dạng văn xuôi từ JSON và phần thân phản hồi ngược dòng dạng văn bản thuần túy. Trường này chỉ xuất hiện trong các phản hồi được tạo bởi `unavailableResponse()`; các phần thân 429/503 khác không thay đổi.                                                                                                                                                                                                                                       |
| `PROTECTED_PRIORITY_INFRA_502_ENABLED`      | boolean | `false`  |               | Khi một mục tiêu combo `priority` được đánh dấu chỉ dự phòng khi hết hạn mức dừng combo vì một nguyên nhân có thể chứng minh là không liên quan đến hạn mức (bộ ngắt mạch của nhà cung cấp đang mở, bỏ qua do độ trễ dự đoán), trả về 502 thay vì 503 trông giống lỗi hạn mức. Các trường hợp dừng do khóa, thời gian chờ, không khả dụng, cạn kiệt và giới hạn đồng thời vẫn giữ mã 503.                                                                                                                                                                                                                                                                                                                                                 |
| `MISTRAL_AMBIGUOUS_401_SOFT_LOCKOUT`        | boolean | `false`  |               | Một phản hồi Mistral 401 đơn thuần (`{"detail":"Unauthorized"}`, không có tín hiệu xác thực rõ ràng) là giống hệt nhau đối với khóa đã bị thu hồi và hạn mức đã cạn. Khi bật, hệ thống đưa kết nối vào thời gian chờ thay vì đánh dấu kết nối là `expired`, tối đa 3 lần mỗi giờ cho mỗi kết nối; lần tiếp theo sẽ đánh dấu kết nối, vì vậy khóa đã bị thu hồi cuối cùng vẫn được xác định. Mặc định là tắt: mọi phản hồi Mistral 401 đơn thuần đều đánh dấu kết nối như trước đây.                                                                                                                                                                                                                                                       |
| `XAI_OAUTH_LIVE_MODEL_DISCOVERY`            | boolean | `false`  |               | Tìm nạp danh mục mô hình xAI trực tiếp cho các kết nối `xai-oauth` từ `https://api.x.ai/v1/models` bằng mã thông báo bearer OAuth, thay vì dữ liệu khởi tạo tĩnh cố định. Mặc định tắt: `xai-oauth` tiếp tục cung cấp nguyên trạng dữ liệu khởi tạo tĩnh. Khi xảy ra bất kỳ lỗi phân giải nào, việc khám phá sẽ quay về dùng dữ liệu khởi tạo (chưa được xác minh liệu x.ai có chấp nhận mã thông báo bearer OAuth tại điểm cuối này hay không).                                                                                                                                                                                                                                                                                          |
| `BATCH_AND_FILE_AUTO_CLEANUP_ENABLED`       | boolean | `false`  |               | Cho phép quá trình dọn dẹp tự động xóa các tác vụ Batch API ở trạng thái kết thúc (đã hoàn thành/thất bại/đã hủy/hết hạn) cũ hơn `OMNIROUTE_BATCH_RETENTION_DAYS`, cùng với các điểm kiểm tra theo từng dòng của chúng, đồng thời xóa nội dung BLOB của các tệp đã tải lên sau thời điểm `expires_at` tương ứng. Mặc định tắt: mọi bản cài đặt hiện có tiếp tục lưu giữ dữ liệu này y như trước cho đến khi người vận hành chủ động bật. Tuyến `DELETE /api/v1/batches/delete-completed` do người vận hành kích hoạt không bị ảnh hưởng trong cả hai trường hợp — đây là một hợp đồng API công khai riêng biệt, vô điều kiện.                                                                                                             |
| `ANTIGRAVITY_ACCOUNT_LEASE_ENABLED`         | boolean | `false`  |               | Giữ riêng tài khoản Antigravity đã chọn trong suốt vòng đời phát trực tuyến của yêu cầu đã chọn tài khoản đó, để một lần thử lại đồng thời hoặc quá trình bàn giao thông tin xác thực không thể chọn lại một tài khoản đã được dành cho luồng đang xử lý. Việc giữ chỗ được giới hạn theo (kết nối, mô hình thượng nguồn có thể gọi), vì vậy một tài khoản vẫn có thể phục vụ đồng thời hai mô hình khác nhau. Khi mọi tài khoản đủ điều kiện đều đã được giữ chỗ cho mô hình đó, yêu cầu sẽ trả về phản hồi 503 có cấu trúc `antigravity_pool_busy` với `Retry-After` được giới hạn, thay vì dồn thêm tải lên một tài khoản đang bận. Mặc định tắt: việc chọn tài khoản vẫn giữ nguyên như trước và không có tài khoản nào được giữ chỗ. |

### CLI (5)

| Khóa                                  | Kiểu    | Mặc định | Khởi động lại | Mô tả                                                                                                                                                                                                                              |
| ------------------------------------- | ------- | -------- | ------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `CLI_COMPAT_ALL`                      | boolean | `false`  | ✓             | Bật chế độ tương thích cho tất cả máy khách CLI.                                                                                                                                                                                   |
| `MODEL_ALIAS_COMPAT_ENABLED`          | boolean | `false`  |               | Bật lớp tương thích bí danh mô hình.                                                                                                                                                                                               |
| `PRICING_SYNC_ENABLED`                | boolean | `false`  |               | Bật tính năng tự động đồng bộ dữ liệu giá (cũng yêu cầu biến môi trường `PRICING_SYNC_ENABLED`).                                                                                                                                   |
| `OMNIROUTE_AUTO_SYNC_CODEX_PROFILES`  | boolean | `false`  |               | Sau khi đồng bộ mô hình của nhà cung cấp, tự động ghi (lại) các tệp hồ sơ ~/.codex/*.config.toml từ danh mục trực tiếp. Không bao giờ thay đổi cấu hình Codex đang hoạt động/mặc định. Mặc định tắt.                               |
| `OMNIROUTE_AUTO_SYNC_CLAUDE_PROFILES` | boolean | `false`  |               | Sau khi đồng bộ mô hình của nhà cung cấp, tự động ghi (lại) các hồ sơ Claude Code tại ~/.claude/profiles/<name>/settings.json từ danh mục trực tiếp. Không bao giờ thay đổi cấu hình Claude đang hoạt động/mặc định. Mặc định tắt. |

### Tình trạng (5)

| Khóa                                      | Kiểu    | Mặc định | Mô tả                                                                                                                                                                                                                                                                                                                                           |
| ----------------------------------------- | ------- | -------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `OMNIROUTE_DISABLE_LOCAL_HEALTHCHECK`     | boolean | `false`  | Vô hiệu hóa endpoint kiểm tra tình trạng của phiên bản cục bộ.                                                                                                                                                                                                                                                                                  |
| `OMNIROUTE_DISABLE_TOKEN_HEALTHCHECK`     | boolean | `false`  | Vô hiệu hóa kiểm tra tình trạng xác thực token.                                                                                                                                                                                                                                                                                                 |
| `SKILLS_SANDBOX_NETWORK_ENABLED`          | boolean | `false`  | Bật quyền truy cập mạng trong môi trường sandbox của kỹ năng.                                                                                                                                                                                                                                                                                   |
| `PROXY_HEALTH_BLOCKED_RESETS_STREAK`      | boolean | `false`  | Trong quá trình quét tình trạng proxy, một lần thăm dò bị đích từ chối (401/403/429) sẽ đặt lại chuỗi lỗi liên tiếp của proxy. Mặc định là tắt: một lần từ chối vẫn được coi là trung lập (#10654). Phản hồi 5xx vẫn không mang tính kết luận trong cả hai trường hợp; một lần từ chối không bao giờ xóa, vô hiệu hóa hoặc kích hoạt lại proxy. |
| `DB_HEALTHCHECK_STARTUP_DEFERRED_ENABLED` | boolean | `false`  | Chạy kiểm tra tính toàn vẹn/tình trạng DB khi khởi động sau khi máy chủ bắt đầu chấp nhận yêu cầu (thông qua `setImmediate`), thay vì chặn quá trình khởi động cho đến khi kiểm tra hoàn tất (#13717). Mặc định là tắt: quá trình khởi động bị chặn giống hệt như trước PR này.                                                                 |

> [!NOTE]
> `INPUT_SANITIZER_BLOCK_THRESHOLD` và bí danh cũ của nó
> `INJECTION_GUARD_BLOCK_THRESHOLD` điều chỉnh chế độ `block` của
> `INJECTION_GUARD_MODE`, nhưng chúng là các biến môi trường thông thường được đọc bởi
> [`src/shared/utils/injectionSeverity.ts`](../../src/shared/utils/injectionSeverity.ts),
> không phải cờ tính năng: chúng không có giá trị ghi đè trong DB và không có nút chuyển đổi trên bảng điều khiển. Xem
> [`ENVIRONMENT.md`](./ENVIRONMENT.md#4-security--authentication).

> [!NOTE]
> Cột `Restart` đánh dấu các cờ có `requiresRestart: true` — giá trị được
> lưu ngay lập tức nhưng chỉ có hiệu lực sau khi tiến trình tải lại. Các cờ enum
> từ chối mọi giá trị nằm ngoài tập giá trị được phép của chúng (được xác thực phía máy chủ trong
> cả `setFeatureFlagOverride()` và trình xử lý REST `PUT`).

---

## Bật/Tắt Cờ

### Bảng điều khiển

Đi tới **Bảng điều khiển → Cài đặt → Cờ tính năng**
(`/dashboard/settings/feature-flags`). Lưới
(`src/app/(dashboard)/dashboard/settings/components/FeatureFlagsGrid.tsx`)
hỗ trợ:

- **Tìm kiếm** theo khóa hoặc mô tả và **lọc** theo danh mục (cùng với chế độ xem tổng hợp
  **Yêu cầu khởi động lại**).
- Một **công tắc** cho các cờ boolean và một **danh sách thả xuống** cho các cờ enum
  (`src/app/(dashboard)/dashboard/settings/components/FeatureFlagCard.tsx`).
- Một **huy hiệu nguồn** cho mỗi cờ — `DB`, `ENV` hoặc `DEF` — cho biết giá trị
  hiệu lực đến từ đâu.
- Nút **Đặt lại** (chỉ hiển thị cho các cờ có nguồn `DB`) để xóa giá trị ghi đè
  và nút **Đặt lại tất cả giá trị ghi đè** ở phía dưới.
- Biểu ngữ **Khởi động lại máy chủ** khi một cờ `requiresRestart` được thay đổi.

### API REST

Tất cả thao tác đều đi qua một route duy nhất:
[`src/app/api/settings/feature-flags/route.ts`](../../src/app/api/settings/feature-flags/route.ts).
Mỗi phương thức đều yêu cầu một phiên bảng điều khiển đã xác thực (nếu không sẽ trả về `401`).

#### `GET /api/settings/feature-flags`

Trả về mọi cờ cùng với giá trị hiệu lực, nguồn và thông tin tóm tắt.

```jsonc
{
  "flags": [
    {
      "key": "REQUIRE_API_KEY",
      "label": "Require API Key",
      "description": "Require an API key for all incoming requests",
      "category": "security",
      "type": "boolean",
      "enumValues": null,
      "defaultValue": "false",
      "effectiveValue": "false",
      "source": "default", // "db" | "env" | "default"
      "requiresRestart": false,
      "warningLevel": "caution",
    },
    // ... tất cả 75 cờ
  ],
  "summary": {
    "total": 56,
    "active": 0,
    "inactive": 0,
    "overriddenByDb": 0,
    "overriddenByEnv": 0,
  },
}
```

#### `PUT /api/settings/feature-flags`

Đặt hoặc xóa một giá trị ghi đè riêng lẻ. Nội dung yêu cầu: `{ key: string; value?: string }`.
Việc bỏ qua `value` sẽ xóa giá trị ghi đè (khôi phục giá trị env / mặc định).

```bash
# Đặt một giá trị ghi đè trong DB
curl -X PUT http://localhost:20128/api/settings/feature-flags \
  -H "Content-Type: application/json" \
  -d '{"key":"REQUIRE_API_KEY","value":"true"}'

# Xóa giá trị ghi đè (không có "value")
curl -X PUT http://localhost:20128/api/settings/feature-flags \
  -H "Content-Type: application/json" \
  -d '{"key":"REQUIRE_API_KEY"}'
```

Phản hồi trả lại `effectiveValue`/`source` mới, `previousValue`/
`previousSource` và `requiresRestart`. Các khóa không xác định và giá trị enum
nằm ngoài phạm vi sẽ bị từ chối với mã `400`.

#### `DELETE /api/settings/feature-flags`

Xóa **tất cả** giá trị ghi đè trong DB cùng lúc, khôi phục mọi cờ về giá trị env / mặc định.
Trả về `{ cleared: <count>, message: "..." }`.

> [!NOTE]
> Các cờ có `requiresRestart: true` chỉ có hiệu lực sau khi tiến trình được tải lại.
> Quy trình khởi động lại của bảng điều khiển gọi `POST /api/restart`, sau đó liên tục kiểm tra
> `GET /api/health/ping` cho đến khi máy chủ hoạt động trở lại.

---

## Cơ chế dự phòng ngân sách khẩn cấp

`OMNIROUTE_EMERGENCY_FALLBACK` (danh mục `runtime`, mặc định `true`) kiểm soát
luồng dự phòng miễn phí khẩn cấp trong
[`open-sse/services/emergencyFallback.ts`](../../open-sse/services/emergencyFallback.ts).
Khi được bật, các yêu cầu dùng hết ngân sách sẽ được định tuyến đến một
nhà cung cấp/mô hình dự phòng miễn phí thay vì thất bại hoàn toàn. Đặt thành `false` (hoặc `0`) — thông qua
nút bật/tắt trên bảng điều khiển, giá trị ghi đè trong DB hoặc biến môi trường
`OMNIROUTE_EMERGENCY_FALLBACK` — để vô hiệu hóa hành vi này và cho phép các yêu cầu
đã dùng hết ngân sách thất bại. (Được đưa lên thành nút bật/tắt trên bảng điều khiển trong các PR #3741 / #3752.)

---

## Xem thêm

- [Tài liệu tham khảo về biến môi trường](./ENVIRONMENT.md) — hầu hết các cờ đều có một biến môi trường cùng tên được ghi lại tại đây (giá trị ghi đè trong DB được ưu tiên hơn).
- [`src/shared/constants/featureFlagDefinitions.ts`](../../src/shared/constants/featureFlagDefinitions.ts)
  — nguồn thông tin chuẩn xác cho mọi cờ.
- [`src/shared/utils/featureFlags.ts`](../../src/shared/utils/featureFlags.ts)
  — logic phân giải (`resolveFeatureFlag`, `isFeatureFlagEnabled`,
  `resolveAllFeatureFlags`).
- [`src/lib/db/featureFlags.ts`](../../src/lib/db/featureFlags.ts) — cơ chế lưu trữ bền vững các giá trị ghi đè trong DB
  thuộc không gian tên `feature_flags` của bảng `key_value`.
