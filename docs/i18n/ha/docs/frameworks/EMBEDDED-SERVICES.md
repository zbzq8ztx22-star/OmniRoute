# Embedded Services (Hausa)

🌐 **Languages:** 🇺🇸 [English](../../../../frameworks/EMBEDDED-SERVICES.md) · 🇪🇹 [am](../../../am/docs/frameworks/EMBEDDED-SERVICES.md) · 🇸🇦 [ar](../../../ar/docs/frameworks/EMBEDDED-SERVICES.md) · 🇦🇿 [az](../../../az/docs/frameworks/EMBEDDED-SERVICES.md) · 🇧🇬 [bg](../../../bg/docs/frameworks/EMBEDDED-SERVICES.md) · 🇧🇩 [bn](../../../bn/docs/frameworks/EMBEDDED-SERVICES.md) · 🇨🇿 [cs](../../../cs/docs/frameworks/EMBEDDED-SERVICES.md) · 🇩🇰 [da](../../../da/docs/frameworks/EMBEDDED-SERVICES.md) · 🇩🇪 [de](../../../de/docs/frameworks/EMBEDDED-SERVICES.md) · 🇬🇷 [el](../../../el/docs/frameworks/EMBEDDED-SERVICES.md) · 🇪🇸 [es](../../../es/docs/frameworks/EMBEDDED-SERVICES.md) · 🇪🇪 [et](../../../et/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇷 [fa](../../../fa/docs/frameworks/EMBEDDED-SERVICES.md) · 🇫🇮 [fi](../../../fi/docs/frameworks/EMBEDDED-SERVICES.md) · 🇫🇷 [fr](../../../fr/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇪 [ga](../../../ga/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇳 [gu](../../../gu/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇱 [he](../../../he/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇳 [hi](../../../hi/docs/frameworks/EMBEDDED-SERVICES.md) · 🇭🇷 [hr](../../../hr/docs/frameworks/EMBEDDED-SERVICES.md) · 🇭🇺 [hu](../../../hu/docs/frameworks/EMBEDDED-SERVICES.md) · 🇦🇲 [hy](../../../hy/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇩 [id](../../../id/docs/frameworks/EMBEDDED-SERVICES.md) · 🇳🇬 [ig](../../../ig/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇹 [it](../../../it/docs/frameworks/EMBEDDED-SERVICES.md) · 🇯🇵 [ja](../../../ja/docs/frameworks/EMBEDDED-SERVICES.md) · 🇬🇪 [ka](../../../ka/docs/frameworks/EMBEDDED-SERVICES.md) · 🇰🇭 [km](../../../km/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇳 [kn](../../../kn/docs/frameworks/EMBEDDED-SERVICES.md) · 🇰🇷 [ko](../../../ko/docs/frameworks/EMBEDDED-SERVICES.md) · 🇱🇹 [lt](../../../lt/docs/frameworks/EMBEDDED-SERVICES.md) · 🇱🇻 [lv](../../../lv/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇳 [ml](../../../ml/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇳 [mr](../../../mr/docs/frameworks/EMBEDDED-SERVICES.md) · 🇲🇾 [ms](../../../ms/docs/frameworks/EMBEDDED-SERVICES.md) · 🇲🇹 [mt](../../../mt/docs/frameworks/EMBEDDED-SERVICES.md) · 🇲🇲 [my](../../../my/docs/frameworks/EMBEDDED-SERVICES.md) · 🇳🇵 [ne](../../../ne/docs/frameworks/EMBEDDED-SERVICES.md) · 🇳🇱 [nl](../../../nl/docs/frameworks/EMBEDDED-SERVICES.md) · 🇳🇴 [no](../../../no/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇳 [or](../../../or/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇳 [pa](../../../pa/docs/frameworks/EMBEDDED-SERVICES.md) · 🇵🇭 [phi](../../../phi/docs/frameworks/EMBEDDED-SERVICES.md) · 🇵🇱 [pl](../../../pl/docs/frameworks/EMBEDDED-SERVICES.md) · 🇵🇹 [pt](../../../pt/docs/frameworks/EMBEDDED-SERVICES.md) · 🇧🇷 [pt-BR](../../../pt-BR/docs/frameworks/EMBEDDED-SERVICES.md) · 🇷🇴 [ro](../../../ro/docs/frameworks/EMBEDDED-SERVICES.md) · 🇷🇺 [ru](../../../ru/docs/frameworks/EMBEDDED-SERVICES.md) · 🇱🇰 [si](../../../si/docs/frameworks/EMBEDDED-SERVICES.md) · 🇸🇰 [sk](../../../sk/docs/frameworks/EMBEDDED-SERVICES.md) · 🇸🇮 [sl](../../../sl/docs/frameworks/EMBEDDED-SERVICES.md) · 🇷🇸 [sr](../../../sr/docs/frameworks/EMBEDDED-SERVICES.md) · 🇸🇪 [sv](../../../sv/docs/frameworks/EMBEDDED-SERVICES.md) · 🇰🇪 [sw](../../../sw/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇳 [ta](../../../ta/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇳 [te](../../../te/docs/frameworks/EMBEDDED-SERVICES.md) · 🇹🇭 [th](../../../th/docs/frameworks/EMBEDDED-SERVICES.md) · 🇹🇷 [tr](../../../tr/docs/frameworks/EMBEDDED-SERVICES.md) · 🇺🇦 [uk-UA](../../../uk-UA/docs/frameworks/EMBEDDED-SERVICES.md) · 🇵🇰 [ur](../../../ur/docs/frameworks/EMBEDDED-SERVICES.md) · 🇺🇿 [uz](../../../uz/docs/frameworks/EMBEDDED-SERVICES.md) · 🇻🇳 [vi](../../../vi/docs/frameworks/EMBEDDED-SERVICES.md) · 🇳🇬 [yo](../../../yo/docs/frameworks/EMBEDDED-SERVICES.md) · 🇨🇳 [zh-CN](../../../zh-CN/docs/frameworks/EMBEDDED-SERVICES.md) · 🇹🇼 [zh-TW](../../../zh-TW/docs/frameworks/EMBEDDED-SERVICES.md)

---

> **Siga:** v3.8.44
> **Sabuntawa ta ƙarshe:** 2026-09-09
> **Masu karantawa:** Injiniyoyin da ke ƙara, kulawa, ko gyara matsalolin ayyukan da aka haɗa ciki (9Router, CLIProxyAPI, Mux, Bifrost, open-wa).

Ayyukan da aka haɗa ciki kayan aikin sidecar ne da ake girka a cikin na'ura, waɗanda OmniRoute ke girkawa, sa ido a kansu, kuma
ke gabatar da su a matsayin cikakkun wuraren da za a iya tura zirga-zirga zuwa gare su. Ba kamar masu samar da sabis na waje ba (waɗanda ake isa gare su ta intanet
ta amfani da maɓallan API), ayyukan da aka haɗa ciki suna gudana a na'ura ɗaya da OmniRoute kuma suna sadarwa ta loopback.

---

## Jadawalin Abubuwan da ke Ciki

1. [Bayani](#1-overview)
2. [Tsarin gine-gine — matakai 4](#2-architecture--4-layers)
3. [Na'urar yanayin zagayen rayuwa](#3-lifecycle-state-machine)
4. [Manunin API](#4-api-reference)
5. [Tsaro](#5-security)
6. [Ƙara sabon aikin da aka haɗa ciki](#6-adding-a-new-embedded-service)
7. [Warware matsaloli](#7-troubleshooting)
8. [Tambayoyin da ake yawan yi](#8-faq)

---

## 1. Bayani

### Me ya sa ake amfani da ayyukan da aka haɗa ciki?

An haɗa ayyuka shida:

| Aiki            | Kunshin npm                         | Tsohuwar tashar | Manufa                                                                                                                                                                                                     |
| --------------- | ----------------------------------- | :-------------: | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **9Router**     | `9router`                           |      20130      | Na'urar tura zirga-zirgar AI da OmniRoute zai iya amfani da ita a matsayin ƙaramin mai samar da sabis. Ana gabatar da samfura a matsayin `9router/{sub}/{model}`                                           |
| **CLIProxyAPI** | Binary na sakin GitHub (`cliproxy`) |      8317       | Adaftan proxy na cikin gida don hanyoyin tabbatar da izinin Anthropic CLI. Yana samar da madadin hanyar tura zirga-zirga idan token na OAuth ya ƙare                                                       |
| **Mux**         | `mux` (`mux server` marar GUI)      |      8322       | Daemon na tsara ayyukan wakilai na cikin gida (coder/mux). Ana sarrafa zagayen rayuwarsa kawai — ba wurin tura zirga-zirga ba ne (babu proxy na LLM).                                                      |
| **Bifrost**     | `@maximhq/bifrost`                  |      8080       | Backend na isar da AI-gateway na Go. Lokacin da yake gudana, hanyar relay (`/v1/relay/`) tana zaɓarsa ta atomatik                                                                                          |
| **Dario**       | `@askalf/dario`                     |      3456       | Proxy na rajistar Claude — madadi/mafita idan CLIProxyAPI ya gaza ga zirga-zirga mai tsarin Claude-Code; maɓallin da aka saka ya zama `DARIO_ADMIN_TOKEN` wanda ke kare tsarin sarrafa OAuth na `/admin/*` |
| **open-wa**     | `@open-wa/wa-automate`              |      8323       | Sarrafa WhatsApp Web ta atomatik (Chromium marar GUI ta Puppeteer). Ana sarrafa zagayen rayuwarsa kawai — ba wurin tura zirga-zirga ba ne.                                                                 |

Dukkan ayyukan shida suna bin tsarin sa ido iri ɗaya:

- OmniRoute yana girka su a ƙarƙashin `DATA_DIR/services/{name}/` (a ware daga `package.json` na OmniRoute)
- OmniRoute yana fara su kuma yana sa ido a kansu a matsayin ƙananan processes
- OmniRoute yana saka maɓallin API na wucin gadi a cikin muhallin ƙaramin process ɗin kuma yana sauya shi ba tare da dakatar da aiki ba (inda hakan ya dace)
- Dukkan hanyoyin gudanarwa (`/api/services/*`) **LOCAL_ONLY** ne — ana iya samun damar su daga loopback kawai (ƙa'ida mai tsauri #17)

### Muhimman shawarwari (daga tsarin ƙira)

| Shawara                                           | Ƙima                                                                                                |
| ------------------------------------------------- | --------------------------------------------------------------------------------------------------- |
| Samun damar dashboard zuwa UI na asali na 9Router | Reverse proxy a `/dashboard/providers/services/9router/embed/*`                                     |
| Hanyar girkawa                                    | `npm install {package}` ta `execFile` (babu shell interpolation)                                    |
| Yanayin amfani                                    | An yi rajistar mai samar da sabis a matsayin `9router/{sub}/{model}` a cikin injin tura zirga-zirga |
| Gudanar da maɓallin API                           | OmniRoute yana ƙirƙira, ɓoye shi lokacin ajiya (AES-256-GCM), sannan ya saka shi ta env             |
| Wurin dashboard                                   | `/dashboard/providers/services` (shafuka uku)                                                       |
| Farawa ta atomatik                                | Maɓallin kunnawa/kashewa ga kowane aiki, a kashe ta tsohuwa                                         |

---

## 2. Tsarin gine-gine — matakai 4

```
┌────────────────────────────────────────────────────────────────────┐
│  Mataki na 1 — UI                                                  │
│  /dashboard/providers/services  (shafuka: CLIProxyAPI | 9Router | Mux)│
│  Logs kai tsaye (SSE), Farawa/Tsayawa/Sake farawa/Sabuntawa, Saituna, Shigarwa│
│                                                                    │
│  src/app/(dashboard)/dashboard/providers/services/                 │
│    ├── page.tsx               Shell + karkatar da shafuka ta ?tab= │
│    ├── tabs/                  CliproxyServiceTab, NinerouterServiceTab,│
│    │                          MuxServiceTab                        │
│    └── components/            ServiceStatusCard, ServiceLifecycleButtons,│
│                               ServiceLogsPanel, ApiKeyCard, ...    │
└──────────────────────┬─────────────────────────────────────────────┘
                       │ HTTP (Next.js fetch)
┌──────────────────────▼─────────────────────────────────────────────┐
│  Mataki na 2 — API (LOCAL_ONLY — loopback kawai)                   │
│                                                                    │
│  /api/services/9router/{install|start|stop|restart|update|         │
│                          rotate-key|status|auto-start|logs}        │
│  /api/services/cliproxy/{install|start|stop|restart|update|        │
│                           status|auto-start|logs}                  │
│  /api/services/mux/{install|start|stop|restart|update|             │
│                      status|auto-start|logs}                       │
│  /dashboard/providers/services/9router/embed/[...path]             │
│    (reverse HTTP + WebSocket proxy → 9Router upstream)             │
│                                                                    │
│  Ƙofa: LOCAL_ONLY_API_PREFIXES ya ƙunshi "/api/services/" da       │
│        "/dashboard/providers/services/*/embed/"                    │
└──────────────────────┬─────────────────────────────────────────────┘
                       │ kiraye-kiraye a cikin tsari
┌──────────────────────▼─────────────────────────────────────────────┐
│  Mataki na 3 — ServiceSupervisor (src/lib/services/)               │
│                                                                    │
│  ServiceSupervisor.ts   Babban mai kulawa (child_process.spawn)    │
│    ├── install:    execFile('npm', ['install', pkg, '--prefix'])    │
│    ├── start:      spawn(node, [entrypoint], {env, cwd})           │
│    ├── api_key:    crypto.randomBytes(32) → env NINEROUTER_API_KEY  │
│    ├── port:       20130 don 9Router (ana iya saita shi)           │
│    ├── logs:       ma'ajiyar stdio mai zagaye 5 MB → al'amuran SSE │
│    ├── health:     HTTP GET /health kowane sakan 2–5, murmurewa lokacin buƙata│
│    └── lifecycle:  SIGTERM sakan 15 → SIGKILL                      │
│                                                                    │
│  registry.ts        getSupervisor(name) / registerSupervisor()     │
│  bootstrap.ts       Yana ƙaddamar da duk SERVICES[] lokacin fara tsari│
│  apiKey.ts          getOrCreateApiKey(), generateServiceApiKey()   │
│  modelSync.ts       GET /v1/models lokaci-lokaci → teburin service_models│
│  ringBuffer.ts      Ma'ajiyar log mai zagaye (5 MB ga kowace sabis)│
│  healthCheck.ts     Binciken lafiyar HTTP na lokaci-lokaci         │
│  installers/        ninerouter.ts, cliproxy.ts, mux.ts, openwa.ts  │
│                      (masu daidaita shigarwa)                      │
└──────────────────────┬─────────────────────────────────────────────┘
                       │ HTTP mai dacewa da OpenAI (loopback)
┌──────────────────────▼─────────────────────────────────────────────┐
│  Mataki na 4 — Mai bayarwa / Karkatarwa                            │
│                                                                    │
│  open-sse/executors/ninerouter.ts                                  │
│    Yana sake nemo port da API key ga kowace buƙata (babu caching). │
│    Yana cire prefix ɗin "9router/" daga model id kafin proxying.   │
│    Yana mayar da 503 service_not_running idan supervisor ba ya cikin "running".│
│                                                                    │
│  src/shared/constants/providers.ts                                 │
│    Shigarwa don "9router": isEmbeddedService: true                 │
│                                                                    │
│  open-sse/config/providerRegistry.ts                               │
│    Ana adana models a matsayin "9router/{sub}/{model}" (tare da prefix).│
│    modelSync.ts yana daidaita su kowane minti 5.                   │
│                                                                    │
│  Ana sarrafa lifecycle na Mux KAWAI (Matakai 1-3) — daemon ne na   │
│  tsara gudanarwar agent, ba LLM proxy ba, don haka ba shi da       │
│  executor/provider entry na Mataki na 4 kuma ba ya zama makasudin  │
│  karkatarwa.                                                       │
└────────────────────────────────────────────────────────────────────┘
```

### Muhimman fayilolin tushe

| Fayil                                       | Aiki                                                     |
| ------------------------------------------- | -------------------------------------------------------- |
| `src/lib/services/ServiceSupervisor.ts`     | Babban aji: zagayowar rayuwa, kulle, lafiya, ring buffer |
| `src/lib/services/bootstrap.ts`             | Rijista da farawa ta atomatik a matakin process          |
| `src/lib/services/registry.ts`              | Taswirar singleton `tool → supervisor`                   |
| `src/lib/services/apiKey.ts`                | Samar da maɓalli, ɓoyewar AES-256-GCM yayin ajiya        |
| `src/lib/services/modelSync.ts`             | Daidaita model lokaci-lokaci (minti 5) + bisa buƙata     |
| `src/lib/services/ringBuffer.ts`            | Ma'ajiyar log mai zagaye ta 5 MB tare da biyan kuɗin SSE |
| `src/lib/services/healthCheck.ts`           | Binciken lafiya na HTTP (tazara mai iya saitawa)         |
| `src/lib/services/installers/ninerouter.ts` | Shigarwa/sabuntawa/cirewa ta npm don 9Router             |
| `src/lib/services/installers/cliproxy.ts`   | Shigarwa/sabuntawa/cirewa ta npm don CLIProxyAPI         |
| `src/lib/services/installers/mux.ts`        | Shigarwa/sabuntawa/cirewa ta npm don Mux                 |
| `src/lib/services/installers/openwa.ts`     | Shigarwa/sabuntawa/cirewa ta npm don open-wa             |
| `src/app/api/services/9router/_lib.ts`      | Mataimakin `getOrInitSupervisor()`                       |
| `src/app/api/services/[name]/logs/route.ts` | Wurin samun logs na SSE da ake rabawa                    |
| `open-sse/executors/ninerouter.ts`          | Mai aiwatar da provider (Mataki na 4)                    |

---

## 3. Injin yanayin zagayen rayuwa

```
                    install()
  ┌─────────────┐ ──────────► ┌─────────────┐
  │ not_installed│             │   stopped   │◄──────────────────┐
  └─────────────┘             └──────┬──────┘                   │
                                     │ start()                   │
                                     ▼                           │ stop()
                               ┌──────────┐                      │
                               │ starting │                      │
                               └────┬─────┘                     │
             binciken lafiya ya yi  │         faɗuwa / SIGTERM   │
                               ┌────▼─────┐  (fita cikin 5s)     │
                               │ running  │──── faɗuwa ─────────►┤
                               └────┬─────┘                   ┌─▼────┐
                             stop() │                          │error │
                                    ▼                          └──────┘
                               ┌──────────┐
                               │ stopping │
                               └──────────┘
```

Ana adana yanayi a cikin teburin DB na `version_manager` (ginshiƙin `status`) kuma ana
kwafarsu a yanayin ƙwaƙwalwar-ciki na `ServiceSupervisor`. Yanayin ƙwaƙwalwar-ciki shi ne
mafi inganci ga tsari mai gudana; yanayin DB shi ne madadin dindindin yayin farawa.

### Sauye-sauyen yanayi

| Daga            | Lamari                                | Zuwa                      |
| --------------- | ------------------------------------- | ------------------------- |
| `not_installed` | `install()` ya yi nasara              | `stopped`                 |
| `stopped`       | an kira `start()`                     | `starting`                |
| `starting`      | binciken lafiya ya dawo da 200        | `running`                 |
| `starting`      | tsari ya fita kafin ya zama lafiyayye | `error`                   |
| `running`       | an kira `stop()`                      | `stopping` → `stopped`    |
| `running`       | tsari ya fita ba zato (< 5 s)         | `error` (faɗuwa da sauri) |
| `running`       | tsari ya fita ba zato (> 5 s)         | `error`                   |
| `error`         | an kira `start()`                     | `starting`                |
| kowane          | `stop()` yayin `stopping`             | babu aiki                 |

### Makullin aiki

`ServiceSupervisor` yana jera ayyukan zagayen rayuwa ta hanyar makullin aiki na async
(`withLock()`). Kiran `start()` da yawa a lokaci guda a kan supervisor ɗaya yana haifar da
spawn guda ɗaya tak; mai kira na biyu zai jira sannan ya dawo da matsayin da yake akwai.
Wannan yana hana yanayin tseren aiki lokacin da, misali, farawa ta atomatik da maɓallin UI
suka kunna a lokaci guda.

---

## 4. Bayanin API

Duk hanyoyin da ke ƙarƙashin `/api/services/` **LOCAL_ONLY** ne (loopback kawai, ƙa’ida
mai tsauri #17). Buƙatun da ba na loopback ba suna samun `403 LOCAL_ONLY` ba tare da
la’akari da auth token ba.

### 4.1 Ƙarshen hanyoyin 9Router (hanyoyi 11)

#### `POST /api/services/9router/install`

Shigar da 9Router daga npm. Yana ƙirƙirar `DATA_DIR/services/9router/` tare da nasa
`package.json` da `node_modules/`. Ba ya cin karo da deps na OmniRoute.

**Jikin buƙata** (duk na zaɓi ne):

```json
{ "version": "latest" }
```

| Fili      | Nau'i    | Tsoho      | Bayani                                    |
| --------- | -------- | ---------- | ----------------------------------------- |
| `version` | `string` | `"latest"` | Alamar sigar npm ko semver da za a shigar |

**Amsoshi:**

| Matsayi | Bayani                                                                    |
| ------- | ------------------------------------------------------------------------- |
| `200`   | `{ ok: true, installedVersion: "x.y.z", path: "..." }`                    |
| `400`   | Jikin buƙata mara inganci (gazawar tantancewar Zod)                       |
| `409`   | Ana kan shigarwa (an riƙe makulli)                                        |
| `500`   | npm install ya gaza — duba `message` don saƙon kuskure mai sauƙin fahimta |

**Bayanan kula:** Yana amfani da `execFile('npm', [...])` — babu shell, babu interpolation
(ƙa’ida mai tsauri #13). Ana gabatar da kurakuran EACCES a matsayin saƙonni masu sauƙin fahimta.

---

#### `POST /api/services/9router/start`

Fara 9Router. Yana rajistar supervisor idan ba a riga an rajista ba, sannan ya kira
`supervisor.start()`. Ba ya canza sakamako idan yana gudana tuni.

**Jikin buƙata:** babu

**Amsoshi:**

| Matsayi | Bayani                                                |
| ------- | ----------------------------------------------------- |
| `200`   | Abun `ServiceStatus` (duba schema a ƙasa)             |
| `409`   | Ba a shigar da 9Router ba (`status: "not_installed"`) |
| `503`   | Farawa ya gaza (kuskuren tsari — duba `lastError`)    |

**Schema na ServiceStatus:**

```json
{
  "tool": "9router",
  "state": "running",
  "pid": 12345,
  "port": 20130,
  "health": "healthy",
  "startedAt": "2026-05-25T10:00:00.000Z",
  "lastError": null
}
```

---

#### `POST /api/services/9router/stop`

Tsayar da 9Router cikin tsari. Yana aika SIGTERM, ya jira 15 s, sannan ya aika SIGKILL
idan har yanzu yana aiki. Ba ya canza sakamako idan an riga an tsayar da shi.

**Jikin buƙata:** babu

**Amsoshi:**

| Matsayi | Bayani                             |
| ------- | ---------------------------------- |
| `200`   | `ServiceStatus` (state: "stopped") |
| `503`   | Tsayarwa ta gaza ba zato           |

---

#### `POST /api/services/9router/restart`

Daidai yake da `stop()` sannan `start()` a ƙarƙashin makullin aiki.

**Jikin buƙata:** babu

**Amsoshi:** iri ɗaya da `start` (yana dawo da `ServiceStatus` na ƙarshe).

---

#### `POST /api/services/9router/update`

Yana sabunta 9Router zuwa sabuwar sigar npm. Idan sabis ɗin yana gudana, za a fara
tsayar da shi, a gudanar da npm install (ana shigar da sabuwar sigar a wurin da yake),
sannan a sake fara sabis ɗin.

**Jikin buƙata** (duk na zaɓi ne):

```json
{ "version": "latest" }
```

**Amsoshi:**

| Matsayi | Bayani                                                          |
| ------- | --------------------------------------------------------------- |
| `200`   | `{ ok: true, previousVersion: "...", installedVersion: "..." }` |
| `400`   | Jiki mara inganci                                               |
| `500`   | Sabuntawar npm ta gaza                                          |

---

#### `POST /api/services/9router/rotate-key`

Yana samar da sabon maɓallin API ga 9Router, yana ɓoye shi yayin ajiya, sannan yana sake kunna sabis ɗin
(idan yana gudana) domin ya ɗauki sabon maɓallin daga muhallinsa. Ana soke ingancin tsohon maɓallin
nan take.

**Jikin buƙata:** babu

**Amsoshi:**

| Matsayi | Bayani                                     |
| ------- | ------------------------------------------ |
| `200`   | `{ keyRotated: true, restarted: boolean }` |
| `500`   | Juyawar maɓalli ta gaza                    |

**Tsaro:** Ba a taɓa mayar da sabon maɓallin cikin amsa (babu fallasar bayanan sirri).
Ana adana shi a ɓoye (AES-256-GCM) cikin teburin `version_manager`.

---

#### `GET /api/services/9router/status`

Yana mayar da haɗaɗɗen matsayin kai-tsaye + DB, ciki har da metadata na sigar da samfotin maɓallin API.

**Amsoshi:**

| Matsayi | Bayani                         |
| ------- | ------------------------------ |
| `200`   | Duba tsarin bayanan da ke ƙasa |
| `500`   | Karanta matsayin ya gaza       |

**Tsarin amsa:**

```json
{
  "tool": "9router",
  "state": "running",
  "pid": 12345,
  "port": 20130,
  "health": "healthy",
  "startedAt": "2026-05-25T10:00:00.000Z",
  "lastError": null,
  "installedVersion": "1.2.3",
  "latestVersion": "1.2.4",
  "updateAvailable": true,
  "apiKeyMasked": "nr_****abcd",
  "autoStart": false,
  "providerExpose": false
}
```

---

#### `POST /api/services/9router/auto-start`

Kunna ko kashe tutar farawa ta atomatik. Lokacin da `enabled: true`, sabis ɗin yana farawa ta atomatik
a lokacin da OmniRoute zai sake tashi na gaba (idan an girka sabis ɗin).

**Jikin buƙata:**

```json
{ "enabled": true }
```

**Amsoshi:**

| Matsayi | Bayani                |
| ------- | --------------------- |
| `200`   | `{ autoStart: true }` |
| `400`   | Jiki mara inganci     |

---

#### `GET /api/services/9router/logs`

Rafin SSE na rajistocin kai-tsaye daga ring buffer na stdout/stderr na 9Router.

**Sigogin query:**

| Siga     | Nau'i     | Na asali | Bayani                                                                              |
| -------- | --------- | -------- | ----------------------------------------------------------------------------------- |
| `tail`   | `integer` | 200      | Yawan layukan tarihi da za a fara aikawa da su (mafi yawa 1000)                     |
| `filter` | `string`  | babu     | Tace ƙaramin-tsari mara bambancin babba/ƙarami (babu regex — mai kariya daga ReDoS) |

**Abubuwan SSE:**

| Abu         | Bayanai     | Bayani                          |
| ----------- | ----------- | ------------------------------- |
| `snapshot`  | `LogLine[]` | Wutsiyar tarihi ta farko        |
| `log`       | `LogLine`   | Layin rajista na kai-tsaye      |
| `heartbeat` | `{}`        | Ci gaba da haɗi kowane sakan 15 |

**Tsarin LogLine:**

```json
{
  "ts": 1716633600000,
  "stream": "stdout",
  "line": "[9router] Listening on :20130"
}
```

**Amsoshi:**

| Matsayi | Bayani                                                |
| ------- | ----------------------------------------------------- |
| `200`   | `text/event-stream`                                   |
| `400`   | Sigar `filter` ta yi tsawo sosai (> haruffa 200)      |
| `404`   | Ba a sami sabis ba (ba a yi wa supervisor rajista ba) |

---

### 4.2 Endpoints na CLIProxyAPI (routes 10)

CLIProxyAPI yana da tsarin endpoint iri ɗaya da 9Router ban da `rotate-key`, tare da
`accounts`, `provider-expose` da `auto-restart-adopted`. Yanzu yana karɓar
keɓaɓɓen maɓallin API na data-plane da ake saka masa lokacin spawn (`needsApiKey: true` a cikin
`bootstrap.ts`, wanda ake amfani da shi wajen daidaita model); `status` yana ƙunshe da fields kaɗan.

| Method | Path                                | Bayani                                        |
| ------ | ----------------------------------- | --------------------------------------------- |
| `POST` | `/api/services/cliproxy/install`    | Girka CLIProxyAPI daga npm                    |
| `POST` | `/api/services/cliproxy/start`      | Fara CLIProxyAPI                              |
| `POST` | `/api/services/cliproxy/stop`       | Tsayar da CLIProxyAPI                         |
| `POST` | `/api/services/cliproxy/restart`    | Sake kunna CLIProxyAPI                        |
| `POST` | `/api/services/cliproxy/update`     | Sabunta zuwa sabuwar siga                     |
| `GET`  | `/api/services/cliproxy/status`     | Matsayin kai-tsaye + DB (babu `apiKeyMasked`) |
| `POST` | `/api/services/cliproxy/auto-start` | Kunna ko kashe farawa ta atomatik             |

Endpoint ɗin `GET /api/services/{name}/logs` da ake rabawa (duba §4.1) yana aiki ga dukkan
sabis huɗun ta amfani da dynamic segment na `[name]`.

---

### 4.3 Endpoints na Mux (routes 8)

Mux yana da tsarin endpoint iri ɗaya da CLIProxyAPI — babu route na `rotate-key` a saman API
(ana samar da bearer token ɗin kamar na 9Router ta hanyar
`getOrCreateApiKey("mux")` kuma ana saka shi ta env var na `MUX_SERVER_AUTH_TOKEN`, amma
har yanzu babu keɓaɓɓen endpoint na juyawa). Ana sarrafa Mux ne kawai ta fuskar lifecycle: saɓanin
9Router, ba shi da executor na Layer 4 kuma ba a taɓa yi masa rajista a matsayin routing provider.

| Method | Path                           | Bayani                            |
| ------ | ------------------------------ | --------------------------------- |
| `POST` | `/api/services/mux/install`    | Girka Mux daga npm (`npm i mux`)  |
| `POST` | `/api/services/mux/start`      | Fara Mux (`mux server`)           |
| `POST` | `/api/services/mux/stop`       | Tsayar da Mux                     |
| `POST` | `/api/services/mux/restart`    | Sake kunna Mux                    |
| `POST` | `/api/services/mux/update`     | Sabunta zuwa sabuwar sigar npm    |
| `GET`  | `/api/services/mux/status`     | Matsayin kai-tsaye + DB           |
| `POST` | `/api/services/mux/auto-start` | Kunna ko kashe farawa ta atomatik |

---

### 4.4 Endpoints na Bifrost (routes 8)

Bifrost backend ne na relay na AI-gateway da aka rubuta da Go (`@maximhq/bifrost`). Yana amfani da tsarin
endpoint iri ɗaya da CLIProxyAPI (babu `rotate-key` — Bifrost yana sarrafa nasa provider
keys a cikin `config.json` ƙarƙashin `-app-dir`).

| Hanya  | Tafarki                            | Bayani                                                                       |
| ------ | ---------------------------------- | ---------------------------------------------------------------------------- |
| `POST` | `/api/services/bifrost/install`    | Shigar da Bifrost daga npm (`@maximhq/bifrost`)                              |
| `POST` | `/api/services/bifrost/start`      | Fara Bifrost a tashar 8080 (tsoho)                                           |
| `POST` | `/api/services/bifrost/stop`       | Dakatar da Bifrost                                                           |
| `POST` | `/api/services/bifrost/restart`    | Sake kunna Bifrost                                                           |
| `POST` | `/api/services/bifrost/update`     | Sabunta zuwa sabon sigar                                                     |
| `GET`  | `/api/services/bifrost/status`     | Matsayin kai-tsaye + DB                                                      |
| `POST` | `/api/services/bifrost/auto-start` | Kunna/kashe farawa ta atomatik                                               |
| `GET`  | `/api/services/bifrost/logs`       | Ƙarshen log na SSE (ta hanyar haɗaɗɗiyar hanya mai sauyawa ta `[name]/logs`) |

**Haɗin routing:** Lokacin da ba a saita `BIFROST_BASE_URL` ba kuma Bifrost da ake
kulawa da shi yana aiki, `getBifrostRoutingConfig()` (a cikin `routingBackend.ts`) yana
amfani da `http://127.0.0.1:{port}` ta atomatik a matsayin tushen URL na relay. Saitaccen
env na `BIFROST_BASE_URL` koyaushe yana da fifiko.

---

### 4.5 Endpoints na Dario (hanyoyi 12)

Tsarin lifecycle iri ɗaya ne da na sauran sabis (`install`, `start`, `stop`, `restart`,
`update`, `status`, `auto-start`, `auto-restart-adopted`) tare da control plane na OAuth
mai kariyar token a ƙarƙashin `admin/`: `admin/accounts`, `admin/import-from-omniroute`,
`admin/login-start`, `admin/login-complete` (duk suna bayan `DARIO_ADMIN_TOKEN`).

### 4.6 Endpoints na open-wa (hanyoyi 7)

open-wa (`@open-wa/wa-automate`) yana sarrafa Chromium marar taga (ta hanyar
Puppeteer) don sarrafa WhatsApp Web ta atomatik. Yana amfani da tsarin endpoint iri ɗaya
da Mux (har yanzu babu hanyar `rotate-key`). Ana sarrafa lifecycle ɗinsa kawai — ba
makasudin routing ba ne, kuma ba shi da shigarwar executor/provider ta Layer 4.

| Hanya  | Tafarki                           | Bayani                                                                       |
| ------ | --------------------------------- | ---------------------------------------------------------------------------- |
| `POST` | `/api/services/openwa/install`    | Shigar da open-wa daga npm (`@open-wa/wa-automate`)                          |
| `POST` | `/api/services/openwa/start`      | Fara open-wa a tashar 8323 (tsoho)                                           |
| `POST` | `/api/services/openwa/stop`       | Dakatar da open-wa                                                           |
| `POST` | `/api/services/openwa/restart`    | Sake kunna open-wa                                                           |
| `POST` | `/api/services/openwa/update`     | Sabunta zuwa sabon sigar                                                     |
| `GET`  | `/api/services/openwa/status`     | Matsayin kai-tsaye + DB                                                      |
| `POST` | `/api/services/openwa/auto-start` | Kunna/kashe farawa ta atomatik                                               |
| `GET`  | `/api/services/openwa/logs`       | Ƙarshen log na SSE (ta hanyar haɗaɗɗiyar hanya mai sauyawa ta `[name]/logs`) |

**Maɓallin API:** ana saka shi a matsayin `WA_KEY` — env override na open-wa mai
gabaɗayan prefix na `WA_*` yana daidaita shi zuwa zaɓin CLI na `--key`/`-k`
(`dist/cli/setup.js::envArgs()`, an tabbatar da shi da package 4.76.0 da aka
shigar). Ana sanya prefix na `ow_` lokacin da `generateServiceApiKey()` ya ƙirƙire shi.
open-wa yana sake karanta maɓallin daga HTTP header na `key`/`api_key` (ba
`Authorization: Bearer` ba); an keɓe `/api-docs*` a sarari daga binciken
(`setupAuthenticationLayer` a cikin `dist/cli/server.js`), don haka health probe
ba ya buƙatar auth header.

**Haɗawa:** open-wa ba na hukuma ba ne kuma ba shi da alaƙa da WhatsApp —
lambar da aka haɗa tana fuskantar haɗarin dakatarwa saboda tsarin gano sarrafa kansa
na WhatsApp. A farkon farawa, ana buga QR code na haɗawa zuwa stdout kuma ana
bayyana shi ta cikin panel na Logs/stream na SSE da ke akwai — har yanzu babu
keɓaɓɓen endpoint na hoton QR a wannan haɗin.

---

### 4.7 Reverse proxy (saka dashboard na 9Router)

Dashboard yana saka UI na yanar gizo na 9Router a cikin iframe ta hanyar reverse
proxy na ciki a:

```
GET|POST|... /dashboard/providers/services/9router/embed/[...path]
```

Wannan proxy:

- Yana tura request zuwa `http://127.0.0.1:{port}/{path}` (loopback kawai)
- Yana cire headers masu shigowa na `cookie` da `authorization` (babu fallasar session na OmniRoute)
- Yana saka `Authorization: Bearer {apiKey}` don tantancewar 9Router
- Yana cire `set-cookie`, `content-security-policy`, `x-frame-options`, `cross-origin-*` daga response
- Yana sake rubuta responses na HTML don saka `<base href>` da daidaita cikakkun paths (`/foo` → `/dashboard/.../embed/foo`)

Ana sarrafa upgrades na WebSocket don dashboard ɗin da aka saka ta wani companion server a
keɓaɓɓiyar tashar (duba `src/lib/services/embedWsProxy.ts`).

**Tsaro:** An rarraba hanyoyin embed proxy a ƙarƙashin `LOCAL_ONLY_API_PREFIXES`
kuma loopback ne kawai zai iya isa gare su. Maharin da ya samu JWT ta hanyar
tunnel na Cloudflare/Ngrok ba zai iya yin proxy zuwa sabis ɗin da aka saka ba.

---

## 5. Tsaro

### Tilasta LOCAL_ONLY (ƙa'ida mai tsauri #17)

Duk hanyoyin da ke ƙarƙashin `/api/services/` da `/dashboard/providers/services/*/embed/` an
rarraba su a matsayin LOCAL_ONLY a cikin `src/server/authz/routeGuard.ts`. Binciken loopback
yana gudana ba tare da wani sharaɗi ba kafin kowane reshen tantancewa:

```
buƙata ta iso
  → isLocalOnlyPath(path)?
      → ba-loopback ba → 403 LOCAL_ONLY (koyaushe, kafin binciken tantancewa)
      → loopback       → ci gaba zuwa tantancewa ta yau da kullum
```

Wannan yana hana JWT da ya fallasa (misali, ta hanyar tunnel) haifar da `npm install` ko
ƙaddamar da process. Duba `docs/security/ROUTE_GUARD_TIERS.md` don cikakken jadawalin
matakai.

### Saka API key

9Router da Mux suna buƙatar API key/bearer token don HTTP endpoints nasu.
OmniRoute:

1. Yana samar da key ta hanyar `crypto.randomBytes(32).toString("base64url")` tare da
   prefix na musamman ga service (`nr_` don 9Router, `mx_` don Mux).
2. Yana ɓoye shi lokacin da yake ajiye ta amfani da AES-256-GCM (cipher ɗin da ake amfani da shi don bayanan sirrin provider).
3. Yana warware ɓoyewar sannan ya saka shi a matsayin environment variable lokacin spawn —
   `NINEROUTER_API_KEY` don 9Router, `MUX_SERVER_AUTH_TOKEN` don Mux (ba a taɓa amfani da CLI
   flag ba, don haka token ɗin ba zai taɓa bayyana a cikin `ps`/jerin process ba).
4. Ba ya taɓa mayar da key ɗin plaintext a cikin kowace HTTP response.

CLIProxyAPI yana karɓar keɓaɓɓen data-plane key da ake sakawa lokacin spawn
(`needsApiKey: true` — ana amfani da shi don daidaita model da adapter).

### Kariyar SSRF

Reverse HTTP proxy (`/dashboard/.../embed/[...path]`) an ƙayyade shi kai tsaye don tura bayanai
kawai zuwa `http://127.0.0.1:{port}`. Ba ya taɓa bin redirects zuwa wuraren da ba loopback ba.
Ana amfani da library ɗin `ssrf-req-filter` don ƙin duk upstream URL da ke resolve zuwa
wajen kewayon loopback.

### Tsaron shell (ƙa'ida mai tsauri #13)

Ana kiran `npm install` ta hanyar `execFile('npm', ['install', pkg, '--prefix', dir])` —
ba template literals, ba shell, kuma ba a saka external paths cikin command
string. Ana miƙa runtime values (ports, API keys) ta hanyar `env` object na child.

### Tsabtace kurakurai (ƙa'ida mai tsauri #12)

Duk error responses daga `/api/services/*` suna bi ta `buildErrorBody()` ko
`sanitizeErrorMessage()`. Ba a taɓa mayar da ɗanyen `err.stack` da `err.message`
kai tsaye ga mai kira.

---

## 6. Ƙara sabon embedded service

Bi waɗannan matakai 8. Karanta implementations da suke akwai a `src/lib/services/installers/`
da `src/app/api/services/` a matsayin ingantaccen abin koyi.

### Mataki na 1 — Ƙirƙiri installer

Ƙirƙiri `src/lib/services/installers/{name}.ts` bisa tsarin `ninerouter.ts`:

```typescript
export const NAME_PACKAGE = "your-npm-package";
export const NAME_DEFAULT_PORT = 20132; // zaɓi port da babu mai amfani da shi

export async function install(version = "latest"): Promise<InstallResult> { ... }
export async function update(version = "latest"): Promise<InstallResult> { ... }
export async function uninstall(): Promise<void> { ... }
export function resolveSpawnArgs(apiKey: string, port: number): SpawnArgs { ... }
export async function getInstalledVersion(): Promise<string | null> { ... }
export async function getLatestVersion(): Promise<string | null> { ... }
```

Yi amfani da `runNpm(['install', NAME_PACKAGE, '--prefix', dir])` daga `installers/utils.ts`
— kada a taɓa amfani da `execSync` ko shell interpolation.

### Mataki na 2 — Yi rajista a bootstrap

Ƙara `ServiceEntry` zuwa array ɗin `SERVICES` a cikin `src/lib/services/bootstrap.ts`:

```typescript
{
  tool: "myservice",
  port: NAME_DEFAULT_PORT,
  healthPath: "/health",
  healthIntervalMs: 5_000,
  stopTimeoutMs: 15_000,
  logsBufferBytes: 5_242_880,
  needsApiKey: true, // false idan ba a buƙatar API key
}
```

Faɗaɗa `buildSpawnArgsFactory()` don kula da `cfg.tool === "myservice"`.

#### Yarjejeniyar provider-plugin mai sauƙin haɗawa (Phase 1, #7333)

`src/lib/services/providerPlugins/` yana gabatar da yarjejeniyar `ServiceProviderPlugin` wadda
ke haɗa filayen `ServiceEntry` na `bootstrap.ts` na backend da filayen manifest-template na
`serviceBackends.ts` cikin object guda, maimakon a bayyana tsarin backend ɗaya daban-daban
a cikin files biyu marasa alaƙa. A lokacin rubuta wannan, **`9router` kaɗai aka
migrate** — `bootstrap.ts` yana samar da entry ɗinsa na `SERVICES[]` daga
`getServiceProviderPlugin("9router")` (`src/lib/services/providerPlugins/registry.ts`),
yana jefa startup error idan plugin ɗin ya taɓa ɓacewa. `cliproxy`, `mux`, da `bifrost`
suna ci gaba da amfani da inline literals na `SERVICES[]` da suka riga suka kasance ba tare da canji ba.

`open-sse/config/providerPluginManifest.ts` shi ma ya sami ƙarin helper na
`createServiceBackendManifestEntry(pluginId, template)` wanda ke gina ingantaccen
`ProviderPluginManifestEntry` daga entry na `SERVICE_BACKEND_MANIFEST_TEMPLATE` — har yanzu
**ba a haɗa** shi da kowace live request path ba (ba `generateProviderPluginManifestFromRegistry()`
ba kuma ba `/v1/providers/[provider]/models` ba); wannan zai kasance aikin gaba bayan an
tabbatar da yarjejeniyar da backend na biyu.

An ɗage waɗannan zuwa PRs na gaba, ana bibiyarsu ƙarƙashin issue #7333: migrate `cliproxyapi` ta
registry iri ɗaya, faɗaɗa `mux`/`bifrost` cikin union na `ServiceBackendPluginId`,
haɗa keɓantaccen executor-routing (`open-sse/executors/index.ts`,
`open-sse/handlers/chatCore/executorProxy.ts`) cikin yarjejeniyar plugin, da kuma haɗa
`createServiceBackendManifestEntry()` cikin live manifest/models code path.

### Mataki na 3 — Ƙara migration da DB seed

Tabbatar cewa service ɗin yana da row a cikin `version_manager` ta hanyar migration a
`src/lib/db/migrations/`. Row ɗin ya kamata ya kasance da:

```sql
INSERT OR IGNORE INTO version_manager (tool, status, auto_start, provider_expose)
VALUES ('myservice', 'not_installed', 0, 0);
```

### Mataki na 4 — Ƙirƙiri API endpoints guda 7

A ƙarƙashin `src/app/api/services/{name}/`:

```
_lib.ts            helper na getOrInitSupervisor()
install/route.ts   POST — yana kiran installer.install()
start/route.ts     POST — yana kiran supervisor.start()
stop/route.ts      POST — yana kiran supervisor.stop()
restart/route.ts   POST — yana kiran supervisor.restart()
update/route.ts    POST — yana kiran installer.update()
status/route.ts    GET  — yana haɗa live + DB status
auto-start/route.ts POST — yana sauya auto_start flag
```

Hanyar haɗin gwiwa ta `GET /api/services/[name]/logs` an riga an haɗa ta — babu wasu
canje-canje da ake buƙata a can.

Tura duk martanin kurakurai ta hanyar `createErrorResponse()` / `buildErrorBody()`.

### Mataki na 5 — Ƙara zuwa LOCAL_ONLY_API_PREFIXES

A cikin `src/server/authz/routeGuard.ts`, tabbatar cewa an riga an jera `/api/services/`.
Idan ka ƙara sabon prefix (misali, `/api/tools/`), ƙara shi zuwa duka
`LOCAL_ONLY_API_PREFIXES` da kuma, idan yana ƙaddamar da processes, zuwa `SPAWN_CAPABLE_PREFIXES`.
Ƙara gwaji a cikin `tests/unit/authz/routeGuard.test.ts`.

### Mataki na 6 — Ƙara shafin UI

Ƙirƙiri `src/app/(dashboard)/dashboard/providers/services/tabs/{Name}ServiceTab.tsx`.
Sake amfani da shared components:

- `ServiceStatusCard` — halin yanzu kai tsaye + alamar lafiya
- `ServiceLifecycleButtons` — Farawa / Tsayawa / Sake farawa / Sabuntawa
- `ServiceLogsPanel` — ƙarshen log na SSE (yana haɗuwa da `/api/services/{name}/logs`)
- `ApiKeyCard` — nuna key + sauya shi (idan `needsApiKey: true`)

Yi rajistar shafin a cikin `ServicesPageShell.tsx`.

### Mataki na 7 — Ƙara shigarwar provider (idan service ɗin wani routing target ne)

Idan embedded service ɗin yana samar da endpoint mai dacewa da OpenAI na `/v1/chat/completions`:

1. Ƙara shigarwar provider a cikin `src/shared/constants/providers.ts` tare da `isEmbeddedService: true`.
2. Ƙirƙiri `open-sse/executors/{name}.ts` wanda ya gaji `BaseExecutor`. Sake nemo port da
   API key ga kowace request (kada a taɓa adana su a constructor). Mayar da martanin `503 service_not_running`
   lokacin da halin supervisor ba `"running"` ba ne.
3. Yi rajistar models a cikin `open-sse/config/providerRegistry.ts` tare da prefix na service ɗin
   (misali, `myservice/sub/model`). `modelSync.ts` zai ci gaba da sabunta su.

### Mataki na 8 — Rubuta takardu kuma yi gwaji

1. Sabunta `docs/frameworks/EMBEDDED-SERVICES.md` (wannan fayil ɗin) — ƙara service ɗin zuwa
   teburin da ke §1 da duk sabbin endpoints zuwa §4.
2. Ƙara unit tests a cikin `tests/unit/services/` (lifecycle, installer, tsarin API).
3. Ƙara integration test a cikin `tests/integration/services/` (a bayan `RUN_SERVICES_INT=1`).
4. Sabunta `docs/openapi.yaml` da sabbin endpoints.

---

## 7. Magance Matsaloli

### Sabis bai fara aiki ba

**Alamomi:** Maɓallin farawa yana dawo da 503, matsayin yana ci gaba da kasancewa `"error"` ko `"starting"`.

**Jerin dubawa:**

1. Duba `GET /api/services/{name}/logs` (ko sashen Logs a cikin dashboard). Nemo
   layuka kamar `Error: ENOENT`, `address already in use`, ko `Cannot find module`.
2. Tabbatar cewa `npm` yana cikin PATH: gudanar da `which npm` daga asusun mai amfani ɗaya da ke gudanar da OmniRoute.
3. Tabbatar cewa an girka sabis ɗin: duba `GET /api/services/{name}/status` don
   `installedVersion`. Idan `null` ne, fara gudanar da girkawa.
4. Duba cewa `DATA_DIR/services/{name}/node_modules/` yana nan kuma ba fanko ba ne.
5. Duba filin `lastError` a cikin amsar matsayi don ganin dalilin fita da aka tsabtace.

---

### Farawa daga farko yana tafiyar hawainiya (> s 10 kafin kaiwa `running`)

**Alamomi:** Matsayin yana ci gaba da kasancewa `"starting"` na dogon lokaci kafin ya koma `"running"` ko `"error"`.

**Bayani:** Farawar 9Router daga farko ta haɗa da shigo da manyan bishiyoyin abubuwan dogaro (DNS,
tunnel, MITM modules). Tsohon ƙimar tazarar duba lafiyar aiki ita ce s 2 tare da yunƙuri 3 kafin
supervisor ya ayyana ƙarewar lokaci (amma ya ci gaba da duba lokaci-lokaci).

**Gyara:** Ana iya saita lokacin ƙarewar `healthIntervalMs` da `waitForHealthy`
(`healthIntervalMs * 3`) a cikin `bootstrap.ts`. Don sabis-sabis masu ɗaukar tsawon lokaci
wajen farawa, ƙara `healthIntervalMs` zuwa 5000 da `stopTimeoutMs` zuwa 30 000.

---

### Karo na tashar sadarwa (`EADDRINUSE`)

**Alamomi:** Rajistan ayyuka yana nuna `address already in use :::20130`.

**Dalilai:**

- Wani tsari yana amfani da tashar 20130 tuni.
- Ba a dakatar da wani tsohon tsarin 9Router gaba ɗaya ba (zombie PID).

**Gyara:**

1. Canja tsohuwar tashar ta hanyar environment variable na `NINEROUTER_PORT` a cikin `.env`.
2. Nemo tsarin da ke haddasa karo kuma ka dakatar da shi: `lsof -ti :20130 | xargs kill -9`.
3. Ana iya saita tashar ga kowane sabis a cikin `bootstrap.ts` ta hanyar filin `port`.

**Lura:** A tsohon tsari, 9Router yana amfani da tashar 20130 musamman don kauce wa karo da
tsohuwar tashar OmniRoute ta 20128.

---

### An hana izini (EACCES) yayin girkawa

**Alamomi:** Girkawa tana dawo da 500, rajistan ayyuka yana nuna `EACCES` ko `permission denied`.

**Dalilai:**

- Tsarin OmniRoute ba shi da izinin rubutu a cikin `DATA_DIR` ko babban kundinsa.
- Ana gudanarwa a cikin Docker rootless ba tare da izinin rubutu a kan volume da aka haɗa ba.

**Gyara:**

1. Duba `DATA_DIR` (tsohon ƙima: `~/.omniroute/`): `ls -la ~/.omniroute/`
2. Tabbatar cewa mai amfani da tsarin OmniRoute ne ya mallaki kundin: `chown -R $USER ~/.omniroute/`
3. A cikin Docker, tabbatar cewa volume mount yana da ingantattun izini ga mai amfani da container.

---

### Sabuntawa ta gaza (ƙarewar lokacin `npm install` ko kuskuren hanyar sadarwa)

**Alamomi:** Sabuntawa tana dawo da 500 tare da `InstallError`, rajistan ayyuka yana nuna ƙarewar lokacin hanyar sadarwa.

**Jerin dubawa:**

1. Tabbatar cewa ana iya isa ga npm registry: `npm ping`.
2. Duba corporate proxy: `npm config get proxy`, `npm config get https-proxy`.
3. Gwada girkawar da hannu: `npm install {package}@latest --prefix ~/.omniroute/services/{name}/`.
4. Idan ana bayan air-gap, fara sauke tarball ɗin sannan ka yi amfani da `npm install /path/to/tarball.tgz`.

---

### Sabis yana nuna matsayin `"error"` nan take bayan farawa (rushewa da sauri)

**Alamomi:** Matsayin yana sauyawa daga `"starting"` zuwa `"error"` cikin ƙasa da daƙiƙa 5.
`lastError` yana nuna `"Fast crash (exited with code 1)"`.

**Jerin dubawa:**

1. Karanta cikakken ƙarshen rajistan ayyuka: `GET /api/services/{name}/logs?tail=500`.
2. Dalilin da aka fi samu: environment variables da sabis ɗin ke buƙata sun ɓace.
3. Don 9Router: tabbatar cewa `NINEROUTER_DISABLE_MITM=true` da
   `NINEROUTER_DISABLE_TUNNEL=true` suna cikin env da aka aika lokacin spawn (duba
   `installers/ninerouter.ts` `resolveSpawnArgs`).

---

## 8. Tambayoyin da ake yawan yi

**T: Zan iya fallasa endpoints na ayyukan da aka saka ciki ga clients waɗanda ba na loopback ba?**

A'a. Matakin LOCAL_ONLY an tsara shi da gangan (doka mai tsauri #17). Routes waɗanda za su iya kiran
`npm install` ko ƙaddamar da processes na `node` ba dole su kasance masu isa daga
traffic da ba na loopback ba, saboda idan JWT ya zube ta hanyar tunnel (Cloudflare, Ngrok, Tailscale),
hakan zai ba da damar ƙaddamar da kowane irin process. Babu keɓancewar ficewa ga
`/api/services/` — sabanin `/api/mcp/`, an cire shi daga jerin ketarewar manage-scope.
Duba `docs/security/ROUTE_GUARD_TIERS.md`.

---

**T: Shin 9Router da CLIProxyAPI za su kasance samammu a deployments na production/cloud?**

Eh. Duk ayyukan biyu suna bin tsarin local-first iri ɗaya da OmniRoute kansa. Suna aiki
a kan na'ura ɗaya kuma suna sadarwa ta loopback. "Production" a nan yana nufin VPS
ko local server inda aka deploy OmniRoute, ba wani remote cloud provider ba.

---

**T: Ta yaya zan debug supervisor?**

1. Bi SSE log stream kai tsaye: `curl -N http://localhost:20128/api/services/9router/logs`.
2. Duba structured logs a cikin output na pino na OmniRoute waɗanda aka tace ta
   namespace na `service:supervisor`.
3. Bincika row na DB: `sqlite3 ~/.omniroute/omniroute.db "SELECT * FROM version_manager WHERE tool='9router'"`.
4. Yi amfani da `GET /api/services/9router/status` don ganin live state na yanzu, PID, health,
   da `lastError` a kira guda.

---

**T: Supervisor yana nuna `health: "degraded"` ko `health: "unknown"` amma state ɗin shi ne `"running"`. Wannan matsala ce?**

`"degraded"` yana nufin health probe ya dawo da response wanda ba 200 ba. `"unknown"` yana nufin har yanzu babu
probe da ya kammala (gasa da poll na farko). Dukansu na ɗan lokaci ne yayin farawa.
Idan health ya ci gaba da kasancewa `"degraded"` fiye da `healthIntervalMs * 3` ms bayan
`"running"`, embedded service ɗin yana aiki amma HTTP API ɗinsa ba ya amsawa. Duba
ko port ɗin da ke cikin status response daidai ne, da kuma ko service ɗin yana
sauraro a wannan port ɗin da gaske.

---

**T: Zan iya canza API key na 9Router ba tare da cikakken restart ba?**

A'a. Ana aika API key ɗin zuwa 9Router ta hanyar environment variable a lokacin ƙaddamarwa.
Ba za a iya canza environment variables a cikin process da ke gudana ba. `POST .../rotate-key`
yana dakatarwa tare da sake kunna service ɗin ta atomatik don amfani da sabon key. Juyawar key ɗin
za ta fara aiki cikin `stopTimeoutMs` na service ɗin (default 15 s) tare da lokacin
farawarsa.

---

**T: Mene ne iyakar ring buffer kuma me ke faruwa idan ya cika?**

Kowane service yana da keɓaɓɓen ring buffer mai girman 5 MB. Idan buffer ɗin ya cika, ana
cire tsofaffin layukan log don samar wa sababbi sarari. Event na SSE `snapshot` yana dawo da
layuka na baya-bayan nan cikin iyakar `tail`. Ba a adana logs a disk sai dai idan
an saita `logsBufferPath` a cikin row na DB.

---

## Duba kuma

- `docs/security/ROUTE_GUARD_TIERS.md` — cikakkun bayanai game da matakin LOCAL_ONLY
- `docs/architecture/CODEBASE_DOCUMENTATION.md` — §3.2 taswirar module na Embedded Services
- `docs/architecture/ARCHITECTURE.md` — mahallin matakin system
- `docs/openapi.yaml` — ma'anar endpoints da na'ura za ta iya karantawa
- `CLAUDE.md` §"Ƙara Sabon Embedded Service" — checklist na saurin dubawa
