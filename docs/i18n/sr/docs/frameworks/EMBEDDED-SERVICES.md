# Embedded Services (Српски)

🌐 **Languages:** 🇺🇸 [English](../../../../frameworks/EMBEDDED-SERVICES.md) · 🇪🇹 [am](../../../am/docs/frameworks/EMBEDDED-SERVICES.md) · 🇸🇦 [ar](../../../ar/docs/frameworks/EMBEDDED-SERVICES.md) · 🇦🇿 [az](../../../az/docs/frameworks/EMBEDDED-SERVICES.md) · 🇧🇬 [bg](../../../bg/docs/frameworks/EMBEDDED-SERVICES.md) · 🇧🇩 [bn](../../../bn/docs/frameworks/EMBEDDED-SERVICES.md) · 🇨🇿 [cs](../../../cs/docs/frameworks/EMBEDDED-SERVICES.md) · 🇩🇰 [da](../../../da/docs/frameworks/EMBEDDED-SERVICES.md) · 🇩🇪 [de](../../../de/docs/frameworks/EMBEDDED-SERVICES.md) · 🇬🇷 [el](../../../el/docs/frameworks/EMBEDDED-SERVICES.md) · 🇪🇸 [es](../../../es/docs/frameworks/EMBEDDED-SERVICES.md) · 🇪🇪 [et](../../../et/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇷 [fa](../../../fa/docs/frameworks/EMBEDDED-SERVICES.md) · 🇫🇮 [fi](../../../fi/docs/frameworks/EMBEDDED-SERVICES.md) · 🇫🇷 [fr](../../../fr/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇪 [ga](../../../ga/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇳 [gu](../../../gu/docs/frameworks/EMBEDDED-SERVICES.md) · 🇳🇬 [ha](../../../ha/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇱 [he](../../../he/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇳 [hi](../../../hi/docs/frameworks/EMBEDDED-SERVICES.md) · 🇭🇷 [hr](../../../hr/docs/frameworks/EMBEDDED-SERVICES.md) · 🇭🇺 [hu](../../../hu/docs/frameworks/EMBEDDED-SERVICES.md) · 🇦🇲 [hy](../../../hy/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇩 [id](../../../id/docs/frameworks/EMBEDDED-SERVICES.md) · 🇳🇬 [ig](../../../ig/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇹 [it](../../../it/docs/frameworks/EMBEDDED-SERVICES.md) · 🇯🇵 [ja](../../../ja/docs/frameworks/EMBEDDED-SERVICES.md) · 🇬🇪 [ka](../../../ka/docs/frameworks/EMBEDDED-SERVICES.md) · 🇰🇭 [km](../../../km/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇳 [kn](../../../kn/docs/frameworks/EMBEDDED-SERVICES.md) · 🇰🇷 [ko](../../../ko/docs/frameworks/EMBEDDED-SERVICES.md) · 🇱🇹 [lt](../../../lt/docs/frameworks/EMBEDDED-SERVICES.md) · 🇱🇻 [lv](../../../lv/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇳 [ml](../../../ml/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇳 [mr](../../../mr/docs/frameworks/EMBEDDED-SERVICES.md) · 🇲🇾 [ms](../../../ms/docs/frameworks/EMBEDDED-SERVICES.md) · 🇲🇹 [mt](../../../mt/docs/frameworks/EMBEDDED-SERVICES.md) · 🇲🇲 [my](../../../my/docs/frameworks/EMBEDDED-SERVICES.md) · 🇳🇵 [ne](../../../ne/docs/frameworks/EMBEDDED-SERVICES.md) · 🇳🇱 [nl](../../../nl/docs/frameworks/EMBEDDED-SERVICES.md) · 🇳🇴 [no](../../../no/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇳 [or](../../../or/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇳 [pa](../../../pa/docs/frameworks/EMBEDDED-SERVICES.md) · 🇵🇭 [phi](../../../phi/docs/frameworks/EMBEDDED-SERVICES.md) · 🇵🇱 [pl](../../../pl/docs/frameworks/EMBEDDED-SERVICES.md) · 🇵🇹 [pt](../../../pt/docs/frameworks/EMBEDDED-SERVICES.md) · 🇧🇷 [pt-BR](../../../pt-BR/docs/frameworks/EMBEDDED-SERVICES.md) · 🇷🇴 [ro](../../../ro/docs/frameworks/EMBEDDED-SERVICES.md) · 🇷🇺 [ru](../../../ru/docs/frameworks/EMBEDDED-SERVICES.md) · 🇱🇰 [si](../../../si/docs/frameworks/EMBEDDED-SERVICES.md) · 🇸🇰 [sk](../../../sk/docs/frameworks/EMBEDDED-SERVICES.md) · 🇸🇮 [sl](../../../sl/docs/frameworks/EMBEDDED-SERVICES.md) · 🇸🇪 [sv](../../../sv/docs/frameworks/EMBEDDED-SERVICES.md) · 🇰🇪 [sw](../../../sw/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇳 [ta](../../../ta/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇳 [te](../../../te/docs/frameworks/EMBEDDED-SERVICES.md) · 🇹🇭 [th](../../../th/docs/frameworks/EMBEDDED-SERVICES.md) · 🇹🇷 [tr](../../../tr/docs/frameworks/EMBEDDED-SERVICES.md) · 🇺🇦 [uk-UA](../../../uk-UA/docs/frameworks/EMBEDDED-SERVICES.md) · 🇵🇰 [ur](../../../ur/docs/frameworks/EMBEDDED-SERVICES.md) · 🇺🇿 [uz](../../../uz/docs/frameworks/EMBEDDED-SERVICES.md) · 🇻🇳 [vi](../../../vi/docs/frameworks/EMBEDDED-SERVICES.md) · 🇳🇬 [yo](../../../yo/docs/frameworks/EMBEDDED-SERVICES.md) · 🇨🇳 [zh-CN](../../../zh-CN/docs/frameworks/EMBEDDED-SERVICES.md) · 🇹🇼 [zh-TW](../../../zh-TW/docs/frameworks/EMBEDDED-SERVICES.md)

---

> **Верзија:** v3.8.44
> **Последње ажурирање:** 2026-09-09
> **Публика:** Инжењери који додају, одржавају или отклањају грешке у уграђеним сервисима (9Router, CLIProxyAPI, Mux, Bifrost, open-wa).

Уграђени сервиси су локално инсталирани помоћни процесни алати које OmniRoute инсталира, надгледа и
излаже као пуноправна одредишта за усмеравање. За разлику од спољних добављача (којима се приступа путем интернета
помоћу API кључева), уграђени сервиси се извршавају на истом рачунару као OmniRoute и комуницирају преко loopback интерфејса.

---

## Садржај

1. [Преглед](#1-overview)
2. [Архитектура — 4 слоја](#2-architecture--4-layers)
3. [Машина стања животног циклуса](#3-lifecycle-state-machine)
4. [Референца API-ја](#4-api-reference)
5. [Безбедност](#5-security)
6. [Додавање новог уграђеног сервиса](#6-adding-a-new-embedded-service)
7. [Решавање проблема](#7-troubleshooting)
8. [Честа питања](#8-faq)

---

## 1. Преглед

### Зашто уграђени сервиси?

Уграђено је шест сервиса:

| Сервис          | npm пакет                                      | Подразумевани порт | Намена                                                                                                                                                                                                                    |
| --------------- | ---------------------------------------------- | :----------------: | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **9Router**     | `9router`                                      |       20130        | AI рутер који OmniRoute може да користи као поддобављача. Модели су изложени као `9router/{sub}/{model}`                                                                                                                  |
| **CLIProxyAPI** | Бинарна датотека из GitHub издања (`cliproxy`) |        8317        | Локални прокси адаптер за Anthropic CLI токове аутентификације. Обезбеђује резервно рутирање када OAuth токени истекну                                                                                                    |
| **Mux**         | `mux` (без интерфејса, `mux server`)           |        8322        | Локални демон за оркестрацију агената (coder/mux). Управља се само животним циклусом — није одредиште за рутирање (нема LLM проксирања).                                                                                  |
| **Bifrost**     | `@maximhq/bifrost`                             |        8080        | Go позадински релеј AI мрежног пролаза. Док је покренут, аутоматски се бира преко релејне руте (`/v1/relay/`)                                                                                                             |
| **Dario**       | `@askalf/dario`                                |        3456        | Прокси за Claude претплату — алтернатива/резервна опција за CLIProxyAPI за саобраћај у формату Claude Code-а; уметнути кључ постаје `DARIO_ADMIN_TOKEN` који ограничава приступ његовој OAuth контролној равни `/admin/*` |
| **open-wa**     | `@open-wa/wa-automate`                         |        8323        | Аутоматизација WhatsApp Web-а (Chromium без интерфејса преко Puppeteer-а). Управља се само животним циклусом — није одредиште за рутирање.                                                                                |

Свих шест користе исти модел надзора:

- OmniRoute их инсталира у `DATA_DIR/services/{name}/` (изоловано од сопствене датотеке `package.json` система OmniRoute)
- OmniRoute их покреће и надгледа као подређене процесе
- OmniRoute умеће краткотрајни API кључ у окружење подређеног процеса и ротира га без прекида рада (тамо где је применљиво)
- Све управљачке руте (`/api/services/*`) су **LOCAL_ONLY** — доступне само са адресе повратне петље (строго правило #17)

### Кључне одлуке (из плана дизајна)

| Одлука                                                          | Вредност                                                                            |
| --------------------------------------------------------------- | ----------------------------------------------------------------------------------- |
| Приступ контролне табле изворном 9Router корисничком интерфејсу | Обрнути прокси на `/dashboard/providers/services/9router/embed/*`                   |
| Механизам инсталације                                           | `npm install {package}` преко `execFile` (без интерполације љуске)                  |
| Режим коришћења                                                 | Добављач регистрован као `9router/{sub}/{model}` у механизму за рутирање            |
| Управљање API кључевима                                         | OmniRoute их генерише, шифрује при складиштењу (AES-256-GCM) и умеће преко окружења |
| Локација контролне табле                                        | `/dashboard/providers/services` (три картице)                                       |
| Аутоматско покретање                                            | Прекидач за сваки сервис, подразумевано ИСКЉУЧЕНО                                   |

---

## 2. Архитектура — 4 слоја

```
┌────────────────────────────────────────────────────────────────────┐
│  Слој 1 — Кориснички интерфејс                                     │
│  /dashboard/providers/services  (картице: CLIProxyAPI | 9Router | Mux)│
│  Логови уживо (SSE), Покрени/Заустави/Поново покрени/Ажурирај,     │
│  Подешавања, Инсталирај                                            │
│                                                                    │
│  src/app/(dashboard)/dashboard/providers/services/                 │
│    ├── page.tsx               Оквир + усмеравање картица преко ?tab=│
│    ├── tabs/                  CliproxyServiceTab, NinerouterServiceTab,│
│    │                          MuxServiceTab                        │
│    └── components/            ServiceStatusCard, ServiceLifecycleButtons,│
│                               ServiceLogsPanel, ApiKeyCard, ...    │
└──────────────────────┬─────────────────────────────────────────────┘
                       │ HTTP (Next.js fetch)
┌──────────────────────▼─────────────────────────────────────────────┐
│  Слој 2 — API (LOCAL_ONLY — само loopback)                         │
│                                                                    │
│  /api/services/9router/{install|start|stop|restart|update|         │
│                          rotate-key|status|auto-start|logs}        │
│  /api/services/cliproxy/{install|start|stop|restart|update|        │
│                           status|auto-start|logs}                  │
│  /api/services/mux/{install|start|stop|restart|update|             │
│                      status|auto-start|logs}                       │
│  /dashboard/providers/services/9router/embed/[...path]             │
│    (обрнути HTTP + WebSocket прокси → 9Router upstream)            │
│                                                                    │
│  Заштита: LOCAL_ONLY_API_PREFIXES садржи "/api/services/" и        │
│           "/dashboard/providers/services/*/embed/"                 │
└──────────────────────┬─────────────────────────────────────────────┘
                       │ позиви унутар процеса
┌──────────────────────▼─────────────────────────────────────────────┐
│  Слој 3 — ServiceSupervisor (src/lib/services/)                    │
│                                                                    │
│  ServiceSupervisor.ts   Генерички надзорник (child_process.spawn)  │
│    ├── инсталација: execFile('npm', ['install', pkg, '--prefix'])  │
│    ├── покретање:  spawn(node, [entrypoint], {env, cwd})           │
│    ├── api_key:    crypto.randomBytes(32) → env NINEROUTER_API_KEY  │
│    ├── порт:       20130 за 9Router (подесиво)                     │
│    ├── логови:     stdio кружни бафер од 5 MB → SSE догађаји       │
│    ├── провера:    HTTP GET /health сваких 2–5 s, лењи опоравак    │
│    └── животни циклус: SIGTERM 15 s → SIGKILL                      │
│                                                                    │
│  registry.ts        getSupervisor(name) / registerSupervisor()     │
│  bootstrap.ts       Иницијализује све SERVICES[] при покретању процеса│
│  apiKey.ts          getOrCreateApiKey(), generateServiceApiKey()   │
│  modelSync.ts       Периодични GET /v1/models → табела service_models│
│  ringBuffer.ts      Кружни бафер логова (5 MB по сервису)          │
│  healthCheck.ts     Периодична HTTP провера доступности            │
│  installers/        ninerouter.ts, cliproxy.ts, mux.ts, openwa.ts  │
│                      (адаптери за инсталацију)                     │
└──────────────────────┬─────────────────────────────────────────────┘
                       │ HTTP компатибилан са OpenAI-јем (loopback)
┌──────────────────────▼─────────────────────────────────────────────┐
│  Слој 4 — Добављач / Усмеравање                                    │
│                                                                    │
│  open-sse/executors/ninerouter.ts                                  │
│    Поново проналази порт и API кључ за сваки захтев (без кеширања).│
│    Уклања префикс "9router/" из ID-а модела пре прослеђивања.      │
│    Враћа 503 service_not_running ако надзорник није у "running".   │
│                                                                    │
│  src/shared/constants/providers.ts                                 │
│    Унос за "9router": isEmbeddedService: true                      │
│                                                                    │
│  open-sse/config/providerRegistry.ts                               │
│    Модели се чувају као "9router/{sub}/{model}" (са префиксом).    │
│    Синхронизује их modelSync.ts сваких 5 минута.                   │
│                                                                    │
│  Mux има управљање САМО животним циклусом (Слојеви 1–3) — то је    │
│  демон за оркестрацију агената, а не LLM прокси, па нема извршилац │
│  нити унос добављача у Слоју 4 и никада није циљ усмеравања.       │
└────────────────────────────────────────────────────────────────────┘
```

### Кључне изворне датотеке

| Датотека                                    | Улога                                                           |
| ------------------------------------------- | --------------------------------------------------------------- |
| `src/lib/services/ServiceSupervisor.ts`     | Основна класа: животни циклус, закључавање, стање, кружни бафер |
| `src/lib/services/bootstrap.ts`             | Регистрација на нивоу процеса и аутоматско покретање            |
| `src/lib/services/registry.ts`              | Синглтон мапа `алат → супервизор`                               |
| `src/lib/services/apiKey.ts`                | Генерисање кључа, AES-256-GCM шифровање података у мировању     |
| `src/lib/services/modelSync.ts`             | Периодична синхронизација модела (5 мин) + на захтев            |
| `src/lib/services/ringBuffer.ts`            | Кружни бафер евиденције од 5 MB са SSE претплатом               |
| `src/lib/services/healthCheck.ts`           | HTTP провера стања (подесив интервал)                           |
| `src/lib/services/installers/ninerouter.ts` | npm инсталација/ажурирање/деинсталација за 9Router              |
| `src/lib/services/installers/cliproxy.ts`   | npm инсталација/ажурирање/деинсталација за CLIProxyAPI          |
| `src/lib/services/installers/mux.ts`        | npm инсталација/ажурирање/деинсталација за Mux                  |
| `src/lib/services/installers/openwa.ts`     | npm инсталација/ажурирање/деинсталација за open-wa              |
| `src/app/api/services/9router/_lib.ts`      | Помоћна функција `getOrInitSupervisor()`                        |
| `src/app/api/services/[name]/logs/route.ts` | Дељена SSE крајња тачка за евиденције                           |
| `open-sse/executors/ninerouter.ts`          | Извршилац добављача (слој 4)                                    |

---

## 3. Машина стања животног циклуса

```
                    install()
  ┌─────────────┐ ──────────► ┌─────────────┐
  │ not_installed│             │   stopped   │◄──────────────────┐
  └─────────────┘             └──────┬──────┘                   │
                                     │ start()                   │
                                     ▼                           │ stop()
                               ┌──────────┐                      │
                               │ starting │                      │
                               └────┬─────┘                     │
            провера здравља успешна │         пад / SIGTERM      │
                               ┌────▼─────┐  (излаз у року од 5 s)│
                               │ running  │──── пад ─────────────►┤
                               └────┬─────┘                   ┌─▼────┐
                             stop() │                          │error │
                                    ▼                          └──────┘
                               ┌──────────┐
                               │ stopping │
                               └──────────┘
```

Стања се чувају у DB табели `version_manager` (колона `status`) и пресликавају
у стање у меморији компоненте `ServiceSupervisor`. Стање у меморији је меродавно за
покренут процес; стање у DB-у је трајна резервна вредност при покретању система.

### Прелази стања

| Из              | Догађај                                       | У                      |
| --------------- | --------------------------------------------- | ---------------------- |
| `not_installed` | `install()` је успешно извршен                | `stopped`              |
| `stopped`       | `start()` је позван                           | `starting`             |
| `starting`      | провера здравља враћа 200                     | `running`              |
| `starting`      | процес се завршава пре него што постане здрав | `error`                |
| `running`       | `stop()` је позван                            | `stopping` → `stopped` |
| `running`       | процес се неочекивано завршава (< 5 s)        | `error` (брз пад)      |
| `running`       | процес се неочекивано завршава (> 5 s)        | `error`                |
| `error`         | `start()` је позван                           | `starting`             |
| било које       | `stop()` током стања `stopping`               | без операције          |

### Закључавање операција

`ServiceSupervisor` серијализује операције животног циклуса помоћу асинхроног закључавања операција
(`withLock()`). Истовремени позиви `start()` над истим надзорником доводе до тачно
једног покретања процеса; други позивалац чека и враћа постојећи статус. Ово спречава
услове трке када се, на пример, аутоматско покретање и дугме у UI-ју активирају истовремено.

---

## 4. Референца API-ја

Све руте под `/api/services/` су **LOCAL_ONLY** (само loopback, строго правило #17).
Захтеви који не долазе са loopback адресе добијају `403 LOCAL_ONLY` без обзира на токен за аутентификацију.

### 4.1 Крајње тачке 9Router-а (11 рута)

#### `POST /api/services/9router/install`

Инсталира 9Router са npm-а. Креира `DATA_DIR/services/9router/` са сопственим
`package.json` и `node_modules/`. Не долази у сукоб са зависностима самог OmniRoute-а.

**Тело захтева** (све је опционално):

```json
{ "version": "latest" }
```

| Поље      | Тип      | Подразумевано | Опис                                         |
| --------- | -------- | ------------- | -------------------------------------------- |
| `version` | `string` | `"latest"`    | npm ознака верзије или semver за инсталацију |

**Одговори:**

| Статус | Опис                                                                            |
| ------ | ------------------------------------------------------------------------------- |
| `200`  | `{ ok: true, installedVersion: "x.y.z", path: "..." }`                          |
| `400`  | Неважеће тело захтева (неуспешна Zod валидација)                                |
| `409`  | Инсталација је већ у току (закључавање је активно)                              |
| `500`  | npm инсталација није успела — погледајте `message` за разумљиву поруку о грешци |

**Напомене:** Користи `execFile('npm', [...])` — без shell-а и без интерполације (строго правило #13).
EACCES грешке се приказују као разумљиве поруке.

---

#### `POST /api/services/9router/start`

Покреће 9Router. Региструје надзорник ако већ није регистрован, а затим позива
`supervisor.start()`. Идемпотентно ако је већ покренут.

**Тело захтева:** нема

**Одговори:**

| Статус | Опис                                                            |
| ------ | --------------------------------------------------------------- |
| `200`  | Објекат `ServiceStatus` (погледајте шему испод)                 |
| `409`  | 9Router није инсталиран (`status: "not_installed"`)             |
| `503`  | Покретање није успело (грешка процеса — погледајте `lastError`) |

**Шема ServiceStatus:**

```json
{
  "tool": "9router",
  "state": "running",
  "pid": 12345,
  "port": 20130,
  "health": "healthy",
  "startedAt": "2026-05-25T10:00:00.000Z",
  "lastError": null
}
```

---

#### `POST /api/services/9router/stop`

Контролисано зауставља 9Router. Шаље SIGTERM, чека 15 s, а затим шаље SIGKILL ако је процес и даље активан.
Идемпотентно ако је већ заустављен.

**Тело захтева:** нема

**Одговори:**

| Статус | Опис                                 |
| ------ | ------------------------------------ |
| `200`  | `ServiceStatus` (стање: `"stopped"`) |
| `503`  | Неочекивана грешка при заустављању   |

---

#### `POST /api/services/9router/restart`

Еквивалентно позивању `stop()`, а затим `start()` под закључавањем операције.

**Тело захтева:** нема

**Одговори:** исти као за `start` (враћа коначни `ServiceStatus`).

---

#### `POST /api/services/9router/update`

Ажурира 9Router на новију npm верзију. Ако је сервис покренут, прво се
зауставља, покреће се npm инсталација (новија верзија се инсталира на истом месту),
а затим се сервис поново покреће.

**Тело захтева** (све је опционално):

```json
{ "version": "latest" }
```

**Одговори:**

| Статус | Опис                                                            |
| ------ | --------------------------------------------------------------- |
| `200`  | `{ ok: true, previousVersion: "...", installedVersion: "..." }` |
| `400`  | Неважеће тело                                                   |
| `500`  | npm ажурирање није успело                                       |

---

#### `POST /api/services/9router/rotate-key`

Генерише нови API кључ за 9Router, шифрује га при складиштењу и поново покреће сервис
(ако је покренут) како би преузео нови кључ из свог окружења. Стари кључ се
одмах поништава.

**Тело захтева:** нема

**Одговори:**

| Статус | Опис                                       |
| ------ | ------------------------------------------ |
| `200`  | `{ keyRotated: true, restarted: boolean }` |
| `500`  | Ротација није успела                       |

**Безбедност:** Нови кључ се никада не враћа у одговору (нема откривања акредитива).
Чува се шифрован (AES-256-GCM) у табели `version_manager`.

---

#### `GET /api/services/9router/status`

Враћа комбиновани статус уживо и из базе података, укључујући метаподатке о верзији и преглед API кључа.

**Одговори:**

| Статус | Опис                       |
| ------ | -------------------------- |
| `200`  | Погледајте шему испод      |
| `500`  | Читање статуса није успело |

**Шема одговора:**

```json
{
  "tool": "9router",
  "state": "running",
  "pid": 12345,
  "port": 20130,
  "health": "healthy",
  "startedAt": "2026-05-25T10:00:00.000Z",
  "lastError": null,
  "installedVersion": "1.2.3",
  "latestVersion": "1.2.4",
  "updateAvailable": true,
  "apiKeyMasked": "nr_****abcd",
  "autoStart": false,
  "providerExpose": false
}
```

---

#### `POST /api/services/9router/auto-start`

Укључује или искључује заставицу за аутоматско покретање. Када је `enabled: true`, сервис се аутоматски
покреће при следећем покретању OmniRoute-а (ако је сервис инсталиран).

**Тело захтева:**

```json
{ "enabled": true }
```

**Одговори:**

| Статус | Опис                  |
| ------ | --------------------- |
| `200`  | `{ autoStart: true }` |
| `400`  | Неважеће тело         |

---

#### `GET /api/services/9router/logs`

SSE ток логова уживо из кружног бафера stdout/stderr излаза 9Router-а.

**Параметри упита:**

| Параметар | Тип       | Подразумевано | Опис                                                                                     |
| --------- | --------- | ------------- | ---------------------------------------------------------------------------------------- |
| `tail`    | `integer` | 200           | Број историјских редова који се прво шаљу (највише 1000)                                 |
| `filter`  | `string`  | нема          | Филтер подниза који не разликује велика и мала слова (без regex-а — безбедно од ReDoS-а) |

**SSE догађаји:**

| Догађај     | Подаци      | Опис                              |
| ----------- | ----------- | --------------------------------- |
| `snapshot`  | `LogLine[]` | Почетни историјски завршни редови |
| `log`       | `LogLine`   | Ред лога уживо                    |
| `heartbeat` | `{}`        | Одржавање везе на сваких 15 s     |

**Шема LogLine:**

```json
{
  "ts": 1716633600000,
  "stream": "stdout",
  "line": "[9router] Listening on :20130"
}
```

**Одговори:**

| Статус | Опис                                               |
| ------ | -------------------------------------------------- |
| `200`  | `text/event-stream`                                |
| `400`  | Параметар `filter` је предугачак (> 200 знакова)   |
| `404`  | Услуга није пронађена (надзорник није регистрован) |

---

### 4.2 CLIProxyAPI крајње тачке (10 рута)

CLIProxyAPI има исти облик крајњих тачака као 9Router, али без `rotate-key`, уз
додатак `accounts`, `provider-expose` и `auto-restart-adopted`. Сада добија
наменски API кључ равни података који се убризгава при покретању (`needsApiKey: true` у
`bootstrap.ts`, користи се за синхронизацију модела); `status` садржи мање поља.

| Метод  | Путања                              | Опис                                            |
| ------ | ----------------------------------- | ----------------------------------------------- |
| `POST` | `/api/services/cliproxy/install`    | Инсталирај CLIProxyAPI из npm-а                 |
| `POST` | `/api/services/cliproxy/start`      | Покрени CLIProxyAPI                             |
| `POST` | `/api/services/cliproxy/stop`       | Заустави CLIProxyAPI                            |
| `POST` | `/api/services/cliproxy/restart`    | Поново покрени CLIProxyAPI                      |
| `POST` | `/api/services/cliproxy/update`     | Ажурирај на новију верзију                      |
| `GET`  | `/api/services/cliproxy/status`     | Статус уживо + статус базе (без `apiKeyMasked`) |
| `POST` | `/api/services/cliproxy/auto-start` | Укључи или искључи аутоматско покретање         |

Заједничка крајња тачка `GET /api/services/{name}/logs` (погледајте §4.1) ради за све
четири услуге користећи динамички сегмент `[name]`.

---

### 4.3 Mux крајње тачке (8 рута)

Mux има исти облик крајњих тачака као CLIProxyAPI — нема руте `rotate-key` у API
интерфејсу (bearer токен се генерише на исти начин као код 9Router-а, путем
`getOrCreateApiKey("mux")`, и убризгава се путем променљиве окружења `MUX_SERVER_AUTH_TOKEN`, али
још увек не постоји наменска крајња тачка за ротацију). Mux-ом се управља само у оквиру животног циклуса: за разлику од
9Router-а, он нема извршавач слоја 4 и никада се не региструје као добављач рутирања.

| Метод  | Путања                         | Опис                                    |
| ------ | ------------------------------ | --------------------------------------- |
| `POST` | `/api/services/mux/install`    | Инсталирај Mux из npm-а (`npm i mux`)   |
| `POST` | `/api/services/mux/start`      | Покрени Mux (`mux server`)              |
| `POST` | `/api/services/mux/stop`       | Заустави Mux                            |
| `POST` | `/api/services/mux/restart`    | Поново покрени Mux                      |
| `POST` | `/api/services/mux/update`     | Ажурирај на новију npm верзију          |
| `GET`  | `/api/services/mux/status`     | Статус уживо + статус базе              |
| `POST` | `/api/services/mux/auto-start` | Укључи или искључи аутоматско покретање |

---

### 4.4 Bifrost крајње тачке (8 рута)

Bifrost је посреднички позадински систем AI мрежног пролаза написан у Go-у (`@maximhq/bifrost`). Користи исти
облик крајњих тачака као CLIProxyAPI (без `rotate-key` — Bifrost управља сопственим кључевима
добављача у `config.json` у оквиру свог `-app-dir`).

| Метод  | Путања                             | Опис                                                                       |
| ------ | ---------------------------------- | -------------------------------------------------------------------------- |
| `POST` | `/api/services/bifrost/install`    | Инсталирај Bifrost из npm-а (`@maximhq/bifrost`)                           |
| `POST` | `/api/services/bifrost/start`      | Покрени Bifrost на порту 8080 (подразумевано)                              |
| `POST` | `/api/services/bifrost/stop`       | Заустави Bifrost                                                           |
| `POST` | `/api/services/bifrost/restart`    | Поново покрени Bifrost                                                     |
| `POST` | `/api/services/bifrost/update`     | Ажурирај на новију верзију                                                 |
| `GET`  | `/api/services/bifrost/status`     | Статус уживо + статус базе                                                 |
| `POST` | `/api/services/bifrost/auto-start` | Укључи или искључи аутоматско покретање                                    |
| `GET`  | `/api/services/bifrost/logs`       | SSE праћење краја дневника (путем заједничке динамичке руте `[name]/logs`) |

**Повезивање рутирања:** Када `BIFROST_BASE_URL` није постављен и надгледана Bifrost
инстанца је покренута, `getBifrostRoutingConfig()` (у `routingBackend.ts`) аутоматски
користи `http://127.0.0.1:{port}` као основни URL посредника. Експлицитно постављена променљива окружења
`BIFROST_BASE_URL` увек има предност.

---

### 4.5 Dario крајње тачке (12 рута)

Исти облик животног циклуса као код осталих услуга (`install`, `start`, `stop`, `restart`,
`update`, `status`, `auto-start`, `auto-restart-adopted`), уз OAuth
контролну раван заштићену токеном под `admin/`: `admin/accounts`, `admin/import-from-omniroute`,
`admin/login-start`, `admin/login-complete` (све заштићено помоћу `DARIO_ADMIN_TOKEN`).

### 4.6 open-wa крајње тачке (7 рута)

open-wa (`@open-wa/wa-automate`) управља безглавом Chromium инстанцом (путем
Puppeteer-а) ради аутоматизације WhatsApp Web-а. Користи исти облик крајњих тачака као Mux (још увек нема
руту `rotate-key`). Њиме се управља само у оквиру животног циклуса — није циљ рутирања
и нема извршавач слоја 4 нити унос добављача.

| Метод  | Путања                            | Опис                                                               |
| ------ | --------------------------------- | ------------------------------------------------------------------ |
| `POST` | `/api/services/openwa/install`    | Инсталира open-wa са npm-а (`@open-wa/wa-automate`)                |
| `POST` | `/api/services/openwa/start`      | Покреће open-wa на порту 8323 (подразумевано)                      |
| `POST` | `/api/services/openwa/stop`       | Зауставља open-wa                                                  |
| `POST` | `/api/services/openwa/restart`    | Поново покреће open-wa                                             |
| `POST` | `/api/services/openwa/update`     | Ажурира на новију верзију                                          |
| `GET`  | `/api/services/openwa/status`     | Статус уживо + статус у бази података                              |
| `POST` | `/api/services/openwa/auto-start` | Укључује или искључује аутоматско покретање                        |
| `GET`  | `/api/services/openwa/logs`       | SSE праћење евиденције (преко дељене динамичке руте `[name]/logs`) |

**API кључ:** убризгава се као `WA_KEY` — open-wa генеричко замењивање преко променљивих окружења са префиксом `WA_*` мапира га на CLI опцију `--key`/`-k`
(`dist/cli/setup.js::envArgs()`, проверено у односу на инсталирани пакет верзије 4.76.0). Добија префикс `ow_` када га генерише `generateServiceApiKey()`. open-wa
чита кључ из HTTP заглавља `key`/`api_key` (не из `Authorization:
Bearer`); `/api-docs*` је изричито изузет од провере
(`setupAuthenticationLayer` у `dist/cli/server.js`), тако да проба здравља
не захтева заглавље за аутентификацију.

**Упаривање:** open-wa је незваничан и није повезан са WhatsApp-ом — за
повезани број постоји ризик од забране услед WhatsApp-овог сопственог откривања аутоматизације.
При првом покретању, QR код за упаривање исписује се у стандардни излаз и приказује
преко постојећег панела Евиденција/SSE тока — у овој интеграцији још увек
не постоји наменска крајња тачка за QR слику.

---

### 4.7 Обрнути прокси (уграђени приказ 9Router контролне табле)

Контролна табла уграђује 9Router веб кориснички интерфејс у iframe преко интерног обрнутог
проксија на:

```
GET|POST|... /dashboard/providers/services/9router/embed/[...path]
```

Овај прокси:

- Прослеђује захтев на `http://127.0.0.1:{port}/{path}` (само повратна петља)
- Уклања долазна заглавља `cookie` и `authorization` (без цурења OmniRoute сесије)
- Убризгава `Authorization: Bearer {apiKey}` за 9Router аутентификацију
- Уклања `set-cookie`, `content-security-policy`, `x-frame-options`, `cross-origin-*` из одговора
- Преписује HTML одговоре како би убризгао `<base href>` и нормализовао апсолутне путање (`/foo` → `/dashboard/.../embed/foo`)

WebSocket надоградње за уграђену контролну таблу обрађује пратећи сервер на
наменском порту (погледајте `src/lib/services/embedWsProxy.ts`).

**Безбедност:** Руте уграђеног проксија класификоване су под `LOCAL_ONLY_API_PREFIXES`
и може им се приступити само преко повратне петље. Нападач који добије JWT преко
Cloudflare/Ngrok тунела не може да користи прокси за приступ уграђеним сервисима.

---

## 5. Безбедност

### Спровођење правила LOCAL_ONLY (строго правило #17)

Све руте под `/api/services/` и `/dashboard/providers/services/*/embed/`
класификоване су као LOCAL_ONLY у `src/server/authz/routeGuard.ts`. Провера повратне петље
извршава се безусловно пре било које гране аутентификације:

```
захтев стиже
  → isLocalOnlyPath(path)?
      → није повратна петља → 403 LOCAL_ONLY (увек, пре провере аутентификације)
      → повратна петља      → настави са уобичајеном аутентификацијом
```

Ово спречава да процурели JWT (нпр. преко тунела) покрене `npm install` или
стварање процеса. Погледајте `docs/security/ROUTE_GUARD_TIERS.md` за потпуну матрицу
нивоа.

### Уметање API кључа

9Router и Mux захтевају API кључ/носилачки токен за сопствене HTTP крајње тачке.
OmniRoute:

1. Генерише кључ помоћу `crypto.randomBytes(32).toString("base64url")` са
   префиксом специфичним за сервис (`nr_` за 9Router, `mx_` за Mux).
2. Шифрује га при складиштењу користећи AES-256-GCM (исту шифру која се користи за акредитиве добављача).
3. Дешифрује га и умеће као променљиву окружења при покретању —
   `NINEROUTER_API_KEY` за 9Router, `MUX_SERVER_AUTH_TOKEN` за Mux (никада као CLI
   опцију, тако да се токен никада не појављује у `ps`/листама процеса).
4. Никада не враћа кључ у облику обичног текста ни у једном HTTP одговору.

CLIProxyAPI добија наменски кључ равни података који се умеће при покретању
(`needsApiKey: true` — користи се за синхронизацију модела са адаптером).

### Заштита од SSRF-а

Обрнути HTTP прокси (`/dashboard/.../embed/[...path]`) строго је подешен да прослеђује
само на `http://127.0.0.1:{port}`. Никада не прати преусмеравања ка одредиштима која
нису у повратној петљи. Библиотека `ssrf-req-filter` користи се за одбијање сваког
узводног URL-а који се разрешава ван опсега повратне петље.

### Безбедност љуске (строго правило #13)

`npm install` се позива преко `execFile('npm', ['install', pkg, '--prefix', dir])` —
без шаблонских литерала, без љуске и без интерполације спољних путања у ниску
команде. Вредности током извршавања (портови, API кључеви) прослеђују се преко `env` објекта подређеног процеса.

### Санитизација грешака (строго правило #12)

Сви одговори о грешкама из `/api/services/*` пролазе кроз `buildErrorBody()` или
`sanitizeErrorMessage()`. Необрађени `err.stack` и `err.message` никада се не враћају
дословно позиваоцу.

---

## 6. Додавање новог уграђеног сервиса

Пратите ових 8 корака. Користите постојеће имплементације у `src/lib/services/installers/`
и `src/app/api/services/` као канонску референцу.

### Корак 1 — Направите инсталатер

Направите `src/lib/services/installers/{name}.ts` по узору на `ninerouter.ts`:

```typescript
export const NAME_PACKAGE = "your-npm-package";
export const NAME_DEFAULT_PORT = 20132; // изаберите слободан порт

export async function install(version = "latest"): Promise<InstallResult> { ... }
export async function update(version = "latest"): Promise<InstallResult> { ... }
export async function uninstall(): Promise<void> { ... }
export function resolveSpawnArgs(apiKey: string, port: number): SpawnArgs { ... }
export async function getInstalledVersion(): Promise<string | null> { ... }
export async function getLatestVersion(): Promise<string | null> { ... }
```

Користите `runNpm(['install', NAME_PACKAGE, '--prefix', dir])` из `installers/utils.ts`
— никада `execSync` нити интерполацију љуске.

### Корак 2 — Региструјте у покретању система

Додајте `ServiceEntry` у низ `SERVICES` у `src/lib/services/bootstrap.ts`:

```typescript
{
  tool: "myservice",
  port: NAME_DEFAULT_PORT,
  healthPath: "/health",
  healthIntervalMs: 5_000,
  stopTimeoutMs: 15_000,
  logsBufferBytes: 5_242_880,
  needsApiKey: true, // false ако API кључ није потребан
}
```

Проширите `buildSpawnArgsFactory()` тако да обрађује `cfg.tool === "myservice"`.

#### Уговор за прикључиве додатке добављача (фаза 1, #7333)

`src/lib/services/providerPlugins/` уводи уговор `ServiceProviderPlugin` који
обједињује поља `ServiceEntry` из `bootstrap.ts` позадинског система и поља шаблона
манифеста из `serviceBackends.ts` у један објекат, уместо да облик истог позадинског
система буде засебно изражен у две неповезане датотеке. У тренутку писања овога
**мигриран је само `9router`** — `bootstrap.ts` изводи свој унос у `SERVICES[]` из
`getServiceProviderPlugin("9router")` (`src/lib/services/providerPlugins/registry.ts`),
при чему при покретању баца грешку ако додатак икада недостаје. `cliproxy`, `mux` и `bifrost`
остају непромењени на раније постојећим уграђеним литералима `SERVICES[]`.

`open-sse/config/providerPluginManifest.ts` је такође добио адитивну помоћну функцију
`createServiceBackendManifestEntry(pluginId, template)` која прави исправно обликован
`ProviderPluginManifestEntry` из уноса `SERVICE_BACKEND_MANIFEST_TEMPLATE` — она
**још увек није** повезана ни са једном активном путањом захтева (ни са `generateProviderPluginManifestFromRegistry()`
ни са `/v1/providers/[provider]/models`); то остаје накнадни задатак након што се уговор
потврди за други позадински систем.

Одложено за наредне PR-ове, прати се у оквиру проблема #7333: миграција `cliproxyapi`
кроз исти регистар, генерализација `mux`/`bifrost` у унију `ServiceBackendPluginId`,
укључивање посебних случајева усмеравања извршилаца (`open-sse/executors/index.ts`,
`open-sse/handlers/chatCore/executorProxy.ts`) у уговор додатка и повезивање
`createServiceBackendManifestEntry()` са активном путањом кода за манифест/моделе.

### Корак 3 — Додајте миграцију и почетне податке базе

Обезбедите да сервис има ред у `version_manager` путем миграције у
`src/lib/db/migrations/`. Ред треба да садржи:

```sql
INSERT OR IGNORE INTO version_manager (tool, status, auto_start, provider_expose)
VALUES ('myservice', 'not_installed', 0, 0);
```

### Корак 4 — Направите 7 API крајњих тачака

У оквиру `src/app/api/services/{name}/`:

```
_lib.ts            помоћна функција getOrInitSupervisor()
install/route.ts   POST — позива installer.install()
start/route.ts     POST — позива supervisor.start()
stop/route.ts      POST — позива supervisor.stop()
restart/route.ts   POST — позива supervisor.restart()
update/route.ts    POST — позива installer.update()
status/route.ts    GET  — спаја тренутни статус и статус из базе
auto-start/route.ts POST — мења стање ознаке auto_start
```

Дељена рута `GET /api/services/[name]/logs` је већ повезана — ту нису
потребне измене.

Делегирајте све одговоре о грешкама кроз `createErrorResponse()` / `buildErrorBody()`.

### Корак 5 — Додавање у LOCAL_ONLY_API_PREFIXES

У `src/server/authz/routeGuard.ts` проверите да ли је `/api/services/` већ наведен.
Ако уведете нови префикс (нпр. `/api/tools/`), додајте га и у
`LOCAL_ONLY_API_PREFIXES` и, ако покреће процесе, у `SPAWN_CAPABLE_PREFIXES`.
Додајте тест у `tests/unit/authz/routeGuard.test.ts`.

### Корак 6 — Додавање картице корисничког интерфејса

Направите `src/app/(dashboard)/dashboard/providers/services/tabs/{Name}ServiceTab.tsx`.
Поново употребите дељене компоненте:

- `ServiceStatusCard` — стање уживо + ознака здравственог стања
- `ServiceLifecycleButtons` — Покрени / Заустави / Поново покрени / Ажурирај
- `ServiceLogsPanel` — SSE праћење краја евиденције (повезује се на `/api/services/{name}/logs`)
- `ApiKeyCard` — приказивање + ротирање кључа (ако је `needsApiKey: true`)

Региструјте картицу у `ServicesPageShell.tsx`.

### Корак 7 — Додавање уноса добављача (ако је сервис циљ рутирања)

Ако уграђени сервис излаже OpenAI-компатибилну крајњу тачку `/v1/chat/completions`:

1. Додајте унос добављача у `src/shared/constants/providers.ts` са `isEmbeddedService: true`.
2. Направите `open-sse/executors/{name}.ts` који проширује `BaseExecutor`. При сваком захтеву поново потражите порт и
   API кључ (никада их не кеширајте у конструктору). Вратите одговор `503 service_not_running`
   када стање надзорника није `"running"`.
3. Региструјте моделе у `open-sse/config/providerRegistry.ts` са префиксом сервиса
   (нпр. `myservice/sub/model`). `modelSync.ts` ће их одржавати ажурним.

### Корак 8 — Документовање и тестирање

1. Ажурирајте `docs/frameworks/EMBEDDED-SERVICES.md` (ову датотеку) — додајте сервис у
   табелу у §1 и све нове крајње тачке у §4.
2. Додајте јединичне тестове у `tests/unit/services/` (животни циклус, инсталатер, облик API-ја).
3. Додајте интеграциони тест у `tests/integration/services/` (иза `RUN_SERVICES_INT=1`).
4. Ажурирајте `docs/openapi.yaml` новим крајњим тачкама.

---

## 7. Решавање проблема

### Сервис се не покреће

**Симптоми:** Дугме за покретање враћа 503, а стање остаје `"error"` или `"starting"`.

**Контролна листа:**

1. Проверите `GET /api/services/{name}/logs` (или панел Logs на контролној табли). Потражите
   редове као што су `Error: ENOENT`, `address already in use` или `Cannot find module`.
2. Проверите да ли је `npm` у PATH-у: покрените `which npm` са истог корисничког налога под којим се извршава OmniRoute.
3. Проверите да ли је сервис инсталиран: у одговору `GET /api/services/{name}/status`
   проверите `installedVersion`. Ако је `null`, прво покрените инсталацију.
4. Проверите да ли `DATA_DIR/services/{name}/node_modules/` постоји и није празан.
5. У одговору о статусу проверите поље `lastError` за пречишћени разлог прекида рада.

---

### Хладно покретање је споро (> 10 s до достизања стања `running`)

**Симптоми:** Стање дуго остаје `"starting"` пре него што пређе у `"running"` или `"error"`.

**Објашњење:** Хладно покретање 9Router-а обухвата увоз великих стабала зависности (DNS,
тунелски и MITM модули). Подразумевани интервал провере стања је 2 s, са 3 покушаја пре него што
надзорник прогласи истек времена (али наставља са проверама).

**Решење:** Временско ограничење за `healthIntervalMs` и `waitForHealthy`
(`healthIntervalMs * 3`) може се конфигурисати у `bootstrap.ts`. За сервисе којима је потребно више
времена за покретање, повећајте `healthIntervalMs` на 5000 и `stopTimeoutMs` на 30 000.

---

### Сукоб портова (`EADDRINUSE`)

**Симптоми:** Евиденција приказује `address already in use :::20130`.

**Узроци:**

- Други процес већ користи порт 20130.
- Претходни процес 9Router-а није у потпуности заустављен (зомби PID).

**Решење:**

1. Промените подразумевани порт помоћу променљиве окружења `NINEROUTER_PORT` у датотеци `.env`.
2. Пронађите и прекините конфликтни процес: `lsof -ti :20130 | xargs kill -9`.
3. Порт се може конфигурисати за сваки сервис у `bootstrap.ts` путем поља `port`.

**Напомена:** 9Router подразумевано користи порт 20130 како би се избегло преклапање са
подразумеваним портом 20128 који користи OmniRoute.

---

### Дозвола одбијена (EACCES) током инсталације

**Симптоми:** Инсталација враћа 500, а евиденција приказује `EACCES` или `permission denied`.

**Узроци:**

- OmniRoute процес нема дозволу за упис у `DATA_DIR` или његов надређени директоријум.
- Извршавање у Docker-у без root привилегија и без приступа за упис у мапирани волумен.

**Решење:**

1. Проверите `DATA_DIR` (подразумевано: `~/.omniroute/`): `ls -la ~/.omniroute/`
2. Уверите се да је корисник под којим се извршава OmniRoute власник директоријума: `chown -R $USER ~/.omniroute/`
3. У Docker-у се уверите да монтирани волумен има одговарајуће дозволе за корисника контејнера.

---

### Ажурирање не успева (истек времена за `npm install` или мрежна грешка)

**Симптоми:** Ажурирање враћа 500 са `InstallError`, а евиденција приказује истек мрежног времена.

**Контролна листа:**

1. Потврдите да је npm регистар доступан: `npm ping`.
2. Проверите да ли се користи корпоративни proxy: `npm config get proxy`, `npm config get https-proxy`.
3. Покушајте ручну инсталацију: `npm install {package}@latest --prefix ~/.omniroute/services/{name}/`.
4. Ако сте у окружењу без приступа мрежи, унапред преузмите tarball и употребите `npm install /path/to/tarball.tgz`.

---

### Сервис приказује стање `"error"` одмах након покретања (брзи пад)

**Симптоми:** Стање прелази из `"starting"` у `"error"` за мање од 5 секунди.
`lastError` приказује `"Fast crash (exited with code 1)"`.

**Контролна листа:**

1. Прочитајте цео завршни део евиденције: `GET /api/services/{name}/logs?tail=500`.
2. Чест узрок: недостају променљиве окружења које сервис очекује.
3. За 9Router: проверите да ли су `NINEROUTER_DISABLE_MITM=true` и
   `NINEROUTER_DISABLE_TUNNEL=true` у окружењу прослеђеном приликом покретања процеса (погледајте
   `installers/ninerouter.ts` `resolveSpawnArgs`).

---

## 8. Честа питања

**П: Могу ли да изложим крајње тачке уграђених сервиса клијентима који не користе loopback?**

Не. Ниво LOCAL_ONLY је намеран (строго правило #17). Руте које могу да покрену
`npm install` или `node` процесе не смеју бити доступне саобраћају који не долази преко loopback-а,
јер би процурели JWT преко тунела (Cloudflare, Ngrok, Tailscale) у супротном
омогућио произвољно покретање процеса. Не постоји изузетак за
`/api/services/` — за разлику од `/api/mcp/`, он је искључен са листе заобилажења
manage опсега. Погледајте `docs/security/ROUTE_GUARD_TIERS.md`.

---

**П: Да ли ће 9Router и CLIProxyAPI бити доступни у продукционим/cloud окружењима?**

Да. Оба сервиса прате исти local-first модел као и сам OmniRoute. Покрећу се
на истој машини и комуницирају преко loopback-а. „Продукција“ овде означава VPS
или локални сервер на којем је OmniRoute постављен, а не удаљеног cloud провајдера.

---

**П: Како да отклоним грешке у супервизору?**

1. Пратите SSE ток логова: `curl -N http://localhost:20128/api/services/9router/logs`.
2. Проверите структуриране логове у OmniRoute pino излазу филтриране према
   простору имена `service:supervisor`.
3. Прегледајте ред у бази података: `sqlite3 ~/.omniroute/omniroute.db "SELECT * FROM version_manager WHERE tool='9router'"`.
4. Користите `GET /api/services/9router/status` да бисте у једном позиву видели тренутно активно стање, PID, здравствено стање
   и `lastError`.

---

**П: Супервизор приказује `health: "degraded"` или `health: "unknown"`, али је стање `"running"`. Да ли је то проблем?**

`"degraded"` значи да је здравствена провера вратила одговор који није 200. `"unknown"` значи да
ниједна провера још није завршена (истовремено извршавање са првим циклусом провере). Оба стања су пролазна током покретања.
Ако здравствено стање остане `"degraded"` дуже од `healthIntervalMs * 3` ms након
стања `"running"`, уграђени сервис је покренут, али његов HTTP API не одговара. Проверите
да ли је порт у одговору о статусу исправан и да ли сервис заиста
ослушкује на том порту.

---

**П: Могу ли да променим 9Router API кључ без потпуног поновног покретања?**

Не. API кључ се прослеђује сервису 9Router путем променљиве окружења у тренутку покретања процеса.
Променљиве окружења се не могу мењати у процесу који је већ покренут. `POST .../rotate-key`
аутоматски зауставља и поново покреће сервис како би применио нови кључ. Ротација кључа
ступа на снагу унутар вредности `stopTimeoutMs` сервиса (подразумевано 15 s), увећане за време
његовог покретања.

---

**П: Које је ограничење кружног бафера и шта се дешава када се попуни?**

Сваки сервис има наменски кружни бафер од 5 MB. Када се бафер попуни, најстарији
редови логова се уклањају како би се ослободио простор за нове. SSE догађај `snapshot` враћа
најновије редове унутар ограничења `tail`. Логови се не чувају трајно на диску осим ако
`logsBufferPath` није подешен у реду базе података.

---

## Погледајте и

- `docs/security/ROUTE_GUARD_TIERS.md` — детаљи о нивоу LOCAL_ONLY
- `docs/architecture/CODEBASE_DOCUMENTATION.md` — §3.2 мапирање модула уграђених сервиса
- `docs/architecture/ARCHITECTURE.md` — контекст на нивоу система
- `docs/openapi.yaml` — машински читљиве дефиниције крајњих тачака
- `CLAUDE.md` §„Додавање новог уграђеног сервиса“ — контролна листа за брзи преглед
