# Cursor API provider and the Cursor CLI passthrough (Filipino)

🌐 **Languages:** 🇺🇸 [English](../../../../providers/CURSOR-API-KEY-AND-CLI.md) · 🇪🇹 [am](../../../am/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇸🇦 [ar](../../../ar/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇦🇿 [az](../../../az/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇧🇬 [bg](../../../bg/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇧🇩 [bn](../../../bn/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇨🇿 [cs](../../../cs/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇩🇰 [da](../../../da/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇩🇪 [de](../../../de/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇬🇷 [el](../../../el/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇪🇸 [es](../../../es/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇪🇪 [et](../../../et/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇮🇷 [fa](../../../fa/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇫🇮 [fi](../../../fi/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇫🇷 [fr](../../../fr/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇮🇪 [ga](../../../ga/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇮🇳 [gu](../../../gu/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇳🇬 [ha](../../../ha/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇮🇱 [he](../../../he/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇮🇳 [hi](../../../hi/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇭🇷 [hr](../../../hr/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇭🇺 [hu](../../../hu/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇦🇲 [hy](../../../hy/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇮🇩 [id](../../../id/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇳🇬 [ig](../../../ig/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇮🇹 [it](../../../it/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇯🇵 [ja](../../../ja/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇬🇪 [ka](../../../ka/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇰🇭 [km](../../../km/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇮🇳 [kn](../../../kn/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇰🇷 [ko](../../../ko/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇱🇹 [lt](../../../lt/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇱🇻 [lv](../../../lv/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇮🇳 [ml](../../../ml/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇮🇳 [mr](../../../mr/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇲🇾 [ms](../../../ms/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇲🇹 [mt](../../../mt/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇲🇲 [my](../../../my/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇳🇵 [ne](../../../ne/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇳🇱 [nl](../../../nl/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇳🇴 [no](../../../no/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇮🇳 [or](../../../or/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇮🇳 [pa](../../../pa/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇵🇱 [pl](../../../pl/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇵🇹 [pt](../../../pt/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇧🇷 [pt-BR](../../../pt-BR/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇷🇴 [ro](../../../ro/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇷🇺 [ru](../../../ru/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇱🇰 [si](../../../si/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇸🇰 [sk](../../../sk/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇸🇮 [sl](../../../sl/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇷🇸 [sr](../../../sr/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇸🇪 [sv](../../../sv/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇰🇪 [sw](../../../sw/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇮🇳 [ta](../../../ta/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇮🇳 [te](../../../te/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇹🇭 [th](../../../th/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇹🇷 [tr](../../../tr/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇺🇦 [uk-UA](../../../uk-UA/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇵🇰 [ur](../../../ur/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇺🇿 [uz](../../../uz/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇻🇳 [vi](../../../vi/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇳🇬 [yo](../../../yo/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇨🇳 [zh-CN](../../../zh-CN/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇹🇼 [zh-TW](../../../zh-TW/docs/providers/CURSOR-API-KEY-AND-CLI.md)

---

Dalawang paraan upang gamitin ang Cursor sa likod ng OmniRoute nang walang IDE session:

1. **Provider na `cursor-api`** (card na "Cursor API", alias na `cua`): isang provider
   na gumagamit ng API key at naglalaman ng Cursor user API key (`crsr_…`, na binubuo sa
   `https://cursor.com/dashboard/api`). Maa-access ng alinmang OmniRoute client ang
   mga modelo ng Cursor sa pamamagitan ng `/v1/chat/completions` bilang `cursor-api/<model>` o
   `cua/<model>`, kasama ang karaniwang mga layer ng quota, fallback, at logging. Hindi
   nagbabago ang IDE provider (`cursor`, OAuth/IDE session).
2. **Cursor CLI passthrough**: ituro ang Cursor CLI (`agent`) sa OmniRoute upang
   ang bawat RPC na ginagawa ng CLI ay ma-authenticate gamit ang isang OmniRoute API key, ma-forward
   sa Cursor gamit ang credential ng isang `cursor-api` connection, at maitala sa
   pahina ng Logs.

## Bakit ipinagpapalit ang key

Tinatanggihan ng `api2.cursor.sh` ang isang raw na `crsr_…` key bilang Bearer token (401). Ang Cursor
CLI ay nag-POST muna ng key sa `/auth/exchange_user_api_key` at tumatanggap ng session
JWT na mawawalan ng bisa pagkalipas ng isang oras; ang ibinalik na `refreshToken` ay may parehong
`exp`, kaya ang pag-refresh ay nangangahulugang muling pagpapalit ng key.
Ginagawa ng `open-sse/services/cursorApiKeyAuth.ts` ang pagpapalit na iyon, nagka-cache ng isang session
token para sa bawat key, muling nagpapalit limang minuto bago ang pag-expire, at inaalis ang naka-cache na
token kapag sumagot ang Cursor ng 401. Tinatawag ito ng `CursorExecutor` bago mismo buksan
ang upstream stream para sa mga `cursor-api` connection.

## Ang provider na `cursor-api`

Registry: `open-sse/config/providers/registry/cursor/index.ts`
(`cursor_apiProvider`, `authType: "apikey"`, parehong `format`, `baseUrl`, at
`models` gaya ng `cursor`). Catalog card:
`src/shared/constants/providers/apikey/specialty-media.ts`. Executor map:
`open-sse/executors/index.ts` (`"cursor-api"` / `cua` →
`new CursorExecutor("cursor-api")`).

Dashboard: Providers → Cursor API → Add API key.

REST:

```bash
curl -sS -X POST http://localhost:20128/api/providers \
  -H "Content-Type: application/json" \
  -d '{"provider":"cursor-api","name":"cursor-api-key","apiKey":"crsr_…","priority":1}'
```

Pagkatapos:

```bash
curl -sS http://localhost:20128/v1/chat/completions \
  -H "Authorization: Bearer <omniroute-api-key>" \
  -H "Content-Type: application/json" \
  -d '{"model":"cursor-api/auto","messages":[{"role":"user","content":"say PONG"}]}'
```

Mga tala:

- Ang listahan ng mga modelo para sa `cursor-api` ay nagmumula sa static na Cursor registry (ang
  parehong listahan na ginagamit bilang fallback ng IDE provider); hindi kailangang mag-install ng
  `cursor-agent` sa OmniRoute host.
- Ang `POST /api/providers/{id}/refresh-cursor` ay para lamang sa `cursor` IDE provider;
  walang IDE session na kailangang i-renew ang mga `cursor-api` connection.

## Mga native na model ID at effort

Para sa `cursor` / `cu` at `cursor-api` / `cua`, pinananatiling buo ng nakabahaging Claude-effort normalizer
ang hiniling na model ID. Maaaring mag-advertise ang Cursor ng suffix gaya ng
`-low` bilang bahagi ng isang tunay na model ID, sa halip na bilang OmniRoute effort alias.
Pinapanatili ng Cursor executor ang eksaktong katugma mula sa live catalog; kapag walang
katugma, ang kasalukuyang model resolver nito ang nangangasiwa sa suffix-to-parameter fallback.

Hindi nito binabago ang effort normalization para sa mga direktang Claude, Claude-compatible,
o Vertex route. Nakadepende pa rin ang availability sa catalog at entitlement ng napiling Cursor account.

## Passthrough ng Cursor CLI

Ruta: `src/app/api/cursor-cli/[...path]/route.ts` →
`open-sse/handlers/cursorCliProxy.ts`. Nakarehistro ang prefix na `/api/cursor-cli/` sa
`src/shared/constants/publicApiRoutes.ts` dahil ipinapatupad ng handler
ang sarili nitong authentication:

| Path                                                                                                                            | Auth na inaasahan mula sa CLI | Ginagawa ng OmniRoute                                                                                                                                                                   |
| ------------------------------------------------------------------------------------------------------------------------------- | ----------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `POST /auth/exchange_user_api_key`                                                                                              | `Bearer <OmniRoute API key>`  | Bine-validate ang key, gumagawa ng 1h HS256 JWT (nilagdaan gamit ang `JWT_SECRET`), at ibinabalik ito                                                                                   |
| bawat iba pang path (`/aiserver.v1.*`, `/agent.v1.AgentService/RunSSE`, `/aiserver.v1.BidiService/BidiAppend`, `/v1/traces`, …) | `Bearer <that JWT>`           | Bine-verify ang issuer/audience/expiry, pumipili ng aktibong `cursor-api` connection, pinapalitan ang Authorization header ng ipinalit na Cursor token, at ini-stream pabalik ang tugon |

Dine-decode ng CLI ang `exp` mula sa anumang token na natatanggap nito, kaya kapag opaque
token ang ibinigay dito, muli itong nagsasagawa ng exchange bago ang halos bawat request; iniiwasan
ito ng ginawang JWT. Kapag nakatanggap ng 401 mula sa OmniRoute, muling nagsasagawa ng exchange ang CLI.

### Pag-setup

1. Gumawa ng OmniRoute API key (Dashboard → API keys) at isang `cursor-api`
   connection.
2. Itakda ang CLI na gumamit ng HTTP/1.1 para sa agent stream. Sa
   `~/.cursor/cli-config.json`:

   ```json
   { "network": { "useHttp1ForAgent": true } }
   ```

   Kung wala ito, bubuksan ng CLI ang agent turn sa HTTP/2 papunta sa isang hiwalay
   na naka-configure na agent host, at ang mga control-plane RPC lamang ang dadaan sa
   endpoint.

3. Patakbuhin ang CLI gamit ang OmniRoute:

   ```bash
   export CURSOR_API_ENDPOINT=http://localhost:20128/api/cursor-cli
   export CURSOR_API_KEY=<omniroute-api-key>
   agent -p --trust "Reply with exactly OK"
   ```

Lalabas ang bawat hop sa Logs bilang provider na `cursor-api`, request type na `cursor-cli`,
path na `/api/cursor-cli/<rpc>`, na iniuugnay sa OmniRoute API key at sa
connection na nagserbisyo rito.

### Mga uri ng pagkabigo

| Sitwasyon                                              | Tugon sa CLI                                             |
| ------------------------------------------------------ | -------------------------------------------------------- |
| Hindi kilalang OmniRoute key at `REQUIRE_API_KEY=true` | 401 `unauthenticated` sa exchange                        |
| `REQUIRE_API_KEY=false`                                | anonymous session (ginagaya ang gawi ng `/v1/*`)         |
| Nag-expire / banyaga / binagong session JWT            | 401, muling nagsasagawa ng exchange ang CLI              |
| Binawi ang OmniRoute API key pagkatapos ng exchange    | 401 sa susunod na RPC                                    |
| Walang aktibong `cursor-api` connection                | 503 `unavailable`                                        |
| Tinanggihan ng Cursor ang key ng connection            | 401 `unauthenticated`, inaalis ang naka-cache na session |
| Hindi maabot ang upstream                              | 502 `unavailable` (nilinis na mensahe)                   |
| Hindi nakatakda ang `JWT_SECRET`                       | 503 sa exchange                                          |
