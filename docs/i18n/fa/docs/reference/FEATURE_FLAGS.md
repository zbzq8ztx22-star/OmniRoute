# Feature Flags (فارسی)

🌐 **Languages:** 🇺🇸 [English](../../../../reference/FEATURE_FLAGS.md) · 🇪🇹 [am](../../../am/docs/reference/FEATURE_FLAGS.md) · 🇸🇦 [ar](../../../ar/docs/reference/FEATURE_FLAGS.md) · 🇦🇿 [az](../../../az/docs/reference/FEATURE_FLAGS.md) · 🇧🇬 [bg](../../../bg/docs/reference/FEATURE_FLAGS.md) · 🇧🇩 [bn](../../../bn/docs/reference/FEATURE_FLAGS.md) · 🇨🇿 [cs](../../../cs/docs/reference/FEATURE_FLAGS.md) · 🇩🇰 [da](../../../da/docs/reference/FEATURE_FLAGS.md) · 🇩🇪 [de](../../../de/docs/reference/FEATURE_FLAGS.md) · 🇬🇷 [el](../../../el/docs/reference/FEATURE_FLAGS.md) · 🇪🇸 [es](../../../es/docs/reference/FEATURE_FLAGS.md) · 🇪🇪 [et](../../../et/docs/reference/FEATURE_FLAGS.md) · 🇫🇮 [fi](../../../fi/docs/reference/FEATURE_FLAGS.md) · 🇫🇷 [fr](../../../fr/docs/reference/FEATURE_FLAGS.md) · 🇮🇪 [ga](../../../ga/docs/reference/FEATURE_FLAGS.md) · 🇮🇳 [gu](../../../gu/docs/reference/FEATURE_FLAGS.md) · 🇳🇬 [ha](../../../ha/docs/reference/FEATURE_FLAGS.md) · 🇮🇱 [he](../../../he/docs/reference/FEATURE_FLAGS.md) · 🇮🇳 [hi](../../../hi/docs/reference/FEATURE_FLAGS.md) · 🇭🇷 [hr](../../../hr/docs/reference/FEATURE_FLAGS.md) · 🇭🇺 [hu](../../../hu/docs/reference/FEATURE_FLAGS.md) · 🇦🇲 [hy](../../../hy/docs/reference/FEATURE_FLAGS.md) · 🇮🇩 [id](../../../id/docs/reference/FEATURE_FLAGS.md) · 🇳🇬 [ig](../../../ig/docs/reference/FEATURE_FLAGS.md) · 🇮🇹 [it](../../../it/docs/reference/FEATURE_FLAGS.md) · 🇯🇵 [ja](../../../ja/docs/reference/FEATURE_FLAGS.md) · 🇬🇪 [ka](../../../ka/docs/reference/FEATURE_FLAGS.md) · 🇰🇭 [km](../../../km/docs/reference/FEATURE_FLAGS.md) · 🇮🇳 [kn](../../../kn/docs/reference/FEATURE_FLAGS.md) · 🇰🇷 [ko](../../../ko/docs/reference/FEATURE_FLAGS.md) · 🇱🇹 [lt](../../../lt/docs/reference/FEATURE_FLAGS.md) · 🇱🇻 [lv](../../../lv/docs/reference/FEATURE_FLAGS.md) · 🇮🇳 [ml](../../../ml/docs/reference/FEATURE_FLAGS.md) · 🇮🇳 [mr](../../../mr/docs/reference/FEATURE_FLAGS.md) · 🇲🇾 [ms](../../../ms/docs/reference/FEATURE_FLAGS.md) · 🇲🇹 [mt](../../../mt/docs/reference/FEATURE_FLAGS.md) · 🇲🇲 [my](../../../my/docs/reference/FEATURE_FLAGS.md) · 🇳🇵 [ne](../../../ne/docs/reference/FEATURE_FLAGS.md) · 🇳🇱 [nl](../../../nl/docs/reference/FEATURE_FLAGS.md) · 🇳🇴 [no](../../../no/docs/reference/FEATURE_FLAGS.md) · 🇮🇳 [or](../../../or/docs/reference/FEATURE_FLAGS.md) · 🇮🇳 [pa](../../../pa/docs/reference/FEATURE_FLAGS.md) · 🇵🇭 [phi](../../../phi/docs/reference/FEATURE_FLAGS.md) · 🇵🇱 [pl](../../../pl/docs/reference/FEATURE_FLAGS.md) · 🇵🇹 [pt](../../../pt/docs/reference/FEATURE_FLAGS.md) · 🇧🇷 [pt-BR](../../../pt-BR/docs/reference/FEATURE_FLAGS.md) · 🇷🇴 [ro](../../../ro/docs/reference/FEATURE_FLAGS.md) · 🇷🇺 [ru](../../../ru/docs/reference/FEATURE_FLAGS.md) · 🇱🇰 [si](../../../si/docs/reference/FEATURE_FLAGS.md) · 🇸🇰 [sk](../../../sk/docs/reference/FEATURE_FLAGS.md) · 🇸🇮 [sl](../../../sl/docs/reference/FEATURE_FLAGS.md) · 🇷🇸 [sr](../../../sr/docs/reference/FEATURE_FLAGS.md) · 🇸🇪 [sv](../../../sv/docs/reference/FEATURE_FLAGS.md) · 🇰🇪 [sw](../../../sw/docs/reference/FEATURE_FLAGS.md) · 🇮🇳 [ta](../../../ta/docs/reference/FEATURE_FLAGS.md) · 🇮🇳 [te](../../../te/docs/reference/FEATURE_FLAGS.md) · 🇹🇭 [th](../../../th/docs/reference/FEATURE_FLAGS.md) · 🇹🇷 [tr](../../../tr/docs/reference/FEATURE_FLAGS.md) · 🇺🇦 [uk-UA](../../../uk-UA/docs/reference/FEATURE_FLAGS.md) · 🇵🇰 [ur](../../../ur/docs/reference/FEATURE_FLAGS.md) · 🇺🇿 [uz](../../../uz/docs/reference/FEATURE_FLAGS.md) · 🇻🇳 [vi](../../../vi/docs/reference/FEATURE_FLAGS.md) · 🇳🇬 [yo](../../../yo/docs/reference/FEATURE_FLAGS.md) · 🇨🇳 [zh-CN](../../../zh-CN/docs/reference/FEATURE_FLAGS.md) · 🇹🇼 [zh-TW](../../../zh-TW/docs/reference/FEATURE_FLAGS.md)

---

> کلیدهای runtime که رفتار OmniRoute را **بدون استقرار مجدد** تغییر میدهند.
> تمام فلگهای فهرستشده در اینجا در
> [`src/shared/constants/featureFlagDefinitions.ts`](../../src/shared/constants/featureFlagDefinitions.ts)
> تعریف شدهاند — منبع واحد حقیقت. داشبورد و REST API هر دو از
> آن فایل میخوانند، بنابراین جدول زیر بهگونهای تولید شده است که دقیقاً بهصورت 1:1 با آن مطابقت داشته باشد.

---

## فلگهای قابلیت چیستند

فلگ قابلیت یک کلید نامگذاریشده (از نوع boolean یا enum) است که مقدار آن را میتوان در
runtime تغییر داد و در پایگاه داده ذخیره کرد، بدون اینکه نیاز به استقرار مجدد فرایند باشد. هر
فلگ با یک `FeatureFlagDefinition` توصیف میشود که شامل `key`، `label`،
`description`، `category`، `defaultValue`، `type` و راهنمای `requiresRestart` است.

### ترتیب تعیین مقدار

**مقدار مؤثر** یک فلگ توسط
[`resolveFeatureFlag()`](../../src/shared/utils/featureFlags.ts) و با ترتیب
اولویت زیر تعیین میشود (بالاترین اولویت برنده است):

1. **بازنویسی DB** — مقداری که در جدول `key_value` و در فضای نام
   `feature_flags` ذخیره شده است (از طریق داشبورد یا REST API تنظیم میشود).
2. **متغیر محیطی** — `process.env[<KEY>]`، در صورتی که تنظیم شده و خالی نباشد.
3. **مقدار پیشفرض تعریف** — مقدار `defaultValue` از `featureFlagDefinitions.ts`.

یک فلگ boolean زمانی **فعال** در نظر گرفته میشود که مقدار مؤثر آن `"true"`،
`"1"` یا `"yes"` باشد (`isFeatureFlagEnabled()` را ببینید).

> [!NOTE]
> بیشتر فلگها همچنین یک متغیر محیطی متناظر با **همان نام** دارند
> که در [`ENVIRONMENT.md`](./ENVIRONMENT.md) مستند شده است. بازنویسی DB فلگ
> نسبت به آن متغیر محیطی اولویت دارد. فلگی با
> `requiresRestart: true` بلافاصله ذخیره میشود، اما فقط هنگام راهاندازی
> فرایند دوباره خوانده میشود — تغییر آن، بنر **«راهاندازی مجدد سرور»** را در داشبورد نمایش میدهد.

---

## فهرست پرچمها

۷۵ پرچم در ۶ دسته. **پیشفرض** همان مقدار پیشفرض تعریفشده است — مقداری که
وقتی نه بازنویسی پایگاه داده و نه متغیر محیطی وجود داشته باشد، استفاده میشود.

### امنیت (۱۰)

| کلید                                    | نوع     | پیشفرض   | توضیحات                                                                                                                                                                                                                                                                                      |
| --------------------------------------- | ------- | -------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `REQUIRE_API_KEY`                       | boolean | `false`  | برای تمام درخواستهای ورودی، کلید API الزامی باشد.                                                                                                                                                                                                                                            |
| `INPUT_SANITIZER_ENABLED`               | boolean | `true`   | پاکسازی ورودی برای تمام درخواستها فعال شود.                                                                                                                                                                                                                                                  |
| `INJECTION_GUARD_MODE`                  | enum    | `off`    | حالت محافظت در برابر تزریق پرامپت. مقادیر: `off`، `warn`، `block`، `redact`.                                                                                                                                                                                                                 |
| `PII_REDACTION_ENABLED`                 | boolean | `false`  | اطلاعات هویتی شخصی (PII) از درخواستها حذف شود (مستقل از `INPUT_SANITIZER_MODE`).                                                                                                                                                                                                             |
| `PII_RESPONSE_SANITIZATION`             | boolean | `false`  | اطلاعات هویتی شخصی (PII) از پاسخهای ارائهدهنده پاکسازی شود.                                                                                                                                                                                                                                  |
| `PII_RESPONSE_SANITIZATION_MODE`        | enum    | `redact` | حالت پاکسازی اطلاعات هویتی شخصی از پاسخها. مقادیر: `redact`، `warn`، `block`، `off`.                                                                                                                                                                                                         |
| `OUTBOUND_SSRF_GUARD_ENABLED`           | boolean | `true`   | درخواستهای خروجی به محدودههای IP خصوصی/داخلی مسدود شوند.                                                                                                                                                                                                                                     |
| `ALLOW_API_KEY_REVEAL`                  | boolean | `false`  | به کاربران احراز هویتشده داشبورد اجازه داده شود بهجای مشاهده صرفاً مقادیر پوشاندهشده، کلیدهای API ذخیرهشده را آشکار کنند.                                                                                                                                                                    |
| `AUTH_LOG_INCLUDE_ACCOUNT_ID`           | boolean | `false`  | پیشوند حساب در خطوط گزارش AUTH درج شود (برای مثال، «استفاده از حساب <provider>: abc12345...»). این گزینه بهطور پیشفرض غیرفعال است تا شناسههای حساب در گزارشهای مشترک/چندمستاجری فرایند پوشانده شوند. این گزینه مستقل از حالت اشکالزدایی است؛ تغییر حالت اشکالزدایی باعث آشکار شدن آن نمیشود. |
| `OMNIROUTE_OIDC_DISABLE_PASSWORD_LOGIN` | boolean | `false`  | هنگامی که OIDC فعال است، ورود با گذرواژه غیرفعال شود تا کاربران فقط از طریق ورود یکپارچه OIDC احراز هویت کنند. هنگامی که غیرفعال باشد (پیشفرض)، هم ورود با گذرواژه و هم OIDC در دسترس هستند.                                                                                                 |

### شبکه (۱۷)

| کلید                                            | نوع     | پیشفرض  | راهاندازی مجدد | توضیحات                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| ----------------------------------------------- | ------- | ------- | -------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `ENABLE_TLS_FINGERPRINT`                        | بولی    | `false` | ✓              | حالت پنهانسازی اثرانگشت TLS را فعال میکند.                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `AUDIO_REMOTE_PROVIDER_NODES`                   | بولی    | `false` |                | به مسیرهای /v1/audio/* اجازه میدهد از گرههای ارائهدهنده سازگار با OpenAI که خارج از localhost میزبانی میشوند، استفاده کنند. بهطور پیشفرض غیرفعال است — مسیریابی صوت به یک میزبان راهدور، هویت خروجی را تغییر میدهد و باید تصمیمی صریح از سوی اپراتور باشد. گرههای loopback همیشه مجاز و بدون تأثیر هستند.                                                                                                                                                                                 |
| `RERANK_REMOTE_PROVIDER_NODES`                  | بولی    | `false` |                | به POST /v1/rerank (و مرحله بازرتبهبندی loopback موتور حافظه) اجازه میدهد از گرههای ارائهدهنده سازگار با OpenAI که خارج از localhost میزبانی میشوند، استفاده کند. بهطور پیشفرض غیرفعال است — مسیریابی به یک میزبان راهدور، هویت خروجی را تغییر میدهد و باید تصمیمی صریح از سوی اپراتور باشد. گرههای loopback همیشه مجاز هستند؛ گرههای راهدور باید از سیاست URL خروجی ارائهدهنده نیز عبور کنند.                                                                                            |
| `PROXY_AUTO_SELECT_ENABLED`                     | بولی    | `false` |                | وقتی هیچ پراکسیای به یک اتصال اختصاص داده نشده باشد، نخستین پراکسی فعال را بهطور خودکار از رجیستری انتخاب میکند. بهطور پیشفرض غیرفعال است (در غیر این صورت، هر پراکسی موجود در رجیستری به یک گزینه جایگزین سراسری تبدیل میشود — #3332).                                                                                                                                                                                                                                                   |
| `OMNIROUTE_CONTROL_PLANE_PROXY_DIRECT_FALLBACK` | بولی    | `false` |                | به جریانهای اعتبارسنجی OAuth و ارائهدهنده اجازه میدهد وقتی پیشبررسیهای دسترسپذیری پراکسی ناموفق هستند، یک پراکسی ثابتشده را دور بزنند و مستقیماً متصل شوند. بهطور پیشفرض غیرفعال است، زیرا این کار میتواند IP خروجی را تغییر دهد.                                                                                                                                                                                                                                                         |
| `NETWORK_ROTATION_SHARED_EGRESS_GUARD`          | بولی    | `true`  |                | هنگام بروز یک استثنای شبکه (پایان مهلت، رد یا بازنشانی اتصال) در یک اجراکننده چرخشی چندحسابی، اگر حساب ناموفق پراکسی اختصاصی نداشته باشد، یک دوره انتظار کوتاه اعمال میکند و بهجای تلاش مجدد برای تکتک حسابها، دیگر حسابهای بدون پراکسی را در ادامه درخواست نادیده میگیرد. بهطور پیشفرض فعال است (ایمن: IP خروجی تغییر نمیکند و فقط خطر تأخیر/دوره انتظار را برای حسابهای دارای خروجی مشترک کاهش میدهد). برای بازگرداندن انتشار فوری در نخستین خطای حساب بدون پراکسی، آن را غیرفعال کنید. |
| `PROXY_SKIP_RECENTLY_FAILED`                    | بولی    | `false` |                | استخرهای پراکسی و چرخش هر حساب در opencode، ارائه مجدد پراکسیای را که بهتازگی ناموفق بوده است (بررسی TCP ردشده یا دریافت پاسخ 429 از طریق آن) برای مدتی در هر فرایند متوقف میکنند؛ این مدت با هر تکرار دو برابر میشود تا به سقف تعیینشده برسد. هیچ وضعیت پراکسیای ثبت نمیشود؛ اگر همه گزینهها کنار گذاشته شوند، انتخاب بدون تغییر باقی میماند. بهطور پیشفرض غیرفعال است.                                                                                                                  |
| `PROXY_POOL_EGRESS_OBSERVATION`                 | بولی    | `false` |                | در داشبورد و زیر یک استخر پراکسی، تعداد IPهای خروجی مشاهدهشدهای را که طی 24 ساعت گذشته به اعضای آن سرویس دادهاند و تعداد اتصالهایی را که از آنها استفاده کردهاند، نمایش میدهد. فقطخواندنی است، از گزارش پراکسی محاسبه میشود و هرگز برای مسیریابی استفاده نمیشود. بهطور پیشفرض غیرفعال است.                                                                                                                                                                                                |
| `OPENCODE_RESPONSES_STALL_ROTATION`             | بولی    | `false` |                | برای اجراکننده OpenCode، نخستین بایت بدنه پاسخ جریانی Responses را پایش میکند (بازه: `RESPONSES_FIRST_BYTE_TIMEOUT_MS`، پیشفرض `15000`). یک جریان Responses با وضعیت 2xx که پس از پایان این بازه همچنان ساکت بماند، متوقفشده تلقی میشود: حساب وارد دوره انتظار میشود و درخواست یکبار به حساب بعدی منتقل میگردد؛ توقف دوم باعث شکست فوری میشود. بهطور پیشفرض غیرفعال است: جریانهای متوقفشده تا پایان مهلت آمادگی جریان، مطابق رفتار فعلی منتظر میمانند.                                    |
| `OPENCODE_USER_BLOCKED_ROTATION`                | boolean | `false` |                | اجراکننده OpenCode: در صورت دریافت پاسخ 403/451 حاوی امتناع `user_blocked` (نه محدودیت جغرافیایی و نه رد شدن بهدلیل اثرانگشت Cloudflare)، حساب ردشده را موقتاً از گردش خارج کرده و حداکثر یکبار در هر درخواست به حساب بعدی منتقل میشود؛ امتناع دوم بدون تغییر و بدون ثبت موفقیت بازگردانده میشود. بهطور پیشفرض غیرفعال است: مسیریابی برای دور زدن مسدودسازی کاربر در سرویس بالادستی ممکن است بهمنزلهٔ اجتناب از محدودیت تلقی شود و این علامت را در سراسر ناوگان گسترش دهد.                |
| `OPENCODE_TRANSIENT_FAILOVER_BACKOFF`           | boolean | `false` |                | چرخش OpenCode: پس از دو خرابی موقت و متوالی در سرویس بالادستی (5xx یا پاسخ 400 خالی)، پیش از رفتن به حساب بعدی مکث میکند — ابتدا 1.5 ثانیه و با هر خرابی بعدی دو برابر میشود، با سقف 6 ثانیه برای هر مکث و 10 ثانیه برای هر درخواست؛ در صورت قطع اتصال کلاینت، مکث انجام نمیشود؛ بدنهٔ پاسخ ناموفق پیش از انتظار آزاد میشود. بهطور پیشفرض غیرفعال است: جایگزینی در زمان خرابی همچنان فوری باقی میماند.                                                                                    |
| `OPENCODE_PARK_AND_RESUME`                      | boolean | `false` |                | چرخش OpenCode: پس از چندین پاسخ موقت 429 (یا وجود یک نشانگر جدید فشار بر استخر)، درخواست را همراه با سیگنال ضربان قلب در حالت انتظار قرار میدهد، سپس بهجای توزیع همزمان درخواست در کل ناوگان، یک مرحلهٔ محدود شامل حداکثر 3 حساب متوالی را دوباره اجرا میکند. بهطور پیشفرض غیرفعال است: هر پاسخ 429 دقیقاً مانند قبل باعث چرخش به حساب بعدی میشود.                                                                                                                                        |
| `OPENCODE_RATE_LIMITED_429_EARLY_STOP`          | boolean | `false` |                | چرخش OpenCode: در اولین پاسخ 429 که بهعنوان محدودیت نرخ واقعی طبقهبندی شود (دارای `Retry-After` قابل تجزیه، یا بدنهای که به محدودیت نرخ/مصرف اشاره کند)، موج حسابها را متوقف کرده و همان پاسخ 429 سرویس بالادستی را بدون تغییر بازمیگرداند. پاسخهای 429 طبقهبندینشده همچنان باعث چرخش میشوند. بهطور پیشفرض غیرفعال است: سطح رایگان بهازای هر IP خروجی محدود است (#9611)، بنابراین هر پاسخ 429 باعث چرخش میشود و در صورت اتمام موج، آخرین پاسخ 429 سرویس بالادستی بازگردانده میشود.        |
| `MITM_DISABLE_TLS_VERIFY`                       | boolean | `false` | ✓              | تأیید گواهی TLS را برای پروکسی MITM غیرفعال میکند. **خطرناک است.**                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `OMNIROUTE_ALLOW_PRIVATE_PROVIDER_URLS`         | boolean | `false` |                | URLهای ارائهدهنده را که به شبکههای خصوصی/داخلی اشاره میکنند، مجاز میکند.                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `OMNIROUTE_ALLOW_LOCAL_PROVIDER_URLS`           | boolean | `true`  |                | افزودن/اعتبارسنجی ارائهدهندگان روی آدرسهای محلی/خصوصی (127.0.0.1، localhost، LAN) را مجاز میکند. بهطور پیشفرض فعال است (با اولویت محیط محلی)؛ برای مسدودسازی سختگیرانه و محدود به آدرسهای عمومی، آن را غیرفعال کنید. فرادادهٔ ابری همچنان مسدود باقی میماند.                                                                                                                                                                                                                              |
| `ENABLE_CC_COMPATIBLE_PROVIDER`                 | boolean | `false` | ✓              | حالت ارائهدهندهٔ سازگار با Claude Code را فعال میکند.                                                                                                                                                                                                                                                                                                                                                                                                                                     |

### سیاستها (5)

| کلید                            | نوع     | پیشفرض     | توضیحات                                                                                                                                                                                                                         |
| ------------------------------- | ------- | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `TOOL_POLICY_MODE`              | enum    | `disabled` | حالت اعمال سیاست استفاده از ابزار. مقادیر: `disabled`، `warn`، `block`.                                                                                                                                                         |
| `RATE_LIMIT_AUTO_ENABLE`        | boolean | `false`    | محدودسازی نرخ را بر اساس الگوهای استفاده بهطور خودکار فعال میکند.                                                                                                                                                               |
| `DISABLE_CONTEXT_WINDOW_CHECKS` | boolean | `false`    | بررسی محلی پنجرهٔ زمینه / حداکثر توکن ورودی OmniRoute را برای درخواستهای مستقیم تکمدلی نادیده میگیرد. محدودیتهای بالادستی همچنان اعمال میشوند.                                                                                  |
| `CAPABILITY_FILTER_ENABLED`     | boolean | `false`    | وقتی مدل هدف فاقد قابلیتهای موردنیاز (بینایی، ابزارها، خروجی ساختاریافته، پنجرهٔ زمینه) باشد، درخواستها را پیش از ارسال رد میکند. از درخواستهای مستقیم تکارائهدهندهای که فیلتر سازگاری لایهٔ ترکیبی را دور میزنند محافظت میکند. |
| `RADAR_ENABLED`                 | boolean | `false`    | ماژول Radar در OmniRoute (صفحههای خوراک کاتالوگ و همگامسازی) را فعال میکند. بهطور پیشفرض غیرفعال است؛ فعالکردن آن فقط رابط کاربری را در دسترس قرار میدهد — همگامسازی دادهها همچنان نیازمند فعالسازی جداگانه است.                |

### زمان اجرا (33)

| کلید                                        | نوع     | پیشفرض  | راهاندازی مجدد | توضیحات                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| ------------------------------------------- | ------- | ------- | -------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `UNIVERSAL_CONTEXT_HANDOFF_ENABLED`         | بولی    | `true`  |                | هنگام تغییر مدلها توسط مسیریابی ترکیبی، خلاصههای مکالمه را تولید و درج میکند. برای مستقل در نظر گرفتن تغییر مدلها و جلوگیری از درخواستهای انتقال پسزمینه برای همه ترکیبهای موجود و آینده، آن را غیرفعال کنید.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `RESPONSES_PASSTHROUGH_DROP_COMMENTARY`     | بولی    | `true`  |                | پیش از ارسال جریانهای عبوری Responses API به کلاینتها، موارد خروجی داخلی مربوط به مرحله تفسیر را حذف میکند. برای دریافت تفسیر خام بالادستی، آن را غیرفعال کنید.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `OMNIROUTE_MCP_ENFORCE_SCOPES`              | بولی    | `true`  |                | محدودیتهای دامنه را برای دسترسی به ابزارهای MCP اعمال میکند.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `OMNIROUTE_MCP_COMPRESS_DESCRIPTIONS`       | بولی    | `false` |                | برای کاهش مصرف توکن، توضیحات ابزارهای MCP را فشرده میکند.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `OMNIROUTE_ENABLE_RUNTIME_BACKGROUND_TASKS` | بولی    | `false` |                | پردازش وظایف پسزمینه را در زمان اجرا فعال میکند.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `OMNIROUTE_DISABLE_BACKGROUND_SERVICES`     | بولی    | `false` | ✓              | همه سرویسهای پسزمینه (بهروزرسانی سهمیه، همگامسازی و غیره) را غیرفعال میکند.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `OMNIROUTE_RTK_TRUST_PROJECT_FILTERS`       | boolean | `false` |                | اعتماد به فیلترهای RTK در سطح پروژه بدون اعتبارسنجی.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `OMNIROUTE_ENABLE_LIVE_WS`                  | boolean | `true`  | ✓              | راهاندازی سرور WebSocket داشبورد بلادرنگ هنگام import (بهطور پیشفرض روی پورت 20132).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `OMNIROUTE_CODEX_WS_ENABLED`                | boolean | `true`  |                | اجازه به Codex برای استفاده از انتقال Responses-over-WebSocket. در صورت غیرفعال بودن، Codex به HTTP Responses بازمیگردد.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `OMNIROUTE_CODEX_APP_SERVER_ENABLED`        | boolean | `true`  |                | اجازه به Codex برای استفاده از انتقال محلی WebSocket JSON-RPC مربوط به app-server (`codexTransport=app-server`). در صورت غیرفعال بودن، اتصالهایی که app-server را انتخاب کردهاند به سایر روشهای انتقال Codex بازمیگردند.                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `OMNIROUTE_EMERGENCY_FALLBACK`              | boolean | `true`  |                | هدایت درخواستهایی که بودجهشان تمام شده است به ارائهدهنده/مدل جایگزین رایگان اضطراری. (در ادامه، [جایگزین اضطراری بودجه](#emergency-budget-fallback) را ببینید.)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `STREAM_RECOVERY_ENABLED`                   | boolean | `false` |                | فعالسازی تلاش مجدد زودهنگام و شفاف برای جریانهای بالادستی SSE ناقص، پیش از آنکه هیچ بایتی از پاسخ به کلاینت برسد.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `STREAM_RECOVERY_MIDSTREAM_ENABLED`         | boolean | `false` |                | اجازه به بازیابی جریان برای ارسال مجدد درخواست و پیوند دادن پاسخ، پس از آنکه بایتها از قبل به کلاینت رسیدهاند.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `STREAM_RECOVERY_TOOLCALL_ORDER_FIX`        | boolean | `false` |                | ایمنسازی ادامه در میانه جریان برای فراخوانی ابزار: پس از صدور فراخوانی ابزار (در حال اجرا یا از قبل پایانیافته با `finish_reason` برابر با `tool_calls`)، هرگز جریان قطعشده را از سر نگیرد و بهجای مصرف کل بودجه، پس از یک ادامه خالی اتصال را ببندد. غیرفعال: رفتار نسخه انتشاریافته.                                                                                                                                                                                                                                                                                                                                                                                                           |
| `STREAM_EARLY_EOF_SIBLING_FAILOVER_ENABLED` | boolean | `false` |                | وقتی یک جریان SSE پیش از انتشار هرگونه فریم مفید بسته میشود و تلاش مجدد محدود روی همان اتصال به پایان رسیده است، یکبار به اتصال همرده سوئیچ میکند؛ اگر اتصال همرده قابلاستفادهای وجود نداشته باشد، خطای 502 اصلی `STREAM_EARLY_EOF` بازگردانده میشود. بهطور پیشفرض غیرفعال است: EOF زودهنگام پس از تلاش مجدد روی همان اتصال، نهایی باقی میماند.                                                                                                                                                                                                                                                                                                                                                  |
| `MODEL_CATALOG_INCLUDE_NAMES`               | boolean | `true`  |                | فیلدهای نامِ مناسب برای نمایش را در پاسخهای `/v1/models` قرار میدهد. برای کلاینتهایی که فقط انتظار شناسههای مدل را دارند، آن را غیرفعال کنید.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `MODELS_CATALOG_PREFIX_MODE`                | enum    | `dual`  |                | نحوه پیشوندگذاری شناسههای مدل در /v1/models را کنترل میکند. 'dual' (پیشفرض) برای سازگاری با نسخههای قبلی، هم پیشوند نام مستعار و هم پیشوند شناسه رسمی ارائهدهنده را منتشر میکند. 'alias' فقط پیشوند کوتاه نام مستعار را منتشر میکند (برای مثال ds-web/model، نه deepseek-web/model). 'canonical' فقط پیشوند کامل شناسه ارائهدهنده را منتشر میکند. مقادیر: `dual`، `alias`، `canonical`.                                                                                                                                                                                                                                                                                                          |
| `ARENA_ELO_SYNC_ENABLED`                    | boolean | `true`  |                | همگامسازی دورهای ELO تابلوی امتیازات Arena AI را برای رتبهبندی هوشمندی مدلها فعال میکند.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `EXPOSE_CC_DISCOVERY_ALIASES`               | boolean | `false` |                | شناسههای آینهای `claude/<provider>/<model>` را در `/v1/models` اعلام میکند تا قابلیت کشف مدل در درگاه Claude Code، مدلهای غیر Claude را فهرست کند. سطح سراسریِ دروازه سهسطحی است (متغیر محیطی بر بازنویسی داشبورد اولویت دارد). به [پیکربندی Claude Code](../guides/CLAUDE-CODE-CONFIGURATION.md#discovery-aliases--surface-non-claude-models-in-the-model-picker) مراجعه کنید.                                                                                                                                                                                                                                                                                                                  |
| `NO_THINKING_ALIAS_ENABLED`                 | boolean | `true`  |                | کلید اصلی نامهای مستعار درگاه no-think/<provider>/<model> است. در حالت فعال (پیشفرض): /v1/models برای هر مدل واجد شرایط Claude که قابلیت تفکر دارد، یک گونه بدون تفکر اعلام میکند و شناسه no-think/ ارسالشده در یک درخواست، با سرکوب استدلال دوباره به مدل واقعی نگاشت میشود. در حالت غیرفعال: هیچ گونهای اعلام نمیشود و شناسه no-think/ مانند هر شناسه مدل ناشناخته دیگری در نظر گرفته میشود. گزینه فعالسازی/غیرفعالسازی مختص هر مدل، یعنی ModelSpec.noThinkingAlias، در زمان فعال بودن این تنظیم همچنان اعمال میشود.                                                                                                                                                                           |
| `OMNIROUTE_DISABLE_THINKING_LEVEL_VARIANTS` | boolean | `false` |                | تولید گونههای سطح تفکر (برای مثال -low، -medium، -high) را در کاتالوگ /v1/models غیرفعال میکند.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `OMNIROUTE_CHAT_VIRTUAL_LANES`              | boolean | `false` | ✓              | مسیرهای مجازی تطبیقی پذیرش بهازای هر مستأجر را برای ارسال به ارائهدهنده فعال میکند (#9654): افزایش ناگهانی بار یک مستأجر دیگر باعث خطای 503 برای مستأجر دیگر نمیشود. متغیر محیطی OMNIROUTE_CHAT_VIRTUAL_LANES بر این بازنویسی داشبورد اولویت دارد؛ تغییرات پس از راهاندازی مجدد سرور اعمال میشوند.                                                                                                                                                                                                                                                                                                                                                                                               |
| `EXPOSE_FUNCTIONAL_GATEWAY_MIRRORS`         | boolean | `false` |                | شناسههای آینهای <gateway-alias>/<model> را در /v1/models برای مدلهایی اعلام میکند که مالک اصلی آنها اعتبارنامهٔ فعالی ندارد، اما یک درگاه عبوری دارای اعتبارنامهٔ فعال آنها را مسیریابی میکند. هشدار: در صورت فعالسازی سراسری، ورودیهایی برای همهٔ کلاینتها به کاتالوگ اضافه میکند.                                                                                                                                                                                                                                                                                                                                                                                                              |
| `NEWAPI_AGGREGATOR_BALANCE`                 | boolean | `false` |                | تشخیص موجودی را برای گرههای سازگار با تجمیعکنندهٔ New-API / One-API / Sub2API فعال میکند. در صورت فعالسازی، گرههای سازگاری که پرچم تجمیعکننده برایشان تنظیم شده است، موجودی خود را در داشبورد و مسیریابی پیشبررسی سهمیه گزارش میکنند.                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `SERVER_OWNED_TOOL_LOOP_ENABLED`            | boolean | `false` |                | فراخوانیهای غیرجریانی ابزارهای تحت مالکیت سرور را تا زمانی ادامه میدهد که مدل پاسخی قابلاستفاده برای کلاینت برگرداند.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `SEARCH_STATS_HIDE_DELETED_CONNECTIONS`     | boolean | `false` |                | آمار جستوجو و جستوجوهای اخیر فقط ارائهدهندگانی را محاسبه میکنند که همچنان اتصال فعالی دارند (ارائهدهندگان بدون کلید مانند duckduckgo-free همیشه محاسبه میشوند). در حالت غیرفعال، همهٔ ردیفهای جستوجوی نگهداریشده که دارای شناسهٔ ارائهدهنده هستند حفظ میشوند.                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `FREE_BADGE_REQUIRES_PROVIDER_FREE_TIER`    | boolean | `false` |                | صفحات ارائهدهندگان در داشبورد: نشان «رایگان» را فقط بر اساس سیگنالهایی نمایش میدهد که ارائهدهنده به آنها پایبند است — روش ابتکاری مبتنی بر نام نمایشی، فیلدهای رایگان غیربولین و پسوندهای :free را برای ارائهدهندگان ثبتشدهای که سطح رایگان مستندی ندارند کنار میگذارد. در حالت غیرفعال، قاعدهٔ تاریخی نشان حفظ میشود.                                                                                                                                                                                                                                                                                                                                                                           |
| `RETRY_AFTER_PROVENANCE_ENABLED`            | boolean | `false` |                | در پاسخهای تجمیعشدهٔ 429/503 مربوط به در دسترس نبودن، وقتی زمان مشخصی برای تلاش مجدد در آینده معلوم نیست، `Retry-After` را حذف میکند (بهجای مقدار ساختگی 1s)، فیلد `error.retry_after_provenance` (`signal` \| `none`) را اضافه میکند و به مسیرهای تخلیهٔ ترکیبی اجازه میدهد راهنمای متنی تلاش مجدد را از بدنههای بالادستی JSON و متن ساده بخوانند. این فیلد فقط در پاسخهایی ظاهر میشود که توسط `unavailableResponse()` ساخته شدهاند؛ سایر بدنههای 429/503 بدون تغییر باقی میمانند.                                                                                                                                                                                                              |
| `PROTECTED_PRIORITY_INFRA_502_ENABLED`      | boolean | `false` |                | وقتی یک مقصد ترکیبی `priority` که بهعنوان فقط-بازگشت-هنگام-اتمام-سهمیه علامتگذاری شده است، ترکیب را به دلیلی که بهطور اثباتپذیر مرتبط با سهمیه نیست متوقف میکند (باز بودن قطعکنندهٔ مدار ارائهدهنده، رد شدن بهدلیل پیشبینی تأخیر)، بهجای 503 که ظاهری شبیه خطای سهمیه دارد، پاسخ 502 میدهد. توقفهای ناشی از قفلشدن، دورهٔ انتظار، در دسترس نبودن، اتمام منابع و سقف همزمانی همچنان 503 برمیگردانند.                                                                                                                                                                                                                                                                                              |
| `MISTRAL_AMBIGUOUS_401_SOFT_LOCKOUT`        | boolean | `false` |                | پاسخ سادهٔ 401 از Mistral (`{"detail":"Unauthorized"}`، بدون سیگنال صریح احراز هویت) برای کلید لغوشده و سهمیهٔ تمامشده یکسان است. در صورت فعالسازی، بهجای پارک کردن اتصال با وضعیت `expired`، آن را وارد دورهٔ انتظار میکند؛ حداکثر 3 بار در ساعت برای هر اتصال. مورد بعدی اتصال را پارک میکند تا یک کلید لغوشده همچنان در نهایت شناسایی شود. بهطور پیشفرض غیرفعال است: هر پاسخ سادهٔ 401 از Mistral مانند قبل اتصال را پارک میکند.                                                                                                                                                                                                                                                              |
| `XAI_OAUTH_LIVE_MODEL_DISCOVERY`            | boolean | `false` |                | کاتالوگ زندهٔ مدلهای xAI را برای اتصالهای `xai-oauth` با استفاده از توکن حامل OAuth از `https://api.x.ai/v1/models` دریافت میکند، بهجای آنکه از دادهٔ اولیهٔ ایستای تثبیتشده استفاده کند. بهطور پیشفرض غیرفعال است: `xai-oauth` همچنان دادهٔ اولیهٔ ایستا را بدون تغییر ارائه میکند. در صورت بروز هرگونه خطا در تفکیک، فرایند کشف به دادهٔ اولیه بازمیگردد (پذیرش توکن حامل OAuth توسط x.ai در این نقطهٔ پایانی تأیید نشده است).                                                                                                                                                                                                                                                                 |
| `BATCH_AND_FILE_AUTO_CLEANUP_ENABLED`       | boolean | `false` |                | به پیمایش پاکسازی خودکار اجازه میدهد کارهای نهاییشدهٔ Batch API (تکمیلشده/ناموفق/لغوشده/منقضیشده) را که قدیمیتر از `OMNIROUTE_BATCH_RETENTION_DAYS` هستند، همراه با نقاط وارسی هر خط آنها حذف کند و محتوای BLOB فایلهای بارگذاریشدهای را که از `expires_at` خود گذشتهاند پاک کند. بهطور پیشفرض غیرفعال است: تا زمانی که اپراتور آن را فعال نکند، هر نصب موجود این دادهها را دقیقاً مانند قبل نگه میدارد. مسیر `DELETE /api/v1/batches/delete-completed` که توسط اپراتور فعال میشود، در هر دو حالت بدون تأثیر باقی میماند — این مسیر یک قرارداد API عمومی، مجزا و بدون شرط است.                                                                                                                   |
| `ANTIGRAVITY_ACCOUNT_LEASE_ENABLED`         | boolean | `false` |                | حساب Antigravity انتخابشده را برای چرخهٔ حیات استریمِ درخواستی که آن را انتخاب کرده است رزرو میکند تا تلاش مجدد همزمان یا واگذاری اعتبارنامه نتواند حسابی را که از قبل به یک استریم در حال اجرا اختصاص یافته است دوباره انتخاب کند. دامنهٔ رزرو به (اتصال، مدل بالادستی قابل فراخوانی) محدود است، بنابراین یک حساب همچنان میتواند بهطور همزمان دو مدل متفاوت را سرویس دهد. هنگامی که همهٔ حسابهای واجد شرایط از قبل برای آن مدل اجاره شده باشند، درخواست بهجای تحمیل بار بیشتر بر یک حساب مشغول، پاسخ ساختاریافتهٔ 503 با کد `antigravity_pool_busy` و مقدار محدودشدهٔ `Retry-After` را برمیگرداند. بهطور پیشفرض غیرفعال است: انتخاب حساب دقیقاً مانند قبل باقی میماند و هیچ رزروی انجام نمیشود. |

### CLI (5)

| کلید                                  | نوع     | پیشفرض  | راهاندازی مجدد | توضیحات                                                                                                                                                                                                                                 |
| ------------------------------------- | ------- | ------- | -------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `CLI_COMPAT_ALL`                      | boolean | `false` | ✓              | حالت سازگاری را برای همهٔ کلاینتهای CLI فعال میکند.                                                                                                                                                                                     |
| `MODEL_ALIAS_COMPAT_ENABLED`          | boolean | `false` |                | لایهٔ سازگاری نام مستعار مدل را فعال میکند.                                                                                                                                                                                             |
| `PRICING_SYNC_ENABLED`                | boolean | `false` |                | همگامسازی خودکار دادههای قیمتگذاری را فعال میکند (متغیر محیطی `PRICING_SYNC_ENABLED` نیز باید تنظیم شده باشد).                                                                                                                          |
| `OMNIROUTE_AUTO_SYNC_CODEX_PROFILES`  | boolean | `false` |                | پس از همگامسازی مدلهای ارائهدهنده، فایلهای پروفایل ~/.codex/*.config.toml را بهطور خودکار بر اساس کاتالوگ زنده (باز)نویسی میکند. هرگز پیکربندی فعال/پیشفرض Codex را تغییر نمیدهد. بهطور پیشفرض غیرفعال است.                             |
| `OMNIROUTE_AUTO_SYNC_CLAUDE_PROFILES` | boolean | `false` |                | پس از همگامسازی مدلهای ارائهدهنده، پروفایلهای Claude Code در ~/.claude/profiles/<name>/settings.json را بهطور خودکار بر اساس کاتالوگ زنده (باز)نویسی میکند. هرگز پیکربندی فعال/پیشفرض Claude را تغییر نمیدهد. بهطور پیشفرض غیرفعال است. |

### سلامت (5)

| کلید                                      | نوع     | پیشفرض  | توضیحات                                                                                                                                                                                                                                                                                         |
| ----------------------------------------- | ------- | ------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `OMNIROUTE_DISABLE_LOCAL_HEALTHCHECK`     | boolean | `false` | نقطه پایانی بررسی سلامت نمونه محلی را غیرفعال میکند.                                                                                                                                                                                                                                            |
| `OMNIROUTE_DISABLE_TOKEN_HEALTHCHECK`     | boolean | `false` | بررسی سلامت اعتبارسنجی توکن را غیرفعال میکند.                                                                                                                                                                                                                                                   |
| `SKILLS_SANDBOX_NETWORK_ENABLED`          | boolean | `false` | دسترسی به شبکه را در محیط سندباکس مهارتها فعال میکند.                                                                                                                                                                                                                                           |
| `PROXY_HEALTH_BLOCKED_RESETS_STREAK`      | boolean | `false` | در پیمایش سلامت پروکسی، اگر هدف یک درخواست آزمایشی را رد کند (401/403/429)، رشته شکستهای متوالی پروکسی بازنشانی میشود. بهطور پیشفرض غیرفعال است: رد شدن خنثی باقی میماند (#10654). پاسخ 5xx در هر دو حالت نامشخص باقی میماند؛ رد شدن هرگز باعث حذف، غیرفعالسازی یا فعالسازی مجدد پروکسی نمیشود. |
| `DB_HEALTHCHECK_STARTUP_DEFERRED_ENABLED` | boolean | `false` | بررسی یکپارچگی/سلامت DB هنگام راهاندازی را پس از آن اجرا میکند که سرور پذیرش درخواستها را آغاز کرده باشد (از طریق `setImmediate`)، بهجای اینکه راهاندازی را تا تکمیل آن مسدود کند (#13717). بهطور پیشفرض غیرفعال است: راهاندازی دقیقاً مانند پیش از این PR مسدود میشود.                         |

> [!NOTE]
> `INPUT_SANITIZER_BLOCK_THRESHOLD` و نام مستعار قدیمی آن،
> `INJECTION_GUARD_BLOCK_THRESHOLD`، حالت `block` مربوط به
> `INJECTION_GUARD_MODE` را تنظیم میکنند، اما متغیرهای محیطی سادهای هستند که توسط
> [`src/shared/utils/injectionSeverity.ts`](../../src/shared/utils/injectionSeverity.ts)
> خوانده میشوند، نه پرچمهای قابلیت: آنها نه بازنویسی DB دارند و نه کلید تغییر وضعیت در داشبورد. به
> [`ENVIRONMENT.md`](./ENVIRONMENT.md#4-security--authentication) مراجعه کنید.

> [!NOTE]
> ستون `Restart` پرچمهایی با `requiresRestart: true` را مشخص میکند — مقدار
> فوراً ذخیره میشود، اما تنها پس از بارگذاری مجدد فرایند اعمال میگردد. پرچمهای
> enum هر مقداری خارج از مجموعه مجاز خود را رد میکنند (اعتبارسنجی سمت سرور هم در
> `setFeatureFlagOverride()` و هم در کنترلکننده REST `PUT` انجام میشود).

---

## تغییر وضعیت پرچمها

### داشبورد

به **Dashboard → Settings → Feature Flags**
(`/dashboard/settings/feature-flags`) بروید. شبکه
(`src/app/(dashboard)/dashboard/settings/components/FeatureFlagsGrid.tsx`)
از موارد زیر پشتیبانی میکند:

- **جستوجو** بر اساس کلید یا توضیحات و **فیلتر کردن** بر اساس دستهبندی (بهعلاوه نمای ساختگی
  **Requires Restart**).
- یک **کلید تغییر وضعیت** برای پرچمهای بولی و یک **فهرست کشویی** برای پرچمهای enum
  (`src/app/(dashboard)/dashboard/settings/components/FeatureFlagCard.tsx`).
- یک **نشان منبع** برای هر پرچم — `DB`، `ENV` یا `DEF` — که نشان میدهد
  مقدار مؤثر از کجا آمده است.
- دکمه **Reset** (که فقط برای پرچمهای دارای منبع `DB` نمایش داده میشود) برای حذف مقدار بازنویسیشده،
  و دکمه **Reset All Overrides** در پایین صفحه.
- یک نوار **Restart Server** هنگام تغییر پرچمی با `requiresRestart`.

### API مبتنی بر REST

تمام عملیات از طریق یک مسیر واحد انجام میشوند:
[`src/app/api/settings/feature-flags/route.ts`](../../src/app/api/settings/feature-flags/route.ts).
هر متد به یک نشست احراز هویتشده داشبورد نیاز دارد (در غیر این صورت `401` برگردانده میشود).

#### `GET /api/settings/feature-flags`

همه پرچمها را بههمراه مقدار مؤثر، منبع و خلاصه آنها برمیگرداند.

```jsonc
{
  "flags": [
    {
      "key": "REQUIRE_API_KEY",
      "label": "Require API Key",
      "description": "Require an API key for all incoming requests",
      "category": "security",
      "type": "boolean",
      "enumValues": null,
      "defaultValue": "false",
      "effectiveValue": "false",
      "source": "default", // "db" | "env" | "default"
      "requiresRestart": false,
      "warningLevel": "caution",
    },
    // ... همه ۷۵ پرچم
  ],
  "summary": {
    "total": 56,
    "active": 0,
    "inactive": 0,
    "overriddenByDb": 0,
    "overriddenByEnv": 0,
  },
}
```

#### `PUT /api/settings/feature-flags`

یک مقدار بازنویسیشده را تنظیم یا حذف میکند. بدنه: `{ key: string; value?: string }`.
حذف `value` باعث حذف مقدار بازنویسیشده میشود (و مقدار env / پیشفرض را بازیابی میکند).

```bash
# تنظیم یک مقدار بازنویسیشده در DB
curl -X PUT http://localhost:20128/api/settings/feature-flags \
  -H "Content-Type: application/json" \
  -d '{"key":"REQUIRE_API_KEY","value":"true"}'

# حذف مقدار بازنویسیشده (بدون "value")
curl -X PUT http://localhost:20128/api/settings/feature-flags \
  -H "Content-Type: application/json" \
  -d '{"key":"REQUIRE_API_KEY"}'
```

پاسخ، `effectiveValue`/`source` جدید، `previousValue`/
`previousSource` و `requiresRestart` را بازمیگرداند. کلیدهای ناشناخته و مقادیر enum
خارج از محدوده با `400` رد میشوند.

#### `DELETE /api/settings/feature-flags`

**همه** مقادیر بازنویسیشده DB را بهصورت همزمان پاک میکند و مقدار هر پرچم را به مقدار env / پیشفرض
آن بازمیگرداند. `{ cleared: <count>, message: "..." }` را برمیگرداند.

> [!NOTE]
> پرچمهایی با `requiresRestart: true` تنها پس از بارگذاری مجدد فرایند اعمال میشوند.
> فرایند راهاندازی مجدد داشبورد، `POST /api/restart` را فراخوانی میکند و سپس
> `GET /api/health/ping` را تا زمان راهاندازی مجدد سرور پایش میکند.

---

## مسیر جایگزین اضطراری بودجه

`OMNIROUTE_EMERGENCY_FALLBACK` (دستهبندی `runtime`، مقدار پیشفرض `true`) مسیر
جایگزین رایگان اضطراری را در
[`open-sse/services/emergencyFallback.ts`](../../open-sse/services/emergencyFallback.ts)
کنترل میکند. هنگامی که فعال باشد، درخواستهایی که بودجهٔ خود را تمام میکنند، بهجای شکست کامل
به یک ارائهدهنده/مدل جایگزین رایگان هدایت میشوند. برای غیرفعال کردن این رفتار و شکست خوردن درخواستهایی
که بودجهشان تمام شده است، آن را — از طریق کلید تغییر وضعیت داشبورد، یک مقدار بازنویسیشده در DB یا متغیر محیطی
`OMNIROUTE_EMERGENCY_FALLBACK` — روی `false` (یا `0`) تنظیم کنید.
(در PRهای #3741 / #3752 بهصورت یک کلید تغییر وضعیت در داشبورد ارائه شده است.)

---

## همچنین ببینید

- [مرجع متغیرهای محیطی](./ENVIRONMENT.md) — بیشتر پرچمها دارای یک متغیر محیطی
  همنام هستند که در آنجا مستند شده است (مقدار بازنویسیشده در DB بر آن
  اولویت دارد).
- [`src/shared/constants/featureFlagDefinitions.ts`](../../src/shared/constants/featureFlagDefinitions.ts)
  — منبع معتبر برای همهٔ پرچمها.
- [`src/shared/utils/featureFlags.ts`](../../src/shared/utils/featureFlags.ts)
  — منطق تعیین مقدار (`resolveFeatureFlag`، `isFeatureFlagEnabled`،
  `resolveAllFeatureFlags`).
- [`src/lib/db/featureFlags.ts`](../../src/lib/db/featureFlags.ts) — ذخیرهسازی
  پایدار مقادیر بازنویسیشده در DB، در فضای نام `feature_flags` از جدول `key_value`.
