# Environment Variables Reference (فارسی)

🌐 **Languages:** 🇺🇸 [English](../../../../reference/ENVIRONMENT.md) · 🇪🇹 [am](../../../am/docs/reference/ENVIRONMENT.md) · 🇸🇦 [ar](../../../ar/docs/reference/ENVIRONMENT.md) · 🇦🇿 [az](../../../az/docs/reference/ENVIRONMENT.md) · 🇧🇬 [bg](../../../bg/docs/reference/ENVIRONMENT.md) · 🇧🇩 [bn](../../../bn/docs/reference/ENVIRONMENT.md) · 🇨🇿 [cs](../../../cs/docs/reference/ENVIRONMENT.md) · 🇩🇰 [da](../../../da/docs/reference/ENVIRONMENT.md) · 🇩🇪 [de](../../../de/docs/reference/ENVIRONMENT.md) · 🇬🇷 [el](../../../el/docs/reference/ENVIRONMENT.md) · 🇪🇸 [es](../../../es/docs/reference/ENVIRONMENT.md) · 🇪🇪 [et](../../../et/docs/reference/ENVIRONMENT.md) · 🇫🇮 [fi](../../../fi/docs/reference/ENVIRONMENT.md) · 🇫🇷 [fr](../../../fr/docs/reference/ENVIRONMENT.md) · 🇮🇪 [ga](../../../ga/docs/reference/ENVIRONMENT.md) · 🇮🇳 [gu](../../../gu/docs/reference/ENVIRONMENT.md) · 🇳🇬 [ha](../../../ha/docs/reference/ENVIRONMENT.md) · 🇮🇱 [he](../../../he/docs/reference/ENVIRONMENT.md) · 🇮🇳 [hi](../../../hi/docs/reference/ENVIRONMENT.md) · 🇭🇷 [hr](../../../hr/docs/reference/ENVIRONMENT.md) · 🇭🇺 [hu](../../../hu/docs/reference/ENVIRONMENT.md) · 🇦🇲 [hy](../../../hy/docs/reference/ENVIRONMENT.md) · 🇮🇩 [id](../../../id/docs/reference/ENVIRONMENT.md) · 🇳🇬 [ig](../../../ig/docs/reference/ENVIRONMENT.md) · 🇮🇹 [it](../../../it/docs/reference/ENVIRONMENT.md) · 🇯🇵 [ja](../../../ja/docs/reference/ENVIRONMENT.md) · 🇬🇪 [ka](../../../ka/docs/reference/ENVIRONMENT.md) · 🇰🇭 [km](../../../km/docs/reference/ENVIRONMENT.md) · 🇮🇳 [kn](../../../kn/docs/reference/ENVIRONMENT.md) · 🇰🇷 [ko](../../../ko/docs/reference/ENVIRONMENT.md) · 🇱🇹 [lt](../../../lt/docs/reference/ENVIRONMENT.md) · 🇱🇻 [lv](../../../lv/docs/reference/ENVIRONMENT.md) · 🇮🇳 [ml](../../../ml/docs/reference/ENVIRONMENT.md) · 🇮🇳 [mr](../../../mr/docs/reference/ENVIRONMENT.md) · 🇲🇾 [ms](../../../ms/docs/reference/ENVIRONMENT.md) · 🇲🇹 [mt](../../../mt/docs/reference/ENVIRONMENT.md) · 🇲🇲 [my](../../../my/docs/reference/ENVIRONMENT.md) · 🇳🇵 [ne](../../../ne/docs/reference/ENVIRONMENT.md) · 🇳🇱 [nl](../../../nl/docs/reference/ENVIRONMENT.md) · 🇳🇴 [no](../../../no/docs/reference/ENVIRONMENT.md) · 🇮🇳 [or](../../../or/docs/reference/ENVIRONMENT.md) · 🇮🇳 [pa](../../../pa/docs/reference/ENVIRONMENT.md) · 🇵🇭 [phi](../../../phi/docs/reference/ENVIRONMENT.md) · 🇵🇱 [pl](../../../pl/docs/reference/ENVIRONMENT.md) · 🇵🇹 [pt](../../../pt/docs/reference/ENVIRONMENT.md) · 🇧🇷 [pt-BR](../../../pt-BR/docs/reference/ENVIRONMENT.md) · 🇷🇴 [ro](../../../ro/docs/reference/ENVIRONMENT.md) · 🇷🇺 [ru](../../../ru/docs/reference/ENVIRONMENT.md) · 🇱🇰 [si](../../../si/docs/reference/ENVIRONMENT.md) · 🇸🇰 [sk](../../../sk/docs/reference/ENVIRONMENT.md) · 🇸🇮 [sl](../../../sl/docs/reference/ENVIRONMENT.md) · 🇷🇸 [sr](../../../sr/docs/reference/ENVIRONMENT.md) · 🇸🇪 [sv](../../../sv/docs/reference/ENVIRONMENT.md) · 🇰🇪 [sw](../../../sw/docs/reference/ENVIRONMENT.md) · 🇮🇳 [ta](../../../ta/docs/reference/ENVIRONMENT.md) · 🇮🇳 [te](../../../te/docs/reference/ENVIRONMENT.md) · 🇹🇭 [th](../../../th/docs/reference/ENVIRONMENT.md) · 🇹🇷 [tr](../../../tr/docs/reference/ENVIRONMENT.md) · 🇺🇦 [uk-UA](../../../uk-UA/docs/reference/ENVIRONMENT.md) · 🇵🇰 [ur](../../../ur/docs/reference/ENVIRONMENT.md) · 🇺🇿 [uz](../../../uz/docs/reference/ENVIRONMENT.md) · 🇻🇳 [vi](../../../vi/docs/reference/ENVIRONMENT.md) · 🇳🇬 [yo](../../../yo/docs/reference/ENVIRONMENT.md) · 🇨🇳 [zh-CN](../../../zh-CN/docs/reference/ENVIRONMENT.md) · 🇹🇼 [zh-TW](../../../zh-TW/docs/reference/ENVIRONMENT.md)

---

> مرجع کامل همهٔ متغیرهای محیطی که OmniRoute شناسایی میکند.
> برای یک الگوی شروع سریع، به [`.env.example`](../../.env.example) مراجعه کنید.

> [!IMPORTANT]
> هر متغیری که در اینجا مستند شده است باید در `.env.example` نیز وجود داشته باشد و
> هر متغیر موجود در `.env.example` نیز باید در اینجا ذکر شود. `npm run check:env-doc-sync`
> این هماهنگی را هنگام commit و در CI اعمال میکند. برای حذف عمدی یک متغیر، آن را به
> فهرست مجاز درون `scripts/check/check-env-doc-sync.mjs` اضافه کنید.

---

## فهرست مطالب

- [1. اسرار الزامی](#1-required-secrets)
- [2. فضای ذخیرهسازی و پایگاه داده](#2-storage--database)
- [3. شبکه و پورتها](#3-network--ports)
- [4. امنیت و احراز هویت](#4-security--authentication)
- [5. پاکسازی ورودی و محافظت از اطلاعات شناسایی شخصی](#5-input-sanitization--pii-protection)
- [6. سیاستهای ابزار و مسیریابی](#6-tool--routing-policies)
- [7. نشانیهای اینترنتی و همگامسازی ابری](#7-urls--cloud-sync)
- [8. پراکسی خروجی](#8-outbound-proxy)
- [9. یکپارچهسازی ابزار CLI](#9-cli-tool-integration)
- [10. یکپارچهسازیهای عامل داخلی و MCP](#10-internal-agent--mcp-integrations)
- [11. اطلاعات احراز هویت ارائهدهندگان OAuth](#11-oauth-provider-credentials)
- [12. بازنویسیهای User-Agent ارائهدهندگان](#12-provider-user-agent-overrides)
- [13. سازگاری اثر انگشت CLI](#13-cli-fingerprint-compatibility)
- [14. ارائهدهندگان کلید API](#14-api-key-providers)
- [15. تنظیمات مهلت زمانی](#15-timeout-settings)
- [16. ثبت گزارش](#16-logging)
- [17. بهینهسازی حافظه](#17-memory-optimization)
- [18. همگامسازی قیمتگذاری](#18-pricing-sync)
- [19. همگامسازی مدل (توسعه)](#19-model-sync-dev)
- [20. تنظیمات مختص ارائهدهنده](#20-provider-specific-settings)
- [21. سلامت پراکسی](#21-proxy-health)
- [22. اشکالزدایی](#22-debugging)
- [23. یکپارچهسازی GitHub](#23-github-integration)
- [24. محیط ایزولهٔ مهارتها (v3.8.0+)](#24-skills-sandbox-v380)
- [27. خوراک رادار (میزبانی شخصی)](#27-radar-feed-self-hosting)
- [سناریوهای استقرار](#deployment-scenarios)
- [ممیزی: متغیرهای حذفشده / بدون استفاده](#audit-removed--dead-variables)

---

## 1. اسرار الزامی

این موارد **باید** پیش از نخستین اجرا تنظیم شوند. بدون آنها، برنامه یا از شروع به کار خودداری میکند یا با مقادیر پیشفرض ناامن کار خواهد کرد.

| متغیر                        | الزامی                    | پیشفرض                            | فایل منبع                                                 | توضیحات                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| ---------------------------- | ------------------------- | --------------------------------- | --------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `JWT_SECRET`                 | **بله**                   | _(هیچکدام)_                       | `src/lib/auth`                                            | همه کوکیهای نشست داشبورد (JWT) را امضا/اعتبارسنجی میکند. با `openssl rand -base64 48` تولید کنید.                                                                                                                                                                                                                                                                                                                                                    |
| `API_KEY_SECRET`             | **بله**                   | _(هیچکدام)_                       | `src/lib/db/apiKeys.ts`                                   | کلید رمزنگاری AES برای مقادیر کلید API ذخیرهشده در SQLite. با `openssl rand -hex 32` تولید کنید.                                                                                                                                                                                                                                                                                                                                                     |
| `INITIAL_PASSWORD`           | **بله**                   | `CHANGEME`                        | اسکریپت راهاندازی اولیه                                   | گذرواژه اولیه مدیر داشبورد را تنظیم میکند (با مقدار پیشفرض `.env.example` مطابقت دارد — عمداً ناامن نگه داشته شده است تا تغییر آن اجباری باشد). **پیش از نخستین استفاده آن را تغییر دهید.** پس از ورود، آن را از مسیر داشبورد ← تنظیمات ← امنیت تغییر دهید.                                                                                                                                                                                          |
| `OMNIROUTE_WS_BRIDGE_SECRET` | **بله** (در محیط عملیاتی) | _(تنظیمنشده)_                     | `src/app/api/internal/codex-responses-ws/route.ts`        | راز مشترک برای پل داخلی WebSocket مربوط به Codex Responses. درخواستهای پل میان رله WS در Electron/مرورگر و OmniRoute را احراز هویت میکند. ⚠️ **در محیط عملیاتی الزامی است — اگر تنظیم نشده باشد، همه درخواستهای پل WS رد میشوند.** با `openssl rand -base64 32` تولید کنید.                                                                                                                                                                          |
| `OMNIROUTE_SW_BUILD_ID`      | خیر                       | _(SHA مربوط به git)_              | `next.config.mjs`, `scripts/build/assembleStandalone.mjs` | شناسه صریح برای ابطال کش service worker پوسته PWA (#11779)؛ نخستین مورد در زنجیره تفکیک.                                                                                                                                                                                                                                                                                                                                                             |
| `SOURCE_VERSION`             | خیر                       | _(تنظیمنشده)_                     | `next.config.mjs`, `scripts/build/assembleStandalone.mjs` | دومین مورد در زنجیره — توسط سازندههای PaaS (برای مثال، سامانههای مشابه Heroku) روی commit استقراریافته تنظیم میشود.                                                                                                                                                                                                                                                                                                                                  |
| `NEXT_PUBLIC_SW_BUILD_ID`    | خیر                       | _(مشتقشده)_                       | `src/shared/components/PwaRegister.tsx`                   | مقدار عمومی زمان build که کلاینت برای ثبت `/sw.js?v=…` استفاده میکند؛ ابتدا از دو مقدار بالا و سپس از SHA مربوط به git مشتق میشود.                                                                                                                                                                                                                                                                                                                   |
| `OMNIROUTE_PEER_STAMP_TOKEN` | خیر (خودکار)              | _(در هر راهاندازی بهصورت خودکار)_ | `src/server/authz/policies/management.ts`                 | راز مختص هر فرایند که ثابت میکند مُهر IP همتای مورد اعتماد از سرور HTTP خود OmniRoute (`scripts/dev/peer-stamp.mjs`) آمده است. میانافزار authz تنها زمانی به محلیبودن درخواست اعتماد میکند (محدودسازی loopback/LAN برای مسیرهای LOCAL_ONLY) که مُهر حاوی این توکن باشد. در هر راهاندازی بهصورت خودکار تولید میشود — آن را تنظیمنشده باقی بگذارید؛ فقط برای پیکربندیهای چندفرایندی که باید مُهر را به اشتراک بگذارند، مقدار ثابتی برای آن تعیین کنید. |

### فرمانهای تولید

```bash
# تولید هر چهار راز بهصورت همزمان:
echo "JWT_SECRET=$(openssl rand -base64 48)"
echo "API_KEY_SECRET=$(openssl rand -hex 32)"
echo "INITIAL_PASSWORD=$(openssl rand -base64 16)"
echo "OMNIROUTE_WS_BRIDGE_SECRET=$(openssl rand -base64 32)"
```

> [!CAUTION]
> هرگز فایلهای `.env` حاوی رازهای واقعی را در سامانه کنترل نسخه commit نکنید. `.gitignore` از قبل `.env` را مستثنی کرده است، اما پیش از push کردن این مورد را بررسی کنید.

---

## 2. فضای ذخیرهسازی و پایگاه داده

OmniRoute برای تمام دادههای ماندگار از **SQLite** (از طریق `better-sqlite3`) استفاده میکند. این متغیرها محل دادهها، رمزنگاری و چرخه عمر آنها را کنترل میکنند.

| متغیر                                      | پیشفرض                        | فایل منبع                                             | توضیحات                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| ------------------------------------------ | ----------------------------- | ----------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `DATA_DIR`                                 | `~/.omniroute/`               | `src/lib/db/core.ts`                                  | دایرکتوری ریشه برای پایگاه داده SQLite، نسخههای پشتیبان و فایلهای داده. برای volumeهای Docker یا مسیرهای سفارشی، آن را بازنویسی کنید.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `OMNIROUTE_ALLOW_DEFAULT_DATA_DIR`         | _(تنظیمنشده)_                 | `src/lib/dataPaths.ts`                                | راه فرار برای محافظ `DATA_DIR` در تست/eval ‏(#10428). تستها و کاوشهای eval/print در Node (`-e`/`--eval`/`-p`/`--print`، شامل شکلهای `--eval=`/`--print=`) که `DATA_DIR` ندارند، به یک دایرکتوری موقت یکبارمصرف هدایت میشوند تا نتوانند پایگاه داده واقعی اپراتور را باز کنند؛ برای استفاده مجدد از دایرکتوری واقعی، آن را روی `1` تنظیم کنید.                                                                                                                                                                                                                                                                                                                                                                                               |
| `OMNIROUTE_BUILD_SHA`                      | _(تنظیمنشده)_                 | `src/lib/monitoring/buildSha.ts`                      | SHA مربوط به Git برای artifact در حال اجرا. توسط `npm run build:release` ثبت میشود؛ در کانتینرهایی که بدون sentinelِ `dist/BUILD_SHA` عرضه میشوند، قابل تزریق است. در `/api/monitoring/health` با نام `system.buildSha` نمایش داده میشود.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `OMNIROUTE_RELEASE_REF`                    | `origin/main`                 | `scripts/build/buildProvenance.ts`                    | ارجاعی که دروازه منشأ pack-artifact، SHA بیلد را با آن بررسی میکند (#10427).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `OMNIROUTE_ALLOW_CANARY_BUILD`             | _(تنظیمنشده)_                 | `scripts/build/buildProvenance.ts`                    | برای اجازهدادن به بستهبندی بیلدی که SHA آن در خط انتشار نیست، روی `1` تنظیم کنید؛ در این حالت، بهجای شکست دروازه، بیلد بهعنوان یک canary عمدی ثبت میشود (#10427).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `OMNIROUTE_SMOKE_API_KEY`                  | _(تنظیمنشده)_                 | `scripts/ops/deploy-canary.mjs`                       | کلید API برای کاوش smoke مربوط به استقرار canary که در `/v1/chat/completions` بهصورت `Authorization: Bearer` ارسال میشود. فقط توسط اسکریپت استقرار استفاده میشود (#10429)، و هرگز توسط سرور استفاده نمیشود. این متغیر ارتباطی با متغیرهای `OMNIROUTE_SMOKE_*` مربوط به ابزار smoke اختیاری CLI (`RUN_CLI_SMOKE=1`،‏ `OMNIROUTE_SMOKE_BASE_URL/MODEL/API_KEY_ENV/TARGETS/TIMEOUT_MS` در `tests/integration/upstream-cli-smoke.int.test.ts`) ندارد — به [یکپارچهسازیهای CLI ← پیمایش smoke واقعی](../guides/CLI-INTEGRATIONS.md) مراجعه کنید.                                                                                                                                                                                                 |
| `OMNIROUTE_BUILDING`                       | _(تنظیمنشده)_                 | `src/lib/buildPhase.ts`                               | سیگنال مرحله بیلد (#10060): توسط `scripts/build/build-next-isolated.mjs` روی `1` تنظیم میشود و هر worker بیلدی که ایجاد میشود آن را به ارث میبرد؛ در نتیجه، لایه پایگاه داده بهجای بارگذاری افزونه بومی better-sqlite3 (که هنگام خروج، worker را متوقف میکند)، یک stub بدون عملیات برمیگرداند. هرگز برای سرور در حال اجرا تنظیم نمیشود.                                                                                                                                                                                                                                                                                                                                                                                                     |
| `OMNIROUTE_SKIP_NATIVE_DEP_CHECK`          | `0`                           | `scripts/check/check-native-deps.mjs`                 | برای ردکردن بررسی اختیاری پیش از بیلدِ وابستگیهای بومی در درختهای vendored نامتعارف، روی `1` تنظیم کنید. این کار وابستگی مفقود را قابل بیلد نمیکند؛ فقط زمانی استفاده کنید که وابستگیهای بومی از مسیری خارج از روال معمول تأمین شدهاند.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `OMNIROUTE_DATA_DIR`                       | _(تنظیمنشده)_                 | `open-sse/executors/promptql/threadSticky.ts`         | **نام مستعار جایگزین** برای `DATA_DIR` که فقط وقتی `DATA_DIR` تنظیم نشده باشد بررسی میشود. برای یافتن cache نشست thread-sticky روی دیسکِ اجراکننده PromptQL (`<dir>/promptql-thread-sessions.json`) استفاده میشود؛ اگر هیچیک از متغیرها تنظیم نشده باشند، cache فقط در حافظه باقی میماند (و میان راهاندازیهای مجدد ماندگار نیست).                                                                                                                                                                                                                                                                                                                                                                                                           |
| `OMNIROUTE_PLUGINS_DIR`                    | _(تنظیمنشده)_                 | `src/lib/plugins/scanner.ts`                          | دایرکتوریای که **اسکنر پلاگین زمان اجرا** میخواند — و ریشهای که مدیر پلاگین در آن نصب میکند — و پیشفرض مشتقشده از دایرکتوری خانه را بازنویسی میکند (#11827). در Docker/K8s، بهجای جابهجاکردن HOME صرفاً برای تغییر مسیر اسکن، آن را به درخت پلاگین bind-mountشده اشاره دهید (HOME تمام رفتارهای دیگر وابسته به دایرکتوری خانه را نیز کنترل میکند). تنظیمنشده = `~/.omniroute/plugins`، یا وقتی فرایند هیچ دایرکتوری خانهای صادر نمیکند، `/tmp/.omniroute/plugins` — این متغیر مشکل کشفنشدن بیسروصدا را برطرف میکند. دایرکتوری resolveشده هنگام راهاندازی یکبار با نام `scanner.dir_resolved` و همراه با ورودی برنده در log ثبت میشود. فقط سمت سرور: پلاگینهای فرمان CLI همچنان از `OMNIROUTE_PLUGIN_PATH` مخصوص خود استفاده میکنند (بخش 9). |
| `STORAGE_ENCRYPTION_KEY`                   | _(خالی = غیرفعال)_            | `src/lib/db/encryption.ts`                            | کلید AES برای رمزنگاری کامل پایگاه داده SQLite در حالت ذخیرهشده. آن را با `openssl rand -hex 32` تولید کنید.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `STORAGE_ENCRYPTION_KEY_VERSION`           | `v1`                          | `scripts/build/bootstrap-env.mjs`, `electron/main.js` | برچسب نسخه برای کلید رمزنگاری. هنگام چرخش کلید، آن را افزایش دهید تا رمزگشایی نسخههای پشتیبان قدیمی پشتیبانی شود.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `DISABLE_SQLITE_AUTO_BACKUP`               | `false`                       | `src/lib/db/backup.ts`                                | وقتی `true` باشد، از تهیه نسخههای پشتیبان معمول/پیش از نوشتن فایل SQLite صرفنظر میشود (ذخیره/پاکسازی قیمتگذاری models.dev و نوشتن تنظیمات). نسخههای پشتیبان دستی و پیش از بازیابی همچنان اجرا میشوند. این گزینه، اسنپشات ایمنی پایدار و اجباری اجراکننده مهاجرت یا محافظ مهاجرت انبوه برای یک پایگاه داده ماندگار موجود را غیرفعال **نمیکند**. تهیه نسخههای پشتیبان غیردستی به حداکثر یکبار در هر ۶۰ دقیقه محدود میشود. در داشبورد، **تنظیمات ← فضای ذخیرهسازی** میتواند پشتیبانگیری خودکار معمول را بهطور مستقل غیرفعال کند.                                                                                                                                                                                                               |
| `OMNIROUTE_CRYPT_KEY`                      | _(تنظیمنشده)_                 | `src/lib/db/encryption.ts`                            | **نام مستعار قدیمی** برای `STORAGE_ENCRYPTION_KEY`. در صورت نبود متغیر اصلی، بهعنوان جایگزین پذیرفته میشود.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `OMNIROUTE_API_KEY_BASE64`                 | _(تنظیمنشده)_                 | `src/lib/db/encryption.ts`                            | **نام مستعار قدیمی** (با قالب کدگذاریشده Base64) که بهعنوان جایگزین پذیرفته میشود. پیش از استفاده بهطور خودکار رمزگشایی میشود.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `OMNIROUTE_DB_HEALTHCHECK_INTERVAL_MS`     | _(تنظیمنشده)_                 | `src/lib/db/core.ts`                                  | بازه بررسی دورهای سلامت SQLite را بازنویسی میکند (میلیثانیه). وقتی تنظیم نشده باشد، مقادیر پیشفرض از `NODE_ENV` استخراج میشوند.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `OMNIROUTE_WAL_TRUNCATE_INTERVAL_MS`       | _(حذفشده)_                    | `src/lib/db/walMaintenance.ts`                        | **حذف شده است.** اجرای دورهای `wal_checkpoint(TRUNCATE)` در حالت فعال میتواند نگاشت مشترک wal-index را نامعتبر کند و فرایند را با SIGBUS (#13973) از کار بیندازد؛ بنابراین زمانبند دیگر وجود ندارد. این متغیر بیاثر است: یک مقدار مثبت، هشدار منسوخشدن را یکبار ثبت میکند، درحالیکه مقدار `0` یا تنظیمنشدن آن بدون پیام باقی میماند. WAL با checkpointهای PASSIVE (در زیر) نگهداری میشود و توسط checkpoint هنگام خاموششدن کوتاه میشود.                                                                                                                                                                                                                                                                                                      |
| `OMNIROUTE_WAL_PASSIVE_INTERVAL_MS`        | `300000` (۵ دقیقه)            | `src/lib/db/walMaintenance.ts`                        | بازه مکرر `wal_checkpoint(PASSIVE)` را بازنویسی میکند (میلیثانیه). فریمهای معلق WAL را کم نگه میدارد تا checkpointها سریع بمانند و اندازه فایل WAL در فاصله بین کوتاهسازیهای هنگام خاموششدن محدود بماند. `0` آن را غیرفعال میکند.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `OMNIROUTE_WAL_GUARD_MAX_MB`               | `256`                         | `src/lib/db/walMaintenance.ts`                        | وقتی یک نوبت PASSIVE تشخیص دهد که فایل WAL از این اندازه بزرگتر است، `wal_checkpoint(RESTART)` اجرا میشود تا WAL بدون بازنویسی wal-index نگاشتشده از ابتدا شروع کند. checkpointهای زنده در حالت truncate حذف شدهاند (ردیف `OMNIROUTE_WAL_TRUNCATE_INTERVAL_MS` را ببینید).                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `OMNIROUTE_PRESSURE_SELF_RESTART`          | `false`                       | `open-sse/utils/resourcePressure.ts`                  | آن را روی `1`/`true`/`yes`/`on` تنظیم کنید تا پس از تداوم فشار بحرانی منابع بهمدت `OMNIROUTE_PRESSURE_SELF_RESTART_AFTER_MS`، فرایند خارج شود و یک ناظر (systemd با `Restart=always` یا سیاست راهاندازی مجدد Docker) بتواند بهجای ارائه نامحدود خطاهای 503، یک فرایند سالم را دوباره راهاندازی کند.                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `OMNIROUTE_PRESSURE_SELF_RESTART_AFTER_MS` | `120000` (۲ دقیقه)            | `open-sse/utils/resourcePressure.ts`                  | مدتزمانی که فشار بحرانی باید ادامه یابد تا خروج ناشی از راهاندازی مجدد خودکار فعال شود.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `OMNIROUTE_SQLJS_WASM_PATH`                | _(تشخیص خودکار)_              | `src/lib/db/adapters/sqljsAdapter.ts`                 | مسیر صریح (مطلق یا نسبی به cwd) برای `sql-wasm.wasm` هنگام استفاده از آداپتور جایگزین WASM مربوط به `sql.js`. وقتی تنظیم نشده باشد، از طریق وابستگیهای بسته و چیدمانهای کاندید بهطور خودکار شناسایی میشود.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `OMNIROUTE_BATCH_RETENTION_DAYS`           | `30`                          | `src/lib/db/cleanup.ts`                               | تعداد روزهایی که checkpointها، فایلهای ورودی/خروجی/خطای ارجاعشده و ردیف یک کار نهاییشده Batch API (تکمیلشده/ناموفق/لغوشده/منقضیشده) پیش از حذف توسط پاکسازی خودکار نگه داشته میشوند. فقط پس از فعالشدن `BATCH_AND_FILE_AUTO_CLEANUP_ENABLED` اثر میگذارد و با بازه نگهداری خروجی Batch API خود OpenAI مطابقت دارد. بر مسیر `DELETE /api/v1/batches/delete-completed` که توسط اپراتور فعال میشود اثری ندارد؛ این مسیر طبق طراحی بدون قیدوشرط باقی میماند (بدون فیلتر سن).                                                                                                                                                                                                                                                                    |
| `BATCH_AND_FILE_AUTO_CLEANUP_ENABLED`      | `false`                       | `src/lib/db/cleanup.ts`                               | وقتی `true` باشد، پاکسازی خودکار اجازه مییابد کارهای نهاییشده Batch API (و checkpointهای آنها) را پس از گذشت `OMNIROUTE_BATCH_RETENTION_DAYS` حذف کند و محتوای BLOB فایلهای بارگذاریشدهای را که از `expires_at` خود گذشتهاند پاک کند. بهطور پیشفرض خاموش است: تمام نصبهای موجود این دادهها را دقیقاً مانند قبل نگه میدارند تا زمانی که اپراتور آن را فعال کند. همچنین یک پرچم قابلیت قابلویرایش در داشبورد است — `docs/reference/FEATURE_FLAGS.md` ← زمان اجرا را ببینید.                                                                                                                                                                                                                                                                   |
| `OMNIROUTE_SKIP_DB_HEALTHCHECK`            | `0`                           | `src/lib/db/core.ts`, `src/lib/db/healthCheck.ts`     | برای رد شدن کامل از بررسی سلامت DB هنگام راهاندازی، روی `1` تنظیم کنید. برای وظایف کوتاهمدت و آزمونهای یکپارچهسازی مفید است.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `OMNIROUTE_FORCE_DB_HEALTHCHECK`           | `0`                           | `src/lib/db/core.ts`                                  | برای فعالسازی اجباری حلقه بررسی سلامت DB، حتی زمانی که معمولاً از آن صرفنظر میشود (مثلاً در وظایف کوتاهمدت)، روی `1` تنظیم کنید.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `OMNIROUTE_SKIP_POSTINSTALL`               | `0`                           | `scripts/postinstall.mjs`                             | برای رد شدن از آمادهسازی اولیه محیط اجرای بومی در طول `npm install`، روی `1` تنظیم کنید. در نصبهای CI/بدون رابط گرافیکی که sqlite از قبل ساخته شده است مفید است.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `OMNIROUTE_MIGRATIONS_DIR`                 | _(تشخیص خودکار)_              | `src/lib/db/migrationRunner.ts`                       | دایرکتوری اسکنشده توسط اجراکننده مهاجرت را بازنویسی میکند. هنگام ارائه مهاجرتهای بستهبندیشده در بیلدهای سفارشی مفید است.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `OMNIROUTE_EXTRA_MIGRATIONS_DIRS`          | _(تنظیمنشده)_                 | `src/lib/db/migrationRunner/extraDirs.ts`             | دایرکتوریهای مهاجرت اضافی بهصورت ورودیهای `namespace=dir` که با جداکننده مسیر پلتفرم از هم جدا شدهاند (مثلاً `ee=/opt/app/enterprise/db/migrations`). فایلهای یافتشده در آنجا بهشکل `<namespace>-<number>` ثبت میشوند؛ بنابراین توزیعی که مهاجرتهای خودش را ارائه میکند، هرگز با جایگاههای عددی بالادستی تداخل پیدا نمیکند. یک ورودی نادرست، فضای نام نامعتبر یا دایرکتوری مفقود، بهجای نادیده گرفتن بیسروصدای شِما، هنگام راهاندازی خطا ایجاد میکند.                                                                                                                                                                                                                                                                                       |
| `OMNIROUTE_MAX_PENDING_MIGRATIONS`         | `50`                          | `src/lib/db/migrationRunner.ts`                       | آستانه ایمنی برای تعداد زیاد مهاجرتهای در انتظار (#3416). اگر در یک DB موجود، بیش از این تعداد مهاجرت در انتظار باشد، راهاندازی متوقف میشود (برای محافظت در برابر پاک شدن جدول رهگیری). برای بازیابی یک نسخه پشتیبان قدیمی، مقدار آن را افزایش دهید؛ برای غیرفعال کردن بررسی، روی `0` تنظیم کنید.                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `OMNIROUTE_INSTALL_UPGRADE_WORKDIR`        | _(`<repo>/.install-upgrade`)_ | `scripts/check/check-install-upgrade.mjs`             | دایرکتوری کاری برای دروازه انتشار `check:install-upgrade`. این فرایند تقریباً به 12 GB فضا نیاز دارد (دو درخت نصب حدوداً 3 GB بهعلاوه tarball)، بنابراین نباید روی یک tmpfs کوچک اجرا شود — در اجراکننده خودمیزبان، `/tmp` یک tmpfs مبتنی بر RAM با ظرفیت 12 GB است و دروازه انتشار آن را پر کرد و باعث ناقص شدن بسته شد.                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `OMNIROUTE_SPEND_FLUSH_INTERVAL_MS`        | _(پیشفرض در کد)_              | `src/lib/spend/batchWriter.ts`                        | فاصله زمانی تخلیه (ms) برای نویسنده دستهای مصرف/هزینه. مقادیر کمتر تجمیع عملیات نوشتن را کاهش میدهند؛ مقادیر بیشتر رقابت بر سر DB را کاهش میدهند.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `OMNIROUTE_SPEND_MAX_BUFFER_SIZE`          | _(پیشفرض در کد)_              | `src/lib/spend/batchWriter.ts`                        | حداکثر تعداد ورودیهای مصرف بافرشده پیش از تخلیه اجباری. در استقرارهای با QPS بالا آن را افزایش دهید؛ هنگامی که محدود بودن حافظه اهمیت بیشتری دارد، آن را کاهش دهید.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `OMNIROUTE_PROXY_FETCH_DEBUG`              | _(تنظیمنشده)_                 | `open-sse/utils/proxyFetch.ts`                        | برای انتشار گزارشهای اشکالزدایی `[ProxyFetch]` در مسیر رله Vercel، روی `"true"` تنظیم کنید. برای جلوگیری از افشای سرنخهای مسیریابی، بهطور پیشفرض غیرفعال است.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `PROXY_LOG_INCLUDE_IPS`                    | `false`                       | `src/lib/proxyLogger.ts`                              | برای گنجاندن IPهای کلاینت/خروجی و پیشوند حساب در خط مفصل گزارش فرایند `[ProxyEgress]`، روی `"true"` یا `"1"` تنظیم کنید. بهطور پیشفرض خاموش نگه داشته شده است تا گزارش فرایند، IPها یا پیشوند حساب را افشا نکند.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `OMNIROUTE_DEBUG`                          | _(تنظیمنشده)_                 | `bin/cli/commands/quota.mjs`                          | برای چاپ اطلاعات عیبیابی زمانبندی هر درخواست (`[omniroute] GET <path> completed in Nms`) از فرمانهای سهمیه CLI در stderr، روی `1` تنظیم کنید.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `OMNIROUTE_HEALTHCHECK_PATH`               | _(خودکار)_                    | `scripts/dev/healthcheck.mjs`                         | مسیر صریحی که توسط بررسی سلامت کانتینر کاوش میشود. اگر تنظیم نشده باشد، کاوشگر آن را از `OMNIROUTE_BASE_PATH` استخراج میکند؛ تنظیم آن، نقطه پایانی پایش عمیق را دوباره فعال میکند.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `OMNIROUTE_DEBUG_COMPLETION`               | _(تنظیمنشده)_                 | `bin/cli/commands/completion.mjs`                     | برای انتشار اطلاعات عیبیابی `[omniroute completion]` از مسیرهای حافظه نهان تکمیل پوسته CLI (خواندن/بازآوری/نوشتن)، روی هر مقدار غیرخالی تنظیم کنید. بهطور پیشفرض غیرفعال است — خطاهای این حافظههای نهان بدون پیام رخ میدهند تا حافظه نهان مفقود/خراب هرگز تکمیل با Tab را مختل نکند.                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `BATCH_RETRY_DURATION_MS`                  | `86400000` (24h)              | `open-sse/services/batchProcessor.ts`                 | حداکثر بازه تلاش مجدد برای آیتمهای منفرد دسته (ms). آیتمهایی که از این مدت فراتر بروند، شکستخورده علامتگذاری میشوند.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `BATCH_BACKOFF_BASE_MS`                    | `5000`                        | `open-sse/services/batchProcessor.ts`                 | تأخیر پایه (میلیثانیه) برای عقبنشینی نمایی در تلاشهای مجدد اقلام دسته.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `BATCH_BACKOFF_MAX_MS`                     | `3600000` (۱ ساعت)            | `open-sse/services/batchProcessor.ts`                 | سقف تأخیر (میلیثانیه) برای عقبنشینی نمایی بین تلاشهای مجدد اقلام دسته.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `BATCH_MAX_CONCURRENT`                     | `1`                           | `open-sse/services/batchProcessor.ts`                 | حداکثر تعداد دستههایی که بهطور همزمان پردازش میشوند. برای افزایش توان عملیاتی آن را بالا ببرید؛ برای جلوگیری از هجوم درخواستها به محدودیت نرخ، آن را پایین نگه دارید.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |

> [!IMPORTANT]
> پیش از تغییر یک پایگاه داده ماندگار موجود، اجراکننده مهاجرت یک اسنپشات کامل
> با آدرسدهی محتوامحور را در `DATA_DIR/db_backups/` منتشر میکند. انتشار به فایلسیستمی نیاز دارد
> که از پیوندهای سختِ بدون بازنویسی در همان فایلسیستم و همگامسازی پایدار فایل پشتیبانی کند. میزبانهای POSIX همچنین
> به همگامسازی دایرکتوری نیاز دارند؛ در Windows، ممکن است Node هندلهای دایرکتوری را رد کند، بنابراین OmniRoute فایل
> منتشرشده را تخلیه میکند و همگامسازی ورودی دایرکتوری را بهصورت بهترین تلاش ممکن انجام میدهد.
> اگر `DATA_DIR` متصلشده نتواند این تضمینها را فراهم کند، راهاندازی پیش از اعمال
> مهاجرت، بهصورت ایمن متوقف میشود. `DATA_DIR` را به یک والیوم دارای این قابلیتها منتقل کنید؛ برای دور زدن ایمنی مهاجرت از
> `DISABLE_SQLITE_AUTO_BACKUP` استفاده نکنید.

### سناریوها

| سناریو                       | پیکربندی                                                                                                      |
| ---------------------------- | ------------------------------------------------------------------------------------------------------------- |
| **توسعه محلی**               | همه مقادیر پیشفرض را حفظ کنید. پایگاه داده در `~/.omniroute/omniroute.db` قرار دارد.                          |
| **Docker**                   | `DATA_DIR=/data` + یک والیوم را در `/data` متصل کنید.                                                         |
| **رمزگذاریشده در حالت سکون** | `STORAGE_ENCRYPTION_KEY` را تنظیم کنید + از کلید نسخه پشتیبان نگه دارید! از دست دادن آن = از دست دادن دادهها. |
| **CI/آزمایش**                | `DATA_DIR=/tmp/omniroute-test` — موقتی است و نیازی به رمزگذاری ندارد.                                         |

---

## 3. شبکه و پورتها

| متغیر                                       | پیشفرض                                       | فایل منبع                                                                | توضیحات                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| ------------------------------------------- | -------------------------------------------- | ------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `PORT`                                      | `20128`                                      | `src/lib/runtime/ports.ts`                                               | درگاه اصلی برای رابط کاربری داشبورد و نقاط پایانی API (حالت تکدرگاهی).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `OMNIROUTE_BASE_PATH`                       | _(خالی = ریشه)_                              | `next.config.mjs`, `scripts/docker/ensure-docker-base-path.mjs`          | زیرمسیر URL برای ارائهٔ OmniRoute پشت یک پروکسی معکوس (`basePath` در Next.js را تنظیم میکند؛ تغییرمسیرهای احراز هویت از basePath آگاه هستند). برای مثال `/omniroute`. در Docker، مقدار هنگام `docker build` تعبیه میشود (`ARG OMNIROUTE_BASE_PATH`)؛ تصاویر ازپیشساختهشدهٔ ریشه میتوانند پیش از راهاندازی Next.js، یک مقدار متفاوت زمان اجرا را یکبار هنگام شروع کانتینر اعمال کنند. `NEXT_PUBLIC_BASE_URL` را روی مبدأ عمومی شامل همان زیرمسیر تنظیم کنید.                                                                                                                                                                                                                                                                                                                  |
| `NEXT_PUBLIC_OMNIROUTE_BASE_PATH`           | _(خالی = ریشه)_                              | `src/shared/hooks/useDisplayBaseUrl.ts`                                  | نسخهٔ قابلمشاهده در مرورگر از `OMNIROUTE_BASE_PATH` که هنگام ساخت درونگذاری میشود تا نمایش نقطهٔ پایانی داشبورد بهجای `https://host/v1`، مقدار `https://host/omniroute/v1` را نشان دهد. اگر تنظیم نشده باشد، از `OMNIROUTE_BASE_PATH` استفاده میکند. پس از تغییر، دوباره بسازید (`basePath` در Next در زمان ساخت تعیین میشود).                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `DASHBOARD_ALLOW_EMBED`                     | _(تنظیمنشده = هرگز قابل نمایش در فریم نیست)_ | `next.config.mjs`, `scripts/build/dashboardEmbed.mjs`                    | فعالسازی اختیاری جاسازی صفحات HTML در iframe. در حالت تنظیمنشده، همهٔ مسیرها با `frame-ancestors 'none'` و `X-Frame-Options: DENY` ارائه میشوند. آن را روی `vscode` تنظیم کنید تا صفحات (داشبورد، ورود، مستندات و صفحهٔ فرود) با `frame-ancestors 'self' vscode-webview:` و بدون `X-Frame-Options` ارائه شوند؛ در نتیجه VS Code Simple Browser میتواند آنها را رندر کند (حالت `dashboardOpen: "editor"` در OmniCopilot). سطح API (`/api`، `/v1`، `/v1beta`، `/a2a`، `/healthz` و نامهای مستعار سطح ریشه) در هر دو حالت هدرهای سختگیرانه را حفظ میکند. فقط `vscode` شناسایی میشود — `1`/`true` آن را فعال نمیکنند. زمان ساخت: پس از تغییر، دوباره بسازید (`docker build --build-arg DASHBOARD_ALLOW_EMBED=vscode` برای تصاویر؛ تنظیم آن روی یک نصب ازپیشساختهشده اثری ندارد). |
| `API_PORT`                                  | _(تنظیمنشده)_                                | `src/lib/runtime/ports.ts`                                               | در صورت تنظیم، API پروکسی `/v1/*` را روی این درگاه جداگانه ارائه میکند.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `API_HOST`                                  | `0.0.0.0`                                    | `src/lib/runtime/ports.ts`                                               | آدرس اتصال برای درگاه API.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `DASHBOARD_PORT`                            | _(تنظیمنشده)_                                | `src/lib/runtime/ports.ts`                                               | در صورت تنظیم، رابط کاربری داشبورد را روی این درگاه جداگانه ارائه میکند.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `OMNI_MAX_CONCURRENT_CONNECTIONS`           | `0` _(غیرفعال)_                              | `src/sse/utils/backpressure.ts`                                          | تعداد اتصالهای همزمان و در حال پردازش چت را محدود میکند؛ درخواستهای بیشتر از سقف، پاسخ `503` همراه با `Retry-After` دریافت میکنند. یک عدد صحیح مثبت محافظ را فعال میکند؛ تنظیمنشدن/`0` آن را غیرفعال میکند.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `OMNIROUTE_INSTANCE_ID`                     | _(تنظیمنشده)_                                | `src/shared/resilience/peerRouting.ts`                                   | شناسهٔ پایدار و یکتا برای این دروازه هنگام زنجیرهکردن نمونههای OmniRoute. بررسی حلقهٔ همتای ورودی را فعال میکند. نویسههای مجاز: حروف، ارقام، `.`, `_`, `:` و `-`؛ حداکثر ۶۴ نویسه.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `OMNIROUTE_PEER_URLS`                       | _(تنظیمنشده)_                                | `src/shared/resilience/peerRouting.ts`, `open-sse/executors/base.ts`     | URLهای پایهٔ OmniRoute با جداکنندهٔ ویرگول که میتوانند `X-OmniRoute-Peer-Trace` را دریافت کنند. فقط URLهای بالادستی که صراحتاً در فهرست مجاز قرار گرفتهاند، فرادادهٔ همتا را دریافت میکنند؛ سایر ارائهدهندگان بدون تغییر باقی میمانند.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `OMNIROUTE_PEER_MAX_HOPS`                   | `4`                                          | `src/shared/resilience/peerRouting.ts`                                   | حداکثر تعداد نمونههای OmniRoute که پیشتر بازدید شدهاند و در یک درخواست زنجیرهای پذیرفته میشوند (`1`-`32`). نمونههای تکراری یا بودجهٔ تمامشده، پاسخ HTTP با کد `508 Loop Detected` برمیگردانند.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `PROD_DASHBOARD_PORT`                       | `20130`                                      | `docker-compose.prod.yml`                                                | درگاه منتشرشده در سمت میزبان برای داشبورد در حالت تولید Docker.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `PROD_API_PORT`                             | `20131`                                      | `docker-compose.prod.yml`                                                | درگاه منتشرشده در سمت میزبان برای API در حالت تولید Docker.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `OMNIROUTE_PORT`                            | _(تنظیمنشده)_                                | `src/lib/runtime/ports.ts`                                               | هنگام اجرا در Electron یا پوششدهندههای دیگر، نسبت به `PORT` اولویت دارد.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `LIVE_WS_PORT`                              | `20129`                                      | `src/server/ws/liveServer.ts`                                            | درگاه سرور پایش زندهٔ بلادرنگ WebSocket.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `LIVE_WS_HOST`                              | `127.0.0.1`                                  | `src/server/ws/liveServer.ts`                                            | آدرس اتصال برای سرور زندهٔ WebSocket. برای در دسترس قرار دادن آن در LAN، روی `0.0.0.0` تنظیم کنید (`LIVE_WS_ALLOWED_ORIGINS` را نیز پیکربندی کنید).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `LIVE_WS_ALLOWED_ORIGINS`                   | _(تنظیمنشده)_                                | `src/server/ws/liveServer.ts`                                            | مبدأهای اضافی با جداکنندهٔ ویرگول که مجاز به بازکردن WebSocket زنده هستند. مبدأهای داشبورد loopback بهطور پیشفرض از قبل مجازند.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `LIVE_WS_ALLOWED_HOSTS`                     | _(تنظیمنشده)_                                | `src/server/ws/liveServerAllowList.ts`                                   | نامهای میزبان اضافی با جداکنندهٔ ویرگول که برای مبدأهای WebSocket زنده مجازند. برخلاف `LIVE_WS_ALLOWED_ORIGINS` (URLهای کامل مبدأ)، فقط بخش میزبان را تطبیق میدهد — برای پیکربندیهای LAN/Tailscale مفید است.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `NEXT_PUBLIC_LIVE_WS_PUBLIC_URL`            | _(تنظیمنشده)_                                | `src/hooks/useLiveDashboard.ts`                                          | URL عمومی WebSocket داشبورد زنده (سمت مرورگر). هنگام قراردادن سرور WS پشت پروکسی معکوس یا Cloudflare Tunnel آن را تنظیم کنید (برای مثال `wss://ws.my-ai.com/live-ws`)؛ مرورگر بهجای `ws://hostname:20132` به آنجا متصل میشود. بخش نام مسیر نیز بهعنوان مسیر ارتقای WebSocket استفاده میشود (پیشفرض: `/live-ws`).                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `OMNIROUTE_ENABLE_LIVE_WS`                  | `true`                                       | `src/server/ws/liveServer.ts` و `scripts/start-ws-server.mjs`            | برای غیرفعالکردن سرور بلادرنگ WebSocket، آن را روی `0` یا `false` تنظیم کنید (بهطور پیشفرض فعال و متصل به loopback است). گزینهٔ CI/harness که اسکریپت کمکی مستقل WebSocket زنده را غیرفعال میکند.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `RELAY_IP_PER_MINUTE`                       | `30`                                         | `src/app/api/v1/relay/chat/completions/route.ts`                         | محدودیت نرخ relay بهازای (توکن، IP)، برحسب درخواست در دقیقه. درون حافظه و بهازای هر نمونه. `0` یا مقدار منفی، محدودیت بُعد IP را غیرفعال میکند (محدودیت پایگاه داده بهازای توکن همچنان اعمال میشود).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `NODE_ENV`                                  | `production`                                 | هستهٔ Next.js                                                            | میزان جزئیات ثبت رویداد، کشکردن، نمایش جزئیات خطا و بهینهسازیهای Next.js را کنترل میکند.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `OMNIROUTE_USE_TURBOPACK`                   | `1` (Turbopack — پیشفرض کد)                  | `package.json` / Next.js 16                                              | Turbopack بستهساز پیشفرض برای `npm run dev` و `npm run build` است (ساختها طبق بنچمارک ۲ تا ۳ برابر سریعترند). برای بازگشت به webpack در Windows، هنگام مواجهه با ناسازگاریهای بایندینگ بومی / سازگاری بستهساز، **یا روی دستگاههای دارای محدودیت RAM**، آن را روی `0` تنظیم کنید — مشخص است که ساختهای تولیدی Turbopack در این شاخهٔ نسخهٔ Next.js (16.2.x)، در گرافهای ماژولی بزرگ نسبت به webpack به اوج مصرف حافظهٔ بسیار بالاتری میرسند (اصلاح تخلیهٔ حافظهٔ Turbopack در Next 16.3 هنوز پایدار نشده است)؛ حالت جایگزین webpack اوج مصرف بسیار پایینتری دارد. #6409 را ببینید.                                                                                                                                                                                            |
| `OMNIROUTE_SKIP_DB_HEALTHCHECK`             | _(تنظیمنشده)_                                | `src/lib/db/core.ts` / `src/lib/db/healthCheck.ts`                       | برای ردکردن بررسی سلامت یکپارچگی SQLite هنگام راهاندازی، روی `1` تنظیم کنید. برای راهاندازی سریعتر با پایگاههای دادهٔ بزرگ مفید است.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `NOTIFY_SOCKET`                             | _(تنظیمنشده)_                                | systemd (پروتکل sd_notify)                                               | هنگامی که فرایند زیر یک واحد سرویس دارای یکپارچهسازی sd_notify اجرا میشود، توسط systemd تنظیم میشود؛ OmniRoute آن را میخواند (`OMNIROUTE_DISABLE_SD_NOTIFY` را ببینید) تا اعلانهای READY/WATCHDOG را ارسال کند. هرگز نباید توسط کاربر تنظیم شود.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `OMNIROUTE_DISABLE_SD_NOTIFY`               | _(تنظیمنشده)_                                | `scripts/dev/systemd-notify.mjs`                                         | برای غیرفعالکردن systemd sd_notify (`Type=notify` / `WatchdogSec=`)، حتی هنگام اجرا زیر یک واحد systemd، روی `1` تنظیم کنید. در خارج از systemd، اعلانگر در هر صورت هیچ عملی انجام نمیدهد.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `CREDENTIAL_HEALTH_CHECK_INTERVAL`          | `300000`                                     | `open-sse/config/constants.ts` / `src/lib/credentialHealth/scheduler.ts` | فاصلهٔ زمانی (میلیثانیه) برای زمانبند پسزمینهٔ بررسی سلامت اعتبارنامهها. حداقل: 10000 (۱۰ ثانیه).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `CREDENTIAL_HEALTH_CACHE_TTL`               | `300000`                                     | `open-sse/config/constants.ts` / `src/lib/credentialHealth/cache.ts`     | TTL (میلیثانیه) برای وضعیت سلامت اعتبارنامهٔ کششده.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `OMNIROUTE_DISABLE_CREDENTIAL_HEALTH_CHECK` | `false`                                      | `src/lib/credentialHealth/scheduler.ts`                                  | برای غیرفعالکردن آزمون دورهای پسزمینهٔ اتصالهای ارائهدهندگان، روی `1` یا `true` تنظیم کنید. ارائهدهندگان جستوجو (`SEARCH_VALIDATOR_CONFIGS` در `src/lib/providers/validation/searchProviders.ts`، برای مثال `tavily-search`) همیشه از این پیمایش کنار گذاشته میشوند — «اعتبارسنجی» آنها یک کوئری واقعی و هزینهدار به بالادست است، بنابراین هرگز طبق زمانبندی بررسی سلامت نمیشوند (#9970).                                                                                                                                                                                                                                                                                                                                                                                    |
| `HOST`                                      | `0.0.0.0`                                    | `scripts/dev/run-next.mjs`                                               | آدرس اتصال برای سرور dev/start در Next.js. در صورت تنظیم، پیشفرض `0.0.0.0` را بازنویسی میکند.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `HOSTNAME`                                  | `127.0.0.1`                                  | `scripts/dev/run-next-playwright.mjs`                                    | آدرس اتصالی که اجراکنندهٔ Playwright هنگام راهاندازی Next.js استفاده میکند. برای آزمونهای ایزوله، بهطور پیشفرض `127.0.0.1` است. **برای `omniroute serve` استفاده نکنید** — بهجای آن از `OMNIROUTE_SERVER_HOST` استفاده کنید (پوستههای POSIX بهطور خودکار `HOSTNAME` را روی نام دستگاه تنظیم میکنند؛ `.env` نمیتواند آن را بازنویسی کند).                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `OMNIROUTE_SERVER_HOST`                     | `0.0.0.0`                                    | `bin/cli/commands/serve.mjs`                                             | آدرس اتصال برای `omniroute serve`. از تداخل با متغیر `HOSTNAME` پوستهٔ POSIX جلوگیری میکند (که همیشه توسط bash/zsh روی نام دستگاه تنظیم میشود). در صورت تنظیمنشدن، به `0.0.0.0` برمیگردد. (#6194)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |

### حالتهای پورت

```
┌─────────────────────────── پورت واحد (پیشفرض) ────────────────────────────┐
│  PORT=20128                                                                 │
│  → داشبورد: http://localhost:20128                                          │
│  → API:      http://localhost:20128/v1/chat/completions                     │
└─────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────── پورتهای مجزا ──────────────────────────────────┐
│  DASHBOARD_PORT=20128                                                       │
│  API_PORT=20129                                                             │
│  API_HOST=0.0.0.0                                                           │
│  → داشبورد: http://localhost:20128                                          │
│  → API:      http://0.0.0.0:20129/v1/chat/completions                      │
│  کاربرد: ارائه API به LAN و در عین حال محدود کردن داشبورد به localhost.     │
└─────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────── Docker در محیط عملیاتی ────────────────────────┐
│  PROD_DASHBOARD_PORT=443   PROD_API_PORT=8443                              │
│  → پورتهای کانتینر را در docker-compose.prod.yml به پورتهای میزبان نگاشت میکند. │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 4. امنیت و احراز هویت

| متغیر                                       | مقدار پیشفرض                                                                               | فایل منبع                                                | توضیحات                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| ------------------------------------------- | ------------------------------------------------------------------------------------------ | -------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `MACHINE_ID_SALT`                           | `endpoint-proxy-salt`                                                                      | `src/lib/auth`                                           | Salt ترکیبشده با شناسههای سختافزاری برای ایجاد اثر انگشت ماشین. برای جداسازی، آن را در هر استقرار تغییر دهید.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `OMNIROUTE_CLI_SALT`                        | _(تنظیمنشده = Salt تصادفی مختص هر نصب که در `<DATA_DIR>/cli-token-salt.json` ذخیره میشود)_ | `src/lib/machineToken.ts`                                | Salt مربوط به HMAC برای استخراج توکن احراز هویت CLI محلی. تنظیم این مقدار، همه توکنهای CLI روی ماشین را تعویض میکند و همیشه نسبت به Salt ذخیرهشده اولویت دارد. به `docs/security/CLI_TOKEN.md` مراجعه کنید.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `AUTH_COOKIE_SECURE`                        | `false`                                                                                    | `src/lib/auth`                                           | پرچم `Secure` را روی کوکیهای نشست تنظیم میکند. هنگام اجرا پشت HTTPS، **باید `true` باشد**.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `REQUIRE_API_KEY`                           | `false`                                                                                    | میانافزار API                                            | وقتی `true` باشد، همه درخواستهای پروکسی `/v1/*` باید شامل یک کلید API معتبر باشند. **این پرچم دسترسی به `GET /v1/models` را کنترل نمیکند**؛ این مسیر در عوض از وضعیت ورود داشبورد (`requireAuthForModels`) پیروی میکند—بنابراین دریافت `401` از `/v1/models` به این معنا نیست که استنتاج محافظت شده است. به `docs/security/INFERENCE_AUTH_POSTURE.md` مراجعه کنید (#13695).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `ALLOW_API_KEY_REVEAL`                      | `false`                                                                                    | `src/shared/constants/featureFlagDefinitions.ts`         | امکان نمایش کامل مقادیر کلید API را در رابط کاربری داشبورد فراهم میکند. از طریق پرچمهای قابلیت داشبورد قابل پیکربندی است؛ در نمونههای اشتراکی خطر امنیتی دارد.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `NO_LOG_API_KEY_IDS`                        | _(خالی)_                                                                                   | `src/lib/compliance/index.ts`                            | شناسههای کلید API جداشده با ویرگول که ثبت درخواست را دور میزنند (برای انطباق با GDPR).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `DEFAULT_RATE_LIMIT_PER_DAY`                | _(تنظیمنشده = نامحدود)_                                                                    | `src/shared/utils/apiKeyPolicy.ts`                       | بودجه جایگزین روزانه درخواست که برای کلیدهای API با ستون `rate_limits` برابر با null اعمال میشود. تنظیمنشده یا خالی: بدون محدودیت ضمنی (#2289، #11017). مقدار `0` نیز همین معنا را دارد (نامحدود). عدد صحیح مثبت N، محدودیت N در روز، 5N در هفته و 20N در ماه را فعال میکند. مقادیر غیرخالیِ نامعتبر به محدودیتهای قدیمی 1000 در روز، 5000 در هفته و 20000 در ماه بازمیگردند.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `MAX_BODY_SIZE_BYTES`                       | `10485760` (10 MB)                                                                         | `src/shared/middleware/bodySizeGuard.ts`                 | حداکثر اندازه مجاز بدنه درخواست. محمولههایی را که از این محدودیت فراتر روند رد میکند.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `OMNIROUTE_CHAT_LARGE_BODY_BYTES`           | `262144` (256 KB)                                                                          | `src/shared/middleware/chatBodyAdmission.ts`             | بدنههای واقعی درخواست با اندازه برابر یا بیشتر از این آستانه، پیش از تجزیه JSON، اجاره پذیرش سنگینوزن اتمی و محلیِ فرایند را دریافت میکنند (مسیر BYTE، شامل `POST /v1/responses`). همان راه گریزِ فضای آزاد سالم [#10437](https://github.com/diegosouzapw/OmniRoute/pull/10437) که برای ساختارهای سنگین استفاده میشود؛ همچنان توسط `OMNIROUTE_CHAT_MAX_INFLIGHT_BYTES` / [#10110](https://github.com/diegosouzapw/OmniRoute/issues/10110) محدود است تا مشکل [#7849](https://github.com/diegosouzapw/OmniRoute/issues/7849) دوباره رخ ندهد.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `OMNIROUTE_CHAT_HARD_MAX_BODY_BYTES`        | `52428800` (50 MB)                                                                         | `src/shared/middleware/chatBodyAdmission.ts`             | سقف سخت مسیر چت که بر اساس بایتهای خواندهشده طی دریافت کراندار اعمال میشود؛ از جمله برای درخواستهایی که `Content-Length` ندارند یا مقدار آن نامعتبر یا خلاف واقع است؛ موارد بیشازحد پاسخ `413` دریافت میکنند.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `OMNIROUTE_CHAT_MAX_HEAVY_IN_FLIGHT`        | _(تنظیمنشده — بدون سقف تعداد درخواست)_                                                     | `src/shared/middleware/chatBodyAdmission.ts`             | **#503-fanout:** این سقف قدیمیِ تعداد درخواست اکنون فقط زمانی اعمال میشود که صراحتاً تنظیم شده باشد. وقتی تنظیم نشده باقی بماند (حالت پیشفرض)، پذیرش چتهای سنگین در عوض با `OMNIROUTE_CHAT_MAX_INFLIGHT_BYTES` کنترل میشود — بودجهای بر حسب بایت که بهطور خودکار بر اساس سقف واقعی حافظهٔ فرایند در **یک فرایند** (یک heap از V8) تعیین میشود. دو درخواست همپوشان `/v1/responses` با حدود 750k توکن، heapهای تقریباً 12 GiB را از کار میاندازند (#7849) — این یک هشدار دربارهٔ **بودجهٔ حافظه** است، نه سقف سخت محصول برابر با 2. یک فرایند سالم (با heap پایینتر از نسبت کاهش بار) ممکن است از طریق `OMNIROUTE_CHAT_ADMISSION_HEALTHY_HEADROOM` تعداد بیشتری درخواست طولانیِ همزمان `/v1/responses` را بپذیرد. دهها کلاینت طولانی SSE (40–50) به heap و `OMNIROUTE_CHAT_MAX_INFLIGHT_BYTES` / #10110 مربوط است، نه «حداکثر 2». افزایش کورکورانهٔ این مقدار برای «استفاده از میزبان» دوباره مشکل #7849 را ایجاد میکند. **heapها** را با **N عدد `DATA_DIR` مستقل** تکثیر کنید (#11024)؛ هرگز روی یک فایل SQLite از `replicas>1` استفاده نکنید. |
| `OMNIROUTE_CHAT_MAX_INFLIGHT_BYTES`         | _(مشتقشده بهصورت خودکار)_                                                                  | `src/shared/middleware/admissionBudget.ts`               | **#503-fanout:** بازنویسی بودجهٔ بایتی دریافت که بهصورت خودکار مشتق میشود (25٪ از محدودترِ سقف حافظهٔ V8/cgroup، تقسیم بر ضریب تقویت موقتی 8x). مقادیر مشتقشده و صریح به بازهٔ 8 MiB تا 2 GiB محدود میشوند. بدنهای بزرگتر از بودجهٔ مؤثر، بلافاصله با `413 body_exceeds_budget` شکست میخورد؛ رقابت میان بدنههایی که هرکدام بهتنهایی قابل سرویسدهی هستند همچنان با `503` قابل تلاش مجدد است. 40–50 کلاینت طولانیِ همزمان SSE حاصل این بودجه بهعلاوهٔ heap است، نه یک «حداکثر 2» سخت. پیش از تنظیم، `chatAdmission.maxInflightBytes` / `budgetSource` / `pressureSeverity` را در `/api/monitoring/health` بخوانید.                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `OMNIROUTE_CHAT_ADMISSION_HEAP_SHED_RATIO`  | `0.75`                                                                                     | `src/shared/middleware/chatBodyAdmission.ts`             | نسبت کاهش بار تحت فشار heap (`heapUsed / heap_size_limit`) برای پذیرش سنگینِ BYTE و STRUCTURE (#10183، #10268، #10437). یک درخواست سنگین همزمان که از `OMNIROUTE_CHAT_MAX_HEAVY_IN_FLIGHT` عبور کرده باشد، فقط زمانی با `503` قابل تلاش مجدد کنار گذاشته میشود که heap نیز در این نسبت یا بالاتر از آن باشد؛ در heap سالم، درخواست از طریق فضای اضافهٔ سالم پذیرفته میشود.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `OMNIROUTE_CHAT_ADMISSION_HEALTHY_HEADROOM` | `OMNIROUTE_CHAT_MAX_HEAVY_IN_FLIGHT` (پیشفرض `1`)                                          | `src/shared/middleware/chatBodyAdmission.ts`             | ظرفیت اضافی کراندار برای مسیر سریعِ heap سالم (#10437)، هم برای STRUCTURE و هم BYTE (`admitChatRequest`، شامل بدنههای ≥ `OMNIROUTE_CHAT_LARGE_BODY_BYTES`). بدون این کران، هر درخواست در حالت شلوغ ولی با heap سالم، پذیرش را بدون هیچ سقفی دور میزد. پس از فعال شدن این تعداد lease همزمان از طریق مسیر heap سالم، درخواستهای شلوغِ بعدی به همان مسیر انتظار کراندار/کاهش بار میروند که هنگام فشار واقعی heap استفاده میشود. `0` این میانبُر را کاملاً غیرفعال میکند.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `OMNIROUTE_CHAT_HEAVY_MESSAGE_COUNT`        | `200`                                                                                      | `src/shared/middleware/chatBodyAdmission.ts`             | تعداد پیامهایی که باعث میشود یک درخواست چت حتی در صورت کمتر بودن بدنهاش از آستانهٔ بایتی، سنگین طبقهبندی شود.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `OMNIROUTE_CHAT_HEAVY_TOOL_COUNT`           | `64`                                                                                       | `src/shared/middleware/chatBodyAdmission.ts`             | تعداد ابزارهایی که باعث میشود یک درخواست چت حتی در صورت کمتر بودن بدنهاش از آستانهٔ بایتی، سنگین طبقهبندی شود.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `OMNIROUTE_CHAT_HEAVY_ESTIMATED_TOKENS`     | `32000`                                                                                    | `src/shared/middleware/chatBodyAdmission.ts`             | برآورد محافظهکارانهٔ توکن بر اساس اندازهٔ رشته که یک درخواست را سنگین طبقهبندی میکند؛ این یک شاخص تقریبی هزینهٔ پذیرش است، نه توکنسازی صورتحساب ارائهدهنده.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `OMNIROUTE_CHAT_HARD_MAX_MESSAGES`          | `0` (غیرفعال)                                                                              | `src/shared/middleware/chatBodyAdmission.ts`             | سقف اختیاری و نیازمند فعالسازی برای تاریخچهٔ چت. بهطور پیشفرض غیرفعال است: تعداد پیام یک سیاست استقرار است، نه ویژگی همگانی یک درخواست، و اعمال سقف در اینجا مکالمهها را پیش از آنکه خط لولهٔ فشردهسازی بتواند آنها را قابل سرویسدهی کند، با `413` نهایی رد میکند. رشد heap با `OMNIROUTE_CHAT_MAX_HEAVY_IN_FLIGHT` و کاهش بار تحت فشار heap کراندار میشود. در استقرارهای دارای محدودیت حافظه که به سقف سخت نیاز دارند، یک مقدار مثبت تنظیم کنید؛ موارد بیشازحد سپس پاسخ ساختیافتهٔ `413` با الزام فشردهسازی دریافت میکنند.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `OMNIROUTE_MAX_NONSTREAMING_RESPONSE_BYTES` | `67108864` (64 MB)                                                                         | `open-sse/handlers/chatCore/nonStreamingResponseBody.ts` | سقف سخت برای پاسخ بالادستیِ غیرجریانی که بهطور کامل در حافظه بافر میشود. پس از عبور از این سقف، خوانندهٔ بالادستی لغو میشود و درخواست سریعاً شکست میخورد، بهجای آنکه یک رشتهٔ بدون کران تا زمان تمام شدن heap رشد کند.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `OMNIROUTE_FORWARDING_HEADER_BUDGET_BYTES`  | `768`                                                                                      | `open-sse/handlers/chatCore/responseHeaders.ts`          | حداکثر تعداد بایتهای ارسالی که از سرآیندهای پاسخ بالادستی منتقل میشوند. وقتی از این سقف عبور شود، سرآیندهای با اولویت پایینتر (برای مثال، `x-codex-*` سفارشی و `x-oai-request-id`) حذف میشوند تا محدودیتهای رایج سرآیند در پروکسی معکوس رعایت شوند. برای انتقال فرادادههای بیشتر از بالادست، این مقدار را افزایش دهید؛ البته به بهای افزایش اندازه سرآیند پاسخ.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `CORS_ORIGIN`                               | _(تنظیمنشده)_                                                                              | `src/server/cors/origins.ts`                             | فهرست مجاز قدیمی CORS برای یک مبدأ واحد. برای استقرارهای جدید، `CORS_ALLOWED_ORIGINS` را ترجیح دهید. CORS فقط برای کلاینتهای API مرورگر با مبدأ متفاوت است؛ عملیات نوشتن احراز هویتشده در داشبورد، در عوض از درخواستهای هممبدأ بههمراه محافظت CSRF متصل به نشست استفاده میکنند.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `CORS_ALLOWED_ORIGINS`                      | _(تنظیمنشده)_                                                                              | `src/server/cors/origins.ts`                             | فهرست مجاز CORS با مقادیر جداشده با ویرگول. هیچ نویسه عامی ارسال نمیشود، مگر اینکه `CORS_ALLOW_ALL=true` صراحتاً پیکربندی شده باشد.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `CORS_ALLOW_ALL`                            | `false`                                                                                    | `src/server/cors/origins.ts`                             | راه گریز مختص محیط توسعه برای بازتاب هر `Origin` مرورگر. آن را در استقرارهای اشتراکی یا عملیاتی فعال نکنید.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `OUTBOUND_SSRF_GUARD_ENABLED`               | `true`                                                                                     | `src/shared/network/outboundUrlGuard.ts`                 | فراخوانیهای ارائهدهنده را که محدودههای IP خصوصی، loopback یا link-local را هدف میگیرند، مسدود میکند. فقط در محیطهای آزمایشی ایزوله غیرفعال کنید.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `OMNIROUTE_ALLOW_PRIVATE_PROVIDER_URLS`     | `false`                                                                                    | `src/shared/network/outboundUrlGuard.ts`                 | URLهای ارائهدهنده را که به شبکههای خصوصی/محلی اشاره میکنند (localhost، 192.168.x.x، 10.x.x.x و غیره) مجاز میکند. **برای ارائهدهندگان خودمیزبان الزامی است** (LM Studio، Ollama، vLLM، Llamafile، Triton، SearXNG). وقتی مقدار `false` باشد، داشبورد اعتبارسنجی URLهای محلی را رد میکند.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `OMNIROUTE_ALLOW_LOCAL_PROVIDER_URLS`       | `true`                                                                                     | `src/shared/network/outboundUrlGuard.ts`                 | افزودن/اعتبارسنجی ارائهدهندگان روی آدرسهای محلی/خصوصی (127.0.0.1، localhost، LAN، محدودههای خصوصی) را مجاز میکند — با دامنه محدود به مسیر اعتبارسنجی ارائهدهنده. **مقدار پیشفرض `true` است** (اولویت با محیط محلی)؛ برای اعمال مسدودسازی سختگیرانه و صرفاً عمومی، آن را روی `false` تنظیم کنید. نقاط پایانی فراداده ابری (169.254.169.254، metadata.google.internal) در هر صورت مسدود باقی میمانند. (#5066)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `AUDIO_REMOTE_PROVIDER_NODES`               | `false`                                                                                    | `src/app/api/v1/_shared/audioProviderNodes.ts`           | به مسیرهای `/v1/audio/*` (رونویسی، گفتار و ترجمه) اجازه میدهد از یک گره ارائهدهنده سازگار با OpenAI که خارج از localhost میزبانی میشود، استفاده کنند. بهطور پیشفرض غیرفعال است — مسیریابی صوت به میزبان راهدور، هویت خروجی را تغییر میدهد و باید تصمیمی صریح از سوی اپراتور باشد. گرههای loopback/خصوصی (localhost، 127.0.0.1، 172.16-31.x) همیشه مجاز و بدون تأثیر هستند. (#3963)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `RERANK_REMOTE_PROVIDER_NODES`              | `false`                                                                                    | `src/app/api/v1/_shared/rerankProviderNodes.ts`          | به `POST /v1/rerank` (و مرحله بازرتبهبندی loopback موتور حافظه) اجازه میدهد از یک گره ارائهدهنده سازگار با OpenAI که خارج از localhost میزبانی میشود استفاده کند — یک دستگاه LAN یا همتای Tailscale که TEI، Infinity، vLLM و غیره را اجرا میکند. بهطور پیشفرض غیرفعال است — مسیریابی به میزبان راهدور، هویت خروجی را تغییر میدهد و باید تصمیمی صریح از سوی اپراتور باشد. گرههای loopback (localhost، 127.0.0.1، 172.16-31.x) همیشه مجاز و بدون تأثیر هستند. گرههای راهدور همچنین باید از سیاست URL خروجی ارائهدهنده (`OMNIROUTE_ALLOW_LOCAL_PROVIDER_URLS` / `OMNIROUTE_ALLOW_PRIVATE_PROVIDER_URLS`) عبور کنند؛ هیچگاه به میزبانهای فراداده ابری مسیریابی نمیشود.                                                                                                                                                                                                                                                                                                                                                                             |
| `OMNIROUTE_OIDC_DISABLE_PASSWORD_LOGIN`     | `false`                                                                                    | `src/app/api/auth/login/route.ts`                        | وقتی OIDC فعال است، ورود با گذرواژه را غیرفعال میکند تا کاربران فقط بتوانند از طریق ورود یکپارچه OIDC احراز هویت کنند. نام مستعار ساده `OIDC_DISABLE_PASSWORD_LOGIN` نیز پذیرفته میشود؛ پرچم قابلیت داشبورد با همین کلید اولویت دارد. (#10889)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `OIDC_DISABLE_PASSWORD_LOGIN`               | `false`                                                                                    | `src/app/api/auth/login/route.ts`                        | نام مستعار ساده `OMNIROUTE_OIDC_DISABLE_PASSWORD_LOGIN` (#10889).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |

### چکلیست مقاومسازی

```bash
# حداقل الزامات امنیتی محیط عملیاتی:
AUTH_COOKIE_SECURE=true        # به HTTPS نیاز دارد
REQUIRE_API_KEY=true           # همه فراخوانیهای پروکسی را احراز هویت کنید
ALLOW_API_KEY_REVEAL=false     # هرگز کلیدها را در رابط کاربری نمایش ندهید
CORS_ALLOWED_ORIGINS=https://your.domain.com
MAX_BODY_SIZE_BYTES=5242880    # محدودیت 5 MB
```

---

## ۵. پاکسازی ورودی و حفاظت از اطلاعات قابلشناسایی شخصی (PII)

OmniRoute یک دفاع دولایه ارائه میدهد: اسکن تزریق در سمت درخواست و حذف اطلاعات قابلشناسایی شخصی در سمت پاسخ.

> **⚠️ محدودیتها:** این سازوکارهای حفاظتی، تشخیصهای ابتکاریِ _مبتنی بر بهترین تلاش_ هستند، نه یک فایروال کامل برای تزریق پرامپت یا یک سامانه DLP برای اطلاعات قابلشناسایی شخصی. آنها ممکن است نتایج مثبت کاذب (علامتگذاری پرامپتهای بیخطرِ پرسونا/RPG) و نتایج منفی کاذب (الگوهای leetspeak، فاصلهگذاری یا الگوهای غیرانگلیسی) ایجاد کنند. این سازوکارها بهتنهایی برای انطباق کافی نیستند. پیش از اتکا به آنها، حالتها را تنظیم و با ترافیک خود آزمایش کنید.

### سمت درخواست: محافظ تزریق پرامپت

| متغیر                             | پیشفرض        | فایل منبع                                | توضیحات                                                                                                                                                                                                              |
| --------------------------------- | ------------- | ---------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `INPUT_SANITIZER_ENABLED`         | `true`        | `src/middleware/promptInjectionGuard.ts` | اسکن پیامهای ورودی برای یافتن الگوهای تزریق پرامپت را فعال میکند.                                                                                                                                                    |
| `INPUT_SANITIZER_MODE`            | `warn`        | `src/middleware/promptInjectionGuard.ts` | سیاست تزریق: `warn` = فقط ثبت در گزارش، `block` = رد درخواست با کد 400. حالت قدیمی `redact` متن تزریق را حذف **نمیکند**؛ برای بازنویسی اطلاعات قابلشناسایی شخصی در درخواست، از `PII_REDACTION_ENABLED` استفاده کنید. |
| `INJECTION_GUARD_MODE`            | _(تنظیمنشده)_ | `src/middleware/promptInjectionGuard.ts` | نام مستعار قدیمی برای `INPUT_SANITIZER_MODE` — با همان رفتار.                                                                                                                                                        |
| `INPUT_SANITIZER_BLOCK_THRESHOLD` | `high`        | `src/shared/utils/injectionSeverity.ts`  | حداقل شدتی که `MODE=block` رد میکند: `high` (پیشفرض)، `medium` یا `low`. الگوهای متوسط، مگر اینکه این آستانه کاهش یابد، فقط مشاهده میشوند.                                                                           |
| `INJECTION_GUARD_BLOCK_THRESHOLD` | _(تنظیمنشده)_ | `src/shared/utils/injectionSeverity.ts`  | نام مستعار قدیمی برای `INPUT_SANITIZER_BLOCK_THRESHOLD` — با همان رفتار.                                                                                                                                             |
| `PII_REDACTION_ENABLED`           | `false`       | `src/lib/guardrails/piiMasker.ts`        | در صورت `true` بودن، اطلاعات قابلشناسایی شخصی را در درخواستهای ورودی حذف میکند (مستقل از حالت تزریق).                                                                                                                |
| `CREDENTIAL_REDACTION_ENABLED`    | `false`       | `src/lib/guardrails/credentialMasker.ts` | الگوهای شناختهشده کلید API / توکن محرمانه را از بارهای داده درخواست/پاسخ حذف میکند. اختیاری است و از `PII_REDACTION_ENABLED` پیروی میکند.                                                                            |

### سمت پاسخ: پاکساز اطلاعات قابلشناسایی شخصی

| متغیر                            | پیشفرض   | فایل منبع                 | توضیحات                                                                                              |
| -------------------------------- | -------- | ------------------------- | ---------------------------------------------------------------------------------------------------- |
| `PII_RESPONSE_SANITIZATION`      | `false`  | `src/lib/piiSanitizer.ts` | پاسخهای LLM را پیش از بازگرداندن به کلاینت، برای یافتن اطلاعات قابلشناسایی شخصیِ افشاشده اسکن میکند. |
| `PII_RESPONSE_SANITIZATION_MODE` | `redact` | `src/lib/piiSanitizer.ts` | `redact` = پوشاندن اطلاعات قابلشناسایی شخصی، `warn` = فقط ثبت در گزارش، `block` = حذف کامل پاسخ.     |

### پاکساز زمینه مسیر توکندار VS Code

| متغیر                               | پیشفرض | فایل منبع                                   | توضیحات                                                                                                                                                                                                                                                                           |
| ----------------------------------- | ------ | ------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `OMNIROUTE_VSCODE_SANITIZE_CONTEXT` | `1`    | `src/app/api/v1/vscode/contextSanitizer.ts` | زمینه ضمنی ویرایشگر فعال (`editorContext`، `activeEditor`، `currentFile`، `selection`، `openTabs` و غیره) را از درخواستهای `/v1/vscode/[token]/*` حذف میکند و محتوای فایلهای حساسِ صراحتاً پیوستشده را میپوشاند. بهطور پیشفرض امن است؛ برای غیرفعالسازی، آن را روی `0` قرار دهید. |

### سناریوها

| سناریو             | پیکربندی                                                                                                                                                                                                          |
| ------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **انطباق سازمانی** | `INPUT_SANITIZER_ENABLED=true`، `INPUT_SANITIZER_MODE=block`، `PII_REDACTION_ENABLED=true`، `PII_RESPONSE_SANITIZATION=true` (مسدودسازی تزریق + حذف اطلاعات قابلشناسایی شخصی در درخواست/پاسخ؛ حالتها مستقل هستند) |
| **فقط پایش**       | `INPUT_SANITIZER_ENABLED=true`، `INPUT_SANITIZER_MODE=warn` — ثبت در گزارش بدون مسدودسازی                                                                                                                         |
| **استفاده شخصی**   | همه را غیرفعال نگه دارید — بدون سربار                                                                                                                                                                             |

---

## 6. سیاستهای ابزار و مسیریابی

| متغیر                                                       | پیشفرض                       | فایل منبع                                  | توضیحات                                                                                                                                                                                                                                                                                                                                                                                                                 |
| ----------------------------------------------------------- | ---------------------------- | ------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `TOOL_POLICY_MODE`                                          | `disabled`                   | `src/lib/toolPolicy.ts`                    | دسترسی LLM به ابزارها/فراخوانی توابع را کنترل میکند. `allowlist` = فقط ابزارهای فهرستشده، `denylist` = همه بهجز ابزارهای فهرستشده، `disabled` = بدون محدودیت.                                                                                                                                                                                                                                                           |
| `OMNIROUTE_PAYLOAD_RULES_PATH`                              | `./config/payloadRules.json` | `open-sse/services/payloadRules.ts`        | مسیر فایل JSON حاوی قواعد دستکاری payload (تنظیمات بالادستی مختص هر مدل/پروتکل).                                                                                                                                                                                                                                                                                                                                        |
| `OMNIROUTE_PAYLOAD_RULES_RELOAD_MS`                         | `5000`                       | `open-sse/services/payloadRules.ts`        | بازهٔ بارگذاری مجدد (میلیثانیه) برای بازخوانی آنی فایل قواعد payload. حداقل `1000`.                                                                                                                                                                                                                                                                                                                                     |
| `OMNIROUTE_PREFER_CLAUDE_CODE_FOR_UNPREFIXED_CLAUDE_MODELS` | `false`                      | `open-sse/services/model.ts`               | قابلیت اختیاری: شناسههای مدل سادهٔ `claude-*` را از کلاینتهای Claude Code، بهجای الزام به پیشوند ارائهدهنده، از طریق حساب OAuth مربوط به Claude Code مسیریابی میکند. پیشوندهای صریح ارائهدهنده همچنان اولویت دارند. همچنین از طریق کلید تغییر وضعیت در داشبوردِ صفحهٔ ارائهدهندهٔ Claude قابل پیکربندی است.                                                                                                             |
| `COMBO_CONCURRENCY_PER_MODEL`                               | `3`                          | `open-sse/services/comboConfig.ts`         | سقف همزمانی بهازای هر مدل برای ترکیبهای نوبتگردشی (#9100). سمافور ترکیب نوبتگردشی با سقف ثابت ۳ درخواست همزمان بهازای هر مدل و بدون امکان بازنویسی محدود شده بود و ترافیک با همزمانی بالاتر را پشت این سقف بهصورت سریالی پردازش میکرد. مقدار باید `>= 1` باشد و حداکثر به `<= 32` محدود میشود.                                                                                                                          |
| `DISABLE_CONTEXT_WINDOW_CHECKS`                             | `false`                      | `open-sse/handlers/chatCore.ts`            | گزینهٔ اختیاری خطرناکی که بررسی محلی پنجرهٔ زمینه / حداکثر توکن ورودی OmniRoute را برای درخواستهای مستقیم تکمدلی نادیده میگیرد. ارائهدهندگان بالادستی همچنان محدودیتهای واقعی خود را اعمال میکنند؛ فشردهسازی prompt و سقف توکن خروجی خود مدل نیز فعال باقی میمانند. ترتیب مؤثر اولویت عبارت است از: بازنویسی پایگاه دادهٔ Feature Flags > متغیر محیطی > مقدار پیشفرض؛ نیازی به راهاندازی مجدد نیست.                     |
| `OMNIROUTE_SELF_HOSTED_PROVIDERS`                           | _(تنظیمنشده)_                | `open-sse/services/selfHostedEntry.ts`     | سند YAML درونخطی `providers:` ‏(RIC-738، D4). در صورت تنظیم (با یا بدون بلوک `strategy:`)، مسیر `/v1/chat/completions` بهجای pipeline ابری، به ورودی یکپارچهٔ self-hosted سازگار با OpenAI هدایت میشود. در حالت تنظیمنشده (حالت پیشفرض)، مسیر مستقیماً به pipeline ابری موجود منتقل میشود. به `docs/routing/SELF_HOSTED_OPENAI_ENTRY.md` مراجعه کنید.                                                                   |
| `OMNIROUTE_SELF_HOSTED_PROVIDERS_FILE`                      | _(تنظیمنشده)_                | `open-sse/services/selfHostedEntry.ts`     | مسیر یک فایل YAML حاوی همان سند `providers:` موجود در `OMNIROUTE_SELF_HOSTED_PROVIDERS`، برای استقرارهایی که فایل را به متغیر محیطی درونخطی ترجیح میدهند. تنظیم هرکدام از این دو، ورودی self-hosted را فعال میکند.                                                                                                                                                                                                      |
| `OMNIROUTE_SELF_HOSTED_API_KEY`                             | _(تنظیمنشده — مسیر باز)_     | `open-sse/services/selfHostedEntry.ts`     | کلید API مشترک و اختیاری برای ورودی یکپارچهٔ self-hosted (چارچوب اولیهٔ D5، رزروشده برای سیستم سهمیهبندی بهازای هر کلید). در صورت تنظیم، درخواستها باید شامل `Authorization: Bearer <key>` باشند. در حالت تنظیمنشده، مسیر باز است و با الگوی موجودِ ارائهدهندهٔ محلی self-hosted مطابقت دارد (استقرار در شبکهٔ loopback/مورداعتماد).                                                                                    |
| `OMNIROUTE_SELF_HOSTED_STRATEGY`                            | _(تنظیمنشده)_                | `open-sse/services/routingStrategies.ts`   | سند YAML درونخطی `strategy:` برای موتور مسیریابی قطعی (M2/RIC-740، D3) — فهرست سیاه/سفید، قطعکنندهٔ دورهٔ انتظار، اولویت هزینه، آگاه از تأخیر و زنجیرهٔ جایگزین. بلوک درونخطی `strategy:` تودرتو در `OMNIROUTE_SELF_HOSTED_PROVIDERS` را بهازای هر کلید بازنویسی میکند. به `docs/routing/DETERMINISTIC_ROUTING.md` مراجعه کنید.                                                                                         |
| `OMNIROUTE_SELF_HOSTED_STRATEGY_FILE`                       | _(تنظیمنشده)_                | `open-sse/services/routingStrategies.ts`   | مسیر یک فایل YAML حاوی همان سند `strategy:` موجود در `OMNIROUTE_SELF_HOSTED_STRATEGY`، برای استقرارهایی که فایل را به متغیر محیطی درونخطی ترجیح میدهند.                                                                                                                                                                                                                                                                 |
| `OMNIROUTE_DISABLE_CONVERSATION_TRACKING`                   | _(تنظیمنشده)_                | `open-sse/services/conversationTracker.ts` | برای توقف جمعآوری تاریخچهٔ مکالمه، آن را روی `1` تنظیم کنید. `resolveConversationId()` پیش از خواندن SQLite یا تجزیهٔ تاریخچهٔ پیامها، یک نتیجهٔ ردیابینشده برمیگرداند؛ شناسههای نشست ارائهشده توسط کلاینت نیز شامل این رفتار میشوند. مدیریت نشست مسیریابی بدون تغییر باقی میماند و رکوردهای موجود حذف نمیشوند. مناسب برای استقرارهایی که از نمای مکالمهٔ داشبورد استفاده نمیکنند و میخواهند رشد جدول نوبتها متوقف شود. |

---

## 7. نشانیهای URL و همگامسازی ابری

| متغیر                                    | پیشفرض                                          | فایل منبع                                        | توضیحات                                                                                                                                                                                                                                                                                                                                                                         |
| ---------------------------------------- | ----------------------------------------------- | ------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `BASE_URL`                               | `http://localhost:20128`                        | `src/lib/cloudSync.ts`                           | نشانی URL سمت سرور که وظایف همگامسازی داخلی برای فراخوانی `/api/sync/cloud` از آن استفاده میکنند. حتی زمانی که برنامه از طریق پراکسی بهصورت عمومی در دسترس است، این مقدار را بهصورت یک URL لوپبک/کانتینر نگه دارید.                                                                                                                                                             |
| `CLOUD_URL`                              | _(خالی)_                                        | `src/lib/cloudSync.ts`                           | نشانی URL نقطه پایانی رله ابری (قابلیت ویژه).                                                                                                                                                                                                                                                                                                                                   |
| `CLOUD_SYNC_TIMEOUT_MS`                  | `12000`                                         | `src/lib/cloudSync.ts`                           | مهلت زمانی HTTP برای درخواستهای همگامسازی ابری.                                                                                                                                                                                                                                                                                                                                 |
| `OMNIROUTE_BUILD_PROFILE`                | `full`                                          | پیکربندی ساخت Webpack                            | پروفایل زمان ساخت (برای حذف فیزیکی ماژولهای دارای دسترسی ویژه از باندل، روی `minimal` تنظیم کنید).                                                                                                                                                                                                                                                                              |
| `OMNIROUTE_STANDALONE_DIR`               | _خروجی مستقل .build/_                           | `scripts/build/colocate-standalone.mjs`          | بازنویسی زمان ساخت برای دایرکتوری خروجی مستقل که در مرحله هممکانسازی پس از ساخت استفاده میشود. این یک تنظیم زمان اجرا نیست.                                                                                                                                                                                                                                                     |
| `OMNIROUTE_CLOUD_SYNC_SECRET`            | _(خالی)_                                        | `src/lib/cloudSync.ts`                           | راز مشترک مورداستفاده برای تأیید امضای HMAC-SHA256 پاسخهای همگامسازی ابری.                                                                                                                                                                                                                                                                                                      |
| `OMNIROUTE_CLOUD_SYNC_SECRETS`           | `false`                                         | `src/lib/cloudSync.ts`                           | برای اینکه نقطه پایانی همگامسازی ابری بتواند اعتبارنامههای محلی را بازنویسی کند، روی `true` تنظیم کنید. مقدار پیشفرض `false` است.                                                                                                                                                                                                                                               |
| `OMNIROUTE_CLOUD_SYNC_ENFORCE_SIGNATURE` | `false`                                         | `src/lib/cloudSync.ts`                           | برای رد کردن پاسخ بدون امضای همگامسازی ابری در زمانی که هیچ راز محلی پیکربندی نشده است، روی `true` تنظیم کنید (#13679). امضای موجود همواره تأیید میشود — و هنگامی که `OMNIROUTE_CLOUD_SYNC_SECRET` تنظیم نشده باشد، صرفنظر از این پرچم، همیشه رد میشود. از v3.9 به بعد، مقدار پیشفرض به حالت اجباری تغییر میکند.                                                                |
| `OMNIROUTE_ZED_IMPORT_LEGACY_ONE_STEP`   | `false`                                         | `src/app/api/providers/zed/import/route.ts`      | برای بازگشت به رفتار یکمرحلهای «وارد کردن همهچیز» در v3.8.5 بدون تأیید کاربر، روی `true` تنظیم کنید.                                                                                                                                                                                                                                                                            |
| `NEXT_PUBLIC_BASE_URL`                   | `http://localhost:20128`                        | OAuth، داشبورد، همگامسازی                        | نشانی URL عمومی برای redirect_uri مربوط به OAuth، پیوندهای داشبورد و URLهای عمومی تولیدشده. هنگامی که فراخوانیهای بازگشتی OAuth یا پیوندهای مرورگر تولیدشده باید از یک میزبان متعارف پراکسی معکوس استفاده کنند، این مقدار را روی URL عمومی پایدار تنظیم کنید.                                                                                                                   |
| `NEXT_PUBLIC_CLOUD_URL`                  | _(خالی)_                                        | سمت کلاینت                                       | همتای سمت کلاینت `CLOUD_URL`.                                                                                                                                                                                                                                                                                                                                                   |
| `NEXT_PUBLIC_APP_URL`                    | _(تنظیمنشده)_                                   | `src/shared/services/cloudSyncScheduler.ts`      | گزینهٔ بازگشت قدیمی برای `NEXT_PUBLIC_BASE_URL`.                                                                                                                                                                                                                                                                                                                                |
| `OMNIROUTE_PUBLIC_BASE_URL`              | _(تنظیمنشده)_                                   | حلکنندهٔ مبدأ عمومی، URLهای تصویر                | مبدأ OmniRoute قابلمشاهده برای مرورگر با بالاترین اولویت که برای تولید URLهای عمومی و اعتبارسنجی مبدأ مرورگر خارج از داشبورد استفاده میشود. این مقدار را زمانی تنظیم کنید که OpenWebUI یا رلهای دیگر از طریق یک URL داخلی به OmniRoute دسترسی دارد، اما مرورگر کاربر باید رسانهٔ تولیدشده را از مبدأ LAN، تونل یا عمومی دریافت کند. `/v1` را **اضافه نکنید**.                   |
| `OMNIROUTE_PROVIDER_MANIFEST_URL`        | _(تنظیمنشده)_                                   | `open-sse/config/providerPluginManifestUrl.ts`   | URL مطلق مانیفست افزونهٔ ارائهدهنده که به کلاینتهای sidecar اعلام میشود. وقتی تنظیم نشده باشد، OmniRoute مسیر `/api/v1/provider-plugin-manifest` را از مبدأ درخواست یا HOST/PORT استخراج میکند.                                                                                                                                                                                 |
| `OMNIROUTE_PUBLIC_PROTOCOL`              | `http`                                          | `open-sse/config/providerPluginManifestUrl.ts`   | پروتکلی که هنگام استخراج URL مانیفست افزونهٔ ارائهدهنده از HOST/PORT بدون وجود مبدأ درخواست استفاده میشود. اگر `OMNIROUTE_PROVIDER_MANIFEST_URL` صریحی تنظیم نشده است، در پشت یک پراکسی عمومی خاتمهدهندهٔ TLS آن را روی `https` تنظیم کنید.                                                                                                                                     |
| `OMNIROUTE_TRUST_PROXY`                  | _(تنظیمنشده)_                                   | `src/server/origin/publicOrigin.ts`              | حالت اعتماد اختیاری برای هدرهای هدایتشدهٔ مبدأ عمومی. تنظیمنشده = برای تصمیمهای امنیتی به `Forwarded` / `X-Forwarded-*` اعتماد نکنید. `true` / `loopback` تنها از پراکسی loopback دارای مهر توکن به میزبان/پروتکل هدایتشده اعتماد میکند. `private` / `lan` به همتایان پراکسی LAN خصوصی نیز اعتماد میکند. در محیط عملیاتی، `NEXT_PUBLIC_BASE_URL` صریح را ترجیح دهید.            |
| `KIE_CALLBACK_URL`                       | _(تنظیمنشده)_                                   | `open-sse/utils/kieTask.ts`                      | URL عمومی callback برای کارهای ناهمگام kie.ai. بازنویسی با بالاترین اولویت، پیش از `OMNIROUTE_KIE_CALLBACK_URL` و `OMNIROUTE_PUBLIC_URL`.                                                                                                                                                                                                                                       |
| `OMNIROUTE_KIE_CALLBACK_URL`             | _(تنظیمنشده)_                                   | `open-sse/utils/kieTask.ts`                      | نگارش جایگزین `KIE_CALLBACK_URL`. هنگامی که متغیر اصلی تنظیم نشده باشد، بهعنوان گزینهٔ بازگشت استفاده میشود.                                                                                                                                                                                                                                                                    |
| `OMNIROUTE_PUBLIC_URL`                   | _(تنظیمنشده)_                                   | `open-sse/utils/kieTask.ts`                      | مبدأ عمومی مورداستفاده برای ساخت URLهای callback ناهمگام. گزینهٔ بازگشت با کمترین اولویت برای callbackهای kie.ai؛ همچنین بهعنوان یک URL عمومی کلی برای رلههای دیگر استفاده میشود.                                                                                                                                                                                               |
| `OMNIROUTE_CROF_USAGE_URL`               | `https://crof.ai/usage_api/`                    | `open-sse/services/usage.ts`                     | نقطهٔ پایانی جستوجوی سهمیهٔ CrofAI که صفحهٔ Usage از آن استفاده میکند. برای رلهها / دادههای آزمایشی بازنویسی کنید.                                                                                                                                                                                                                                                              |
| `OMNIROUTE_OPENCODE_QUOTA_URL`           | `https://opencode.ai/zen/go/v1/usage`           | `open-sse/services/opencodeQuotaFetcher.ts`      | نقطهٔ پایانی رسمی مصرف OpenCode Go با احراز هویت API key که صفحهٔ Usage از آن استفاده میکند. برای رلهها / دادههای آزمایشی بازنویسی کنید.                                                                                                                                                                                                                                        |
| `OPENCODE_SYNTHESIZE_CLI_HEADERS`        | `true`                                          | `open-sse/executors/opencode.ts`                 | هدرهای هویتی OpenCode CLI ‏(User-Agent، x-opencode-client/project و UUIDهای درخواست/نشست) را در درخواستهای بالادستی opencode-go/zen که کلاینت ارسال نکرده است تولید میکند تا Cloudflare آنها را در خروجی VPS بپذیرد (#6210/#5997). از #10571 بهطور پیشفرض فعال است؛ با `false`/`0`/`no`/`off` غیرفعالش کنید.                                                                    |
| `OPENCODE_USER_AGENT`                    | `opencode/1.18.31`                              | `open-sse/utils/opencodeHeaders.ts`              | User-Agent پیشفرضی که وقتی `OPENCODE_SYNTHESIZE_CLI_HEADERS` فعال است و بازنویسی اختصاصی `<PROVIDER>_USER_AGENT` برای هر ارائهدهنده تنظیم نشده، استفاده میشود. فقط روی اجراکنندههای opencode اعمال میشود. در یک درخواست بدون کلید که بالادست آن را محدود میکند، مقدار پیکربندیشدهای که شامل `opencode/<version >= 1.17>` نباشد، بهجای رد شدن با این مقدار پیشفرض جایگزین میشود. |
| `OPENCODE_CLIENT`                        | `desktop`                                       | `open-sse/executors/opencode.ts`                 | مقدار هدر تولیدشدهٔ `x-opencode-client` هنگامی که `OPENCODE_SYNTHESIZE_CLI_HEADERS` فعال است.                                                                                                                                                                                                                                                                                   |
| `OPENCODE_PROJECT`                       | `global`                                        | `open-sse/executors/opencode.ts`                 | مقدار هدر ترکیبی `x-opencode-project` هنگامی که `OPENCODE_SYNTHESIZE_CLI_HEADERS` فعال است.                                                                                                                                                                                                                                                                                     |
| `OPENCODE_FREE_TIER_REQUEST_CONTRACT`    | _(تنظیمنشده)_                                   | `open-sse/executors/opencodeFreeTierContract.ts` | برای متوقفکردن تنظیم بدنهٔ درخواستهای OpenCode بدون کلید (پرچم استریم و فهرست ابزارها)، روی `off` تنظیم کنید. هدرها همچنان اعمال میشوند. این مقدار برای هر درخواست خوانده میشود، بنابراین تغییر آن بلافاصله اعمال میشود.                                                                                                                                                        |
| `OPENCODE_FREE_TIER_PLACEHOLDER_TOOLS`   | _(تنظیمنشده)_                                   | `open-sse/executors/opencodeFreeTierContract.ts` | نام ابزارهایی با جداکنندهٔ ویرگول که باید در یک درخواست OpenCode بدون کلید و فاقد ابزار اعلام شوند، در صورتی که هنوز چیزی برای آن مدل مشاهده نشده باشد. مقدار خالی باعث استفاده از یک ابزار نگهدارندهٔ تکی میشود که به مدل گفته میشود آن را فراخوانی نکند. حداکثر ۳۲ ورودی با الگوی `[A-Za-z_][A-Za-z0-9_-]{0,63}`؛ ورودیهای نامعتبر نادیده گرفته میشوند.                       |
| `OMNIROUTE_OLLAMA_CLOUD_USAGE_URL`       | `https://ollama.com/settings`                   | `open-sse/services/usage.ts`                     | نشانی URL تنظیمات Ollama Cloud که برای استخراج سهمیه استفاده میشود. برای رلهها / دادههای آزمایشی بازنویسی کنید.                                                                                                                                                                                                                                                                 |
| `OLLAMA_USAGE_COOKIE`                    | _(تنظیمنشده)_                                   | `open-sse/services/usage.ts`                     | کوکی `__Secure-session` مربوط به Ollama Cloud که برای استخراج سهمیه از صفحهٔ تنظیمات استفاده میشود. حساس است؛ هنگامی که چند حساب پیکربندی شدهاند، فیلد مخصوص هر اتصال در داشبورد ترجیح داده میشود.                                                                                                                                                                              |
| `OLLAMA_CLOUD_USAGE_COOKIE`              | _(تنظیمنشده)_                                   | `open-sse/services/usage.ts`                     | متغیر محیطی جایگزین برای کوکی `__Secure-session` مربوط به Ollama Cloud. حساس است؛ هنگامی که چند حساب پیکربندی شدهاند، فیلد مخصوص هر اتصال در داشبورد ترجیح داده میشود.                                                                                                                                                                                                          |
| `OMNIROUTE_OLLAMA_USAGE_COOKIE`          | _(تنظیمنشده)_                                   | `open-sse/services/usage.ts`                     | متغیر محیطی جایگزین برای کوکی `__Secure-session` مربوط به Ollama Cloud که پیش از نامهای مستعار کوتاهتر استفاده میشود. حساس است؛ هنگامی که چند حساب پیکربندی شدهاند، فیلد مخصوص هر اتصال در داشبورد ترجیح داده میشود.                                                                                                                                                            |
| `OMNIROUTE_CODEWHISPERER_BASE_URL`       | `https://codewhisperer.us-east-1.amazonaws.com` | `open-sse/services/usage.ts`                     | نقطهٔ پایانی محدودیتهای مصرف CodeWhisperer (AWS Kiro). برای رلهها / دادههای آزمایشی بازنویسی کنید.                                                                                                                                                                                                                                                                              |

> [!IMPORTANT]
> هنگام استقرار پشت یک پراکسی معکوس (nginx، Caddy)، اگر فراخوانهای برگشتی OAuth یا پیوندهای عمومی تولیدشده باید از آن نام میزبان استفاده کنند، `NEXT_PUBLIC_BASE_URL` را روی نشانی URL عمومی پایدار خود تنظیم کنید (برای مثال، `https://omniroute.example.com`). بدون این تنظیم، فراخوانهای برگشتی OAuth ممکن است ناموفق باشند، زیرا `redirect_uri` مطابقت نخواهد داشت و پیوندهای عمومی تولیدشده ممکن است به مبدأ داخلی کانتینر اشاره کنند.
>
> برای کارهای سروربهسرور، `BASE_URL` را بهصورت یک نشانی داخلی loopback/کانتینر نگه دارید. برای واکشیهای داخلی از خود سامانه که حاوی اطلاعات اعتباری هستند، از `Origin` مرورگر یا نام میزبان عمومی استفاده نکنید.
>
> نوشتنهای احراز هویتشدهٔ داشبورد به نشانی پایهٔ عمومی ثابت نیاز ندارند: داشبورد درخواستهای ناامن هممبدأ را همراه با یک توکن CSRF متصل به نشست ارسال میکند. OmniRoute همچنان اعتبارسنجی مبدأ عمومی را برای یکپارچهسازیهای مرورگری خارج از داشبورد متمرکز میکند: ابتدا به متغیرهای محیطی صریح نشانی URL عمومی اعتماد میشود؛ هدرهای خام `Forwarded` / `X-Forwarded-*` نادیده گرفته میشوند، مگر اینکه `OMNIROUTE_TRUST_PROXY` فعال باشد و همتای پراکسی بلافصل با توکن بهعنوان مورداعتماد علامتگذاری شده باشد. برای رفع مشکلات درخواستهای هممبدأ داشبورد از تنظیمات CORS استفاده نکنید؛ CORS فقط برای کلاینتهای مرورگری میانمبدأ است.

---

## ۸. پروکسی خروجی

فراخوانیهای ارائهدهنده بالادستی LLM را برای کنترل ترافیک خروجی، مسیریابی جغرافیایی یا پنهانسازی IP از طریق یک پروکسی HTTP یا SOCKS5 هدایت کنید.

| متغیر                                    | پیشفرض        | فایل منبع                                    | توضیحات                                                                                                                                                                                                                                                                                                                                                                              |
| ---------------------------------------- | ------------- | -------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `ENABLE_SOCKS5_PROXY`                    | `true`        | `open-sse/executors`                         | عامل پراکسی SOCKS5 را برای فراخوانیهای بالادستی فعال میکند. برای غیرفعالسازی، مقدار `false` را تنظیم کنید.                                                                                                                                                                                                                                                                           |
| `NEXT_PUBLIC_ENABLE_SOCKS5_PROXY`        | `true`        | سمت کلاینت                                   | آگاهی سمت کلاینت از در دسترس بودن SOCKS5.                                                                                                                                                                                                                                                                                                                                            |
| `PROXY_SKIP_RECENTLY_FAILED`             | `false`       | `src/shared/utils/featureFlags.ts`           | پرچم قابلیت انتخابی (به [FEATURE_FLAGS.md](./FEATURE_FLAGS.md) مراجعه کنید؛ بازنویسی DB در داشبورد اولویت دارد). مخازن پراکسی و چرخش مختص هر حساب، عضوی را که بهتازگی ناموفق بوده است (رد شدن پروب TCP یا دریافت خطای 429 از طریق آن) برای مدتی که با هر تکرار دو برابر میشود، تا سقفی مشخص، دوباره ارائه نمیکنند. مقدار `true` (یا `1`، `yes`) آن را فعال میکند.                    |
| `HTTP_PROXY`                             | _(تنظیمنشده)_ | استاندارد Node.js                            | پراکسی HTTP برای فراخوانیهای بالادستی.                                                                                                                                                                                                                                                                                                                                               |
| `HTTPS_PROXY`                            | _(تنظیمنشده)_ | استاندارد Node.js                            | پراکسی HTTPS برای فراخوانیهای بالادستی.                                                                                                                                                                                                                                                                                                                                              |
| `ALL_PROXY`                              | _(تنظیمنشده)_ | استاندارد Node.js                            | پراکسی عمومی (از `socks5://` پشتیبانی میکند).                                                                                                                                                                                                                                                                                                                                        |
| `OMNIROUTE_PROXY_ECHO_URL`               | _(تنظیمنشده)_ | `src/lib/proxyEchoTarget.ts`                 | مقصد بازتاب IP مورداستفاده پروبهای خروجی پراکسی را به یک URL واحد ثابت میکند. در صورت تنظیمنشدن، پروب ابتدا `api64.ipify.org` و سپس `api4.ipify.org` را امتحان میکند تا تونلهای صرفاً IPv4 بهاشتباه ازکارافتاده گزارش نشوند (#9694).                                                                                                                                                 |
| `NO_PROXY`                               | _(تنظیمنشده)_ | استاندارد Node.js                            | نامهای میزبان/IPها با جداکننده ویرگول برای دور زدن پراکسی.                                                                                                                                                                                                                                                                                                                           |
| `OMNIROUTE_PROXY_DISPATCHER_CONNECTIONS` | `32`          | `open-sse/utils/proxyDispatcher.ts`          | حداکثر تعداد سوکتهای همزمان برای هر توزیعکننده پراکسی HTTP/SOCKS کششده. جریانهای SSE طولانیمدت مانند Codex `/v1/responses`، هنگامی که چند درخواست از پراکسی یکسان در سطح حساب استفاده میکنند، به بیش از یک اتصال نیاز دارند. مقادیر بالاتر از `256` محدود میشوند.                                                                                                                    |
| `SOCKS_HANDSHAKE_TIMEOUT_MS`             | `10000`       | `open-sse/utils/socksConnectorWithFamily.ts` | مهلت زمانی دستدهی (اتصال) SOCKS5 برحسب میلیثانیه. هنگامی که یک میزبان درگاه مسکونی با همزمانی بالا مواجه است (برای مثال، 100 درخواست همزمان)، آن را افزایش دهید — در یک مخزن اشباعشده، دستدهی واقعی ممکن است بیش از 10 ثانیه طول بکشد، حتی اگر پراکسی قابل دسترس باشد؛ در غیر این صورت، خطای کاذب `[Proxy Fast-Fail] Proxy unreachable` نمایش داده میشود. حداکثر مقدار `120000` است. |
| `PROXY_FAIL_OPEN`                        | `false`       | `src/sse/handlers/chatHelpers.ts`            | وقتی `false` باشد (پیشفرض)، درخواستی که پراکسی تخصیصیافتهاش resolve نمیشود، بهجای بازگشت به اتصال مستقیم، **رد میشود (fail-closed)** — این کار از نشت IP واقعی جلوگیری میکند. برای بازیابی fallback قدیمی DIRECT، مقدار `true` را تنظیم کنید.                                                                                                                                        |
| `ENABLE_TLS_FINGERPRINT`                 | `false`       | `open-sse/executors`                         | اثرانگشت TLS را با استفاده از wreq-js جعل میکند (از Chrome 124 تقلید میکند). با مسدودسازی JA3/JA4 مقابله میکند.                                                                                                                                                                                                                                                                      |
| `TLS_FINGERPRINT_PROVIDERS`              | _(تنظیمنشده)_ | `open-sse/utils/proxyFetch.ts`               | فهرست مجاز ارائهدهندگان با جداکننده ویرگول برای مسیریابی جدید TLS مبتنی بر پراکسی (`open-sse/utils/proxyFetch.ts`). در صورت تنظیمنشدن، TLS مستقیم رفتار قدیمی خود را حفظ میکند؛ فقط این ارائهدهندگان از طریق پل اثرانگشت Chrome-124 مسیریابی میشوند.                                                                                                                                 |
| `OMNIROUTE_TURNSTILE_IGNORE_TLS_ERRORS`  | `false`       | `open-sse/services/claudeTurnstileSolver.ts` | به context مرورگر Playwright مربوط به Claude Turnstile اجازه میدهد خطاهای گواهی HTTPS را نادیده بگیرد.                                                                                                                                                                                                                                                                               |

### سناریوها

| سناریو                                            | پیکربندی                                                                                                                                                     |
| ------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| **SOCKS5 از طریق تونل SSH**                       | `ALL_PROXY=socks5://127.0.0.1:7890`، `ENABLE_SOCKS5_PROXY=true`                                                                                              |
| **پروکسی HTTP سازمانی**                           | `HTTP_PROXY=http://proxy.corp.com:3128`، `HTTPS_PROXY=http://proxy.corp.com:3128`، `NO_PROXY=localhost,internal.corp.com`                                    |
| **ضد اثر انگشت**                                  | `ENABLE_TLS_FINGERPRINT=true` — به `wreq-js` نیاز دارد (گنجانده شده است)                                                                                     |
| **خروجی کنترلشده / بدون دسترسی مستقیم**           | `PROXY_FAIL_OPEN=false` را بدون تغییر بگذارید (پیشفرض). وقتی پروکسی در دسترس نباشد، درخواستها بهطور قطعی ناموفق میشوند تا از نشت از مسیر مستقیم جلوگیری شود. |
| **قدیمی / توسعه — اجازهٔ بازگشت به اتصال مستقیم** | `PROXY_FAIL_OPEN=true`. رفتار پیش از سختسازی را بازیابی میکند: وقتی تفکیک پروکسی ناموفق باشد، از اتصال مستقیم استفاده میشود.                                 |

> **نکته (دور زدن اعتبارسنجی NVIDIA — #3226):** نقطهٔ پایانی اعتبارسنجی کلید API شرکت NVIDIA
> هنگام مسیریابی از طریق fetch سراسریِ پروکسیشده/وصلهشده برای TLS متوقف میشود (undici dispatcher → 504).
> `src/lib/providers/validation.ts::directHttpsRequest()` عمداً برای همان یک فراخوانی اعتبارسنجی، با استفاده از
> `safeOutboundFetch({ bypassProxyPatch: true })` وصلهٔ پروکسی را دور میزند.
> این یک استثنای مستند و محدود است — و بر خروجی چت/مصرف **تأثیری ندارد**.
> محدودهٔ این دور زدن توسط `tests/unit/proxy-bypass-scope-guard-3226.test.ts` تثبیت شده است.

---

## 9. یکپارچهسازی ابزار CLI

نحوهٔ شناسایی و اجرای سایدکارهای CLI (Claude Code، Codex و غیره) توسط OmniRoute را کنترل میکند.

| متغیر                             | پیشفرض                           | فایل منبع                                           | توضیحات                                                                                                                                                                                                                                                                                   |
| --------------------------------- | -------------------------------- | --------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `CLI_MODE`                        | `auto`                           | `src/shared/services/cliRuntime.ts`                 | `auto` = جستوجو در PATH سیستم؛ `manual` = فقط از مسیرهای صریح استفاده میکند.                                                                                                                                                                                                              |
| `CLI_EXTRA_PATHS`                 | _(تنظیمنشده)_                    | `src/shared/services/cliRuntime.ts`                 | ورودیهای اضافی PATH برای یافتن فایل اجرایی CLI (جداشده با دونقطه).                                                                                                                                                                                                                        |
| `CLI_CONFIG_HOME`                 | _(تنظیمنشده)_                    | `src/shared/services/cliRuntime.ts`                 | دایرکتوری خانگی را برای خواندن پیکربندیهای CLI (`~/.claude`، `~/.codex`) بازنویسی میکند. باید مطلق و داخل دایرکتوری خانگی فرایند باشد — **یا** در یک کانتینر، مسیری باشد که با bind mount متصل شده است (`/host-home` به همین شکل کار میکند). هر مقدار دیگری به دایرکتوری خانگی بازمیگردد. |
| `CLI_ALLOW_CONFIG_WRITES`         | `true`                           | `src/shared/services/cliRuntime.ts`                 | به OmniRoute اجازه میدهد فایلهای پیکربندی CLI را بنویسد (نوسازی توکن، دادههای نشست). آن را روی `false` تنظیم کنید تا هرگونه نوشتن در پیکربندی CLI با خطای صریح «نوشتن غیرفعال است» ناموفق شود.                                                                                            |
| `CLI_CLAUDE_BIN`                  | `claude`                         | `src/shared/services/cliRuntime.ts`                 | مسیر سفارشی فایل اجرایی Claude CLI.                                                                                                                                                                                                                                                       |
| `CLI_CODEX_BIN`                   | `codex`                          | `src/shared/services/cliRuntime.ts`                 | مسیر سفارشی فایل اجرایی Codex CLI.                                                                                                                                                                                                                                                        |
| `CLI_DROID_BIN`                   | `droid`                          | `src/shared/services/cliRuntime.ts`                 | مسیر سفارشی فایل اجرایی Droid CLI.                                                                                                                                                                                                                                                        |
| `CLI_OPENCLAW_BIN`                | `openclaw`                       | `src/shared/services/cliRuntime.ts`                 | مسیر سفارشی فایل اجرایی OpenClaw CLI.                                                                                                                                                                                                                                                     |
| `CLI_CURSOR_BIN`                  | `agent`، سپس `cursor`            | `src/shared/services/cliRuntime.ts`                 | مسیر سفارشی فایل اجرایی عامل Cursor. بدون آن، فرایند تشخیص ابتدا `agent` را امتحان میکند و در صورت شکست به `cursor` بازمیگردد.                                                                                                                                                            |
| `CLI_CLINE_BIN`                   | `cline`                          | `src/shared/services/cliRuntime.ts`                 | مسیر سفارشی فایل اجرایی Cline CLI.                                                                                                                                                                                                                                                        |
| `CLI_5DIVE_BIN`                   | `5dive`                          | `src/shared/services/cliRuntime.ts`                 | مسیر سفارشی فایل اجرایی 5dive CLI.                                                                                                                                                                                                                                                        |
| `CLI_5DIVE_STATE_DIR`             | `/var/lib/5dive`                 | `src/shared/services/cliRuntime.ts`                 | دایرکتوری وضعیت سیستمی 5dive (پروفایلهای احراز هویت متعلق به root)؛ مقدار پیشفرض STATE_DIR خود 5dive را بازتاب میدهد.                                                                                                                                                                     |
| `CLI_CONTINUE_BIN`                | `cn`                             | `src/shared/services/cliRuntime.ts`                 | مسیر سفارشی فایل اجرایی Continue CLI.                                                                                                                                                                                                                                                     |
| `CLI_QODER_BIN`                   | `qodercli`                       | `src/shared/services/cliRuntime.ts`                 | مسیر سفارشی فایل اجرایی Qoder CLI.                                                                                                                                                                                                                                                        |
| `CLI_QWEN_BIN`                    | `qwen`                           | `src/shared/services/cliRuntime.ts`                 | مسیر سفارشی فایل اجرایی Qwen Code CLI.                                                                                                                                                                                                                                                    |
| `CLI_AIDER_BIN`                   | `aider`                          | `src/shared/services/cliRuntime.ts`                 | مسیر سفارشی فایل اجرایی Aider CLI.                                                                                                                                                                                                                                                        |
| `CLI_GOOSE_BIN`                   | `goose`                          | `src/shared/services/cliRuntime.ts`                 | مسیر سفارشی فایل اجرایی Goose CLI.                                                                                                                                                                                                                                                        |
| `CLI_GEMINI_BIN`                  | `gemini`                         | `src/shared/services/cliRuntime.ts`                 | مسیر سفارشی فایل اجرایی Google Gemini CLI — فقط برای تشخیص و بررسیهای سلامت سمت سرور؛ `omniroute run gemini` فایل اجرایی `gemini` را از PATH سیستم پیدا میکند.                                                                                                                            |
| `CLI_KILO_BIN`                    | `kilocode`                       | `src/shared/services/cliRuntime.ts`                 | مسیر سفارشی فایل اجرایی Kilo Code CLI.                                                                                                                                                                                                                                                    |
| `CLI_OPENCODE_BIN`                | `opencode`                       | `src/shared/services/cliRuntime.ts`                 | مسیر سفارشی فایل اجرایی OpenCode CLI.                                                                                                                                                                                                                                                     |
| `CLI_HERMES_BIN`                  | `hermes`                         | `src/shared/services/cliRuntime.ts`                 | مسیر سفارشی فایل اجرایی Hermes. بین هر دو ورودی کاتالوگ (`hermes` و `hermes-agent`) مشترک است.                                                                                                                                                                                            |
| `CLI_FORGE_BIN`                   | `forge`                          | `src/shared/services/cliRuntime.ts`                 | مسیر سفارشی فایل اجرایی ForgeCode CLI.                                                                                                                                                                                                                                                    |
| `CLI_JCODE_BIN`                   | `jcode`                          | `src/shared/services/cliRuntime.ts`                 | مسیر سفارشی فایل اجرایی jcode CLI.                                                                                                                                                                                                                                                        |
| `CLI_DEEPSEEK_TUI_BIN`            | `deepseek-tui`                   | `src/shared/services/cliRuntime.ts`                 | مسیر سفارشی فایل اجرایی DeepSeek TUI.                                                                                                                                                                                                                                                     |
| `CLI_CODEWHALE_BIN`               | `codewhale`                      | `src/shared/services/cliRuntime.ts`                 | مسیر سفارشی فایل اجرایی CodeWhale CLI.                                                                                                                                                                                                                                                    |
| `CLI_SMELT_BIN`                   | `smelt`                          | `src/shared/services/cliRuntime.ts`                 | مسیر سفارشی فایل اجرایی Smelt CLI.                                                                                                                                                                                                                                                        |
| `CLI_PI_BIN`                      | `pi`                             | `src/shared/services/cliRuntime.ts`                 | مسیر سفارشی فایل اجرایی Pi (pi-coding-agent).                                                                                                                                                                                                                                             |
| `CLI_CRUSH_BIN`                   | `crush`                          | `src/shared/services/cliRuntime.ts`                 | مسیر سفارشی فایل اجرایی Crush CLI.                                                                                                                                                                                                                                                        |
| `CLI_OMP_BIN`                     | `omp`                            | `src/shared/services/cliRuntime.ts`                 | مسیر سفارشی فایل اجرایی عامل Oh My Pi (`omp`).                                                                                                                                                                                                                                            |
| `CLI_LETTA_BIN`                   | `letta`                          | `src/shared/services/cliRuntime.ts`                 | مسیر سفارشی فایل اجرایی Letta CLI.                                                                                                                                                                                                                                                        |
| `CLI_PRIME_AGENT_BIN`             | `prime-agent`                    | `src/shared/services/cliRuntime.ts`                 | مسیر سفارشی فایل اجرایی Prime Agent (Prime Intellect).                                                                                                                                                                                                                                    |
| `CLI_WINDSURF_BIN`                | _(هیچکدام)_                      | `src/shared/services/cliRuntime.ts`                 | مسیر سفارشی فایل اجرایی Windsurf. Windsurf **هیچ فرمان پیشفرضی ندارد** — تشخیص فایل اجرایی تا زمان تنظیم این متغیر غیرفعال باقی میماند.                                                                                                                                                   |
| `CLI_DEVIN_BIN`                   | `devin`                          | `open-sse/executors/devin-cli.ts`                   | مسیر سفارشی فایل اجرایی Devin CLI (v3.8.0). توسط اجراکننده Windsurf/Devin استفاده میشود.                                                                                                                                                                                                  |
| `DEVIN_DESKTOP_VERSION`           | `3.6.27`                         | `open-sse/executors/devin-desktop.ts`               | مقدار `ide_version` مربوط به Devin Desktop. مقادیر بازنویسی باید از قالب `x.y.z` استفاده کنند؛ مقادیر نامعتبر به پیشفرض تأییدشده بازمیگردند.                                                                                                                                              |
| `DEVIN_DESKTOP_EXTENSION_VERSION` | `1.48.2`                         | `open-sse/executors/devin-desktop.ts`               | مقدار `extension_version` مربوط به Codeium/language-server همراه برنامه که با `ide_version` دسکتاپ متفاوت است. مقادیر بازنویسی باید از `x.y.z` استفاده کنند؛ برای مقادیر نامعتبر، پیشفرض همراه برنامه استفاده میشود.                                                                      |
| `CLI_DEVIN_AGENTIC_BIN`           | `devin`                          | `open-sse/executors/devin-cli-agentic.ts`           | بازنویسی Devin CLI فقط برای پل عاملی. اجراکننده فقط upstream محلی ACP stdio را میپذیرد.                                                                                                                                                                                                   |
| `DEVIN_AGENTIC_HOME`              | _(الزامی)_                       | `open-sse/executors/devin-cli-agentic.ts`           | دایرکتوری خانگی مطلق و ایزوله برای زیرفرایند عاملی Devin؛ مسیرهای پذیرفتهشده پل عبارتاند از `/home/bridge` و مسیرهای `.sandbox` محلیِ وظیفه (در Windows، مسیرهای `C:\...\.sandbox\...`).                                                                                                  |
| `DEVIN_AGENTIC_ACP_TIMEOUT_MS`    | `120000`                         | `open-sse/executors/devin-cli-agentic.ts`           | حداکثر مدت یک نوبت Devin ACP پیش از آنکه پل فرایند فرزند را خاتمه دهد و یک مهلتپایان صریح برگرداند.                                                                                                                                                                                       |
| `DEVIN_BRIDGE_MODEL`              | `devin-cli-agentic/swe-1-7`      | `docker/devin-bridge/compose.yml`                   | نام مستعار مدل اصلی Claude Code برای پل ایزوله. هارنس زنده، مثال را با مدلی که حساب فعلی Devin برمیگرداند جایگزین میکند.                                                                                                                                                                  |
| `DEVIN_BRIDGE_SONNET_MODEL`       | `DEVIN_BRIDGE_MODEL`             | `docker/devin-bridge/compose.yml`                   | نام مستعار پل ایزوله که هنگام درخواست پیشفرض Sonnet توسط Claude Code استفاده میشود.                                                                                                                                                                                                       |
| `DEVIN_BRIDGE_OPUS_MODEL`         | `DEVIN_BRIDGE_MODEL`             | `docker/devin-bridge/compose.yml`                   | نام مستعار پل ایزوله که هنگام درخواست پیشفرض Opus توسط Claude Code استفاده میشود.                                                                                                                                                                                                         |
| `DEVIN_BRIDGE_HAIKU_MODEL`        | `DEVIN_BRIDGE_MODEL`             | `docker/devin-bridge/compose.yml`                   | نام مستعار پل ایزوله که هنگام درخواست پیشفرض Haiku توسط Claude Code استفاده میشود.                                                                                                                                                                                                        |
| `DEVIN_BRIDGE_SUBAGENT_MODEL`     | `DEVIN_BRIDGE_MODEL`             | `docker/devin-bridge/compose.yml`                   | نام مستعار پل ایزوله که برای زیرعاملهای Claude Code استفاده میشود.                                                                                                                                                                                                                        |
| `DEVIN_SEAT_API_URL`              | `https://server.codeium.com`     | `open-sse/services/usage/devinCli.ts`               | بازنویسی اختیاری API مدیریت مجوز Codeium که سهمیه Devin CLI (`GetUserStatus`) از آن استفاده میکند.                                                                                                                                                                                        |
| `AUGGIE_BIN`                      | `auggie`                         | `open-sse/executors/auggie.ts`                      | بازنویسی با مسیر مطلق برای فایل اجرایی Augment (Auggie) CLI که ارائهدهنده محلی `auggie` از آن استفاده میکند. در صورت نبود، ابتدا به `CLI_AUGGIE_BIN` و سپس به جستوجو در PATH بازمیگردد.                                                                                                   |
| `CLI_AUGGIE_BIN`                  | `auggie`                         | `open-sse/executors/auggie.ts`                      | بازنویسی نام مستعار برای مسیر فایل اجرایی Augment (Auggie) CLI (پس از `AUGGIE_BIN` بررسی میشود).                                                                                                                                                                                          |
| `ZCODE_BIN`                       | `zcode`                          | `open-sse/executors/zcode.ts`                       | فایل اجرایی مورد استفاده کلاینت stdio ارائهدهنده محلی `zcode`. در صورت نبود، به `zcode` در PATH بازمیگردد.                                                                                                                                                                                |
| `ZCODE_ARGS`                      | —                                | `open-sse/executors/zcode.ts`                       | آرایه JSON (حداکثر ۱۶ رشته) از آرگومانهای اضافی که هنگام اجرا از طریق `cliTools` به فایل اجرایی `zcode` ارسال میشوند.                                                                                                                                                                     |
| `ZCODE_CWD`                       | `process.cwd()`                  | `open-sse/executors/zcode.ts`                       | دایرکتوری کاری زیرفرایند app-server مربوط به ZCode.                                                                                                                                                                                                                                       |
| `ZCODE_PROVIDER_ID`               | `builtin:zai-coding-plan`        | `open-sse/executors/zcode.ts`                       | بازنویسی شناسه ارائهدهندهای که به app-server ارسال میشود.                                                                                                                                                                                                                                 |
| `ZCODE_SERVER_RUNTIME_ROOT`       | `~/.zcode/server`                | `open-sse/executors/zcode.ts`                       | ریشه محیط اجرای app-server مربوط به ZCode (محل قرارگیری `node` و `zcode-server.cjs` همراه برنامه).                                                                                                                                                                                        |
| `ZCODE_SERVER_NODE`               | `<runtimeRoot>/node`             | `open-sse/executors/zcode.ts`                       | فایل اجرایی Node که برای میزبانی app-server مربوط به ZCode استفاده میشود.                                                                                                                                                                                                                 |
| `ZCODE_SERVER_ENTRY`              | `<runtimeRoot>/zcode-server.cjs` | `open-sse/executors/zcode.ts`                       | اسکریپت ورودی app-server که برای میزبانی سرور ZCode استفاده میشود.                                                                                                                                                                                                                        |
| `ZCODE_STARTUP_TIMEOUT_MS`        | `10000`                          | `open-sse/executors/zcode.ts`                       | مهلتپایان راهاندازی (میلیثانیه) که پس از آن اجرای app-server مربوط به ZCode ناموفق در نظر گرفته میشود.                                                                                                                                                                                    |
| `ZCODE_RPC_TIMEOUT_MS`            | `30000`                          | `open-sse/executors/zcode.ts`                       | مهلتپایان RPC بهازای هر درخواست (میلیثانیه) برای فراخوانی app-server مربوط به ZCode.                                                                                                                                                                                                      |
| `ZCODE_TURN_TIMEOUT_MS`           | `120000`                         | `open-sse/executors/zcode.ts`                       | حداکثر مدت (میلیثانیه) یک نوبت ZCode پیش از آنکه ناظر آن را بهدلیل پایان مهلت متوقف کند.                                                                                                                                                                                                  |
| `ZCODE_POLL_INTERVAL_MS`          | `250`                            | `open-sse/executors/zcode.ts`                       | فاصله نمونهبرداری (میلیثانیه) برای تکمیل نوبت ZCode.                                                                                                                                                                                                                                      |
| `HERMES_HOME`                     | `~/.hermes`                      | `src/lib/cli-helper/config-generator/hermesHome.ts` | دایرکتوری خانگی Hermes Agent که OmniRoute پیکربندی Hermes CLI را در آن میخواند/مینویسد. با متغیر محیطی تنظیمشده توسط نصبکننده PowerShell مربوط به Hermes در Windows (`%LOCALAPPDATA%\hermes`) مطابقت دارد.                                                                                |

### همگامسازی خودکار پروفایلهای CLI

این پرچمهای قابلیت بهصورت انتخابی فعال میشوند و بهطور پیشفرض غیرفعالاند. همچنین میتوان آنها را از
داشبورد CLI Code فعال یا غیرفعال کرد.

| متغیر                                 | پیشفرض  | فایل منبع                                        | توضیحات                                                                                                                                                                                                                                                                                     |
| ------------------------------------- | ------- | ------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `OMNIROUTE_AUTO_SYNC_CODEX_PROFILES`  | `false` | `src/shared/constants/featureFlagDefinitions.ts` | پس از همگامسازی مدلهای ارائهدهنده، فایلهای پروفایل `~/.codex/*.config.toml` را بهطور خودکار بر اساس کاتالوگ زنده بازنویسی میکند. به `CLI_ALLOW_CONFIG_WRITES` نیاز دارد؛ هرگز پیکربندی فعال/پیشفرض Codex، احراز هویت، تنظیمات Codex-lb یا انتخاب ارائهدهنده را تغییر نمیدهد.                |
| `OMNIROUTE_AUTO_SYNC_CLAUDE_PROFILES` | `false` | `src/shared/constants/featureFlagDefinitions.ts` | پس از همگامسازی مدلهای ارائهدهنده، فایلهای پروفایل Claude Code در `~/.claude/profiles/<name>/settings.json` را بهطور خودکار بر اساس کاتالوگ زنده بازنویسی میکند. به `CLI_ALLOW_CONFIG_WRITES` نیاز دارد؛ هرگز پیکربندی فعال/پیشفرض Claude، احراز هویت یا انتخاب ارائهدهنده را تغییر نمیدهد. |

### نمونه Docker

```bash
# فایلهای اجرایی میزبان را در کانتینر مانت کنید و مسیر آنها را به OmniRoute اعلام کنید:
CLI_EXTRA_PATHS=/host-cli/bin
CLI_CONFIG_HOME=/host-home
CLI_ALLOW_CONFIG_WRITES=true
CLI_CLAUDE_BIN=/host-cli/bin/claude
```

`CLI_CONFIG_HOME` تنها زمانی اثر میگذارد که مسیر واقعاً از میزبان بهصورت bind-mounted
متصل شده باشد — آن را با مانتهایی مانند `~/.codex:/host-home/.codex:rw` همراه کنید (به پروفایل
`host` در `docker-compose.yml` مراجعه کنید). مسیری که نه داخل پوشه home کاربر
کانتینر باشد و نه یک bind mount، نادیده گرفته میشود؛ زیرا هنگام ایجاد مجدد کانتینر،
دادههای نوشتهشده در آن از بین خواهند رفت.

ایمیج با `USER node` اجرا میشود، بنابراین `/root` مانتنشده یک override
معتبر **نیست**.

| متغیر                                    | پیشفرض     | فایل منبع                           | توضیحات                                                                                                                                           |
| ---------------------------------------- | ---------- | ----------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------- |
| `OMNIROUTE_CONTAINER`                    | _(خودکار)_ | `src/shared/utils/containerEnv.ts`  | تشخیص کانتینر را بهاجبار فعال (`1`/`true`) یا غیرفعال (`0`/`false`) میکند. فقط در runtimeهایی لازم است که تشخیص خودکار قادر به شناسایی آنها نیست. |
| `OMNIROUTE_ALLOW_CONTAINER_CONFIG_WRITE` | `false`    | `src/shared/services/cliRuntime.ts` | در هر صورت اجازه میدهد پیکربندی ابزار CLI در یک مسیر مانتنشده کانتینر نوشته شود. معادل آن در CLI، گزینه `--allow-container-write` است.            |

### ابزارهای کمکی فایل اجرایی CLI (`omniroute`)

این متغیرها رفتار خود فایل اجرایی CLI مربوط به `omniroute` را تنظیم میکنند (نه تشخیص sidecar
در بالا).

| متغیر                        | پیشفرض        | فایل منبع                               | توضیحات                                                                                                                                                                                                                                       |
| ---------------------------- | ------------- | --------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `OMNIROUTE_LANG`             | _(سیستم)_     | `bin/cli/i18n.mjs`                      | زبان خروجی CLI را بهاجبار تعیین میکند. locale مطابق BCP-47 (برای مثال `en` یا `pt-BR`). متغیرهای محیطی locale سیستم (LC_ALL، LC_MESSAGES) را بازنویسی میکند.                                                                                  |
| `OMNIROUTE_SHOW_LOG`         | _(تنظیمنشده)_ | `bin/cli/runtime/processSupervisor.mjs` | برای ارسال stdout/stderr سرور به ترمینال در حالت تحت نظارت، آن را روی `1` تنظیم کنید. معادل پرچم `--log` در `omniroute serve` است.                                                                                                            |
| `OMNIROUTE_CLI_TOKEN`        | _(تنظیمنشده)_ | `bin/cli/api.mjs`                       | توکن احراز هویت ماشین که بهعنوان هدر `x-omniroute-cli-token` تزریق میشود. در وظیفه 8.12 بهطور خودکار تولید میشود.                                                                                                                             |
| `OMNIROUTE_HTTP_TIMEOUT_MS`  | `30000`       | `bin/cli/api.mjs`                       | مهلت زمانی HTTP برای هر تلاش (برحسب ms) در درخواستهای CLI → سرور.                                                                                                                                                                             |
| `OMNIROUTE_READY_TIMEOUT_MS` | `60000`       | `bin/cli/utils/pid.mjs`                 | حداکثر زمانی (برحسب ms) که CLI پیش از نمایش هشدار پایان مهلت، برای endpoint سلامت سرور منتظر میماند. برای شروعهای سرد و کند (برای مثال Windows) مفید است. همچنین از طریق `--ready-timeout` قابل تنظیم است.                                    |
| `OMNIROUTE_VERBOSE`          | `0`           | `bin/cli/api.mjs`                       | برای نمایش اطلاعات تشخیصی تلاش مجدد/backoff در stderr هنگام اجرای فرمانهای CLI، آن را روی `1` تنظیم کنید.                                                                                                                                     |
| `OMNIROUTE_PLUGIN_PATH`      | _(تنظیمنشده)_ | `bin/cli/plugins.mjs`                   | پوشه سفارشی برای کشف افزونههای CLI (بستههای `omniroute-cmd-*`). در صورت تنظیمنشدن، مقدار پیشفرض `~/.omniroute/plugins/` است. فقط مختص CLI است — هرگز به اسکنر افزونه سمت سرور نمیرسد؛ مسیر آن با `OMNIROUTE_PLUGINS_DIR` (بخش 2) تعیین میشود. |

---

## 10. یکپارچهسازیهای عامل داخلی و MCP

| متغیر                                           | مقدار پیشفرض                                        | فایل منبع                                                   | توضیحات                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| ----------------------------------------------- | --------------------------------------------------- | ----------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `OMNIROUTE_BASE_URL`                            | تشخیص خودکار                                        | `open-sse/mcp-server/server.ts`                             | نشانی URL صریح برای دسترسی ابزارهای MCP/A2A به OmniRoute. تشخیص خودکار localhost را نادیده میگیرد.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `OMNIROUTE_API_KEY`                             | _(تنظیمنشده)_                                       | ماژولهای MCP/A2A                                            | کلید API برای فراخوانیهای ابزار داخلی MCP و مهارت A2A.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `OMNIROUTE_API_KEY_ID`                          | _(تنظیمنشده)_                                       | `open-sse/mcp-server/audit.ts`                              | شناسه کلید برای انتساب گزارش ممیزی MCP.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `ROUTER_API_KEY`                                | _(تنظیمنشده)_                                       | قدیمی                                                       | نام مستعار قدیمی برای `OMNIROUTE_API_KEY`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `OMNIROUTE_A2A_HISTORY_RETENTION_DAYS`          | `30`                                                | `src/lib/a2a/taskManager.ts`                                | تعداد روزهایی که تاریخچه وظایف A2A پیش از حذف یک ردیف در پاکسازی روزانه، در پایگاه داده محلی نگه داشته میشود. اگر تنظیمنشده، غیرعددی یا `<= 0` باشد، مقدار `30` استفاده میشود.                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `OMNIROUTE_A2A_MEMORY_HITS`                     | `1`                                                 | `src/lib/a2a/taskExecution.ts`                              | کلید توقف اضطراری برای قابلیت مشاهدهپذیری اصابتهای حافظه A2A. برای صرفنظر کامل از جستوجوی بازیابی حافظه برای یک وظیفه، آن را روی `0` تنظیم کنید؛ هر مقدار دیگری (از جمله تنظیمنشده) آن را فعال نگه میدارد.                                                                                                                                                                                                                                                                                                                                                                                                               |
| `OMNIROUTE_ISSUE_AGENT_ENABLED`                 | `false`                                             | `src/app/api/issue-agent/runs/route.ts`                     | نقطه پایانی تریاژ ثبتشده آفلاین/محلی Issue Agent را فعال میکند. مگر هنگام اجرای صریح گردشکارهای تریاژ ثبتشده محلی، آن را غیرفعال نگه دارید.                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `OMNIROUTE_ISSUE_AGENT_TIMEOUT_MS`              | _(تنظیمنشده)_                                       | `src/lib/issueAgent/execution.ts`                           | مهلت زمانی (ms) برای یک اجرای تریاژ ثبتشده Issue Agent. این مقدار به حداکثر داخلی محدود میشود؛ اگر تنظیمنشده یا نامعتبر باشد، مقدار پیشفرض داخلی استفاده میشود.                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `OMNIROUTE_CONTEXT`                             | _(زمینه فعال)_                                      | `bin/cli/program.mjs`, `bin/cli/api.mjs`                    | زمینه/پروفایل حالت راهدور CLI برای فرمانهای `omniroute`؛ زمینه فعال در مخزن محلی زمینهها را نادیده میگیرد. معادل `--context <name>` است.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `OMNIROUTE_CONTEXT_KEYCHAIN_DISABLED`           | `0`                                                 | `bin/cli/contexts.mjs`                                      | پشتیبان اختیاری زنجیرهکلید سیستمعامل `keytar` را برای اعتبارنامههای زمینه CLI غیرفعال میکند. هنگام فعالبودن، اعتبارنامهها با حالت `0600` در `config.json` باقی میمانند و CLI یک هشدار یکباره درباره استفاده از حالت جایگزین صادر میکند؛ برای عملیات آگاهانه در محیطهای بدون رابط یا کانتینری در نظر گرفته شده است.                                                                                                                                                                                                                                                                                                       |
| `OMNIROUTE_MCP_ENFORCE_SCOPES`                  | `true`                                              | `open-sse/mcp-server/server.ts`                             | کنترل دسترسی مبتنی بر دامنه را برای فراخوانیهای ابزار MCP اعمال میکند.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `OMNIROUTE_MCP_SCOPES`                          | _(همه)_                                             | `open-sse/mcp-server/server.ts`                             | دامنههای جداشده با ویرگول: `admin`، `combos`، `health`، `models`، `routing`، `budget`، `metrics`، `pricing`، `memory`، `skills`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `OMNIROUTE_MCP_COMPRESS_DESCRIPTIONS`           | `false`                                             | `open-sse/mcp-server/descriptionCompressor.ts`              | توضیحات ابزار MCP را پیش از سریالسازی مانیفست فشرده میکند. مقادیر فعالسازی: `1`، `true`، `on`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `OMNIROUTE_MCP_DESCRIPTION_COMPRESSION`         | `rtk`                                               | `open-sse/mcp-server/descriptionCompressor.ts`              | الگوریتم/پروفایل فشردهسازی. مقادیر غیرفعالسازی: `0`، `false`، `off`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `OMNIROUTE_MCP_FETCH_TIMEOUT_MS`                | `10000`                                             | `open-sse/mcp-server/fetchTimeout.ts`                       | مهلت لغو (ms) برای خواندنهای مدیریتی داخلی سرور MCP (سلامت، تابآوری، ترکیبها، سهمیه، مصرف).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `OMNIROUTE_MCP_UPSTREAM_TIMEOUT_MS`             | `60000`                                             | `open-sse/mcp-server/fetchTimeout.ts`                       | مهلت لغو (میلیثانیه) برای گامهای MCP که منتظر یک ارائهدهنده میمانند (`route_request`، `web_search`، `web_fetch`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `OMNIROUTE_CORPUS_CACHE_SIZE`                   | `5`                                                 | `src/lib/localCorpus/configured.ts`                         | حداکثر تعداد نمونههای ایندکس پیکرهٔ محلی که در حافظه نهان ذخیره میشوند (LRU، یک نمونه برای هر پوشهٔ ریشهٔ ایندکسشده). حداقل مقدار آن `1` است.                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `MODEL_SYNC_INTERVAL_HOURS`                     | `24`                                                | `src/shared/services/modelSyncScheduler.ts`                 | فاصلهٔ همگامسازی کاتالوگ مدل بر حسب ساعت.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `PROVIDER_LIMITS_SYNC_INTERVAL_MINUTES`         | `70`                                                | `src/lib/usage/providerLimits.ts`                           | فاصلهٔ زمانی نظرسنجی محدودیت نرخ و سهمیهٔ ارائهدهنده.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `PROVIDER_LIMITS_SYNC_SPACING_MS`               | `1500`                                              | `src/lib/usage/providerLimits.ts`                           | فاصله (میلیثانیه) میان دریافتهای متوالی سهمیهٔ OAuth در یک همگامسازی انبوه؛ اتصالهای OAuth یکییکی دریافت میشوند تا از ارسال ناگهانی درخواستها به سرویس بالادستی جلوگیری شود. مقدار `0` این رفتار را غیرفعال میکند (همزمان).                                                                                                                                                                                                                                                                                                                                                                                              |
| `OMNIROUTE_QUOTA_FETCH_MIN_INTERVAL_MS`         | `250`                                               | `open-sse/services/quotaFetchThrottle.ts`                   | حداقل فاصله (میلیثانیه) میان دریافتهای متوالی سهمیه از سرویس بالادستی در مسیر پیشبررسی/پایش هر درخواست؛ فراخوانیهای همزمان شبکه را با فاصله انجام میدهد تا تعداد زیادی حساب روی یک IP باعث هجوم درخواستها به سرویس بالادستی نشوند. به دریافتکنندههای سهمیهٔ Codex (`/wham/usage`)، DeepSeek، Bailian (هر دو محل دریافت)، OpenCode و Crof متصل شده است (#6009، #6911). مسیر عمومی توزیع `usage.ts::getUsageForProvider` (github/glm/minimax/nanogpt/xai/etc.) هنوز پوشش داده نشده است — بهصورت جداگانه پیگیری میشود. اصابتهای حافظه نهان تحت تأثیر نیستند. مقدار `0` آن را غیرفعال میکند؛ به بازهٔ `0..5000` محدود میشود. |
| `PROVIDER_LIMITS_POST_USAGE_REFRESH_DELAY_MS`   | `5000`                                              | `src/lib/usage/providerLimits.ts`                           | تأخیر (میلیثانیه) پیش از تازهسازی محدودیتهای ارائهدهنده پس از یک رویداد مصرف واقعی، تا API سهمیهٔ بالادستی فرصت ثبت مصرف را داشته باشد.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `OMNIROUTE_LOGIN_BROWSER_PATH`                  | تشخیص خودکار                                        | `open-sse/services/adobeFireflyBrowserLogin.ts`             | مسیر مطلق فایل اجرایی Chrome یا Edge سیستم که برای ورود تعاملی به Adobe Firefly و تمدید خارج از صفحه استفاده میشود.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `ADOBE_FIREFLY_BROWSER_REFRESH`                 | فعال                                                | `open-sse/services/adobeFireflySession.ts`                  | وضعیت IMS و ریسک مرورگر را با نشستهای Chrome CDP مختص هر حساب تازه نگه میدارد. برای غیرفعالکردن تمدید مرورگر، آن را روی `0` تنظیم کنید.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `ADOBE_FIREFLY_SESSION_DISK`                    | فعال                                                | `open-sse/services/adobeFireflySession.ts`                  | نشستهای ترمیمشدهٔ Adobe را در `DATA_DIR` نگه میدارد تا پس از راهاندازی مجدد فرایند نیز باقی بمانند. برای نگهداری نشستها فقط در حافظه، آن را روی `0` تنظیم کنید.                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `ADOBE_FIREFLY_MIN_SUBMIT_GAP_MS`               | `12000`                                             | `open-sse/services/adobeFireflySession.ts`                  | حداقل فاصله بر حسب میلیثانیه میان ارسالهای تولید Adobe Firefly؛ مقدار `0` فاصلهگذاری را غیرفعال میکند.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `ADOBE_FIREFLY_BATCH_EXTRA_GAP_MS`              | `15000`                                             | `open-sse/services/adobeFireflySession.ts`                  | دورهٔ سکون اضافی بر حسب میلیثانیه پس از هر سومین ارسال موفق Adobe.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `ADOBE_FIREFLY_SUBMIT_BASE_DELAY_MS`            | `8000`                                              | `open-sse/services/adobeFireflyClient.ts`                   | تأخیر پایهٔ عقبنشینی بر حسب میلیثانیه پس از پاسخهای موقت 408 از Adobe؛ همراه با فاصلهگذاری ارسالها در حداکثر پنج تلاش اعمال میشود.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `OMNIROUTE_DISABLE_BACKGROUND_SERVICES`         | `false`                                             | `src/instrumentation-node.ts`                               | همهٔ سرویسهای پسزمینه (همگامسازی، قیمتگذاری، تازهسازی مدل) را غیرفعال میکند. برای CI/آزمون مفید است.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `OMNIROUTE_ENABLE_RUNTIME_BACKGROUND_TASKS`     | _(تنظیمنشده)_                                       | `src/lib/config/runtimeSettings.ts`                         | وظایف پسزمینه را هنگام تشخیص آزمون خودکار بهاجبار فعال میکند. برای نادیدهگرفتن روش اکتشافی آزمون، مقدار `1` را تنظیم کنید.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `OMNIROUTE_BUDGET_RESET_JOB_INTERVAL_MS`        | `600000`                                            | `src/lib/jobs/budgetResetJob.ts`                            | تناوب بررسی بازنشانی بودجه (میلیثانیه). حداقل `10000`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `OMNIROUTE_CONNECTION_RECOVERY_INTERVAL_MS`     | `60000`                                             | `src/lib/quota/connectionRecovery.ts`                       | تناوب بازیابی پیشدستانه پس از دورهٔ انتظار اتصال (میلیثانیه): اتصالهایی را که مقدار گذرای `rate_limited_until` آنها منقضی شده است، خارج از مسیر داغ درخواست مجدداً اعتبارسنجی میکند. حداقل `5000`.                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `OMNIROUTE_DISABLE_CONNECTION_RECOVERY`         | `false`                                             | `src/lib/quota/connectionRecovery.ts`                       | زمانبند بازیابی پیشدستانه پس از دورهٔ انتظار اتصال را غیرفعال میکند (بازیابی تنبل در `getProviderCredentials` همچنان اعمال میشود).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `OMNIROUTE_REASONING_CACHE_CLEANUP_INTERVAL_MS` | `1800000`                                           | `src/lib/jobs/reasoningCacheCleanupJob.ts`                  | تناوب پاکسازی کش استدلال (میلیثانیه). حداقل `60000`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `OMNIROUTE_REASONING_MIN_BUDGET`                | _تنظیمنشده_ (غیرفعال)                               | `open-sse/services/reasoningTokenBuffer.ts`                 | حداقل اختیاری برای بودجهٔ خروجی مدلهای متفکر: مقدار `max_tokens` فراخوان در بازهٔ `[256, floor)` تا حداقل تعیینشده افزایش مییابد (با محدودیت سقف خروجی مدل). تنظیمنشده = بودجههای کلاینت هرگز افزایش نمییابند (#9507).                                                                                                                                                                                                                                                                                                                                                                                                   |
| `OMNIROUTE_LOG_EXPORT_CRON`                     | `0 * * * *`                                         | `src/lib/jobs/logExportJob.ts`                              | عبارت Cron ‏(UTC) برای کارِ صادر کردن گزارش فراخوانی که همهٔ مقصدهای فعالِ صدور گزارش را تخلیه میکند.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `OMNIROUTE_CONFIG_HOT_RELOAD_MS`                | `5000`                                              | `src/lib/config/hotReload.ts`                               | فاصلهٔ زمانی نظرسنجی (میلیثانیه) برای بارگذاری مجدد داغ پیکربندی. مقادیر کمتر از `1000` رد میشوند.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `OMNIROUTE_DISABLE_REDIS_AUTH_CACHE`            | _(فعال)_                                            | `src/lib/db/apiKeys.ts`                                     | برای دور زدن کش احراز هویت کلید API مبتنی بر Redis (و اجبار خواندن از DB)، روی `1` تنظیم کنید.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `OMNIROUTE_RTK_TRUST_PROJECT_FILTERS`           | `0`                                                 | `open-sse/services/compression/engines/rtk/filterLoader.ts` | به قوانین فیلتر پروژهٔ RTK که توسط کاربر مدیریت میشوند، بدون بررسی سختگیرانهٔ امضا اعتماد میکند.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `OMNIROUTE_LITE_MAX_TOOL_LENGTH`                | `2000`                                              | `open-sse/services/compression/lite.ts`                     | سقف نویسه برای کوتاهسازی پیشدستانهٔ نتیجهٔ ابزار در Lite، زمانی که `lite.maxToolLength` تنظیم نشده است. بازهٔ 256–1000000. تنظیم داشبورد بر این متغیر محیطی اولویت دارد.                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `OMNI_COMPRESSION_WORKERS`                      | `2`                                                 | `open-sse/services/compression/compressionWorkerPool.ts`    | حداکثر تعداد workerهای همزمان و همگام RTK/Caveman؛ کارهای مازاد بهترتیب FIFO منتظر میمانند.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `OMNI_COMPRESSION_WORKER_TIMEOUT_MS`            | `120000`                                            | `open-sse/services/compression/compressionWorkerPool.ts`    | مهلت زمانی هر کار بر حسب میلیثانیه. workerهای منقضیشده خاتمه مییابند و درخواست بهصورت fail-open و بدون تغییر ادامه مییابد.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `OMNI_COMPRESSION_WORKER_IDLE_MS`               | `60000`                                             | `open-sse/services/compression/compressionWorkerPool.ts`    | طول عمر بیکاری بر حسب میلیثانیه، پیش از آنکه یک worker فشردهسازیِ استفادهنشده خاتمه یابد.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `COMPRESSION_PIPELINE_BREAKER_ENABLED`          | `false`                                             | `open-sse/services/compression/pipelineEngineBreaker.ts`    | کلید اصلی قطعکنندهٔ مدارِ هر موتور برای خط لولهٔ پشتهای T02. **اختیاری (پیشفرض خاموش)** — در صورت روشن بودن، موتوری که در درخواستهای مختلف مکرراً خطا ایجاد کند، طی یک دورهٔ انتظار نادیده گرفته میشود (fail-open)؛ خاموش = رفتار قدیمیِ بایتبهبایت یکسان.                                                                                                                                                                                                                                                                                                                                                               |
| `COMPRESSION_PIPELINE_BREAKER_THRESHOLD`        | `3`                                                 | `open-sse/services/compression/pipelineEngineBreaker.ts`    | تعداد خطاهای متوالی در درخواستهای مختلف، پیش از باز شدن قطعکنندهٔ یک موتور.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `COMPRESSION_PIPELINE_BREAKER_COOLDOWN_MS`      | `30000`                                             | `open-sse/services/compression/pipelineEngineBreaker.ts`    | مدتزمانی بر حسب میلیثانیه که یک موتورِ بازشده، پیش از کاوش نیمهباز، نادیده گرفته میشود.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `COMPRESSION_CCR_RETRIEVAL_RAMP_FACTOR`         | `2`                                                 | `open-sse/services/compression/engines/ccr/index.ts`        | شیب بازخورد بازیابی T08/H8 CCR: هر بازیابی قبلیِ یک بلوک ذخیرهشده، مقدار مؤثر `minChars` آن را بهصورت خطی افزایش میدهد (محتوایی که مکرراً بازیابی میشود کمتر فشرده میشود؛ `>=3` بازیابی = هرگز فشرده نمیشود). مقدار `1` شیب را غیرفعال میکند (فقط رد دودویی در آستانه).                                                                                                                                                                                                                                                                                                                                                  |
| `COMPRESSION_CCR_DURABLE_STORE`                 | `true`                                              | `open-sse/services/compression/engines/ccr/index.ts`        | ذخیرهساز پایدار بلوک CCR (#9061). با پشتیبانی از ذخیرهساز درونحافظهای توسط SQLite، تضمین میکند که بلوک پس از حذف LRU، انقضای TTL، راهاندازی مجدد یا رسیدن درخواست بازیابی به نمونهای دیگر باقی بماند. برای نگهداری بلوکها فقط در حافظه، آن را روی `false` تنظیم کنید. بلوکهای بزرگتر از 512KB و محیطهای اجرای ابری، صرفنظر از این تنظیم، فقط در حافظه باقی میمانند.                                                                                                                                                                                                                                                      |
| `COMPRESSION_PREFIX_FREEZE_ENABLED`             | `false`                                             | `open-sse/services/compression/prefixFreeze.ts`             | کلید اصلی انجماد پیشوند T08/H5 مبتنی بر کاربرد مشاهدهشده. **اختیاری (بهطور پیشفرض غیرفعال)** — در صورت فعال بودن، یک پرامپت سیستمی که به تعداد `>=` آستانه مشاهده شده باشد، بهعنوان پیشوندی پایدار و قابل کش در نظر گرفته میشود و حتی برای ارائهدهندگانی که روش ابتکاری کش ایستا آنها را تشخیص نمیدهد، در برابر فشردهسازی حفظ میشود (انجماد فقط آن را _حفظ میکند_ و هرگز تغییر نمیدهد).                                                                                                                                                                                                                                  |
| `COMPRESSION_PREFIX_FREEZE_THRESHOLD`           | `3`                                                 | `open-sse/services/compression/prefixFreeze.ts`             | تعداد دفعات مشاهده یک پرامپت سیستمی پیش از آنکه بهعنوان پیشوندی پایدار و منجمد در نظر گرفته شود.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `OMNIROUTE_BOOTSTRAPPED`                        | `false`                                             | `src/app/(dashboard)/dashboard/page.tsx`                    | پس از راهاندازی اولیه، توسط اسکریپت bootstrap روی `true` تنظیم میشود. نمایش جادوگر راهاندازی را کنترل میکند.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `OMNIROUTE_ALLOW_BODY_PROJECT_OVERRIDE`         | `0`                                                 | `open-sse/executors/antigravity.ts`                         | راه گریز: به بدنه درخواست اجازه میدهد فیلد پروژه Antigravity را بازنویسی کند.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `ANTIGRAVITY_CREDITS`                           | `off`                                               | `open-sse/services/antigravityCredits.ts`                   | سیاست اعتبارهای Google One AI: مقدار `off` هرگز اعتبارها را تزریق نمیکند، `retry` پس از خطای سهمیه واجد شرایط 429 یکبار آنها را تزریق میکند و `always` در نخستین درخواست آنها را تزریق میکند.                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `ANTIGRAVITY_ALLOW_SIGNATURE_BYPASS`            | `0`                                                 | `open-sse/translator/request/openai-to-gemini.ts`           | به مترجم درخواست Antigravity اجازه میدهد وقتی سرویس بالادستی امضاهای واقعی را رد میکند، از اعتبارسنجی سختگیرانه امضای درخواست CLI صرفنظر کند (حالت اشکالزدایی/CLI منسوخ). مقدار غیرصفر این دور زدن را فعال میکند.                                                                                                                                                                                                                                                                                                                                                                                                        |
| `AGY_TOKEN_FILE`                                | `~/.gemini/antigravity-cli/antigravity-oauth-token` | `src/app/api/providers/agy-auth/apply-local/route.ts`       | مسیر فایل توکن CLI نرمافزار Antigravity (agy) را برای وارد کردن ورود محلی با تشخیص خودکار بازنویسی میکند.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |

### پل OAuth برای CLI (داخلی)

| متغیر               | پیشفرض        | فایل منبع                       | توضیحات                                          |
| ------------------- | ------------- | ------------------------------- | ------------------------------------------------ |
| `OMNIROUTE_SERVER`  | تشخیص خودکار  | `src/lib/oauth/config/index.ts` | نشانی URL سرور برای پل احراز هویت CLI↔OmniRoute. |
| `OMNIROUTE_TOKEN`   | _(تنظیمنشده)_ | `src/lib/oauth/config/index.ts` | توکن احراز هویت برای پل CLI.                     |
| `OMNIROUTE_USER_ID` | `cli`         | `src/lib/oauth/config/index.ts` | شناسه کاربر برای نشستهای پل CLI.                 |
| `SERVER_URL`        | _(تنظیمنشده)_ | `src/lib/oauth/config/index.ts` | نام مستعار قدیمی برای `OMNIROUTE_SERVER`.        |
| `CLI_TOKEN`         | _(تنظیمنشده)_ | `src/lib/oauth/config/index.ts` | نام مستعار قدیمی برای `OMNIROUTE_TOKEN`.         |
| `CLI_USER_ID`       | _(تنظیمنشده)_ | `src/lib/oauth/config/index.ts` | نام مستعار قدیمی برای `OMNIROUTE_USER_ID`.       |

---

## 11. اطلاعات احراز هویت ارائهدهندگان OAuth

اطلاعات احراز هویت داخلی برای **توسعه روی localhost**. برای استقرارهای راهدور، اطلاعات احراز هویت اختصاصی خود را در کنسول توسعهدهندگان هر ارائهدهنده ثبت کنید.

| متغیر                             | ارائهدهنده              | توضیحات                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| --------------------------------- | ----------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `CLAUDE_OAUTH_CLIENT_ID`          | Claude Code (Anthropic) | کلاینت عمومی — نیازی به رمز نیست.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `CLAUDE_CODE_REDIRECT_URI`        | Claude Code             | بازنویسی URI تغییر مسیر. پیشفرض: `https://platform.claude.com/oauth/code/callback`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `CODEX_OAUTH_CLIENT_ID`           | Codex / OpenAI          | کلاینت عمومی.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `GEMINI_OAUTH_CLIENT_ID`          | Gemini (Google)         | به `_SECRET` متناظر نیاز دارد.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `GEMINI_OAUTH_CLIENT_SECRET`      | Gemini (Google)         | —                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `KIMI_CODING_OAUTH_CLIENT_ID`     | Kimi Coding (Moonshot)  | کلاینت عمومی.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `ANTIGRAVITY_OAUTH_CLIENT_ID`     | Antigravity (Google)    | به `_SECRET` متناظر نیاز دارد.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `ANTIGRAVITY_OAUTH_CLIENT_SECRET` | Antigravity (Google)    | —                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `GITHUB_OAUTH_CLIENT_ID`          | GitHub Copilot          | کلاینت عمومی.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `GHE_COPILOT_OAUTH_CLIENT_ID`     | GHE Copilot             | بازنویسی اختیاری شناسه کلاینت OAuth مربوط به GitHub Enterprise Copilot. اگر تنظیم نشده باشد، از مقدار عمومی پیشفرض `GITHUB_OAUTH_CLIENT_ID` استفاده میکند.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `COPILOT_INTEGRATION_ID`          | GitHub Copilot          | بازنویسی اختیاری شناسه یکپارچهسازی کلاینت GitHub Copilot که در هدرهای `Copilot-Integration-Id` و `Editor-Plugin-Version` ارسال میشود. مقدار پیشفرض `copilot-developer-cli` است.                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `WINDSURF_API_KEY`                | Windsurf / Devin (v3.8) | کلید API جایگزین که وقتی اطلاعات احراز هویت مختص اتصال موجود نباشد، توسط `open-sse/executors/devin-cli.ts` استفاده میشود. اختیاری است.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `CLI_DEVIN_BIN`                   | Devin CLI (v3.8)        | مسیر سفارشی فایل اجرایی Devin CLI ‏(`devin`). توسط `open-sse/executors/devin-cli.ts` تفکیک میشود.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `GITLAB_DUO_OAUTH_CLIENT_ID`      | GitLab Duo (v3.8)       | شناسه کلاینت OAuth برای GitLab Duo. یک برنامه در `https://gitlab.com/-/profile/applications` با URI تغییر مسیر `<NEXT_PUBLIC_BASE_URL>/callback` و دامنههای دسترسی `api, read_user, openid, profile, email` ثبت کنید. در صورت نبود مقدار، از `GITLAB_OAUTH_CLIENT_ID` استفاده میکند.                                                                                                                                                                                                                                                                                                                                                         |
| `GITLAB_DUO_OAUTH_CLIENT_SECRET`  | GitLab Duo (v3.8)       | رمز کلاینت OAuth برای GitLab Duo. اختیاری است — جریان PKCE به رمز نیاز ندارد. در صورت نبود مقدار، از `GITLAB_OAUTH_CLIENT_SECRET` استفاده میکند.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `GITLAB_DUO_BASE_URL`             | GitLab Duo (v3.8)       | بازنویسی URL پایه GitLab (برای GitLab خودمیزبان). مقدار پیشفرض `https://gitlab.com` است. در صورت نبود مقدار، از `GITLAB_BASE_URL` استفاده میکند.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `GITLAB_BASE_URL`                 | GitLab Duo (v3.8)       | مقدار جایگزین قدیمی برای `GITLAB_DUO_BASE_URL`. وقتی گونه `_DUO_` تنظیم نشده باشد، استفاده میشود.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `GITLAB_OAUTH_CLIENT_ID`          | GitLab Duo (v3.8)       | مقدار جایگزین قدیمی برای `GITLAB_DUO_OAUTH_CLIENT_ID` که توسط `src/lib/oauth/constants/oauth.ts` استفاده میشود.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `GITLAB_OAUTH_CLIENT_SECRET`      | GitLab Duo (v3.8)       | مقدار جایگزین قدیمی برای `GITLAB_DUO_OAUTH_CLIENT_SECRET` که توسط `src/lib/oauth/constants/oauth.ts` استفاده میشود.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `QODER_OAUTH_CLIENT_SECRET`       | Qoder                   | —                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `QODER_OAUTH_AUTHORIZE_URL`       | Qoder                   | برای فعالکردن OAuth در Qoder تنظیم کنید.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `QODER_OAUTH_TOKEN_URL`           | Qoder                   | —                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `QODER_OAUTH_USERINFO_URL`        | Qoder                   | —                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `QODER_OAUTH_CLIENT_ID`           | Qoder                   | —                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `QODER_PERSONAL_ACCESS_TOKEN`     | Qoder                   | کلید API مستقیم جایگزین (OAuth را دور میزند).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `QODER_CLI_WORKSPACE`             | Qoder                   | شناسه فضای کاری برای Qoder CLI.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `OMNIROUTE_QODER_WORKSPACE`       | Qoder                   | نام مستعار `QODER_CLI_WORKSPACE`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `QODER_CLI_CONFIG_DIR`            | Qoder                   | بازنویسی پوشه پیکربندی Qoder CLI (نشست PAT ایزوله، بدون اختلال در ورود مرورگر).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `BLACKBOX_WEB_VALIDATED_TOKEN`    | Blackbox Web            | توکن `tk` فرانتاند که بهصورت `validated` در `/api/chat` ارسال میشود. وقتی Blackbox تطبیق توکن را اجباری میکند، الزامی است؛ در غیر این صورت، OmniRoute از یک UUID تصادفی بهعنوان مقدار جایگزین استفاده میکند. مسئله #2252 را ببینید.                                                                                                                                                                                                                                                                                                                                                                                                          |
| `VISION_BRIDGE_BASE_URL`          | محافظ Vision Bridge     | نشانی URL پایه سازگار با OpenAI برای فراخوانیهای vision-bridge غیر Anthropic. مقدار پیشفرض، متغیر محیطی قدیمی URL مربوط به OpenAI یا api.openai.com است. آن را به حلقه خودارجاع `/v1` متعلق به OmniRoute یا هر نقطه پایانی سازگار با OpenAI (Gemini OpenAI-compat، OpenRouter) اشاره دهید. مسئله #2232. وقتی URL همان `/v1` متعلق به OmniRoute باشد، زیردرخواست توصیف، `x-omniroute-admission-bypass: internal` را ارسال میکند و با اعتبارنامه حلشده حلقه خودارجاع (`sk_omniroute` بهعنوان مقدار نگهبان در حالت محلی، یا `OMNIROUTE_API_KEY` / `ROUTER_API_KEY` — #1350) احراز هویت میشود تا استقرارهایی با `REQUIRE_API_KEY=true` کار کنند. |
| `VISION_BRIDGE_API_KEY`           | محافظ Vision Bridge     | کلید API برای URL بالا. متغیرهای محیطی OpenAI / Google مختص هر ارائهدهنده را برای فراخوانیهای vision-bridge غیر Anthropic بازنویسی میکند. مدلهای Anthropic همچنان از مسیر اختصاصی کلید Anthropic خود استفاده میکنند. مسئله #2232.                                                                                                                                                                                                                                                                                                                                                                                                            |

> [!WARNING]
>
> 1. به [Google Cloud Console → Credentials](https://console.cloud.google.com/apis/credentials) بروید
> 2. یک شناسه کلاینت OAuth 2.0 ایجاد کنید (نوع: «برنامه وب»)
> 3. URL سرور خود را بهعنوان URI مجاز تغییر مسیر اضافه کنید
> 4. مقادیر اعتبارنامه را در `.env` جایگزین کنید.

---

## 12. بازنویسیهای User-Agent ارائهدهنده

هدر `User-Agent` ارسالشده به هر ارائهدهنده بالادستی را بازنویسی کنید. این مقدار در زمان اجرا بهصورت پویا توسط کلاس پایه executor تعیین میشود:

```
process.env[`${PROVIDER_ID}_USER_AGENT`]
```

> **منبع:** `open-sse/executors/base.ts` → `buildHeaders()`

| متغیر | مقدار پیشفرض | زمان بهروزرسانی |
| -------------------------------- | --------------------------------------------- | ------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `CLAUDE_USER_AGENT` | `claude-cli/2.1.258 (external, cli)` | هنگامی که Anthropic نسخه جدیدی از CLI منتشر میکند |
| `CLAUDE_DISABLE_TOOL_NAME_CLOAK` | `false` | `executors/base.ts` + `executors/cliproxyapi.ts` | برای ارسال عین نام ابزارهای harness شخص ثالث به Anthropic در هر دو مسیر منتهی به Anthropic (OAuth بومی و CLIProxyAPI)، آن را روی `1`/`true` تنظیم کنید. بهطور پیشفرض، executor برای نام ابزارهایی که متعلق به Claude Code نیستند، نام مستعار قطعی ایجاد میکند (در صورت وجود، از نگاشت متعارف Claude Code استفاده میشود؛ در غیر این صورت از PascalCase) و آنها را در پاسخ از طریق `_toolNameMap` معکوس میکند تا harnessهای دارای ابزارهای snake_case بهعنوان کلاینتهای شخص ثالث دارای اثرانگشت رد نشوند. فقط برای اشکالزدایی. |
| `CODEX_USER_AGENT` | `codex-cli/0.142.0 (Windows 10.0.26200; x64)` | هنگامی که OpenAI، Codex CLI را بهروزرسانی میکند |
| `CODEX_CLIENT_VERSION` | `0.131.0` | بازنویسی نسخه کلاینت Codex، مستقل از رشته کامل UA |
| `CLAUDE_CODE_CLIENT_VERSION` | `2.1.258` | بازنویسی نسخه اعلامشده Claude Code، مستقل از `CLAUDE_USER_AGENT`. Anthropic دسترسی به برخی مدلها را بر اساس این مقدار کنترل میکند (#12417). |
| `GITHUB_COPILOT_CLI_VERSION` | `1.0.81-6` | بازنویسی نسخه اعلامشده Copilot CLI، مستقل از `GITHUB_USER_AGENT` |
| `GITHUB_USER_AGENT` | `GitHubCopilotChat/0.54.0` | هنگامی که GitHub Copilot Chat بهروزرسانی میشود |
| `ANTIGRAVITY_USER_AGENT` | `antigravity/2.0.1 darwin/arm64` | هنگامی که Antigravity IDE بهروزرسانی میشود |
| `KIRO_USER_AGENT` | `AWS-SDK-JS/3.0.0 kiro-ide/1.0.0` | هنگامی که Kiro IDE بهروزرسانی میشود |
| `KIRO_OAUTH_CLIENT_ID` | `kiro-cli` | بازنویسی `clientId` عمومی مربوط به کد دستگاه اجتماعی Kiro |
| `KIRO_VERIFY_FULL_CRC` | `false` | انتخاب اختیاری: اعتبارسنجی کامل CRC برای هر فریم در جریان رویداد Kiro (برای اشکالزدایی جریانهای خراب) |
| `QODER_USER_AGENT` | `Qoder-Cli` | هنگامی که Qoder CLI بهروزرسانی میشود |
| `CURSOR_USER_AGENT` | `Cursor/3.3` | هنگامی که Cursor بهروزرسانی میشود |

> [!TIP]
> میتوانید با استفاده از الگوی `{PROVIDER_ID}_USER_AGENT` برای **هر** ارائهدهندهای بازنویسی User-Agent اضافه کنید. executor نام متغیر محیطی را بهصورت پویا ایجاد میکند.

---

## ۱۳. سازگاری اثر انگشت CLI

در صورت فعالسازی، OmniRoute ترتیب هدرهای HTTP و فیلدهای بدنه JSON را تغییر میدهد تا دقیقاً با امضای ابزارهای رسمی CLI مطابقت داشته باشد. این کار ضمن حفظ IP پروکسی شما، خطر علامتگذاری حساب را کاهش میدهد.

**منبع:** `open-sse/config/cliFingerprints.ts`، `open-sse/executors/base.ts`

### بهازای هر ارائهدهنده

| متغیر                    | فعالسازی | تأثیر                                       |
| ------------------------ | -------- | ------------------------------------------- |
| `CLI_COMPAT_CODEX`       | `=1`     | امضای درخواست Codex CLI را تقلید میکند      |
| `CLI_COMPAT_CLAUDE`      | `=1`     | امضای درخواست Claude Code را تقلید میکند    |
| `CLI_COMPAT_GITHUB`      | `=1`     | امضای درخواست GitHub Copilot را تقلید میکند |
| `CLI_COMPAT_ANTIGRAVITY` | `=1`     | امضای درخواست Antigravity را تقلید میکند    |
| `CLI_COMPAT_CURSOR`      | `=1`     | امضای درخواست Cursor را تقلید میکند         |
| `CLI_COMPAT_KIMI_CODING` | `=1`     | امضای درخواست Kimi Coding را تقلید میکند    |
| `CLI_COMPAT_KILOCODE`    | `=1`     | امضای درخواست Kilo Code را تقلید میکند      |
| `CLI_COMPAT_CLINE`       | `=1`     | امضای درخواست Cline را تقلید میکند          |

### سراسری

| متغیر            | فعالسازی | تأثیر                                                                   |
| ---------------- | -------- | ----------------------------------------------------------------------- |
| `CLI_COMPAT_ALL` | `=1`     | سازگاری اثر انگشت را بهطور همزمان برای **همه** ارائهدهندگان فعال میکند. |

### بازنویسیهای هویت Kimi Coding CLI

| متغیر                   | مقدار پیشفرض            | فایل منبع                                | توضیحات                                                                    |
| ----------------------- | ----------------------- | ---------------------------------------- | -------------------------------------------------------------------------- |
| `KIMI_CLI_VERSION`      | `1.36.0`                | `src/lib/oauth/providers/kimi-coding.ts` | نسخه Kimi CLI ارسالشده هنگام فراخوانیهای OAuth/API را بازنویسی میکند.      |
| `KIMI_CODING_DEVICE_ID` | _(مقدار پیشفرض ثبتشده)_ | `src/lib/oauth/providers/kimi-coding.ts` | شناسه دستگاه Kimi ثبتشده و مورداستفاده در هدرهای کلاینت را بازنویسی میکند. |

> [!NOTE]
> این قابلیت در کنار بازنویسیهای User-Agent (§۱۲) کار میکند. سامانه اثر انگشت ترتیب هدرها و فیلدهای بدنه را مدیریت میکند، درحالیکه بازنویسیهای User-Agent رشته مشخص UA را مدیریت میکنند. هر دو را میتوان بهطور مستقل فعال کرد.

---

## ۱۴. ارائهدهندگان کلید API

کلیدهای API برای ارائهدهندگانی که از احراز هویت مستقیم استفاده میکنند. **روش ترجیحی راهاندازی:** داشبورد ← ارائهدهندگان ← افزودن کلید API.

تنظیم از طریق متغیرهای محیطی، گزینهای جایگزین برای استقرارهای Docker یا بدون رابط کاربری است.

الگوی شناختهشده: `{PROVIDER_ID}_API_KEY`

| متغیر              | ارائهدهنده                                                   |
| ------------------ | ------------------------------------------------------------ |
| `DEEPSEEK_API_KEY` | DeepSeek                                                     |
| `NVIDIA_API_KEY`   | NVIDIA NIM                                                   |
| `JINA_AI_API_KEY`  | Jina AI ‏(Foundation API + روش جایگزین Reader)               |
| `JINA_API_KEY`     | Jina AI ‏(نام مستعار `JINA_AI_API_KEY`)                      |
| `GEMINI_API_KEY`   | Gemini ‏(Google AI Studio) برای embeddingها + روش جایگزین چت |
| `GOOGLE_API_KEY`   | Gemini ‏(نام مستعار `GEMINI_API_KEY`)                        |

> [!NOTE]
> ورودیهای ایستای `${PROVIDER}_API_KEY` برای Groq، xAI، Mistral، Perplexity، Together AI، Fireworks، Cerebras، Cohere، Nebius و Qianfan در v3.8.0 حذف شدند، زیرا محیط اجرا دیگر آنها را نمیخواند — این ارائهدهندگان منحصراً به داشبورد / `data/provider-credentials.json` / پایگاه داده رمزگذاریشده متکی هستند. برای مسیر مهاجرت، بخش _ممیزی: متغیرهای حذفشده / بلااستفاده_ را در انتهای این سند ببینید.

> [!TIP]
> کلیدهایی که از طریق داشبورد تنظیم میشوند، بهصورت رمزگذاریشده در SQLite ذخیره شده و بر متغیرهای محیطی اولویت دارند.
>
> **Jina:** ‏embedding، rerank، classify، segment و `jina-search` متعلق به `jina-ai/…`، هنگامی که یک اتصال `jina-ai` (یا `jina-reader` مشترک) در داشبورد وجود داشته باشد، هزینهای به کلید محیطی کلاستر تحمیل **نمیکنند** — `getProviderCredentials` بر اساس اصل «ابتدا تکمیل» عمل میکند. `JINA_AI_API_KEY` / `JINA_API_KEY` فقط زمانی استفاده میشوند که هیچ کلید قابلاستفادهای در داشبورد وجود نداشته باشد. گزارشهای فراخوانی، روش جایگزین محیطی را با `connection_id=env:JINA_AI_API_KEY` ثبت میکنند. کارت Reader ‏(`jina-reader`، `r.jina.ai`) هرگز `/v1/embeddings` یا `/v1/rerank` را ارائه نمیکند.
>
> **Gemini:** ‏`gemini/gemini-embedding-2` (با نام مستعار `google/gemini-embedding-2`) ابتدا از اتصال `gemini` در داشبورد استفاده میکند. `GEMINI_API_KEY` / `GOOGLE_API_KEY` فقط زمانی استفاده میشوند که هیچ کلید قابلاستفادهای در داشبورد وجود نداشته باشد. گزارشهای فراخوانی، روش جایگزین محیطی را با `connection_id=env:GEMINI_API_KEY` ثبت میکنند. ترافیک چندوجهی بومی از `x-goog-api-key` برای دسترسی به `:embedContent` / `:batchEmbedContents` استفاده میکند — N مورد OpenAI در `input` به N بردار تبدیل میشوند.

---

## 15. تنظیمات مهلت زمانی

همه مقادیر بر حسب **میلیثانیه** هستند. منطق تعیین متمرکز مقادیر در `src/shared/utils/runtimeTimeouts.ts` قرار دارد.

### سلسلهمراتب مهلتهای زمانی

```
REQUEST_TIMEOUT_MS (بازنویسی سراسری)
├─→ FETCH_TIMEOUT_MS (فراخوانیهای ارائهدهنده بالادستی، پیشفرض: 600000)
│   ├─→ FETCH_HEADERS_TIMEOUT_MS (از FETCH_TIMEOUT_MS به ارث میبرد)
│   ├─→ FETCH_BODY_TIMEOUT_MS (از FETCH_TIMEOUT_MS به ارث میبرد)
│   ├─→ TLS_CLIENT_TIMEOUT_MS (از FETCH_TIMEOUT_MS به ارث میبرد)
│   │   └── TLS_FIRST_BYTE_WATCHDOG_MS (مستقل، پیشفرض: 10000)
│   ├── RESPONSES_FIRST_BYTE_TIMEOUT_MS (مستقل، پیشفرض: 15000)
│   ├── FETCH_CONNECT_TIMEOUT_MS (مستقل، پیشفرض: 30000)
│   └── FETCH_KEEPALIVE_TIMEOUT_MS (مستقل، پیشفرض: 4000)
├─→ STREAM_IDLE_TIMEOUT_MS (از REQUEST_TIMEOUT_MS به ارث میبرد، پیشفرض: 600000)
├─→ STREAM_ACTIVE_TIMEOUT_MS (مستقل، پیشفرض: 1260000؛ مقدار 0 آن را غیرفعال میکند)
├─→ STREAM_READINESS_TIMEOUT_MS (از REQUEST_TIMEOUT_MS به ارث میبرد، پیشفرض: 80000)
├─→ STREAM_READINESS_MAX_TIMEOUT_MS (افزایشهای تطبیقی زمان آمادگی را محدود میکند، پیشفرض: 180000)
└─→ API_BRIDGE_PROXY_TIMEOUT_MS (از REQUEST_TIMEOUT_MS به ارث میبرد، پیشفرض: 30000)
    ├─→ API_BRIDGE_SERVER_REQUEST_TIMEOUT_MS (محاسبهشده، پیشفرض: 300000)
    ├── API_BRIDGE_SERVER_HEADERS_TIMEOUT_MS (پیشفرض: 60000)
    ├── API_BRIDGE_SERVER_KEEPALIVE_TIMEOUT_MS (پیشفرض: 5000)
    └── API_BRIDGE_SERVER_SOCKET_TIMEOUT_MS (پیشفرض: 0 = غیرفعال)
```

| متغیر                                           | پیشفرض                                                            | توضیحات                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| ----------------------------------------------- | ----------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `REQUEST_TIMEOUT_MS`                            | _(تنظیمنشده)_                                                     | میانبُر سراسری — مقادیر پیشفرض `FETCH_TIMEOUT_MS` و `STREAM_IDLE_TIMEOUT_MS` را بازنویسی میکند.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `FETCH_TIMEOUT_MS`                              | `600000`                                                          | مهلت زمانی کل درخواست HTTP برای فراخوانیهای ارائهدهنده بالادستی.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `STREAM_IDLE_TIMEOUT_MS`                        | `600000`                                                          | حداکثر زمان سکوت میان بایتهای خام بالادستی پیش از لغو. مدلهای دارای استدلال گسترده بهندرت بیش از ۹۰ ثانیه مکث میکنند.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `STREAM_ACTIVE_TIMEOUT_MS`                      | `1260000`                                                         | حداکثر طول عمر کل جریان فعال SSE؛ با دریافت بایتهای بالادستی هرگز بازنشانی نمیشود و مستقل از `REQUEST_TIMEOUT_MS` است. این مقدار از بزرگترین `timeoutMs` مختص هر مدل در رجیستری (1200000، Codex)، بهاضافه حاشیهای 60000 محاسبه شده است تا مدلی که اجازه دارد از کل بودجه زمانی خود استفاده کند، هرگز در میانه پاسخ متوقف نشود. برای غیرفعالسازی روی `0` تنظیم کنید.                                                                                                                                                                                                                                                                                                                                                                                           |
| `OMNIROUTE_SSE_COMMENTS`                        | _(غیرفعال)_                                                       | مشخص میکند که آیا OmniRoute میتواند خطوط توضیحی `:` در SSE منتشر کند یا خیر (برای مثال، ضربان قلب `: keepalive` و تریلرهای فراداده `x-omniroute-*`). این قابلیت بهطور پیشفرض غیرفعال است (#10524)، زیرا کلاینتهای سختگیرِ سازگار با OpenAI هر خط SSE را با JSON.parse پردازش میکنند و در مواجهه با توضیحات `:` از کار میافتند؛ ضربانهای قلب `data:` تحت تأثیر قرار نمیگیرند. برای فعالسازی مجدد، آن را روی `on`/`true`/`1`/`yes` تنظیم کنید. در `open-sse/utils/sseHeartbeat.ts` استفاده میشود.                                                                                                                                                                                                                                                               |
| `STREAM_READINESS_TIMEOUT_MS`                   | `80000`                                                           | زمان انتظار برای دریافت نخستین رویداد SSE که ping نباشد. در صورت تنظیم `REQUEST_TIMEOUT_MS`، مقدار آن را به ارث میبرد.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `STREAM_READINESS_MAX_TIMEOUT_MS`               | `180000`                                                          | حداکثر بازه تطبیقی آمادگی برای نخستین رویداد در درخواستهای جریانی بزرگ، پرکاربرد از ابزار، یا دارای استدلال زیاد.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `OMNIROUTE_AGENT_GOAL_POLICY_ENABLED`           | `true`                                                            | کلید توقف اضطراری برای سازوکار اکتشافی `/goal`. برای غیرفعالسازی کامل تشخیص، آن را روی `false`/`0`/`off` تنظیم کنید — در این حالت، مهلتهای آمادگی و بازیابی جریان هرگز بر اساس بدنه/سرآیندهای درخواست افزایش نمییابند و خطر تقویت مهلت زمانی تحت کنترل کلاینت کاهش مییابد.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `OMNIROUTE_AGENT_GOAL_READINESS_MAX_TIMEOUT_MS` | `600000`                                                          | حداکثر بازه آمادگی نخستین رویداد برای اجرای عاملهای `/goal` تشخیصدادهشده یا درخواستهایی که با `x-omniroute-agent-goal` اجباری شدهاند.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `OMNIROUTE_AGENT_GOAL_STREAM_RECOVERY`          | `true`                                                            | بازیابی زودهنگام جریان را بهصورت خودکار برای اجرای عاملهای `/goal` تشخیصدادهشده فعال میکند. برای غیرفعالکردن این فعالسازی مختص هدف، آن را روی `false`/`0`/`off` تنظیم کنید. این گزینه فقط میتواند بازیابی را به پیشفرض اپراتور اضافه کند — هرگز انصراف صریح از طریق `STREAM_RECOVERY_ENABLED`/تنظیمات DB را بازنویسی نمیکند.                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `OMNIROUTE_CODEX_DROP_NONSTANDARD_EVENTS`       | `true`                                                            | رویدادهای غیراستاندارد SSE با الگوی `codex.*` (برای مثال، `codex.rate_limits`) را که باعث شکست `responses.stream()` در OpenAI SDK با خطای 502 میشوند، حذف میکند. بهطور پیشفرض روشن است (#11014). برای ارسال آنها، مقدار را روی `0`/`false`/`no`/`off` تنظیم کنید.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `OMNIROUTE_CODEX_APPSERVER_WS`                  | _(تنظیمنشده)_                                                     | انتقال اختیاری app-server برای Codex. نقطه پایانی WebSocket (`ws://`/`wss://`) متعلق به یک سایدکار محلی `codex app-server`. هنگامی که همراه با یک توکن تنظیم شود، درخواستهای Codex بهجای HTTP Responses API، از طریق JSON-RPC به سایدکار هدایت میشوند. همچنین برای هر اتصال از طریق `providerSpecificData.codexAppServerUrl` قابل تنظیم است. در `open-sse/executors/codex/appServerConfig.ts` استفاده میشود.                                                                                                                                                                                                                                                                                                                                                  |
| `OMNIROUTE_CODEX_APPSERVER_WS_TOKEN`            | _(تنظیمنشده)_                                                     | توکن قابلیت/حاملِ درونخطی که به app-server ارائه میشود. بازنویسی برای هر اتصال: `providerSpecificData.codexAppServerToken`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `OMNIROUTE_CODEX_APPSERVER_WS_TOKEN_FILE`       | _(تنظیمنشده)_                                                     | مسیر فایلی که توکن قابلیت app-server را نگه میدارد (از `codex app-server --ws-token-file`). هنگامی استفاده میشود که `OMNIROUTE_CODEX_APPSERVER_WS_TOKEN` تنظیم نشده باشد. بازنویسی برای هر اتصال: `providerSpecificData.codexAppServerTokenFile`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `OMNIROUTE_CODEX_APPSERVER_CWD`                 | `/tmp`                                                            | دایرکتوری کاری که نوبت app-server در آن اجرا میشود. بازنویسی برای هر اتصال: `providerSpecificData.codexAppServerCwd`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `OMNIROUTE_CODEX_APPSERVER_APPROVAL`            | _(تنظیمنشده)_                                                     | سیاست تأییدی که به نوبت app-server ارسال میشود (برای مثال، `never`، `on-request`). بازنویسی برای هر اتصال: `providerSpecificData.codexAppServerApprovalPolicy`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `OMNIROUTE_CODEX_APPSERVER_SANDBOX`             | _(تنظیمنشده)_                                                     | سیاست sandbox که به نوبت app-server ارسال میشود (برای مثال، `read-only`، `workspace-write`، `danger-full-access`). در صورت تنظیمنبودن، اجراکننده بهطور پیشفرض از `workspace-write` استفاده میکند (ایمنسازیشده؛ قبلاً `danger-full-access`). بازنویسی برای هر اتصال: `providerSpecificData.codexAppServerSandbox`.                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `OMNIROUTE_CODEX_APPSERVER_AUTO_APPROVE`        | `false`                                                           | درخواستهای تأیید خود app-server را بهطور خودکار تأیید میکند (اجرای فرمان/فایل/مجوز روی میزبان). بهطور پیشفرض خاموش است — درخواستها بهطور خودکار رد میشوند؛ فراخوانیهای ابزار harness تحت تأثیر قرار نمیگیرند (آنها از مسیر عبوری جداگانه `item/tool/call` منتقل میشوند). مقادیر `true`/`1`/`yes` را میپذیرد. بازنویسی برای هر اتصال: `providerSpecificData.codexAppServerAutoApprove`.                                                                                                                                                                                                                                                                                                                                                                        |
| `FETCH_HEADERS_TIMEOUT_MS`                      | = `FETCH_TIMEOUT_MS`                                              | زمان انتظار برای دریافت سرآیندهای پاسخ.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `OMNIROUTE_DIRECT_HEADERS_TIMEOUT_MS`           | `30000` (۳۰ ثانیه)                                                | حداکثر زمان انتظار برای شروع پاسخ (میلیثانیه) در هر تلاش مستقیم بدون پروکسی. در صورت پایان مهلت، یک بار دیگر با یک سوکت جدید تلاش میشود؛ برای غیرفعالکردن این محدودیت و حفظ رفتار قبلی، مقدار را روی `0` تنظیم کنید.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `OMNIROUTE_DIRECT_RESPONSE_RETRY_TIMEOUT_MS`    | `600000` (۱۰ دقیقه)                                               | سقف زمانی (میلیثانیه) برای تلاش مجدد RETRY با سوکت جدید، پس از پایان مهلت شروع پاسخ در تلاش مبتنی بر سوکت تجمیعشده در بالا (#13703). این مقدار فقط زمانی اعمال میشود که فراخوان از قبل سیگنال مهلت اختصاصی خود را متصل کرده باشد (زنجیره اتصال/مدل/ارائهدهنده/`FETCH_TIMEOUT_MS` حلشده)؛ آن سیگنال محدودیت واقعی است و در مسیر موردنظر زودتر فعال میشود، بنابراین این مقدار بهجای یک سقف ثابت، یک حد پشتیبان سخاوتمندانه است — بدون آن، تلاش مجدد از همان بازه کوتاه `OMNIROUTE_DIRECT_HEADERS_TIMEOUT_MS` مربوط به تلاش تجمیعشده استفاده میکرد و برای مدلهای استدلالی سالم با TTFB کُند، خطای 504 ایجاد میشد. این مقدار هرگز نمیتواند کمتر از کف ثابت بالا باشد؛ وقتی فراخوان هیچ سیگنال مهلتی ارائه نمیکند، تلاش مجدد همان کف ثابت را بدون تغییر حفظ میکند. |
| `FETCH_BODY_TIMEOUT_MS`                         | = `FETCH_TIMEOUT_MS`                                              | زمان دریافت کامل بدنه پاسخ.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `FETCH_CONNECT_TIMEOUT_MS`                      | `30000`                                                           | مهلت زمانی برقراری اتصال TCP.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `FETCH_KEEPALIVE_TIMEOUT_MS`                    | `4000`                                                            | مهلت بیکاری سوکت keep-alive.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `TLS_CLIENT_TIMEOUT_MS`                         | = `FETCH_TIMEOUT_MS`                                              | مهلت زمانی پروکسی اثرانگشت TLS ‏(wreq-js).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `TLS_FIRST_BYTE_WATCHDOG_MS`                    | `10000`                                                           | زمان تا دریافت نخستین بایت را مشخصاً برای بدنه انتقال اثرانگشت TLS مبتنی بر wreq-js محدود میکند؛ `TLS_CLIENT_TIMEOUT_MS` بهتنهایی نمیتواند توقف بدنه را تشخیص دهد، زیرا بهمحض رسیدن سرآیندها خاتمه مییابد (#12656). پایان مهلت، خوانشگر wreq را لغو میکند و به dispatcher مستقیم/پروکسی بازمیگردد؛ `0` این ناظر را غیرفعال میکند.                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `RESPONSES_FIRST_BYTE_TIMEOUT_MS`               | `15000`                                                           | فقط برای اجراکننده OpenCode و تنها هنگامی که پرچم قابلیت `OPENCODE_RESPONSES_STALL_ROTATION` روشن باشد (بهطور پیشفرض خاموش است): انتظار برای نخستین بایت بدنه یک پاسخ جریانی Responses پس از سرآیندهای آن را محدود میکند (#13484). یک جریان Responses با `response.created` باز میشود، بنابراین سکوت پس از این بازه بهمعنای توقف است: حساب وارد دوره انتظار میشود و درخواست یک بار به حساب بعدی منتقل میشود؛ توقف دوم سریعاً با شکست مواجه میشود. `0` حتی در صورت روشنبودن پرچم، این محافظ را غیرفعال میکند.                                                                                                                                                                                                                                                  |
| `OPENCODE_PARK_AND_RESUME`                      | `false`                                                           | فقط برای اجراکننده OpenCode: پس از دریافت مکرر خطاهای گذرای 429 (یا وجود یک نشانگر تازه فشار بر استخر)، درخواست را همراه با ضربان قلب متوقف کرده و سپس بهجای پخشکردن درخواست در کل مجموعه، یک مرحله محدود شامل حداکثر ۳ حساب متوالی را دوباره اجرا میکند (#13924). بهطور پیشفرض خاموش است: هر خطای 429 دقیقاً مانند قبل به حساب بعدی منتقل میشود.                                                                                                                                                                                                                                                                                                                                                                                                             |
| `OPENCODE_POOL_STRAIN_MARKER_PATH`              | _(تنظیمنشده)_                                                     | فقط برای اجراکننده OpenCode: مسیر نشانگر فشار بر استخر را که پیش از توقف خوانده میشود بازنویسی میکند (`{since, reason, ttl_s}`، پیشفرض `/tmp/opencode-pool-strain.json`، ‏#13924). یک نشانگر تازه بدون شمارش مجدد باعث توقف میشود؛ در صورت نبودن یا منقضیبودن نشانگر، از شمارنده جهش استفاده میشود.                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `API_BRIDGE_PROXY_TIMEOUT_MS`                   | `30000`                                                           | مهلت زمانی گام پروکسی برای درخواستهای پل `/v1`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `FIRECRAWL_BASE_URL`                            | `https://api.firecrawl.dev`                                       | اجراکننده واکشی وب Firecrawl را به یک نمونه خودمیزبان هدایت میکند (کلید API در خارج از فضای ابری اختیاری است).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `FIRECRAWL_TIMEOUT_MS`                          | `30000`                                                           | مهلت زمانی هر درخواست برای اجراکننده واکشی وب Firecrawl.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `API_BRIDGE_SERVER_REQUEST_TIMEOUT_MS`          | `300000`                                                          | مهلت زمانی کلی درخواست سرور برای پل.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `API_BRIDGE_SERVER_HEADERS_TIMEOUT_MS`          | `60000`                                                           | زمان ارسال سرآیندهای پاسخ از طریق پل.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `API_BRIDGE_SERVER_KEEPALIVE_TIMEOUT_MS`        | `5000`                                                            | مهلت بیکاری keep-alive پل.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `API_BRIDGE_SERVER_SOCKET_TIMEOUT_MS`           | `0`                                                               | مهلت زمانی سوکت خام (0 = غیرفعال).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `SHUTDOWN_TIMEOUT_MS`                           | `30000`                                                           | مهلت تنفس پس از SIGTERM/SIGINT پیش از خروج اجباری.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `OMNIROUTE_DEFAULT_FETCH_TIMEOUT_MS`            | `120000`                                                          | مقدار جایگزین مورداستفاده توسط `src/shared/utils/fetchTimeout.ts` هنگامی که `FETCH_TIMEOUT_MS` تنظیم نشده باشد.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `OMNIROUTE_PROVIDER_PROBE_TIMEOUT_MS`           | `8000`                                                            | مهلت زمانی (میلیثانیه) برای پیشتنظیمهای `validationRead` و `modelsProbe` در `src/shared/network/safeOutboundFetch.ts`. برای نقاط پایانی کُند (Cerebras، Cloudflare AI، Groq) آن را افزایش دهید تا از نوسان میان وضعیت فعال/خطا در داشبورد جلوگیری شود. برای مقادیر نامعتبر (<1000) یا غیرعددی، به 8000ms بازمیگردد.                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `OMNIROUTE_RELAY_FETCH_TIMEOUT_MS`              | `25000`                                                           | مهلت واکشی ویژهٔ رله در `open-sse/utils/proxyFetch.ts` (#9158). یک رلهٔ معلق باید پیش از پایان مهلت کلاینت/عامل (~30s) با شکست مواجه شود تا فراخوانندهها بهجای مهلتپایان عمومی بالادستی، خطایی مختص رله ببینند. این مقدار به `29000` محدود شده است تا همیشه زودتر فعال شود.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `OMNIROUTE_RETRY_BACKOFF_MS`                    | `10`                                                              | تأخیر مشترک تلاش مجدد برای مسیرهای مستقیم/رله/پراکسی با یکبار تلاش مجدد در `open-sse/utils/proxyFetch.ts` (#9158). `0` = تلاش مجدد فوری.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `OMNIROUTE_CLAUDE_TLS_TIMEOUT_MS`               | `60000`                                                           | مهلت درخواست بومی wreq-js (`claudeTlsClient.ts`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `OMNIROUTE_CLAUDE_TLS_GRACE_MS`                 | `10000`                                                           | مهلت ارفاقی قطعی JS که به مهلت بومی افزوده میشود.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `OMNIROUTE_PPLX_TLS_TIMEOUT_MS`                 | `30000`                                                           | مهلت درخواست بومی wreq-js (`perplexityTlsClient.ts`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `OMNIROUTE_PPLX_TLS_GRACE_MS`                   | `10000`                                                           | مهلت ارفاقی قطعی JS که به مهلت بومی افزوده میشود.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `OMNIROUTE_PPLX_SEARCH_HINT`                    | `0` (خاموش)                                                       | عبارت «شما جستوجوی وب داخلی دارید. با استفاده از نتایج جستوجو مستقیماً به پرسشها پاسخ دهید.» را به پیام سیستمی فراخواننده اضافه میکند (`perplexity-web/protocol.ts`). بهطور پیشفرض خاموش است — Perplexity در هر صورت جستوجو میکند و این جمله بهشکل فراتوضیح در پاسخهای کلاینتهای کدنویسی نشت میکند. برای بازیابی، آن را روی `1`/`true`/`yes`/`on` تنظیم کنید.                                                                                                                                                                                                                                                                                                                                                                                                 |
| `OMNIROUTE_GROK_TLS_TIMEOUT_MS`                 | `60000`                                                           | مهلت درخواست بومی wreq-js (`grokTlsClient.ts`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `OMNIROUTE_GROK_TLS_GRACE_MS`                   | `10000`                                                           | مهلت ارفاقی قطعی JS که به مهلت بومی افزوده میشود.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `OMNIROUTE_NOTION_TLS_TIMEOUT_MS`               | `30000`                                                           | مهلت درخواست بومی wreq-js (`notionTlsClient.ts`)؛ `notion-web` برای تولیدهای طولانی، آن را در هر درخواست به `180000` افزایش میدهد.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `OMNIROUTE_NOTION_TLS_GRACE_MS`                 | `10000`                                                           | مهلت ارفاقی قطعی JS که به مهلت بومی افزوده میشود.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `OMNIROUTE_BROWSER_POOL`                        | `on`                                                              | مخزن مشترک مرورگر Playwright برای گفتوگوی کوکی وب مبتنی بر مرورگر (`browserPool.ts`)؛ برای غیرفعالسازی روی `off` تنظیم کنید.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `OBSCURA_BIN`                                   | `auto-detect`                                                     | مسیر فایل باینری `obscura` که مخزن مرورگر و اجراکنندهٔ Cloudflare Playground از آن بهعنوان موتور اصلی استفاده میکنند (`open-sse/services/obscura.ts`)؛ اگر تنظیم نشده باشد، بهطور خودکار از PATH سیستم شناسایی میشود.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `OBSCURA_CDP_ENDPOINT`                          | _(تنظیمنشده)_                                                     | بهجای راهاندازی نمونهای جدید، به یک Obscura از پیش در حال اجرا (`http://host:port`) اشاره میکند؛ ماژول مالک آن فرایند نیست (`open-sse/services/obscura.ts`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `OBSCURA_PORT`                                  | `درگاه آزاد تصادفی`                                               | درگاه صریح برای `obscura serve` راهاندازیشده؛ اگر تنظیم نشده باشد، یک درگاه آزاد بهطور خودکار انتخاب میشود (`open-sse/services/obscura.ts`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `WEB_COOKIE_USE_BROWSER`                        | `0`                                                               | درخواست گفتوگوی کوکی وب را برای استفاده از مسیر مبتنی بر مرورگر فعال میکند (`browserBackedChat.ts`)؛ برای فعالسازی از `1` استفاده کنید.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `KIMI_WEB_BASE_URL`                             | `https://www.kimi.ai`                                             | نشانی پایه برای اجراکنندهٔ Kimi Web (نسخهٔ بینالمللی kimi.ai Connect-RPC) (`kimi-web.ts`)؛ فقط برای نقاط پایانی آینه/پراکسی بازنویسی کنید.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `KIMI_WEB_CHAT_URL`                             | `<KIMI_WEB_BASE_URL>/apiv2/kimi.gateway.chat.v1.ChatService/Chat` | نقطهٔ پایانی کامل گفتوگو برای اجراکنندهٔ Kimi Web (`kimi-web.ts`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `OMNIROUTE_LOGIN_BROWSER_PATH`                  | _(شناسایی خودکار)_                                                | مسیر فایل اجرایی Chrome/Edge سیستم برای ورود تعاملی از طریق مرورگر Adobe Firefly (`adobeFireflyBrowserLogin.ts`)؛ شناسایی خودکار مختص هر سیستمعامل را بازنویسی میکند.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `OMNIROUTE_STANDALONE_DIR`                      | _خروجی مستقل .build/_                                             | بازنویسی زمان ساخت برای دایرکتوری خروجی مستقل که مرحلهٔ هممکانی پس از ساخت از آن استفاده میکند (`scripts/build/colocate-standalone.mjs`)؛ مربوط به ابزارهای ساخت است، نه زمان اجرا.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |

تلاشهای مقصد ترکیبی، مهلت حلشدهٔ درخواست بالادستی را به ارث میبرند (`FETCH_TIMEOUT_MS`، یا
`REQUEST_TIMEOUT_MS` هنگامی که مقدار پیشفرض واکشی را تأمین میکند). `targetTimeoutMs` را در یک ترکیب،
پیشفرضهای ترکیب یا بازنویسی ارائهدهنده فقط برای سریعتر کردن بازگشت جایگزین ترکیب تنظیم کنید؛ مقادیر بالاتر از
مهلت فعلی بالادستی به همان مهلت بالادستی محدود میشوند.

`comboTimeoutMs` یک بودجهٔ زمانی مستقل برای کل ترکیب بر اساس زمان واقعی و در میان مقصدهای جایگزین است.
برای حفظ تکرار نامحدود، آن را تنظیمنشده یا روی `0` نگه دارید (توقف در صورت تعلیق ۱۰دقیقهای
`COMBO_LOOP_SAFETY_TIMEOUT_MS` همچنان اعمال میشود). یک مقدار مثبت، این
سازوکار ایمنی را برای ترکیب جایگزین میکند. `comboTimeoutMs` را طولانیتر از `targetTimeoutMs` نگه دارید تا پس از
کند بودن مقصد نخست، هنوز برای جایگزینی زمان باقی بماند.

### آستانههای قطعکنندهٔ مدار

تنظیم قطعکنندهٔ مدار در سطح ارائهدهنده. مقادیر پیشفرض منعکسکنندهٔ مقادیر مقیاسبندیشدهای هستند که از v3.6 برای بیش از 500 اتصال استفاده شدهاند.

| متغیر                                                         | پیشفرض    | فایل منبع                      | توضیحات                                                                                                                                     |
| ------------------------------------------------------------- | --------- | ------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------- |
| `OMNIROUTE_CIRCUIT_BREAKER_OAUTH_THRESHOLD`                   | `8`       | `open-sse/config/constants.ts` | آستانهٔ خطاهای متوالی برای ارائهدهندگان OAuth پیش از فعال شدن قطعکنندهٔ مدار.                                                               |
| `OMNIROUTE_CIRCUIT_BREAKER_OAUTH_RESET_MS`                    | `60000`   | `open-sse/config/constants.ts` | بازهٔ بازنشانی (میلیثانیه) برای قطعکنندهٔ ارائهدهندهٔ OAuth.                                                                                |
| `OMNIROUTE_CIRCUIT_BREAKER_API_KEY_THRESHOLD`                 | `12`      | `open-sse/config/constants.ts` | آستانهٔ خطاهای متوالی برای ارائهدهندگان مبتنی بر کلید API.                                                                                  |
| `OMNIROUTE_CIRCUIT_BREAKER_API_KEY_RESET_MS`                  | `30000`   | `open-sse/config/constants.ts` | بازهٔ بازنشانی (میلیثانیه) برای قطعکنندهٔ ارائهدهندهٔ مبتنی بر کلید API.                                                                    |
| `OMNIROUTE_CIRCUIT_BREAKER_LOCAL_THRESHOLD`                   | `2`       | `open-sse/config/constants.ts` | آستانهٔ خطاهای متوالی برای ارائهدهندگان محلی (Ollama، LM Studio و ...).                                                                     |
| `OMNIROUTE_CIRCUIT_BREAKER_LOCAL_RESET_MS`                    | `15000`   | `open-sse/config/constants.ts` | بازهٔ بازنشانی (میلیثانیه) برای قطعکنندهٔ ارائهدهندهٔ محلی.                                                                                 |
| `OMNIROUTE_PROVIDER_BREAKER_OAUTH_FAILURE_THRESHOLD`          | `10`      | `open-sse/config/constants.ts` | قطعکننده در سطح ارائهدهنده: تعداد خطاهای داخل بازه پیش از آنکه کل ارائهدهندهٔ OAuth وارد دورهٔ انتظار شود.                                  |
| `OMNIROUTE_PROVIDER_BREAKER_OAUTH_FAILURE_WINDOW_MS`          | `900000`  | `open-sse/config/constants.ts` | قطعکننده در سطح ارائهدهنده: بازهٔ لغزان شمارش خطا (میلیثانیه) برای ارائهدهندگان OAuth.                                                      |
| `OMNIROUTE_PROVIDER_BREAKER_OAUTH_COOLDOWN_MS`                | `300000`  | `open-sse/config/constants.ts` | قطعکننده در سطح ارائهدهنده: دورهٔ انتظار (میلیثانیه) پس از رسیدن ارائهدهندهٔ OAuth به آستانه.                                               |
| `OMNIROUTE_PROVIDER_BREAKER_OAUTH_DEGRADATION_THRESHOLD`      | `5`       | `open-sse/config/constants.ts` | ارائهدهندهٔ OAuth پس از این تعداد خطا وارد وضعیت DEGRADED میشود.                                                                            |
| `OMNIROUTE_PROVIDER_BREAKER_OAUTH_MAX_BACKOFF_MULTIPLIER`     | `8`       | `open-sse/config/constants.ts` | حداکثر ضریب افزایش `resetTimeout` برای ارائهدهندهٔ OAuth.                                                                                   |
| `OMNIROUTE_PROVIDER_BREAKER_OAUTH_BACKOFF_ESCALATION_COUNT`   | `2`       | `open-sse/config/constants.ts` | ارائهدهندهٔ OAuth پس از این تعداد چرخهٔ باز، سطح عقبنشینی را افزایش میدهد.                                                                  |
| `OMNIROUTE_PROVIDER_BREAKER_API_KEY_FAILURE_THRESHOLD`        | `15`      | `open-sse/config/constants.ts` | قطعکننده در سطح ارائهدهنده: تعداد خطاهای داخل بازه پیش از آنکه کل ارائهدهندهٔ مبتنی بر کلید API وارد دورهٔ انتظار شود.                      |
| `OMNIROUTE_PROVIDER_BREAKER_API_KEY_FAILURE_WINDOW_MS`        | `1800000` | `open-sse/config/constants.ts` | قطعکننده در سطح ارائهدهنده: بازهٔ لغزان شمارش خطا (میلیثانیه) برای ارائهدهندگان مبتنی بر کلید API.                                          |
| `OMNIROUTE_PROVIDER_BREAKER_API_KEY_COOLDOWN_MS`              | `600000`  | `open-sse/config/constants.ts` | قطعکننده در سطح ارائهدهنده: دورهٔ انتظار (میلیثانیه) پس از رسیدن ارائهدهندهٔ مبتنی بر کلید API به آستانه.                                   |
| `OMNIROUTE_PROVIDER_BREAKER_API_KEY_DEGRADATION_THRESHOLD`    | `7`       | `open-sse/config/constants.ts` | ارائهدهندهٔ مبتنی بر کلید API پس از این تعداد خطا وارد وضعیت DEGRADED میشود.                                                                |
| `OMNIROUTE_PROVIDER_BREAKER_API_KEY_MAX_BACKOFF_MULTIPLIER`   | `4`       | `open-sse/config/constants.ts` | حداکثر ضریب افزایش `resetTimeout` برای ارائهدهندهٔ مبتنی بر کلید API.                                                                       |
| `OMNIROUTE_PROVIDER_BREAKER_API_KEY_BACKOFF_ESCALATION_COUNT` | `3`       | `open-sse/config/constants.ts` | ارائهدهندهٔ مبتنی بر کلید API پس از این تعداد چرخهٔ باز، سطح عقبنشینی را افزایش میدهد.                                                      |
| `OMNIROUTE_PROVIDER_BREAKER_LOCAL_FAILURE_THRESHOLD`          | `2`       | `open-sse/config/constants.ts` | قطعکننده در سطح ارائهدهنده: تعداد خطاها پیش از آنکه کل ارائهدهندهٔ محلی وارد دورهٔ انتظار شود.                                              |
| `OMNIROUTE_PROVIDER_BREAKER_LOCAL_FAILURE_WINDOW_MS`          | `300000`  | `open-sse/config/constants.ts` | قطعکننده در سطح ارائهدهنده: بازهٔ لغزان شمارش خطا (میلیثانیه) برای ارائهدهندگان محلی.                                                       |
| `OMNIROUTE_PROVIDER_BREAKER_LOCAL_COOLDOWN_MS`                | `60000`   | `open-sse/config/constants.ts` | قطعکننده در سطح ارائهدهنده: دورهٔ انتظار (میلیثانیه) پس از رسیدن ارائهدهندهٔ محلی به آستانه.                                                |
| `PIN_DROP_BACKOFF_LEVEL`                                      | `2`       | `open-sse/services/combo.ts`   | عمق عقبنشینیای که در آن ارائهدهندهٔ سنجاق حافظهٔ نهان زمینه بهطور پایدار ناسالم تلقی میشود و سنجاق برای تغییر مسیر در زمان خرابی حذف میشود. |
| `PIN_DROP_GRACE_MS`                                           | `20000`   | `open-sse/services/combo.ts`   | بازهٔ ضدنوسان (میلیثانیه) که دورههای انتظار گذرا و کوتاه را پیش از حذف سنجاق حافظهٔ نهان زمینه تحمل میکند.                                  |

### سناریوها

| سناریو                              | پیکربندی                                             |
| ----------------------------------- | ---------------------------------------------------- |
| **تولید کد طولانیمدت**              | `REQUEST_TIMEOUT_MS=900000` (15 دقیقه)               |
| **محدود کردن طول عمر کل جریان**     | `STREAM_ACTIVE_TIMEOUT_MS=1260000` (21 دقیقه)        |
| **شکست سریع برای API محیط عملیاتی** | `API_BRIDGE_PROXY_TIMEOUT_MS=10000`                  |
| **مدلهای دارای تفکر طولانی**        | `STREAM_IDLE_TIMEOUT_MS=300000` (5 دقیقه بین قطعهها) |

---

## 16. ثبت لاگ

سیستم ثبت لاگ، خروجی را هم در stdout و هم در فایلهای لاگ چرخشی مینویسد. تمام پیکربندیها توسط `src/lib/logEnv.ts` خوانده میشوند.

| متغیر                                     | مقدار پیشفرض               | توضیحات                                                                                                                                                                                                                  |
| ----------------------------------------- | -------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `APP_LOG_LEVEL`                           | `info`                     | حداقل سطح لاگ: `debug`، `info`، `warn`، `error`.                                                                                                                                                                         |
| `APP_LOG_FORMAT`                          | `text`                     | قالب خروجی: `text` (خوانا برای انسان) یا `json` (ساختیافته).                                                                                                                                                             |
| `APP_LOG_TO_FILE`                         | `true`                     | لاگها را علاوه بر stdout در فایل نیز مینویسد.                                                                                                                                                                            |
| `APP_LOG_FILE_PATH`                       | `logs/application/app.log` | مسیر فایل لاگ (نسبت به ریشهٔ پروژه یا `DATA_DIR`).                                                                                                                                                                       |
| `APP_LOG_MAX_FILE_SIZE`                   | `50M`                      | حداکثر اندازهٔ فایل پیش از چرخش. مقادیر قابلقبول: `50M`، `1G`، `512K` یا تعداد بایت بهصورت عدد ساده.                                                                                                                     |
| `APP_LOG_RETENTION_DAYS`                  | `7`                        | تعداد روزهای نگهداری فایلهای چرخشی لاگ برنامه.                                                                                                                                                                           |
| `APP_LOG_MAX_FILES`                       | `20`                       | حداکثر تعداد نسخههای پشتیبان فایل لاگ چرخشی.                                                                                                                                                                             |
| `CALL_LOG_RETENTION_DAYS`                 | `7`                        | تعداد روزهای نگهداری ورودیهای لاگ درخواست/فراخوانی در پایگاه داده.                                                                                                                                                       |
| `CALL_LOG_MAX_ENTRIES`                    | `10000`                    | حداکثر تعداد ورودیهای لاگ فراخوانی در بافر درونحافظهای.                                                                                                                                                                  |
| `CALL_LOGS_TABLE_MAX_ROWS`                | `100000`                   | حداکثر تعداد ردیفهای جدول SQLite با نام `call_logs` پیش از پاکسازی.                                                                                                                                                      |
| `ENABLE_REQUEST_LOGS`                     | _(تنظیمنشده)_              | ثبت جزئیات درخواستها را با نادیده گرفتن تنظیمات داشبورد، بهاجبار فعال یا غیرفعال میکند.                                                                                                                                  |
| `MAX_PENDING_REQUEST_AGE_MS`              | `3600000` (۱ ساعت)         | حداکثر عمر ورودیهای لاگ درخواست فعالِ بدون وابستگی، پیش از پاکسازی از حافظه.                                                                                                                                             |
| `CALL_LOG_PIPELINE_CAPTURE_STREAM_CHUNKS` | `false`                    | وقتی `call_log_pipeline_enabled=true` است، قطعههای جریان را در مصنوعات خط لوله ذخیره میکند. این قابلیت اختیاری است (`true`) و برای صرفهجویی در فضای دیسک بهطور پیشفرض غیرفعال است.                                       |
| `CALL_LOG_PIPELINE_MAX_SIZE_KB`           | `512`                      | حداکثر اندازهٔ مصنوع لاگ فراخوانی خط لوله برحسب KB، وقتی `call_log_pipeline_enabled=true` است.                                                                                                                           |
| `PROXY_LOGS_TABLE_MAX_ROWS`               | `100000`                   | حداکثر تعداد ردیفهای جدول SQLite با نام `proxy_logs` پیش از پاکسازی.                                                                                                                                                     |
| `PROXY_LOG_INCLUDE_IPS`                   | `false`                    | نشانیهای IP کلاینت/خروجی و پیشوندهای حساب را در لاگهای کنسول `[ProxyEgress]` درج میکند. رکوردهای لاگ پراکسی در داشبورد/پایگاه داده، جزئیات کامل را حفظ میکنند.                                                           |
| `APP_LOG_ROTATION_CHECK_INTERVAL_MS`      | `60000` (۱ دقیقه)          | فاصلهٔ زمانی بررسی مجدد اندازهٔ فایل لاگ فعال توسط `src/lib/logRotation.ts`.                                                                                                                                             |
| `CHAT_LOG_TEXT_LIMIT`                     | `65536`                    | حداکثر طول رشتهٔ نگهداریشده در مصنوعات لاگ چت (مقدار پیشفرض ۶۴ KB).                                                                                                                                                      |
| `CHAT_LOG_ARRAY_TAIL_ITEMS`               | `128`                      | تعداد آیتمهای آرایه که هنگام کوتاهسازی بارهای دادهٔ لاگ چت از انتهای آرایه نگهداری میشوند.                                                                                                                               |
| `CHAT_LOG_MAX_DEPTH`                      | `6`                        | حداکثر عمق تودرتویی پیش از کوتاهسازی بارهای دادهٔ لاگ چت.                                                                                                                                                                |
| `CHAT_LOG_MAX_OBJECT_KEYS`                | `80`                       | حداکثر تعداد کلیدهای شیء که در بارهای دادهٔ لاگ چت نگهداری میشوند (0 = نامحدود).                                                                                                                                         |
| `CHAT_LOG_MAX_BODY_KB`                    | `1024`                     | اندازهٔ کل بدنهٔ درخواست/پاسخ (KB) که پس از عبور از آن، بهجای نسخهٔ کامل با یک خلاصهٔ ساده جایگزین میشود. اگر مکالمات عاملمحور طولانی بهجای پیامهای واقعی در داشبورد یک جاینگهدار نشان میدهند، این مقدار را افزایش دهید. |
| `CHAT_DEBUG_FILE`                         | `false`                    | وقتی مقدار آن true باشد، `serializeArtifactForStorage` از کوتاهسازی مبتنی بر اندازه صرفنظر میکند. فقط برای اشکالزدایی.                                                                                                   |

---

## 17. بهینهسازی حافظه

| متغیر                         | پیشفرض                                                     | توضیحات                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| ----------------------------- | ---------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `OMNIROUTE_MEMORY_MB`         | _خودکار_ (روی سختافزار مستقیم)؛ **`1024` در ایمیج Docker** | محدودیت پیشنهادی heap در V8 برای Docker/حالت مستقل (MB). وقتی تنظیم نشده باشد، بهصورت پویا کالیبره میشود (حدود ۳۵٪ از RAM سیستم، محدودشده به بازهٔ `[512, 4096]`)؛ `512` فقط زمانی حداقل است که خواندن کل حافظه ممکن نباشد. در `run-standalone.mjs` (Docker CMD)، یک مقدار **صریح** بهشکل `--max-old-space-size` افزوده میشود و بر فلگ متعارض heap در NODE_OPTIONS **اولویت دارد** (آخرین فلگ V8). دستور `omniroute serve` همچنان heap موجود در NODE_OPTIONS را ترجیح میدهد (#5238). هر دو را با اعداد متفاوت تنظیم نکنید — فرایند هشداری ثبت میکند که هر دو مقدار و مقدار برنده را نام میبرد. **ایمیج رسمی Docker همیشه `1024` را تنظیم میکند، بنابراین کالیبراسیون هرگز در آن اجرا نمیشود.** عامل کدنویسی `/v1/responses` به `8192` تا `12288` بهعلاوهٔ فضای اضافی cgroup نیاز دارد — به [راهنمای Docker — RAM زمان اجرا](../guides/DOCKER_GUIDE.md#runtime-ram-for-coding-agents) مراجعه کنید. |
| `PROMPT_CACHE_MAX_SIZE`       | `50`                                                       | حداکثر تعداد ورودیهای کششدهٔ پرامپت سیستمی.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `PROMPT_CACHE_MAX_BYTES`      | `2097152` (2 MB)                                           | حداکثر اندازهٔ کل کش پرامپت.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `PROMPT_CACHE_TTL_MS`         | `300000` (۵ دقیقه)                                         | TTL ورودی کش پرامپت.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `SEMANTIC_CACHE_MAX_SIZE`     | `100`                                                      | حداکثر تعداد پاسخهای کششده با temperature=0.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `SEMANTIC_CACHE_MAX_BYTES`    | `4194304` (4 MB)                                           | حداکثر اندازهٔ کل کش معنایی.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `SEMANTIC_CACHE_TTL_MS`       | `1800000` (۳۰ دقیقه)                                       | TTL ورودی کش معنایی.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `OMNIROUTE_CORPUS_CACHE_SIZE` | `5`                                                        | ریشههای پیکرهٔ محلی که بهطور همزمان یک ایندکس فعال در حافظه نگه میدارند (`src/lib/localCorpus/configured.ts`). LRU: با رسیدن به حد، ایندکس ریشهای که کمترین استفادهٔ اخیر را داشته است خارج میشود و در پرسوجوی بعدی آن دوباره ساخته میشود. حداقل مقدار آن `1` است؛ مقدار غیرعددی به مقدار پیشفرض بازمیگردد.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `STREAM_HISTORY_MAX`          | `50`                                                       | حداکثر تعداد رویدادهای اخیر جریان در بافر نمای زندهٔ داشبورد.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `CONTEXT_LENGTH_DEFAULT`      | `128000`                                                   | حداکثر طول زمینهٔ جایگزین سراسری برای مدلهای بدون پیکربندی صریح.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `USAGE_TOKEN_BUFFER`          | `100`                                                      | حاشیهٔ اضافی توکن که هنگام ردیابی سهمیههای مصرف رزرو میشود.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |

### فشردهسازی

| متغیر                                 | پیشفرض    | توضیحات                                                                                                  |
| ------------------------------------- | --------- | -------------------------------------------------------------------------------------------------------- |
| `OMNIROUTE_RTK_TRUST_PROJECT_FILTERS` | تنظیمنشده | اعتماد به `.rtk/filters.json` پروژه بدون هش `.rtk/trust.json`. فقط در توسعهٔ محلی کنترلشده استفاده کنید. |

### موتور حافظه (طرح 21)

### هزینهٔ حلقهٔ رویداد برای حافظه، مهارتها و نوسازی توکن (#10349)

OmniRoute یک **فرایند واحد Node** است. استخراج/بازیابی حافظه، تزریق مهارتها و نوسازی توکن ارائهدهنده در **همان حلقهٔ رویداد** مربوط به `GET /healthz` و داشبورد اجرا میشوند. آنها worker thread نیستند.

| کار                     | کد                                 | پیشفرض                                           | کنترل اپراتور                                                                                                                              |
| ----------------------- | ---------------------------------- | ------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------ |
| استخراج / بازیابی حافظه | `src/lib/memory/`                  | **memoryEnabled** در داشبورد (بهطور پیشفرض روشن) | **Settings → Memory** را خاموش کنید. بهجز غیرفعالکردن این قابلیت در تنظیمات، هیچ کلید توقف اضطراری مجزایی در متغیرهای محیطی وجود ندارد.    |
| تزریق مهارتها           | `src/lib/skills/injection.ts`      | **skillsEnabled** در داشبورد (بهطور پیشفرض روشن) | **Settings → Memory/Skills** (`skillsEnabled`) را خاموش کنید. گزینههای سندباکس در ادامه فقط پس از فعالشدن تزریق، اجرای آن را محدود میکنند. |
| نوسازی توکن             | `src/sse/services/tokenRefresh.ts` | برای ارائهدهندگان متصل OAuth/وب روشن است         | اتصال ارائهدهنده را قطع کنید یا اجازه دهید توکنها معتبر بمانند؛ در حال حاضر هیچ متغیر محیطی `TOKEN_REFRESH=0` وجود ندارد.                  |

اگر `/healthz` روی سیستمی کمبار کند است، ابتدا حافظه و مهارتها را غیرفعال کنید، سپس بار کاتالوگ/فشردهسازی را بررسی کنید (#10303، #9685). این قابلیتها در نقاط `await` کنترل را واگذار میکنند، اما همچنان برای همان یک رشته رقابت میکنند.

گزینههای تنظیمی لایه تعبیهسازی، فروشگاه برداری و رتبهبندی مجدد برای زیرسامانه حافظه ماندگار (`src/lib/memory/`).

| متغیر                                | پیشفرض                      | توضیحات                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| ------------------------------------ | --------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `MEMORY_EMBEDDING_CACHE_TTL_MS`      | `300000` (۵ دقیقه)          | TTL برای کش درونحافظهای تعبیهها (بهازای هر امضای منبع/مدل/بُعد).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `MEMORY_EMBEDDING_CACHE_MAX`         | `1000`                      | حداکثر تعداد ورودیهای LRU نگهداریشده در کش تعبیهها.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `MEMORY_TRANSFORMERS_MODEL`          | `Xenova/all-MiniLM-L6-v2`   | شناسه مخزن HF برای خط لوله محلی و اختیاری MiniLM مبتنی بر `@huggingface/transformers` (حدود ۲۳ مگابایت int8 و حدود ۴۰۰ مگابایت RAM).                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `MEMORY_STATIC_MODEL`                | `minishlab/potion-base-8M`  | شناسه مخزن HF برای تعبیهساز جدول جستوجوی ایستای potion/Model2Vec. در صورت نیاز و با تأخیر در پوشه کش دانلود میشود.                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `MEMORY_STATIC_CACHE_DIR`            | `<DATA_DIR>/embeddings`     | پوشه مورداستفاده برای کشکردن فایلهای مدل ایستای potion. در صورت تنظیمنشدن، بهطور پیشفرض زیر `DATA_DIR` قرار میگیرد.                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `HF_HUB_ENDPOINT`                    | `https://huggingface.co`    | بازنویسی URL پایه Hugging Face Hub که `staticPotion.ts` از آن استفاده میکند (برای مثال، نقطه پایانی آینه در محیطهای جدا از شبکه).                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `MEMORY_VEC_TOP_K`                   | `20`                        | مقدار پیشفرض top-K که جستوجوی برداری brute-force در `sqlite-vec` داخل `src/lib/memory/vectorStore.ts` از آن استفاده میکند.                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `MEMORY_RRF_K`                       | `60`                        | ثابت `k` در Reciprocal Rank Fusion برای بازیابی ترکیبی FTS5 و برداری (دستورالعمل sqlite-vec).                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `VECTOR_STORE_DISABLE_VEC`           | `false`                     | نقطه اتصال آزمایشی/تشخیصی در `getVectorStore()` (`src/lib/memory/vectorStore.ts`): وقتی `true` باشد، فروشگاه برداری را بهاجبار روی `null` قرار میدهد (محیط ابری/WASM فاقد `sqlite-vec` را شبیهسازی میکند) و بازیابی حافظه را به جستوجوی کلیدواژهای FTS5 تنزل میدهد. در محیط عملیاتی تنظیمنشده باقی بماند.                                                                                                                                                                                                                                                      |
| `NOTION_API_KEY`                     | _(تنظیمنشده)_               | کلید API برای بکاند Notion (مورداستفاده توسط پیشتنظیم بکاند شناختهشده `genericBackend.ts`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `NOTION_API_URL`                     | `https://api.notion.com/v1` | URL پایه برای API مربوط به Notion (برای جایگزینهای خودمیزبان Notion قابل بازنویسی است).                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `OBSIDIAN_API_KEY`                   | _(تنظیمنشده)_               | کلید API برای بکاند Obsidian Vault (مورداستفاده توسط پیشتنظیم بکاند شناختهشده `genericBackend.ts`).                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `OBSIDIAN_API_URL`                   | `http://localhost:27123`    | URL پایه برای API مربوط به Obsidian Vault (برای vault راهدور قابل بازنویسی است).                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `MEMORY_TYPED_DECAY_ENABLED`         | `false`                     | کلید اصلی زوال حافظه نوعدار TV6. **انتخابی (بهطور پیشفرض خاموش)** — پیمایش، حافظههای زوالیافته را **حذف میکند**. وقتی خاموش باشد، `access_count`/`last_accessed_at` صرفاً دادههای تلهمتری هستند و هیچچیز هرگز حذف نمیشود.                                                                                                                                                                                                                                                                                                                                      |
| `MEMORY_TYPED_DECAY_EPISODIC_DAYS`   | `30`                        | TTL (برحسب روز) که پس از آن یک حافظه `episodic` استفادهنشده زوال مییابد. مقدار `0` حافظه episodic را نیز مصون میکند. انواع ماندگار (`factual`/`procedural`/`semantic`) همیشه مصون هستند. ساعت زوال بر اساس `last_accessed_at` از نو مبناگذاری میشود.                                                                                                                                                                                                                                                                                                           |
| `MEMORY_TYPED_DECAY_ACCESS_IMMUNITY` | `3`                         | حافظهای که دستکم `>=` این تعداد بار تزریق شده باشد، صرفنظر از نوع آن در برابر زوال مصون میشود. مقدار `0` مصونیت مبتنی بر دسترسی را غیرفعال میکند.                                                                                                                                                                                                                                                                                                                                                                                                              |
| `MEMORY_TYPED_DECAY_SWEEP_INTERVAL`  | `0` (غیرفعال)               | بازه زمانی (برحسب ثانیه) برای پیمایش دورهای و اختیاری زوال در `src/lib/memory/typedDecay.ts`. مقدار `0`/تنظیمنشده = بدون پیمایش دورهای. نیازمند فعالسازی دوگانه است: `MEMORY_TYPED_DECAY_ENABLED=true` نیز باید تنظیم شود.                                                                                                                                                                                                                                                                                                                                     |
| `OMNIROUTE_STRICT_SYSTEM_PROVIDERS`  | _(تنظیمنشده)_               | شناسههای ارائهدهندگان جداشده با ویرگول (بدون حساسیت به بزرگی و کوچکی حروف) که پیام `system` را **فقط در اندیس 0** میپذیرند (`src/lib/memory/injection.ts`). برای این ارائهدهندگان، درج حافظه در میانه آرایه بهشکل سازگار با کش، در مکالمات چندمرحلهای ناامن است؛ بنابراین حافظه با پیام system ابتدایی ادغام یا به ابتدای آن افزوده میشود. مقدار پیشفرض فقط `xiaomi-mimo`/`mimo` است؛ برای نقاط پایانی خودمیزبان سازگار با OpenAI (برای مثال Qwen3.5+/3.6) که الگوی گفتوگوی آنها همین محدودیتِ یک پیام system ابتدایی را اعمال میکند، این فهرست را گسترش دهید. |

### نمونه Docker با RAM کم

`128` فقط برای داشبورد مناسب است. عاملهای کدنویسی با این اندازه heap هنگام اجرای طولانیمدت `/v1/responses` با `FATAL ERROR` مواجه میشوند. از این نمونه بهعنوان دروازه Claude/Codex/Grok استفاده نکنید.

```bash
OMNIROUTE_MEMORY_MB=128
PROMPT_CACHE_MAX_SIZE=20
PROMPT_CACHE_MAX_BYTES=524288        # ۵۱۲ کیلوبایت
SEMANTIC_CACHE_MAX_SIZE=25
SEMANTIC_CACHE_MAX_BYTES=1048576     # ۱ مگابایت
STREAM_HISTORY_MAX=10
```

---

## 18. همگامسازی قیمتگذاری

همگامسازی خودکار دادههای قیمتگذاری مدل از منابع خارجی.

| متغیر                   | پیشفرض        | فایل منبع                | توضیحات                               |
| ----------------------- | ------------- | ------------------------ | ------------------------------------- |
| `PRICING_SYNC_ENABLED`  | `false`       | `src/lib/pricingSync.ts` | همگامسازی دورهای و اختیاری قیمتگذاری. |
| `PRICING_SYNC_INTERVAL` | `86400` (24h) | `src/lib/pricingSync.ts` | فاصلهٔ زمانی همگامسازی برحسب ثانیه.   |
| `PRICING_SYNC_SOURCES`  | `litellm`     | `src/lib/pricingSync.ts` | منابع داده با جداکنندهٔ ویرگول.       |

---

## همگامسازی ELO در Arena

| متغیر                        | پیشفرض        | فایل منبع                                                                            | توضیحات                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ---------------------------- | ------------- | ------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `ARENA_ELO_SYNC_ENABLED`     | `true`        | `src/shared/constants/featureFlagDefinitions.ts`                                     | همگامسازی دورهای ELO جدول رتبهبندی Arena AI که از طریق پرچمهای قابلیت داشبورد قابل پیکربندی است؛ برای انصراف، آن را روی `false` تنظیم کنید.                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `MODELS_CATALOG_PREFIX_MODE` | `dual`        | `src/shared/constants/featureFlagDefinitions.ts`, `src/app/api/v1/models/catalog.ts` | شکل پیشوند استفادهشده برای شناسههای مدل در `GET /v1/models`. مقدار `dual` برای هر مدل، هم پیشوند نام مستعار کوتاه و هم پیشوند رسمی ارائهدهنده را اعلام میکند (برای سازگاری با نسخههای پیشین — اندازهٔ کاتالوگ را تقریباً دو برابر میکند)؛ `alias` برای هر مدل یک شناسه منتشر میکند؛ `canonical` فقط پیشوند کامل شناسهٔ ارائهدهنده را منتشر میکند (ارائهدهندگانی که نام مستعارشان از قبل همان شناسهٔ رسمی است، تنها ورودی خود را حفظ میکنند). کلاینتها میتوانند این مقدار را در هر درخواست با `?prefix=alias` بازنویسی کنند. به [API_REFERENCE](API_REFERENCE.md#model-id-prefixes-prefix) مراجعه کنید. |
| `ARENA_ELO_SYNC_INTERVAL`    | `86400` (24h) | `src/lib/arenaEloSync.ts`                                                            | فاصلهٔ زمانی همگامسازی برحسب ثانیه.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |

---

## ارائهدهندهٔ محیط آزمایشی PromptQL (غیررسمی/آزمایشی)

پل نشست GraphQL مهندسی معکوسشده برای prompt.ql.app (`src/shared/constants/providers/web-cookie.ts`). همهٔ گزینهها اختیاری هستند — مقادیر پیشفرض به نقاط پایانی عمومی محیط آزمایشی اشاره میکنند؛ فقط برای استقرار خودمیزبان/جایگزین PromptQL آنها را بازنویسی کنید.

| متغیر                        | پیشفرض                                                             | فایل منبع                                                               | توضیحات                                                           |
| ---------------------------- | ------------------------------------------------------------------ | ----------------------------------------------------------------------- | ----------------------------------------------------------------- |
| `PROMPTQL_GRAPHQL_ENDPOINT`  | `https://data.prompt.ql.app/promptql/playground-v2-hge/v1/graphql` | `open-sse/executors/promptql.ts`                                        | نقطهٔ پایانی GraphQL مورداستفاده برای عملیات چت/نشست.             |
| `PROMPTQL_CREDITS_ENDPOINT`  | `https://data.pro.ql.app/v1/graphql`                               | `open-sse/executors/promptql.ts`, `open-sse/services/usage/promptql.ts` | نقطهٔ پایانی GraphQL مورداستفاده برای استعلام موجودی/مصرف اعتبار. |
| `PROMPTQL_TOKEN_REFRESH_URL` | `https://auth.pro.ql.app/ddn/project/token`                        | `open-sse/executors/promptql.ts`                                        | نقطهٔ پایانی مورداستفاده برای نوسازی توکن بهصورت بهترین تلاش.     |
| `PROMPTQL_POLL_TIMEOUT_MS`   | `180000`                                                           | `open-sse/executors/promptql.ts`                                        | حداکثر زمان (ms) برای نظرسنجی `thread_events` پیش از پایان مهلت.  |

---

## ارائهدهنده وب HyperAgent (غیررسمی/آزمایشی)

پل نشست مهندسی معکوسشده برای hyperagent.com (`src/shared/constants/providers/web-cookie.ts`). اختیاری است — مقدار پیشفرض به نقطه پایانی عمومی صورتحساب/مصرف اشاره میکند؛ فقط برای استقرار خودمیزبان/جایگزین HyperAgent آن را بازنویسی کنید.

| متغیر                  | پیشفرض                                              | فایل منبع                               | توضیحات                                                           |
| ---------------------- | --------------------------------------------------- | --------------------------------------- | ----------------------------------------------------------------- |
| `HYPERAGENT_USAGE_URL` | `https://hyperagent.com/api/settings/billing/usage` | `open-sse/services/usage/hyperagent.ts` | نقطه پایانی مورداستفاده برای دریافت بلوکهای اعتبار صورتحساب/مصرف. |

---

## سهمیههای مصرف Kilo Code

استعلام موجودی شخصی برحسب USD و میزان مصرف Kilo Pass برای ارائهدهنده Kilo Code. اختیاری است — مقدار پیشفرض به API عمومی Kilo اشاره میکند؛ فقط برای یک رله/فیکسچر آزمایشی آن را بازنویسی کنید. احراز هویت از توکن دسترسی OAuth موجود اتصال استفاده میکند.

| متغیر          | پیشفرض                | فایل منبع                             | توضیحات                                                                        |
| -------------- | --------------------- | ------------------------------------- | ------------------------------------------------------------------------------ |
| `KILO_API_URL` | `https://api.kilo.ai` | `open-sse/services/usage/kilocode.ts` | URL پایه مورداستفاده برای دریافت موجودی شخصی Kilo Code و میزان مصرف Kilo Pass. |

---

## ارائهدهنده وب Adobe Firefly (غیررسمی/آزمایشی)

تازهسازی نشست مبتنی بر مرورگر برای ارائهدهنده وب Adobe Firefly
(`open-sse/services/adobeFireflyBrowserLogin.ts`، `open-sse/services/adobeFireflySession.ts`،
`open-sse/services/adobeFireflyClient.ts`). اختیاری است — همه مقادیر پیشفرض برای یک نصب معمولی
رومیزی تنظیم شدهاند.

> **در #9255 حذف شد.** زماناجرای قدیمی Chrome متصل به CDP (adobeFireflyChromeRuntime.ts)
> با یک سرویس ورود مرورگری Playwright جایگزین شد و گزینههای تنظیم آن دیگر وجود ندارند. متغیرهای
> ADOBE_FIREFLY_CHROME_ CDP_PORT / VISIBLE / HEADED / PING / FORCE_RESTART، بهعلاوه
> ADOBE_FIREFLY_LOGIN_WAIT_MS و ADOBE_FIREFLY_FORTER_WAIT_MS، در هیچ بخشی از
> پایگاه کد خوانده نمیشوند — تنظیم آنها هیچ اثری ندارد.

| متغیر                                | پیشفرض        | فایل منبع                                       | توضیحات                                                                                                                                    |
| ------------------------------------ | ------------- | ----------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------ |
| `ADOBE_FIREFLY_CHROME_HEADLESS`      | `0`           | `open-sse/services/adobeFireflyBrowserLogin.ts` | برای Chrome واقعاً بدون رابط گرافیکی روی `1` تنظیم کنید (برای تولید دارای مشکل شناختهشده است؛ فقط برای اشکالزدایی).                        |
| `ADOBE_FIREFLY_BROWSER_REFRESH`      | `1`           | `open-sse/services/adobeFireflySession.ts`      | فعال/غیرفعالسازی پیشگرمسازی پیشدستانه مرورگر. `0` پیشگرمسازی پیشدستانه را غیرفعال میکند (بازیابی از 408 در میانه دسته همچنان اعمال میشود). |
| `ADOBE_FIREFLY_SESSION_DISK`         | `1`           | `open-sse/services/adobeFireflySession.ts`      | برای غیرفعال کردن ذخیره پایدار نشست Adobe Firefly روی دیسک، روی `0` تنظیم کنید.                                                            |
| `ADOBE_FIREFLY_MIN_SUBMIT_GAP_MS`    | _(تنظیمنشده)_ | `open-sse/services/adobeFireflySession.ts`      | حداقل فاصله (ms) اجباری بین ارسالهای متوالی که مقدار پیشفرض داخلی را بازنویسی میکند.                                                       |
| `ADOBE_FIREFLY_BATCH_EXTRA_GAP_MS`   | _(تنظیمنشده)_ | `open-sse/services/adobeFireflySession.ts`      | فاصله اضافی (ms) که پس از یک دسته موفق افزوده میشود و مقدار پیشفرض داخلی را بازنویسی میکند.                                                |
| `ADOBE_FIREFLY_SUBMIT_BASE_DELAY_MS` | _(تنظیمنشده)_ | `open-sse/services/adobeFireflyClient.ts`       | تأخیر پایه (ms) پیش از ارسال درخواست تولید که مقدار پیشفرض داخلی را بازنویسی میکند.                                                        |

---

## 19. همگامسازی مدل (توسعه)

| متغیر                               | پیشفرض            | فایل منبع                          | توضیحات                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| ----------------------------------- | ----------------- | ---------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `MODELS_DEV_SYNC_ENABLED`           | _(تنظیمنشده)_     | `src/lib/modelsDevSync.ts`         | بازنویسی قطعی برای همگامسازی قیمتگذاری models.dev. تنظیمنشده = پیروی از تنظیمات > هوش مصنوعی (`modelsDevSyncEnabled`). مقادیر `0`/`false`/`off`/`no` **بر پایگاه داده اولویت دارند** و هم همگامسازی دورهای و هم اسکنهای SQL/JSON تابع `getModelsDevPricing()` را رد میکنند (برای بازیابی زمانی که داشبورد در همان حلقه رویداد گیر کرده است). مقادیر `1`/`true`/`on`/`yes` همگامسازی را بهاجبار فعال میکنند. ذخیره/پاکسازی قیمتگذاری همچنان `backupDbFile("pre-write")` را فراخوانی میکند که تحت محدودسازی ۶۰ دقیقهای یا `DISABLE_SQLITE_AUTO_BACKUP` هیچ عملی انجام نمیدهد. |
| `MODELS_DEV_SYNC_INTERVAL`          | `86400` (۲۴ ساعت) | `src/lib/modelsDevSync.ts`         | فاصله زمانی همگامسازی کاتالوگ مدل در زمان توسعه، بر حسب ثانیه.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `CONTEXT_WINDOW_RECONCILE_INTERVAL` | `86400` (۲۴ ساعت) | `src/lib/contextWindowResolver.ts` | فاصله زمانی (بر حسب ثانیه) برای تطبیقدهنده خوداصلاحگر پنجره زمینه (5004): وقتی پنجرههای اعلامشده توسط ارائهدهنده از طریق کشف `/models` با کاتالوگ تفاوت داشته باشند، آنها را بهعنوان بازنویسیهای `auto:discovery` تثبیت میکند. برای غیرفعالسازی روی `0` تنظیم کنید. از دادههای ازپیشهمگامشده دوباره استفاده میکند (بدون واکشی جدید)؛ هرگز بازنویسیهای `manual` را جایگزین نمیکند.                                                                                                                                                                                           |

---

## 20. تنظیمات مختص ارائهدهنده

| متغیر                                          | پیشفرض                                 | فایل منبع                                                                          | توضیحات                                                                                                                                                                                                                                                                                                                                           |
| ---------------------------------------------- | -------------------------------------- | ---------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `OPENROUTER_CATALOG_TTL_MS`                    | `86400000` (24 ساعت)                   | `src/lib/catalog/openrouterCatalog.ts`                                             | TTL کش کاتالوگ مدلهای OpenRouter.                                                                                                                                                                                                                                                                                                                 |
| `MODEL_CATALOG_INCLUDE_NAMES`                  | `true`                                 | `src/shared/constants/featureFlagDefinitions.ts`                                   | فیلدهای نمایشی و خوانای `name` را در پاسخهای `/v1/models` لحاظ میکند. برای کلاینتهایی که فقط انتظار شناسه دارند، غیرفعال کنید.                                                                                                                                                                                                                    |
| `CATALOG_BUILD_TIMEOUT_MS`                     | `8000` (8 ثانیه)                       | `src/app/api/v1/models/catalogCache.ts`                                            | حداکثر زمان انتظار در مسیر سرد برای بازسازی تجمیعشدهٔ کاتالوگ با `GET /v1/models` (#12627). در صورت اتمام مهلت، اگر آخرین پاسخ سالم 200 موجود باشد، همان ارائه میشود.                                                                                                                                                                             |
| `OMNIROUTE_SYNCED_CATALOG_STALE_AFTER_MS`      | `2592000000` (30 روز)                  | `src/lib/db/models/activeSyncedCatalog.ts`                                         | مدتزمانی که پس از آن، فهرست مدل همگامسازیشدهٔ یک اتصال دیگر برای مسیریابی معتبر نیست و بهصورت fail-open از رجیستری استفاده میشود (#12849). ردیفهایی که هرگز برچسب زمانی نخوردهاند، منقضی محسوب میشوند.                                                                                                                                            |
| `NANOBANANA_POLL_TIMEOUT_MS`                   | `120000`                               | `open-sse/handlers/imageGeneration.ts`                                             | حداکثر زمان انتظار برای کارهای تولید تصویر NanoBanana.                                                                                                                                                                                                                                                                                            |
| `NANOBANANA_POLL_INTERVAL_MS`                  | `2500`                                 | `open-sse/handlers/imageGeneration.ts`                                             | بسامد نظرسنجی وضعیت کار NanoBanana.                                                                                                                                                                                                                                                                                                               |
| `ADOBE_FIREFLY_SUBMIT_BASE_DELAY_MS`           | `8000`                                 | `open-sse/services/adobeFireflyUpscale.ts`                                         | تأخیر پایه برای عقبنشینی نماییِ تلاش مجدد جهت ثبت بزرگنمایی Adobe Firefly.                                                                                                                                                                                                                                                                        |
| `AWS_REGION`                                   | _(تنظیمنشده)_                          | `src/lib/providers/validation.ts`, `open-sse/handlers/audioSpeech.ts`              | منطقهٔ مورد استفاده برای ساخت نقاط پایانی AWS Bedrock ‏(Kiro، صوت).                                                                                                                                                                                                                                                                               |
| `AWS_DEFAULT_REGION`                           | _(تنظیمنشده)_                          | `src/lib/providers/validation.ts`, `open-sse/handlers/audioSpeech.ts`              | مقدار جایگزین هنگامی که `AWS_REGION` تنظیم نشده باشد.                                                                                                                                                                                                                                                                                             |
| `CLOUDFLARE_ACCOUNT_ID`                        | _(تنظیمنشده)_                          | `open-sse/executors/cloudflare-ai.ts`                                              | شناسهٔ حساب برای Cloudflare Workers AI.                                                                                                                                                                                                                                                                                                           |
| `CLOUDFLARE_PLAYGROUND_CHROME_PATH`            | _(تنظیمنشده)_                          | `open-sse/executors/cloudflare-playground.ts`                                      | مسیر کامل فایل اجرایی نسخهٔ دسکتاپ Chrome برای اجراکنندهٔ Cloudflare AI Playground؛ زمانی استفاده میشود که بررسی اثرانگشت حالت headless، نسخهٔ Chromium همراه Playwright را مسدود کند.                                                                                                                                                            |
| `CLOUDFLARE_API_BASE`                          | `https://api.cloudflare.com/client/v4` | `src/app/api/settings/proxy/cloudflare-deploy/route.ts`                            | جایگزینی نشانی پایهٔ REST API مربوط به Cloudflare که توسط استقراردهندهٔ رلهٔ Workers در مجموعهٔ پروکسی استفاده میشود (#4640 / 9router#1360).                                                                                                                                                                                                      |
| `NEXT_PUBLIC_CLOUDFLARE_RELAY_DEFAULT_PROJECT` | `omniroute-relay`                      | `src/app/(dashboard)/dashboard/settings/components/proxy/CloudflareRelayModal.tsx` | نام پیشفرض پروژهٔ worker که در پنجرهٔ «استقرار رله» مجموعهٔ پروکسی پیشنهاد میشود.                                                                                                                                                                                                                                                                 |
| `NEXT_PUBLIC_CLOUDFLARE_RELAY_ENABLED`         | `true`                                 | `src/app/(dashboard)/dashboard/settings/components/proxy/ProxyPoolTab.tsx`         | برای پنهانکردن گزینهٔ رلهٔ Cloudflare Workers از زبانهٔ مجموعهٔ پروکسی، روی `false` تنظیم کنید.                                                                                                                                                                                                                                                   |
| `CLOUDFLARED_BIN`                              | تشخیص خودکار                           | `src/lib/cloudflaredTunnel.ts`                                                     | مسیر سفارشی فایل اجرایی `cloudflared`.                                                                                                                                                                                                                                                                                                            |
| `CLOUDFLARED_PROTOCOL`                         | `http2`                                | `src/lib/cloudflaredTunnel.ts`                                                     | پروتکل انتقال تونل: `http2` (پیشفرض)، `quic` یا `auto`.                                                                                                                                                                                                                                                                                           |
| `CLOUDFLARED_CONFIG`                           | _(تنظیمنشده)_                          | `src/lib/cloudflaredTunnel.ts`                                                     | مسیر فایل `config.yml` مدیریتشده بهصورت محلی برای cloudflared (شامل `tunnel:`، `credentials-file:` و `ingress:`). در صورت تنظیم، OmniRoute بهجای یک تونل سریع موقت، دستور `tunnel --config <path> run` را برای تونل نامگذاریشده اجرا میکند.                                                                                                       |
| `CLOUDFLARED_HOSTNAME`                         | _(از ingress پیکربندی)_                | `src/lib/cloudflaredTunnel.ts`                                                     | مقدار جایگزین برای نام میزبان عمومی تونل نامگذاریشده (برای مثال `ai.example.com`) که بهصورت `publicUrl`/`apiUrl` گزارش میشود. اگر تنظیم نشده باشد، از نخستین نام میزبان `ingress` در پیکربندی خوانده میشود.                                                                                                                                       |
| `DENO_DEPLOY_API_BASE`                         | `https://api.deno.com/v2`              | `src/app/api/settings/proxy/deno-deploy/route.ts`                                  | جایگزینی نشانی پایهٔ REST API مربوط به Deno Deploy که توسط استقراردهندهٔ رلهٔ مجموعهٔ پروکسی استفاده میشود (#4643 / 9router#1437).                                                                                                                                                                                                                |
| `NEXT_PUBLIC_DENO_RELAY_DEFAULT_PROJECT`       | `omniroute-deno-relay`                 | `src/app/(dashboard)/dashboard/settings/components/proxy/DenoRelayModal.tsx`       | نام پیشفرض برنامهٔ Deno Deploy که در پنجرهٔ «استقرار رله» مجموعهٔ پروکسی پیشنهاد میشود.                                                                                                                                                                                                                                                           |
| `NEXT_PUBLIC_DENO_RELAY_ENABLED`               | `true`                                 | `src/app/(dashboard)/dashboard/settings/components/proxy/ProxyPoolTab.tsx`         | برای پنهانکردن گزینهٔ رلهٔ Deno Deploy از زبانهٔ مجموعهٔ پروکسی، روی `false` تنظیم کنید.                                                                                                                                                                                                                                                          |
| `SEARCH_CACHE_TTL_MS`                          | `300000` (5 دقیقه)                     | `open-sse/services/searchCache.ts`                                                 | TTL ذخیرهسازی پاسخهای API جستوجو (Perplexity، Brave و غیره) در کش.                                                                                                                                                                                                                                                                                |
| `ENABLE_CC_COMPATIBLE_PROVIDER`                | `false`                                | `src/shared/utils/featureFlags.ts`                                                 | رابط کاربری آزمایشی ارائهدهندهٔ سازگار با CC را برای رلههای مختص Claude Code نمایش میدهد.                                                                                                                                                                                                                                                         |
| `NINEROUTER_HOST`                              | `127.0.0.1`                            | `open-sse/executors/ninerouter.ts`                                                 | جایگزینی میزبان محل شنود نمونهٔ تعبیهشدهٔ 9router.                                                                                                                                                                                                                                                                                                |
| `NINEROUTER_PORT`                              | `20130`                                | `open-sse/executors/ninerouter.ts`                                                 | جایگزینی درگاه محل شنود نمونهٔ تعبیهشدهٔ 9router.                                                                                                                                                                                                                                                                                                 |
| `EMBED_WS_PROXY_HOST`                          | `127.0.0.1`                            | `src/lib/services/embedWsProxy.ts`                                                 | میزبان اتصال برای پروکسی WebSocket سرویس تعبیهشده (بهطور پیشفرض فقط loopback).                                                                                                                                                                                                                                                                    |
| `EMBED_WS_PROXY_PORT`                          | `20131`                                | `src/lib/services/embedWsProxy.ts`                                                 | درگاه سرور پروکسی WebSocket سرویس تعبیهشده.                                                                                                                                                                                                                                                                                                       |
| `CLIPROXYAPI_HOST`                             | `127.0.0.1`                            | `open-sse/executors/cliproxyapi.ts`                                                | میزبان پل CLIProxyAPI (یکپارچهسازی قدیمی).                                                                                                                                                                                                                                                                                                        |
| `CLIPROXYAPI_PORT`                             | `5544`                                 | `open-sse/executors/cliproxyapi.ts`                                                | درگاه پل CLIProxyAPI.                                                                                                                                                                                                                                                                                                                             |
| `CLIPROXYAPI_API_KEY`                          | _(خالی)_                               | `open-sse/handlers/chatCore/cliproxyapiCredentials.ts`                             | کلید جایگزین صفحهٔ داده در صورت نبود تنظیم `cliproxyapi_api_key`.                                                                                                                                                                                                                                                                                 |
| `CLIPROXYAPI_MANAGEMENT_KEY`                   | _(خالی)_                               | `src/lib/services/cliproxyAccountHealth.ts`                                        | کلید مدیریتی برای خواندن سلامت حساب از یک نمونهٔ CLIProxyAPI با مدیریت خارجی.                                                                                                                                                                                                                                                                     |
| `CLIPROXYAPI_CONFIG_DIR`                       | `~/.cli-proxy-api`                     | `src/lib/versionManager/processManager.ts`                                         | پوشهٔ پیکربندی CLIProxyAPI.                                                                                                                                                                                                                                                                                                                       |
| `CLIPROXY_BIND_HOST`                           | `127.0.0.1`                            | `docker-compose.yml`                                                               | رابط میزبانی که docker-compose سرویس جانبی `cliproxyapi` را روی آن منتشر میکند (#12578). حجم دادهٔ آن حاوی اعتبارنامههای OAuth/API ارائهدهنده است و ایمیج سنجاقشده فاقد امکان جایگزینی `api-keys` صفحهٔ داده از طریق متغیر محیطی است (فقط یک `config.yaml` سوارشده دارد)، بنابراین `0.0.0.0` سرویسی حاوی اعتبارنامه را در معرض کل LAN قرار میدهد. |
| `MUX_SERVICE_PORT`                             | `8322`                                 | `src/lib/services/bootstrap.ts`                                                    | جایگزینی درگاه محل شنود daemon تعبیهشدهٔ هماهنگسازی عامل Mux ‏(coder/mux) که همیشه `127.0.0.1` است.                                                                                                                                                                                                                                               |
| `DARIO_HOST`                                   | `127.0.0.1`                            | `open-sse/executors/dario.ts`                                                      | میزبان اتصال/برقراری ارتباط سرویس تعبیهشدهٔ Dario (بهطور پیشفرض فقط loopback).                                                                                                                                                                                                                                                                    |
| `DARIO_PORT`                                   | `3456`                                 | `open-sse/executors/dario.ts`                                                      | درگاه سرویس تعبیهشدهٔ Dario.                                                                                                                                                                                                                                                                                                                      |
| `DARIO_HOST`                                   | `127.0.0.1`                            | `open-sse/executors/dario.ts`                                                      | میزبان اتصال/برقراری ارتباط سرویس تعبیهشدهٔ Dario (بهطور پیشفرض فقط loopback).                                                                                                                                                                                                                                                                    |
| `DARIO_PORT`                                   | `3456`                                 | `open-sse/executors/dario.ts`                                                      | درگاه سرویس تعبیهشدهٔ Dario.                                                                                                                                                                                                                                                                                                                      |
| `LOCAL_HOSTNAMES`                              | _(خالی)_                               | `open-sse/config/providerRegistry.ts`                                              | نامهای میزبان اضافی جداشده با ویرگول که «محلی» تلقی میشوند (نام سرویسهای Docker و غیره).                                                                                                                                                                                                                                                          |

`ENABLE_CC_COMPATIBLE_PROVIDER` فقط برای رلههای شخص ثالثی است که منحصراً کلاینتهای Claude Code را میپذیرند. OmniRoute درخواستها را بازنویسی میکند تا آن رلهها آنها را بپذیرند. اگر فقط میخواهید از Claude Code CLI استفاده کنید، یا مطمئن نیستید این رلهها چه هستند، این گزینه را غیرفعال نگه دارید و در عوض یک ارائهدهنده معمولی سازگار با Anthropic اضافه کنید.

---

## 21. سلامت پراکسی

| متغیر                                             | مقدار پیشفرض             | فایل منبع                                                                                        | توضیحات                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| ------------------------------------------------- | ------------------------ | ------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `PROXY_FAST_FAIL_TIMEOUT_MS`                      | `2000`                   | `src/lib/proxyHealth.ts`                                                                         | مهلت زمانی بررسی سلامت برای شکست سریع.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `PROXY_LATENCY_WINDOW_HOURS`                      | `3`                      | `src/lib/db/proxies.ts`                                                                          | بازه زمانی (برحسب ساعت) برای محاسبه میانگین تأخیر پراکسیهای نامزد در راهبرد مجموعه بهینهشده برای تأخیر.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `PROXY_HEALTH_CACHE_TTL_MS`                       | `30000`                  | `src/lib/proxyHealth.ts`                                                                         | TTL حافظه پنهان نتیجه بررسی سلامت.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `PROXY_HEALTH_UNHEALTHY_CACHE_TTL_MS`             | `2000`                   | `src/lib/proxyHealth.ts`                                                                         | TTL حافظه پنهان برای کاوشهای ناموفق سلامت پراکسی. این مقدار را کوتاهتر از `PROXY_HEALTH_CACHE_TTL_MS` نگه دارید تا در صورت بروز مهلتهای زمانی گذرای پراکسی تحت همزمانی بالا، تلاش مجدد بهسرعت انجام شود، بدون اینکه شکست سریع برای پراکسیهای واقعاً ازکارافتاده غیرفعال شود.                                                                                                                                                                                                                                                                                                                                                                                             |
| `PROXY_HEALTH_ENABLED`                            | `true`                   | `src/lib/proxyHealth/scheduler.ts`                                                               | برای غیرفعالکردن زمانبند پسزمینه سلامت پراکسی که بهصورت دورهای پراکسیهای ثبتشده را بررسی میکند، آن را روی `false` تنظیم کنید.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `PROXY_HEALTH_INTERVAL_MS`                        | `600000`                 | `src/lib/proxyHealth/scheduler.ts`                                                               | فاصله زمانی پیمایش زمانبند پسزمینه سلامت برحسب میلیثانیه (حداقل `60000`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `PROXY_HEALTH_TEST_URL`                           | `https://httpbin.org/ip` | `src/lib/proxyHealth/probeTarget.ts`                                                             | هدف کاوش دسترسیپذیری که توسط زمانبند و نقطه پایانی `/api/settings/proxies/auto-test` استفاده میشود. برای اجتناب از مقدار پیشفرض عمومی، آن را روی یک URL داخلی/خودمیزبان تنظیم کنید.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `PROXY_HEALTH_TEST_CONCURRENCY`                   | `10`                     | `src/lib/proxyHealth/probeTarget.ts`                                                             | تعداد کاوشهایی که در هر دسته بهطور همزمان آغاز میشوند و میان زمانبند و نقطه پایانی `/api/settings/proxies/auto-test` مشترک هستند. حداقل آن 1 و حداکثر آن 50 است.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `PROXY_HEALTH_TEST_STAGGER_MS`                    | `100`                    | `src/lib/proxyHealth/probeTarget.ts`                                                             | تأخیر بر حسب میلیثانیه بین آغاز دو کاوش درون یک دسته. بدون آن، کل دسته در یک لحظه ارسال میشود و یک IP خروجی مشترک میتواند محدودیت نرخ هدف را فعال کند. برای غیرفعالکردن فاصلهگذاری، آن را روی `0` تنظیم کنید؛ حداکثر مقدار 5000 است.                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `PROXY_HEALTH_USE_PROVIDER_TARGET`                | `true`                   | `src/lib/proxyHealth/providerProbeTarget.ts`                                                     | برای متوقفکردن کاوش میزبان واقعی ارائهدهنده تخصیصیافته به پراکسی (`GET /models`، بدون کلید API) و استفاده همیشگی از `PROXY_HEALTH_TEST_URL` بهجای آن، مقدار را روی "false" تنظیم کنید.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `PROXY_HEALTH_AUTO_DEACTIVATE`                    | `false`                  | `src/lib/proxyHealth/statusPolicy.ts`                                                            | وقتی مقدار `false` است (پیشفرض)، کاوشهای خودکار دسترسیپذیری (زمانبند + دکمه «آزمایش همه» در `/api/settings/proxies/auto-test`) **فقطخواندنی** هستند و هرگز وضعیت پراکسی را تغییر نمیدهند — فقط اپراتور وضعیت فعال/غیرفعال را تعیین میکند، بنابراین یک کاوش ناپایدار نمیتواند پراکسی تخصیصیافته را بلااستفاده کند (#6246). برای بازگرداندن رفتار قدیمیِ آزمایش و تنظیم وضعیت، مقدار را روی `true` تنظیم کنید.                                                                                                                                                                                                                                                             |
| `PROXY_POOL_EGRESS_OBSERVATION`                   | `false`                  | `src/shared/utils/featureFlags.ts`                                                               | پرچم قابلیت اختیاری (به [FEATURE_FLAGS.md](./FEATURE_FLAGS.md) مراجعه کنید؛ مقدار بازنویسیشده در پایگاه داده داشبورد اولویت دارد). مقدار `true` (یا `1`، `yes`) مشاهده فقطخواندنی خروجی مجموعه پراکسی را زیر یک مجموعه پراکسی در داشبورد نمایش میدهد (IPهای خروجی متمایز، اتصالها و بیشترین تعداد مشاهدهشده پشت یک IP طی 24 ساعت گذشته، بر اساس گزارش پراکسی). هرگز برای مسیریابی استفاده نمیشود.                                                                                                                                                                                                                                                                        |
| `PROXY_AUTO_REMOVE`                               | `false`                  | `src/lib/proxyHealth/scheduler.ts`                                                               | برای اینکه زمانبند پس از چند شکست متوالی، پراکسیها را بهطور خودکار حذف کند، مقدار را روی `true` تنظیم کنید.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `PROXY_AUTO_REMOVE_AFTER`                         | `3`                      | `src/lib/proxyHealth/scheduler.ts`                                                               | تعداد شکستهای متوالی پیش از آنکه زمانبند یک پراکسی را بهطور خودکار حذف کند (وقتی `PROXY_AUTO_REMOVE=true` است).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `PROXY_AUTO_DISABLE`                              | `false`                  | `src/lib/proxyHealth/scheduler.ts`                                                               | روی `true` تنظیم کنید تا زمانبند پس از شکستهای متوالی و تکرارشونده، یک پراکسی را بهصورت نرم غیرفعال کند (وضعیت `dead`، بدون حذف شدن)، بهجای آنکه آن را حذف کند. جایگزینی غیرمخرب برای `PROXY_AUTO_REMOVE`: پراکسی بلافاصله از تفکیک استخر/چرخش خارج میشود (فیلتر وضعیت فعال که در تفکیک استخرِ محدوده استفاده میشود، از قبل آن را مستثنا میکند) و بهمحض اینکه دوباره بررسیها را با موفقیت پشت سر بگذارد، بهطور خودکار فعال میشود. از آستانه `PROXY_AUTO_REMOVE_AFTER` بهصورت مشترک استفاده میکند. اگر هر دو پرچم `true` باشند، `PROXY_AUTO_REMOVE` اولویت دارد.                                                                                                          |
| `OMNIROUTE_CONTROL_PLANE_PROXY_DIRECT_FALLBACK`   | `false`                  | `src/shared/constants/featureFlagDefinitions.ts`                                                 | به جریانهای اعتبارسنجی OAuth و ارائهدهنده اجازه میدهد وقتی پیشبررسیهای دسترسیپذیری پراکسی ناموفق هستند، پراکسی سنجاقشده را دور بزنند و مستقیماً متصل شوند. ترتیب اولویت مؤثر عبارت است از: بازنویسی پایگاه داده Feature Flags > متغیر محیطی > مقدار پیشفرض.                                                                                                                                                                                                                                                                                                                                                                                                              |
| `RATE_LIMIT_MAX_WAIT_MS`                          | `30000` (۳۰ ثانیه)       | `src/lib/resilience/settings.ts`                                                                 | بودجه پیشفرض **انتظار در صف**: مدتزمانی که یک درخواست میتواند برای یک جایگاه ارائهدهنده منتظر بماند و پیش از رد شدن در صف باقی بماند، بدون آنکه هرگز به بالادست برسد. پس از شروع اجرای کار، زمانسنج پاک میشود — اجرای آن بهطور جداگانه توسط `RATE_LIMIT_EXECUTION_MAX_WAIT_MS` محدود میشود. متغیر محیطی فقط مقدار پیشفرض را تعیین میکند: ابتدا `resilienceSettings.requestQueue.maxWaitMs` ذخیرهشده و سپس `rateLimitOverrides.maxWaitMs` مختص هر اتصال، هرکدام بر آن اولویت دارند.                                                                                                                                                                                       |
| `RATE_LIMIT_EXECUTION_MAX_WAIT_MS`                | `600000` (۱۰ دقیقه)      | `open-sse/services/rateLimitManager.ts`                                                          | سقف مدتزمانی که یک درخواست پذیرفتهشده میتواند پیش از انقضای رزرو محدودیت نرخ خود در حال اجرا بماند — این مقدار از بودجه انتظار در صف جدا شده است تا شروع کند واکشی در درگاههای غیرتدریجی منقضی نشود (#12027).                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `RATE_LIMIT_MAX_QUEUE_DEPTH`                      | `0` (غیرفعال)            | `open-sse/services/rateLimitManager.ts`                                                          | سقف پذیرش صف: وقتی این تعداد درخواست از قبل در صف هستند، با خطای 429 و `queue_full` رد شود. `0` = بدون محدودیت (پیشفرض).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `RATE_LIMIT_AUTO_ENABLE`                          | _(تنظیمنشده)_            | `open-sse/services/rateLimitManager.ts`                                                          | شبکه ایمنی محدودیت نرخ با فعالسازی خودکار را، صرفنظر از تنظیم ذخیرهشده داشبورد، بهاجبار روشن/خاموش میکند. مقادیر `true`/`1`/`on` برای اجبار به روشن بودن و `false`/`0`/`off` برای اجبار به خاموش بودن پذیرفته میشوند.                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `PROVIDER_COOLDOWN_ENABLED`                       | _(تنظیمنشده ← خاموش)_    | `open-sse/services/providerCooldownTracker.ts`                                                   | ردیابی سراسری و بیندرخواستی زمان انتظار ارائهدهنده/اتصال بهصورت انتخابی. بهطور پیشفرض خاموش است (با زمان انتظار اتصال / مدارشکن ارائهدهنده همپوشانی دارد). برای فعالسازی، مقادیر `true`/`1`/`on` پذیرفته میشوند.                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `PROVIDER_COOLDOWN_MIN_MS`                        | `5000`                   | `open-sse/services/providerCooldownTracker.ts`                                                   | حداقل زمان انتظار (میلیثانیه) پیش از تلاش مجدد برای ارائهدهنده/اتصال ناموفق. این مدت با شکستهای متوالی بهصورت نمایی افزایش مییابد. فقط زمانی استفاده میشود که `PROVIDER_COOLDOWN_ENABLED` فعال باشد.                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `PROVIDER_COOLDOWN_MAX_MS`                        | `300000` (5 دقیقه)       | `open-sse/services/providerCooldownTracker.ts`                                                   | حداکثر سقف دوره انتظار (میلیثانیه) پیش از تلاش مجدد برای ارائهدهنده/اتصال ناموفق، صرفنظر از شرایط. فقط زمانی استفاده میشود که `PROVIDER_COOLDOWN_ENABLED` فعال باشد.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `STREAM_RECOVERY_ENABLED`                         | _(تنظیمنشده ← غیرفعال)_  | `src/lib/resilience/settings.ts` (مقداردهی اولیه) → `open-sse/services/streamRecovery.ts` (منطق) | **چیست:** بازیابی شفاف جریانهای بالادستی ناقص (پورت free-claude-code). پنجره آغازین SSE را تا `STREAM_RECOVERY.HOLDBACK_MS` (750 میلیثانیه) نگه میدارد تا قطعی _پیش از تثبیت_ — قطعیای که پیش از رسیدن هرگونه بایت به کلاینت رخ میدهد — دوباره باز شده و بهصورت نامرئی تلاش مجدد انجام شود. **چه زمانی فعال شود:** برای بالادستیهای ناپایدار/بالادستیهایی که اغلب در شروع جریان با صفر بایت قطع میشوند؛ اگر نمیتوانید افزودن حداکثر 750 میلیثانیه به زمان دریافت نخستین توکن در هر جریان را بپذیرید، آن را غیرفعال نگه دارید. مقادیر `true`/`1`/`on` را میپذیرد. مقدار اولیه تنظیم ماندگار Resilience را تعیین میکند؛ پس از تنظیم گزینه Dashboard، مقدار آن اولویت دارد. |
| `STREAM_RECOVERY_MIDSTREAM_ENABLED`               | _(تنظیمنشده ← غیرفعال)_  | `src/lib/resilience/settings.ts` (مقداردهی اولیه) → `open-sse/services/streamRecovery.ts` (منطق) | **چیست:** ادامه جریان در میانه آن (Fase 4.4) — پس از یک قطعی _پس از تثبیت_ (بایتها از قبل به کلاینت رسیدهاند)، با استفاده از متن جزئی بهعنوان پیشپرکننده دستیار، درخواست دوباره ارسال و پسوند ازدسترفته به آن متصل میشود. فقط برای جریانهای متن ساده سازگار با OpenAI؛ هنگام اجرای فراخوانی ابزار هرگز فعال نمیشود. **چه زمانی فعال شود:** برای تولیدهای طولانی که در میانه پاسخ قطع میشوند و میپذیرید بخش بازیابیشده بهجای توکنبهتوکن، یکباره دریافت شود. مستقل از `STREAM_RECOVERY_ENABLED` است (نمایه ریسک متفاوت). مقادیر `true`/`1`/`on` را میپذیرد.                                                                                                                |
| `STREAM_THROUGHPUT_WATCHDOG_ENABLED`              | _(تنظیمنشده ← غیرفعال)_  | `src/lib/resilience/settings.ts` → `open-sse/services/throughputWatchdog.ts`                     | ناظر اختیاری خروجی مفید جریان فعال. جریانهایی را شناسایی میکند که همچنان قطعهها را ارسال میکنند، اما نرخ خروجی دستیار آنها پایینتر از مقدار پیکربندیشده باقی میماند؛ سیگنالهای حیات، رویدادهای مصرف، دلتاهای خالی و مراحل ابزار/استدلال بهاشتباه پیشرفت تلقی نمیشوند. مستقل از مهلتهای عدم فعالیت و ضربالاجل قطعی است.                                                                                                                                                                                                                                                                                                                                                   |
| `STREAM_THROUGHPUT_WATCHDOG_WARMUP_MS`            | `30000`                  | `src/lib/resilience/settings/normalize.ts`                                                       | مهلت اولیه پیش از ارزیابی توان عملیاتی که به بازه 0–600000 میلیثانیه محدود میشود.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `STREAM_THROUGHPUT_WATCHDOG_WINDOW_MS`            | `30000`                  | `src/lib/resilience/settings/normalize.ts`                                                       | پنجره لغزان خروجی مفید که به بازه 1000–600000 میلیثانیه محدود میشود؛ پیش از لغو، یک پنجره کامل لازم است.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `STREAM_THROUGHPUT_WATCHDOG_MIN_BYTES_PER_SECOND` | `4`                      | `src/lib/resilience/settings/normalize.ts`                                                       | حداقل نرخ بایت خروجی UTF-8 دستیار (تقریب محافظهکارانه توکن) که به بازه 1–1000000 محدود میشود.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `STREAM_THROUGHPUT_WATCHDOG_MIN_USEFUL_BYTES`     | `1`                      | `src/lib/resilience/settings/normalize.ts`                                                       | حداقل نمونه غیرصفر خروجی مفید که قابلاندازهگیری تلقی میشود و به بازه 1–1000000 بایت محدود است.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `HEALTHCHECK_STAGGER_MS`                          | `3000`                   | `src/lib/tokenHealthCheck.ts`                                                                    | فاصله زمانی پلکانی (میلیثانیه) میان بررسیهای سلامت توکن ارائهدهندگان هنگام راهاندازی.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `HEALTHCHECK_JITTER_MIN_MS`                       | `500`                    | `src/lib/tokenHealthCheck.ts`                                                                    | حداقل تأخیر تصادفی (میلیثانیه) که برای جلوگیری از هجوم ناگهانی، میان بررسیهای سلامت توکن ارائهدهندگان به `HEALTHCHECK_STAGGER_MS` افزوده میشود (Issue #1220).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `HEALTHCHECK_JITTER_MAX_MS`                       | `5000`                   | `src/lib/tokenHealthCheck.ts`                                                                    | حداکثر تأخیر تصادفی (میلیثانیه) که برای جلوگیری از هجوم ناگهانی، میان بررسیهای سلامت توکن ارائهدهندگان به `HEALTHCHECK_STAGGER_MS` افزوده میشود (Issue #1220).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `HEALTHCHECK_BATCH_SIZE`                          | `20`                     | `src/lib/tokenHealthCheck.ts`                                                                    | اندازهٔ دستهٔ بررسیهای همزمان برای پیمایش بررسی سلامت توکن هنگام راهاندازی؛ مقادیر بزرگتر اتصالهای بیشتری را بهصورت موازی بررسی میکنند و مقادیر کوچکتر بار ناگهانی را کاهش میدهند (Issue #7875، پسرفتِ #7719).                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `REQUEST_RETRY`                                   | `2`                      | `src/sse/services/cooldownAwareRetry.ts`                                                         | تعداد تلاشهای مجدد خودکار در پاسخهای دورهٔ انتظارِ محدود به مدل، پیش از بازگرداندن خطا به کلاینت.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `MAX_RETRY_INTERVAL_SEC`                          | `30`                     | `src/sse/services/cooldownAwareRetry.ts`                                                         | حداکثر فاصلهٔ عقبنشینی (برحسب ثانیه) میان تلاشهای مجدد دورهٔ انتظار. صرفنظر از مقدار بالادستی `Retry-After`، به این مقدار محدود میشود.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `HEADROOM_URL`                                    | `http://localhost:8787`  | `src/lib/headroom/detect.ts`                                                                     | URL پراکسی صرفهجویی در توکن Headroom. چرخهٔ حیات داشبورد (`api/headroom/*`) بهطور پیشفرض یک CLI محلی `headroom-ai` را روی رابط loopback اجرا میکند؛ این مقدار را فقط برای اشاره به یک پراکسی sidecar خارجی Docker بازنویسی کنید.                                                                                                                                                                                                                                                                                                                                                                                                                                         |

### ثابتهای تنظیم بازیابی استریم

رفتار نگهداری بازیابی با ثابتهای کدنویسیشده در
`open-sse/config/constants.ts` (`STREAM_RECOVERY`) تنظیم میشود که برای مرجع در اینجا نشان داده شدهاند —
تغییر آنها به ویرایش کد نیاز دارد، نه یک متغیر محیطی:

- `STREAM_RECOVERY.HOLDBACK_MS = 750` — مدتزمانی که پنجرهٔ آغازین SSE نگه داشته میشود
  تا در صورت قطع زودهنگام، پیش از ارسال قطعی هر بایت به کلاینت بتوان دوباره تلاش کرد.
- `STREAM_RECOVERY.BUFFER_MAX_BYTES = 65536` — سقف قطعی پنجرهٔ نگهداشتهشده؛ بهمحض انباشتهشدن
  این تعداد بایت، صرفنظر از تایمر، ارسال قطعی (تخلیه + عبور مستقیم) انجام میشود.
- `STREAM_RECOVERY.EARLY_RETRY_MAX = 4` — حداکثر تعداد بازگشاییهای شفاف استریم بالادستی
  تا زمانی که دادههای نگهداشتهشده هنوز بهصورت قطعی ارسال نشدهاند.

> **محدودیت نرخ پنجرهٔ لغزان بهازای هر ارائهدهنده (بدون متغیر محیطی):** سازوکار _جایگزینِ_
> محدودیت نرخ پنجرهٔ لغزان بهازای هر ارائهدهنده که از FCC منتقل شده، در کد وجود دارد
> (`open-sse/services/providerDefaultRateLimit.ts`، متصلشده از طریق
> `open-sse/services/rateLimitManager.ts`)، اما با یک **نگاشت پیشفرض خالی** عرضه میشود
> و در حال حاضر **هیچ متغیر محیطی برای اپراتور ندارد** — فقط از طریق یک قلاب آزمایشی /
> ویرایش کد فعال میشود. عمداً در جدول بالا فهرست نشده است. محدودکنندهٔ رله بهازای هر `(token, IP)`
> که _دارای_ گزینهٔ تنظیم است، `RELAY_IP_PER_MINUTE` است (§3 شبکه و درگاهها).

---

## 22. اشکالزدایی

> [!CAUTION]
> این متغیرها خروجی **پرمفصل** تولید میکنند و ممکن است دادههای حساس را افشا کنند. **هرگز آنها را در محیط عملیاتی فعال نکنید.**

| متغیر                                 | مقدار پیشفرض                        | فایل منبع                                                                                             | توضیحات                                                                                                                                                                                                                                                                                           |
| ------------------------------------- | ----------------------------------- | ----------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `CURSOR_DEBUG`                        | _(تنظیمنشده)_                       | `open-sse/executors/cursor.ts`                                                                        | برای فعالکردن گزارشهای پرجزئیات اجراکننده Cursor (قطعههای SSE رمزگشاییشده و غیره)، روی `1` تنظیم کنید.                                                                                                                                                                                            |
| `CURSOR_STREAM_DEBUG`                 | _(تنظیمنشده)_                       | `open-sse/executors/cursor.ts`                                                                        | نام مستعار سازگار با نسخههای قبلی برای `CURSOR_DEBUG`.                                                                                                                                                                                                                                            |
| `CURSOR_DUMP_FILE`                    | _(تنظیمنشده)_                       | `open-sse/executors/cursor.ts`                                                                        | مسیر فایل اختیاری که هنگام `CURSOR_DEBUG=1` قطعههای خام و رمزگشاییشده Cursor را دریافت میکند.                                                                                                                                                                                                     |
| `CURSOR_STREAM_TIMEOUT_MS`            | `300000`                            | `open-sse/executors/cursor.ts`                                                                        | مهلت بیکاری جریان (میلیثانیه) برای اجراکننده Cursor.                                                                                                                                                                                                                                              |
| `CURSOR_KV_GRACE_MS`                  | `2000`                              | `open-sse/executors/cursor.ts`                                                                        | بازه ارفاقی (میلیثانیه) پس از پایاندهنده نرم composer kv_after_text، هنگامی که هنوز بایتهایی در بافر باقی ماندهاند — به فراخوانی ابزار exec_mcp انتهایی فرصت میدهد تا فریم خود را کامل کند.                                                                                                       |
| `CURSOR_TOOL_DIRECTIVE`               | فعال (`!== "0"`)                    | `open-sse/executors/cursor.ts`                                                                        | دستورالعمل ثبت ابزار که باعث میشود composer-2.5 فراخوانیهای ابزار را با اطمینان صادر کند. برای غیرفعالکردن، روی `0` تنظیم کنید.                                                                                                                                                                   |
| `OMNIROUTE_SYSTEM_INSTRUCTION_APPEND` | _(تنظیمنشده)_                       | `open-sse/translator/request/claude-to-openai.ts`, `open-sse/translator/response/openai-to-claude.ts` | متن اعلان سیستمی تعریفشده توسط اپراتور که پس از ترجمه به پیام سیستمی افزوده میشود (تزریق پس از ترجمه) و به مسیرهای codex/Responses و `/v1/messages` میرسد. همچنین بهعنوان پیشوند دستورالعملی استفاده میشود که از بلوکهای مقدمه سیستمی بازتابشده حذف میگردد. برای غیرفعالماندن، آن را تنظیم نکنید. |
| `OMNIROUTE_STRIP_SYSTEM_PREAMBLE`     | `0` (خاموش)                         | `open-sse/translator/response/openai-to-claude.ts`, `open-sse/utils/directivePreambleStripper.ts`     | برای حذف بلوکهای مقدمه اعلان سیستمی بازتابشده از ابتدای یک جریان openai→claude، روی `1` تنظیم کنید. بهطور پیشفرض خاموش است — روشهای اکتشافی بر اساس نثر انگلیسی طراحی شدهاند و محتوای پاسخ را تغییر میدهند؛ بنابراین پاسخی که بهطور مجاز با چنین بخشی آغاز شود، آن را از دست خواهد داد.           |
| `CURSOR_IMAGE_FETCH_TIMEOUT_MS`       | `15000`                             | `open-sse/utils/cursorImages.ts`                                                                      | مهلت واکشی هر تصویر (میلیثانیه) برای ورودی بینایی راهدور `image_url`.                                                                                                                                                                                                                             |
| `CURSOR_STATE_DB_PATH`                | _(کاوششده)_                         | `open-sse/utils/cursorVersionDetector.ts`                                                             | جستوجوی پایگاه داده وضعیت Cursor IDE را که برای تشخیص نسخه IDE استفاده میشود، بازنویسی میکند.                                                                                                                                                                                                     |
| `CURSOR_AGENT_CLI_VERSION`            | _(تشخیص / تثبیت)_                   | `open-sse/utils/cursorAgentCliVersion.ts`                                                             | شناسه ساخت Agent CLI با قالب (`YYYY.MM.DD-<hash>`) برای `x-cursor-client-version: cli-…` در Agent Run.                                                                                                                                                                                            |
| `CURSOR_AGENT_BIN`                    | _(تنظیمنشده)_                       | `open-sse/handlers/imageGeneration/providers/cursorAgentImage.ts`                                     | مسیر فایل اجرایی Cursor Agent که برای تولید تصویر استفاده میشود. اگر تنظیم نشده باشد، کنترلگر ابتدا از `providerSpecificData.agentBin` و سپس PATH استفاده میکند.                                                                                                                                  |
| `CURSOR_IMG_TIMEOUT_MS`               | `210000`                            | `open-sse/handlers/imageGeneration/providers/cursorAgentImage.ts`                                     | زمان سپریشده مجاز برای هر تصویر (میلیثانیه) در کارهای تصویری Cursor Agent.                                                                                                                                                                                                                        |
| `CURSOR_IMG_MAX_CONCURRENT`           | `2`                                 | `open-sse/handlers/imageGeneration/providers/cursorAgentImage.ts`                                     | محدودکننده همزمانی مجوز اشتراکی برای کارهای تصویری Cursor.                                                                                                                                                                                                                                        |
| `CURSOR_IMG_MODEL`                    | درخواست / `auto`                    | `open-sse/handlers/imageGeneration/providers/cursorAgentImage.ts`                                     | بازنویسی `--model` در Cursor CLI برای کارهای تصویری.                                                                                                                                                                                                                                              |
| `UC_IMAGE_POLL_INTERVAL_MS`           | `2000`                              | `open-sse/handlers/imageGeneration/providers/ucImage.ts`                                              | تناوب نظرسنجی نتیجه تولید تصویر UC (uncensored.com) (میلیثانیه).                                                                                                                                                                                                                                  |
| `UC_IMAGE_POLL_TIMEOUT_MS`            | `60000`                             | `open-sse/handlers/imageGeneration/providers/ucImage.ts`                                              | زمان سپریشده مجاز برای نظرسنجی نتیجه تولید تصویر UC (میلیثانیه).                                                                                                                                                                                                                                  |
| `UC_VIDEO_POLL_INTERVAL_MS`           | `3000`                              | `open-sse/handlers/videoGeneration/providers/ucVideo.ts`                                              | تناوب نظرسنجی نتیجه تولید ویدئوی UC (uncensored.com) (میلیثانیه).                                                                                                                                                                                                                                 |
| `UC_VIDEO_POLL_TIMEOUT_MS`            | `300000`                            | `open-sse/handlers/videoGeneration/providers/ucVideo.ts`                                              | زمان سپریشده مجاز برای نظرسنجی نتیجه تولید ویدئوی UC (میلیثانیه).                                                                                                                                                                                                                                 |
| `CURSOR_DATA_DIR`                     | _(کاوششده)_                         | `open-sse/utils/cursorAgentCliVersion.ts`                                                             | مسیر پوشه داده Cursor Agent CLI (`…/versions/<id>`) را بازنویسی میکند؛ همان متغیری که عامل رسمی استفاده میکند.                                                                                                                                                                                    |
| `CURSOR_TOKEN`                        | _(تنظیمنشده)_                       | `scripts/ad-hoc/cursor-tap.cjs`                                                                       | توکن حامل مستقیم Cursor که ابزارهای توسعهدهنده از آن استفاده میکنند.                                                                                                                                                                                                                              |
| `OMNIROUTE_LOG_REQUEST_SHAPE`         | غیرفعال (فعالسازی اختیاری با `"1"`) | `src/app/api/v1/chat/completions/route.ts`                                                            | هنگامی که روی `"1"` تنظیم شود، نشانگرهای نوع/طول محتوا را برای محمولههای بزرگ گفتوگو ثبت میکند. برای کاهش شلوغی گزارشها، بهطور پیشفرض خاموش است.                                                                                                                                                  |
| `DEBUG_RESPONSES_SSE_TO_JSON`         | _(تنظیمنشده)_                       | `open-sse/handlers/responseTranslator.ts`                                                             | برای ثبت جزئیات ترجمه SSE→JSON در Responses API، روی `true` تنظیم کنید.                                                                                                                                                                                                                           |
| `DEBUG_CLAUDE_NONSTREAM`              | _(تنظیمنشده)_                       | `open-sse/handlers/responseTranslator.ts`                                                             | برای آشکارکردن قطعههای خالی textContent در مسیر ترجمه پاسخ Claude، روی `true` تنظیم کنید (فقط برای اشکالزدایی).                                                                                                                                                                                   |
| `NEXT_PUBLIC_OMNIROUTE_E2E_MODE`      | _(تنظیمنشده)_                       | زیرساخت آزمون E2E                                                                                     | برای فعالکردن حالت آزمون E2E (احراز هویت آسانگیرانه، قلابهای آزمون)، روی `true` تنظیم کنید.                                                                                                                                                                                                       |

---

## 23. یکپارچهسازی GitHub

به کاربران اجازه دهید مشکلات را مستقیماً از Dashboard گزارش کنند.

| متغیر                 | پیشفرض        | فایل منبع                               | توضیحات                                                                                                                                                  |
| --------------------- | ------------- | --------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `GITHUB_ISSUES_REPO`  | _(تنظیمنشده)_ | `src/app/api/v1/issues/report/route.ts` | مخزن با قالب `owner/repo`.                                                                                                                               |
| `GITHUB_ISSUES_TOKEN` | _(تنظیمنشده)_ | `src/app/api/v1/issues/report/route.ts` | GitHub Personal Access Token با حوزهٔ دسترسی `issues:write`.                                                                                             |
| `GITHUB_TOKEN`        | _(تنظیمنشده)_ | ابزارهای اولویتبندی مشکلات / عامل ابری  | توکن عمومی دسترسی GitHub که بهعنوان جایگزین `GITHUB_ISSUES_TOKEN` استفاده میشود و ابزارهای عامل ابری در `src/lib/cloudAgent/*` نیز از آن استفاده میکنند. |

---

## سناریوهای استقرار

برای راهنمای SRE بکاند رله (رفتار ts/bifrost/auto، نحوهٔ استقرار 9router در مقایسه با CLIProxyAPI و راهبرد جایگزین برای توان عملیاتی بالا)، به [راهبرد بکاند رله](/docs/reference/RELAY_BACKEND_STRATEGY.md) مراجعه کنید.

### توسعهٔ محلی حداقلی

```bash
JWT_SECRET=$(openssl rand -base64 48)
API_KEY_SECRET=$(openssl rand -hex 32)
INITIAL_PASSWORD=dev123
PORT=20128
NODE_ENV=development
```

### محیط عملیاتی Docker

```bash
JWT_SECRET=<generated>
API_KEY_SECRET=<generated>
INITIAL_PASSWORD=<generated>
STORAGE_ENCRYPTION_KEY=<generated>
DATA_DIR=/data
PORT=20128
API_PORT=20129
NODE_ENV=production
AUTH_COOKIE_SECURE=true
REQUIRE_API_KEY=true
NEXT_PUBLIC_BASE_URL=https://omniroute.example.com
BASE_URL=http://localhost:20128
OMNIROUTE_MEMORY_MB=8192
CORS_ORIGIN=https://your-frontend.example.com
```

### محیط ایزوله / CI

```bash
JWT_SECRET=test-jwt-secret-for-ci
API_KEY_SECRET=test-api-key-secret-for-ci
INITIAL_PASSWORD=testpass
NODE_ENV=production
OMNIROUTE_DISABLE_BACKGROUND_SERVICES=true
APP_LOG_TO_FILE=false
```

### VPS با پراکسی معکوس (nginx + Cloudflare)

```bash
JWT_SECRET=<generated>
API_KEY_SECRET=<generated>
STORAGE_ENCRYPTION_KEY=<generated>
PORT=20128
AUTH_COOKIE_SECURE=true
REQUIRE_API_KEY=true
NEXT_PUBLIC_BASE_URL=https://omniroute.example.com
BASE_URL=http://127.0.0.1:20128
CORS_ORIGIN=https://omniroute.example.com
ENABLE_TLS_FINGERPRINT=true
CLI_COMPAT_ALL=1
```

---

## 24. محیط ایزولهٔ Skills (v3.8.0+)

محدودیتها و تنظیمات ایمنی که هنگام اجرای خودکارسازیهای تعریفشده توسط کاربر در یک محیط ایزوله، بهوسیلهٔ چارچوب Skills (`src/lib/skills/`) اعمال میشوند.

| متغیر                             | پیشفرض                                    | فایل منبع                                                          | توضیحات                                                                                                                                                                                                                                                                                                          |
| --------------------------------- | ----------------------------------------- | ------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `SKILLS_SANDBOX_TIMEOUT_MS`       | `10000` (10 ثانیه)                        | `src/lib/skills/builtins.ts`                                       | مهلت زمانی واقعی بهازای هر اجرا برای کد مهارت در محیط ایزوله. این سقف قطعی است و هر اجرای طولانیتر متوقف میشود.                                                                                                                                                                                                  |
| `SKILLS_EXECUTION_TIMEOUT_MS`     | _(بازگشت به `SKILLS_SANDBOX_TIMEOUT_MS`)_ | `src/lib/skills/`                                                  | مهلت زمانی سطحبالا برای هماهنگسازی مهارت. برای امکان اجرای گردشکارهای چندمرحلهای، آن را بیشتر از `SKILLS_SANDBOX_TIMEOUT_MS` تنظیم کنید.                                                                                                                                                                         |
| `SKILLS_MAX_FILE_BYTES`           | `1048576` (1 MB)                          | `src/lib/skills/builtins.ts`                                       | حداکثر تعداد بایتهایی که یک مهارت میتواند از هر فایل منفرد در محیط ایزوله بخواند.                                                                                                                                                                                                                                |
| `SKILLS_MAX_HTTP_RESPONSE_BYTES`  | `256000` (250 KB)                         | `src/lib/skills/builtins.ts`                                       | حداکثر تعداد بایتهایی که از هر پاسخ HTTP منفرد درون یک مهارت ثبت میشود.                                                                                                                                                                                                                                          |
| `SKILLS_MAX_SANDBOX_OUTPUT_CHARS` | `100000`                                  | `src/lib/skills/builtins.ts`                                       | سقف قطعی تعداد نویسههای stdout/stderr که از یک فراخوانی محیط ایزوله بازگردانده میشوند.                                                                                                                                                                                                                           |
| `SKILLS_SANDBOX_NETWORK_ENABLED`  | `false`                                   | `src/lib/skills/builtins.ts`                                       | برای اجازهدادن به دسترسی خروجی شبکه از درون محیط ایزوله، آن را روی `1`/`true` تنظیم کنید. برای ایمنی، مقدار پیشفرض **ایزوله** است.                                                                                                                                                                               |
| `SKILLS_ALLOWED_SANDBOX_IMAGES`   | _(خالی)_                                  | `src/lib/skills/builtins.ts`                                       | فهرست مجاز تصاویر کانتینر برای اجرای محیط ایزوله که با ویرگول از هم جدا شدهاند. خالیبودن یعنی فقط مقدار پیشفرض داخلی مجاز است.                                                                                                                                                                                   |
| `SKILLS_SANDBOX_DOCKER_IMAGE`     | _(پیشفرض داخلی)_                          | `src/lib/skills/`                                                  | تصویر کانتینری که هنگام ایجاد محیط ایزولهٔ مبتنی بر Docker استفاده میشود. برای تثبیت یک تصویر پایهٔ سفارشی و سختسازیشده، آن را بازنویسی کنید.                                                                                                                                                                    |
| `SKILLS_SANDBOX_RUNTIME`          | `auto`                                    | `src/lib/skills/sandbox.ts`, `src/lib/skills/containerProvider.ts` | زماناجرای کانتینر برای ایزولهسازی مهارت: `auto` \| `docker` \| `apple` \| `wsl` \| `orbstack` \| `podman`. گزینهٔ `auto` بهترین زماناجرای نصبشده را متناسب با سیستمعامل میزبان انتخاب میکند (Apple Container/OrbStack در macOS، ‏WSL Container در Windows و Podman در Linux) و در صورت نیاز به Docker بازمیگردد. |

> [!CAUTION]
> فعالکردن `SKILLS_SANDBOX_NETWORK_ENABLED=true` یک مسیر خروجی از کد دلخواه مهارت ایجاد میکند. در استقرارهای اشتراکی، آن را همراه با `OUTBOUND_SSRF_GUARD_ENABLED=true` و یک سیاست سختگیرانه برای `CORS_ORIGIN`/پروکسی استفاده کنید.

---

## 25. سهمیههای ارائهدهنده، تونلها، پشتیبانگیریها و تنظیمات متفرقهٔ زمان اجرا

نقاط پایانی سهمیهٔ ارائهدهنده، تونلهای شبکه (Tailscale، Ngrok، پراکسی اشکالزدایی MITM)، استخر خروجی 1Proxy، پشتیبانگیریهای پایگاه داده و تنظیمات جایگزین کوچک و مختص هر قابلیت که لایهٔ اجراکننده یا اسکریپتها به آنها ارجاع میدهند.

| متغیر                                          | پیشفرض                                                                      | فایل منبع                                                                 | توضیحات                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| ---------------------------------------------- | --------------------------------------------------------------------------- | ------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `REDIS_URL`                                    | `redis://localhost:6379`                                                    | `src/shared/utils/rateLimiter.ts`                                         | رشتهٔ اتصال Redis برای بکاند محدودکنندهٔ نرخ.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `ALIBABA_CODING_PLAN_HOST`                     | _(میزبان تولید)_                                                            | `open-sse/services/bailianQuotaFetcher.ts`                                | میزبان مورد استفاده برای دریافت سهمیههای coding-plan سرویس Alibaba Bailian را بازنویسی میکند.                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `ALIBABA_CODING_PLAN_QUOTA_URL`                | برگرفته از میزبان                                                           | `open-sse/services/bailianQuotaFetcher.ts`                                | بازنویسی URL کامل سهمیه برای Alibaba Bailian.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `QWEN_CLOUD_COOKIE`                            | _(تنظیمنشده)_                                                               | `open-sse/services/qwenTokenPlanQuotaFetcher.ts`                          | کوکی نشست کنسول برای درگاه سهمیهٔ Token Plan شخصی Qwen Cloud / Model Studio (کلید API استنتاج نمیتواند آن را بخواند). کل هدر درخواست `Cookie` را — که شامل `login_qwencloud_ticket` است — از هر فراخوانی `api.json` به `cs-data.qwencloud.com` در home.qwencloud.com › Billing › Subscription (F12 › Network) کپی کنید. این مقدار حساس و محدود به نشست است؛ ترجیحاً از فیلد `qwenCloudCookie` مختص هر اتصال در داشبورد استفاده کنید.                                                                                                                                     |
| `QWEN_CLOUD_SEC_TOKEN`                         | _(تنظیمنشده)_                                                               | `open-sse/services/qwenTokenPlanQuotaFetcher.ts`                          | بازنویسی دستی `sec_token` برای درگاه کنسول Token Plan. حساس است؛ اگر تنظیم نشده باشد، دریافتکننده با استفاده از کوکی آن را از HTML داشبورد استخراج میکند.                                                                                                                                                                                                                                                                                                                                                                                                                |
| `QWEN_TOKEN_PLAN_HOST`                         | `https://cs-data.qwencloud.com`                                             | `open-sse/services/qwenTokenPlanQuotaFetcher.ts`                          | بازنویسی میزبان درگاه برای دریافتکنندهٔ سهمیهٔ Token Plan شخصی (برای مثال، `bailian-singapore-cs.alibabacloud.com` برای کنسول Model Studio).                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `QWEN_TOKEN_PLAN_DASHBOARD_URL`                | `https://home.qwencloud.com/`                                               | `open-sse/services/qwenTokenPlanQuotaFetcher.ts`                          | URL داشبورد که برای استخراج `sec_token` از HTML واردشده به حساب استفاده میشود.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `ALIBABA_FREE_TIER_VISION_FE_PATH`             | `/costing-balance/free-quota-image-video`                                   | `open-sse/services/alibabaFreeTierQuotaFetcher.ts`                        | بازنویسی مسیر فرانتاند کنسول برای دریافت سهمیهٔ رایگان بینایی/رسانه در Alibaba Model Studio.                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `ALIBABA_FREE_TIER_MULTIMODAL_FE_PATH`         | `/costing-balance/free-quota-multimodal`                                    | `open-sse/services/alibabaFreeTierQuotaFetcher.ts`                        | بازنویسی مسیر فرانتاند کنسول برای دریافت سهمیهٔ رایگان چندوجهی در Alibaba Model Studio.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `ALIBABA_FREE_TIER_AUDIO_FE_PATH`              | `/costing-balance/free-quota-audio`                                         | `open-sse/services/alibabaFreeTierQuotaFetcher.ts`                        | بازنویسی مسیر فرانتاند کنسول برای دریافت سهمیهٔ رایگان صوت در Alibaba Model Studio.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `ALIBABA_FREE_TIER_ALLOWLIST_PATH`             | _(تنظیمنشده)_                                                               | `open-sse/services/alibabaFreeTierAllowlist.ts`                           | مسیر اختیاری به یک فایل JSON محلی برای بازنویسی فهرست مجاز داخلی مدلهای متنی ردهٔ رایگان Alibaba. در صورت نبود، ابتدا به `$DATA_DIR/alibaba-free-tier-allowlist.json` و سپس به `config/alibaba-free-tier-allowlist.json` بازمیگردد.                                                                                                                                                                                                                                                                                                                                      |
| `CONTEXT_RESERVE_TOKENS`                       | `1024`                                                                      | `open-sse/services/contextManager.ts`                                     | توکنهای رزروشده برای خروجی تکمیل هنگام محاسبهٔ بودجهٔ پرامپت.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `CONTEXT_KEEP_LATEST_IMAGES`                   | `2`                                                                         | `open-sse/services/contextManager.ts`                                     | تعداد جدیدترین تصاویر درونخطی که هنگام حذف تصاویر قدیمیتر برای جایگرفتن در پنجرهٔ زمینه نگه داشته میشوند (#8560).                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `MODEL_ALIAS_COMPAT_ENABLED`                   | فعال                                                                        | `open-sse/services/model.ts`                                              | لایهٔ سازگاری قدیمی نام مستعار مدل را که کلاینتهای قدیمیتر استفاده میکنند، فعال یا غیرفعال میکند.                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `OMNIROUTE_EMERGENCY_FALLBACK`                 | فعال                                                                        | `open-sse/services/emergencyFallback.ts`                                  | برای غیرفعالکردن بازگشت اضطراری ناشی از اتمام بودجه که درخواستهای ناموفق را به مدل رایگان `nvidia`/`openai/gpt-oss-120b` هدایت مجدد میکند، مقدار `false` (یا `0`) را تنظیم کنید. ترتیب تقدم مؤثر عبارت است از بازنویسی پایگاه دادهٔ Feature Flags > متغیر محیطی > پیشفرض؛ اگر در دسترس نباشد، سرویس به مقدار خام متغیر محیطی بازمیگردد.                                                                                                                                                                                                                                  |
| `COMMAND_CODE_CALLBACK_PORT`                   | _(تنظیمنشده)_                                                               | `src/app/api/providers/command-code/auth/shared.ts`                       | پورت محلی مورد استفاده برای callbackهای سبک OAuth از ابزار کمکی CLI مربوط به Command Code.                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `COMMAND_CODE_VERSION`                         | `0.33.2`                                                                    | `open-sse/executors/commandCode.ts`                                       | مقداری که بهعنوان هدر `x-command-code-version` به سرویس بالادستی Command Code ارسال میشود. برای افزایش نسخهٔ CLI آن را بازنویسی کنید.                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `COMMANDCODE_API_URL`                          | `https://api.commandcode.ai`                                                | `open-sse/services/usage/command-code.ts`                                 | URL پایه برای سرویس بالادستی مصرف/سهمیهٔ Command Code که تلهمتری دریافتکنندهٔ سهمیهٔ گوشی هوشمند استفاده میکند. برای API خودمیزبان یا جایگزین Command Code بازنویسی کنید.                                                                                                                                                                                                                                                                                                                                                                                                |
| `MITM_LOCAL_PORT`                              | `443`                                                                       | `src/mitm/server.cjs`                                                     | پورت اتصال محلی برای پراکسی اشکالزدایی MITM.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `MITM_DISABLE_TLS_VERIFY`                      | `0`                                                                         | `src/mitm/server.cjs`                                                     | برای غیرفعالکردن اعتبارسنجی TLS بالادستی، مقدار `1` را تنظیم کنید (فقط برای توسعه).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `MITM_IDLE_TIMEOUT_MS`                         | `60000`                                                                     | `src/mitm/socketTimeouts.ts`, `src/mitm/server.cjs`                       | مهلت پایان بیکاری سوکت (میلیثانیه) برای اتصالهای پراکسیشده؛ سوکتهای بیکار پس از این زمان بسته میشوند تا از نشت تونلهای نیمهباز جلوگیری شود.                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `MITM_VERBOSE`                                 | `1`                                                                         | `src/mitm/server.cjs`, `src/mitm/_internal/bypass.cjs`                    | میزان جزئیات گزارش تصمیمهای مسیریابی: `0` گزارشها را ساکت میکند و مقادیر بالاتر تصمیمهای بیشتری دربارهٔ دورزدن/مسیر ثبت میکنند.                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `MITM_ROOT_CA_ENABLED`                         | `false`                                                                     | `src/mitm/manager.ts`                                                     | برای انتخاب اختیاری مدل گواهی CA ریشه + گواهی leaf مختص هر میزبان، مقدار `true` را تنظیم کنید (#6684). نصبهای جدید آن را خودکار دریافت میکنند؛ نصبهایی که از قبل گواهی leaf قدیمی و مورد اعتماد دارند، تا زمانی که این گزینه را فعال نکنند گواهی قدیمی با SAN ثابت را نگه میدارند.                                                                                                                                                                                                                                                                                       |
| `MITM_CERT_MODE`                               | `legacy`                                                                    | `src/mitm/manager.ts`, `src/mitm/server.cjs`                              | توسط مدیر MITM برای فرایند پراکسی ایجادشده تنظیم میشود (`root-ca` \| `legacy`) — تصمیم مهاجرت گواهی را منعکس میکند و برای تنظیم دستی در نظر گرفته نشده است.                                                                                                                                                                                                                                                                                                                                                                                                              |
| `OMNIROUTE_NO_SUDO`                            | `0`                                                                         | `src/mitm/systemCommands.ts`                                              | مقدار `1` (truthy) را تنظیم کنید تا `sudo` ابتدایی از فرمانهای اعتماد به گواهی MITM حذف شود — برای استقرارهای بدون root یا دارای فضای نام کاربر که در آن اپراتور بهصورت دستی به CA اعتماد میکند (مثلاً از طریق سازوکار extra-CA-certs در Node).                                                                                                                                                                                                                                                                                                                          |
| `SKIP_ANTIGRAVITY_DNS`                         | _(تنظیمنشده)_                                                               | `src/mitm/dns/provision.ts`                                               | برای ردکردن کامل ایجاد ورودیهای DNS در `/etc/hosts` برای نامهای میزبان پراکسی Antigravity، مقدار `true` را تنظیم کنید — مناسب کانتینرهایی که sudo/root ندارند.                                                                                                                                                                                                                                                                                                                                                                                                           |
| `OMNIROUTE_SKIP_DNS_WRITE`                     | _(تنظیمنشده)_                                                               | `src/mitm/dns/dnsConfig.ts`                                               | برای جلوگیری از نوشتن در فایل hosts هنگام افزودن/حذف ورودیهای DNS، مقدار `1` را تنظیم کنید — مناسب محیطهای آزمون sandbox یا فقطخواندنی.                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `OMNIROUTE_SKIP_SYSTEM_TRUST`                  | `0`                                                                         | `src/mitm/cert/install.ts`, `src/mitm/tproxy/caTrust.ts`                  | محافظ مخصوص آزمون/CI: مقدار `1` را تنظیم کنید تا نصب/حذف اعتماد گواهی بیاثر شود و مجموعهآزمون هرگز مخزن اعتماد سیستمعامل را تغییر ندهد. این مقدار بهطور خودکار توسط پیکربندی آزمون و گردشکارهای CI تنظیم میشود.                                                                                                                                                                                                                                                                                                                                                          |
| `CHANGELOG_BASE_REF`                           | _(خودکار)_                                                                  | `scripts/check/check-changelog-integrity.mjs`                             | مرجع پایهٔ صریح برای گیت جلوگیری از بلعیدهشدن CHANGELOG (بهطور پیشفرض شاخهٔ پایهٔ PR در CI، یا بالاترین `release/v*`).                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `FREE_PROXY_AUTO_SYNC_ENABLED`                 | `false`                                                                     | `src/lib/freeProxyProviders/scheduler.ts`                                 | برای فعالکردن زمانبند همگامسازی خودکار استخر پراکسی رایگان در پسزمینه، مقدار `true` را تنظیم کنید. این قابلیت اختیاری است و بهطور پیشفرض خاموش است.                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `FREE_PROXY_AUTO_SYNC_INTERVAL_MS`             | `1800000`                                                                   | `src/lib/freeProxyProviders/scheduler.ts`                                 | فاصلهٔ همگامسازی خودکار برحسب میلیثانیه (پیشفرض ۳۰ دقیقه).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `FREE_PROXY_1PROXY_ENABLED`                    | `true`                                                                      | `src/lib/freeProxyProviders/oneproxy.ts`                                  | منبع پراکسی رایگان 1proxy را فعال میکند. برای غیرفعالکردن، مقدار `false` را تنظیم کنید.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `FREE_PROXY_1PROXY_API_URL`                    | _(به oneproxy.ts مراجعه کنید)_                                              | `src/lib/freeProxyProviders/oneproxy.ts`                                  | بازنویسی URL مربوط به API سرویس 1proxy.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `FREE_PROXY_1PROXY_MAX`                        | `500`                                                                       | `src/lib/freeProxyProviders/oneproxy.ts`                                  | حداکثر تعداد پراکسیهای دریافتشده در هر همگامسازی از 1proxy.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `FREE_PROXY_1PROXY_MIN_QUALITY`                | `50`                                                                        | `src/lib/freeProxyProviders/oneproxy.ts`                                  | حداقل آستانهٔ امتیاز کیفیت برای واردکردن داده از 1proxy.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `FREE_PROXY_PROXIFLY_ENABLED`                  | `true`                                                                      | `src/lib/freeProxyProviders/proxifly.ts`                                  | منبع پراکسی رایگان Proxifly را فعال میکند. برای غیرفعالکردن، مقدار `false` را تنظیم کنید.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `FREE_PROXY_PROXIFLY_QUANTITY`                 | `100`                                                                       | `src/lib/freeProxyProviders/proxifly.ts`                                  | تعداد پراکسیهایی که در هر همگامسازی Proxifly دریافت میشوند.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `FREE_PROXY_PROXIFLY_ANONYMITY`                | `elite`                                                                     | `src/lib/freeProxyProviders/proxifly.ts`                                  | فیلتر سطح ناشناسبودن برای Proxifly (`elite`، `anonymous`، `transparent`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `FREE_PROXY_IPLOCATE_ENABLED`                  | `false`                                                                     | `src/lib/freeProxyProviders/iplocate.ts`                                  | منبع پراکسی رایگان IPLocate را فعال میکند. فقط بهصورت اختیاری.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `FREE_PROXY_IPLOCATE_BASE_URL`                 | `https://raw.githubusercontent.com/iplocate/free-proxy-list/main/protocols` | `src/lib/freeProxyProviders/iplocate.ts`                                  | بازنویسی URL پایهٔ فهرست پراکسی IPLocate.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `FREE_PROXY_WEBSHARE_ENABLED`                  | `true`                                                                      | `src/lib/freeProxyProviders/webshare.ts`                                  | منبع استخر پراکسی Webshare را فعال میکند. برای غیرفعالکردن، مقدار `false` را تنظیم کنید؛ همچنین لازم است `FREE_PROXY_WEBSHARE_API_KEY` تنظیم شده باشد.                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `FREE_PROXY_WEBSHARE_API_KEY`                  | _(هیچکدام)_                                                                 | `src/lib/freeProxyProviders/webshare.ts`                                  | توکن API حساب Webshare (`Authorization: Token <key>`). الزامی است — بدون آن ارائهدهنده غیرفعال میماند.                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `FREE_PROXY_WEBSHARE_API_URL`                  | `https://proxy.webshare.io/api/v2/proxy/list/`                              | `src/lib/freeProxyProviders/webshare.ts`                                  | بازنویسی URL مربوط به API فهرست پراکسی Webshare.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `FREE_PROXY_WEBSHARE_MAX`                      | `500`                                                                       | `src/lib/freeProxyProviders/webshare.ts`                                  | حداکثر تعداد پراکسیهای واردشده در هر همگامسازی Webshare.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `NEXT_PUBLIC_VERCEL_RELAY_ENABLED`             | `true`                                                                      | `src/app/(dashboard)/…/ProxyPoolTab.tsx`                                  | دکمهٔ Deploy Vercel Relay را در زبانهٔ Proxy Pool نمایش میدهد یا پنهان میکند.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `VERCEL_API_BASE`                              | `https://api.vercel.com`                                                    | `src/app/api/settings/proxy/vercel-deploy/route.ts`                       | بازنویسی URL پایهٔ API مربوط به Vercel (برای آزمون).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `NEXT_PUBLIC_VERCEL_RELAY_DEFAULT_PROJECT`     | `omniroute-relay`                                                           | `src/app/(dashboard)/…/VercelRelayModal.tsx`                              | نام پیشفرض پروژه که از قبل در پنجرهٔ استقرار Vercel Relay وارد میشود.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `TAILSCALE_BIN`                                | _(تشخیص خودکار)_                                                            | `src/lib/tailscaleTunnel.ts`                                              | مسیر صریح فایل اجرایی `tailscale`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `TAILSCALED_BIN`                               | _(تشخیص خودکار)_                                                            | `src/lib/tailscaleTunnel.ts`                                              | مسیر صریح فایل اجرایی daemon مربوط به `tailscaled`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `TAILSCALE_AUTHKEY`                            | _(تنظیمنشده)_                                                               | `src/lib/tailscaleTunnel.ts`                                              | کلید احراز هویت ازپیشاشتراکگذاشتهشدهٔ Tailscale برای اجرای غیرتعاملی/بدون رابط `tailscale up` (از طریق `--auth-key=` ارسال میشود). اگر تنظیم نشده باشد، ورود به URL تعاملی احراز هویت در مرورگر بازمیگردد.                                                                                                                                                                                                                                                                                                                                                               |
| `NGROK_AUTHTOKEN`                              | _(تنظیمنشده)_                                                               | `src/lib/ngrokTunnel.ts`                                                  | تونلهای خروجی ngrok را احراز هویت میکند.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `DB_BACKUP_MAX_FILES`                          | `20`                                                                        | `src/lib/db/backup.ts`                                                    | حداکثر تعداد فایلهای پشتیبان SQLite که هنگام پاکسازی دستی/زمانبندیشدهٔ پشتیبان نگه داشته میشوند. snapshotهای مهاجرت بر اساس محتوا آدرسدهی میشوند و برای وضعیت یکسان پایگاه داده دوباره استفاده میشوند؛ در پنجرهٔ مهاجرت همزمان حذف نمیشوند. مقدار ذخیرهشده در Settings → Database backup retention را بازنویسی میکند.                                                                                                                                                                                                                                                    |
| `DB_BACKUP_RETENTION_DAYS`                     | `0`                                                                         | `src/lib/db/backup.ts`                                                    | حداکثر عمر (روز) فایلها در پاکسازی دستی/زمانبندیشدهٔ پشتیبان. `0` حذف بر اساس سن را غیرفعال میکند. snapshotهای مهاجرت در پنجرهٔ مهاجرت همزمان حذف نمیشوند. مقدار ذخیرهشده در Settings → Database backup retention را بازنویسی میکند.                                                                                                                                                                                                                                                                                                                                     |
| `OMNIROUTE_BACKUP_SCHEDULE_JOB_INTERVAL_MS`    | `30000`                                                                     | `src/lib/jobs/backupScheduleJob.ts`                                       | فاصلهٔ tick (میلیثانیه) کار سمت سرور که `backup-schedule.json` را اجرا میکند. باید بسیار کمتر از دقت یکدقیقهای cron باقی بماند؛ مقادیر کمتر از `5000` یا مقادیر تجزیهناپذیر به `30000` بازمیگردند.                                                                                                                                                                                                                                                                                                                                                                       |
| `CONTAINER_HOST`                               | `docker`                                                                    | `scripts/check-permissions.sh`                                            | راهنمای runtime کانتینر برای بررسی مجوزهای entrypoint. برای هر توپولوژی Podman، مقدار `podman` را تنظیم کنید. چون کانتینر نمیتواند تشخیص دهد موتور محلی است یا از طریق Podman Machine در دسترس قرار گرفته، هشدار نسبت به توپولوژی خنثی میماند و به `contrib/podman/README.md` اشاره میکند.                                                                                                                                                                                                                                                                               |
| `QUOTA_STORE_DRIVER`                           | `sqlite`                                                                    | `src/lib/quota/storeFactory.ts`                                           | بکاند مخزن مصرف سهمیهٔ اشتراکی: `sqlite` (پیشفرض) یا `redis`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `QUOTA_STORE_REDIS_URL`                        | _(تنظیمنشده)_                                                               | `src/lib/quota/storeFactory.ts`                                           | رشتهٔ اتصال Redis که هنگام `QUOTA_STORE_DRIVER=redis` استفاده میشود (برای مثال، `redis://localhost:6379`).                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `QUOTA_SATURATION_THRESHOLD`                   | `0.5`                                                                       | `src/lib/quota/enforce.ts`                                                | نسبت اشباع استخر (0..1)؛ در این مقدار یا بالاتر، استخر وارد حالت سختگیرانه میشود (بدون قرضگرفتن).                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `QUOTA_SOFT_DEPRIORITIZE_FACTOR`               | `0.7`                                                                       | `open-sse/services/combo.ts`                                              | ضریب امتیاز (0..1) که وقتی سیاست نرم سهمیه اولویت یک هدف را کاهش میدهد، روی آن اعمال میشود.                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `STATUS_SOFT_DEPRIORITIZE_FACTOR`              | `0.5`                                                                       | `open-sse/services/combo/autoStrategy.ts`                                 | ضریب امتیاز (0..1) که در امتیازدهی خودکار combo، وقتی قطع پیشپرواز سهمیه خاموش است، روی ارائهدهندهٔ تمامشده (`credits_exhausted`/`rate_limited`) اعمال میشود (#4540).                                                                                                                                                                                                                                                                                                                                                                                                    |
| `QUOTA_CONSUMPTION_RETENTION_DAYS`             | `14`                                                                        | `src/lib/db/quotaConsumption.ts`                                          | پنجرهٔ نگهداری (روز) برای bucketهای `quota_consumption` پیش از GC (`gcQuotaConsumption`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `QUOTA_PREFLIGHT_CUTOFF_ENABLED`               | `false`                                                                     | `src/lib/resilience/settings.ts`                                          | اختیاری (بهطور پیشفرض خاموش): قطع سخت سهمیه در مسیریابی خودکار را فعال میکند تا نامزدهای دارای سهمیهٔ کم پیش از امتیازدهی حذف شوند.                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `OMNIROUTE_AUTO_FREE_FALLBACK_TO_FULL_POOL`    | `false`                                                                     | `open-sse/services/autoCombo/virtualFactory.ts`                           | اختیاری (بهطور پیشفرض خاموش): وقتی فیلتر `auto/<category>:<tier>` هیچ نامزد متصلی پیدا نمیکند، بهجای بازگرداندن استخر خالی، رفتار قدیمی بازگشت به کل استخر (بدون فیلتر) را بازیابی میکند. خاموشبودن پیشفرض باعث میشود `:free` بهمعنای «فقط ردهٔ رایگان» باشد.                                                                                                                                                                                                                                                                                                            |
| `OMNIROUTE_CHAOS_MAX_PANEL`                    | `5`                                                                         | `open-sse/services/autoCombo/virtualFactory.ts`                           | سقف اندازهٔ پنل (محدودشده به 1–10) برای گونهٔ پخش `auto/*:chaos` — هر درخواست حداکثر به این تعداد مدل متنوع از نظر ارائهدهنده منشعب میشود.                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `OMNIROUTE_CHAOS_MIN_PANEL`                    | _(پیشفرض موتور)_                                                            | `open-sse/services/autoCombo/virtualFactory.ts`                           | تنظیم حداقل اندازهٔ پنل که به کنترلکنندهٔ پخش chaos ارسال میشود؛ تنظیمنشدن، پیشفرض موتور را حفظ میکند.                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `OMNIROUTE_CHAOS_PANEL_TIMEOUT_MS`             | _(پیشفرض موتور)_                                                            | `open-sse/services/autoCombo/virtualFactory.ts`                           | مهلت پایان سخت (میلیثانیه) برای کل انشعاب پنل chaos؛ تنظیمنشدن، پیشفرض موتور را حفظ میکند.                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `GROK_AUTH_PATH`                               | `~/.grok/auth.json`                                                         | `open-sse/services/grokQuotaFetcher.ts`                                   | مسیر `auth.json` مربوط به CLI سرویس Grok که برای دریافت سهمیهٔ هفتگی grok-web استفاده میشود؛ برای آزمونها یا نصب غیراستاندارد CLI بازنویسی کنید.                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `AGENTBRIDGE_UPSTREAM_CA_CERT`                 | _(تنظیمنشده)_                                                               | `src/mitm/manager.ts`                                                     | گواهی CA اضافی (PEM) مورد اعتماد برای اتصالهای TLS بالادستی AgentBridge.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `INSPECTOR_BUFFER_SIZE`                        | `1000`                                                                      | `src/mitm/inspector/buffer.ts`                                            | حداکثر تعداد درخواستهای ثبتشدهای که در ring buffer بازرس ترافیک نگه داشته میشوند.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `INSPECTOR_MAX_BODY_KB`                        | `1024`                                                                      | `src/mitm/inspector/buffer.ts`                                            | حداکثر اندازهٔ بدنهٔ درخواست/پاسخ ثبتشده (KB) پیش از کوتاهسازی.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `INSPECTOR_HTTP_PROXY_PORT`                    | `8080`                                                                      | `src/mitm/inspector/httpProxyServer.ts`                                   | پورت محلی پراکسی HTTP مربوط به بازرس ترافیک.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `INSPECTOR_HTTP_PROXY_AUTOSTART`               | `false`                                                                     | `src/mitm/inspector/httpProxyServer.ts`                                   | پراکسی HTTP بازرس را هنگام راهاندازی بهطور خودکار شروع میکند.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `INSPECTOR_TLS_INTERCEPT`                      | `false`                                                                     | `src/lib/inspector/captureState.ts`                                       | رهگیری TLS ‏(MITM) را برای ترافیک HTTPS ثبتشده فعال میکند.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `INSPECTOR_LLM_HOSTS_EXTRA`                    | _(تنظیمنشده)_                                                               | `src/lib/inspector/captureState.ts`                                       | نامهای میزبان اضافی (جداشده با ویرگول) که برای ثبت بهعنوان endpointهای LLM در نظر گرفته میشوند.                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `INSPECTOR_MASK_SECRETS`                       | `true`                                                                      | `src/mitm/inspector/buffer.ts`                                            | اطلاعات محرمانه (هدرهای احراز هویت / کلیدهای API) را در ترافیک ثبتشده پوشانده میکند.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `INSPECTOR_SYSTEM_PROXY_GUARD_MINUTES`         | `30`                                                                        | `src/app/api/tools/traffic-inspector/capture-modes/system-proxy/route.ts` | تعداد دقیقه پیش از آنکه محافظ پراکسی سیستم، تنظیمات پراکسی سیستمعامل را خودکار بازگرداند.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `INSPECTOR_INTERNAL_INGEST_TOKEN`              | _(خودکار)_                                                                  | `src/app/api/tools/traffic-inspector/internal/ingest/route.ts`            | توکنی که ورود داخلی دادههای ثبتشده به بازرس را احراز هویت میکند.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `PLAYGROUND_COMPARE_MAX_COLUMNS`               | `4`                                                                         | `src/app/(dashboard)/dashboard/playground/`                               | حداکثر تعداد ستونهای کنارهم در حالت مقایسهٔ Playground.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `PLAYGROUND_IMPROVE_PROMPT_DEFAULT_MODEL`      | _(تنظیمنشده)_                                                               | `src/app/(dashboard)/dashboard/playground/`                               | مدل پیشفرض برای اقدام «بهبود پرامپت» در Playground (اگر تنظیم نشده باشد، به مدل فعال بازمیگردد).                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `BIFROST_ENABLED`                              | `1`                                                                         | `src/app/api/v1/relay/chat/completions/bifrost/route.ts`                  | کلید توقف اصلی برای پراکسی sidecar مربوط به Bifrost. وقتی روی `0` تنظیم شود، مسیر پاسخ 503 را با هدر `X-Bifrost-Killswitch` بازمیگرداند و اپراتور به مسیر TS هدایت میشود. برای غیرفعالکردن sidecar بدون استقرار مجدد استفاده کنید (رخداد روتر tier-1، چرخش کلید).                                                                                                                                                                                                                                                                                                        |
| `BIFROST_BASE_URL`                             | _(تنظیمنشده)_                                                               | `src/app/api/v1/relay/chat/completions/bifrost/route.ts`                  | وقتی تنظیم شود، مسیر پراکسی sidecar مربوط به Bifrost ترافیک `/v1/chat/completions` را بهجای کنترلکنندهٔ relay در TS به این درگاه Go ارسال میکند. تنظیمنشده → پاسخ 503 همراه با fallback. اسلش انتهایی حذف میشود.                                                                                                                                                                                                                                                                                                                                                         |
| `BIFROST_PORT`                                 | `8080`                                                                      | `src/lib/services/bootstrap.ts`                                           | پورتی که سرویس تعبیهشده و نظارتشدهٔ Bifrost هنگام مدیریت چرخهٔ حیات sidecar توسط OmniRoute به آن متصل میشود (`127.0.0.1:<port>`). پیشفرض `8080` است.                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `BIFROST_API_KEY`                              | _(تنظیمنشده)_                                                               | `src/app/api/v1/relay/chat/completions/bifrost/route.ts`                  | کلید API درگاه Bifrost (بهشکل `Authorization: Bearer ...` ارسال میشود). اگر تنظیم نشده باشد، مسیر انتظار دارد درخواست دارای یک کلید API معتبر OmniRoute باشد؛ این کلید فقط برای احراز هویت سمت درگاه است.                                                                                                                                                                                                                                                                                                                                                                |
| `BIFROST_STREAMING_ENABLED`                    | `true`                                                                      | `src/app/api/v1/relay/chat/completions/bifrost/route.ts`                  | وقتی true باشد، مسیر sidecar مربوط به Bifrost پاسخها را بهجای executor استریم TS، از طریق SSE و درگاه بازمیگرداند. برای اجبار پاسخهای JSON غیرجریانی از طریق درگاه، مقدار `0` را تنظیم کنید.                                                                                                                                                                                                                                                                                                                                                                             |
| `BIFROST_TIMEOUT_MS`                           | `30000`                                                                     | `src/app/api/v1/relay/chat/completions/bifrost/route.ts`                  | مهلت پایان هر درخواست هنگام پراکسیکردن به درگاه Bifrost (میلیثانیه). در صورت پایان مهلت، مسیر از طریق هدر `X-Bifrost-Fallback` مسیر relay در TS را بازمیگرداند.                                                                                                                                                                                                                                                                                                                                                                                                          |
| `OMNIROUTE_BIFROST_KEY`                        | _(تنظیمنشده)_                                                               | `src/app/api/v1/relay/chat/completions/bifrost/route.ts`                  | نام مستعار `BIFROST_API_KEY` (مورد استفادهٔ اسکریپتهایی که متغیر محیطی را از طریق `OMNIROUTE_*` میخوانند). اگر هر دو تنظیم شده باشند، `BIFROST_API_KEY` اولویت دارد.                                                                                                                                                                                                                                                                                                                                                                                                     |
| `OMNIROUTE_RELAY_BACKEND`                      | `ts` / `auto`                                                               | `src/app/api/v1/relay/chat/completions/routingBackend.ts`                 | بکاند relay برای `/api/v1/relay/chat/completions`: `ts \| bifrost \| auto`. ‏`ts` = relay تایپاسکریپت (وقتی Bifrost پیکربندی نشده، پیشفرض است)؛ `auto` وقتی `BIFROST_BASE_URL` تنظیم شده و `BIFROST_ENABLED` ≠ `0` باشد Bifrost را انتخاب میکند و اگر sidecar در دسترس نباشد بهصورت خودکار به TS بازمیگردد؛ `bifrost` استفاده از Bifrost را اجباری میکند (سختگیرانه، بدون fallback). محافظ احراز هویت/محدودیت نرخ/injection/فهرست مجاز همیشه ابتدا در مسیر Next اجرا میشوند. پاسخها شامل `X-Routing-Backend` / `X-Routing-Fallback` / `X-Routing-Fallback-Reason` هستند. |
| `RELAY_ROUTING_BACKEND`                        | _(تنظیمنشده)_                                                               | `src/app/api/v1/relay/chat/completions/routingBackend.ts`                 | نام مستعار پذیرفتهشده برای `OMNIROUTE_RELAY_BACKEND` (با همان مقادیر `ts \| bifrost \| auto`). اگر هر دو تنظیم شده باشند، `OMNIROUTE_RELAY_BACKEND` اولویت دارد.                                                                                                                                                                                                                                                                                                                                                                                                         |
| `OMNIROUTE_BIFROST_FAILURE_COOLDOWN_MS`        | `5000`                                                                      | `src/app/api/v1/relay/chat/completions/bifrostCooldown.ts`                | دورهٔ انتظار (میلیثانیه) پس از ناموفقبودن یک پرش به sidecar مربوط به Bifrost در حالت `auto`، پیش از آنکه relay دوباره sidecar را امتحان کند؛ تا پایان دورهٔ انتظار مستقیماً به مسیر TS هدایت میشود و سپس دوباره کاوش میکند. `0` آن را غیرفعال میکند. فقط وقتی `OMNIROUTE_RELAY_BACKEND=auto` باشد اعمال میشود.                                                                                                                                                                                                                                                           |
| `OMNIROUTE_TLS_CERT`                           | _(تنظیمنشده)_                                                               | `bin/cli/commands/serve.mjs`                                              | مسیر یک گواهی TLS با قالب PEM برای ارائهٔ `omniroute serve` روی HTTPS (معادل `--tls-cert`). باید همراه با `OMNIROUTE_TLS_KEY` باشد؛ سپس سرور مستقل TLS را روی همان listener خاتمه میدهد (`wss://` بدون تغییر کار میکند). تنظیمنشده → HTTP ساده. ارائهٔ تنها یکی از گواهی/کلید یا مسیری خواندهنشدنی، هشداری ثبت میکند و HTTP را حفظ میکند.                                                                                                                                                                                                                                |
| `OMNIROUTE_TLS_KEY`                            | _(تنظیمنشده)_                                                               | `bin/cli/commands/serve.mjs`                                              | مسیر کلید خصوصی TLS با قالب PEM برای HTTPS در `omniroute serve` (معادل `--tls-key`). باید همراه با `OMNIROUTE_TLS_CERT` باشد. به `OMNIROUTE_TLS_CERT` مراجعه کنید.                                                                                                                                                                                                                                                                                                                                                                                                       |
| `OMNIROUTE_LOCAL_ENDPOINTS_ENABLED`            | `0`                                                                         | `src/lib/security/localEndpoints.ts`                                      | کلید اصلی مسیرهای `/api/local/*`. وقتی تنظیم نشده یا `0` باشد، همهٔ مسیرهای `/api/local/*` در محیط تولید پاسخ 503 میدهند. در استقرارهای غیر-loopback برای فعالکردن راهانداز Redis و آغازگرهای مشابه سرویس محلی با یک کلیک، باید `1` باشد. همراه با طبقهبندی محافظ مسیر `isLocalOnlyPath()` دفاع چندلایه ایجاد میکند (`LOCAL_ONLY_API_PREFIXES` در `src/server/authz/routeGuard.ts`).                                                                                                                                                                                     |
| `OMNIROUTE_LOCAL_ENDPOINTS_TOKEN`              | _(تنظیمنشده)_                                                               | `src/lib/security/localEndpoints.ts`                                      | توکن Bearer برای فراخوانندگان `/api/local/*` که روی loopback نیستند (برای مثال، برنامهٔ دسکتاپ). وقتی تنظیم شود، درخواستها از IPهای غیر-loopback باید `Authorization: Bearer <token>` را حمل کنند. وقتی `OMNIROUTE_LOCAL_ENDPOINTS_ENABLED=1` در استقرارهای غیر-loopback باشد، الزامی است.                                                                                                                                                                                                                                                                               |
| `OMNIROUTE_REDIS_CONTAINER_NAME`               | `omniroute-redis`                                                           | `bin/cli/commands/redis.mjs`                                              | نام کانتینر راهانداز یککلیکی Redis (`omniroute redis up`). هم CLI و هم رابط گرافیکی `RedisLauncherPanel` از آن استفاده میکنند.                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `OMNIROUTE_REDIS_HOST_PORT`                    | `6379`                                                                      | `bin/cli/commands/redis.mjs`                                              | پورت میزبان برای راهانداز یککلیکی Redis. اگر میزبان از قبل به 6379 متصل است، آن را تغییر دهید. پورت داخلی کانتینر 6379 باقی میماند.                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `OMNIROUTE_REDIS_BIND_HOST`                    | `127.0.0.1`                                                                 | `bin/cli/commands/redis.mjs`                                              | رابط میزبانی که راهانداز یککلیکی Redis روی آن منتشر میشود. راهانداز Redis را بدون گذرواژه شروع میکند؛ بنابراین اتصال به `0.0.0.0` یک Redis بدون احراز هویت را در اختیار همهٔ میزبانهای LAN شما قرار میدهد — فقط وقتی دامنهٔ اتصال را گسترش دهید که خودتان نیز برای نمونه گذرواژه تنظیم کرده باشید.                                                                                                                                                                                                                                                                       |
| `REDIS_BIND_HOST`                              | `127.0.0.1`                                                                 | `docker-compose.yml`                                                      | رابط میزبانی که docker-compose، ‏sidecar مربوط به Redis را روی آن منتشر میکند (#9286). ‏Redis در compose بدون `requirepass` اجرا میشود؛ کانتینرهای برنامه از طریق شبکهٔ compose (`redis:6379`) به آن دسترسی دارند — پورت منتشرشده فقط برای ابزارهای سمت میزبان است. `0.0.0.0` یک Redis بدون احراز هویت را در معرض کل LAN قرار میدهد.                                                                                                                                                                                                                                     |
| `REDIS_PORT`                                   | `6379`                                                                      | `docker-compose.yml`                                                      | پورت میزبان برای sidecar مربوط به Redis در compose.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `APP_BIND_HOST`                                | `127.0.0.1`                                                                 | `docker-compose.yml`, `docker-compose.prod.yml`                           | رابط میزبانی که docker-compose پورتهای داشبورد/API/live-WS خود برنامه را روی آن منتشر میکند (#12568). با توجه به اینکه `REQUIRE_API_KEY=false` بهعنوان پیشفرض `.env.example` ارائه میشود، `0.0.0.0` پراکسی ناشناس LLM در `/v1` را در معرض کل LAN/WAN قرار میدهد — فقط وقتی دامنهٔ اتصال را گسترش دهید که `REQUIRE_API_KEY=true` باشد یا یک reverse proxy جلویی احراز هویت خود را اعمال کند.                                                                                                                                                                              |
| `QDRANT_BIND_HOST`                             | `127.0.0.1`                                                                 | `docker-compose.yml`                                                      | رابط میزبانی که docker-compose، ‏sidecar حافظهٔ Qdrant را روی آن منتشر میکند (#12578). همان ملاحظات افشای LAN مربوط به `REDIS_BIND_HOST` اعمال میشود.                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `BIFROST_BIND_HOST`                            | `127.0.0.1`                                                                 | `docker-compose.yml`                                                      | رابط میزبانی که docker-compose، ‏sidecar روتر Bifrost را روی آن منتشر میکند (#12578). همان ملاحظات افشای LAN مربوط به `REDIS_BIND_HOST` اعمال میشود.                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `REDIS_KEY_PREFIX`                             | `omniroute:`                                                                | `src/shared/utils/rateLimiter.ts`                                         | پیشوند فضای نام که به هر کلید Redis مربوط به OmniRoute اعمال میشود (محدودکنندهٔ نرخ، cache احراز هویت، مخزن سهمیه، circuit breaker مربوط به warmup). وقتی نمونهٔ Redis با برنامههای دیگر مشترک است، از تداخل کلیدها جلوگیری میکند (#11042).                                                                                                                                                                                                                                                                                                                              |
| `OMNIROUTE_INTERNAL_SERVICE_TOKEN`             | _(تنظیمنشده — سازوکار غیرفعال است)_                                         | `src/lib/api/internalServiceAuth.ts`                                      | راز مشترک برای پرشهای داخلی REST با حفظ هویت (#9260): مؤلفههای OmniRoute که مسیرهای محلی دیگر OmniRoute را فراخوانی میکنند، آن را بهشکل `x-omniroute-internal-service-token` ارسال میکنند تا هویت فراخوانندهٔ اصلی حفظ شود. با `timingSafeEqual` مقایسه میشود.                                                                                                                                                                                                                                                                                                           |
| `OMNIROUTE_INTERNAL_SERVICE_TOKEN_FILE`        | _(تنظیمنشده)_                                                               | `src/lib/api/internalServiceAuth.ts`                                      | گونهٔ فایل محرمانه برای توکن سرویس داخلی: مسیر فایلی که محتوای trimشدهٔ آن توکن است. فقط وقتی متغیر درونخطی تنظیم نشده باشد بررسی میشود.                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `OPENROUTER_PROVIDER_STATS_ENABLED`            | `true`                                                                      | `src/lib/catalog/openrouterProviderStats.ts`                              | فهرست ارائهدهندگان داشبورد را با آمار رتبهبندی هفتگی OpenRouter غنی میکند (#9324). بهطور پیشفرض روشن است؛ برای ردکردن کامل دریافت پسزمینه، مقدار `false` را تنظیم کنید (غیرمسدودکننده و هرگز بحرانی نیست).                                                                                                                                                                                                                                                                                                                                                               |
| `OPENROUTER_PROVIDER_STATS_TTL_MS`             | `86400000` (۲۴ ساعت)                                                        | `src/lib/catalog/openrouterProviderStats.ts`                              | TTL حافظهٔ نهان snapshot آمار ارائهدهندگان OpenRouter، برحسب میلیثانیه.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `OMNIROUTE_REDIS_IMAGE`                        | `redis:7-alpine`                                                            | `bin/cli/commands/redis.mjs`                                              | image مربوط به Redis که راهانداز یککلیکی Redis استفاده میکند. در صورت نیاز آن را با `redis:8-alpine` یا mirror یک registry خصوصی بازنویسی کنید.                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `QDRANT_HOST`                                  | `qdrant`                                                                    | _(پروفایل خوشهٔ اختیاری)_                                                 | نام میزبان sidecar مربوط به Qdrant وقتی `--profile memory` فعال است. پیشفرض به نام سرویس qdrant درون شبکه اشاره میکند؛ برای استقرار خارجی بازنویسی کنید. فقط وقتی `qdrantEnabled` در کد `true` باشد مصرف میشود (`src/lib/memory/vectorStore.ts:108`).                                                                                                                                                                                                                                                                                                                    |
| `QDRANT_PORT`                                  | `6333`                                                                      | _(پروفایل خوشهٔ اختیاری)_                                                 | پورت REST مربوط به sidecar سرویس Qdrant.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `QDRANT_GRPC_PORT`                             | `6334`                                                                      | _(پروفایل خوشهٔ اختیاری)_                                                 | پورت gRPC مربوط به sidecar سرویس Qdrant. کتابخانههای کلاینتی که برای عملیات جریانی gRPC را به REST ترجیح میدهند، از آن استفاده میکنند.                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `QDRANT_API_KEY`                               | _(تنظیمنشده)_                                                               | _(پروفایل خوشهٔ اختیاری)_                                                 | کلید API اختیاری برای Qdrant Cloud یا یک نمونهٔ داخلی احرازشده. خالی → هدر `api-key` ارسال نمیشود.                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `QDRANT_COLLECTION`                            | `omniroute-memory`                                                          | _(پروفایل خوشهٔ اختیاری)_                                                 | نام collection برای embeddingهای حافظهٔ مکالمه در OmniRoute. در نخستین اجرا با ابعاد `QDRANT_VECTOR_SIZE` ایجاد میشود.                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `QDRANT_EMBEDDING_MODEL`                       | `text-embedding-3-small`                                                    | _(پروفایل خوشهٔ اختیاری)_                                                 | نام مدل embedding پیشفرض که در فرادادهٔ collection مربوط به Qdrant ثبت میشود. embeddingهای واقعی توسط ارائهدهندهای تولید میشوند که فیلد `embeddingModel` در تنظیمات OmniRoute به آن اشاره دارد.                                                                                                                                                                                                                                                                                                                                                                          |
| `QDRANT_VECTOR_SIZE`                           | `1536`                                                                      | _(پروفایل خوشهٔ اختیاری)_                                                 | بُعد بردار embedding. باید با مدلی که برای embedding استفاده میکنید مطابقت داشته باشد (text-embedding-3-small → 1536؛ ada-002 → 1536؛ nomic-embed-text → 768).                                                                                                                                                                                                                                                                                                                                                                                                           |
| `QDRANT_HNSW_EF_CONSTRUCT`                     | `128`                                                                       | _(پروفایل خوشهٔ اختیاری)_                                                 | دقت HNSW هنگام ساخت ایندکس. مقدار بالاتر = ساخت کندتر و جستوجوی سریعتر.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `OMNIROUTE_ROTATION_ENABLED`                   | `true`                                                                      | `open-sse/services/rotationConfig.ts`                                     | کلید اصلی چرخش حساب قابلپیکربندی توسط اپراتور. وقتی `false` باشد، هیچیک از کلاسهای `OMNIROUTE_ROTATE_*` زیر fallback حساب را فعال نمیکنند (حالت خاموش اصلی، کلاسهای 429/500/502 را که بهطور پیشفرض فعالاند نیز مسدود میکند). به یک فرانتاند نظارتکننده (برای مثال، برنامهٔ دسکتاپ VibeProxy) اجازه میدهد قوانین چرخش خود را روی موتور fallback حساب بکاند بازتاب دهد.                                                                                                                                                                                                    |
| `OMNIROUTE_ROTATION_RATE_LIMIT_RESET_SECONDS`  | `0`                                                                         | `open-sse/services/rotationConfig.ts`                                     | دورهٔ انتظار (ثانیه) که وقتی سرویس بالادستی هیچ راهنمای صریحی برای زمان بازنشانی ارائه نمیکند، روی حساب محدودشده از نظر نرخ اعمال میشود. `0` = بهجای بازنویسی ثابت، از دورهٔ انتظار پیشفرض موتور استفاده شود.                                                                                                                                                                                                                                                                                                                                                            |
| `OMNIROUTE_ROTATION_DISABLE_TAG_WITHOUT_RESET` | `true`                                                                      | `open-sse/services/rotationConfig.ts`                                     | بازتاب ترجیح فرانتاند برای «بدون زمان بازنشانی، برچسب محدودیت نرخ نزن».                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `OMNIROUTE_ROTATE_ON_429`                      | `true`                                                                      | `open-sse/services/rotationConfig.ts`                                     | فعالسازی fallback برای خطاهای وضعیت `429`. وقتی `false` باشد (و `OMNIROUTE_ROTATION_ENABLED=true`)، یک `429` دیگر چرخش حساب را فعال نمیکند و در عوض به کلاینت بازگردانده میشود.                                                                                                                                                                                                                                                                                                                                                                                          |
| `OMNIROUTE_ROTATE_429_THRESHOLD`               | `1`                                                                         | `open-sse/services/rotationConfig.ts`                                     | تعداد خطاهای `429` در محدودهٔ `OMNIROUTE_ROTATE_429_WINDOW_SECONDS` که پیش از چرخش حساب لازم است. `1` (پیشفرض) فوراً چرخش میدهد و رفتار تاریخی را حفظ میکند.                                                                                                                                                                                                                                                                                                                                                                                                             |
| `OMNIROUTE_ROTATE_429_WINDOW_SECONDS`          | `120`                                                                       | `open-sse/services/rotationConfig.ts`                                     | پنجرهٔ لغزان (ثانیه) که در آن خطاهای `429` برای رسیدن به `OMNIROUTE_ROTATE_429_THRESHOLD` شمرده میشوند.                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `OMNIROUTE_ROTATE_ON_500`                      | `true`                                                                      | `open-sse/services/rotationConfig.ts`                                     | فعالسازی fallback برای خطاهای سرور `5xx` (بهجز `502` که کلاس خود را دارد). وقتی `false` باشد، این خطاها دیگر چرخش حساب را فعال نمیکنند.                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `OMNIROUTE_ROTATE_500_THRESHOLD`               | `1`                                                                         | `open-sse/services/rotationConfig.ts`                                     | تعداد خطاهای `5xx` در محدودهٔ `OMNIROUTE_ROTATE_500_WINDOW_SECONDS` که پیش از چرخش حساب لازم است. `1` (پیشفرض) فوراً چرخش میدهد.                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `OMNIROUTE_ROTATE_500_WINDOW_SECONDS`          | `120`                                                                       | `open-sse/services/rotationConfig.ts`                                     | پنجرهٔ لغزان (ثانیه) که در آن خطاهای `5xx` برای رسیدن به `OMNIROUTE_ROTATE_500_THRESHOLD` شمرده میشوند.                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `OMNIROUTE_ROTATE_ON_502`                      | `true`                                                                      | `open-sse/services/rotationConfig.ts`                                     | فعالسازی fallback برای خطاهای `502` (درگاه نامعتبر). وقتی `false` باشد، خطاهای `502` دیگر چرخش حساب را فعال نمیکنند.                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `OMNIROUTE_ROTATE_502_THRESHOLD`               | `1`                                                                         | `open-sse/services/rotationConfig.ts`                                     | تعداد خطاهای `502` در محدودهٔ `OMNIROUTE_ROTATE_502_WINDOW_SECONDS` که پیش از چرخش حساب لازم است. `1` (پیشفرض) فوراً چرخش میدهد.                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `OMNIROUTE_ROTATE_502_WINDOW_SECONDS`          | `120`                                                                       | `open-sse/services/rotationConfig.ts`                                     | پنجرهٔ لغزان (ثانیه) که در آن خطاهای `502` برای رسیدن به `OMNIROUTE_ROTATE_502_THRESHOLD` شمرده میشوند.                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `OMNIROUTE_ROTATE_ON_400`                      | `false`                                                                     | `open-sse/services/rotationConfig.ts`                                     | اختیاری (بهطور پیشفرض خاموش): وقتی `true` باشد، یک `400` ساده (درخواست نامعتبر) نیز چرخش حساب را فعال میکند. این قابلیت فقط افزایشی است — هرگز رفتار موجود موتور را که در آن یک `400` حاوی متن محدودیت نرخ/سهمیه مستقل از این پرچم fallback میکند، مسدود نمیکند.                                                                                                                                                                                                                                                                                                         |
| `OMNIROUTE_ROTATE_400_THRESHOLD`               | `1`                                                                         | `open-sse/services/rotationConfig.ts`                                     | تعداد خطاهای `400` در محدودهٔ `OMNIROUTE_ROTATE_400_WINDOW_SECONDS` که پیش از چرخش حساب لازم است (فقط وقتی `OMNIROUTE_ROTATE_ON_400=true` باشد بررسی میشود).                                                                                                                                                                                                                                                                                                                                                                                                             |
| `OMNIROUTE_ROTATE_400_WINDOW_SECONDS`          | `120`                                                                       | `open-sse/services/rotationConfig.ts`                                     | پنجرهٔ لغزان (ثانیه) که در آن خطاهای `400` برای رسیدن به `OMNIROUTE_ROTATE_400_THRESHOLD` شمرده میشوند.                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |

### زمانبند آمادهسازی Claude

آمادهسازی مبتنی بر cron برای اتصالهای Anthropic OAuth که در آن شرکت داده شده است؛ بهاینترتیب، پنجرهٔ محدودیت نرخ ۵ ساعته بهجای نخستین درخواست واقعی، با یک درخواست زمانبندیشدهٔ ساده باز میشود (#8848). زمانبند غیرفعال است، مگر اینکه `OMNIROUTE_WARMUP_ENABLED` دارای مقدار truthy باشد **و** اتصال در `settings.claudeWarmup.connections` علامتگذاری شده باشد؛ خالی بودن فهرست اتصالها به این معنی است که حتی با فعال بودن متغیر محیطی نیز هیچ اتصالی آمادهسازی نمیشود.

| متغیر                          | پیشفرض                      | فایل منبع                    | توضیحات                                                                                                                                                                                          |
| ------------------------------ | --------------------------- | ---------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `OMNIROUTE_WARMUP_ENABLED`     | _(تنظیمنشده ← غیرفعال)_     | `src/lib/warmupScheduler.ts` | کلید اصلی زمانبند آمادهسازی. مقادیر `1`/`true`/`yes`/`on` را میپذیرد (بدون حساسیت به حروف بزرگ و کوچک و پس از حذف فاصلههای اضافی). هر مقدار دیگر یا تنظیمنشدن آن، زمانبند را غیرفعال نگه میدارد. |
| `OMNIROUTE_WARMUP_CRON`        | `0 7 * * *`                 | `src/lib/warmupScheduler.ts` | عبارت cron پنجفیلدی برای نوبت اجرای آمادهسازی که بدون توجه به ساعت میزبان، در منطقهٔ زمانی `America/Los_Angeles` (منطقهٔ زمانی بازنشانی Anthropic) ارزیابی میشود.                                |
| `OMNIROUTE_WARMUP_CONCURRENCY` | `3`                         | `src/lib/warmupScheduler.ts` | تعداد اتصالهایی که در هر نوبت بهصورت موازی آمادهسازی میشوند. مقدار به بازهٔ `1`-`10` محدود میشود؛ مقدار غیرعددی به `3` بازمیگردد.                                                                |
| `OMNIROUTE_WARMUP_MODEL`       | `claude-3-5-haiku-20241022` | `src/lib/warmupScheduler.ts` | مدل مورد استفاده برای درخواست آمادهسازی. فقط در صورتی آن را تغییر دهید که مدل پیشفرض در طرح شما در دسترس نباشد؛ ارزانترین مدلی را انتخاب کنید که همچنان پنجره را باز میکند.                      |

### نشستهای VNC برای ورود از طریق مرورگر و نام مستعار پوشهٔ داده

Chromium+VNC کانتینریشده برای ثبت تعاملی اعتبارنامههای ورود از طریق مرورگر (`/api/vnc-session`)، بههمراه نام مستعار قدیمی `DATA_DIR`. همهٔ این موارد اختیاری هستند — مقادیر پیشفرض VNC، تصویر همراه `omniroute-vnc-chromium:local` را هدف قرار میدهند و فقط برای یک تصویر کانتینر سفارشی، پورتهای سفارشی یا تنظیم چرخهٔ عمر بازنویسی میشوند.

| متغیر                                 | پیشفرض                                                                   | فایل منبع                                   | توضیحات                                                                                                                                                                                                             |
| ------------------------------------- | ------------------------------------------------------------------------ | ------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `OMNIROUTE_VNC_IMAGE`                 | `omniroute-vnc-chromium:local`                                           | `src/lib/vncSession/manifest.ts`            | برچسب تصویر Docker برای کانتینر ورود Chromium+VNC. `docker/vnc-browser/chromium` را بسازید یا این مقدار را به یک تصویر سفارشی ارجاع دهید.                                                                           |
| `OMNIROUTE_DOCKER_BIN`                | `docker`                                                                 | `src/lib/vncSession/manifest.ts`            | فایل اجرایی زماناجرای کانتینر که برای راهاندازی کانتینر VNC استفاده میشود (برای مثال، آن را روی `podman` تنظیم کنید).                                                                                               |
| `OMNIROUTE_VNC_CONTAINER_VNC_PORT`    | `3000`                                                                   | `src/lib/vncSession/manifest.ts`            | پورت VNC/noVNC که درون کانتینر در معرض دسترسی قرار میگیرد.                                                                                                                                                          |
| `OMNIROUTE_VNC_CONTAINER_CDP_PORT`    | `9223`                                                                   | `src/lib/vncSession/manifest.ts`            | پورت Chrome DevTools Protocol درون کانتینر.                                                                                                                                                                         |
| `OMNIROUTE_VNC_CONTAINER_PROFILE_DIR` | `/config`                                                                | `src/lib/vncSession/manifest.ts`            | مسیر پوشهٔ پروفایل Chromium درون کانتینر.                                                                                                                                                                           |
| `OMNIROUTE_VNC_PROFILE_DIR`           | `$HOME/.omniroute/browser-login-profiles`                                | `src/lib/vncSession/manifest.ts`            | پوشهٔ میزبان که پروفایلهای ماندگار ورود از طریق مرورگر را نگه میدارد.                                                                                                                                               |
| `OMNIROUTE_VNC_IDLE_MS`               | `600000` (۱۰ دقیقه)                                                      | `src/lib/vncSession/manifest.ts`            | مهلت عدم فعالیت (میلیثانیه) پیش از حذف یک نشست VNC غیرفعال.                                                                                                                                                         |
| `OMNIROUTE_VNC_MAX_MS`                | `1800000` (۳۰ دقیقه)                                                     | `src/lib/vncSession/manifest.ts`            | سقف قطعی (میلیثانیه) برای طول عمر یک نشست VNC.                                                                                                                                                                      |
| `OMNIROUTE_VNC_MAX_SESSIONS`          | `4`                                                                      | `src/lib/vncSession/manifest.ts`            | حداکثر تعداد نشستهای همزمان VNC.                                                                                                                                                                                    |
| `OMNIROUTE_VNC_READY_MS`              | `45000`                                                                  | `src/lib/vncSession/manifest.ts`            | مهلت انتظار (میلیثانیه) برای آمادهشدن مرورگر کانتینریشده جهت استفاده از CDP.                                                                                                                                        |
| `OMNIROUTE_VNC_HARVEST_MS`            | `20000`                                                                  | `src/lib/vncSession/manifest.ts`            | مهلت زمانی (میلیثانیه) برای استخراج نشست/کوکیهای ثبتشده پس از تکمیل ورود.                                                                                                                                           |
| `OMNIROUTE_VNC_CHROMIUM_ARGS`         | `--remote-debugging-port=9222 --no-first-run --no-default-browser-check` | `src/lib/vncSession/manifest.ts`            | پرچمهای اضافی خط فرمان که به Chromium کانتینریشده ارسال میشوند.                                                                                                                                                     |
| `OMNIROUTE_VNC_NETWORK`               | `omniroute-vnc-browser-login`                                            | `src/lib/vncSession/manifest.ts`            | شبکهٔ اختصاصی Docker که کانتینر ورود VNC بهجای پل پیشفرض به آن میپیوندد (#12571)، تا کانتینرهای همسطح نتوانند به پورت پل CDP آن دسترسی پیدا کنند.                                                                   |
| `VIBEPROXY_DATA_DIR`                  | _(تنظیمنشده)_                                                            | `open-sse/services/notionThreadSessions.ts` | **نام مستعار قدیمی** برای `DATA_DIR` که فقط پس از تنظیمنبودن هر دو متغیر `DATA_DIR` و `OMNIROUTE_DATA_DIR` بررسی میشود. محل کش نشست رشتهگفتوگوی وب Notion را تعیین میکند (`<dir>/notion-web-thread-sessions.json`). |

---

## 26. چارچوب تست و E2E

توسط `scripts/dev/run-next-playwright.mjs`، `scripts/dev/smoke-electron-packaged.mjs`،
`scripts/dev/run-ecosystem-tests.mjs` و `scripts/build/uninstall.mjs` استفاده میشود. در استقرارهای
محیط عملیاتی، تمام مقادیر زیر را تنظیمنشده باقی بگذارید.

| متغیر                                  | پیشفرض                                            | فایل منبع                                 | توضیحات                                                                                                                                                                                                                                                                 |
| -------------------------------------- | ------------------------------------------------- | ----------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `OMNIROUTE_E2E_BOOTSTRAP_MODE`         | `auth`                                            | `scripts/dev/run-next-playwright.mjs`     | حالت راهاندازی اولیه E2E (`auth`، `fresh`، `reuse`) برای اجراکننده Playwright.                                                                                                                                                                                          |
| `OMNIROUTE_E2E_PASSWORD`               | در صورت نبود، از `INITIAL_PASSWORD` استفاده میکند | `scripts/dev/run-next-playwright.mjs`     | گذرواژه مدیر که به محیط Playwright تزریق میشود.                                                                                                                                                                                                                         |
| `OMNIROUTE_DISABLE_LOCAL_HEALTHCHECK`  | `true`                                            | `scripts/dev/run-next-playwright.mjs`     | غیرفعالکردن نظرسنجی بررسی سلامت محلی هنگام اجرای Playwright.                                                                                                                                                                                                            |
| `OMNIROUTE_DISABLE_TOKEN_HEALTHCHECK`  | `true`                                            | `scripts/dev/run-next-playwright.mjs`     | غیرفعالکردن حلقه بررسی سلامت توکن OAuth هنگام آزمایشها.                                                                                                                                                                                                                 |
| `OMNIROUTE_HEALTHCHECK_SKIP_PROVIDERS` | _(تنظیمنشده)_                                     | `src/lib/tokenHealthCheck.ts`             | ارائهدهندگانی با جداکننده ویرگول که از پیمایش پیشگیرانه نوسازی توکن مستثنا میشوند (برای مثال `codex,openai`). جایگزینی هدفمند برای غیرفعالکردن کامل بررسی سلامت — ارائهدهندگان با TTL کوتاه همچنان نوسازی میشوند، درحالیکه ارائهدهندگان آبشاری فقط واکنشی باقی میمانند. |
| `OMNIROUTE_HIDE_HEALTHCHECK_LOGS`      | `true`                                            | `scripts/dev/run-next-playwright.mjs`     | حذف پیامهای اضافی بررسی سلامت از stdout مربوط به Playwright.                                                                                                                                                                                                            |
| `OMNIROUTE_PLAYWRIGHT_SKIP_BUILD`      | `0`                                               | `scripts/dev/run-next-playwright.mjs`     | ردکردن ساخت تولیدی Next.js پیش از شروع Playwright (بهینهسازی CI).                                                                                                                                                                                                       |
| `OMNIROUTE_SKIP_UNINSTALL_HOOK`        | `0`                                               | `scripts/build/uninstall.mjs`             | ردکردن هوک حذف نصب OmniRoute (برای دستنخورده نگهداشتن `node_modules` در CI استفاده میشود).                                                                                                                                                                              |
| `ECOSYSTEM_SERVER_WAIT_MS`             | `180000`                                          | `scripts/dev/run-ecosystem-tests.mjs`     | زمان انتظار (میلیثانیه) برای سالمشدن سرور پیش از اجرای آزمایشهای اکوسیستم/پروتکل.                                                                                                                                                                                       |
| `ELECTRON_SMOKE_URL`                   | `http://127.0.0.1:20128/login`                    | `scripts/dev/smoke-electron-packaged.mjs` | نشانی URL که چارچوب آزمایش دود Electron انتظار دارد برنامه بستهبندیشده در آن ارائه شود.                                                                                                                                                                                 |
| `ELECTRON_SMOKE_TIMEOUT_MS`            | `45000`                                           | `scripts/dev/smoke-electron-packaged.mjs` | مهلت زمانی کل (میلیثانیه) پیش از آنکه چارچوب آزمایش دود منصرف شود.                                                                                                                                                                                                      |
| `ELECTRON_SMOKE_SETTLE_MS`             | `2000`                                            | `scripts/dev/smoke-electron-packaged.mjs` | بازه تثبیت (میلیثانیه) پس از بارگذاری صفحه.                                                                                                                                                                                                                             |
| `ELECTRON_SMOKE_APP_EXECUTABLE`        | _(خودکار)_                                        | `scripts/dev/smoke-electron-packaged.mjs` | مسیر صریح فایل اجرایی بستهبندیشده Electron.                                                                                                                                                                                                                             |
| `ELECTRON_SMOKE_DATA_DIR`              | _(tmpdir)_                                        | `scripts/dev/smoke-electron-packaged.mjs` | پوشه داده برای اجرای آزمایش دود Electron.                                                                                                                                                                                                                               |
| `ELECTRON_SMOKE_KEEP_DATA`             | `0`                                               | `scripts/dev/smoke-electron-packaged.mjs` | برای حفظ پوشه داده آزمایش دود پس از اجرا، روی `1` تنظیم کنید.                                                                                                                                                                                                           |
| `ELECTRON_SMOKE_STREAM_LOGS`           | `0`                                               | `scripts/dev/smoke-electron-packaged.mjs` | برای پخش جریانی گزارشهای Electron در stdout هنگام اجرا، روی `1` تنظیم کنید.                                                                                                                                                                                             |
| `ELECTRON_SMOKE_COLD_RESTART`          | `0`                                               | `scripts/dev/smoke-electron-packaged.mjs` | #7592: راهاندازی مجدد با همان پوشه داده و اطمینان از اینکه اجرای دوم درایور بومی SQLite را انتخاب میکند.                                                                                                                                                                |
| `CLI_DEVIN_BIN`                        | _(جستوجو در PATH)_                                | `open-sse/executors/devin-cli.ts`         | بازنویسی مسیر فایل باینری Devin CLI.                                                                                                                                                                                                                                    |

### خط لوله ترجمه مستندات

توسط `scripts/i18n/run-translation.mjs` (فرمان `npm run i18n:run`) استفاده میشود.
هر پنج متغیر بهطور پیشفرض تنظیم نشدهاند — آنها را فقط روی دستگاههایی که باید قادر به اجرای مترجم مستندات باشند، در `.env` تنظیم کنید.

| متغیر                               | مقدار پیشفرض  | فایل منبع                          | توضیحات                                                      |
| ----------------------------------- | ------------- | ---------------------------------- | ------------------------------------------------------------ |
| `OMNIROUTE_TRANSLATION_API_URL`     | _(تنظیمنشده)_ | `scripts/i18n/run-translation.mjs` | نشانی URL پایه سازگار با OpenAI برای بکاند ترجمه.            |
| `OMNIROUTE_TRANSLATION_API_KEY`     | _(تنظیمنشده)_ | `scripts/i18n/run-translation.mjs` | توکن Bearer برای بکاند ترجمه (هرگز ثبت نمیشود).              |
| `OMNIROUTE_TRANSLATION_MODEL`       | _(تنظیمنشده)_ | `scripts/i18n/run-translation.mjs` | شناسه مدل، برای مثال `gpt-4o-mini` یا `cx/gpt-5.4-mini`.     |
| `OMNIROUTE_TRANSLATION_TIMEOUT_MS`  | `60000`       | `scripts/i18n/run-translation.mjs` | مهلت زمانی هر درخواست بر حسب میلیثانیه.                      |
| `OMNIROUTE_TRANSLATION_CONCURRENCY` | `4`           | `scripts/i18n/run-translation.mjs` | درخواستهای ترجمه موازی هنگام اجرا روی چندین فایل / زبانمحلی. |

---

## 27. خوراک Radar (میزبانی شخصی)

افزونهای اختیاری که توسط پرچم قابلیت RADAR_ENABLED کنترل میشود (بهطور پیشفرض غیرفعال است — این پرچم
از طریق Settings/DB تغییر میکند و متغیر محیطی نیست؛ مراجعه کنید به
[docs/frameworks/RADAR.md](../frameworks/RADAR.md#flag-radar_enabled-default-off)).
چهار متغیر نخست زیر، جایگزینهای اختیاری برای خوراکی با میزبانی شخصی یا فورکشده و
جریانهای کلید حامی هستند. متغیر پنجم، `RADAR_ADMIN_URL`، پیوندی جداگانه و بدون مقدار پیشفرض به پنل
عملیات خصوصی مالک است. برای مستندات کامل ماژول و
[توالی فعالسازی سرتاسری و راهاندازی هدایتشده آن](../frameworks/RADAR.md#end-to-end-activation-and-guided-setup)، به [docs/frameworks/RADAR.md](../frameworks/RADAR.md) مراجعه کنید.

خواننده عمومی اعلانهای Home/Changelog با یک متغیر محیطی
پیکربندی نمیشود و به پرچم قابلیت RADAR_ENABLED وابسته نیست. این خواننده URL عمومی
`news.json` مخزن را که در `src/shared/utils/releaseNotes.ts` تعریف شده است، فقط از طریق
GET میخواند؛ شناسههای اعلانهای ردشده در فضای ذخیرهسازی محلی مرورگر باقی میمانند.

| متغیر                         | مقدار پیشفرض                                 | فایل منبع                                                    | توضیحات                                                                                                                                                                             |
| ----------------------------- | -------------------------------------------- | ------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `RADAR_FEED_URL`              | `https://radar.omniroute.online`             | `src/lib/radar/{sync,referralsSync,offersSync,intelSync}.ts` | URL پایه مشترک میان خوراکهای کاتالوگ، ارجاعها، پیشنهادهای حامیان و Intel که بهصورت جداگانه امضا شدهاند. برای اشاره به یک سرویس با میزبانی شخصی یا فورکشده، آن را بازنویسی کنید.     |
| `RADAR_FEED_PUBKEY`           | _(کلید پیشفرض سنجاقشده)_                     | `src/lib/radar/pinnedKeys.ts`                                | کلید عمومی Ed25519 (با قالب base64-DER SPKI یا PEM) که برای تأیید امضاهای خوراک از یک خوراک سفارشی استفاده میشود.                                                                   |
| `RADAR_CONTRIBUTOR_CLAIM_URL` | `https://radar.omniroute.online/auth/github` | `src/lib/radar/links.ts`                                     | URLای که دکمه «من مشارکتکننده هستم» در داشبورد باز میکند (جریان دریافت کلید حامی از طریق GitHub OAuth).                                                                             |
| `RADAR_SUPPORTER_PLANS_URL`   | `https://radar.omniroute.online/planos`      | `src/lib/radar/links.ts`                                     | URLای که دکمه «حمایت از پروژه» در داشبورد باز میکند (صفحه پرداخت/طرحها).                                                                                                            |
| `RADAR_ADMIN_URL`             | _(تنظیمنشده)_                                | `src/lib/radar/links.ts`                                     | پیوند پنل عملیات خصوصی که فقط برای مالک است. استفاده از HTTPS الزامی است، مگر برای یک انتقال SSH حلقهبازگشتی HTTP؛ مقادیر تنظیمنشده یا نامعتبر هیچ موردی برای پیمایش ایجاد نمیکنند. |

---

## ممیزی: متغیرهای حذفشده / بلااستفاده

متغیرهای زیر در نسخههای پیشین `.env.example` وجود داشتند، اما در پایگاه کد فعلی **هیچ ارجاعی در زمان اجرا ندارند**. آنها حذف شدهاند:

| متغیر                                                                                                                                                                           | دلیل                                                                                                                                                                          |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `STORAGE_DRIVER=sqlite`                                                                                                                                                         | هرگز توسط هیچ فایل منبعی خوانده نمیشود. SQLite تنها درایور پشتیبانیشده است — نیازی به انتخاب نیست.                                                                            |
| `INSTANCE_NAME=omniroute`                                                                                                                                                       | در مستندات/قالبهای قدیمی env وجود دارد، اما هنگام اجرا استفاده نمیشود. ممکن است در قابلیت چندنمونهای آینده بازگردد.                                                           |
| `SQLITE_MAX_SIZE_MB=2048`                                                                                                                                                       | در کد منبع به آن ارجاع نشده است. اندازه پایگاه داده بهصورت مصنوعی محدود نمیشود.                                                                                               |
| `SQLITE_CLEAN_LEGACY_FILES=true`                                                                                                                                                | در کد منبع به آن ارجاع نشده است. پاکسازی فایلهای قدیمی احتمالاً حذف شده است.                                                                                                  |
| `CLI_ROO_BIN`                                                                                                                                                                   | در `src/shared/services/cliRuntime.ts` ثبت نشده است.                                                                                                                          |
| `CLI_KIMI_CODING_BIN`                                                                                                                                                           | در `src/shared/services/cliRuntime.ts` ثبت نشده است (Kimi Coding از OAuth استفاده میکند، نه فایل اجرایی CLI).                                                                 |
| `IFLOW_OAUTH_CLIENT_ID` / `IFLOW_OAUTH_CLIENT_SECRET`                                                                                                                           | در هیچجای کد منبع به آنها ارجاع نشده است.                                                                                                                                     |
| `CEREBRAS_API_KEY` / `COHERE_API_KEY` / `FIREWORKS_API_KEY` / `GROQ_API_KEY` / `MISTRAL_API_KEY` / `NEBIUS_API_KEY` / `PERPLEXITY_API_KEY` / `TOGETHER_API_KEY` / `XAI_API_KEY` | در v3.8.0 حذف شدند. زمان اجرا دیگر این متغیرهای env را نمیخواند — اطلاعات احراز هویت از Dashboard / `data/provider-credentials.json` / پایگاه داده رمزگذاریشده دریافت میشوند. |
| `CURSOR_PROTOBUF_DEBUG`                                                                                                                                                         | در v3.8.0 حذف شد. اجراکننده Cursor از `CURSOR_DEBUG` / `CURSOR_STREAM_DEBUG` استفاده میکند (به §22 مراجعه کنید).                                                              |
| `CLI_COMPAT_KIRO`                                                                                                                                                               | در v3.8.0 حذف شد. Kiro در `CLI_COMPAT_OMITTED_PROVIDER_IDS` قرار دارد — کلید تغییر وضعیت آن هیچ اثری ندارد.                                                                   |
| `QIANFAN_API_KEY`                                                                                                                                                               | همراه با سایر جایخالیهای بلااستفاده کلید API ارائهدهندگان در v3.8.0 حذف شد.                                                                                                   |

### اصلاح مقادیر پیشفرض

| متغیر                     | مقدار قدیمی `.env.example` | پیشفرض واقعی کد | اصلاح                                                   |
| ------------------------- | -------------------------- | --------------- | ------------------------------------------------------- |
| `APP_LOG_RETENTION_DAYS`  | `90`                       | `7`             | ✅ مقدار گمراهکننده حذف شد؛ `7` بهعنوان پیشفرض مستند شد |
| `CALL_LOG_RETENTION_DAYS` | `90`                       | `7`             | ✅ مقدار گمراهکننده حذف شد؛ `7` بهعنوان پیشفرض مستند شد |

### بازتولید پیکربندی OpenCode (ابزار موردی)

توسط `scripts/ad-hoc/regen-opencode-config.ts` برای بازتولید یک `opencode.json`
با مقادیر دقیق `limit.context` و `limit.output` که از نمونه در حال اجرای
OmniRoute دریافت میشوند، استفاده میشود. هیچیک از این موارد برای عملکرد عادی لازم نیستند — این اسکریپت
صرفاً ابزاری برای توسعهدهندگان است.

| متغیر              | مقدار پیشفرض             | فایل منبع                                 | توضیحات                                                                                                                                  |
| ------------------ | ------------------------ | ----------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------- |
| `OMNIROUTE_URL`    | `http://localhost:20128` | `scripts/ad-hoc/regen-opencode-config.ts` | نشانی پایه نمونه OmniRoute برای درخواست از `/v1/models`.                                                                                 |
| `OMNIROUTE_KEY`    | _(تنظیمنشده)_            | `scripts/ad-hoc/regen-opencode-config.ts` | کلید API برای احراز هویت در نقطه پایانی `/v1/models` متعلق به OmniRoute. در صورت تنظیمنبودن، از `OPENCODE_API_KEY` استفاده میشود.        |
| `OPENCODE_API_KEY` | _(تنظیمنشده)_            | `scripts/ad-hoc/regen-opencode-config.ts` | کلید API به سبک OpenCode (`sk-...`) که در `opencode.json` بازتولیدشده نوشته میشود. در صورت تنظیمنبودن، از `OMNIROUTE_KEY` استفاده میشود. |

### ابزار ارزیابی آفلاین فشردهسازی (ابزار موقت)

مورد استفاده `scripts/compression-eval/index.ts`، رابط خط فرمان ارزیابی آفلاین فشردهسازی.
برای عملکرد عادی لازم نیست — فقط ابزار توسعهدهندگان است.

| متغیر                        | مقدار پیشفرض | فایل منبع                           | توضیحات                                                                                                                                                                         |
| ---------------------------- | ------------ | ----------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `OMNIROUTE_EVAL_CREDENTIALS` | `{}` (خالی)  | `scripts/compression-eval/index.ts` | اعتبارنامههای JSON ارائهشده توسط اپراتور برای ارائهدهندهای که رابط خط فرمان ارزیابی آفلاین فشردهسازی آزمایش میکند (با `JSON.parse` تجزیه میشود). برای اجرای آزمایشی تنظیم نشود. |

### نشستهای مرورگر VNC

مورد استفاده `src/lib/vncSession/manifest.ts` برای پیکربندی نشستهای بدون رابط گرافیکی Chromium مبتنی بر Docker جهت ارائهدهندگان خودکارسازی مرورگر. همه اختیاری هستند — مقادیر پیشفرض در زیر نشان داده شدهاند.

| متغیر                                 | مقدار پیشفرض                   | فایل منبع                                   | توضیحات                                                                               |
| ------------------------------------- | ------------------------------ | ------------------------------------------- | ------------------------------------------------------------------------------------- |
| `OMNIROUTE_DOCKER_BIN`                | `docker`                       | `src/lib/vncSession/manifest.ts`            | مسیر فایل اجرایی Docker که برای ایجاد کانتینرهای VNC استفاده میشود.                   |
| `OMNIROUTE_VNC_IMAGE`                 | `omniroute-vnc-chromium:local` | `src/lib/vncSession/manifest.ts`            | ایمیج Docker برای کانتینر VNC Chromium.                                               |
| `OMNIROUTE_VNC_CHROMIUM_ARGS`         | _(پرچمهای داخلی)_              | `src/lib/vncSession/manifest.ts`            | آرگومانهای اضافی CLI مربوط به Chromium که به مرورگر داخل کانتینر ارسال میشوند.        |
| `OMNIROUTE_VNC_CONTAINER_VNC_PORT`    | `3000`                         | `src/lib/vncSession/manifest.ts`            | پورت VNC داخل کانتینر.                                                                |
| `OMNIROUTE_VNC_CONTAINER_CDP_PORT`    | `9223`                         | `src/lib/vncSession/manifest.ts`            | پورت Chrome DevTools Protocol داخل کانتینر.                                           |
| `OMNIROUTE_VNC_CONTAINER_PROFILE_DIR` | `/config`                      | `src/lib/vncSession/manifest.ts`            | پوشه پروفایل داخل کانتینر.                                                            |
| `OMNIROUTE_VNC_PROFILE_DIR`           | _(تنظیمنشده)_                  | `src/lib/vncSession/manifest.ts`            | پوشه سمت میزبان برای پروفایلهای پایدار مرورگر.                                        |
| `OMNIROUTE_VNC_IDLE_MS`               | `600000`                       | `src/lib/vncSession/manifest.ts`            | مهلت عدم فعالیت (میلیثانیه) پیش از جمعآوری نشست VNC.                                  |
| `OMNIROUTE_VNC_MAX_MS`                | `1800000`                      | `src/lib/vncSession/manifest.ts`            | حداکثر مدت نشست (میلیثانیه).                                                          |
| `OMNIROUTE_VNC_MAX_SESSIONS`          | `4`                            | `src/lib/vncSession/manifest.ts`            | حداکثر تعداد نشستهای همزمان VNC.                                                      |
| `OMNIROUTE_VNC_READY_MS`              | `45000`                        | `src/lib/vncSession/manifest.ts`            | مهلت آمادهشدن مرورگر (میلیثانیه).                                                     |
| `OMNIROUTE_VNC_HARVEST_MS`            | `20000`                        | `src/lib/vncSession/manifest.ts`            | مهلت جمعآوری/پاکسازی (میلیثانیه).                                                     |
| `OMNIROUTE_VNC_NETWORK`               | `omniroute-vnc-browser-login`  | `src/lib/vncSession/manifest.ts`            | شبکه اختصاصی Docker که کانتینر به آن میپیوندد (#12571) و خارج از پل پیشفرض قرار دارد. |
| `VIBEPROXY_DATA_DIR`                  | _(تنظیمنشده)_                  | `open-sse/services/notionThreadSessions.ts` | پوشه نگهداری پایدار نشستهای رشتهای Notion.                                            |

### احراز هویت سرویس داخلی

| متغیر                                   | مقدار پیشفرض | توضیحات                                                                                                   |
| --------------------------------------- | ------------ | --------------------------------------------------------------------------------------------------------- |
| `OMNIROUTE_INTERNAL_SERVICE_TOKEN`      | –            | توکن درونخطی برای احراز هویت سرویسبهسرویس در صفحه مدیریت.                                                 |
| `OMNIROUTE_INTERNAL_SERVICE_TOKEN_FILE` | –            | مسیر فایلی که حاوی توکن سرویس داخلی است (در کانتینرها ترجیح داده میشود؛ متغیر درونخطی را بازنویسی میکند). |

### آمار ارائهدهنده OpenRouter

| متغیر                               | مقدار پیشفرض | توضیحات                                                                                          |
| ----------------------------------- | ------------ | ------------------------------------------------------------------------------------------------ |
| `OPENROUTER_PROVIDER_STATS_ENABLED` | `true`       | برای صرفنظر از دریافت آمار هر ارائهدهنده OpenRouter جهت غنیسازی کاتالوگ، روی `false` تنظیم کنید. |
| `OPENROUTER_PROVIDER_STATS_TTL_MS`  | `3600000`    | مدت اعتبار کش (میلیثانیه) برای آمار دریافتشده ارائهدهندگان OpenRouter.                           |

### اتصال Redis تعبیهشده

| متغیر                       | مقدار پیشفرض | توضیحات                                                 |
| --------------------------- | ------------ | ------------------------------------------------------- |
| `REDIS_BIND_HOST`           | `127.0.0.1`  | آدرس اتصال برای سرویس Redis تعبیهشده.                   |
| `REDIS_PORT`                | `6379`       | پورت سرویس Redis تعبیهشده.                              |
| `OMNIROUTE_REDIS_BIND_HOST` | –            | بازنویسی مختص OmniRoute برای آدرس اتصال Redis تعبیهشده. |

---

## 24. افزودههای نسخه v3.8.50

این تنظیمات پس از اسنپشات قبلی قرارداد محیط معرفی شدند.

| متغیر                                       | مقدار پیشفرض               | فایل منبع                                                                                | توضیحات                                                                                                                                                                                                                                                                                                                                               |
| ------------------------------------------- | -------------------------- | ---------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `OMNIROUTE_CHAT_ADMISSION_QUEUE_MS`         | `2000`                     | `src/shared/middleware/chatBodyAdmission.ts`                                             | حداکثر زمان انتظار برای یک جایگاه پذیرش چت سنگین، پیش از بازگرداندن خطای قابلتلاشمجدد `503`؛ یک انتظار کوتاه و محدود، جهشهای ناگهانی عاملها را بهجای بازگرداندن فوری `503` بهصورت ترتیبی پردازش میکند. مقدار `0` رد فوری را دوباره فعال میکند.                                                                                                        |
| `OMNIROUTE_CHAT_ADMISSION_MAX_QUEUED_BYTES` | `4194304` (4 مگابایت)      | `src/shared/middleware/chatBodyAdmission.ts`                                             | بودجه بایتهای صفشده برای انتظار پذیرش: مجموع بایتهای بدنه بافرشده و متوقفشده در کل فرایند را محدود میکند تا انتظار موجب افزایش مصرف heap نشود (#4380). انتظارهای بیشازبودجه فوراً یک `503` قابلتلاشمجدد دریافت میکنند.                                                                                                                                |
| `OMNIROUTE_CHAT_VIRTUAL_TTL_MS`             | `60000` (60 ثانیه)         | `src/shared/middleware/chatBodyAdmission.ts`                                             | از #10110 به بعد منسوخ و بدون اثر است: مسیرهای پذیرش بهازای هر نشست، به نفع یک بودجه سراسری برای کل فرایند حذف شدند. برای سازگاری پیکربندی پذیرفته میشود، اما نادیده گرفته خواهد شد.                                                                                                                                                                  |
| `OMNIROUTE_CHAT_VIRTUAL_MAX_SESSIONS`       | `64`                       | `src/shared/middleware/chatBodyAdmission.ts`                                             | از #10110 به بعد منسوخ و بدون اثر است: مسیرهای پذیرش بهازای هر نشست، به نفع یک بودجه سراسری برای کل فرایند حذف شدند. برای سازگاری پیکربندی پذیرفته میشود، اما نادیده گرفته خواهد شد.                                                                                                                                                                  |
| `OMNIROUTE_CHAT_VIRTUAL_LANES`              | `0` (خاموش)                | `open-sse/services/admission/runtime.ts`                                                 | مسیرهای مجازی تطبیقی پذیرش در زمان اجرا (#9654): کلید اصلی دروازه تطبیقی بهازای هر مستأجر (سیستم 2). این مورد با متغیرهای منسوخ مسیر بهازای هر اتصال در بالا (`TTL_MS` / `MAX_SESSIONS`، بدون اثر از #10110) متفاوت است. پرچم قابلیت داشبورد نیز همین نام را دارد؛ متغیر محیطی بر تنظیم بازنویسی داشبورد اولویت دارد و به راهاندازی مجدد نیازمند است. |
| `OMNIROUTE_RUNNOW_TIMEOUT_MS`               | `30000`                    | `src/app/api/jobs/[id]/run-now/route.ts`                                                 | مدتزمان انتظار یک فراخوانی اجرای فوری برای یک کار در حال اجرا، پیش از آغاز اجرای صفشده را محدود میکند.                                                                                                                                                                                                                                                |
| `ADOBE_FIREFLY_BROWSER_REFRESH`             | فعال                       | `open-sse/services/adobeFireflySession.ts`                                               | وضعیت IMS و ریسک مرورگر را از طریق نشستهای Chrome CDP در سطح حساب تازه نگه میدارد؛ برای غیرفعالکردن، مقدار `0` را تنظیم کنید.                                                                                                                                                                                                                         |
| `ADOBE_FIREFLY_SESSION_DISK`                | فعال                       | `open-sse/services/adobeFireflySession.ts`                                               | نشستهای ترمیمشده Adobe را در `DATA_DIR` ذخیره میکند؛ برای نگهداری وضعیت فقط در حافظه، مقدار `0` را تنظیم کنید.                                                                                                                                                                                                                                        |
| `ADOBE_FIREFLY_MIN_SUBMIT_GAP_MS`           | `12000`                    | `open-sse/services/adobeFireflySession.ts`                                               | حداقل فاصله زمانی میان ارسال درخواستهای تولید Adobe Firefly.                                                                                                                                                                                                                                                                                          |
| `ADOBE_FIREFLY_BATCH_EXTRA_GAP_MS`          | `15000`                    | `open-sse/services/adobeFireflySession.ts`                                               | دوره سکون اضافی پس از هر سومین ارسال موفق Adobe.                                                                                                                                                                                                                                                                                                      |
| `ADOBE_FIREFLY_CHROME_HEADLESS`             | `0`                        | `open-sse/services/adobeFireflyBrowserLogin.ts`                                          | حالت واقعاً بدون رابط گرافیکی، صرفاً برای اشکالزدایی؛ Adobe colligo معمولاً نشست ریسک حاصل را رد میکند.                                                                                                                                                                                                                                               |
| `CHROME_PATH`                               | تشخیص خودکار               | `open-sse/executors/cloudflare-playground.ts`, `open-sse/executors/chatgpt-web-codex.ts` | فایل اجرایی مطلق و اختیاری Chrome که وقتی تشخیص خودکار پلتفرم کافی نباشد، توسط اجراکنندههای مبتنی بر مرورگر استفاده میشود.                                                                                                                                                                                                                            |
| `TELEGRAM_BOT_TOKEN`                        | _(تنظیمنشده)_              | `src/lib/telegram/config.ts`                                                             | توکن BotFather که webhook ورودی را فعال و `initData` مربوط به Mini App را امضا میکند.                                                                                                                                                                                                                                                                 |
| `TELEGRAM_WEBHOOK_SECRET`                   | _(تنظیمنشده)_              | `src/lib/telegram/config.ts`                                                             | رمز مشترکی که از طریق `setWebhook` ثبت میشود و در هر تحویل webhook با هدر `X-Telegram-Bot-Api-Secret-Token` اعتبارسنجی میگردد. برای مسیر webhook الزامی است؛ تنظیمنبودن آن باعث میشود تحویلهای webhook با 503 رد شوند.                                                                                                                                |
| `TELEGRAM_DEFAULT_MODEL`                    | `auto/chat`                | `src/lib/telegram/chatProxy.ts`                                                          | مدل مورد استفاده برای پاسخهای چت Telegram.                                                                                                                                                                                                                                                                                                            |
| `TELEGRAM_BOT_API_BASE`                     | `https://api.telegram.org` | `src/lib/telegram/config.ts`                                                             | بازنویسی URL پایه Bot API برای پروکسیها یا سرورهای Bot API خودمیزبان.                                                                                                                                                                                                                                                                                 |
| `TELEGRAM_WEBHOOK_TIMEOUT_MS`               | `60000`                    | `src/lib/telegram/config.ts`                                                             | مهلت زمانی بر حسب میلیثانیه برای فراخوانیهای خروجی Bot API.                                                                                                                                                                                                                                                                                           |
| `OMNIROUTE_OPTIONAL_PACK_TAR`               | `1` (فعال)                 | `scripts/build/optionalPackStaging.mjs`                                                  | مقدار `0` را تنظیم کنید تا هنگام آمادهسازی بستههای اختیاری ML/مرورگر برای درخت مستقل Electron، از تولید فایلهای tarball با پسوند `.tar.gz` صرفنظر شود (دایرکتوریهای بسته و `optional-packs.index.json` همچنان تولید میشوند). گردشکار انتشار دسکتاپ از این گزینه برای کاهش اندازه بارگذاری مصنوعات استفاده میکند.                                      |

### ChatGPT Web (Codex)

مقادیر پیشفرض سراسری برای مرورگر بدون رابط گرافیکی و تونل خروجی ابزار. مقادیر اتصال تنظیمشده در داشبورد اولویت دارند.

| متغیر                                   | پیشفرض                         | فایل منبع                                                                  | توضیحات                                                                                                                                                                                                                                                                                                                               |
| --------------------------------------- | ------------------------------ | -------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `CHATGPT_WEB_CODEX_CHROME_PATH`         | _(تشخیص خودکار)_               | `open-sse/executors/chatgpt-web-codex.ts`                                  | مسیر صریح Chrome/Chromium برای اجرا با npm، systemd و PM2.                                                                                                                                                                                                                                                                            |
| `CHROME_PATH`                           | _(تشخیص خودکار)_               | `open-sse/executors/chatgpt-web-codex.ts`                                  | مسیر جایگزین مشترک برای مسیر صریح Chrome/Chromium.                                                                                                                                                                                                                                                                                    |
| `CHATGPT_WEB_CODEX_CDP_URL`             | _(تنظیمنشده)_                  | `open-sse/executors/chatgpt-web-codex.ts`                                  | نقطه پایانی داخلی CDP؛ Docker از Sidecar روی پورت `9223` استفاده میکند.                                                                                                                                                                                                                                                               |
| `CDP_PROXY_TOKEN`                       | _(تنظیمنشده)_                  | `docker/chatgpt-web-codex-browser/cdp-proxy.mjs`                           | در صورت تنظیم، هر درخواست به CDP Proxy Sidecar باید این مقدار را در هدر `X-Omni-Cdp-Token` ارسال کند (#13679). بدون مقدار، پروکسی درخواستها را بدون احراز هویت هدایت میکند — در این حالت فقط جداسازی شبکهایِ شبکه Compose با نام `chatgpt-web-codex-net` از آن محافظت میکند. برای تولید مقدار از `openssl rand -hex 32` استفاده کنید. |
| `CHATGPT_WEB_CODEX_TUNNEL_ID`           | _(تنظیمنشده)_                  | `open-sse/executors/chatgpt-web-codex.ts`                                  | شناسه سراسری تونل OpenAI برای چرخههای محلی ابزار Codex.                                                                                                                                                                                                                                                                               |
| `CHATGPT_WEB_CODEX_RUNTIME_KEY`         | _(تنظیمنشده)_                  | `open-sse/executors/chatgpt-web-codex.ts`                                  | کلید Runtime سراسری تونل؛ هرگز آن را در لاگها ثبت نکنید.                                                                                                                                                                                                                                                                              |
| `CHATGPT_WEB_CODEX_CONNECTOR_NAME`      | `OmniRoute Codex v2`           | `open-sse/executors/chatgpt-web-codex.ts`                                  | نام دقیق ChatGPT Custom Connector تازهساختهشده برای پل MCP.                                                                                                                                                                                                                                                                           |
| `CODEX_CHATGPT_WEB_HOME`                | `<DATA_DIR>/chatgpt-web-codex` | `open-sse/vendor/codex-chatgpt-web/config.ts`                              | دایرکتوری اختصاصی برای وضعیت مرورگر، Broker و تونل.                                                                                                                                                                                                                                                                                   |
| `CODEX_CHATGPT_WEB_BROWSER_DIAGNOSTICS` | `0`                            | `open-sse/vendor/codex-chatgpt-web/adapters/chatgpt-web/browser-worker.ts` | با مقدار `1`، تصاویر عیبیابی مرورگر در هر Checkpoint ثبت میشوند.                                                                                                                                                                                                                                                                      |
| `CODEX_CHATGPT_WEB_LAUNCHER`            | _(تنظیمنشده)_                  | `open-sse/vendor/codex-chatgpt-web/config.ts`                              | مسیر مطلق اختیاری به یک فایل باینری Launcher دائمی.                                                                                                                                                                                                                                                                                   |
| `CODEX_CHATGPT_WEB_BUN`                 | _(تشخیص خودکار)_               | `open-sse/vendor/codex-chatgpt-web/config.ts`                              | مسیر مطلق اختیاری به فایل باینری Bun Runtime.                                                                                                                                                                                                                                                                                         |
| `CODEX_WEB_GPT_BUN`                     | _(تنظیمنشده)_                  | `open-sse/vendor/codex-chatgpt-web/config.ts`                              | جایگزین قدیمی برای `CODEX_CHATGPT_WEB_BUN`؛ راهاندازیهای جدید از نام استاندارد استفاده میکنند.                                                                                                                                                                                                                                        |

---

## پل OmniConductor

مصرفکنندهٔ SSE با عمر طولانی که وظایف هاب OmniConductor را در A2A TaskManager محلی (`src/lib/conductor/`) بازتاب میدهد. استفاده از آن اختیاری است — پل فقط زمانی راهاندازی میشود که `CONDUCTOR_HUB_URL` تنظیم شده باشد. فقط سمت سرور: توکن هاب هرگز نباید به مرورگر برسد.

| متغیر                          | پیشفرض                  | فایل منبع                        | توضیحات                                                                                                                      |
| ------------------------------ | ----------------------- | -------------------------------- | ---------------------------------------------------------------------------------------------------------------------------- |
| `CONDUCTOR_HUB_URL`            | _(خالی)_                | `src/lib/conductor/boot.ts`      | آدرس URL پایهٔ هاب OmniConductor (برای مثال `http://127.0.0.1:7910`). تنظیمنشده = پل غیرفعال است.                            |
| `CONDUCTOR_HUB_TOKEN`          | _(خالی)_                | `src/lib/conductor/boot.ts`      | اعتبارنامهٔ هاب برای خوراک SSE — یک همتای از نوع `spokesperson` در هاب ایجاد کنید (`POST /v1/peers`، مدیر).                  |
| `CONDUCTOR_ORCHESTRATOR_TOKEN` | _(خالی)_                | `src/lib/conductor/hubProxy.ts`  | اعتبارنامه برای واگذاری وظایف ورودی A2A→هاب (`POST /v1/tasks`)؛ اگر تنظیم نشده باشد، از `CONDUCTOR_HUB_TOKEN` استفاده میکند. |
| `CONDUCTOR_SPOKESPERSON_URL`   | `http://127.0.0.1:7920` | `src/lib/conductor/faroProxy.ts` | آدرس URL پایهٔ سرویس spokesperson (Faro) در پشت پراکسی گفتوگوی داشبورد (`/api/conductor/ask`).                               |

### زمانبندی آگاه از سهمیه

برای بررسی بودجهٔ توکن پیش از درخواست توسط `open-sse/services/combo.ts` و `src/lib/quota/quotaScheduler.ts` استفاده میشود. استفاده از آن اختیاری است — وقتی تنظیم نشده باشد، رفتار پیشفرض مسیریابی بدون تغییر میماند.

| متغیر                           | پیشفرض | فایل منبع                    | توضیحات                                                                                                                                                                                                                                                           |
| ------------------------------- | ------ | ---------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `OMNIROUTE_QUOTA_AWARE_ROUTING` | `0`    | `open-sse/services/combo.ts` | وقتی مقدار آن `1` باشد، اتصالهایی را که بودجهٔ توکن آنها در هر بازه (`rateLimitOverrides.tpm`، جدول `provider_quota_state`) پیش از ارسال توان پوشش هزینهٔ تخمینی درخواست را ندارد، نادیده میگیرد. اگر هیچ بودجهای پیکربندی نشده باشد، بهصورت fail-open عمل میکند. |
