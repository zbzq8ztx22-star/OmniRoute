# Embedded Services (Bahasa Melayu)

🌐 **Languages:** 🇺🇸 [English](../../../../frameworks/EMBEDDED-SERVICES.md) · 🇪🇹 [am](../../../am/docs/frameworks/EMBEDDED-SERVICES.md) · 🇸🇦 [ar](../../../ar/docs/frameworks/EMBEDDED-SERVICES.md) · 🇦🇿 [az](../../../az/docs/frameworks/EMBEDDED-SERVICES.md) · 🇧🇬 [bg](../../../bg/docs/frameworks/EMBEDDED-SERVICES.md) · 🇧🇩 [bn](../../../bn/docs/frameworks/EMBEDDED-SERVICES.md) · 🇨🇿 [cs](../../../cs/docs/frameworks/EMBEDDED-SERVICES.md) · 🇩🇰 [da](../../../da/docs/frameworks/EMBEDDED-SERVICES.md) · 🇩🇪 [de](../../../de/docs/frameworks/EMBEDDED-SERVICES.md) · 🇬🇷 [el](../../../el/docs/frameworks/EMBEDDED-SERVICES.md) · 🇪🇸 [es](../../../es/docs/frameworks/EMBEDDED-SERVICES.md) · 🇪🇪 [et](../../../et/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇷 [fa](../../../fa/docs/frameworks/EMBEDDED-SERVICES.md) · 🇫🇮 [fi](../../../fi/docs/frameworks/EMBEDDED-SERVICES.md) · 🇫🇷 [fr](../../../fr/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇪 [ga](../../../ga/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇳 [gu](../../../gu/docs/frameworks/EMBEDDED-SERVICES.md) · 🇳🇬 [ha](../../../ha/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇱 [he](../../../he/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇳 [hi](../../../hi/docs/frameworks/EMBEDDED-SERVICES.md) · 🇭🇷 [hr](../../../hr/docs/frameworks/EMBEDDED-SERVICES.md) · 🇭🇺 [hu](../../../hu/docs/frameworks/EMBEDDED-SERVICES.md) · 🇦🇲 [hy](../../../hy/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇩 [id](../../../id/docs/frameworks/EMBEDDED-SERVICES.md) · 🇳🇬 [ig](../../../ig/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇹 [it](../../../it/docs/frameworks/EMBEDDED-SERVICES.md) · 🇯🇵 [ja](../../../ja/docs/frameworks/EMBEDDED-SERVICES.md) · 🇬🇪 [ka](../../../ka/docs/frameworks/EMBEDDED-SERVICES.md) · 🇰🇭 [km](../../../km/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇳 [kn](../../../kn/docs/frameworks/EMBEDDED-SERVICES.md) · 🇰🇷 [ko](../../../ko/docs/frameworks/EMBEDDED-SERVICES.md) · 🇱🇹 [lt](../../../lt/docs/frameworks/EMBEDDED-SERVICES.md) · 🇱🇻 [lv](../../../lv/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇳 [ml](../../../ml/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇳 [mr](../../../mr/docs/frameworks/EMBEDDED-SERVICES.md) · 🇲🇹 [mt](../../../mt/docs/frameworks/EMBEDDED-SERVICES.md) · 🇲🇲 [my](../../../my/docs/frameworks/EMBEDDED-SERVICES.md) · 🇳🇵 [ne](../../../ne/docs/frameworks/EMBEDDED-SERVICES.md) · 🇳🇱 [nl](../../../nl/docs/frameworks/EMBEDDED-SERVICES.md) · 🇳🇴 [no](../../../no/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇳 [or](../../../or/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇳 [pa](../../../pa/docs/frameworks/EMBEDDED-SERVICES.md) · 🇵🇭 [phi](../../../phi/docs/frameworks/EMBEDDED-SERVICES.md) · 🇵🇱 [pl](../../../pl/docs/frameworks/EMBEDDED-SERVICES.md) · 🇵🇹 [pt](../../../pt/docs/frameworks/EMBEDDED-SERVICES.md) · 🇧🇷 [pt-BR](../../../pt-BR/docs/frameworks/EMBEDDED-SERVICES.md) · 🇷🇴 [ro](../../../ro/docs/frameworks/EMBEDDED-SERVICES.md) · 🇷🇺 [ru](../../../ru/docs/frameworks/EMBEDDED-SERVICES.md) · 🇱🇰 [si](../../../si/docs/frameworks/EMBEDDED-SERVICES.md) · 🇸🇰 [sk](../../../sk/docs/frameworks/EMBEDDED-SERVICES.md) · 🇸🇮 [sl](../../../sl/docs/frameworks/EMBEDDED-SERVICES.md) · 🇷🇸 [sr](../../../sr/docs/frameworks/EMBEDDED-SERVICES.md) · 🇸🇪 [sv](../../../sv/docs/frameworks/EMBEDDED-SERVICES.md) · 🇰🇪 [sw](../../../sw/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇳 [ta](../../../ta/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇳 [te](../../../te/docs/frameworks/EMBEDDED-SERVICES.md) · 🇹🇭 [th](../../../th/docs/frameworks/EMBEDDED-SERVICES.md) · 🇹🇷 [tr](../../../tr/docs/frameworks/EMBEDDED-SERVICES.md) · 🇺🇦 [uk-UA](../../../uk-UA/docs/frameworks/EMBEDDED-SERVICES.md) · 🇵🇰 [ur](../../../ur/docs/frameworks/EMBEDDED-SERVICES.md) · 🇺🇿 [uz](../../../uz/docs/frameworks/EMBEDDED-SERVICES.md) · 🇻🇳 [vi](../../../vi/docs/frameworks/EMBEDDED-SERVICES.md) · 🇳🇬 [yo](../../../yo/docs/frameworks/EMBEDDED-SERVICES.md) · 🇨🇳 [zh-CN](../../../zh-CN/docs/frameworks/EMBEDDED-SERVICES.md) · 🇹🇼 [zh-TW](../../../zh-TW/docs/frameworks/EMBEDDED-SERVICES.md)

---

> **Versi:** v3.8.44
> **Kemas kini terakhir:** 2026-09-09
> **Khalayak:** Jurutera yang menambah, menyelenggara atau menyahpepijat perkhidmatan terbenam (9Router, CLIProxyAPI, Mux, Bifrost, open-wa).

Perkhidmatan terbenam ialah alat proses sampingan yang dipasang secara setempat, diselia dan didedahkan oleh OmniRoute sebagai sasaran penghalaan kelas pertama. Tidak seperti penyedia luaran (yang dicapai melalui internet menggunakan kunci API), perkhidmatan terbenam berjalan pada mesin yang sama dengan OmniRoute dan berkomunikasi melalui gelung balik.

---

## Kandungan

1. [Gambaran keseluruhan](#1-overview)
2. [Seni bina — 4 lapisan](#2-architecture--4-layers)
3. [Mesin keadaan kitar hayat](#3-lifecycle-state-machine)
4. [Rujukan API](#4-api-reference)
5. [Keselamatan](#5-security)
6. [Menambah perkhidmatan terbenam baharu](#6-adding-a-new-embedded-service)
7. [Penyelesaian masalah](#7-troubleshooting)
8. [Soalan lazim](#8-faq)

---

## 1. Gambaran Keseluruhan

### Mengapa perkhidmatan terbenam?

Enam perkhidmatan diterapkan:

| Perkhidmatan    | Pakej npm                           | Port lalai | Tujuan                                                                                                                                                                                                     |
| --------------- | ----------------------------------- | :--------: | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **9Router**     | `9router`                           |   20130    | Penghala AI yang boleh digunakan oleh OmniRoute sebagai subpenyedia. Model didedahkan sebagai `9router/{sub}/{model}`                                                                                      |
| **CLIProxyAPI** | Binari keluaran GitHub (`cliproxy`) |    8317    | Penyesuai proksi setempat untuk aliran pengesahan Anthropic CLI. Menyediakan penghalaan sandaran apabila token OAuth tamat tempoh                                                                          |
| **Mux**         | `mux` (`mux server` tanpa paparan)  |    8322    | Daemon orkestrasi ejen setempat (coder/mux). Hanya diurus kitar hayatnya — bukan sasaran penghalaan (tiada pemproksian LLM).                                                                               |
| **Bifrost**     | `@maximhq/bifrost`                  |    8080    | Bahagian belakang geganti get laluan AI Go. Apabila berjalan, dipilih secara automatik oleh laluan geganti (`/v1/relay/`)                                                                                  |
| **Dario**       | `@askalf/dario`                     |    3456    | Proksi langganan Claude — alternatif/sokongan gagal kepada CLIProxyAPI untuk trafik berformat Claude Code; kunci yang disuntik menjadi `DARIO_ADMIN_TOKEN` yang mengehadkan satah kawalan OAuth `/admin/*` |
| **open-wa**     | `@open-wa/wa-automate`              |    8323    | Automasi WhatsApp Web (Chromium tanpa paparan melalui Puppeteer). Hanya diurus kitar hayatnya — bukan sasaran penghalaan.                                                                                  |

Kesemua enam perkhidmatan menggunakan model penyeliaan yang sama:

- OmniRoute memasangnya di bawah `DATA_DIR/services/{name}/` (diasingkan daripada `package.json` milik OmniRoute)
- OmniRoute memulakan dan memantaunya sebagai proses anak
- OmniRoute menyuntik kunci API sementara ke dalam persekitaran proses anak dan menggilirkan kunci tersebut tanpa masa henti (jika berkenaan)
- Semua laluan pengurusan (`/api/services/*`) adalah **LOCAL_ONLY** — hanya boleh dicapai daripada gelung balik (peraturan tegas #17)

### Keputusan utama (daripada pelan reka bentuk)

| Keputusan                                 | Nilai                                                                                                          |
| ----------------------------------------- | -------------------------------------------------------------------------------------------------------------- |
| Akses papan pemuka kepada UI asli 9Router | Proksi songsang di `/dashboard/providers/services/9router/embed/*`                                             |
| Mekanisme pemasangan                      | `npm install {package}` melalui `execFile` (tanpa interpolasi shell)                                           |
| Mod penggunaan                            | Penyedia didaftarkan sebagai `9router/{sub}/{model}` dalam enjin penghalaan                                    |
| Pengurusan kunci API                      | OmniRoute menjana, menyulitkan ketika disimpan (AES-256-GCM), dan menyuntik melalui pemboleh ubah persekitaran |
| Lokasi papan pemuka                       | `/dashboard/providers/services` (tiga tab)                                                                     |
| Mula automatik                            | Togol bagi setiap perkhidmatan, lalai DIMATIKAN                                                                |

---

## 2. Seni bina — 4 lapisan

```
┌────────────────────────────────────────────────────────────────────┐
│  Lapisan 1 — UI                                                    │
│  /dashboard/providers/services  (tab: CLIProxyAPI | 9Router | Mux) │
│  Log langsung (SSE), Mula/Henti/Mula Semula/Kemas Kini, Tetapan,   │
│  Pasang                                                            │
│                                                                    │
│  src/app/(dashboard)/dashboard/providers/services/                 │
│    ├── page.tsx               Kerangka + penghalaan tab melalui    │
│    │                          ?tab=                                 │
│    ├── tabs/                  CliproxyServiceTab, NinerouterServiceTab,│
│    │                          MuxServiceTab                        │
│    └── components/            ServiceStatusCard, ServiceLifecycleButtons,│
│                               ServiceLogsPanel, ApiKeyCard, ...    │
└──────────────────────┬─────────────────────────────────────────────┘
                       │ HTTP (Next.js fetch)
┌──────────────────────▼─────────────────────────────────────────────┐
│  Lapisan 2 — API (LOCAL_ONLY — gelung balik sahaja)                │
│                                                                    │
│  /api/services/9router/{install|start|stop|restart|update|         │
│                          rotate-key|status|auto-start|logs}        │
│  /api/services/cliproxy/{install|start|stop|restart|update|        │
│                           status|auto-start|logs}                  │
│  /api/services/mux/{install|start|stop|restart|update|             │
│                      status|auto-start|logs}                       │
│  /dashboard/providers/services/9router/embed/[...path]             │
│    (proksi songsang HTTP + WebSocket → huluan 9Router)             │
│                                                                    │
│  Gerbang: LOCAL_ONLY_API_PREFIXES merangkumi "/api/services/" dan  │
│           "/dashboard/providers/services/*/embed/"                 │
└──────────────────────┬─────────────────────────────────────────────┘
                       │ panggilan dalam proses
┌──────────────────────▼─────────────────────────────────────────────┐
│  Lapisan 3 — ServiceSupervisor (src/lib/services/)                 │
│                                                                    │
│  ServiceSupervisor.ts   Penyelia generik (child_process.spawn)     │
│    ├── pasang:     execFile('npm', ['install', pkg, '--prefix'])    │
│    ├── mula:       spawn(node, [entrypoint], {env, cwd})           │
│    ├── kunci API:  crypto.randomBytes(32) → env NINEROUTER_API_KEY  │
│    ├── port:       20130 untuk 9Router (boleh dikonfigurasikan)    │
│    ├── log:        penimbal gelang stdio 5 MB → peristiwa SSE      │
│    ├── kesihatan:  HTTP GET /health setiap 2–5 s, pemulihan malas  │
│    └── kitar hayat: SIGTERM 15 s → SIGKILL                         │
│                                                                    │
│  registry.ts        getSupervisor(name) / registerSupervisor()     │
│  bootstrap.ts       Memulakan semua SERVICES[] ketika proses mula  │
│  apiKey.ts          getOrCreateApiKey(), generateServiceApiKey()   │
│  modelSync.ts       GET /v1/models berkala → jadual service_models │
│  ringBuffer.ts      Penimbal log bulat (5 MB bagi setiap servis)   │
│  healthCheck.ts     Prob kesihatan HTTP secara tinjauan            │
│  installers/        ninerouter.ts, cliproxy.ts, mux.ts, openwa.ts  │
│                      (penyesuai pemasang)                          │
└──────────────────────┬─────────────────────────────────────────────┘
                       │ HTTP serasi OpenAI (gelung balik)
┌──────────────────────▼─────────────────────────────────────────────┐
│  Lapisan 4 — Penyedia / Penghalaan                                 │
│                                                                    │
│  open-sse/executors/ninerouter.ts                                  │
│    Mencari semula port dan kunci API bagi setiap permintaan        │
│    (tanpa cache).                                                  │
│    Membuang awalan "9router/" daripada ID model sebelum memproksi. │
│    Mengembalikan 503 service_not_running jika penyelia tidak       │
│    berada dalam keadaan "running".                                 │
│                                                                    │
│  src/shared/constants/providers.ts                                 │
│    Entri untuk "9router": isEmbeddedService: true                  │
│                                                                    │
│  open-sse/config/providerRegistry.ts                               │
│    Model disimpan sebagai "9router/{sub}/{model}" (berawalan).     │
│    Disegerakkan setiap 5 min oleh modelSync.ts.                    │
│                                                                    │
│  Mux diuruskan dari segi kitar hayat SAHAJA (Lapisan 1-3) — ia     │
│  ialah daemon pengorkestraan ejen, bukan proksi LLM, maka ia tidak │
│  mempunyai entri pelaksana/penyedia Lapisan 4 dan tidak pernah     │
│  menjadi sasaran penghalaan.                                       │
└────────────────────────────────────────────────────────────────────┘
```

### Fail sumber utama

| Fail                                        | Peranan                                                        |
| ------------------------------------------- | -------------------------------------------------------------- |
| `src/lib/services/ServiceSupervisor.ts`     | Kelas teras: kitar hayat, kunci, kesihatan, penimbal gelang    |
| `src/lib/services/bootstrap.ts`             | Pendaftaran peringkat proses dan permulaan automatik           |
| `src/lib/services/registry.ts`              | Peta tunggal `tool → supervisor`                               |
| `src/lib/services/apiKey.ts`                | Penjanaan kunci, penyulitan AES-256-GCM bagi data tersimpan    |
| `src/lib/services/modelSync.ts`             | Penyegerakan model berkala (5 min) + atas permintaan           |
| `src/lib/services/ringBuffer.ts`            | Penimbal log bulat 5 MB dengan langganan SSE                   |
| `src/lib/services/healthCheck.ts`           | Pemeriksaan kesihatan HTTP (selang boleh dikonfigurasikan)     |
| `src/lib/services/installers/ninerouter.ts` | Pemasangan/pengemaskinian/penyahpasangan npm untuk 9Router     |
| `src/lib/services/installers/cliproxy.ts`   | Pemasangan/pengemaskinian/penyahpasangan npm untuk CLIProxyAPI |
| `src/lib/services/installers/mux.ts`        | Pemasangan/pengemaskinian/penyahpasangan npm untuk Mux         |
| `src/lib/services/installers/openwa.ts`     | Pemasangan/pengemaskinian/penyahpasangan npm untuk open-wa     |
| `src/app/api/services/9router/_lib.ts`      | Fungsi bantuan `getOrInitSupervisor()`                         |
| `src/app/api/services/[name]/logs/route.ts` | Titik akhir log SSE yang dikongsi                              |
| `open-sse/executors/ninerouter.ts`          | Pelaksana penyedia (Lapisan 4)                                 |

---

## 3. Mesin keadaan kitar hayat

```
                    install()
  ┌─────────────┐ ──────────► ┌─────────────┐
  │ not_installed│             │   stopped   │◄──────────────────┐
  └─────────────┘             └──────┬──────┘                   │
                                     │ start()                   │
                                     ▼                           │ stop()
                               ┌──────────┐                      │
                               │ starting │                      │
                               └────┬─────┘                     │
             semakan kesihatan ok   │         ranap / SIGTERM    │
                               ┌────▼─────┐  (keluar dalam 5s)  │
                               │ running  │──── ranap ──────────►┤
                               └────┬─────┘                   ┌─▼────┐
                             stop() │                          │error │
                                    ▼                          └──────┘
                               ┌──────────┐
                               │ stopping │
                               └──────────┘
```

Keadaan disimpan dalam jadual DB `version_manager` (lajur `status`) dan dicerminkan
dalam keadaan dalam memori `ServiceSupervisor`. Keadaan dalam memori ialah sumber
berwibawa bagi proses yang sedang berjalan; keadaan DB ialah sandaran berkekalan semasa but.

### Peralihan keadaan

| Dari            | Peristiwa                            | Ke                     |
| --------------- | ------------------------------------ | ---------------------- |
| `not_installed` | `install()` berjaya                  | `stopped`              |
| `stopped`       | `start()` dipanggil                  | `starting`             |
| `starting`      | semakan kesihatan mengembalikan 200  | `running`              |
| `starting`      | proses keluar sebelum sihat          | `error`                |
| `running`       | `stop()` dipanggil                   | `stopping` → `stopped` |
| `running`       | proses keluar tanpa dijangka (< 5 s) | `error` (ranap pantas) |
| `running`       | proses keluar tanpa dijangka (> 5 s) | `error`                |
| `error`         | `start()` dipanggil                  | `starting`             |
| mana-mana       | `stop()` semasa `stopping`           | tiada operasi          |

### Kunci operasi

`ServiceSupervisor` menyusun operasi kitar hayat secara bersiri melalui kunci operasi tak segerak
(`withLock()`). Panggilan `start()` serentak pada penyelia yang sama menghasilkan tepat
satu proses baharu; pemanggil kedua menunggu dan mengembalikan status sedia ada. Ini mencegah
keadaan perlumbaan apabila, contohnya, mula automatik dan butang UI dicetuskan serentak.

---

## 4. Rujukan API

Semua laluan di bawah `/api/services/` adalah **LOCAL_ONLY** (gelung balik sahaja, peraturan tegas #17).
Permintaan bukan gelung balik menerima `403 LOCAL_ONLY` tanpa mengira token pengesahan.

### 4.1 Titik akhir 9Router (11 laluan)

#### `POST /api/services/9router/install`

Pasang 9Router daripada npm. Mencipta `DATA_DIR/services/9router/` dengan
`package.json` dan `node_modules/` tersendiri. Tidak bercanggah dengan kebergantungan OmniRoute sendiri.

**Badan permintaan** (semuanya pilihan):

```json
{ "version": "latest" }
```

| Medan     | Jenis    | Lalai      | Penerangan                               |
| --------- | -------- | ---------- | ---------------------------------------- |
| `version` | `string` | `"latest"` | Tag versi npm atau semver untuk dipasang |

**Respons:**

| Status | Penerangan                                                             |
| ------ | ---------------------------------------------------------------------- |
| `200`  | `{ ok: true, installedVersion: "x.y.z", path: "..." }`                 |
| `400`  | Badan permintaan tidak sah (kegagalan pengesahan Zod)                  |
| `409`  | Sedang memasang (kunci sedang dipegang)                                |
| `500`  | Pemasangan npm gagal — lihat `message` untuk ralat yang mudah difahami |

**Nota:** Menggunakan `execFile('npm', [...])` — tiada shell, tiada interpolasi (peraturan tegas #13).
Ralat EACCES dipaparkan sebagai mesej yang mudah difahami.

---

#### `POST /api/services/9router/start`

Mulakan 9Router. Mendaftarkan penyelia jika belum didaftarkan, kemudian memanggil
`supervisor.start()`. Idempoten apabila sudah berjalan.

**Badan permintaan:** tiada

**Respons:**

| Status | Penerangan                                         |
| ------ | -------------------------------------------------- |
| `200`  | Objek `ServiceStatus` (lihat skema di bawah)       |
| `409`  | 9Router tidak dipasang (`status: "not_installed"`) |
| `503`  | Gagal dimulakan (ralat proses — lihat `lastError`) |

**Skema ServiceStatus:**

```json
{
  "tool": "9router",
  "state": "running",
  "pid": 12345,
  "port": 20130,
  "health": "healthy",
  "startedAt": "2026-05-25T10:00:00.000Z",
  "lastError": null
}
```

---

#### `POST /api/services/9router/stop`

Hentikan 9Router dengan teratur. Menghantar SIGTERM, menunggu 15 s, kemudian SIGKILL jika masih aktif.
Idempoten apabila sudah dihentikan.

**Badan permintaan:** tiada

**Respons:**

| Status | Penerangan                              |
| ------ | --------------------------------------- |
| `200`  | `ServiceStatus` (state: "stopped")      |
| `503`  | Penghentian gagal secara tidak dijangka |

---

#### `POST /api/services/9router/restart`

Bersamaan dengan `stop()` diikuti `start()` di bawah kunci operasi.

**Badan permintaan:** tiada

**Respons:** sama seperti `start` (mengembalikan `ServiceStatus` akhir).

---

#### `POST /api/services/9router/update`

Mengemas kini 9Router kepada versi npm yang lebih baharu. Jika perkhidmatan sedang berjalan, ia dihentikan
terlebih dahulu, pemasangan npm dijalankan (memasang versi lebih baharu di lokasi yang sama), kemudian
perkhidmatan dimulakan semula.

**Badan permintaan** (semuanya pilihan):

```json
{ "version": "latest" }
```

**Respons:**

| Status | Penerangan                                                      |
| ------ | --------------------------------------------------------------- |
| `200`  | `{ ok: true, previousVersion: "...", installedVersion: "..." }` |
| `400`  | Badan tidak sah                                                 |
| `500`  | Kemas kini npm gagal                                            |

---

#### `POST /api/services/9router/rotate-key`

Menjana kunci API baharu untuk 9Router, menyulitkannya ketika disimpan dan memulakan semula perkhidmatan
(jika sedang berjalan) supaya ia mengambil kunci baharu daripada persekitarannya. Kunci lama
dinyahaktifkan serta-merta.

**Badan permintaan:** tiada

**Respons:**

| Status | Penerangan                                 |
| ------ | ------------------------------------------ |
| `200`  | `{ keyRotated: true, restarted: boolean }` |
| `500`  | Putaran gagal                              |

**Keselamatan:** Kunci baharu tidak pernah dikembalikan dalam respons (tiada kebocoran kelayakan).
Ia disimpan dalam bentuk tersulit (AES-256-GCM) dalam jadual `version_manager`.

---

#### `GET /api/services/9router/status`

Mengembalikan gabungan status langsung + DB, termasuk metadata versi dan pratonton kunci API.

**Respons:**

| Status | Penerangan             |
| ------ | ---------------------- |
| `200`  | Lihat skema di bawah   |
| `500`  | Pembacaan status gagal |

**Skema respons:**

```json
{
  "tool": "9router",
  "state": "running",
  "pid": 12345,
  "port": 20130,
  "health": "healthy",
  "startedAt": "2026-05-25T10:00:00.000Z",
  "lastError": null,
  "installedVersion": "1.2.3",
  "latestVersion": "1.2.4",
  "updateAvailable": true,
  "apiKeyMasked": "nr_****abcd",
  "autoStart": false,
  "providerExpose": false
}
```

---

#### `POST /api/services/9router/auto-start`

Togol bendera mula automatik. Apabila `enabled: true`, perkhidmatan bermula secara automatik
pada kali seterusnya OmniRoute dibut (jika perkhidmatan telah dipasang).

**Badan permintaan:**

```json
{ "enabled": true }
```

**Respons:**

| Status | Penerangan            |
| ------ | --------------------- |
| `200`  | `{ autoStart: true }` |
| `400`  | Badan tidak sah       |

---

#### `GET /api/services/9router/logs`

Strim SSE bagi log langsung daripada penimbal gelang stdout/stderr 9Router.

**Parameter pertanyaan:**

| Parameter | Jenis     | Lalai | Penerangan                                                                                      |
| --------- | --------- | ----- | ----------------------------------------------------------------------------------------------- |
| `tail`    | `integer` | 200   | Bilangan baris sejarah yang dihantar terlebih dahulu (maksimum 1000)                            |
| `filter`  | `string`  | tiada | Penapis subrentetan tanpa mengira huruf besar atau kecil (tiada regex — selamat daripada ReDoS) |

**Peristiwa SSE:**

| Peristiwa   | Data        | Penerangan                     |
| ----------- | ----------- | ------------------------------ |
| `snapshot`  | `LogLine[]` | Penghujung sejarah awal        |
| `log`       | `LogLine`   | Baris log langsung             |
| `heartbeat` | `{}`        | Kekalkan sambungan setiap 15 s |

**Skema LogLine:**

```json
{
  "ts": 1716633600000,
  "stream": "stdout",
  "line": "[9router] Listening on :20130"
}
```

**Respons:**

| Status | Penerangan                                                |
| ------ | --------------------------------------------------------- |
| `200`  | `text/event-stream`                                       |
| `400`  | Parameter `filter` terlalu panjang (> 200 aksara)         |
| `404`  | Perkhidmatan tidak ditemukan (penyelia tidak didaftarkan) |

---

### 4.2 Titik akhir CLIProxyAPI (10 laluan)

CLIProxyAPI mempunyai bentuk titik akhir yang sama seperti 9Router tanpa `rotate-key`, serta
`accounts`, `provider-expose` dan `auto-restart-adopted`. Kini ia menerima
kunci API satah data khusus yang disuntik semasa pelancaran (`needsApiKey: true` dalam
`bootstrap.ts`, digunakan untuk penyegerakan model); `status` merangkumi lebih sedikit medan.

| Kaedah | Laluan                              | Penerangan                                  |
| ------ | ----------------------------------- | ------------------------------------------- |
| `POST` | `/api/services/cliproxy/install`    | Pasang CLIProxyAPI daripada npm             |
| `POST` | `/api/services/cliproxy/start`      | Mulakan CLIProxyAPI                         |
| `POST` | `/api/services/cliproxy/stop`       | Hentikan CLIProxyAPI                        |
| `POST` | `/api/services/cliproxy/restart`    | Mulakan semula CLIProxyAPI                  |
| `POST` | `/api/services/cliproxy/update`     | Kemas kini kepada versi lebih baharu        |
| `GET`  | `/api/services/cliproxy/status`     | Status langsung + DB (tanpa `apiKeyMasked`) |
| `POST` | `/api/services/cliproxy/auto-start` | Togol mula automatik                        |

Titik akhir dikongsi `GET /api/services/{name}/logs` (lihat §4.1) berfungsi untuk keempat-empat
perkhidmatan menggunakan segmen dinamik `[name]`.

---

### 4.3 Titik akhir Mux (8 laluan)

Mux mempunyai bentuk titik akhir yang sama seperti CLIProxyAPI — tiada laluan `rotate-key` pada
permukaan API (token pembawa dijana dengan cara yang sama seperti milik 9Router melalui
`getOrCreateApiKey("mux")` dan disuntik melalui pemboleh ubah persekitaran `MUX_SERVER_AUTH_TOKEN`, tetapi
belum ada titik akhir penggiliran khusus). Mux hanya diuruskan dari segi kitaran hayat: tidak seperti
9Router, ia tidak mempunyai pelaksana Lapisan 4 dan tidak pernah didaftarkan sebagai penyedia penghalaan.

| Kaedah | Laluan                         | Penerangan                               |
| ------ | ------------------------------ | ---------------------------------------- |
| `POST` | `/api/services/mux/install`    | Pasang Mux daripada npm (`npm i mux`)    |
| `POST` | `/api/services/mux/start`      | Mulakan Mux (`mux server`)               |
| `POST` | `/api/services/mux/stop`       | Hentikan Mux                             |
| `POST` | `/api/services/mux/restart`    | Mulakan semula Mux                       |
| `POST` | `/api/services/mux/update`     | Kemas kini kepada versi npm lebih baharu |
| `GET`  | `/api/services/mux/status`     | Status langsung + DB                     |
| `POST` | `/api/services/mux/auto-start` | Togol mula automatik                     |

---

### 4.4 Titik akhir Bifrost (8 laluan)

Bifrost ialah bahagian belakang geganti get laluan AI Go (`@maximhq/bifrost`). Ia menggunakan bentuk
titik akhir yang sama seperti CLIProxyAPI (tiada `rotate-key` — Bifrost menguruskan kunci penyedianya
sendiri dalam `config.json` di bawah `-app-dir`).

| Kaedah | Laluan                             | Penerangan                                                     |
| ------ | ---------------------------------- | -------------------------------------------------------------- |
| `POST` | `/api/services/bifrost/install`    | Pasang Bifrost daripada npm (`@maximhq/bifrost`)               |
| `POST` | `/api/services/bifrost/start`      | Mulakan Bifrost pada port 8080 (lalai)                         |
| `POST` | `/api/services/bifrost/stop`       | Hentikan Bifrost                                               |
| `POST` | `/api/services/bifrost/restart`    | Mulakan semula Bifrost                                         |
| `POST` | `/api/services/bifrost/update`     | Kemas kini kepada versi lebih baharu                           |
| `GET`  | `/api/services/bifrost/status`     | Status langsung + DB                                           |
| `POST` | `/api/services/bifrost/auto-start` | Togol mula automatik                                           |
| `GET`  | `/api/services/bifrost/logs`       | Hujung log SSE (melalui laluan dinamik `[name]/logs` dikongsi) |

**Pendawaian penghalaan:** Apabila `BIFROST_BASE_URL` tidak ditetapkan dan tika Bifrost
yang diselia sedang berjalan, `getBifrostRoutingConfig()` (dalam `routingBackend.ts`) secara automatik
menggunakan `http://127.0.0.1:{port}` sebagai URL asas geganti. Pemboleh ubah persekitaran
`BIFROST_BASE_URL` yang ditetapkan secara eksplisit sentiasa diutamakan.

---

### 4.5 Titik akhir Dario (12 laluan)

Bentuk kitaran hayat yang sama seperti perkhidmatan lain (`install`, `start`, `stop`, `restart`,
`update`, `status`, `auto-start`, `auto-restart-adopted`) serta satah kawalan OAuth yang
dilindungi token di bawah `admin/`: `admin/accounts`, `admin/import-from-omniroute`,
`admin/login-start`, `admin/login-complete` (semuanya dilindungi oleh `DARIO_ADMIN_TOKEN`).

### 4.6 Titik akhir open-wa (7 laluan)

open-wa (`@open-wa/wa-automate`) mengendalikan tika Chromium tanpa kepala (melalui
Puppeteer) untuk mengautomatikkan WhatsApp Web. Ia menggunakan bentuk titik akhir yang sama seperti Mux (belum
ada laluan `rotate-key`). Ia hanya diuruskan dari segi kitaran hayat — bukan sasaran penghalaan,
tiada entri pelaksana/penyedia Lapisan 4.

| Kaedah | Laluan                            | Penerangan                                                        |
| ------ | --------------------------------- | ----------------------------------------------------------------- |
| `POST` | `/api/services/openwa/install`    | Pasang open-wa daripada npm (`@open-wa/wa-automate`)              |
| `POST` | `/api/services/openwa/start`      | Mulakan open-wa pada port 8323 (lalai)                            |
| `POST` | `/api/services/openwa/stop`       | Hentikan open-wa                                                  |
| `POST` | `/api/services/openwa/restart`    | Mulakan semula open-wa                                            |
| `POST` | `/api/services/openwa/update`     | Kemas kini kepada versi yang lebih baharu                         |
| `GET`  | `/api/services/openwa/status`     | Status langsung + DB                                              |
| `POST` | `/api/services/openwa/auto-start` | Togol permulaan automatik                                         |
| `GET`  | `/api/services/openwa/logs`       | Ekor log SSE (melalui laluan dinamik `[name]/logs` yang dikongsi) |

**Kunci API:** disuntik sebagai `WA_KEY` — penggantian env generik open-wa yang
diawali `WA_*` memetakannya kepada pilihan CLI `--key`/`-k`
(`dist/cli/setup.js::envArgs()`, disahkan terhadap pakej 4.76.0 yang dipasang).
Diawali `ow_` apabila dijana oleh `generateServiceApiKey()`. open-wa membaca
semula kunci daripada pengepala HTTP `key`/`api_key` (bukan `Authorization:
Bearer`); `/api-docs*` dikecualikan secara eksplisit daripada semakan
(`setupAuthenticationLayer` dalam `dist/cli/server.js`), jadi prob kesihatan
tidak memerlukan pengepala pengesahan.

**Penggandingan:** open-wa tidak rasmi dan tidak mempunyai kaitan dengan WhatsApp —
nombor yang disambungkan menghadapi risiko disekat akibat pengesanan automasi
WhatsApp sendiri. Pada permulaan pertama, kod QR penggandingan dicetak ke stdout
dan dipaparkan melalui panel Log/strim SSE sedia ada — masih belum terdapat titik
akhir imej QR khusus dalam penyepaduan ini.

---

### 4.7 Proksi songsang (benaman papan pemuka 9Router)

Papan pemuka membenamkan UI web 9Router di dalam iframe melalui proksi songsang
dalaman di:

```
GET|POST|... /dashboard/providers/services/9router/embed/[...path]
```

Proksi ini:

- Memajukan permintaan kepada `http://127.0.0.1:{port}/{path}` (gelung balik sahaja)
- Membuang pengepala `cookie` dan `authorization` yang masuk (tiada kebocoran sesi OmniRoute)
- Menyuntik `Authorization: Bearer {apiKey}` untuk pengesahan 9Router
- Membuang `set-cookie`, `content-security-policy`, `x-frame-options`, `cross-origin-*` daripada respons
- Menulis semula respons HTML untuk menyuntik `<base href>` dan menormalkan laluan mutlak (`/foo` → `/dashboard/.../embed/foo`)

Peningkatan WebSocket untuk papan pemuka terbenam dikendalikan oleh pelayan
pendamping pada port khusus (lihat `src/lib/services/embedWsProxy.ts`).

**Keselamatan:** Laluan proksi benaman diklasifikasikan di bawah `LOCAL_ONLY_API_PREFIXES`
dan hanya boleh dicapai daripada gelung balik. Penyerang yang memperoleh JWT melalui
terowong Cloudflare/Ngrok tidak boleh membuat proksi ke dalam perkhidmatan terbenam.

---

## 5. Keselamatan

### Penguatkuasaan LOCAL_ONLY (peraturan keras #17)

Semua laluan di bawah `/api/services/` dan `/dashboard/providers/services/*/embed/`
diklasifikasikan sebagai LOCAL_ONLY dalam `src/server/authz/routeGuard.ts`. Semakan loopback
dijalankan tanpa syarat sebelum mana-mana cabang pengesahan:

```
permintaan tiba
  → isLocalOnlyPath(path)?
      → bukan loopback → 403 LOCAL_ONLY (sentiasa, sebelum semakan pengesahan)
      → loopback       → teruskan kepada pengesahan biasa
```

Ini menghalang JWT yang bocor (contohnya, melalui terowong) daripada mencetuskan `npm install` atau
penciptaan proses. Lihat `docs/security/ROUTE_GUARD_TIERS.md` untuk matriks peringkat
penuh.

### Suntikan kunci API

9Router dan Mux memerlukan kunci API/token pembawa untuk titik akhir HTTP mereka sendiri.
OmniRoute:

1. Menjana kunci melalui `crypto.randomBytes(32).toString("base64url")` dengan
   awalan khusus perkhidmatan (`nr_` untuk 9Router, `mx_` untuk Mux).
2. Menyulitkannya ketika disimpan menggunakan AES-256-GCM (sifer yang sama digunakan untuk kelayakan penyedia).
3. Menyahsulit dan menyuntiknya sebagai pemboleh ubah persekitaran semasa penciptaan proses —
   `NINEROUTER_API_KEY` untuk 9Router, `MUX_SERVER_AUTH_TOKEN` untuk Mux (tidak sekali-kali sebagai bendera CLI,
   supaya token tidak pernah muncul dalam penyenaraian `ps`/proses).
4. Tidak pernah mengembalikan kunci teks biasa dalam sebarang respons HTTP.

CLIProxyAPI menerima kunci satah data khusus yang disuntik semasa penciptaan proses
(`needsApiKey: true` — digunakan untuk penyegerakan model terhadap penyesuai).

### Pertahanan SSRF

Proksi HTTP songsang (`/dashboard/.../embed/[...path]`) dikodkan secara tetap untuk memajukan
hanya kepada `http://127.0.0.1:{port}`. Ia tidak pernah mengikuti ubah hala ke destinasi
bukan loopback. Pustaka `ssrf-req-filter` digunakan untuk menolak sebarang URL huluan yang
ditentukan berada di luar julat loopback.

### Keselamatan shell (peraturan keras #13)

`npm install` dipanggil melalui `execFile('npm', ['install', pkg, '--prefix', dir])` —
tiada literal templat, tiada shell dan tiada interpolasi laluan luaran ke dalam rentetan
perintah. Nilai masa jalan (port, kunci API) dihantar melalui objek `env` proses anak.

### Pensanitasian ralat (peraturan keras #12)

Semua respons ralat daripada `/api/services/*` melalui `buildErrorBody()` atau
`sanitizeErrorMessage()`. `err.stack` dan `err.message` mentah tidak pernah dikembalikan
secara verbatim kepada pemanggil.

---

## 6. Menambah perkhidmatan terbenam baharu

Ikuti 8 langkah ini. Baca pelaksanaan sedia ada dalam `src/lib/services/installers/`
dan `src/app/api/services/` sebagai rujukan kanonik.

### Langkah 1 — Cipta pemasang

Cipta `src/lib/services/installers/{name}.ts` berdasarkan `ninerouter.ts`:

```typescript
export const NAME_PACKAGE = "your-npm-package";
export const NAME_DEFAULT_PORT = 20132; // pilih port yang belum digunakan

export async function install(version = "latest"): Promise<InstallResult> { ... }
export async function update(version = "latest"): Promise<InstallResult> { ... }
export async function uninstall(): Promise<void> { ... }
export function resolveSpawnArgs(apiKey: string, port: number): SpawnArgs { ... }
export async function getInstalledVersion(): Promise<string | null> { ... }
export async function getLatestVersion(): Promise<string | null> { ... }
```

Gunakan `runNpm(['install', NAME_PACKAGE, '--prefix', dir])` daripada `installers/utils.ts`
— jangan sekali-kali gunakan `execSync` atau interpolasi shell.

### Langkah 2 — Daftarkan dalam pemula

Tambahkan `ServiceEntry` pada tatasusunan `SERVICES` dalam `src/lib/services/bootstrap.ts`:

```typescript
{
  tool: "myservice",
  port: NAME_DEFAULT_PORT,
  healthPath: "/health",
  healthIntervalMs: 5_000,
  stopTimeoutMs: 15_000,
  logsBufferBytes: 5_242_880,
  needsApiKey: true, // false jika kunci API tidak diperlukan
}
```

Lanjutkan `buildSpawnArgsFactory()` untuk mengendalikan `cfg.tool === "myservice"`.

#### Kontrak pemalam penyedia boleh pasang (Fasa 1, #7333)

`src/lib/services/providerPlugins/` memperkenalkan kontrak `ServiceProviderPlugin` yang
menghimpunkan medan `ServiceEntry` daripada `bootstrap.ts` bahagian belakang dan medan
templat manifes `serviceBackends.ts` ke dalam satu objek, dan bukannya bentuk bahagian
belakang yang sama dinyatakan secara berasingan dalam dua fail yang tidak berkaitan. Setakat penulisan ini, **hanya `9router` telah
dimigrasikan** — `bootstrap.ts` memperoleh entri `SERVICES[]` daripada
`getServiceProviderPlugin("9router")` (`src/lib/services/providerPlugins/registry.ts`),
dan mencetuskan ralat permulaan jika pemalam tersebut tiada. `cliproxy`, `mux` dan `bifrost`
kekal menggunakan literal `SERVICES[]` sebaris sedia ada tanpa perubahan.

`open-sse/config/providerPluginManifest.ts` turut menerima fungsi pembantu tambahan
`createServiceBackendManifestEntry(pluginId, template)` yang membina
`ProviderPluginManifestEntry` yang terbentuk dengan baik daripada entri `SERVICE_BACKEND_MANIFEST_TEMPLATE` — ia
**belum** disambungkan kepada mana-mana laluan permintaan aktif (bukan
`generateProviderPluginManifestFromRegistry()` mahupun `/v1/providers/[provider]/models`);
perkara itu kekal sebagai tindakan susulan selepas kontrak tersebut dibuktikan untuk bahagian belakang kedua.

Ditangguhkan kepada PR susulan dan dijejaki di bawah isu #7333: memigrasikan `cliproxyapi` melalui
pendaftaran yang sama, menggeneralisasikan `mux`/`bifrost` ke dalam kesatuan `ServiceBackendPluginId`,
menggabungkan pengendalian khas penghalaan pelaksana (`open-sse/executors/index.ts`,
`open-sse/handlers/chatCore/executorProxy.ts`) ke dalam kontrak pemalam, dan menyambungkan
`createServiceBackendManifestEntry()` kepada laluan kod manifes/model yang aktif.

### Langkah 3 — Tambahkan migrasi dan data awal DB

Pastikan perkhidmatan mempunyai baris dalam `version_manager` melalui migrasi dalam
`src/lib/db/migrations/`. Baris tersebut hendaklah mengandungi:

```sql
INSERT OR IGNORE INTO version_manager (tool, status, auto_start, provider_expose)
VALUES ('myservice', 'not_installed', 0, 0);
```

### Langkah 4 — Cipta 7 titik akhir API

Di bawah `src/app/api/services/{name}/`:

```
_lib.ts            pembantu getOrInitSupervisor()
install/route.ts   POST — memanggil installer.install()
start/route.ts     POST — memanggil supervisor.start()
stop/route.ts      POST — memanggil supervisor.stop()
restart/route.ts   POST — memanggil supervisor.restart()
update/route.ts    POST — memanggil installer.update()
status/route.ts    GET  — menggabungkan status langsung + DB
auto-start/route.ts POST — menogol bendera auto_start
```

Laluan dikongsi `GET /api/services/[name]/logs` telah pun disambungkan — tiada perubahan
diperlukan di situ.

Delegasikan semua respons ralat melalui `createErrorResponse()` / `buildErrorBody()`.

### Langkah 5 — Tambahkan pada LOCAL_ONLY_API_PREFIXES

Dalam `src/server/authz/routeGuard.ts`, sahkan bahawa `/api/services/` telah pun disenaraikan.
Jika anda memperkenalkan awalan baharu (contohnya, `/api/tools/`), tambahkannya pada kedua-dua
`LOCAL_ONLY_API_PREFIXES` dan, jika ia melancarkan proses, pada `SPAWN_CAPABLE_PREFIXES`.
Tambahkan ujian dalam `tests/unit/authz/routeGuard.test.ts`.

### Langkah 6 — Tambahkan tab UI

Cipta `src/app/(dashboard)/dashboard/providers/services/tabs/{Name}ServiceTab.tsx`.
Gunakan semula komponen dikongsi:

- `ServiceStatusCard` — keadaan masa nyata + lencana kesihatan
- `ServiceLifecycleButtons` — Mulakan / Hentikan / Mulakan Semula / Kemas Kini
- `ServiceLogsPanel` — ekor log SSE (bersambung ke `/api/services/{name}/logs`)
- `ApiKeyCard` — dedahkan + putar kunci (jika `needsApiKey: true`)

Daftarkan tab tersebut dalam `ServicesPageShell.tsx`.

### Langkah 7 — Tambahkan entri penyedia (jika perkhidmatan ialah sasaran penghalaan)

Jika perkhidmatan terbenam mendedahkan titik akhir serasi OpenAI `/v1/chat/completions`:

1. Tambahkan entri penyedia dalam `src/shared/constants/providers.ts` dengan `isEmbeddedService: true`.
2. Cipta `open-sse/executors/{name}.ts` yang memperluas `BaseExecutor`. Dapatkan semula port dan
   kunci API bagi setiap permintaan (jangan sekali-kali cache dalam pembina). Kembalikan respons
   `503 service_not_running` apabila keadaan penyelia bukan `"running"`.
3. Daftarkan model dalam `open-sse/config/providerRegistry.ts` dengan awalan perkhidmatan
   (contohnya, `myservice/sub/model`). `modelSync.ts` akan memastikan model sentiasa dikemas kini.

### Langkah 8 — Dokumentasikan dan uji

1. Kemas kini `docs/frameworks/EMBEDDED-SERVICES.md` (fail ini) — tambahkan perkhidmatan pada
   jadual dalam §1 dan mana-mana titik akhir baharu pada §4.
2. Tambahkan ujian unit dalam `tests/unit/services/` (kitaran hayat, pemasang, bentuk API).
3. Tambahkan ujian integrasi dalam `tests/integration/services/` (di sebalik `RUN_SERVICES_INT=1`).
4. Kemas kini `docs/openapi.yaml` dengan titik akhir baharu.

---

## 7. Penyelesaian Masalah

### Perkhidmatan tidak dapat dimulakan

**Gejala:** Butang mula mengembalikan 503, keadaan kekal `"error"` atau `"starting"`.

**Senarai semak:**

1. Semak `GET /api/services/{name}/logs` (atau panel Log dalam papan pemuka). Cari
   baris seperti `Error: ENOENT`, `address already in use`, atau `Cannot find module`.
2. Sahkan `npm` berada dalam PATH: jalankan `which npm` daripada akaun pengguna yang sama yang menjalankan OmniRoute.
3. Sahkan perkhidmatan telah dipasang: semak `GET /api/services/{name}/status` untuk
   `installedVersion`. Jika `null`, jalankan pemasangan terlebih dahulu.
4. Semak bahawa `DATA_DIR/services/{name}/node_modules/` wujud dan tidak kosong.
5. Semak medan `lastError` dalam respons status untuk sebab keluar yang telah disanitasi.

---

### Permulaan sejuk adalah perlahan (> 10 s untuk mencapai `running`)

**Gejala:** Keadaan kekal `"starting"` untuk tempoh yang lama sebelum bertukar kepada `"running"` atau `"error"`.

**Penjelasan:** Permulaan sejuk 9Router merangkumi pengimportan pepohon kebergantungan yang besar (modul DNS,
terowong, MITM). Selang pemeriksaan kesihatan lalai ialah 2 s dengan 3 percubaan sebelum
penyelia mengisytiharkan tamat masa (tetapi terus membuat tinjauan).

**Penyelesaian:** `healthIntervalMs` dan tamat masa `waitForHealthy`
(`healthIntervalMs * 3`) boleh dikonfigurasikan dalam `bootstrap.ts`. Untuk perkhidmatan dengan masa
permulaan yang lebih lama, tingkatkan `healthIntervalMs` kepada 5000 dan `stopTimeoutMs` kepada 30 000.

---

### Pertembungan port (`EADDRINUSE`)

**Gejala:** Log menunjukkan `address already in use :::20130`.

**Punca:**

- Proses lain sedang menggunakan port 20130.
- Proses 9Router terdahulu tidak dihentikan sepenuhnya (PID zombi).

**Penyelesaian:**

1. Tukar port lalai melalui pemboleh ubah persekitaran `NINEROUTER_PORT` dalam `.env`.
2. Cari dan matikan proses yang berkonflik: `lsof -ti :20130 | xargs kill -9`.
3. Port boleh dikonfigurasikan bagi setiap perkhidmatan dalam `bootstrap.ts` melalui medan `port`.

**Nota:** 9Router menggunakan port 20130 secara lalai khusus untuk mengelakkan pertembungan dengan
port lalai OmniRoute, iaitu 20128.

---

### Kebenaran ditolak (EACCES) semasa pemasangan

**Gejala:** Pemasangan mengembalikan 500, log menunjukkan `EACCES` atau `permission denied`.

**Punca:**

- `DATA_DIR` atau direktori induknya tidak boleh ditulis oleh proses OmniRoute.
- Berjalan dalam Docker tanpa akar tanpa akses tulis kepada volum yang dipetakan.

**Penyelesaian:**

1. Semak `DATA_DIR` (lalai: `~/.omniroute/`): `ls -la ~/.omniroute/`
2. Pastikan pengguna proses OmniRoute memiliki direktori tersebut: `chown -R $USER ~/.omniroute/`
3. Dalam Docker, pastikan lekapan volum mempunyai kebenaran yang betul untuk pengguna bekas.

---

### Kemas kini gagal (tamat masa `npm install` atau ralat rangkaian)

**Gejala:** Kemas kini mengembalikan 500 dengan `InstallError`, log menunjukkan tamat masa rangkaian.

**Senarai semak:**

1. Sahkan pendaftaran npm boleh dicapai: `npm ping`.
2. Semak proksi korporat: `npm config get proxy`, `npm config get https-proxy`.
3. Cuba pemasangan secara manual: `npm install {package}@latest --prefix ~/.omniroute/services/{name}/`.
4. Jika berada di belakang rangkaian terpencil, muat turun awal fail tar dan gunakan `npm install /path/to/tarball.tgz`.

---

### Perkhidmatan menunjukkan keadaan `"error"` serta-merta selepas dimulakan (ranap pantas)

**Gejala:** Keadaan berubah daripada `"starting"` kepada `"error"` dalam masa kurang daripada 5 saat.
`lastError` menunjukkan `"Fast crash (exited with code 1)"`.

**Senarai semak:**

1. Baca keseluruhan penghujung log: `GET /api/services/{name}/logs?tail=500`.
2. Punca lazim: pemboleh ubah persekitaran yang diperlukan oleh perkhidmatan tiada.
3. Untuk 9Router: sahkan `NINEROUTER_DISABLE_MITM=true` dan
   `NINEROUTER_DISABLE_TUNNEL=true` terdapat dalam persekitaran yang dihantar semasa proses diwujudkan (lihat
   `installers/ninerouter.ts` `resolveSpawnArgs`).

---

## 8. Soalan Lazim

**S: Bolehkah saya mendedahkan titik akhir perkhidmatan terbenam kepada klien bukan gelung balik?**

Tidak. Tahap LOCAL_ONLY ditetapkan sedemikian dengan sengaja (peraturan mutlak #17). Laluan yang boleh menjalankan
`npm install` atau memulakan proses `node` tidak boleh dicapai daripada trafik bukan gelung balik,
kerana JWT yang bocor melalui terowong (Cloudflare, Ngrok, Tailscale) akan membolehkan
proses sewenang-wenangnya dimulakan. Tiada pengecualian untuk
`/api/services/` — tidak seperti `/api/mcp/`, laluan ini dikecualikan daripada senarai pintasan skop pengurusan.
Lihat `docs/security/ROUTE_GUARD_TIERS.md`.

---

**S: Adakah 9Router dan CLIProxyAPI akan tersedia dalam penggunaan produksi/awan?**

Ya. Kedua-dua perkhidmatan menggunakan model mengutamakan setempat yang sama seperti OmniRoute. Perkhidmatan tersebut berjalan
pada mesin yang sama dan berkomunikasi melalui gelung balik. "Produksi" di sini bermaksud VPS
atau pelayan setempat tempat OmniRoute digunakan, bukan penyedia awan jauh.

---

**S: Bagaimanakah cara untuk saya menyahpepijat penyelia?**

1. Ikuti strim log SSE: `curl -N http://localhost:20128/api/services/9router/logs`.
2. Semak log berstruktur dalam output pino OmniRoute yang ditapis mengikut
   ruang nama `service:supervisor`.
3. Periksa baris DB: `sqlite3 ~/.omniroute/omniroute.db "SELECT * FROM version_manager WHERE tool='9router'"`.
4. Gunakan `GET /api/services/9router/status` untuk melihat keadaan langsung semasa, PID, kesihatan
   dan `lastError` dalam satu panggilan.

---

**S: Penyelia menunjukkan `health: "degraded"` atau `health: "unknown"` tetapi keadaannya ialah `"running"`. Adakah itu suatu masalah?**

`"degraded"` bermaksud prob kesihatan mengembalikan respons selain 200. `"unknown"` bermaksud belum ada
prob yang selesai (keadaan perlumbaan dengan tinjauan pertama). Kedua-duanya bersifat sementara semasa permulaan.
Jika kesihatan kekal `"degraded"` selama lebih daripada `healthIntervalMs * 3` ms selepas
`"running"`, perkhidmatan terbenam sedang berjalan tetapi API HTTP perkhidmatan tersebut tidak memberikan respons. Semak
sama ada port dalam respons status adalah betul dan sama ada perkhidmatan benar-benar
mendengar pada port tersebut.

---

**S: Bolehkah saya menukar kunci API 9Router tanpa memulakan semula sepenuhnya?**

Tidak. Kunci API dihantar kepada 9Router melalui pemboleh ubah persekitaran semasa proses dimulakan.
Pemboleh ubah persekitaran tidak boleh diubah dalam proses yang sedang berjalan. `POST .../rotate-key`
menghentikan dan memulakan semula perkhidmatan secara automatik untuk menggunakan kunci baharu. Pemutaran kunci
berkuat kuasa dalam tempoh `stopTimeoutMs` perkhidmatan (lalai 15 s) ditambah masa
permulaannya.

---

**S: Apakah had penimbal gelang dan apakah yang berlaku apabila ia penuh?**

Setiap perkhidmatan mempunyai penimbal gelang khusus sebesar 5 MB. Apabila penimbal penuh, baris
log paling lama disingkirkan untuk memberi ruang kepada baris baharu. Peristiwa `snapshot` SSE mengembalikan
baris terkini dalam had `tail`. Log tidak disimpan secara berterusan pada cakera melainkan
`logsBufferPath` ditetapkan dalam baris DB.

---

## Lihat juga

- `docs/security/ROUTE_GUARD_TIERS.md` — butiran tahap LOCAL_ONLY
- `docs/architecture/CODEBASE_DOCUMENTATION.md` — §3.2 pemetaan modul Perkhidmatan Terbenam
- `docs/architecture/ARCHITECTURE.md` — konteks peringkat sistem
- `docs/openapi.yaml` — takrif titik akhir yang boleh dibaca mesin
- `CLAUDE.md` §"Menambah Perkhidmatan Terbenam Baharu" — senarai semak rujukan pantas
