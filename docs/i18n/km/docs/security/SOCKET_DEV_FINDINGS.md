# Socket.dev / supply-chain finding attestation (ខ្មែរ)

🌐 **Languages:** 🇺🇸 [English](../../../../security/SOCKET_DEV_FINDINGS.md) · 🇪🇹 [am](../../../am/docs/security/SOCKET_DEV_FINDINGS.md) · 🇸🇦 [ar](../../../ar/docs/security/SOCKET_DEV_FINDINGS.md) · 🇦🇿 [az](../../../az/docs/security/SOCKET_DEV_FINDINGS.md) · 🇧🇬 [bg](../../../bg/docs/security/SOCKET_DEV_FINDINGS.md) · 🇧🇩 [bn](../../../bn/docs/security/SOCKET_DEV_FINDINGS.md) · 🇨🇿 [cs](../../../cs/docs/security/SOCKET_DEV_FINDINGS.md) · 🇩🇰 [da](../../../da/docs/security/SOCKET_DEV_FINDINGS.md) · 🇩🇪 [de](../../../de/docs/security/SOCKET_DEV_FINDINGS.md) · 🇬🇷 [el](../../../el/docs/security/SOCKET_DEV_FINDINGS.md) · 🇪🇸 [es](../../../es/docs/security/SOCKET_DEV_FINDINGS.md) · 🇪🇪 [et](../../../et/docs/security/SOCKET_DEV_FINDINGS.md) · 🇮🇷 [fa](../../../fa/docs/security/SOCKET_DEV_FINDINGS.md) · 🇫🇮 [fi](../../../fi/docs/security/SOCKET_DEV_FINDINGS.md) · 🇫🇷 [fr](../../../fr/docs/security/SOCKET_DEV_FINDINGS.md) · 🇮🇪 [ga](../../../ga/docs/security/SOCKET_DEV_FINDINGS.md) · 🇮🇳 [gu](../../../gu/docs/security/SOCKET_DEV_FINDINGS.md) · 🇳🇬 [ha](../../../ha/docs/security/SOCKET_DEV_FINDINGS.md) · 🇮🇱 [he](../../../he/docs/security/SOCKET_DEV_FINDINGS.md) · 🇮🇳 [hi](../../../hi/docs/security/SOCKET_DEV_FINDINGS.md) · 🇭🇷 [hr](../../../hr/docs/security/SOCKET_DEV_FINDINGS.md) · 🇭🇺 [hu](../../../hu/docs/security/SOCKET_DEV_FINDINGS.md) · 🇦🇲 [hy](../../../hy/docs/security/SOCKET_DEV_FINDINGS.md) · 🇮🇩 [id](../../../id/docs/security/SOCKET_DEV_FINDINGS.md) · 🇳🇬 [ig](../../../ig/docs/security/SOCKET_DEV_FINDINGS.md) · 🇮🇹 [it](../../../it/docs/security/SOCKET_DEV_FINDINGS.md) · 🇯🇵 [ja](../../../ja/docs/security/SOCKET_DEV_FINDINGS.md) · 🇬🇪 [ka](../../../ka/docs/security/SOCKET_DEV_FINDINGS.md) · 🇮🇳 [kn](../../../kn/docs/security/SOCKET_DEV_FINDINGS.md) · 🇰🇷 [ko](../../../ko/docs/security/SOCKET_DEV_FINDINGS.md) · 🇱🇹 [lt](../../../lt/docs/security/SOCKET_DEV_FINDINGS.md) · 🇱🇻 [lv](../../../lv/docs/security/SOCKET_DEV_FINDINGS.md) · 🇮🇳 [ml](../../../ml/docs/security/SOCKET_DEV_FINDINGS.md) · 🇮🇳 [mr](../../../mr/docs/security/SOCKET_DEV_FINDINGS.md) · 🇲🇾 [ms](../../../ms/docs/security/SOCKET_DEV_FINDINGS.md) · 🇲🇹 [mt](../../../mt/docs/security/SOCKET_DEV_FINDINGS.md) · 🇲🇲 [my](../../../my/docs/security/SOCKET_DEV_FINDINGS.md) · 🇳🇵 [ne](../../../ne/docs/security/SOCKET_DEV_FINDINGS.md) · 🇳🇱 [nl](../../../nl/docs/security/SOCKET_DEV_FINDINGS.md) · 🇳🇴 [no](../../../no/docs/security/SOCKET_DEV_FINDINGS.md) · 🇮🇳 [or](../../../or/docs/security/SOCKET_DEV_FINDINGS.md) · 🇮🇳 [pa](../../../pa/docs/security/SOCKET_DEV_FINDINGS.md) · 🇵🇭 [phi](../../../phi/docs/security/SOCKET_DEV_FINDINGS.md) · 🇵🇱 [pl](../../../pl/docs/security/SOCKET_DEV_FINDINGS.md) · 🇵🇹 [pt](../../../pt/docs/security/SOCKET_DEV_FINDINGS.md) · 🇧🇷 [pt-BR](../../../pt-BR/docs/security/SOCKET_DEV_FINDINGS.md) · 🇷🇴 [ro](../../../ro/docs/security/SOCKET_DEV_FINDINGS.md) · 🇷🇺 [ru](../../../ru/docs/security/SOCKET_DEV_FINDINGS.md) · 🇱🇰 [si](../../../si/docs/security/SOCKET_DEV_FINDINGS.md) · 🇸🇰 [sk](../../../sk/docs/security/SOCKET_DEV_FINDINGS.md) · 🇸🇮 [sl](../../../sl/docs/security/SOCKET_DEV_FINDINGS.md) · 🇷🇸 [sr](../../../sr/docs/security/SOCKET_DEV_FINDINGS.md) · 🇸🇪 [sv](../../../sv/docs/security/SOCKET_DEV_FINDINGS.md) · 🇰🇪 [sw](../../../sw/docs/security/SOCKET_DEV_FINDINGS.md) · 🇮🇳 [ta](../../../ta/docs/security/SOCKET_DEV_FINDINGS.md) · 🇮🇳 [te](../../../te/docs/security/SOCKET_DEV_FINDINGS.md) · 🇹🇭 [th](../../../th/docs/security/SOCKET_DEV_FINDINGS.md) · 🇹🇷 [tr](../../../tr/docs/security/SOCKET_DEV_FINDINGS.md) · 🇺🇦 [uk-UA](../../../uk-UA/docs/security/SOCKET_DEV_FINDINGS.md) · 🇵🇰 [ur](../../../ur/docs/security/SOCKET_DEV_FINDINGS.md) · 🇺🇿 [uz](../../../uz/docs/security/SOCKET_DEV_FINDINGS.md) · 🇻🇳 [vi](../../../vi/docs/security/SOCKET_DEV_FINDINGS.md) · 🇳🇬 [yo](../../../yo/docs/security/SOCKET_DEV_FINDINGS.md) · 🇨🇳 [zh-CN](../../../zh-CN/docs/security/SOCKET_DEV_FINDINGS.md) · 🇹🇼 [zh-TW](../../../zh-TW/docs/security/SOCKET_DEV_FINDINGS.md)

---

> **កំណត់សម្គាល់អំពីវិសាលភាព៖** `socket.yml` កំណត់រចនាសម្ព័ន្ធការស្កេនក្រោយការបោះពុម្ពនៅផ្នែក registry របស់ Socket.dev លើ artifact របស់ npm (មិនអើពើ path សម្រាប់មាតិកាដែលមិនត្រូវបានដាក់បញ្ចូលក្នុងការចែកចាយ ដូចជា `tests/`, `docs/` និងរបាយការណ៍ build)។ វាមិនតភ្ជាប់ gate សម្រាប់ការ merge នៅ CI/PR ទេ — គ្មាន workflow នៅក្នុង `.github/workflows` គ្មាន script ក្នុង `package.json` និងគ្មាន target ក្នុង `Makefile` ណាមួយហៅប្រើ Socket.dev ទេ។

ឯកសារនេះគឺជាសេចក្តីបញ្ជាក់ដែលរៀបចំដោយអ្នកថែទាំ សម្រាប់លទ្ធផលរកឃើញចំនួនប្រាំមួយអំពី
`AI-detected potential malware` ដែលត្រូវបានរាយការណ៍ចំពោះ `omniroute@3.8.5` និង
វិធានការកាត់បន្ថយដែលបានអនុវត្តក្នុង `omniroute@3.8.6`។ ឯកសារនេះមានឡើងដើម្បីឱ្យ៖

1. អ្នកប្រតិបត្តិប្រព័ន្ធសុវត្ថិភាពមានឯកសារយោងតែមួយសម្រាប់ដកស្រង់ នៅពេលពួកគេត្រូវ
   វាយតម្លៃលទ្ធផលរកឃើញទាំងនេះធៀបនឹង source ពិតប្រាកដ។
2. ឧបករណ៍ស្កេន AI នាពេលអនាគតអាចចាប់យកការអះអាងដែលចុះហត្ថលេខាដោយអ្នកថែទាំថា path
   ដែលត្រូវបានដាក់ទង់នីមួយៗគឺមានចេតនា ជាជម្រើសដែលត្រូវបើកប្រើ និងមានឯកសារពន្យល់។
3. យើងមានកំណត់ត្រាជាលាយលក្ខណ៍អក្សរអំពី _មូលហេតុ_ ដែល call site នីមួយៗត្រូវបានរៀបចំតាមរបៀបនេះ —
   ដើម្បីកុំឱ្យការ refactor នាពេលអនាគតបញ្ចូល fingerprint ដែលត្រូវបានដកចេញដោយចេតនាមកវិញ
   ដោយអចេតនា។

ប្រសិនបើអ្នកដំណើរការឧបករណ៍ស្កេនដែលដាក់ទង់ call site ណាមួយខាងក្រោមម្តងទៀត បន្ទាប់ពី
វិធានការកាត់បន្ថយរបស់ v3.8.6 ត្រូវបានចេញផ្សាយ សូមបើក issue មួយជាមួយ scan trace ហើយ
យើងនឹងពង្រីកសេចក្តីបញ្ជាក់នៅទីនេះ។

---

## §1 — ការដំឡើង root-CA សម្រាប់ MITM (`77484.js`)

**ឯកសារប្រភព**៖

- `src/mitm/cert/install.ts` — `installCert()` / `uninstallCert()` សាធារណៈ
  និង `installCertWindows/Mac/Linux` សម្រាប់វេទិកានីមួយៗ។
- `src/mitm/systemCommands.ts` — កម្មវិធីជំនួយ `execFile` / `spawn` / PowerShell រួម
  ដែលប្រើដោយផ្លូវដំឡើងទាំងនេះ។

**ការបង្កឱ្យដំណើរការ**៖ អ្នកប្រើចុច "បើកប្រូកស៊ី MITM" ក្នុងផ្ទាំងគ្រប់គ្រងមូលដ្ឋាននៅ
`/dashboard/cli-tools/mitm`។ ផ្លូវនេះអាចចូលប្រើបានតែតាម loopback ប៉ុណ្ណោះ — សូមមើលច្បាប់តឹងរ៉ឹងលេខ 17 ក្នុង
`CLAUDE.md` និង `src/server/authz/routeGuard.ts::isLocalOnlyPath()`។ JWT ដែលលេចធ្លាយ
តាមរយៈ tunnel **មិនអាច** បង្កឱ្យផ្លូវកូដនេះដំណើរការបានទេ។

**ប្រតិបត្តិការដែលទាមទារសិទ្ធិខ្ពស់ (តាមវេទិកានីមួយៗ)**៖

| ប្រព័ន្ធប្រតិបត្តិការ  | ពាក្យបញ្ជា                                                                                                        |
| ---------------------- | ----------------------------------------------------------------------------------------------------------------- |
| Windows                | `certutil -addstore Root <cert>` តាមរយៈ UAC                                                                       |
| macOS                  | `sudo security add-trusted-cert -d -r trustRoot -k /Library/Keychains/System.keychain <cert>`                     |
| Linux                  | `sudo cp <cert> <distro-trust-dir>` + `sudo update-ca-certificates` (Debian) / `sudo update-ca-trust` (RHEL/SUSE) |
| Linux+Firefox/Chromium | ការធ្វើបច្ចុប្បន្នភាព NSS DB តាម profile នីមួយៗ តាមរយៈ `certutil -d sql:<profile>`                                |

ទាំងនេះគឺជាពាក្យបញ្ជាដូចគ្នាដែលប្រើដោយ `mitmproxy`, Charles Proxy, Fiddler និង
Caddy។ ការដែលពាក្យបញ្ជាទាំងនេះមានក្នុង OmniRoute ត្រូវបានកត់ត្រានៅ
`docs/security/STEALTH_GUIDE.md`។

**វិធានការកាត់បន្ថយក្នុង v3.8.6**៖

- `runElevatedPowerShell()` លែងប្រើ `-EncodedCommand <base64utf16le>` ទៀតហើយ។
  payload ដែលត្រូវដំណើរការដោយសិទ្ធិខ្ពស់ ត្រូវបានសរសេរទៅក្នុងឯកសារ `.ps1` បណ្ដោះអាសន្នសម្រាប់ការហៅនីមួយៗ (mode 0o600,
  នៅក្នុងថតឯកជន `mkdtempSync`) ហើយត្រូវបានយោងតាមរយៈ `-File`។ ឯកសារ
  ត្រូវបានលុបតំណក្នុង `finally`។ ការនេះដកចេញនូវលក្ខណៈសម្គាល់បែបគំរូស្តង់ដារនៃ
  ការដំឡើងសិទ្ធិខ្ពស់តាមរយៈ PowerShell ដោយប្រើ base64 ដែលត្រូវបានសម្គាល់ដោយកម្មវិធីចាត់ថ្នាក់ AI របស់ Socket.dev។
- `installCertWindows` មានប្លុក `SECURITY-AUDITOR-NOTE:` ក្នុងបន្ទាត់កូដ
  ដែលចង្អុលមកទីនេះ។

**មូលហេតុដែលយើងរក្សាវាទុក**៖ ប្រូកស៊ី MITM គឺជាមុខងារដែលមានឯកសារពន្យល់ និងត្រូវបានប្រើដោយ
`docs/security/STEALTH_GUIDE.md` និង `docs/frameworks/MITM-PROXY.md`។ ការដកវាចេញ
នឹងធ្វើឱ្យសំណុំមុខងារ agent-bridge ខូច។

---

## §2 — ការនាំចូលព័ត៌មានសម្ងាត់ពី Zed (`app/api/providers/zed/import/route.js`)

**ឯកសារប្រភព**៖

- `src/app/api/providers/zed/discover/route.ts` _(ថ្មីក្នុង v3.8.6)_
- `src/app/api/providers/zed/import/route.ts`
- `src/lib/zed-oauth/keychain-reader.ts`
- `src/lib/zed-oauth/credentialFingerprint.ts` _(ថ្មីក្នុង v3.8.6)_

**ការកេះដំណើរការ**៖ អ្នកប្រើចុច "នាំចូលពី Zed" នៅលើទំព័រ Providers ក្នុងផ្ទាំងគ្រប់គ្រងមូលដ្ឋាន។ Endpoint នេះត្រូវបានការពារដោយ `requireManagementAuth`។ កម្មវិធីកែសម្រួល Zed ខ្លួនវាផ្ទាល់សរសេរ provider API keys របស់វាទៅកាន់ keychain របស់ OS ក្រោមឈ្មោះ service ដែលមានឯកសារបញ្ជាក់ — សូមមើល https://zed.dev/docs/ai/llm-providers។

**ឥរិយាបថរបស់ v3.8.5 (កំណែដែល Socket.dev បានដាក់ទង់សម្គាល់)**៖

`POST /import` បានស្វែងរកព័ត៌មានសម្ងាត់ និងរក្សាទុកពួកវាដោយស្វ័យប្រវត្តិទៅក្នុងឃ្លាំង SQLite មូលដ្ឋាន ដោយប្រើការផ្ញើ-ត្រឡប់តែមួយលើក។ មិនមានការបញ្ជាក់តាមគណនីនីមួយៗ មិនមាន fingerprint ទេ មានតែ "បានរកឃើញ token ចំនួន N ហើយបាននាំចូលទាំងអស់។"

**ការកាត់បន្ថយហានិភ័យក្នុង v3.8.6 — ការបញ្ជាក់ 2 ជំហាន**៖

1. **`POST /api/providers/zed/discover`** ត្រឡប់
   `{ candidates: [{ provider, service, account, fingerprint }] }`។ token ដើម
   **មិនដែល** ត្រូវបានបញ្ជូនឡើយ។ fingerprint គឺ
   `sha256(service|account|token).slice(0,16)`។
2. ផ្ទាំងគ្រប់គ្រងបង្ហាញបញ្ជី candidate ហើយប្រតិបត្តិករជ្រើសរើសអ្វីដែលត្រូវ
   នាំចូល រួចផ្ញើ `{ confirmedAccounts: [{ service, account, fingerprint }] }`
   ទៅកាន់ **`POST /api/providers/zed/import`**។
3. Endpoint សម្រាប់នាំចូល **អាន keychain ឡើងវិញនៅលើ server** និងត្រងតាម
   `(service, account, fingerprint)`។ response របស់ discover ដែលត្រូវបានកែបន្លំ
   ឬចាក់ឡើងវិញ មិនអាចបញ្ឆោត endpoint សម្រាប់នាំចូលឱ្យរក្សាទុក token ដែលមិនពាក់ព័ន្ធបានទេ —
   ប្រសិនបើ token បច្ចុប្បន្នបានផ្លាស់ប្តូរចាប់តាំងពីការស្វែងរក នោះ fingerprint នឹងលែង
   ត្រូវគ្នា ហើយព័ត៌មានសម្ងាត់នោះនឹងត្រូវបានរំលង។

env flag `OMNIROUTE_ZED_IMPORT_LEGACY_ONE_STEP=true` រក្សាឥរិយាបថរបស់ v3.8.5
សម្រាប់ប្រតិបត្តិករដែលមិនទាន់បានធ្វើបច្ចុប្បន្នភាពស្វ័យប្រវត្តិកម្មរបស់ពួកគេ។ វានឹងត្រូវបាន
ដកចេញក្នុង v3.9។

**មូលហេតុដែលយើងរក្សាវាទុក**៖ ការនាំចូលពី Zed គឺជាវិធីចាប់ផ្ដើមប្រើប្រាស់ដែលងាយស្រួលបំផុតសម្រាប់អ្នកប្រើ
ដែលប្រើ Zed រួចហើយ និងចង់ចម្លង provider keys របស់ពួកគេចូលទៅក្នុង OmniRoute
ដោយមិនចាំបាច់បិទភ្ជាប់ឡើងវិញ។

---

## §3 — `execFile` / `spawn` / PowerShell ដែលមានសិទ្ធិកម្រិតខ្ពស់ (`21843.js`)

**ឯកសារប្រភព**៖ `src/mitm/systemCommands.ts`។

**មូលហេតុដែលត្រូវបានដាក់ទង់សម្គាល់**៖ chunk នេះនាំចេញឡើងវិញនូវ `execFileWithPassword`,
`runElevatedPowerShell` និង helper រួម `quotePowerShell`។ AI classifier របស់ Socket.dev
ចាត់ទុកពួកវាជា "ឧបករណ៍ទូទៅសម្រាប់ការប្រតិបត្តិលើ host + ការដំឡើងកម្រិតសិទ្ធិ"។
នៅក្នុង OmniRoute ពួកវាត្រូវបានប្រើតែដោយដំណើរការដំឡើងវិញ្ញាបនបត្រ MITM
(§1) និងដោយ `execFileWithPassword` សម្រាប់ការប្រតិបត្តិ command របស់ `sudo` ប៉ុណ្ណោះ។

**ការកាត់បន្ថយហានិភ័យក្នុង v3.8.6**៖

- ការកែសម្រួលរចនាសម្ព័ន្ធ `runElevatedPowerShell` (សូមមើល §1)។
- block `SECURITY-AUDITOR-NOTE:` ក្នុងបន្ទាត់នៅទាំង
  `runElevatedPowerShell` និង `execFileWithPassword` កត់ត្រាអំពី caller ដែលមានក្នុង allowlist
  និងបញ្ជី executable ដែលបានកំណត់ជាក់លាក់។
- ការ call `spawn()` របស់ `execFileWithPassword` មាន marker `nosemgrep` ជាមួយ
  allowlist នៃ executable ដែល helper ត្រូវបានអនុញ្ញាតឱ្យទទួល — **មិនមានផ្លូវពី input របស់អ្នកប្រើទៅកាន់ `finalCommand`/`finalArgs` ទេ**។

---

## §4 / §6 — កម្មវិធីត្រួតពិនិត្យ service របស់ 9router (`api/services/9router/{start,restart}/route.js`)

**ឯកសារប្រភព**៖

- `src/app/api/services/9router/_lib.ts` — factory របស់កម្មវិធីត្រួតពិនិត្យ។
- `src/app/api/services/9router/{start,stop,restart,status,install,update,auto-start}/route.ts`។
- `src/lib/services/ServiceSupervisor.ts` — spawn / health-poll / log-buffer ទូទៅ។

**ការកេះដំណើរការ**៖ អ្នកប្រើចុច "ដំឡើង" / "ចាប់ផ្ដើម" នៅលើទំព័រ service ដែលបានបង្កប់ក្នុង
ផ្ទាំងគ្រប់គ្រងមូលដ្ឋាន។

**ការការពារដែលមានរួចហើយ**៖

- route `/api/services/*` ទាំងអស់គឺ LOCAL_ONLY ស្របតាម
  `src/server/authz/routeGuard.ts` (ច្បាប់តឹងរ៉ឹង #17)។ ការអនុវត្ត loopback
  កើតឡើងមុនការត្រួតពិនិត្យការផ្ទៀងផ្ទាត់ណាមួយ — JWT ដែលលេចធ្លាយមិនអាចចូលដល់ពួកវាបានទេ។
- row របស់ 9router ក្នុង DB ត្រូវបានបង្កើតជាមុនជា `status='not_installed', auto_start=0` (សូមមើល
  `src/lib/db/migrations/071_services.sql:19`)។ service នេះ **មិន** ចាប់ផ្ដើម
  នៅពេលបើកដំណើរការលើកដំបូងទេ។
- `spawn()` ត្រូវបានហៅដោយប្រើ binary path ដែលត្រឡប់ដោយ
  `resolveSpawnArgs(apiKey, PORT)` ក្នុង `src/lib/services/installers/ninerouter.ts`
  ដែលជាបញ្ជីថេរនៃ binary ដែលគាំទ្រ។
- Stdout/stderr ត្រូវបានផ្ទុកក្នុង buffer នៅក្នុង memory (កំណត់ត្រឹម 5 MB សូមមើល `_lib.ts`) — មិនមានការសរសេរ
  ទៅ disk ទេ លុះត្រាតែអ្នកប្រើបើក logging ពីផ្ទាំងគ្រប់គ្រង។

**ការកាត់បន្ថយហានិភ័យក្នុង v3.8.6**៖ មិនមានការផ្លាស់ប្តូរមុខងារទេ។ build profile អប្បបរមា
(`OMNIROUTE_BUILD_PROFILE=minimal`) ជំនួស
`src/lib/services/installers/ninerouter.ts` ដោយ stub សម្រាប់អ្នកប្រើដែលចង់ឱ្យ
ផ្លូវដែលមានសិទ្ធិខ្ពស់ត្រូវបានដកចេញពី bundle ទាំងស្រុង។

**មូលហេតុដែលយើងរក្សាវាទុក**៖ 9router គឺជា service ដៃគូដែលអាចដំឡើងនៅមូលដ្ឋានជាជម្រើស
(ស្រដៀងនឹង៖ plugin បែប WordPress) — ត្រូវតែជ្រើសរើសបើកប្រើយ៉ាងជាក់លាក់។

---

## §5 — ការសរសេរព័ត៌មានសម្ងាត់ត្រឡប់ទៅ OmniRoute Cloud Sync (`api/keys/[id]/route.js`)

**ឯកសារប្រភព**:

- `src/lib/cloudSync.ts` — `syncToCloud()` / `updateLocalTokens()`។
- `src/app/api/keys/[id]/route.ts` — ហៅ `syncKeysToCloudIfEnabled()`។

**លក្ខខណ្ឌបង្ក**: `isCloudEnabled()` ត្រឡប់ `true` (កំណត់ពីផ្ទាំងគ្រប់គ្រង) **និង**
`CLOUD_URL` ត្រូវបានកំណត់រចនាសម្ព័ន្ធ។ នៅពេលទាំងពីរត្រូវបានបិទ វាមិនធ្វើការហៅបណ្ដាញចេញទៅកាន់
Cloud endpoint ទេ។

**ឥរិយាបថ v3.8.5 (កំហុសដែល Socket.dev បានរកឃើញតាមវិធីត្រឹមត្រូវ)**:

`updateLocalTokens()` បានសរសេរជាន់លើ `accessToken`, `refreshToken` និង
`providerSpecificData` ពីការឆ្លើយតបរបស់ Cloud នៅពេល
`cloudUpdatedAt > localUpdatedAt`។ គ្មាន HMAC គ្មានហត្ថលេខា និងគ្មាន checksum។ `CLOUD_URL`
ដែលបានកំណត់រចនាសម្ព័ន្ធខុស ឬមានចេតនាព្យាបាទ (ឬ MITM នៅលើឆានែល) អាចប្ដូរ
OAuth token របស់ provider ដោយស្ងៀមស្ងាត់។

**ការកាត់បន្ថយហានិភ័យក្នុង v3.8.6**:

1. **ការផ្ទៀងផ្ទាត់ HMAC**: `verifyCloudSignature(rawBody, sigHeader)` ផ្ទៀងផ្ទាត់
   header `X-Cloud-Sig` (`HMAC-SHA256(OMNIROUTE_CLOUD_SYNC_SECRET,
rawBody)`) មុនពេល parse JSON។ ប្រសិនបើ secret ត្រូវបានកំណត់ ហត្ថលេខាគឺជា
   តម្រូវការចាំបាច់។ បើមិនបានកំណត់ទេ (របៀប legacy) ការព្រមានមួយនឹងត្រូវបានកត់ត្រា ហើយការឆ្លើយតបនឹងត្រូវបាន
   ទទួលយក — secret នឹងក្លាយជាតម្រូវការចាំបាច់ក្នុង v3.9។
2. **ការជ្រើសរើសចូលសម្រាប់វាលសម្ងាត់**: `accessToken` / `refreshToken` /
   `providerSpecificData` ត្រូវបានសរសេរជាន់លើ **តែនៅពេល**
   `OMNIROUTE_CLOUD_SYNC_SECRETS=true` ប៉ុណ្ណោះ។ របៀបលំនាំដើមធ្វើសមកាលកម្មតែ
   metadata ដែលមិនមែនជាព័ត៌មានសម្ងាត់ (`expiresAt`, `status`, `lastError*`,
   `rateLimitedUntil`, `updatedAt`)។ នេះគឺជា **ការផ្លាស់ប្ដូរដែលមិនឆបគ្នាជាមួយកំណែមុន** សម្រាប់អ្នកប្រើ
   ដែលបានពឹងផ្អែកលើការធ្វើសមកាលកម្ម token ពីចម្ងាយ — ពួកគេត្រូវតែជ្រើសរើសចូលដោយជាក់លាក់។

**ហេតុអ្វីយើងរក្សាវា**: Cloud Sync គឺជាមធ្យោបាយតែមួយគត់សម្រាប់ OmniRoute Cloud tenant
ក្នុងការប្រមូលផ្ដុំព័ត៌មានសម្ងាត់របស់ក្រុមនៅកន្លែងតែមួយ។ ការកែតម្រូវនេះធ្វើឱ្យគំរូគំរាមកំហែងមានភាពត្រឹមត្រូវ៖
"server ចុះហត្ថលេខា, client ផ្ទៀងផ្ទាត់, operator ជ្រើសរើសចូល។"

---

## ទម្រង់ build: `minimal`

សម្រាប់អ្នកប្រើដែលត្រូវការ artifact ដែលសមស្របជាមួយ Socket សូម build ដោយប្រើ៖

```bash
OMNIROUTE_BUILD_PROFILE=minimal npm run build
```

webpack `NormalModuleReplacementPlugin` កំណត់ alias ម៉ូឌុលចំនួនបួនទៅជា stub៖

| ម៉ូឌុល                                      | Stub                                             |
| ------------------------------------------- | ------------------------------------------------ |
| `src/mitm/cert/install.ts`                  | `src/mitm/cert/install.stub.ts`                  |
| `src/lib/zed-oauth/keychain-reader.ts`      | `src/lib/zed-oauth/keychain-reader.stub.ts`      |
| `src/lib/cloudSync.ts`                      | `src/lib/cloudSync.stub.ts`                      |
| `src/lib/services/installers/ninerouter.ts` | `src/lib/services/installers/ninerouter.stub.ts` |

Stub នីមួយៗ export ផ្ទៃ API ដូចគ្នា ប៉ុន្តែគ្រប់ function ទាំងអស់បោះចេញ
`featureDisabledError(name)` នៅពេល runtime។ Route ដែលពឹងផ្អែកលើម៉ូឌុលដែលបានបិទ
ត្រឡប់ HTTP 503 ជាមួយសារច្បាស់លាស់ ជំនួសឱ្យការធ្វើឱ្យ
ផ្លូវកូដរសើបសកម្ម។

Bundle ដែលបានបង្កើតឡើងនេះមានបំណងត្រូវបានចេញផ្សាយជា `omniroute-secure`។ សូមមើល
`docs/ops/PUBLISHING_SECURE.md` សម្រាប់វិធីសាស្ត្រចេញផ្សាយ។

---

## ការបំបែកកម្មវិធីជំនួយ (តាមដានសម្រាប់ v4)

ក្នុងរយៈពេលវែង យើងមានបំណងបំបែកកញ្ចប់ npm ទៅជាម៉ូឌុលដែលអាចធ្វើសវនកម្មដោយឡែកពីគ្នា។
សូមមើល milestone v4 នៅក្នុងកម្មវិធីតាមដាន issue របស់ GitHub សម្រាប់ issue ដែលប្រើដើម្បីតាមដាន។
