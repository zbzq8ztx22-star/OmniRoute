# Embedded Services (ខ្មែរ)

🌐 **Languages:** 🇺🇸 [English](../../../../frameworks/EMBEDDED-SERVICES.md) · 🇪🇹 [am](../../../am/docs/frameworks/EMBEDDED-SERVICES.md) · 🇸🇦 [ar](../../../ar/docs/frameworks/EMBEDDED-SERVICES.md) · 🇦🇿 [az](../../../az/docs/frameworks/EMBEDDED-SERVICES.md) · 🇧🇬 [bg](../../../bg/docs/frameworks/EMBEDDED-SERVICES.md) · 🇧🇩 [bn](../../../bn/docs/frameworks/EMBEDDED-SERVICES.md) · 🇨🇿 [cs](../../../cs/docs/frameworks/EMBEDDED-SERVICES.md) · 🇩🇰 [da](../../../da/docs/frameworks/EMBEDDED-SERVICES.md) · 🇩🇪 [de](../../../de/docs/frameworks/EMBEDDED-SERVICES.md) · 🇬🇷 [el](../../../el/docs/frameworks/EMBEDDED-SERVICES.md) · 🇪🇸 [es](../../../es/docs/frameworks/EMBEDDED-SERVICES.md) · 🇪🇪 [et](../../../et/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇷 [fa](../../../fa/docs/frameworks/EMBEDDED-SERVICES.md) · 🇫🇮 [fi](../../../fi/docs/frameworks/EMBEDDED-SERVICES.md) · 🇫🇷 [fr](../../../fr/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇪 [ga](../../../ga/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇳 [gu](../../../gu/docs/frameworks/EMBEDDED-SERVICES.md) · 🇳🇬 [ha](../../../ha/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇱 [he](../../../he/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇳 [hi](../../../hi/docs/frameworks/EMBEDDED-SERVICES.md) · 🇭🇷 [hr](../../../hr/docs/frameworks/EMBEDDED-SERVICES.md) · 🇭🇺 [hu](../../../hu/docs/frameworks/EMBEDDED-SERVICES.md) · 🇦🇲 [hy](../../../hy/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇩 [id](../../../id/docs/frameworks/EMBEDDED-SERVICES.md) · 🇳🇬 [ig](../../../ig/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇹 [it](../../../it/docs/frameworks/EMBEDDED-SERVICES.md) · 🇯🇵 [ja](../../../ja/docs/frameworks/EMBEDDED-SERVICES.md) · 🇬🇪 [ka](../../../ka/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇳 [kn](../../../kn/docs/frameworks/EMBEDDED-SERVICES.md) · 🇰🇷 [ko](../../../ko/docs/frameworks/EMBEDDED-SERVICES.md) · 🇱🇹 [lt](../../../lt/docs/frameworks/EMBEDDED-SERVICES.md) · 🇱🇻 [lv](../../../lv/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇳 [ml](../../../ml/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇳 [mr](../../../mr/docs/frameworks/EMBEDDED-SERVICES.md) · 🇲🇾 [ms](../../../ms/docs/frameworks/EMBEDDED-SERVICES.md) · 🇲🇹 [mt](../../../mt/docs/frameworks/EMBEDDED-SERVICES.md) · 🇲🇲 [my](../../../my/docs/frameworks/EMBEDDED-SERVICES.md) · 🇳🇵 [ne](../../../ne/docs/frameworks/EMBEDDED-SERVICES.md) · 🇳🇱 [nl](../../../nl/docs/frameworks/EMBEDDED-SERVICES.md) · 🇳🇴 [no](../../../no/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇳 [or](../../../or/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇳 [pa](../../../pa/docs/frameworks/EMBEDDED-SERVICES.md) · 🇵🇭 [phi](../../../phi/docs/frameworks/EMBEDDED-SERVICES.md) · 🇵🇱 [pl](../../../pl/docs/frameworks/EMBEDDED-SERVICES.md) · 🇵🇹 [pt](../../../pt/docs/frameworks/EMBEDDED-SERVICES.md) · 🇧🇷 [pt-BR](../../../pt-BR/docs/frameworks/EMBEDDED-SERVICES.md) · 🇷🇴 [ro](../../../ro/docs/frameworks/EMBEDDED-SERVICES.md) · 🇷🇺 [ru](../../../ru/docs/frameworks/EMBEDDED-SERVICES.md) · 🇱🇰 [si](../../../si/docs/frameworks/EMBEDDED-SERVICES.md) · 🇸🇰 [sk](../../../sk/docs/frameworks/EMBEDDED-SERVICES.md) · 🇸🇮 [sl](../../../sl/docs/frameworks/EMBEDDED-SERVICES.md) · 🇷🇸 [sr](../../../sr/docs/frameworks/EMBEDDED-SERVICES.md) · 🇸🇪 [sv](../../../sv/docs/frameworks/EMBEDDED-SERVICES.md) · 🇰🇪 [sw](../../../sw/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇳 [ta](../../../ta/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇳 [te](../../../te/docs/frameworks/EMBEDDED-SERVICES.md) · 🇹🇭 [th](../../../th/docs/frameworks/EMBEDDED-SERVICES.md) · 🇹🇷 [tr](../../../tr/docs/frameworks/EMBEDDED-SERVICES.md) · 🇺🇦 [uk-UA](../../../uk-UA/docs/frameworks/EMBEDDED-SERVICES.md) · 🇵🇰 [ur](../../../ur/docs/frameworks/EMBEDDED-SERVICES.md) · 🇺🇿 [uz](../../../uz/docs/frameworks/EMBEDDED-SERVICES.md) · 🇻🇳 [vi](../../../vi/docs/frameworks/EMBEDDED-SERVICES.md) · 🇳🇬 [yo](../../../yo/docs/frameworks/EMBEDDED-SERVICES.md) · 🇨🇳 [zh-CN](../../../zh-CN/docs/frameworks/EMBEDDED-SERVICES.md) · 🇹🇼 [zh-TW](../../../zh-TW/docs/frameworks/EMBEDDED-SERVICES.md)

---

> **កំណែ:** v3.8.44
> **បានធ្វើបច្ចុប្បន្នភាពចុងក្រោយ:** 2026-09-09
> **អ្នកអានគោលដៅ:** វិស្វករដែលបន្ថែម ថែទាំ ឬបំបាត់កំហុសសេវាកម្មដែលបានបង្កប់ (9Router, CLIProxyAPI, Mux, Bifrost, open-wa)។

សេវាកម្មដែលបានបង្កប់ គឺជាឧបករណ៍ដំណើរការជំនួយដែលបានដំឡើងក្នុងម៉ាស៊ីន ដែល OmniRoute ដំឡើង ត្រួតពិនិត្យ និង
ផ្តល់ឱ្យជាគោលដៅកំណត់ផ្លូវកម្រិតទីមួយ។ ខុសពីអ្នកផ្តល់សេវាខាងក្រៅ (ដែលត្រូវបានចូលប្រើតាមអ៊ីនធឺណិត
តាមរយៈ API keys) សេវាកម្មដែលបានបង្កប់ដំណើរការលើម៉ាស៊ីនដូចគ្នានឹង OmniRoute ហើយទំនាក់ទំនងតាមរយៈ loopback។

---

## តារាងមាតិកា

1. [ទិដ្ឋភាពទូទៅ](#1-overview)
2. [ស្ថាបត្យកម្ម — 4 ស្រទាប់](#2-architecture--4-layers)
3. [ម៉ាស៊ីនស្ថានភាពវដ្តជីវិត](#3-lifecycle-state-machine)
4. [ឯកសារយោង API](#4-api-reference)
5. [សុវត្ថិភាព](#5-security)
6. [ការបន្ថែមសេវាកម្មដែលបានបង្កប់ថ្មី](#6-adding-a-new-embedded-service)
7. [ការដោះស្រាយបញ្ហា](#7-troubleshooting)
8. [សំណួរដែលសួរញឹកញាប់](#8-faq)

---

## 1. ទិដ្ឋភាពទូទៅ

### ហេតុអ្វីត្រូវមានសេវាកម្មបង្កប់?

សេវាកម្មចំនួនប្រាំមួយត្រូវបានបង្កប់៖

| សេវាកម្ម        | កញ្ចប់ npm                                      | ច្រកលំនាំដើម | គោលបំណង                                                                                                                                                                                                 |
| --------------- | ----------------------------------------------- | :----------: | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **9Router**     | `9router`                                       |    20130     | រ៉ោតទ័រ AI ដែល OmniRoute អាចប្រើជាអ្នកផ្តល់រង។ ម៉ូដែលត្រូវបានបង្ហាញជា `9router/{sub}/{model}`                                                                                                           |
| **CLIProxyAPI** | ឯកសារប្រតិបត្តិពី GitHub release (`cliproxy`)   |     8317     | អាដាប់ទ័រប្រូកស៊ីមូលដ្ឋានសម្រាប់លំហូរផ្ទៀងផ្ទាត់អត្តសញ្ញាណរបស់ Anthropic CLI។ ផ្តល់ការកំណត់ផ្លូវបម្រុងនៅពេលថូខឹន OAuth ផុតកំណត់                                                                         |
| **Mux**         | `mux` (`mux server` គ្មានចំណុចប្រទាក់ក្រាហ្វិក) |     8322     | daemon មូលដ្ឋានសម្រាប់រៀបចំសម្របសម្រួលភ្នាក់ងារ (coder/mux)។ គ្រប់គ្រងតែវដ្តជីវិតប៉ុណ្ណោះ — មិនមែនជាគោលដៅកំណត់ផ្លូវទេ (គ្មានការធ្វើប្រូកស៊ី LLM)។                                                       |
| **Bifrost**     | `@maximhq/bifrost`                              |     8080     | កម្មវិធីខាងក្រោយសម្រាប់បញ្ជូនបន្តតាមច្រកទ្វារ AI ដែលសរសេរដោយ Go។ នៅពេលដំណើរការ វាត្រូវបានជ្រើសរើសដោយស្វ័យប្រវត្តិដោយផ្លូវបញ្ជូនបន្ត (`/v1/relay/`)                                                      |
| **Dario**       | `@askalf/dario`                                 |     3456     | ប្រូកស៊ីការជាវ Claude — ជម្រើស/បម្រុងសម្រាប់ CLIProxyAPI សម្រាប់ចរាចរណ៍ដែលមានទម្រង់ដូច Claude Code; សោដែលបានបញ្ចូលក្លាយជា `DARIO_ADMIN_TOKEN` ដែលគ្រប់គ្រងការចូលប្រើផ្ទាំងបញ្ជា OAuth `/admin/*` របស់វា |
| **open-wa**     | `@open-wa/wa-automate`                          |     8323     | ស្វ័យប្រវត្តិកម្ម WhatsApp Web (Chromium គ្មានចំណុចប្រទាក់ក្រាហ្វិកតាមរយៈ Puppeteer)។ គ្រប់គ្រងតែវដ្តជីវិតប៉ុណ្ណោះ — មិនមែនជាគោលដៅកំណត់ផ្លូវទេ។                                                         |

សេវាកម្មទាំងប្រាំមួយអនុវត្តតាមគំរូត្រួតពិនិត្យដូចគ្នា៖

- OmniRoute ដំឡើងពួកវាក្រោម `DATA_DIR/services/{name}/` (ដាច់ដោយឡែកពី `package.json` របស់ OmniRoute)
- OmniRoute បើកដំណើរការ និងត្រួតពិនិត្យពួកវាជាដំណើរការរង
- OmniRoute បញ្ចូលសោ API បណ្ដោះអាសន្នទៅក្នុងបរិស្ថានរបស់ដំណើរការរង និងប្ដូរសោដោយគ្មានការផ្អាកដំណើរការ (នៅកន្លែងដែលអាចអនុវត្តបាន)
- ផ្លូវគ្រប់គ្រងទាំងអស់ (`/api/services/*`) គឺ **LOCAL_ONLY** — អាចចូលប្រើបានតែពី loopback ប៉ុណ្ណោះ (ច្បាប់តឹងរ៉ឹង #17)

### ការសម្រេចចិត្តសំខាន់ៗ (ពីផែនការរចនា)

| ការសម្រេចចិត្ត                                   | តម្លៃ                                                                      |
| ------------------------------------------------ | -------------------------------------------------------------------------- |
| ការចូលប្រើ UI ដើមរបស់ 9Router តាមផ្ទាំងគ្រប់គ្រង | ប្រូកស៊ីបញ្ច្រាសនៅ `/dashboard/providers/services/9router/embed/*`         |
| យន្តការដំឡើង                                     | `npm install {package}` តាមរយៈ `execFile` (គ្មានការជំនួសតម្លៃដោយ shell)    |
| របៀបប្រើប្រាស់                                   | អ្នកផ្តល់ត្រូវបានចុះឈ្មោះជា `9router/{sub}/{model}` ក្នុងម៉ាស៊ីនកំណត់ផ្លូវ |
| ការគ្រប់គ្រងសោ API                               | OmniRoute បង្កើត អ៊ិនគ្រីបនៅពេលរក្សាទុក (AES-256-GCM) និងបញ្ចូលតាមរយៈ env  |
| ទីតាំងផ្ទាំងគ្រប់គ្រង                            | `/dashboard/providers/services` (ផ្ទាំងបី)                                 |
| ការចាប់ផ្តើមស្វ័យប្រវត្តិ                        | បិទបើកសម្រាប់សេវាកម្មនីមួយៗ ដោយលំនាំដើមគឺបិទ                               |

---

## 2. ស្ថាបត្យកម្ម — 4 ស្រទាប់

```
┌────────────────────────────────────────────────────────────────────┐
│  ស្រទាប់ 1 — UI                                                    │
│  /dashboard/providers/services  (ផ្ទាំង៖ CLIProxyAPI | 9Router | Mux)│
│  កំណត់ហេតុផ្ទាល់ (SSE), ចាប់ផ្ដើម/បញ្ឈប់/ចាប់ផ្ដើមឡើងវិញ/ធ្វើបច្ចុប្បន្នភាព, ការកំណត់, ដំឡើង│
│                                                                    │
│  src/app/(dashboard)/dashboard/providers/services/                 │
│    ├── page.tsx               ស៊ុមកម្មវិធី + ការបញ្ជូនផ្លូវផ្ទាំងតាម ?tab=│
│    ├── tabs/                  CliproxyServiceTab, NinerouterServiceTab,│
│    │                          MuxServiceTab                        │
│    └── components/            ServiceStatusCard, ServiceLifecycleButtons,│
│                               ServiceLogsPanel, ApiKeyCard, ...    │
└──────────────────────┬─────────────────────────────────────────────┘
                       │ HTTP (Next.js fetch)
┌──────────────────────▼─────────────────────────────────────────────┐
│  ស្រទាប់ 2 — API (LOCAL_ONLY — សម្រាប់តែ loopback)                 │
│                                                                    │
│  /api/services/9router/{install|start|stop|restart|update|         │
│                          rotate-key|status|auto-start|logs}        │
│  /api/services/cliproxy/{install|start|stop|restart|update|        │
│                           status|auto-start|logs}                  │
│  /api/services/mux/{install|start|stop|restart|update|             │
│                      status|auto-start|logs}                       │
│  /dashboard/providers/services/9router/embed/[...path]             │
│    (ប្រូកស៊ីបញ្ច្រាស HTTP + WebSocket → upstream របស់ 9Router)      │
│                                                                    │
│  ច្រកត្រួតពិនិត្យ៖ LOCAL_ONLY_API_PREFIXES រួមបញ្ចូល "/api/services/" និង│
│        "/dashboard/providers/services/*/embed/"                    │
└──────────────────────┬─────────────────────────────────────────────┘
                       │ ការហៅក្នុងដំណើរការតែមួយ
┌──────────────────────▼─────────────────────────────────────────────┐
│  ស្រទាប់ 3 — ServiceSupervisor (src/lib/services/)                 │
│                                                                    │
│  ServiceSupervisor.ts   កម្មវិធីត្រួតពិនិត្យទូទៅ (child_process.spawn)│
│    ├── install:    execFile('npm', ['install', pkg, '--prefix'])    │
│    ├── start:      spawn(node, [entrypoint], {env, cwd})           │
│    ├── api_key:    crypto.randomBytes(32) → env NINEROUTER_API_KEY  │
│    ├── port:       20130 សម្រាប់ 9Router (អាចកំណត់បាន)             │
│    ├── logs:       អង្គចងចាំបណ្ដុំកំណត់ហេតុ stdio 5 MB → ព្រឹត្តិការណ៍ SSE│
│    ├── health:     HTTP GET /health រៀងរាល់ 2–5 វិនាទី, ស្ដារឡើងវិញពេលចាំបាច់│
│    └── lifecycle:  SIGTERM 15 វិនាទី → SIGKILL                     │
│                                                                    │
│  registry.ts        getSupervisor(name) / registerSupervisor()     │
│  bootstrap.ts       ចាប់ផ្ដើមដំបូង SERVICES[] ទាំងអស់នៅពេលដំណើរការចាប់ផ្ដើម│
│  apiKey.ts          getOrCreateApiKey(), generateServiceApiKey()   │
│  modelSync.ts       GET /v1/models ជាប្រចាំ → តារាង service_models│
│  ringBuffer.ts      អង្គចងចាំបណ្ដុំកំណត់ហេតុវិលជុំ (5 MB ក្នុងមួយសេវា)│
│  healthCheck.ts     ការស្ទង់ពិនិត្យសុខភាព HTTP                     │
│  installers/        ninerouter.ts, cliproxy.ts, mux.ts, openwa.ts  │
│                      (អាដាប់ទ័រកម្មវិធីដំឡើង)                     │
└──────────────────────┬─────────────────────────────────────────────┘
                       │ HTTP ដែលឆបគ្នាជាមួយ OpenAI (loopback)
┌──────────────────────▼─────────────────────────────────────────────┐
│  ស្រទាប់ 4 — អ្នកផ្ដល់សេវា / ការបញ្ជូនផ្លូវ                     │
│                                                                    │
│  open-sse/executors/ninerouter.ts                                  │
│    ស្វែងរក port និង API key ឡើងវិញតាមសំណើនីមួយៗ (គ្មាន caching)។ │
│    ដកបុព្វបទ "9router/" ចេញពី model id មុនពេលបញ្ជូនតាមប្រូកស៊ី។  │
│    ត្រឡប់ 503 service_not_running ប្រសិនបើកម្មវិធីត្រួតពិនិត្យមិនស្ថិតក្នុង "running"។│
│                                                                    │
│  src/shared/constants/providers.ts                                 │
│    ធាតុសម្រាប់ "9router"៖ isEmbeddedService: true                  │
│                                                                    │
│  open-sse/config/providerRegistry.ts                               │
│    ម៉ូដែលត្រូវបានរក្សាទុកជា "9router/{sub}/{model}" (មានបុព្វបទ)។│
│    ធ្វើសមកាលកម្មរៀងរាល់ 5 នាទីដោយ modelSync.ts។                   │
│                                                                    │
│  Mux ត្រូវបានគ្រប់គ្រងតែវដ្ដជីវិតប៉ុណ្ណោះ (ស្រទាប់ 1-3) — វាជា   │
│  daemon សម្របសម្រួល agent មិនមែនជាប្រូកស៊ី LLM ទេ ដូច្នេះវាគ្មាន│
│  ធាតុ executor/provider ក្នុងស្រទាប់ 4 ហើយមិនដែលជាគោលដៅបញ្ជូនផ្លូវទេ។│
└────────────────────────────────────────────────────────────────────┘
```

### ឯកសារប្រភពសំខាន់ៗ

| ឯកសារ                                       | តួនាទី                                                        |
| ------------------------------------------- | ------------------------------------------------------------- |
| `src/lib/services/ServiceSupervisor.ts`     | ថ្នាក់ស្នូល៖ វដ្តជីវិត, សោ, សុខភាព, ring buffer               |
| `src/lib/services/bootstrap.ts`             | ការចុះឈ្មោះកម្រិតដំណើរការ និងការចាប់ផ្ដើមដោយស្វ័យប្រវត្តិ     |
| `src/lib/services/registry.ts`              | ផែនទី Singleton `tool → supervisor`                           |
| `src/lib/services/apiKey.ts`                | ការបង្កើតសោ និងការអ៊ិនគ្រីប AES-256-GCM ពេលរក្សាទុក           |
| `src/lib/services/modelSync.ts`             | ការធ្វើសមកាលកម្មម៉ូដែលតាមកាលកំណត់ (5 នាទី) + តាមតម្រូវការ     |
| `src/lib/services/ringBuffer.ts`            | អង្គចងចាំបណ្ដោះអាសន្នកំណត់ហេតុវិលជុំទំហំ 5 MB ជាមួយការជាវ SSE |
| `src/lib/services/healthCheck.ts`           | ការត្រួតពិនិត្យសុខភាពតាម HTTP (ចន្លោះពេលអាចកំណត់បាន)          |
| `src/lib/services/installers/ninerouter.ts` | npm ដំឡើង/ធ្វើបច្ចុប្បន្នភាព/លុបការដំឡើង សម្រាប់ 9Router      |
| `src/lib/services/installers/cliproxy.ts`   | npm ដំឡើង/ធ្វើបច្ចុប្បន្នភាព/លុបការដំឡើង សម្រាប់ CLIProxyAPI  |
| `src/lib/services/installers/mux.ts`        | npm ដំឡើង/ធ្វើបច្ចុប្បន្នភាព/លុបការដំឡើង សម្រាប់ Mux          |
| `src/lib/services/installers/openwa.ts`     | npm ដំឡើង/ធ្វើបច្ចុប្បន្នភាព/លុបការដំឡើង សម្រាប់ open-wa      |
| `src/app/api/services/9router/_lib.ts`      | អនុគមន៍ជំនួយ `getOrInitSupervisor()`                          |
| `src/app/api/services/[name]/logs/route.ts` | ចំណុចចុងក្រោយកំណត់ហេតុ SSE ដែលប្រើរួមគ្នា                     |
| `open-sse/executors/ninerouter.ts`          | កម្មវិធីប្រតិបត្តិរបស់អ្នកផ្ដល់សេវា (ស្រទាប់ទី 4)             |

---

## 3. ម៉ាស៊ីនស្ថានភាពវដ្តជីវិត

```
                    install()
  ┌─────────────┐ ──────────► ┌─────────────┐
  │ not_installed│             │   stopped   │◄──────────────────┐
  └─────────────┘             └──────┬──────┘                   │
                                     │ start()                   │
                                     ▼                           │ stop()
                               ┌──────────┐                      │
                               │ starting │                      │
                               └────┬─────┘                     │
             ការស្ទង់សុខភាពជោគជ័យ │         គាំង / SIGTERM      │
                               ┌────▼─────┐  (ចាកចេញក្នុង 5s)   │
                               │ running  │──── គាំង ───────────►┤
                               └────┬─────┘                   ┌─▼────┐
                             stop() │                          │error │
                                    ▼                          └──────┘
                               ┌──────────┐
                               │ stopping │
                               └──────────┘
```

ស្ថានភាពត្រូវបានរក្សាទុកក្នុងតារាង DB `version_manager` (ជួរឈរ `status`) និងត្រូវបានឆ្លុះបញ្ចាំង
ក្នុងស្ថានភាពនៅក្នុងអង្គចងចាំរបស់ `ServiceSupervisor`។ ស្ថានភាពនៅក្នុងអង្គចងចាំគឺជាប្រភពដែលមានសិទ្ធិសម្រេចសម្រាប់
ដំណើរការដែលកំពុងរត់ ចំណែកស្ថានភាព DB គឺជាជម្រើសបម្រុងដែលរក្សាទុកបានយូរនៅពេលចាប់ផ្ដើមប្រព័ន្ធ។

### ការផ្លាស់ប្ដូរស្ថានភាព

| ពី              | ព្រឹត្តិការណ៍                        | ទៅ                     |
| --------------- | ------------------------------------ | ---------------------- |
| `not_installed` | `install()` ជោគជ័យ                   | `stopped`              |
| `stopped`       | បានហៅ `start()`                      | `starting`             |
| `starting`      | ការស្ទង់សុខភាពត្រឡប់ 200             | `running`              |
| `starting`      | ដំណើរការចាកចេញមុនពេលមានសុខភាពល្អ     | `error`                |
| `running`       | បានហៅ `stop()`                       | `stopping` → `stopped` |
| `running`       | ដំណើរការចាកចេញដោយមិនបានរំពឹង (< 5 s) | `error` (គាំងរហ័ស)     |
| `running`       | ដំណើរការចាកចេញដោយមិនបានរំពឹង (> 5 s) | `error`                |
| `error`         | បានហៅ `start()`                      | `starting`             |
| ណាមួយ           | `stop()` ខណៈពេល `stopping`           | មិនធ្វើអ្វី            |

### សោប្រតិបត្តិការ

`ServiceSupervisor` រៀបចំប្រតិបត្តិការវដ្តជីវិតឱ្យដំណើរការតាមលំដាប់តាមរយៈសោប្រតិបត្តិការអសមកាល
(`withLock()`)។ ការហៅ `start()` ក្នុងពេលដំណាលគ្នាលើ supervisor ដូចគ្នា នឹងបង្កើតដំណើរការថ្មីតែមួយគត់;
អ្នកហៅទីពីរនឹងរង់ចាំ ហើយត្រឡប់ស្ថានភាពដែលមានស្រាប់។ វាការពារ
លក្ខខណ្ឌប្រណាំង នៅពេលដែលឧទាហរណ៍ ការចាប់ផ្ដើមស្វ័យប្រវត្តិ និងប៊ូតុង UI ត្រូវបានបើកដំណើរការក្នុងពេលដំណាលគ្នា។

---

## 4. ឯកសារយោង API

Route ទាំងអស់នៅក្រោម `/api/services/` គឺ **LOCAL_ONLY** (សម្រាប់តែ loopback ប៉ុណ្ណោះ ដែលជាច្បាប់តឹងរ៉ឹង #17)។
សំណើដែលមិនមែនជា loopback នឹងទទួលបាន `403 LOCAL_ONLY` ដោយមិនគិតពី auth token ឡើយ។

### 4.1 Endpoint របស់ 9Router (11 routes)

#### `POST /api/services/9router/install`

ដំឡើង 9Router ពី npm។ បង្កើត `DATA_DIR/services/9router/` ជាមួយនឹង
`package.json` និង `node_modules/` ផ្ទាល់ខ្លួន។ វាមិនប៉ះទង្គិចជាមួយ dependency ផ្ទាល់ខ្លួនរបស់ OmniRoute ទេ។

**តួសំណើ** (ទាំងអស់សុទ្ធតែជាជម្រើស):

```json
{ "version": "latest" }
```

| វាល       | ប្រភេទ   | លំនាំដើម   | ការពិពណ៌នា                             |
| --------- | -------- | ---------- | -------------------------------------- |
| `version` | `string` | `"latest"` | npm version tag ឬ semver ដែលត្រូវដំឡើង |

**ការឆ្លើយតប:**

| ស្ថានភាព | ការពិពណ៌នា                                                         |
| -------- | ------------------------------------------------------------------ |
| `200`    | `{ ok: true, installedVersion: "x.y.z", path: "..." }`             |
| `400`    | តួសំណើមិនត្រឹមត្រូវ (ការផ្ទៀងផ្ទាត់ Zod បរាជ័យ)                    |
| `409`    | កំពុងដំឡើងរួចហើយ (lock កំពុងត្រូវបានកាន់កាប់)                      |
| `500`    | ការដំឡើង npm បានបរាជ័យ — សូមមើល `message` សម្រាប់សារកំហុសដែលងាយយល់ |

**ចំណាំ:** ប្រើ `execFile('npm', [...])` — គ្មាន shell និងគ្មាន interpolation (ច្បាប់តឹងរ៉ឹង #13)។
កំហុស EACCES ត្រូវបានបង្ហាញជាសារដែលងាយយល់។

---

#### `POST /api/services/9router/start`

ចាប់ផ្ដើម 9Router។ ចុះឈ្មោះ supervisor ប្រសិនបើមិនទាន់បានចុះឈ្មោះ រួចហៅ
`supervisor.start()`។ ប្រតិបត្តិការនេះជា idempotent នៅពេលកំពុងដំណើរការរួចហើយ។

**តួសំណើ:** គ្មាន

**ការឆ្លើយតប:**

| ស្ថានភាព | ការពិពណ៌នា                                               |
| -------- | -------------------------------------------------------- |
| `200`    | Object `ServiceStatus` (សូមមើល schema ខាងក្រោម)          |
| `409`    | 9Router មិនទាន់ត្រូវបានដំឡើង (`status: "not_installed"`) |
| `503`    | ការចាប់ផ្ដើមបរាជ័យ (កំហុស process — សូមមើល `lastError`)  |

**Schema របស់ ServiceStatus:**

```json
{
  "tool": "9router",
  "state": "running",
  "pid": 12345,
  "port": 20130,
  "health": "healthy",
  "startedAt": "2026-05-25T10:00:00.000Z",
  "lastError": null
}
```

---

#### `POST /api/services/9router/stop`

បញ្ឈប់ 9Router ដោយរលូន។ ផ្ញើ SIGTERM រង់ចាំ 15 វិនាទី បន្ទាប់មកផ្ញើ SIGKILL ប្រសិនបើវានៅតែដំណើរការ។
ប្រតិបត្តិការនេះជា idempotent នៅពេលបានបញ្ឈប់រួចហើយ។

**តួសំណើ:** គ្មាន

**ការឆ្លើយតប:**

| ស្ថានភាព | ការពិពណ៌នា                          |
| -------- | ----------------------------------- |
| `200`    | `ServiceStatus` (state: "stopped")  |
| `503`    | ការបញ្ឈប់បានបរាជ័យដោយមិនបានរំពឹងទុក |

---

#### `POST /api/services/9router/restart`

ស្មើនឹង `stop()` បន្ទាប់មក `start()` នៅក្រោម operation lock។

**តួសំណើ:** គ្មាន

**ការឆ្លើយតប:** ដូចគ្នានឹង `start` (ត្រឡប់ `ServiceStatus` ចុងក្រោយ)។

---

#### `POST /api/services/9router/update`

ធ្វើបច្ចុប្បន្នភាព 9Router ទៅកំណែ npm ថ្មីជាងមុន។ ប្រសិនបើ service កំពុងដំណើរការ វានឹងត្រូវបានបញ្ឈប់
ជាមុន បន្ទាប់មកដំណើរការ npm install (ដំឡើងកំណែថ្មីជាងនៅទីតាំងដដែល) ហើយបន្ទាប់មក
service នឹងត្រូវបានចាប់ផ្ដើមឡើងវិញ។

**តួសំណើ** (ទាំងអស់សុទ្ធតែជាជម្រើស):

```json
{ "version": "latest" }
```

**ការឆ្លើយតប:**

| ស្ថានភាព | ការពិពណ៌នា                                                      |
| -------- | --------------------------------------------------------------- |
| `200`    | `{ ok: true, previousVersion: "...", installedVersion: "..." }` |
| `400`    | តួសំណើមិនត្រឹមត្រូវ                                             |
| `500`    | ការធ្វើបច្ចុប្បន្នភាព npm បានបរាជ័យ                             |

---

#### `POST /api/services/9router/rotate-key`

បង្កើត API key ថ្មីសម្រាប់ 9Router អ៊ិនគ្រីបវានៅពេលរក្សាទុក និងចាប់ផ្ដើម service ឡើងវិញ
(ប្រសិនបើកំពុងដំណើរការ) ដើម្បីឱ្យវាទទួលយក key ថ្មីពី environment របស់វា។ key ចាស់នឹងត្រូវបាន
ធ្វើឱ្យអសកម្មភ្លាមៗ។

**តួសំណើ:** គ្មាន

**ការឆ្លើយតប:**

| ស្ថានភាព | ការពិពណ៌នា                                 |
| -------- | ------------------------------------------ |
| `200`    | `{ keyRotated: true, restarted: boolean }` |
| `500`    | ការប្ដូរ key បានបរាជ័យ                     |

**សុវត្ថិភាព:** key ថ្មីមិនត្រូវបានបញ្ជូនត្រឡប់ក្នុងការឆ្លើយតបឡើយ (គ្មានការលេចធ្លាយព័ត៌មានសម្ងាត់)។
វាត្រូវបានរក្សាទុកក្នុងទម្រង់អ៊ិនគ្រីប (AES-256-GCM) នៅក្នុងតារាង `version_manager`។

---

#### `GET /api/services/9router/status`

ត្រឡប់ស្ថានភាពរួមបញ្ចូលគ្នារវាងស្ថានភាពបច្ចុប្បន្ន + DB រួមទាំង metadata របស់កំណែ និងការមើលជាមុននៃ API key។

**ការឆ្លើយតប:**

| ស្ថានភាព | ការពិពណ៌នា              |
| -------- | ----------------------- |
| `200`    | សូមមើល schema ខាងក្រោម  |
| `500`    | ការអានស្ថានភាពបានបរាជ័យ |

**Schema នៃការឆ្លើយតប:**

```json
{
  "tool": "9router",
  "state": "running",
  "pid": 12345,
  "port": 20130,
  "health": "healthy",
  "startedAt": "2026-05-25T10:00:00.000Z",
  "lastError": null,
  "installedVersion": "1.2.3",
  "latestVersion": "1.2.4",
  "updateAvailable": true,
  "apiKeyMasked": "nr_****abcd",
  "autoStart": false,
  "providerExpose": false
}
```

---

#### `POST /api/services/9router/auto-start`

បិទ/បើក flag ចាប់ផ្ដើមដោយស្វ័យប្រវត្តិ។ នៅពេល `enabled: true` service នឹងចាប់ផ្ដើមដោយស្វ័យប្រវត្តិ
នៅពេល OmniRoute boot លើកក្រោយ (ប្រសិនបើ service ត្រូវបានដំឡើង)។

**តួសំណើ:**

```json
{ "enabled": true }
```

**ការឆ្លើយតប:**

| ស្ថានភាព | ការពិពណ៌នា            |
| -------- | --------------------- |
| `200`    | `{ autoStart: true }` |
| `400`    | តួសំណើមិនត្រឹមត្រូវ   |

---

#### `GET /api/services/9router/logs`

SSE stream នៃ log បច្ចុប្បន្នពី ring buffer នៃ stdout/stderr របស់ 9Router។

**Query parameters:**

| ប៉ារ៉ាម៉ែត្រ | ប្រភេទ    | លំនាំដើម | ការពិពណ៌នា                                                                    |
| ------------ | --------- | -------- | ----------------------------------------------------------------------------- |
| `tail`       | `integer` | 200      | ចំនួនបន្ទាត់ប្រវត្តិដែលត្រូវផ្ញើមុនគេ (អតិបរមា 1000)                          |
| `filter`     | `string`  | គ្មាន    | តម្រង substring ដែលមិនប្រកាន់អក្សរធំតូច (គ្មាន regex — មានសុវត្ថិភាពពី ReDoS) |

**ព្រឹត្តិការណ៍ SSE:**

| ព្រឹត្តិការណ៍ | ទិន្នន័យ    | ការពិពណ៌នា                       |
| ------------- | ----------- | -------------------------------- |
| `snapshot`    | `LogLine[]` | បន្ទាត់ប្រវត្តិដំបូង             |
| `log`         | `LogLine`   | បន្ទាត់ log បច្ចុប្បន្ន          |
| `heartbeat`   | `{}`        | រក្សាការតភ្ជាប់រៀងរាល់ 15 វិនាទី |

**Schema របស់ LogLine:**

```json
{
  "ts": 1716633600000,
  "stream": "stdout",
  "line": "[9router] Listening on :20130"
}
```

**ការឆ្លើយតប:**

| ស្ថានភាព | ការពិពណ៌នា                                   |
| -------- | -------------------------------------------- |
| `200`    | `text/event-stream`                          |
| `400`    | ប៉ារ៉ាម៉ែត្រ `filter` វែងពេក (> 200 តួអក្សរ) |
| `404`    | រកមិនឃើញសេវា (supervisor មិនបានចុះឈ្មោះ)     |

---

### 4.2 ចំណុចចុង CLIProxyAPI (10 routes)

CLIProxyAPI មានទម្រង់ចំណុចចុងដូចគ្នានឹង 9Router ដោយដក `rotate-key` ចេញ ហើយបន្ថែម
`accounts`, `provider-expose` និង `auto-restart-adopted`។ ឥឡូវនេះ វាទទួលបាន
API key សម្រាប់ data-plane ដាច់ដោយឡែក ដែលត្រូវបានបញ្ចូលនៅពេល spawn (`needsApiKey: true` ក្នុង
`bootstrap.ts` ដែលប្រើសម្រាប់ធ្វើសមកាលកម្មម៉ូដែល); `status` មានវាលតិចជាងមុន។

| វិធីសាស្ត្រ | ផ្លូវ                               | ការពិពណ៌នា                                 |
| ----------- | ----------------------------------- | ------------------------------------------ |
| `POST`      | `/api/services/cliproxy/install`    | ដំឡើង CLIProxyAPI ពី npm                   |
| `POST`      | `/api/services/cliproxy/start`      | ចាប់ផ្ដើម CLIProxyAPI                      |
| `POST`      | `/api/services/cliproxy/stop`       | បញ្ឈប់ CLIProxyAPI                         |
| `POST`      | `/api/services/cliproxy/restart`    | ចាប់ផ្ដើម CLIProxyAPI ឡើងវិញ               |
| `POST`      | `/api/services/cliproxy/update`     | ធ្វើបច្ចុប្បន្នភាពទៅកំណែថ្មីជាងមុន         |
| `GET`       | `/api/services/cliproxy/status`     | ស្ថានភាពផ្ទាល់ + DB (គ្មាន `apiKeyMasked`) |
| `POST`      | `/api/services/cliproxy/auto-start` | បិទ/បើកការចាប់ផ្ដើមដោយស្វ័យប្រវត្តិ        |

ចំណុចចុងរួម `GET /api/services/{name}/logs` (សូមមើល §4.1) ដំណើរការសម្រាប់
សេវាទាំងបួន ដោយប្រើ dynamic segment `[name]`។

---

### 4.3 ចំណុចចុង Mux (8 routes)

Mux មានទម្រង់ចំណុចចុងដូចគ្នានឹង CLIProxyAPI — មិនមាន route `rotate-key` នៅក្នុងផ្ទៃ
API ទេ (bearer token ត្រូវបានបង្កើតតាមវិធីដូចគ្នានឹងរបស់ 9Router តាមរយៈ
`getOrCreateApiKey("mux")` និងត្រូវបានបញ្ចូលតាមរយៈ env var `MUX_SERVER_AUTH_TOKEN` ប៉ុន្តែ
បច្ចុប្បន្នមិនទាន់មានចំណុចចុងសម្រាប់ប្ដូរដោយឡែកទេ)។ Mux ត្រូវបានគ្រប់គ្រងតែវដ្តជីវិតប៉ុណ្ណោះ៖ ខុសពី
9Router វាមិនមាន Layer 4 executor និងមិនដែលត្រូវបានចុះឈ្មោះជាអ្នកផ្ដល់សេវា routing ទេ។

| វិធីសាស្ត្រ | ផ្លូវ                          | ការពិពណ៌នា                              |
| ----------- | ------------------------------ | --------------------------------------- |
| `POST`      | `/api/services/mux/install`    | ដំឡើង Mux ពី npm (`npm i mux`)          |
| `POST`      | `/api/services/mux/start`      | ចាប់ផ្ដើម Mux (`mux server`)            |
| `POST`      | `/api/services/mux/stop`       | បញ្ឈប់ Mux                              |
| `POST`      | `/api/services/mux/restart`    | ចាប់ផ្ដើម Mux ឡើងវិញ                    |
| `POST`      | `/api/services/mux/update`     | ធ្វើបច្ចុប្បន្នភាពទៅកំណែ npm ថ្មីជាងមុន |
| `GET`       | `/api/services/mux/status`     | ស្ថានភាពផ្ទាល់ + DB                     |
| `POST`      | `/api/services/mux/auto-start` | បិទ/បើកការចាប់ផ្ដើមដោយស្វ័យប្រវត្តិ     |

---

### 4.4 ចំណុចចុង Bifrost (8 routes)

Bifrost គឺជា backend relay សម្រាប់ AI gateway ដែលសរសេរដោយ Go (`@maximhq/bifrost`)។ វាប្រើ
ទម្រង់ចំណុចចុងដូចគ្នានឹង CLIProxyAPI (គ្មាន `rotate-key` — Bifrost គ្រប់គ្រង key របស់
provider ដោយខ្លួនឯងក្នុង `config.json` ក្រោម `-app-dir`)។

| វិធីសាស្ត្រ | ផ្លូវ                              | ការពិពណ៌នា                                                           |
| ----------- | ---------------------------------- | -------------------------------------------------------------------- |
| `POST`      | `/api/services/bifrost/install`    | ដំឡើង Bifrost ពី npm (`@maximhq/bifrost`)                            |
| `POST`      | `/api/services/bifrost/start`      | ចាប់ផ្ដើម Bifrost នៅលើ port 8080 (លំនាំដើម)                          |
| `POST`      | `/api/services/bifrost/stop`       | បញ្ឈប់ Bifrost                                                       |
| `POST`      | `/api/services/bifrost/restart`    | ចាប់ផ្ដើម Bifrost ឡើងវិញ                                             |
| `POST`      | `/api/services/bifrost/update`     | ធ្វើបច្ចុប្បន្នភាពទៅកំណែថ្មីជាងមុន                                   |
| `GET`       | `/api/services/bifrost/status`     | ស្ថានភាពផ្ទាល់ + DB                                                  |
| `POST`      | `/api/services/bifrost/auto-start` | បិទ/បើកការចាប់ផ្ដើមដោយស្វ័យប្រវត្តិ                                  |
| `GET`       | `/api/services/bifrost/logs`       | ផ្នែកចុងក្រោយនៃ log តាម SSE (តាមរយៈ dynamic route `[name]/logs` រួម) |

**ការតភ្ជាប់ routing:** នៅពេលមិនបានកំណត់ `BIFROST_BASE_URL` ហើយ instance Bifrost ដែលស្ថិតក្រោម
ការគ្រប់គ្រងកំពុងដំណើរការ `getBifrostRoutingConfig()` (ក្នុង `routingBackend.ts`) នឹងប្រើ
`http://127.0.0.1:{port}` ជា URL មូលដ្ឋានរបស់ relay ដោយស្វ័យប្រវត្តិ។ env `BIFROST_BASE_URL` ដែលបានកំណត់ជាក់លាក់
តែងតែមានអាទិភាព។

---

### 4.5 ចំណុចចុង Dario (12 routes)

មានទម្រង់វដ្តជីវិតដូចគ្នានឹងសេវាផ្សេងទៀត (`install`, `start`, `stop`, `restart`,
`update`, `status`, `auto-start`, `auto-restart-adopted`) ព្រមទាំង control plane OAuth
ដែលការពារដោយ token នៅក្រោម `admin/`: `admin/accounts`, `admin/import-from-omniroute`,
`admin/login-start`, `admin/login-complete` (ទាំងអស់នៅពីក្រោយ `DARIO_ADMIN_TOKEN`)។

### 4.6 ចំណុចចុង open-wa (7 routes)

open-wa (`@open-wa/wa-automate`) បញ្ជា instance Chromium ដែលគ្មាន UI (តាមរយៈ
Puppeteer) ដើម្បីធ្វើស្វ័យប្រវត្តិកម្ម WhatsApp Web។ វាប្រើទម្រង់ចំណុចចុងដូចគ្នានឹង Mux (មិនទាន់មាន
route `rotate-key` ទេ)។ វាត្រូវបានគ្រប់គ្រងតែវដ្តជីវិតប៉ុណ្ណោះ — មិនមែនជា routing target ទេ
ហើយមិនមានធាតុ Layer 4 executor/provider ទេ។

| វិធីសាស្ត្រ | ផ្លូវ                             | ការពិពណ៌នា                                                                 |
| ----------- | --------------------------------- | -------------------------------------------------------------------------- |
| `POST`      | `/api/services/openwa/install`    | ដំឡើង open-wa ពី npm (`@open-wa/wa-automate`)                              |
| `POST`      | `/api/services/openwa/start`      | ចាប់ផ្តើម open-wa នៅលើច្រក 8323 (លំនាំដើម)                                 |
| `POST`      | `/api/services/openwa/stop`       | បញ្ឈប់ open-wa                                                             |
| `POST`      | `/api/services/openwa/restart`    | ចាប់ផ្តើម open-wa ឡើងវិញ                                                   |
| `POST`      | `/api/services/openwa/update`     | ធ្វើបច្ចុប្បន្នភាពទៅកំណែថ្មីជាងនេះ                                         |
| `GET`       | `/api/services/openwa/status`     | ស្ថានភាពផ្ទាល់ + មូលដ្ឋានទិន្នន័យ                                          |
| `POST`      | `/api/services/openwa/auto-start` | បិទ/បើកការចាប់ផ្តើមដោយស្វ័យប្រវត្តិ                                        |
| `GET`       | `/api/services/openwa/logs`       | ការតាមដានកំណត់ហេតុ SSE (តាមរយៈ route ឌីណាមិក `[name]/logs` ដែលប្រើរួមគ្នា) |

**API key:** ត្រូវបានបញ្ចូលជា `WA_KEY` — ការកំណត់ជំនួសតាម env ដែលមានបុព្វបទ `WA_*` ទូទៅរបស់ open-wa
ផ្គូផ្គងវាទៅនឹងជម្រើស CLI `--key`/`-k`
(`dist/cli/setup.js::envArgs()` ដែលបានផ្ទៀងផ្ទាត់ជាមួយ package កំណែ 4.76.0
ដែលបានដំឡើង)។ មានបុព្វបទ `ow_` នៅពេលបង្កើតដោយ `generateServiceApiKey()`។ open-wa
អាន key ត្រឡប់ពី HTTP header `key`/`api_key` (មិនមែន `Authorization:
Bearer` ទេ)។ `/api-docs*` ត្រូវបានលើកលែងពីការត្រួតពិនិត្យនេះយ៉ាងច្បាស់
(`setupAuthenticationLayer` នៅក្នុង `dist/cli/server.js`) ដូច្នេះ health probe
មិនត្រូវការ auth header ទេ។

**ការផ្គូផ្គង:** open-wa មិនមែនជាផ្លូវការ និងមិនមានទំនាក់ទំនងជាមួយ WhatsApp ទេ —
លេខដែលបានភ្ជាប់មានហានិភ័យត្រូវបានហាមឃាត់ដោយការរកឃើញស្វ័យប្រវត្តិកម្មផ្ទាល់របស់ WhatsApp។
នៅពេលចាប់ផ្តើមលើកដំបូង កូដ QR សម្រាប់ផ្គូផ្គងត្រូវបានបោះពុម្ពទៅ stdout និងបង្ហាញតាមរយៈ
ផ្ទាំងកំណត់ហេតុ/ស្ទ្រីម SSE ដែលមានស្រាប់ — មិនទាន់មាន endpoint រូបភាព QR ដាច់ដោយឡែក
នៅក្នុងការរួមបញ្ចូលនេះនៅឡើយទេ។

---

### 4.7 ប្រូកស៊ីបញ្ច្រាស (ការបង្កប់ dashboard របស់ 9Router)

dashboard បង្កប់ UI វេបរបស់ 9Router នៅក្នុង iframe តាមរយៈប្រូកស៊ីបញ្ច្រាសខាងក្នុង
នៅ៖

```
GET|POST|... /dashboard/providers/services/9router/embed/[...path]
```

ប្រូកស៊ីនេះ៖

- បញ្ជូនបន្តសំណើទៅ `http://127.0.0.1:{port}/{path}` (តែ loopback ប៉ុណ្ណោះ)
- ដក header `cookie` និង `authorization` ដែលចូលមក (គ្មានការលេចធ្លាយ session របស់ OmniRoute)
- បញ្ចូល `Authorization: Bearer {apiKey}` សម្រាប់ការផ្ទៀងផ្ទាត់របស់ 9Router
- ដក `set-cookie`, `content-security-policy`, `x-frame-options`, `cross-origin-*` ចេញពី response
- សរសេរ response HTML ឡើងវិញ ដើម្បីបញ្ចូល `<base href>` និងធ្វើឱ្យ absolute path មានទម្រង់ស្តង់ដារ (`/foo` → `/dashboard/.../embed/foo`)

ការដំឡើងកម្រិត WebSocket សម្រាប់ dashboard ដែលបានបង្កប់ ត្រូវបានគ្រប់គ្រងដោយ server ដៃគូនៅលើ
ច្រកដាច់ដោយឡែក (សូមមើល `src/lib/services/embedWsProxy.ts`)។

**សុវត្ថិភាព:** route របស់ប្រូកស៊ីបង្កប់ត្រូវបានចាត់ថ្នាក់ក្រោម `LOCAL_ONLY_API_PREFIXES`
ហើយអាចចូលប្រើបានតែពី loopback ប៉ុណ្ណោះ។ អ្នកវាយប្រហារដែលទទួលបាន JWT តាមរយៈ
tunnel Cloudflare/Ngrok មិនអាចប្រើប្រូកស៊ីចូលទៅកាន់សេវាកម្មដែលបានបង្កប់បានទេ។

---

## 5. សុវត្ថិភាព

### ការអនុវត្ត LOCAL_ONLY (ច្បាប់តឹងរ៉ឹង #17)

រាល់ route ក្រោម `/api/services/` និង `/dashboard/providers/services/*/embed/` ត្រូវបាន
ចាត់ថ្នាក់ជា LOCAL_ONLY នៅក្នុង `src/server/authz/routeGuard.ts`។ ការត្រួតពិនិត្យ loopback
ដំណើរការដោយគ្មានលក្ខខណ្ឌ មុនពេល auth branch ណាមួយ៖

```
សំណើមកដល់
  → isLocalOnlyPath(path)?
      → មិនមែន loopback → 403 LOCAL_ONLY (ជានិច្ច មុនការត្រួតពិនិត្យ auth)
      → loopback         → បន្តទៅ auth ធម្មតា
```

វារារាំង JWT ដែលបានលេចធ្លាយ (ឧទាហរណ៍ តាមរយៈ tunnel) មិនឱ្យកេះ `npm install` ឬ
ការបង្កើត process។ សូមមើល `docs/security/ROUTE_GUARD_TIERS.md` សម្រាប់ម៉ាទ្រីស
កម្រិតពេញលេញ។

### ការបញ្ចូល API key

9Router និង Mux តម្រូវឱ្យមាន API key/bearer token សម្រាប់ HTTP endpoint របស់ពួកវា។
OmniRoute៖

1. បង្កើត key តាមរយៈ `crypto.randomBytes(32).toString("base64url")` ជាមួយ
   prefix ជាក់លាក់តាម service (`nr_` សម្រាប់ 9Router និង `mx_` សម្រាប់ Mux)។
2. អ៊ិនគ្រីបវាពេលរក្សាទុកដោយប្រើ AES-256-GCM (cipher ដូចគ្នាដែលប្រើសម្រាប់ provider credentials)។
3. ឌិគ្រីប និងបញ្ចូលវាជា environment variable នៅពេលបង្កើត process —
   `NINEROUTER_API_KEY` សម្រាប់ 9Router និង `MUX_SERVER_AUTH_TOKEN` សម្រាប់ Mux (មិនប្រើជា CLI
   flag ឡើយ ដូច្នេះ token មិនដែលបង្ហាញក្នុង `ps`/បញ្ជី process ទេ)។
4. មិនដែលត្រឡប់ plaintext key នៅក្នុង HTTP response ណាមួយទេ។

CLIProxyAPI ទទួលបាន data-plane key ដាច់ដោយឡែក ដែលត្រូវបានបញ្ចូលនៅពេលបង្កើត process
(`needsApiKey: true` — ប្រើសម្រាប់ការធ្វើសមកាលកម្ម model ជាមួយ adapter)។

### ការការពារ SSRF

reverse HTTP proxy (`/dashboard/.../embed/[...path]`) ត្រូវបានកំណត់ជាប់ថា forward
បានតែទៅ `http://127.0.0.1:{port}` ប៉ុណ្ណោះ។ វាមិនដែលធ្វើតាម redirect ទៅកាន់
គោលដៅដែលមិនមែនជា loopback ទេ។ library `ssrf-req-filter` ត្រូវបានប្រើដើម្បីបដិសេធ upstream URL ណាមួយដែល
resolve ទៅក្រៅចន្លោះ loopback។

### សុវត្ថិភាព shell (ច្បាប់តឹងរ៉ឹង #13)

`npm install` ត្រូវបានហៅតាមរយៈ `execFile('npm', ['install', pkg, '--prefix', dir])` —
គ្មាន template literal គ្មាន shell និងគ្មានការធ្វើ interpolation នៃ path ខាងក្រៅទៅក្នុង command
string ទេ។ តម្លៃ runtime (port និង API key) ត្រូវបានបញ្ជូនតាម object `env` របស់ child។

### ការសម្អាត error (ច្បាប់តឹងរ៉ឹង #12)

រាល់ error response ពី `/api/services/*` ឆ្លងកាត់ `buildErrorBody()` ឬ
`sanitizeErrorMessage()`។ `err.stack` និង `err.message` ដើម មិនដែលត្រូវបានត្រឡប់
ដោយផ្ទាល់ទៅអ្នកហៅទេ។

---

## 6. ការបន្ថែម embedded service ថ្មី

អនុវត្តតាម 8 ជំហានខាងក្រោម។ សូមអាន implementation ដែលមានស្រាប់នៅក្នុង `src/lib/services/installers/`
និង `src/app/api/services/` ជាឯកសារយោងគោល។

### ជំហានទី 1 — បង្កើត installer

បង្កើត `src/lib/services/installers/{name}.ts` ដោយយកលំនាំតាម `ninerouter.ts`៖

```typescript
export const NAME_PACKAGE = "your-npm-package";
export const NAME_DEFAULT_PORT = 20132; // ជ្រើសរើស port ទំនេរមួយ

export async function install(version = "latest"): Promise<InstallResult> { ... }
export async function update(version = "latest"): Promise<InstallResult> { ... }
export async function uninstall(): Promise<void> { ... }
export function resolveSpawnArgs(apiKey: string, port: number): SpawnArgs { ... }
export async function getInstalledVersion(): Promise<string | null> { ... }
export async function getLatestVersion(): Promise<string | null> { ... }
```

ប្រើ `runNpm(['install', NAME_PACKAGE, '--prefix', dir])` ពី `installers/utils.ts`
— កុំប្រើ `execSync` ឬ shell interpolation ឡើយ។

### ជំហានទី 2 — ចុះឈ្មោះក្នុង bootstrap

បន្ថែម `ServiceEntry` មួយទៅក្នុង array `SERVICES` នៅក្នុង `src/lib/services/bootstrap.ts`៖

```typescript
{
  tool: "myservice",
  port: NAME_DEFAULT_PORT,
  healthPath: "/health",
  healthIntervalMs: 5_000,
  stopTimeoutMs: 15_000,
  logsBufferBytes: 5_242_880,
  needsApiKey: true, // false ប្រសិនបើមិនត្រូវការ API key
}
```

ពង្រីក `buildSpawnArgsFactory()` ដើម្បីគ្រប់គ្រង `cfg.tool === "myservice"`។

#### កិច្ចសន្យា provider-plugin ដែលអាចដោតបន្ថែមបាន (ដំណាក់កាលទី 1, #7333)

`src/lib/services/providerPlugins/` ណែនាំកិច្ចសន្យា `ServiceProviderPlugin` ដែល
វេចខ្ចប់ field `ServiceEntry` របស់ `bootstrap.ts` និង field manifest-template របស់
`serviceBackends.ts` នៃ backend មួយទៅក្នុង object តែមួយ ជំនួសឱ្យការបង្ហាញទម្រង់របស់
backend ដូចគ្នាដាច់ដោយឡែកនៅក្នុង file ពីរដែលមិនទាក់ទងគ្នា។ នៅពេលសរសេរនេះ **មានតែ `9router` ប៉ុណ្ណោះដែលត្រូវបាន
ផ្ទេរ** — `bootstrap.ts` បង្កើត entry `SERVICES[]` របស់វាពី
`getServiceProviderPlugin("9router")` (`src/lib/services/providerPlugins/registry.ts`)
ដោយបញ្ចេញ startup error ប្រសិនបើ plugin នោះបាត់។ `cliproxy`, `mux` និង `bifrost`
នៅតែប្រើ literal `SERVICES[]` inline ដែលមានស្រាប់ ដោយមិនមានការផ្លាស់ប្តូរ។

`open-sse/config/providerPluginManifest.ts` ក៏បានបន្ថែម helper
`createServiceBackendManifestEntry(pluginId, template)` ដែលបង្កើត
`ProviderPluginManifestEntry` ត្រឹមត្រូវមួយពី entry `SERVICE_BACKEND_MANIFEST_TEMPLATE` — វា
**មិនទាន់** ត្រូវបានតភ្ជាប់ទៅ request path ដែលកំពុងដំណើរការណាមួយទេ (ទាំង `generateProviderPluginManifestFromRegistry()`
និង `/v1/providers/[provider]/models`); ការងារនេះនៅតែជាការងារបន្តបន្ទាប់ បន្ទាប់ពីកិច្ចសន្យានេះ
ត្រូវបានបញ្ជាក់សុពលភាពសម្រាប់ backend ទីពីរ។

ការងារដែលពន្យារទៅ PR បន្តបន្ទាប់ និងតាមដានក្រោម issue #7333 រួមមាន៖ ការផ្ទេរ `cliproxyapi` តាម
registry ដូចគ្នា ការធ្វើ generalize `mux`/`bifrost` ទៅក្នុង union `ServiceBackendPluginId`
ការបញ្ចូលករណីពិសេសនៃ executor-routing (`open-sse/executors/index.ts`,
`open-sse/handlers/chatCore/executorProxy.ts`) ទៅក្នុងកិច្ចសន្យា plugin និងការតភ្ជាប់
`createServiceBackendManifestEntry()` ទៅកាន់ manifest/models code path ដែលកំពុងដំណើរការ។

### ជំហានទី 3 — បន្ថែម migration និង DB seed

ត្រូវប្រាកដថា service មាន row មួយនៅក្នុង `version_manager` តាមរយៈ migration នៅក្នុង
`src/lib/db/migrations/`។ row នោះគួរតែមាន៖

```sql
INSERT OR IGNORE INTO version_manager (tool, status, auto_start, provider_expose)
VALUES ('myservice', 'not_installed', 0, 0);
```

### ជំហានទី 4 — បង្កើត API endpoint ទាំង 7

ក្រោម `src/app/api/services/{name}/`៖

```
_lib.ts            helper getOrInitSupervisor()
install/route.ts   POST — ហៅ installer.install()
start/route.ts     POST — ហៅ supervisor.start()
stop/route.ts      POST — ហៅ supervisor.stop()
restart/route.ts   POST — ហៅ supervisor.restart()
update/route.ts    POST — ហៅ installer.update()
status/route.ts    GET  — បញ្ចូលស្ថានភាព live + DB ចូលគ្នា
auto-start/route.ts POST — បិទ/បើក flag auto_start
```

Route `GET /api/services/[name]/logs` ដែលប្រើរួមគ្នាត្រូវបានភ្ជាប់រួចហើយ — មិនចាំបាច់ធ្វើការផ្លាស់ប្តូរនៅទីនោះទេ។

ផ្ទេរការឆ្លើយតបកំហុសទាំងអស់តាមរយៈ `createErrorResponse()` / `buildErrorBody()`។

### ជំហានទី 5 — បន្ថែមទៅ LOCAL_ONLY_API_PREFIXES

នៅក្នុង `src/server/authz/routeGuard.ts` ផ្ទៀងផ្ទាត់ថា `/api/services/` ត្រូវបានរាយបញ្ជីរួចហើយ។
ប្រសិនបើអ្នកបង្កើត prefix ថ្មី (ឧ. `/api/tools/`) សូមបន្ថែមវាទៅទាំង
`LOCAL_ONLY_API_PREFIXES` និង `SPAWN_CAPABLE_PREFIXES` ប្រសិនបើវាបង្កើត process។
បន្ថែម test មួយនៅក្នុង `tests/unit/authz/routeGuard.test.ts`។

### ជំហានទី 6 — បន្ថែមផ្ទាំង UI

បង្កើត `src/app/(dashboard)/dashboard/providers/services/tabs/{Name}ServiceTab.tsx`។
ប្រើ component រួមឡើងវិញ៖

- `ServiceStatusCard` — ស្ថានភាពបន្តផ្ទាល់ + ផ្លាកសញ្ញាសុខភាព
- `ServiceLifecycleButtons` — ចាប់ផ្ដើម / បញ្ឈប់ / ចាប់ផ្ដើមឡើងវិញ / ធ្វើបច្ចុប្បន្នភាព
- `ServiceLogsPanel` — កន្ទុយ log តាម SSE (ភ្ជាប់ទៅ `/api/services/{name}/logs`)
- `ApiKeyCard` — បង្ហាញ key + ប្ដូរ key (ប្រសិនបើ `needsApiKey: true`)

ចុះបញ្ជីផ្ទាំងនៅក្នុង `ServicesPageShell.tsx`។

### ជំហានទី 7 — បន្ថែមធាតុ provider (ប្រសិនបើ service ជាគោលដៅ routing)

ប្រសិនបើ service ដែលបានបង្កប់ផ្ដល់ endpoint `/v1/chat/completions` ដែលត្រូវគ្នាជាមួយ OpenAI៖

1. បន្ថែមធាតុ provider នៅក្នុង `src/shared/constants/providers.ts` ជាមួយ `isEmbeddedService: true`។
2. បង្កើត `open-sse/executors/{name}.ts` ដែលពង្រីក `BaseExecutor`។ ស្វែងរក port និង
   API key ឡើងវិញក្នុង request នីមួយៗ (កុំ cache នៅក្នុង constructor)។ ត្រឡប់ response `503 service_not_running`
   នៅពេលស្ថានភាព supervisor មិនមែនជា `"running"`។
3. ចុះបញ្ជី model នៅក្នុង `open-sse/config/providerRegistry.ts` ជាមួយ prefix របស់ service
   (ឧ. `myservice/sub/model`)។ `modelSync.ts` នឹងរក្សាពួកវាឱ្យទាន់សម័យ។

### ជំហានទី 8 — រៀបចំឯកសារ និងធ្វើតេស្ត

1. ធ្វើបច្ចុប្បន្នភាព `docs/frameworks/EMBEDDED-SERVICES.md` (ឯកសារនេះ) — បន្ថែម service ទៅក្នុង
   តារាងនៅ §1 និង endpoint ថ្មីណាមួយទៅ §4។
2. បន្ថែម unit test នៅក្នុង `tests/unit/services/` (វដ្តជីវិត កម្មវិធីដំឡើង និងទម្រង់ API)។
3. បន្ថែម integration test នៅក្នុង `tests/integration/services/` (នៅពីក្រោយ `RUN_SERVICES_INT=1`)។
4. ធ្វើបច្ចុប្បន្នភាព `docs/openapi.yaml` ជាមួយ endpoint ថ្មីៗ។

---

## 7. ការដោះស្រាយបញ្ហា

### សេវាមិនចាប់ផ្ដើម

**រោគសញ្ញា:** ប៊ូតុងចាប់ផ្ដើមត្រឡប់ 503 ស្ថានភាពនៅតែជា `"error"` ឬ `"starting"`។

**បញ្ជីត្រួតពិនិត្យ:**

1. ពិនិត្យ `GET /api/services/{name}/logs` (ឬផ្ទាំង Logs ក្នុង dashboard)។ ស្វែងរក
   បន្ទាត់ដូចជា `Error: ENOENT`, `address already in use` ឬ `Cannot find module`។
2. ផ្ទៀងផ្ទាត់ថា `npm` ស្ថិតក្នុង PATH៖ ដំណើរការ `which npm` ពីគណនីអ្នកប្រើដូចគ្នាដែលដំណើរការ OmniRoute។
3. ផ្ទៀងផ្ទាត់ថាសេវាត្រូវបានដំឡើង៖ ពិនិត្យ `GET /api/services/{name}/status` សម្រាប់
   `installedVersion`។ ប្រសិនបើវាជា `null` សូមដំណើរការការដំឡើងជាមុន។
4. ពិនិត្យថា `DATA_DIR/services/{name}/node_modules/` មាន និងមិនទទេ។
5. ពិនិត្យវាល `lastError` ក្នុងការឆ្លើយតបស្ថានភាព ដើម្បីមើលមូលហេតុចាកចេញដែលបានសម្អាតព័ត៌មានរសើប។

---

### ការចាប់ផ្ដើមត្រជាក់យឺត (> 10 វិនាទីដើម្បីឈានដល់ `running`)

**រោគសញ្ញា:** ស្ថានភាពនៅតែជា `"starting"` អស់រយៈពេលយូរ មុនពេលប្ដូរទៅជា `"running"` ឬ `"error"`។

**ការពន្យល់:** ការចាប់ផ្ដើមត្រជាក់របស់ 9Router រួមបញ្ចូលការនាំចូលមែកធាង dependency ធំៗ (DNS,
tunnel និងម៉ូឌុល MITM)។ ចន្លោះពេលពិនិត្យសុខភាពលំនាំដើមគឺ 2 វិនាទី ដោយសាកល្បង 3 ដង មុនពេល
supervisor ប្រកាសថាអស់ពេលកំណត់ (ប៉ុន្តែបន្តធ្វើ polling)។

**ដំណោះស្រាយ:** `healthIntervalMs` និងពេលអស់កំណត់របស់ `waitForHealthy`
(`healthIntervalMs * 3`) អាចកំណត់រចនាសម្ព័ន្ធបានក្នុង `bootstrap.ts`។ សម្រាប់សេវាដែលត្រូវការ
ពេលចាប់ផ្ដើមយូរជាងនេះ សូមបង្កើន `healthIntervalMs` ទៅ 5000 និង `stopTimeoutMs` ទៅ 30 000។

---

### ការប៉ះទង្គិច port (`EADDRINUSE`)

**រោគសញ្ញា:** កំណត់ហេតុបង្ហាញ `address already in use :::20130`។

**មូលហេតុ:**

- ដំណើរការផ្សេងទៀតកំពុងប្រើ port 20130 រួចហើយ។
- ដំណើរការ 9Router មុនមួយមិនបានបញ្ឈប់ទាំងស្រុង (zombie PID)។

**ដំណោះស្រាយ:**

1. ផ្លាស់ប្ដូរ port លំនាំដើមតាមរយៈអថេរបរិស្ថាន `NINEROUTER_PORT` ក្នុង `.env`។
2. ស្វែងរក និងបញ្ឈប់ដំណើរការដែលប៉ះទង្គិច៖ `lsof -ti :20130 | xargs kill -9`។
3. port អាចកំណត់រចនាសម្ព័ន្ធតាមសេវានីមួយៗក្នុង `bootstrap.ts` តាមរយៈវាល `port`។

**ចំណាំ:** 9Router ប្រើ port 20130 ជាលំនាំដើមជាក់លាក់ ដើម្បីជៀសវាងការប៉ះទង្គិចជាមួយ
port លំនាំដើម 20128 របស់ OmniRoute។

---

### ការអនុញ្ញាតត្រូវបានបដិសេធ (EACCES) ពេលដំឡើង

**រោគសញ្ញា:** ការដំឡើងត្រឡប់ 500 ហើយកំណត់ហេតុបង្ហាញ `EACCES` ឬ `permission denied`។

**មូលហេតុ:**

- `DATA_DIR` ឬថតមេរបស់វា មិនអាចសរសេរបានដោយដំណើរការ OmniRoute។
- កំពុងដំណើរការក្នុង Docker rootless ដោយគ្មានសិទ្ធិសរសេរទៅកាន់ volume ដែលបានផ្គូផ្គង។

**ដំណោះស្រាយ:**

1. ពិនិត្យ `DATA_DIR` (លំនាំដើម៖ `~/.omniroute/`)៖ `ls -la ~/.omniroute/`
2. ធានាថាអ្នកប្រើដែលដំណើរការ OmniRoute ជាម្ចាស់ថត៖ `chown -R $USER ~/.omniroute/`
3. ក្នុង Docker សូមធានាថា volume mount មានសិទ្ធិត្រឹមត្រូវសម្រាប់អ្នកប្រើ container។

---

### ការធ្វើបច្ចុប្បន្នភាពបរាជ័យ (`npm install` អស់ពេលកំណត់ ឬមានកំហុសបណ្ដាញ)

**រោគសញ្ញា:** ការធ្វើបច្ចុប្បន្នភាពត្រឡប់ 500 ជាមួយ `InstallError` ហើយកំណត់ហេតុបង្ហាញថាបណ្ដាញអស់ពេលកំណត់។

**បញ្ជីត្រួតពិនិត្យ:**

1. បញ្ជាក់ថាអាចចូលប្រើ npm registry បាន៖ `npm ping`។
2. ពិនិត្យ corporate proxy៖ `npm config get proxy`, `npm config get https-proxy`។
3. សាកល្បងដំឡើងដោយដៃ៖ `npm install {package}@latest --prefix ~/.omniroute/services/{name}/`។
4. ប្រសិនបើនៅក្រោយ air-gap សូមទាញយក tarball ជាមុន ហើយប្រើ `npm install /path/to/tarball.tgz`។

---

### សេវាបង្ហាញស្ថានភាព `"error"` ភ្លាមៗបន្ទាប់ពីចាប់ផ្ដើម (គាំងរហ័ស)

**រោគសញ្ញា:** ស្ថានភាពប្ដូរពី `"starting"` ទៅ `"error"` ក្នុងរយៈពេលក្រោម 5 វិនាទី។
`lastError` បង្ហាញ `"Fast crash (exited with code 1)"`។

**បញ្ជីត្រួតពិនិត្យ:**

1. អានកំណត់ហេតុចុងក្រោយទាំងមូល៖ `GET /api/services/{name}/logs?tail=500`។
2. មូលហេតុទូទៅ៖ ខ្វះអថេរបរិស្ថានដែលសេវាត្រូវការ។
3. សម្រាប់ 9Router៖ ផ្ទៀងផ្ទាត់ថា `NINEROUTER_DISABLE_MITM=true` និង
   `NINEROUTER_DISABLE_TUNNEL=true` ស្ថិតក្នុង env ដែលបានបញ្ជូននៅពេល spawn (សូមមើល
   `installers/ninerouter.ts` `resolveSpawnArgs`)។

---

## 8. សំណួរដែលសួរញឹកញាប់

**សំណួរ៖ តើខ្ញុំអាចបើកឱ្យម៉ាស៊ីនភ្ញៀវដែលមិនមែនជា loopback ចូលប្រើ endpoint របស់សេវាកម្មដែលបានបង្កប់បានទេ?**

មិនបានទេ។ កម្រិត LOCAL_ONLY ត្រូវបានកំណត់ឡើងដោយចេតនា (វិធានតឹងរ៉ឹង #17)។ Route ដែលអាចហៅ
`npm install` ឬបង្កើត process `node` មិនត្រូវអាចចូលប្រើបានពី traffic ដែលមិនមែនជា loopback
នោះទេ ព្រោះបើមិនដូច្នោះទេ JWT ដែលបែកធ្លាយតាមរយៈ tunnel (Cloudflare, Ngrok, Tailscale) អាច
អនុញ្ញាតឱ្យបង្កើត process តាមអំពើចិត្តបាន។ មិនមានករណីលើកលែងដើម្បីដកចេញពីវិធាននេះសម្រាប់
`/api/services/` ទេ — ខុសពី `/api/mcp/` វាត្រូវបានដកចេញពីបញ្ជី bypass នៃ manage-scope។
សូមមើល `docs/security/ROUTE_GUARD_TIERS.md`។

---

**សំណួរ៖ តើ 9Router និង CLIProxyAPI នឹងអាចប្រើបាននៅក្នុងការដាក់ឱ្យដំណើរការបែប production/cloud ដែរឬទេ?**

បាន។ សេវាកម្មទាំងពីរអនុវត្តតាមគំរូដែលផ្តោតលើមូលដ្ឋានជាមុនដូច OmniRoute ផ្ទាល់។ ពួកវាដំណើរការ
លើម៉ាស៊ីនតែមួយ និងទាក់ទងគ្នាតាមរយៈ loopback។ "Production" នៅទីនេះមានន័យថា VPS
ឬម៉ាស៊ីនមេមូលដ្ឋានដែល OmniRoute ត្រូវបានដាក់ឱ្យដំណើរការ មិនមែនជាអ្នកផ្តល់សេវា cloud ពីចម្ងាយទេ។

---

**សំណួរ៖ តើខ្ញុំអាច debug supervisor ដោយរបៀបណា?**

1. តាមដាន stream កំណត់ហេតុ SSE៖ `curl -N http://localhost:20128/api/services/9router/logs`។
2. ពិនិត្យកំណត់ហេតុដែលមានរចនាសម្ព័ន្ធក្នុង output របស់ pino នៃ OmniRoute ដោយ filter តាម
   namespace `service:supervisor`។
3. ពិនិត្យ row ក្នុង DB៖ `sqlite3 ~/.omniroute/omniroute.db "SELECT * FROM version_manager WHERE tool='9router'"`។
4. ប្រើ `GET /api/services/9router/status` ដើម្បីមើលស្ថានភាពផ្ទាល់បច្ចុប្បន្ន PID, សុខភាព
   និង `lastError` ក្នុងការហៅតែមួយ។

---

**សំណួរ៖ Supervisor បង្ហាញ `health: "degraded"` ឬ `health: "unknown"` ប៉ុន្តែ state គឺ `"running"`។ តើវាជាបញ្ហាដែរឬទេ?**

`"degraded"` មានន័យថា health probe បានត្រឡប់ response ដែលមិនមែនជា 200។ `"unknown"` មានន័យថា
មិនទាន់មាន probe ណាមួយបានបញ្ចប់នៅឡើយ (ការប្រណាំងពេលវេលាជាមួយ poll ដំបូង)។ ទាំងពីរជាស្ថានភាពបណ្ដោះអាសន្ន
ក្នុងអំឡុងពេលចាប់ផ្ដើម។ ប្រសិនបើសុខភាពនៅតែ `"degraded"` លើសពី `healthIntervalMs * 3` ms បន្ទាប់ពី
`"running"` នោះសេវាកម្មដែលបានបង្កប់កំពុងដំណើរការ ប៉ុន្តែ HTTP API របស់វាមិនឆ្លើយតប។ សូមពិនិត្យ
ថាតើ port ក្នុង status response ត្រឹមត្រូវដែរឬទេ និងថាតើសេវាកម្មពិតជាកំពុង
listen លើ port នោះដែរឬទេ។

---

**សំណួរ៖ តើខ្ញុំអាចផ្លាស់ប្តូរ API key របស់ 9Router ដោយមិនចាំបាច់ restart ទាំងស្រុងបានទេ?**

មិនបានទេ។ API key ត្រូវបានបញ្ជូនទៅ 9Router តាមរយៈ environment variable នៅពេល spawn។
Environment variable មិនអាចត្រូវបានផ្លាស់ប្តូរក្នុង process ដែលកំពុងដំណើរការបានទេ។ `POST .../rotate-key`
នឹងបញ្ឈប់ និងចាប់ផ្តើមសេវាកម្មឡើងវិញដោយស្វ័យប្រវត្តិ ដើម្បីអនុវត្ត key ថ្មី។ ការប្ដូរ key
ចូលជាធរមានក្នុងរយៈពេល `stopTimeoutMs` របស់សេវាកម្ម (លំនាំដើម 15 s) បូកនឹងរយៈពេលចាប់ផ្ដើម
របស់វា។

---

**សំណួរ៖ តើដែនកំណត់របស់ ring buffer ជាអ្វី ហើយតើមានអ្វីកើតឡើងនៅពេលវាពេញ?**

សេវាកម្មនីមួយៗមាន ring buffer ទំហំ 5 MB ដាច់ដោយឡែក។ នៅពេល buffer ពេញ បន្ទាត់
កំណត់ហេតុចាស់បំផុតនឹងត្រូវបានដកចេញ ដើម្បីទុកកន្លែងសម្រាប់បន្ទាត់ថ្មី។ ព្រឹត្តិការណ៍ SSE `snapshot` ត្រឡប់
បន្ទាត់ថ្មីៗបំផុតនៅក្នុងដែនកំណត់ `tail`។ កំណត់ហេតុមិនត្រូវបានរក្សាទុកជាអចិន្ត្រៃយ៍ទៅ disk ទេ លុះត្រាតែ
`logsBufferPath` ត្រូវបានកំណត់ក្នុង row របស់ DB។

---

## សូមមើលផងដែរ

- `docs/security/ROUTE_GUARD_TIERS.md` — ព័ត៌មានលម្អិតអំពីកម្រិត LOCAL_ONLY
- `docs/architecture/CODEBASE_DOCUMENTATION.md` — §3.2 ការផ្គូផ្គងម៉ូឌុលសេវាកម្មដែលបានបង្កប់
- `docs/architecture/ARCHITECTURE.md` — បរិបទកម្រិតប្រព័ន្ធ
- `docs/openapi.yaml` — និយមន័យ endpoint ដែលម៉ាស៊ីនអាចអានបាន
- `CLAUDE.md` §"ការបន្ថែមសេវាកម្មដែលបានបង្កប់ថ្មី" — បញ្ជីត្រួតពិនិត្យសម្រាប់យោងរហ័ស
