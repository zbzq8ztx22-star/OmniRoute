# Embedded Services (Igbo)

🌐 **Languages:** 🇺🇸 [English](../../../../frameworks/EMBEDDED-SERVICES.md) · 🇪🇹 [am](../../../am/docs/frameworks/EMBEDDED-SERVICES.md) · 🇸🇦 [ar](../../../ar/docs/frameworks/EMBEDDED-SERVICES.md) · 🇦🇿 [az](../../../az/docs/frameworks/EMBEDDED-SERVICES.md) · 🇧🇬 [bg](../../../bg/docs/frameworks/EMBEDDED-SERVICES.md) · 🇧🇩 [bn](../../../bn/docs/frameworks/EMBEDDED-SERVICES.md) · 🇨🇿 [cs](../../../cs/docs/frameworks/EMBEDDED-SERVICES.md) · 🇩🇰 [da](../../../da/docs/frameworks/EMBEDDED-SERVICES.md) · 🇩🇪 [de](../../../de/docs/frameworks/EMBEDDED-SERVICES.md) · 🇬🇷 [el](../../../el/docs/frameworks/EMBEDDED-SERVICES.md) · 🇪🇸 [es](../../../es/docs/frameworks/EMBEDDED-SERVICES.md) · 🇪🇪 [et](../../../et/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇷 [fa](../../../fa/docs/frameworks/EMBEDDED-SERVICES.md) · 🇫🇮 [fi](../../../fi/docs/frameworks/EMBEDDED-SERVICES.md) · 🇫🇷 [fr](../../../fr/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇪 [ga](../../../ga/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇳 [gu](../../../gu/docs/frameworks/EMBEDDED-SERVICES.md) · 🇳🇬 [ha](../../../ha/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇱 [he](../../../he/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇳 [hi](../../../hi/docs/frameworks/EMBEDDED-SERVICES.md) · 🇭🇷 [hr](../../../hr/docs/frameworks/EMBEDDED-SERVICES.md) · 🇭🇺 [hu](../../../hu/docs/frameworks/EMBEDDED-SERVICES.md) · 🇦🇲 [hy](../../../hy/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇩 [id](../../../id/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇹 [it](../../../it/docs/frameworks/EMBEDDED-SERVICES.md) · 🇯🇵 [ja](../../../ja/docs/frameworks/EMBEDDED-SERVICES.md) · 🇬🇪 [ka](../../../ka/docs/frameworks/EMBEDDED-SERVICES.md) · 🇰🇭 [km](../../../km/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇳 [kn](../../../kn/docs/frameworks/EMBEDDED-SERVICES.md) · 🇰🇷 [ko](../../../ko/docs/frameworks/EMBEDDED-SERVICES.md) · 🇱🇹 [lt](../../../lt/docs/frameworks/EMBEDDED-SERVICES.md) · 🇱🇻 [lv](../../../lv/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇳 [ml](../../../ml/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇳 [mr](../../../mr/docs/frameworks/EMBEDDED-SERVICES.md) · 🇲🇾 [ms](../../../ms/docs/frameworks/EMBEDDED-SERVICES.md) · 🇲🇹 [mt](../../../mt/docs/frameworks/EMBEDDED-SERVICES.md) · 🇲🇲 [my](../../../my/docs/frameworks/EMBEDDED-SERVICES.md) · 🇳🇵 [ne](../../../ne/docs/frameworks/EMBEDDED-SERVICES.md) · 🇳🇱 [nl](../../../nl/docs/frameworks/EMBEDDED-SERVICES.md) · 🇳🇴 [no](../../../no/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇳 [or](../../../or/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇳 [pa](../../../pa/docs/frameworks/EMBEDDED-SERVICES.md) · 🇵🇭 [phi](../../../phi/docs/frameworks/EMBEDDED-SERVICES.md) · 🇵🇱 [pl](../../../pl/docs/frameworks/EMBEDDED-SERVICES.md) · 🇵🇹 [pt](../../../pt/docs/frameworks/EMBEDDED-SERVICES.md) · 🇧🇷 [pt-BR](../../../pt-BR/docs/frameworks/EMBEDDED-SERVICES.md) · 🇷🇴 [ro](../../../ro/docs/frameworks/EMBEDDED-SERVICES.md) · 🇷🇺 [ru](../../../ru/docs/frameworks/EMBEDDED-SERVICES.md) · 🇱🇰 [si](../../../si/docs/frameworks/EMBEDDED-SERVICES.md) · 🇸🇰 [sk](../../../sk/docs/frameworks/EMBEDDED-SERVICES.md) · 🇸🇮 [sl](../../../sl/docs/frameworks/EMBEDDED-SERVICES.md) · 🇷🇸 [sr](../../../sr/docs/frameworks/EMBEDDED-SERVICES.md) · 🇸🇪 [sv](../../../sv/docs/frameworks/EMBEDDED-SERVICES.md) · 🇰🇪 [sw](../../../sw/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇳 [ta](../../../ta/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇳 [te](../../../te/docs/frameworks/EMBEDDED-SERVICES.md) · 🇹🇭 [th](../../../th/docs/frameworks/EMBEDDED-SERVICES.md) · 🇹🇷 [tr](../../../tr/docs/frameworks/EMBEDDED-SERVICES.md) · 🇺🇦 [uk-UA](../../../uk-UA/docs/frameworks/EMBEDDED-SERVICES.md) · 🇵🇰 [ur](../../../ur/docs/frameworks/EMBEDDED-SERVICES.md) · 🇺🇿 [uz](../../../uz/docs/frameworks/EMBEDDED-SERVICES.md) · 🇻🇳 [vi](../../../vi/docs/frameworks/EMBEDDED-SERVICES.md) · 🇳🇬 [yo](../../../yo/docs/frameworks/EMBEDDED-SERVICES.md) · 🇨🇳 [zh-CN](../../../zh-CN/docs/frameworks/EMBEDDED-SERVICES.md) · 🇹🇼 [zh-TW](../../../zh-TW/docs/frameworks/EMBEDDED-SERVICES.md)

---

> **Ụdị:** v3.8.44
> **Emelitere ikpeazụ:** 2026-09-09
> **Ndị e bu n’uche:** Ndị injinia na-agbakwụnye, na-elekọta, ma ọ bụ na-achọ ma na-edozi nsogbu dị na ọrụ agbakwunyere n’ime sistemụ (9Router, CLIProxyAPI, Mux, Bifrost, open-wa).

Ọrụ agbakwunyere n’ime sistemụ bụ ngwa enyemaka usoro arụnyere na kọmputa mpaghara nke OmniRoute na-arụnye, na-elekọta, ma na-eme ka ha bụrụ ebe ntụgharị okporo ụzọ zuru oke. N’adịghị ka ndị na-eweta ọrụ mpụga (nke a na-enweta site na ịntanetị
site na API keys), ọrụ agbakwunyere n’ime sistemụ na-arụ ọrụ n’otu kọmputa ahụ OmniRoute nọ na ya ma na-ekwurịta okwu site na loopback.

---

## Ndepụta Ọdịnaya

1. [Nchịkọta](#1-overview)
2. [Nhazi — oyi akwa 4](#2-architecture--4-layers)
3. [Igwe ọnọdụ usoro ndụ](#3-lifecycle-state-machine)
4. [Ntụaka API](#4-api-reference)
5. [Nchekwa](#5-security)
6. [Ịgbakwunye ọrụ agbakwunyere ọhụrụ](#6-adding-a-new-embedded-service)
7. [Nchọpụta na ndozi nsogbu](#7-troubleshooting)
8. [Ajụjụ ndị a na-ajụkarị](#8-faq)

---

## 1. Nchịkọta

### Gịnị mere e ji eji ọrụ agbakwunyere?

E tinyere ọrụ isii:

| Ọrụ             | ngwugwu npm                                        | Port ndabara | Ebumnuche                                                                                                                                                                                              |
| --------------- | -------------------------------------------------- | :----------: | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| **9Router**     | `9router`                                          |    20130     | Router AI nke OmniRoute nwere ike iji dị ka onye na-eweta ọrụ nta. A na-egosipụta model dị ka `9router/{sub}/{model}`                                                                                  |
| **CLIProxyAPI** | Faịlụ binary sitere na mbipụta GitHub (`cliproxy`) |     8317     | Ihe nkwụnye proxy mpaghara maka usoro nkwenye Anthropic CLI. Ọ na-enye ụzọ ndabere mgbe token OAuth kubie ume                                                                                          |
| **Mux**         | `mux` (`mux server` na-enweghị UI)                 |     8322     | Daemon mpaghara maka ịhazi agent (coder/mux). Naanị a na-achịkwa usoro ndụ ya — ọ bụghị ebe a na-eziga routing (enweghị proxy LLM).                                                                    |
| **Bifrost**     | `@maximhq/bifrost`                                 |     8080     | Backend relay nke AI gateway e ji Go wuo. Mgbe ọ na-arụ ọrụ, ụzọ relay (`/v1/relay/`) na-ahọrọ ya na-akpaghị aka                                                                                       |
| **Dario**       | `@askalf/dario`                                    |     3456     | Proxy ndenye aha Claude — nhọrọ ọzọ/ndabere maka CLIProxyAPI maka okporoụzọ e mere n'ụdị Claude Code; key etinyere na-aghọ `DARIO_ADMIN_TOKEN`, nke na-echekwa control plane OAuth ya dị na `/admin/*` |
| **open-wa**     | `@open-wa/wa-automate`                             |     8323     | Akpaaka WhatsApp Web (Chromium na-enweghị UI site na Puppeteer). Naanị a na-achịkwa usoro ndụ ya — ọ bụghị ebe a na-eziga routing.                                                                     |

Ọrụ isii niile na-agbaso otu usoro nlekọta a:

- OmniRoute na-etinye ha n'okpuru `DATA_DIR/services/{name}/` (e kewapụrụ ya na `package.json` nke OmniRoute n'onwe ya)
- OmniRoute na-amalite ma na-enyocha ha dị ka child process
- OmniRoute na-etinye API key nwa oge n'ime environment nke child ma na-agbanwe ya n'enweghị nkwụsị ọrụ (ebe ọ dabara)
- Ụzọ njikwa niile (`/api/services/*`) bụ **LOCAL_ONLY** — naanị loopback nwere ike ịnweta ha (iwu siri ike #17)

### Mkpebi ndị bụ isi (site na atụmatụ nhazi)

| Mkpebi                                   | Uru                                                                                             |
| ---------------------------------------- | ----------------------------------------------------------------------------------------------- |
| Nnweta dashboard na UI izizi nke 9Router | Reverse proxy na `/dashboard/providers/services/9router/embed/*`                                |
| Usoro ntinye                             | `npm install {package}` site na `execFile` (enweghị shell interpolation)                        |
| Ụdị ojiji                                | E debanyere provider dị ka `9router/{sub}/{model}` n'ime routing engine                         |
| Njikwa API key                           | OmniRoute na-emepụta ya, na-ezo ya mgbe echekwara ya (AES-256-GCM), ma na-etinye ya site na env |
| Ebe dashboard dị                         | `/dashboard/providers/services` (taabụ atọ)                                                     |
| Mmalite akpaaka                          | Toggle maka ọrụ ọ bụla, ndabara bụ OFF                                                          |

---

## 2. Nhazi — oyi akwa 4

```
┌────────────────────────────────────────────────────────────────────┐
│  Oyi akwa 1 — UI                                                   │
│  /dashboard/providers/services  (taabụ: CLIProxyAPI | 9Router | Mux)│
│  Ndekọ dị ndụ (SSE), Bido/Kwụsị/Malitegharị/Melite, Ntọala, Wụnye │
│                                                                    │
│  src/app/(dashboard)/dashboard/providers/services/                 │
│    ├── page.tsx               Shell + iji ?tab= eduzi taabụ        │
│    ├── tabs/                  CliproxyServiceTab, NinerouterServiceTab,│
│    │                          MuxServiceTab                        │
│    └── components/            ServiceStatusCard, ServiceLifecycleButtons,│
│                               ServiceLogsPanel, ApiKeyCard, ...    │
└──────────────────────┬─────────────────────────────────────────────┘
                       │ HTTP (Next.js fetch)
┌──────────────────────▼─────────────────────────────────────────────┐
│  Oyi akwa 2 — API (LOCAL_ONLY — naanị loopback)                    │
│                                                                    │
│  /api/services/9router/{install|start|stop|restart|update|         │
│                          rotate-key|status|auto-start|logs}        │
│  /api/services/cliproxy/{install|start|stop|restart|update|        │
│                           status|auto-start|logs}                  │
│  /api/services/mux/{install|start|stop|restart|update|             │
│                      status|auto-start|logs}                       │
│  /dashboard/providers/services/9router/embed/[...path]             │
│    (reverse HTTP + WebSocket proxy → 9Router upstream)             │
│                                                                    │
│  Ọnụ ụzọ: LOCAL_ONLY_API_PREFIXES gụnyere "/api/services/" na      │
│        "/dashboard/providers/services/*/embed/"                    │
└──────────────────────┬─────────────────────────────────────────────┘
                       │ oku n'ime process
┌──────────────────────▼─────────────────────────────────────────────┐
│  Oyi akwa 3 — ServiceSupervisor (src/lib/services/)                │
│                                                                    │
│  ServiceSupervisor.ts   Onye nlekọta izugbe (child_process.spawn)  │
│    ├── wụnye:      execFile('npm', ['install', pkg, '--prefix'])   │
│    ├── bido:       spawn(node, [entrypoint], {env, cwd})           │
│    ├── api_key:    crypto.randomBytes(32) → env NINEROUTER_API_KEY │
│    ├── ọdụ ụgbọ:   20130 maka 9Router (enwere ike ịhazi ya)        │
│    ├── ndekọ:      stdio ring buffer 5 MB → mmemme SSE             │
│    ├── ahụike:     HTTP GET /health kwa s 2–5, mgbake mgbe achọrọ │
│    └── usoro ndụ:  SIGTERM s 15 → SIGKILL                          │
│                                                                    │
│  registry.ts        getSupervisor(name) / registerSupervisor()     │
│  bootstrap.ts       Na-ebido SERVICES[] niile mgbe process bidoro │
│  apiKey.ts          getOrCreateApiKey(), generateServiceApiKey()   │
│  modelSync.ts       GET /v1/models oge niile → tebụl service_models│
│  ringBuffer.ts      Ebe nchekwa ndekọ okirikiri (5 MB kwa ọrụ)     │
│  healthCheck.ts     Nnyocha ahụike HTTP a na-eme ugboro ugboro     │
│  installers/        ninerouter.ts, cliproxy.ts, mux.ts, openwa.ts  │
│                      (ihe nkwụnye installer)                       │
└──────────────────────┬─────────────────────────────────────────────┘
                       │ HTTP dakọtara na OpenAI (loopback)
┌──────────────────────▼─────────────────────────────────────────────┐
│  Oyi akwa 4 — Provider / Nduzi ụzọ                                │
│                                                                    │
│  open-sse/executors/ninerouter.ts                                  │
│    Ọ na-achọgharị ọdụ ụgbọ na API key maka arịrịọ ọ bụla           │
│    (enweghị caching).                                              │
│    Ọ na-ewepụ prefix "9router/" na model id tupu proxying.         │
│    Ọ na-eweghachi 503 service_not_running ma ọ bụrụ na onye        │
│    nlekọta anọghị na "running".                                    │
│                                                                    │
│  src/shared/constants/providers.ts                                 │
│    Ndenye maka "9router": isEmbeddedService: true                  │
│                                                                    │
│  open-sse/config/providerRegistry.ts                               │
│    A na-echekwa models dịka "9router/{sub}/{model}" (nwere prefix).│
│    modelSync.ts na-emekọrịta ha kwa nkeji 5.                       │
│                                                                    │
│  A na-achịkwa NANI usoro ndụ Mux (Oyi akwa 1-3) — ọ bụ daemon      │
│  nhazi agent, ọ bụghị LLM proxy, ya mere o nweghị ndenye executor/ │
│  provider nke Oyi akwa 4, ọ dịghịkwa mgbe ọ bụ ebumnuche nduzi ụzọ.│
└────────────────────────────────────────────────────────────────────┘
```

### Faịlụ isi mmalite ndị bụ isi

| Faịlụ                                       | Ọrụ                                                     |
| ------------------------------------------- | ------------------------------------------------------- |
| `src/lib/services/ServiceSupervisor.ts`     | Klas isi: usoro ndụ, mkpọchi, ahụike, ring buffer       |
| `src/lib/services/bootstrap.ts`             | Ndebanye aha n'ogo process na mmalite akpaaka           |
| `src/lib/services/registry.ts`              | Maapụ singleton `tool → supervisor`                     |
| `src/lib/services/apiKey.ts`                | Mmepụta igodo, izochi AES-256-GCM mgbe echekwara        |
| `src/lib/services/modelSync.ts`             | Mmekọrịta model kwa oge (nkeji 5) + mgbe achọrọ         |
| `src/lib/services/ringBuffer.ts`            | Ring buffer ndekọ okirikiri nke 5 MB nwere ndebanye SSE |
| `src/lib/services/healthCheck.ts`           | Nnwale ahụike HTTP (nkeji oge a pụrụ ịhazi)             |
| `src/lib/services/installers/ninerouter.ts` | npm install/update/uninstall maka 9Router               |
| `src/lib/services/installers/cliproxy.ts`   | npm install/update/uninstall maka CLIProxyAPI           |
| `src/lib/services/installers/mux.ts`        | npm install/update/uninstall maka Mux                   |
| `src/lib/services/installers/openwa.ts`     | npm install/update/uninstall maka open-wa               |
| `src/app/api/services/9router/_lib.ts`      | Ihe enyemaka `getOrInitSupervisor()`                    |
| `src/app/api/services/[name]/logs/route.ts` | Ebe njedebe ndekọ SSE a na-ekekọrịta                    |
| `open-sse/executors/ninerouter.ts`          | Onye mmejuputa provider (Layer 4)                       |

---

## 3. Igwe ọnọdụ okirikiri ndụ

```
                    install()
  ┌─────────────┐ ──────────► ┌─────────────┐
  │ not_installed│             │   stopped   │◄──────────────────┐
  └─────────────┘             └──────┬──────┘                   │
                                     │ start()                   │
                                     ▼                           │ stop()
                               ┌──────────┐                      │
                               │ starting │                      │
                               └────┬─────┘                     │
             nyocha ahụike dị mma   │         ndakpọ / SIGTERM  │
                               ┌────▼─────┐  (ọpụpụ n'ime 5s)   │
                               │ running  │──── ndakpọ ─────────►┤
                               └────┬─────┘                   ┌─▼────┐
                             stop() │                          │error │
                                    ▼                          └──────┘
                               ┌──────────┐
                               │ stopping │
                               └──────────┘
```

A na-echekwa ọnọdụ na tebụl DB `version_manager` (kọlụm `status`) ma na-emepụtakwa ha
n'ọnọdụ ebe nchekwa `ServiceSupervisor`. Ọnọdụ ebe nchekwa bụ nke a ga-adabere na ya maka
usoro na-arụ ọrụ; ọnọdụ DB bụ ndabere na-adịgide adịgide mgbe a na-ebido sistemụ.

### Mgbanwe ọnọdụ

| Site na         | Ihe omume                            | Gaa na                   |
| --------------- | ------------------------------------ | ------------------------ |
| `not_installed` | `install()` gara nke ọma             | `stopped`                |
| `stopped`       | akpọrọ `start()`                     | `starting`               |
| `starting`      | nyocha ahụike weghachiri 200         | `running`                |
| `starting`      | usoro kwụsịrị tupu ọ dị mma          | `error`                  |
| `running`       | akpọrọ `stop()`                      | `stopping` → `stopped`   |
| `running`       | usoro kwụsịrị na-atụghị anya (< 5 s) | `error` (ndakpọ ọsọ ọsọ) |
| `running`       | usoro kwụsịrị na-atụghị anya (> 5 s) | `error`                  |
| `error`         | akpọrọ `start()`                     | `starting`               |
| nke ọ bụla      | `stop()` mgbe ọ nọ na `stopping`     | enweghị ihe a ga-eme     |

### Mkpọchi ọrụ

`ServiceSupervisor` na-ahazi ọrụ okirikiri ndụ n'otu n'otu site na mkpọchi ọrụ async
(`withLock()`). Ọkpụkpọ `start()` ndị na-eme n'otu oge n'otu supervisor na-ebute naanị
otu mmalite usoro; onye kpọrọ nke abụọ na-echere ma weghachite ọnọdụ dị adị. Nke a na-egbochi
ọnọdụ mpi mgbe, dịka ọmụmaatụ, mmalite-akpaka na bọtịnụ UI malitere n'otu oge.

---

## 4. Ntụaka API

Ụzọ niile dị n'okpuru `/api/services/` bụ **LOCAL_ONLY** (naanị loopback, iwu siri ike #17).
Arịrịọ ndị na-abụghị loopback na-enweta `403 LOCAL_ONLY` n'agbanyeghị token nyocha njirimara.

### 4.1 Ebe njedebe 9Router (ụzọ 11)

#### `POST /api/services/9router/install`

Wụnye 9Router site na npm. Ọ na-emepụta `DATA_DIR/services/9router/` nwere
`package.json` na `node_modules/` nke ya. Ọ naghị emegide deps nke OmniRoute n'onwe ya.

**Ahụ arịrịọ** (ha niile bụ nke nhọrọ):

```json
{ "version": "latest" }
```

| Mpaghara  | Ụdị      | Ndabara    | Nkọwa                                     |
| --------- | -------- | ---------- | ----------------------------------------- |
| `version` | `string` | `"latest"` | mkpado ụdị npm ma ọ bụ semver a ga-etinye |

**Nzaghachi:**

| Ọnọdụ | Nkọwa                                                     |
| ----- | --------------------------------------------------------- |
| `200` | `{ ok: true, installedVersion: "x.y.z", path: "..." }`    |
| `400` | Ahụ arịrịọ ezighi ezi (ọdịda nkwado Zod)                  |
| `409` | Nwụnye amalitela (ejidere mkpọchi)                        |
| `500` | Nwụnye npm dara — lee `message` maka njehie dị mfe nghọta |

**Ihe edeturu:** Ọ na-eji `execFile('npm', [...])` — enweghị shell, enweghị interpolation (iwu siri ike #13).
A na-egosipụta njehie EACCES dị ka ozi dị mfe nghọta.

---

#### `POST /api/services/9router/start`

Malite 9Router. Ọ na-edebanye supervisor ma ọ bụrụ na edebanyebeghị ya, wee kpọọ
`supervisor.start()`. Ọ bụ idempotent mgbe ọ na-arụ ọrụ ugbua.

**Ahụ arịrịọ:** ọ dịghị

**Nzaghachi:**

| Ọnọdụ | Nkọwa                                            |
| ----- | ------------------------------------------------ |
| `200` | Ọbjektị `ServiceStatus` (lee schema dị n'okpuru) |
| `409` | Awụnyeghị 9Router (`status: "not_installed"`)    |
| `503` | Mmalite dara (njehie process — lee `lastError`)  |

**Schema ServiceStatus:**

```json
{
  "tool": "9router",
  "state": "running",
  "pid": 12345,
  "port": 20130,
  "health": "healthy",
  "startedAt": "2026-05-25T10:00:00.000Z",
  "lastError": null
}
```

---

#### `POST /api/services/9router/stop`

Kwụsị 9Router n'ụzọ dị mma. Ọ na-eziga SIGTERM, chere 15 s, wee ziga SIGKILL ma ọ bụrụ na ọ ka na-arụ ọrụ.
Ọ bụ idempotent mgbe ọ kwụsịrị ugbua.

**Ahụ arịrịọ:** ọ dịghị

**Nzaghachi:**

| Ọnọdụ | Nkọwa                              |
| ----- | ---------------------------------- |
| `200` | `ServiceStatus` (state: "stopped") |
| `503` | Nkwụsị dara na-atụghị anya ya      |

---

#### `POST /api/services/9router/restart`

Ọ bụ otu ihe na `stop()` e mesịa `start()` n'okpuru mkpọchi ọrụ ahụ.

**Ahụ arịrịọ:** ọ dịghị

**Nzaghachi:** dị ka nke `start` (ọ na-eweghachi `ServiceStatus` ikpeazụ).

---

#### `POST /api/services/9router/update`

Ọ na-emelite 9Router gaa na ụdị npm ọhụrụ. Ọ bụrụ na ọrụ ahụ na-arụ ọrụ, a ga-ebu ụzọ
kwụsị ya, mee npm install (na-etinye ụdị ọhụrụ ahụ n'otu ebe), ma malitegharị
ọrụ ahụ.

**Ahụ arịrịọ** (ha niile bụ nke nhọrọ):

```json
{ "version": "latest" }
```

**Nzaghachi:**

| Ọnọdụ | Nkọwa                                                           |
| ----- | --------------------------------------------------------------- |
| `200` | `{ ok: true, previousVersion: "...", installedVersion: "..." }` |
| `400` | Ahụ ezighi ezi                                                  |
| `500` | Mmelite npm dara                                                |

---

#### `POST /api/services/9router/rotate-key`

Ọ na-emepụta API key ọhụrụ maka 9Router, zoo ya mgbe echekwara ya, ma malitegharị ọrụ ahụ
(ma ọ bụrụ na ọ na-arụ ọrụ) ka o wee nweta key ọhụrụ ahụ site na environment ya. A na-eme ka key ochie
ghara ịdị irè ozugbo.

**Ahụ arịrịọ:** ọ dịghị

**Nzaghachi:**

| Ọnọdụ | Nkọwa                                      |
| ----- | ------------------------------------------ |
| `200` | `{ keyRotated: true, restarted: boolean }` |
| `500` | Ntughari dara                              |

**Nchekwa:** A naghị eweghachi key ọhụrụ ahụ na nzaghachi (enweghị ntapu credential).
A na-echekwa ya n'ụdị ezoro ezo (AES-256-GCM) na tebụl `version_manager`.

---

#### `GET /api/services/9router/status`

Ọ na-eweghachi ọnọdụ live + DB ejikọtara ọnụ, gụnyere metadata ụdị na nlele API key.

**Nzaghachi:**

| Ọnọdụ | Nkọwa                  |
| ----- | ---------------------- |
| `200` | Lee schema dị n'okpuru |
| `500` | Ọgụgụ ọnọdụ dara       |

**Schema nzaghachi:**

```json
{
  "tool": "9router",
  "state": "running",
  "pid": 12345,
  "port": 20130,
  "health": "healthy",
  "startedAt": "2026-05-25T10:00:00.000Z",
  "lastError": null,
  "installedVersion": "1.2.3",
  "latestVersion": "1.2.4",
  "updateAvailable": true,
  "apiKeyMasked": "nr_****abcd",
  "autoStart": false,
  "providerExpose": false
}
```

---

#### `POST /api/services/9router/auto-start`

Gbanye ma ọ bụ gbanyụọ ọkọlọtọ auto-start. Mgbe `enabled: true`, ọrụ ahụ na-amalite na-akpaghị aka
oge ọzọ OmniRoute malitere (ma ọ bụrụ na awụnyela ọrụ ahụ).

**Ahụ arịrịọ:**

```json
{ "enabled": true }
```

**Nzaghachi:**

| Ọnọdụ | Nkọwa                 |
| ----- | --------------------- |
| `200` | `{ autoStart: true }` |
| `400` | Ahụ ezighi ezi        |

---

#### `GET /api/services/9router/logs`

SSE stream nke log live sitere na stdout/stderr ring buffer nke 9Router.

**Paramita query:**

| Paramita | Ụdị       | Ndabara | Nkọwa                                                                                             |
| -------- | --------- | ------- | ------------------------------------------------------------------------------------------------- |
| `tail`   | `integer` | 200     | Ọnụọgụ ahịrị akụkọ gara aga a ga-ebu ụzọ zipụ (kachasị 1000)                                      |
| `filter` | `string`  | ọ dịghị | Nzacha substring na-adịghị ele nnukwu/obere mkpụrụedemede (enweghị regex — echedoro megide ReDoS) |

**Ihe omume SSE:**

| Ihe omume   | Data        | Nkọwa               |
| ----------- | ----------- | ------------------- |
| `snapshot`  | `LogLine[]` | Akụkọ tail mbụ      |
| `log`       | `LogLine`   | Ahịrị log live      |
| `heartbeat` | `{}`        | Keep-alive kwa 15 s |

**Schema LogLine:**

```json
{
  "ts": 1716633600000,
  "stream": "stdout",
  "line": "[9router] Listening on :20130"
}
```

**Nzaghachi:**

| Ọnọdụ | Nkọwa                                                    |
| ----- | -------------------------------------------------------- |
| `200` | `text/event-stream`                                      |
| `400` | Paramita `filter` dị ogologo karịa (> mkpụrụedemede 200) |
| `404` | Ahụghị ọrụ ahụ (edebanyeghị supervisor ahụ n'akwụkwọ)    |

---

### 4.2 Ebe njedebe CLIProxyAPI (ụzọ 10)

CLIProxyAPI nwere otu nhazi ebe njedebe ahụ 9Router nwere ma e wepụ `rotate-key`, tinyekwara
`accounts`, `provider-expose` na `auto-restart-adopted`. Ugbu a, ọ na-anata
igodo API data-plane pụrụ iche nke a na-etinye mgbe a na-ebido ya (`needsApiKey: true` n'ime
`bootstrap.ts`, nke a na-eji maka mmekọrịta model); `status` nwere field ole na ole.

| Usoro  | Ụzọ                                 | Nkọwa                                            |
| ------ | ----------------------------------- | ------------------------------------------------ |
| `POST` | `/api/services/cliproxy/install`    | Wụnye CLIProxyAPI site na npm                    |
| `POST` | `/api/services/cliproxy/start`      | Bido CLIProxyAPI                                 |
| `POST` | `/api/services/cliproxy/stop`       | Kwụsị CLIProxyAPI                                |
| `POST` | `/api/services/cliproxy/restart`    | Malitegharịa CLIProxyAPI                         |
| `POST` | `/api/services/cliproxy/update`     | Melite gaa na ụdị ọhụrụ                          |
| `GET`  | `/api/services/cliproxy/status`     | Ọnọdụ ozugbo + ọnọdụ DB (enweghị `apiKeyMasked`) |
| `POST` | `/api/services/cliproxy/auto-start` | Gbanyụọ ma ọ bụ gbanye mbido akpaka              |

Ebe njedebe `GET /api/services/{name}/logs` a na-ekekọrịta (lee §4.1) na-arụ ọrụ maka
ọrụ anọ niile site n'iji akụkụ dynamic `[name]`.

---

### 4.3 Ebe njedebe Mux (ụzọ 8)

Mux nwere otu nhazi ebe njedebe ahụ CLIProxyAPI nwere — enweghị ụzọ `rotate-key` n'elu API
(a na-emepụta bearer token n'otu ụzọ ahụ a na-emepụta nke 9Router site na
`getOrCreateApiKey("mux")`, a na-etinyekwa ya site na env var `MUX_SERVER_AUTH_TOKEN`, mana
enwebeghị ebe njedebe pụrụ iche maka ịtụgharị ya). Naanị lifecycle ka a na-ejikwa maka Mux: n'adịghị ka
9Router, o nweghị executor Layer 4, a naghị edebanyekwa ya dịka provider routing.

| Usoro  | Ụzọ                            | Nkọwa                               |
| ------ | ------------------------------ | ----------------------------------- |
| `POST` | `/api/services/mux/install`    | Wụnye Mux site na npm (`npm i mux`) |
| `POST` | `/api/services/mux/start`      | Bido Mux (`mux server`)             |
| `POST` | `/api/services/mux/stop`       | Kwụsị Mux                           |
| `POST` | `/api/services/mux/restart`    | Malitegharịa Mux                    |
| `POST` | `/api/services/mux/update`     | Melite gaa na ụdị npm ọhụrụ         |
| `GET`  | `/api/services/mux/status`     | Ọnọdụ ozugbo + ọnọdụ DB             |
| `POST` | `/api/services/mux/auto-start` | Gbanyụọ ma ọ bụ gbanye mbido akpaka |

---

### 4.4 Ebe njedebe Bifrost (ụzọ 8)

Bifrost bụ backend relay nke ọnụ ụzọ AI e ji Go rụọ (`@maximhq/bifrost`). Ọ na-eji otu
nhazi ebe njedebe ahụ CLIProxyAPI nwere (enweghị `rotate-key` — Bifrost na-ejikwa igodo
provider nke ya n'ime `config.json` n'okpuru `-app-dir` ya).

| Usoro  | Ụzọ                                | Nkọwa                                                          |
| ------ | ---------------------------------- | -------------------------------------------------------------- |
| `POST` | `/api/services/bifrost/install`    | Wụnye Bifrost site na npm (`@maximhq/bifrost`)                 |
| `POST` | `/api/services/bifrost/start`      | Bido Bifrost na port 8080 (ndabara)                            |
| `POST` | `/api/services/bifrost/stop`       | Kwụsị Bifrost                                                  |
| `POST` | `/api/services/bifrost/restart`    | Malitegharịa Bifrost                                           |
| `POST` | `/api/services/bifrost/update`     | Melite gaa na ụdị ọhụrụ                                        |
| `GET`  | `/api/services/bifrost/status`     | Ọnọdụ ozugbo + ọnọdụ DB                                        |
| `POST` | `/api/services/bifrost/auto-start` | Gbanyụọ ma ọ bụ gbanye mbido akpaka                            |
| `GET`  | `/api/services/bifrost/logs`       | Ọdụ log SSE (site na ụzọ dynamic `[name]/logs` a na-ekekọrịta) |

**Njikọ routing:** Mgbe edobeghị `BIFROST_BASE_URL` ma instance Bifrost a na-elekọta
na-arụ ọrụ, `getBifrostRoutingConfig()` (n'ime `routingBackend.ts`) na-eji
`http://127.0.0.1:{port}` akpaghị aka dịka URL ntọala relay. Env
`BIFROST_BASE_URL` e depụtara hoo haa na-ebute ụzọ mgbe niile.

---

### 4.5 Ebe njedebe Dario (ụzọ 12)

Otu nhazi lifecycle ahụ ọrụ ndị ọzọ nwere (`install`, `start`, `stop`, `restart`,
`update`, `status`, `auto-start`, `auto-restart-adopted`) tinyere control plane OAuth
n'okpuru `admin/` nke token na-echekwa: `admin/accounts`, `admin/import-from-omniroute`,
`admin/login-start`, `admin/login-complete` (ha niile dị n'azụ `DARIO_ADMIN_TOKEN`).

### 4.6 Ebe njedebe open-wa (ụzọ 7)

open-wa (`@open-wa/wa-automate`) na-achị instance Chromium na-enweghị isi (site na
Puppeteer) iji mee WhatsApp Web akpaghị aka. Ọ na-eji otu nhazi ebe njedebe ahụ Mux nwere (enweghị
ụzọ `rotate-key` ugbu a). Naanị lifecycle ka a na-ejikwa maka ya — ọ bụghị ebe routing na-aga,
enweghị executor Layer 4/ntinye provider.

| Usoro  | Ụzọ                               | Nkọwa                                                          |
| ------ | --------------------------------- | -------------------------------------------------------------- |
| `POST` | `/api/services/openwa/install`    | Wụnye open-wa site na npm (`@open-wa/wa-automate`)             |
| `POST` | `/api/services/openwa/start`      | Malite open-wa na port 8323 (nke ndabara)                      |
| `POST` | `/api/services/openwa/stop`       | Kwụsị open-wa                                                  |
| `POST` | `/api/services/openwa/restart`    | Malitegharịa open-wa                                           |
| `POST` | `/api/services/openwa/update`     | Melite gaa na ụdị ọhụrụ                                        |
| `GET`  | `/api/services/openwa/status`     | Ọnọdụ ozugbo + DB                                              |
| `POST` | `/api/services/openwa/auto-start` | Gbanye ma ọ bụ gbanyụọ mmalite-akpaka                          |
| `GET`  | `/api/services/openwa/logs`       | Ọgwụgwụ ndekọ SSE (site n'ụzọ mgbanwe `[name]/logs` nkekọrịta) |

**Igodo API:** a na-etinye ya dị ka `WA_KEY` — ngbanwe env izugbe nke open-wa
nwere nganiihu `WA_*` na-etinye ya na nhọrọ CLI `--key`/`-k`
(`dist/cli/setup.js::envArgs()`, nke e nyochara megide ngwugwu 4.76.0
arụnyere). A na-etinye nganiihu `ow_` mgbe `generateServiceApiKey()` mepụtara ya. open-wa
na-agụghachi igodo ahụ site na nkụnyeisi HTTP `key`/`api_key` (ọ bụghị `Authorization:
Bearer`); ewepụrụ `/api-docs*` n'ụzọ doro anya na nyocha ahụ
(`setupAuthenticationLayer` n'ime `dist/cli/server.js`), ya mere nyocha ahụike
achọghị nkụnyeisi nkwenye.

**Njikọ:** open-wa abụghị nke gọọmentị ma ọ nweghị njikọ na WhatsApp — nọmba
ejikọrọ nwere ihe ize ndụ nke WhatsApp igbochi ya site na nchọpụta akpaaka nke ha.
Na mmalite mbụ, a na-ebipụta koodu QR njikọ na stdout ma gosipụta ya site na
panel Ndekọ/SSE stream dị ugbu a — enweghị endpoint pụrụ iche maka onyonyo QR
na njikọ a ugbu a.

---

### 4.7 Reverse proxy (ntinye dashboard 9Router)

Dashboard ahụ na-etinye UI weebụ 9Router n'ime iframe site na reverse
proxy dị n'ime na:

```
GET|POST|... /dashboard/providers/services/9router/embed/[...path]
```

Proxy a:

- Na-ebuga arịrịọ ahụ na `http://127.0.0.1:{port}/{path}` (naanị loopback)
- Na-ewepụ nkụnyeisi `cookie` na `authorization` ndị na-abata (ọ dịghị ntapu nke session OmniRoute)
- Na-etinye `Authorization: Bearer {apiKey}` maka nkwenye 9Router
- Na-ewepụ `set-cookie`, `content-security-policy`, `x-frame-options`, `cross-origin-*` na nzaghachi
- Na-edegharị nzaghachi HTML iji tinye `<base href>` ma hazie ụzọ zuru oke ka ha bụrụ otu (`/foo` → `/dashboard/.../embed/foo`)

Companion server dị na port pụrụ iche na-ahụ maka nkwalite WebSocket maka dashboard
etinyere (lee `src/lib/services/embedWsProxy.ts`).

**Nchekwa:** A na-ekewa ụzọ embed proxy n'okpuru `LOCAL_ONLY_API_PREFIXES`
ma enwere ike iru ha naanị site na loopback. Onye mwakpo nwetara JWT site na
tunnel Cloudflare/Ngrok enweghị ike iji proxy banye na ọrụ ndị etinyere.

---

## 5. Nchekwa

### Mmanye LOCAL_ONLY (iwu siri ike #17)

A na-ekewa ụzọ niile dị n'okpuru `/api/services/` na `/dashboard/providers/services/*/embed/`
dị ka LOCAL_ONLY n'ime `src/server/authz/routeGuard.ts`. Nlele loopback
na-agba ọsọ mgbe niile tupu ngalaba nyocha njirimara ọ bụla:

```
arịrịọ bịarutere
  → isLocalOnlyPath(path)?
      → abụghị loopback → 403 LOCAL_ONLY (mgbe niile, tupu nlele njirimara)
      → loopback        → gaa n'ihu na nyocha njirimara nkịtị
```

Nke a na-egbochi JWT agbapụtara (dịka ọmụmaatụ, site na tunnel) ịkpalite `npm install` ma ọ bụ
ịmalite process. Lee `docs/security/ROUTE_GUARD_TIERS.md` maka matriks tier
zuru ezu.

### Itinye API key

9Router na Mux chọrọ API key/bearer token maka endpoint HTTP nke ha.
OmniRoute:

1. Na-emepụta key site na `crypto.randomBytes(32).toString("base64url")` nke nwere
   prefix pụrụ iche maka service (`nr_` maka 9Router, `mx_` maka Mux).
2. Na-eji AES-256-GCM ezoro ya ezo mgbe echekwara ya (otu cipher ahụ a na-eji maka credentials provider).
3. Na-ewepụ nzuzo ya ma tinye ya dịka environment variable n'oge spawn —
   `NINEROUTER_API_KEY` maka 9Router, `MUX_SERVER_AUTH_TOKEN` maka Mux (ọ bụghị CLI
   flag ma ọlị, ka token ahụ ghara ịpụta na `ps`/ndepụta process).
4. Ọ dịghị mgbe ọ na-eweghachi key plaintext na nzaghachi HTTP ọ bụla.

CLIProxyAPI na-enweta key data-plane raara onwe ya nye nke a na-etinye n'oge spawn
(`needsApiKey: true` — a na-eji ya maka mmekọrịta model sync na adapter).

### Nchedo SSRF

E debere reverse HTTP proxy (`/dashboard/.../embed/[...path]`) ka o zipụ
naanị na `http://127.0.0.1:{port}`. Ọ dịghị mgbe ọ na-eso redirects gaa ebe
na-abụghị loopback. A na-eji library `ssrf-req-filter` ajụ URL upstream ọ bụla nke
na-resolve n'èzí loopback range.

### Nchekwa shell (iwu siri ike #13)

A na-akpọ `npm install` site na `execFile('npm', ['install', pkg, '--prefix', dir])` —
enweghị template literals, enweghị shell, enweghịkwa itinye external paths n'ime command
string site na interpolation. A na-ebufe runtime values (ports, API keys) site na object `env` nke child ahụ.

### Nhicha error (iwu siri ike #12)

Nzaghachi error niile sitere na `/api/services/*` na-agafe na `buildErrorBody()` ma ọ bụ
`sanitizeErrorMessage()`. Ọ dịghị mgbe a na-eweghachi raw `err.stack` na `err.message`
kpọmkwem nye onye kpọrọ ya.

---

## 6. Ịgbakwunye embedded service ọhụrụ

Soro usoro 8 ndị a. Gụọ implementations ndị dị ugbu a na `src/lib/services/installers/`
na `src/app/api/services/` dịka ntụaka ikike.

### Nzọụkwụ 1 — Mepụta installer

Mepụta `src/lib/services/installers/{name}.ts` nke e mere n'ụdị `ninerouter.ts`:

```typescript
export const NAME_PACKAGE = "your-npm-package";
export const NAME_DEFAULT_PORT = 20132; // họrọ port na-adịghị eji

export async function install(version = "latest"): Promise<InstallResult> { ... }
export async function update(version = "latest"): Promise<InstallResult> { ... }
export async function uninstall(): Promise<void> { ... }
export function resolveSpawnArgs(apiKey: string, port: number): SpawnArgs { ... }
export async function getInstalledVersion(): Promise<string | null> { ... }
export async function getLatestVersion(): Promise<string | null> { ... }
```

Jiri `runNpm(['install', NAME_PACKAGE, '--prefix', dir])` sitere na `installers/utils.ts`
— ejila `execSync` ma ọ bụ shell interpolation ma ọlị.

### Nzọụkwụ 2 — Debanye aha na bootstrap

Tinye `ServiceEntry` na array `SERVICES` dị na `src/lib/services/bootstrap.ts`:

```typescript
{
  tool: "myservice",
  port: NAME_DEFAULT_PORT,
  healthPath: "/health",
  healthIntervalMs: 5_000,
  stopTimeoutMs: 15_000,
  logsBufferBytes: 5_242_880,
  needsApiKey: true, // false ma ọ bụrụ na API key adịghị mkpa
}
```

Gbasaa `buildSpawnArgsFactory()` ka ọ nwee ike ijikwa `cfg.tool === "myservice"`.

#### Nkwekọrịta provider-plugin enwere ike itinye dochie ọzọ (Phase 1, #7333)

`src/lib/services/providerPlugins/` na-ewebata nkwekọrịta `ServiceProviderPlugin` nke
na-achịkọta fields `ServiceEntry` nke `bootstrap.ts` nke backend na fields manifest-template
nke `serviceBackends.ts` n'ime otu object, kama ịkọwa ọdịdị otu backend ahụ
iche iche n'ime faịlụ abụọ na-enweghị njikọ. Ka ọ dị n'oge a na-ede ihe a, **naanị `9router` ka
ebufere** — `bootstrap.ts` na-enweta entry `SERVICES[]` ya site na
`getServiceProviderPlugin("9router")` (`src/lib/services/providerPlugins/registry.ts`),
ma na-atụpụ startup error ma ọ bụrụ na plugin ahụ efuo n'oge ọ bụla. `cliproxy`, `mux`, na `bifrost`
ka na-eji inline literals `SERVICES[]` dịbu adị na-enweghị mgbanwe.

`open-sse/config/providerPluginManifest.ts` nwetakwara helper mgbakwunye
`createServiceBackendManifestEntry(pluginId, template)` nke na-ewulite
`ProviderPluginManifestEntry` ahaziri nke ọma site na entry `SERVICE_BACKEND_MANIFEST_TEMPLATE` — e
**jikọbeghị** ya na live request path ọ bụla ugbu a (ma `generateProviderPluginManifestFromRegistry()`
ma `/v1/providers/[provider]/models`); nke ahụ ka bụ ọrụ ga-esochi mgbe egosiri na
nkwekọrịta ahụ na-arụ ọrụ maka backend nke abụọ.

E yigharịrị ihe ndị a gaa na PR ndị ga-esochi, nke a na-enyocha n'okpuru issue #7333: ibufe `cliproxyapi` site na
otu registry ahụ, ime ka `mux`/`bifrost` bụrụ akụkụ izugbe nke union `ServiceBackendPluginId`,
itinye special-casing nke executor-routing (`open-sse/executors/index.ts`,
`open-sse/handlers/chatCore/executorProxy.ts`) n'ime nkwekọrịta plugin, na ijikọ
`createServiceBackendManifestEntry()` na live manifest/models code path.

### Nzọụkwụ 3 — Tinye migration na DB seed

Gbaa mbọ hụ na service ahụ nwere row na `version_manager` site na migration dị na
`src/lib/db/migrations/`. Row ahụ kwesịrị inwe:

```sql
INSERT OR IGNORE INTO version_manager (tool, status, auto_start, provider_expose)
VALUES ('myservice', 'not_installed', 0, 0);
```

### Nzọụkwụ 4 — Mepụta API endpoints 7 ahụ

N'okpuru `src/app/api/services/{name}/`:

```
_lib.ts            helper getOrInitSupervisor()
install/route.ts   POST — na-akpọ installer.install()
start/route.ts     POST — na-akpọ supervisor.start()
stop/route.ts      POST — na-akpọ supervisor.stop()
restart/route.ts   POST — na-akpọ supervisor.restart()
update/route.ts    POST — na-akpọ installer.update()
status/route.ts    GET  — na-ejikọta status live + DB
auto-start/route.ts POST — na-agbanye ma ọ bụ gbanyụọ flag auto_start
```

Ụzọ `GET /api/services/[name]/logs` a na-ekekọrịta ejikọtalarị — enweghị mgbanwe
dị mkpa n’ebe ahụ.

Nyefee nzaghachi njehie niile site na `createErrorResponse()` / `buildErrorBody()`.

### Nzọụkwụ 5 — Tinye na LOCAL_ONLY_API_PREFIXES

N’ime `src/server/authz/routeGuard.ts`, chọpụta na edepụtalarị `/api/services/`.
Ọ bụrụ na i webata prefix ọhụrụ (dịka, `/api/tools/`), tinye ya na
`LOCAL_ONLY_API_PREFIXES` nakwa, ọ bụrụ na ọ na-amalite process, na `SPAWN_CAPABLE_PREFIXES`.
Tinye ule na `tests/unit/authz/routeGuard.test.ts`.

### Nzọụkwụ 6 — Tinye taabụ UI

Mepụta `src/app/(dashboard)/dashboard/providers/services/tabs/{Name}ServiceTab.tsx`.
Jirikwa components ndị a na-ekekọrịta:

- `ServiceStatusCard` — ọnọdụ ozugbo + baajị ahụike
- `ServiceLifecycleButtons` — Malite / Kwụsị / Malitegharị / Melite
- `ServiceLogsPanel` — ngwụcha log SSE (na-ejikọ na `/api/services/{name}/logs`)
- `ApiKeyCard` — ikpughe + ịgbanwe key (ọ bụrụ na `needsApiKey: true`)

Debanye taabụ ahụ na `ServicesPageShell.tsx`.

### Nzọụkwụ 7 — Tinye ndenye provider (ọ bụrụ na service ahụ bụ ebe routing na-aga)

Ọ bụrụ na embedded service ahụ na-ekpughe endpoint `/v1/chat/completions` nke dakọtara na OpenAI:

1. Tinye ndenye provider na `src/shared/constants/providers.ts` nke nwere `isEmbeddedService: true`.
2. Mepụta `open-sse/executors/{name}.ts` nke na-agbatị `BaseExecutor`. Chọọgharịa port na
   API key maka request ọ bụla (echekwala ha mgbe ọ bụla n’ime constructor). Weghachite nzaghachi
   `503 service_not_running` mgbe ọnọdụ supervisor abụghị `"running"`.
3. Debanye models na `open-sse/config/providerRegistry.ts` site na iji prefix nke service ahụ
   (dịka, `myservice/sub/model`). `modelSync.ts` ga-eme ka ha na-emelite mgbe niile.

### Nzọụkwụ 8 — Dee akwụkwọ ma nwalee

1. Melite `docs/frameworks/EMBEDDED-SERVICES.md` (faịlụ a) — tinye service ahụ na
   tebụl dị na §1 yana endpoints ọhụrụ ọ bụla na §4.
2. Tinye unit tests na `tests/unit/services/` (lifecycle, installer, ọdịdị API).
3. Tinye integration test na `tests/integration/services/` (n’azụ `RUN_SERVICES_INT=1`).
4. Melite `docs/openapi.yaml` site na endpoints ọhụrụ ahụ.

---

## 7. Nchọpụta na ndozi nsogbu

### Ọrụ anaghị amalite

**Mgbaàmà:** Bọtịnụ mmalite na-eweghachi 503, ọnọdụ na-anọgide na `"error"` ma ọ bụ `"starting"`.

**Ndepụta nyocha:**

1. Lelee `GET /api/services/{name}/logs` (ma ọ bụ ogwe Logs dị na dashboard). Chọọ
   ahịrị dịka `Error: ENOENT`, `address already in use`, ma ọ bụ `Cannot find module`.
2. Nyochaa na `npm` dị na PATH: jiri `which npm` site n'otu akaụntụ onye ọrụ ahụ na-agba OmniRoute.
3. Nyochaa na arụnyere ọrụ ahụ: lelee `GET /api/services/{name}/status` maka
   `installedVersion`. Ọ bụrụ na ọ bụ `null`, buru ụzọ mee nrụnye.
4. Lelee na `DATA_DIR/services/{name}/node_modules/` dị ma ọ bụghị ihe tọgbọ chakoo.
5. Lelee oghere `lastError` na nzaghachi ọnọdụ maka ihe kpatara nkwụsị ahụ nke e mere ka ọ dị nchebe.

---

### Mmalite oyi na-adị nwayọọ (> 10 s iji ruo `running`)

**Mgbaàmà:** Ọnọdụ na-anọgide na `"starting"` ogologo oge tupu ọ gaa na `"running"` ma ọ bụ `"error"`.

**Nkọwa:** Mmalite oyi nke 9Router gụnyere ibubata nnukwu osisi ndabere (DNS,
tunnel, modul MITM). Oge ndabara maka nyocha ahụike bụ 2 s, yana mbọ 3 tupu
supervisor ekwupụta na oge agwụla (mana ọ na-aga n'ihu na-enyocha).

**Ndozi:** Enwere ike ịhazi oge nkwụsị `healthIntervalMs` na `waitForHealthy`
(`healthIntervalMs * 3`) na `bootstrap.ts`. Maka ọrụ ndị na-ewe ogologo oge
ịmalite, bulie `healthIntervalMs` ruo 5000 na `stopTimeoutMs` ruo 30 000.

---

### Esemokwu ọdụ ụgbọ (`EADDRINUSE`)

**Mgbaàmà:** Logs na-egosi `address already in use :::20130`.

**Ihe kpatara ya:**

- Usoro ọzọ na-eji ọdụ ụgbọ 20130 ugbu a.
- Usoro 9Router gara aga akwụsịghị kpamkpam (PID zombie).

**Ndozi:**

1. Gbanwee ọdụ ụgbọ ndabara site na mgbanwe gburugburu `NINEROUTER_PORT` n'ime `.env`.
2. Chọta ma kwụsị usoro na-akpata esemokwu ahụ: `lsof -ti :20130 | xargs kill -9`.
3. Enwere ike ịhazi ọdụ ụgbọ maka ọrụ ọ bụla na `bootstrap.ts` site na oghere `port`.

**Rịba ama:** 9Router na-eji ọdụ ụgbọ 20130 na ndabara kpọmkwem iji zere esemokwu na
ọdụ ụgbọ ndabara 20128 nke OmniRoute.

---

### Ajụrụ ikike (EACCES) n'oge nrụnye

**Mgbaàmà:** Nrụnye na-eweghachi 500, logs na-egosi `EACCES` ma ọ bụ `permission denied`.

**Ihe kpatara ya:**

- Usoro OmniRoute enweghị ikike ide ihe na `DATA_DIR` ma ọ bụ ndekọ nne na nna ya.
- Ịgba ọsọ n'ime Docker rootless na-enweghị ikike ide ihe na volume e jikọtara.

**Ndozi:**

1. Lelee `DATA_DIR` (ndabara: `~/.omniroute/`): `ls -la ~/.omniroute/`
2. Gbaa mbọ hụ na onye ọrụ nke usoro OmniRoute bụ onye nwe ndekọ ahụ: `chown -R $USER ~/.omniroute/`
3. N'ime Docker, gbaa mbọ hụ na volume mount nwere ikike ziri ezi maka onye ọrụ container ahụ.

---

### Mmelite anaghị aga nke ọma (oge `npm install` agwụla ma ọ bụ njehie netwọkụ)

**Mgbaàmà:** Mmelite na-eweghachi 500 yana `InstallError`, logs na-egosi na oge netwọkụ agwụla.

**Ndepụta nyocha:**

1. Kwenye na enwere ike iru npm registry: `npm ping`.
2. Lelee ma e nwere proxy ụlọ ọrụ: `npm config get proxy`, `npm config get https-proxy`.
3. Gbalịa iji aka mee nrụnye ahụ: `npm install {package}@latest --prefix ~/.omniroute/services/{name}/`.
4. Ọ bụrụ na ị nọ n'azụ air-gap, buru ụzọ budata tarball ahụ ma jiri `npm install /path/to/tarball.tgz`.

---

### Ọrụ na-egosi ọnọdụ `"error"` ozugbo ọ malitechara (nkwụsị ngwa ngwa)

**Mgbaàmà:** Ọnọdụ na-agbanwe site na `"starting"` gaa na `"error"` n'ime ihe na-erughị sekọnd 5.
`lastError` na-egosi `"Fast crash (exited with code 1)"`.

**Ndepụta nyocha:**

1. Gụọ njedebe log zuru ezu: `GET /api/services/{name}/logs?tail=500`.
2. Ihe na-akpatakarị ya: mgbanwe gburugburu ndị ọrụ ahụ na-atụ anya adịghị.
3. Maka 9Router: nyochaa na `NINEROUTER_DISABLE_MITM=true` na
   `NINEROUTER_DISABLE_TUNNEL=true` dị na env ebufere n'oge spawn (lee
   `installers/ninerouter.ts` `resolveSpawnArgs`).

---

## 8. Ajụjụ ndị a na-ajụkarị

**Ajụjụ: Enwere m ike ime ka endpoint nke ọrụ ndị agbakwunyere dịrị ndị ahịa na-abụghị loopback?**

Mba. Ọkwa LOCAL_ONLY bụ ihe e mere ụma (iwu siri ike #17). Route ndị nwere ike ịkpọ
`npm install` ma ọ bụ ịmalite process `node` agaghị abụ ndị traffic na-abụghị loopback
nwere ike iru, n’ihi na JWT e kpughere site na tunnel (Cloudflare, Ngrok, Tailscale)
ga-eme ka e nwee ike ịmalite process ọ bụla. Enweghị ohere ịpụ na iwu a maka
`/api/services/` — n’adịghị ka `/api/mcp/`, ewepụla ya na ndepụta ndị nwere ike ịgafe
manage-scope. Lee `docs/security/ROUTE_GUARD_TIERS.md`.

---

**Ajụjụ: 9Router na CLIProxyAPI hà ga-adị na deployment production/cloud?**

Ee. Ọrụ abụọ ahụ na-agbaso otu model local-first ahụ OmniRoute n’onwe ya na-agbaso.
Ha na-arụ ọrụ n’otu igwe ma na-ekwurịta okwu site na loopback. “Production” n’ebe a
pụtara VPS ma ọ bụ sava mpaghara e tinyere OmniRoute na ya, ọ bụghị provider cloud
dịpụrụ adịpụ.

---

**Ajụjụ: Olee otu m ga-esi debug supervisor?**

1. Soro stream log SSE: `curl -N http://localhost:20128/api/services/9router/logs`.
2. Lelee log ahaziri ahazi na output pino nke OmniRoute, nke e ji namespace
   `service:supervisor` nyochaa.
3. Nyochaa row DB: `sqlite3 ~/.omniroute/omniroute.db "SELECT * FROM version_manager WHERE tool='9router'"`.
4. Jiri `GET /api/services/9router/status` hụ state dị ugbu a, PID, health,
   na `lastError` n’otu oku.

---

**Ajụjụ: Supervisor na-egosi `health: "degraded"` ma ọ bụ `health: "unknown"` mana state bụ `"running"`. Nke ahụ ọ bụ nsogbu?**

`"degraded"` pụtara na health probe weghachiri response na-abụghị 200. `"unknown"`
pụtara na ọ nweghị probe mechara (ọsọ dị n’etiti ya na poll mbụ). Ha abụọ na-adị
nwa oge mgbe startup na-eme. Ọ bụrụ na health anọgide na-abụ `"degraded"` ruo ihe
karịrị `healthIntervalMs * 3` ms mgbe ọ ghọrọ `"running"`, ọrụ agbakwunyere ahụ
na-arụ ọrụ mana HTTP API ya anaghị aza. Lelee ma port ahụ ò ziri ezi na status
response nakwa ma ọrụ ahụ ọ̀ na-ege ntị n’ezie na port ahụ.

---

**Ajụjụ: Enwere m ike ịgbanwe API key nke 9Router n’emeghị restart zuru ezu?**

Mba. A na-enyefe 9Router API key site na environment variable mgbe a na-amalite
process. A pụghị ịgbanwe environment variable n’ime process na-arụ ọrụ. `POST .../rotate-key`
na-akwụsị ma malitegharịa ọrụ ahụ na-akpaghị aka ka o jiri key ọhụrụ ahụ. Mgbanwe key
ahụ na-amalite ịrụ ọrụ n’ime `stopTimeoutMs` nke ọrụ ahụ (default bụ 15 s) gbakwunyere
oge startup ya.

---

**Ajụjụ: Gịnị bụ oke ring buffer, gịnịkwa na-eme mgbe o juputara?**

Ọrụ ọ bụla nwere ring buffer 5 MB nke ya. Mgbe buffer ahụ juputara, a na-ewepụ
ahịrị log ndị kacha ochie iji nye ndị ọhụrụ ohere. Event SSE `snapshot` na-eweghachi
ahịrị ndị kacha ọhụrụ n’ime oke `tail`. A naghị echekwa log na disk ọ gwụla ma
edobere `logsBufferPath` na row DB ahụ.

---

## Hụkwa

- `docs/security/ROUTE_GUARD_TIERS.md` — nkọwa gbasara ọkwa LOCAL_ONLY
- `docs/architecture/CODEBASE_DOCUMENTATION.md` — §3.2 mapping nke modul Ọrụ Ndị Agbakwunyere
- `docs/architecture/ARCHITECTURE.md` — ọnọdụ gbasara sistemụ n’ozuzu
- `docs/openapi.yaml` — nkọwa endpoint igwe nwere ike ịgụ
- `CLAUDE.md` §“Adding a New Embedded Service” — checklist ntụaka ngwa ngwa
