# Cursor API provider and the Cursor CLI passthrough (Italiano)

🌐 **Languages:** 🇺🇸 [English](../../../../providers/CURSOR-API-KEY-AND-CLI.md) · 🇪🇹 [am](../../../am/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇸🇦 [ar](../../../ar/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇦🇿 [az](../../../az/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇧🇬 [bg](../../../bg/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇧🇩 [bn](../../../bn/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇨🇿 [cs](../../../cs/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇩🇰 [da](../../../da/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇩🇪 [de](../../../de/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇬🇷 [el](../../../el/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇪🇸 [es](../../../es/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇪🇪 [et](../../../et/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇮🇷 [fa](../../../fa/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇫🇮 [fi](../../../fi/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇫🇷 [fr](../../../fr/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇮🇪 [ga](../../../ga/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇮🇳 [gu](../../../gu/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇳🇬 [ha](../../../ha/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇮🇱 [he](../../../he/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇮🇳 [hi](../../../hi/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇭🇷 [hr](../../../hr/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇭🇺 [hu](../../../hu/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇦🇲 [hy](../../../hy/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇮🇩 [id](../../../id/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇳🇬 [ig](../../../ig/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇯🇵 [ja](../../../ja/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇬🇪 [ka](../../../ka/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇰🇭 [km](../../../km/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇮🇳 [kn](../../../kn/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇰🇷 [ko](../../../ko/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇱🇹 [lt](../../../lt/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇱🇻 [lv](../../../lv/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇮🇳 [ml](../../../ml/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇮🇳 [mr](../../../mr/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇲🇾 [ms](../../../ms/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇲🇹 [mt](../../../mt/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇲🇲 [my](../../../my/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇳🇵 [ne](../../../ne/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇳🇱 [nl](../../../nl/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇳🇴 [no](../../../no/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇮🇳 [or](../../../or/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇮🇳 [pa](../../../pa/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇵🇭 [phi](../../../phi/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇵🇱 [pl](../../../pl/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇵🇹 [pt](../../../pt/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇧🇷 [pt-BR](../../../pt-BR/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇷🇴 [ro](../../../ro/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇷🇺 [ru](../../../ru/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇱🇰 [si](../../../si/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇸🇰 [sk](../../../sk/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇸🇮 [sl](../../../sl/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇷🇸 [sr](../../../sr/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇸🇪 [sv](../../../sv/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇰🇪 [sw](../../../sw/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇮🇳 [ta](../../../ta/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇮🇳 [te](../../../te/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇹🇭 [th](../../../th/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇹🇷 [tr](../../../tr/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇺🇦 [uk-UA](../../../uk-UA/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇵🇰 [ur](../../../ur/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇺🇿 [uz](../../../uz/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇻🇳 [vi](../../../vi/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇳🇬 [yo](../../../yo/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇨🇳 [zh-CN](../../../zh-CN/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇹🇼 [zh-TW](../../../zh-TW/docs/providers/CURSOR-API-KEY-AND-CLI.md)

---

Due modi per usare Cursor dietro OmniRoute senza una sessione IDE:

1. **Provider `cursor-api`** (scheda "Cursor API", alias `cua`): un provider con
   chiave API che conserva una chiave API utente Cursor (`crsr_…`, generata su
   `https://cursor.com/dashboard/api`). Qualsiasi client OmniRoute può quindi
   accedere ai modelli Cursor tramite `/v1/chat/completions` come
   `cursor-api/<model>` o `cua/<model>`, con i consueti livelli di quota,
   fallback e logging. Il provider IDE (`cursor`, sessione OAuth/IDE) rimane
   invariato.
2. **Passthrough della CLI Cursor**: configura la CLI Cursor (`agent`) affinché
   punti a OmniRoute, così ogni RPC effettuata dalla CLI viene autenticata con
   una chiave API OmniRoute, inoltrata a Cursor con le credenziali di una
   connessione `cursor-api` e registrata nella pagina Logs.

## Perché la chiave viene scambiata

`api2.cursor.sh` rifiuta una chiave `crsr_…` non elaborata come token Bearer
(401). La CLI Cursor invia prima la chiave tramite POST a
`/auth/exchange_user_api_key` e riceve un JWT di sessione che scade dopo un'ora;
il `refreshToken` restituito contiene lo stesso `exp`, quindi l'aggiornamento
richiede un nuovo scambio della chiave.
`open-sse/services/cursorApiKeyAuth.ts` esegue tale scambio, memorizza nella cache
un token di sessione per ogni chiave, effettua nuovamente lo scambio cinque
minuti prima della scadenza ed elimina il token memorizzato nella cache quando
Cursor risponde con 401. `CursorExecutor` lo richiama immediatamente prima di
aprire lo stream upstream per le connessioni `cursor-api`.

## Il provider `cursor-api`

Registro: `open-sse/config/providers/registry/cursor/index.ts`
(`cursor_apiProvider`, `authType: "apikey"`, stessi `format`, `baseUrl` e
`models` di `cursor`). Scheda del catalogo:
`src/shared/constants/providers/apikey/specialty-media.ts`. Mappa degli executor:
`open-sse/executors/index.ts` (`"cursor-api"` / `cua` →
`new CursorExecutor("cursor-api")`).

Dashboard: Providers → Cursor API → Add API key.

REST:

```bash
curl -sS -X POST http://localhost:20128/api/providers \
  -H "Content-Type: application/json" \
  -d '{"provider":"cursor-api","name":"cursor-api-key","apiKey":"crsr_…","priority":1}'
```

Quindi:

```bash
curl -sS http://localhost:20128/v1/chat/completions \
  -H "Authorization: Bearer <omniroute-api-key>" \
  -H "Content-Type: application/json" \
  -d '{"model":"cursor-api/auto","messages":[{"role":"user","content":"say PONG"}]}'
```

Note:

- L'elenco dei modelli per `cursor-api` proviene dal registro statico di Cursor
  (lo stesso elenco usato come fallback dal provider IDE); non è necessario
  installare `cursor-agent` sull'host OmniRoute.
- `POST /api/providers/{id}/refresh-cursor` è riservato al provider IDE
  `cursor`; le connessioni `cursor-api` non dispongono di una sessione IDE da
  rinnovare.

## ID nativi dei modelli e livello di effort

Per `cursor` / `cu` e `cursor-api` / `cua`, il normalizzatore condiviso
dell'effort di Claude mantiene invariato l'ID del modello richiesto. Cursor può
pubblicizzare un suffisso come `-low` come parte di un ID modello reale, anziché
come alias di effort di OmniRoute. L'executor Cursor mantiene una corrispondenza
esatta con il catalogo attivo; quando non vi è alcuna corrispondenza, il resolver
di modelli esistente gestisce il fallback dal suffisso al parametro.

Ciò non modifica la normalizzazione dell'effort per le route dirette Claude,
compatibili con Claude o Vertex. La disponibilità continua a dipendere dal
catalogo e dai diritti dell'account Cursor selezionato.

## Passthrough della CLI di Cursor

Route: `src/app/api/cursor-cli/[...path]/route.ts` →
`open-sse/handlers/cursorCliProxy.ts`. Il prefisso `/api/cursor-cli/` è
registrato in `src/shared/constants/publicApiRoutes.ts` perché l'handler
applica la propria autenticazione:

| Percorso                                                                                                                        | Autenticazione prevista dalla CLI | Operazioni eseguite da OmniRoute                                                                                                                                                                |
| ------------------------------------------------------------------------------------------------------------------------------- | --------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `POST /auth/exchange_user_api_key`                                                                                              | `Bearer <chiave API OmniRoute>`   | Convalida la chiave, genera un JWT HS256 valido 1 ora (firmato con `JWT_SECRET`) e lo restituisce                                                                                               |
| ogni altro percorso (`/aiserver.v1.*`, `/agent.v1.AgentService/RunSSE`, `/aiserver.v1.BidiService/BidiAppend`, `/v1/traces`, …) | `Bearer <quel JWT>`               | Verifica emittente/destinatario/scadenza, seleziona una connessione `cursor-api` attiva, sostituisce l'header Authorization con il token Cursor ottenuto e ritrasmette la risposta in streaming |

La CLI decodifica `exp` da qualsiasi token ricevuto, quindi fornirle un token
opaco la induce a ripetere lo scambio prima di quasi ogni richiesta; il JWT
generato evita questo problema. Una risposta 401 da OmniRoute fa sì che la CLI
esegua nuovamente lo scambio.

### Configurazione

1. Crea una chiave API OmniRoute (Dashboard → Chiavi API) e una connessione
   `cursor-api`.
2. Indica alla CLI di usare HTTP/1.1 per lo stream dell'agente. In
   `~/.cursor/cli-config.json`:

   ```json
   { "network": { "useHttp1ForAgent": true } }
   ```

   Senza questa impostazione, la CLI apre il turno dell'agente tramite HTTP/2
   verso un host dell'agente configurato separatamente e solo le RPC del piano
   di controllo passano attraverso l'endpoint.

3. Esegui la CLI tramite OmniRoute:

   ```bash
   export CURSOR_API_ENDPOINT=http://localhost:20128/api/cursor-cli
   export CURSOR_API_KEY=<chiave-api-omniroute>
   agent -p --trust "Rispondi esattamente con OK"
   ```

Ogni passaggio viene registrato nei log con provider `cursor-api`, tipo di
richiesta `cursor-cli`, percorso `/api/cursor-cli/<rpc>` e attribuito alla
chiave API OmniRoute e alla connessione che lo ha gestito.

### Modalità di errore

| Situazione                                            | Risposta alla CLI                                      |
| ----------------------------------------------------- | ------------------------------------------------------ |
| Chiave OmniRoute sconosciuta e `REQUIRE_API_KEY=true` | 401 `unauthenticated` durante lo scambio               |
| `REQUIRE_API_KEY=false`                               | sessione anonima (rispecchia il comportamento `/v1/*`) |
| JWT di sessione scaduto / esterno / manomesso         | 401, la CLI ripete lo scambio                          |
| Chiave API OmniRoute revocata dopo lo scambio         | 401 alla RPC successiva                                |
| Nessuna connessione `cursor-api` attiva               | 503 `unavailable`                                      |
| Cursor rifiuta la chiave della connessione            | 401 `unauthenticated`, sessione memorizzata eliminata  |
| Upstream non raggiungibile                            | 502 `unavailable` (messaggio sanificato)               |
| `JWT_SECRET` non impostato                            | 503 durante lo scambio                                 |
