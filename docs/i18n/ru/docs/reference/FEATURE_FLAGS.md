# Feature Flags (Русский)

🌐 **Languages:** 🇺🇸 [English](../../../../reference/FEATURE_FLAGS.md) · 🇪🇹 [am](../../../am/docs/reference/FEATURE_FLAGS.md) · 🇸🇦 [ar](../../../ar/docs/reference/FEATURE_FLAGS.md) · 🇦🇿 [az](../../../az/docs/reference/FEATURE_FLAGS.md) · 🇧🇬 [bg](../../../bg/docs/reference/FEATURE_FLAGS.md) · 🇧🇩 [bn](../../../bn/docs/reference/FEATURE_FLAGS.md) · 🇨🇿 [cs](../../../cs/docs/reference/FEATURE_FLAGS.md) · 🇩🇰 [da](../../../da/docs/reference/FEATURE_FLAGS.md) · 🇩🇪 [de](../../../de/docs/reference/FEATURE_FLAGS.md) · 🇬🇷 [el](../../../el/docs/reference/FEATURE_FLAGS.md) · 🇪🇸 [es](../../../es/docs/reference/FEATURE_FLAGS.md) · 🇪🇪 [et](../../../et/docs/reference/FEATURE_FLAGS.md) · 🇮🇷 [fa](../../../fa/docs/reference/FEATURE_FLAGS.md) · 🇫🇮 [fi](../../../fi/docs/reference/FEATURE_FLAGS.md) · 🇫🇷 [fr](../../../fr/docs/reference/FEATURE_FLAGS.md) · 🇮🇪 [ga](../../../ga/docs/reference/FEATURE_FLAGS.md) · 🇮🇳 [gu](../../../gu/docs/reference/FEATURE_FLAGS.md) · 🇳🇬 [ha](../../../ha/docs/reference/FEATURE_FLAGS.md) · 🇮🇱 [he](../../../he/docs/reference/FEATURE_FLAGS.md) · 🇮🇳 [hi](../../../hi/docs/reference/FEATURE_FLAGS.md) · 🇭🇷 [hr](../../../hr/docs/reference/FEATURE_FLAGS.md) · 🇭🇺 [hu](../../../hu/docs/reference/FEATURE_FLAGS.md) · 🇦🇲 [hy](../../../hy/docs/reference/FEATURE_FLAGS.md) · 🇮🇩 [id](../../../id/docs/reference/FEATURE_FLAGS.md) · 🇳🇬 [ig](../../../ig/docs/reference/FEATURE_FLAGS.md) · 🇮🇹 [it](../../../it/docs/reference/FEATURE_FLAGS.md) · 🇯🇵 [ja](../../../ja/docs/reference/FEATURE_FLAGS.md) · 🇬🇪 [ka](../../../ka/docs/reference/FEATURE_FLAGS.md) · 🇰🇭 [km](../../../km/docs/reference/FEATURE_FLAGS.md) · 🇮🇳 [kn](../../../kn/docs/reference/FEATURE_FLAGS.md) · 🇰🇷 [ko](../../../ko/docs/reference/FEATURE_FLAGS.md) · 🇱🇹 [lt](../../../lt/docs/reference/FEATURE_FLAGS.md) · 🇱🇻 [lv](../../../lv/docs/reference/FEATURE_FLAGS.md) · 🇮🇳 [ml](../../../ml/docs/reference/FEATURE_FLAGS.md) · 🇮🇳 [mr](../../../mr/docs/reference/FEATURE_FLAGS.md) · 🇲🇾 [ms](../../../ms/docs/reference/FEATURE_FLAGS.md) · 🇲🇹 [mt](../../../mt/docs/reference/FEATURE_FLAGS.md) · 🇲🇲 [my](../../../my/docs/reference/FEATURE_FLAGS.md) · 🇳🇵 [ne](../../../ne/docs/reference/FEATURE_FLAGS.md) · 🇳🇱 [nl](../../../nl/docs/reference/FEATURE_FLAGS.md) · 🇳🇴 [no](../../../no/docs/reference/FEATURE_FLAGS.md) · 🇮🇳 [or](../../../or/docs/reference/FEATURE_FLAGS.md) · 🇮🇳 [pa](../../../pa/docs/reference/FEATURE_FLAGS.md) · 🇵🇭 [phi](../../../phi/docs/reference/FEATURE_FLAGS.md) · 🇵🇱 [pl](../../../pl/docs/reference/FEATURE_FLAGS.md) · 🇵🇹 [pt](../../../pt/docs/reference/FEATURE_FLAGS.md) · 🇧🇷 [pt-BR](../../../pt-BR/docs/reference/FEATURE_FLAGS.md) · 🇷🇴 [ro](../../../ro/docs/reference/FEATURE_FLAGS.md) · 🇱🇰 [si](../../../si/docs/reference/FEATURE_FLAGS.md) · 🇸🇰 [sk](../../../sk/docs/reference/FEATURE_FLAGS.md) · 🇸🇮 [sl](../../../sl/docs/reference/FEATURE_FLAGS.md) · 🇷🇸 [sr](../../../sr/docs/reference/FEATURE_FLAGS.md) · 🇸🇪 [sv](../../../sv/docs/reference/FEATURE_FLAGS.md) · 🇰🇪 [sw](../../../sw/docs/reference/FEATURE_FLAGS.md) · 🇮🇳 [ta](../../../ta/docs/reference/FEATURE_FLAGS.md) · 🇮🇳 [te](../../../te/docs/reference/FEATURE_FLAGS.md) · 🇹🇭 [th](../../../th/docs/reference/FEATURE_FLAGS.md) · 🇹🇷 [tr](../../../tr/docs/reference/FEATURE_FLAGS.md) · 🇺🇦 [uk-UA](../../../uk-UA/docs/reference/FEATURE_FLAGS.md) · 🇵🇰 [ur](../../../ur/docs/reference/FEATURE_FLAGS.md) · 🇺🇿 [uz](../../../uz/docs/reference/FEATURE_FLAGS.md) · 🇻🇳 [vi](../../../vi/docs/reference/FEATURE_FLAGS.md) · 🇳🇬 [yo](../../../yo/docs/reference/FEATURE_FLAGS.md) · 🇨🇳 [zh-CN](../../../zh-CN/docs/reference/FEATURE_FLAGS.md) · 🇹🇼 [zh-TW](../../../zh-TW/docs/reference/FEATURE_FLAGS.md)

---

> Переключатели среды выполнения, которые изменяют поведение OmniRoute **без повторного развертывания**.
> Каждый перечисленный здесь флаг определен в
> [`src/shared/constants/featureFlagDefinitions.ts`](../../src/shared/constants/featureFlagDefinitions.ts)
> — единственном источнике истины. Панель управления и REST API считывают данные
> из этого файла, поэтому приведенная ниже таблица сформирована в точном соответствии с ним.

---

## Что такое флаги функций

Флаг функции — это именованный переключатель (логический или перечисляемый), значение которого можно изменять
во время выполнения и сохранять в базе данных без необходимости повторного развертывания процесса. Каждый
флаг описывается объектом `FeatureFlagDefinition` с полями `key`, `label`,
`description`, `category`, `defaultValue`, `type` и подсказкой `requiresRestart`.

### Порядок определения значения

**Эффективное значение** флага определяется функцией
[`resolveFeatureFlag()`](../../src/shared/utils/featureFlags.ts) со следующим
приоритетом (побеждает значение с наивысшим приоритетом):

1. **Переопределение в БД** — значение, хранящееся в таблице `key_value` в пространстве имен
   `feature_flags` (задается через панель управления или REST API).
2. **Переменная окружения** — `process.env[<KEY>]`, если она задана и не пуста.
3. **Значение по умолчанию из определения** — `defaultValue` из `featureFlagDefinitions.ts`.

Логический флаг считается **включенным**, если его эффективное значение равно `"true"`,
`"1"` или `"yes"` (см. `isFeatureFlagEnabled()`).

> [!NOTE]
> Большинство флагов также имеют соответствующую переменную окружения **с тем же именем**,
> задокументированную в [`ENVIRONMENT.md`](./ENVIRONMENT.md). Переопределение флага
> в БД имеет приоритет над этой переменной окружения. Флаг с
> `requiresRestart: true` сохраняется немедленно, но повторно считывается только при запуске
> процесса — его переключение приводит к появлению баннера **«Перезапустить сервер»** в панели управления.

---

## Каталог флагов

75 флагов в 6 категориях. **Значение по умолчанию** — это значение, заданное в определении и используемое, когда отсутствуют как переопределение в БД, так и переменная окружения.

### Безопасность (10)

| Ключ                                    | Тип     | По умолчанию | Описание                                                                                                                                                                                                                                                                                                                        |
| --------------------------------------- | ------- | ------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `REQUIRE_API_KEY`                       | boolean | `false`      | Требовать API-ключ для всех входящих запросов.                                                                                                                                                                                                                                                                                  |
| `INPUT_SANITIZER_ENABLED`               | boolean | `true`       | Включить очистку входных данных для всех запросов.                                                                                                                                                                                                                                                                              |
| `INJECTION_GUARD_MODE`                  | enum    | `off`        | Режим защиты от инъекций в промпты. Значения: `off`, `warn`, `block`, `redact`.                                                                                                                                                                                                                                                 |
| `PII_REDACTION_ENABLED`                 | boolean | `false`      | Редактировать персональные данные в запросах (независимо от `INPUT_SANITIZER_MODE`).                                                                                                                                                                                                                                            |
| `PII_RESPONSE_SANITIZATION`             | boolean | `false`      | Очищать ответы провайдера от персональных данных.                                                                                                                                                                                                                                                                               |
| `PII_RESPONSE_SANITIZATION_MODE`        | enum    | `redact`     | Режим очистки ответов от персональных данных. Значения: `redact`, `warn`, `block`, `off`.                                                                                                                                                                                                                                       |
| `OUTBOUND_SSRF_GUARD_ENABLED`           | boolean | `true`       | Блокировать исходящие запросы к приватным/внутренним диапазонам IP-адресов.                                                                                                                                                                                                                                                     |
| `ALLOW_API_KEY_REVEAL`                  | boolean | `false`      | Разрешить аутентифицированным пользователям панели управления просматривать сохранённые API-ключи вместо отображения только замаскированных значений.                                                                                                                                                                           |
| `AUTH_LOG_INCLUDE_ACCOUNT_ID`           | boolean | `false`      | Включать префикс учётной записи в строки журнала AUTH (например, «Используется учётная запись <provider>: abc12345...»). По умолчанию отключено, поэтому идентификаторы учётных записей скрываются в общих/мультитенантных журналах процессов. Не зависит от режима отладки; включение режима отладки не раскрывает эти данные. |
| `OMNIROUTE_OIDC_DISABLE_PASSWORD_LOGIN` | boolean | `false`      | Если OIDC включён, отключить вход по паролю, чтобы пользователи могли проходить аутентификацию только через единый вход OIDC. Если параметр отключён (по умолчанию), доступны как вход по паролю, так и OIDC.                                                                                                                   |

### Сеть (17)

| Ключ                                            | Тип     | По умолчанию | Перезапуск | Описание                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| ----------------------------------------------- | ------- | ------------ | ---------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `ENABLE_TLS_FINGERPRINT`                        | boolean | `false`      | ✓          | Включает скрытый режим с использованием TLS-отпечатка.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `AUDIO_REMOTE_PROVIDER_NODES`                   | boolean | `false`      |            | Разрешает маршрутам /v1/audio/* использовать совместимые с OpenAI узлы провайдеров, размещённые за пределами localhost. По умолчанию отключено — маршрутизация аудио на удалённый хост изменяет идентификатор исходящего подключения и должна быть явным решением оператора. Узлы loopback разрешены всегда, и этот параметр на них не влияет.                                                                                                                                                                                                                                                                   |
| `RERANK_REMOTE_PROVIDER_NODES`                  | boolean | `false`      |            | Разрешает POST /v1/rerank (а также этапу loopback-переранжирования механизма памяти) использовать совместимые с OpenAI узлы провайдеров, размещённые за пределами localhost. По умолчанию отключено — маршрутизация на удалённый хост изменяет идентификатор исходящего подключения и должна быть явным решением оператора. Узлы loopback разрешены всегда; удалённые узлы также должны соответствовать политике исходящих URL-адресов провайдера.                                                                                                                                                               |
| `PROXY_AUTO_SELECT_ENABLED`                     | boolean | `false`      |            | Если соединению не назначен прокси, автоматически выбирает первый работоспособный прокси из реестра. По умолчанию отключено (иначе любой прокси из реестра становится глобальным резервным вариантом — #3332).                                                                                                                                                                                                                                                                                                                                                                                                   |
| `OMNIROUTE_CONTROL_PLANE_PROXY_DIRECT_FALLBACK` | boolean | `false`      |            | Разрешает потокам OAuth и проверки провайдеров обходить закреплённый прокси и подключаться напрямую, если предварительная проверка доступности прокси завершается неудачно. По умолчанию отключено, поскольку это может изменить исходящий IP-адрес.                                                                                                                                                                                                                                                                                                                                                             |
| `NETWORK_ROTATION_SHARED_EGRESS_GUARD`          | boolean | `true`       |            | При сетевом исключении (тайм-аут, отказ или сброс соединения) в исполнителе ротации с несколькими учётными записями, если у проблемной учётной записи нет выделенного прокси, применяет короткий период ожидания и пропускает другие учётные записи без прокси до конца запроса вместо повторной попытки для каждой из них. По умолчанию включено (безопасно: исходящий IP-адрес не меняется, лишь снижается риск задержек и периодов ожидания для учётных записей с общим исходящим подключением). Отключите, чтобы восстановить немедленное распространение первого исключения от учётной записи без прокси.   |
| `PROXY_SKIP_RECENTLY_FAILED`                    | boolean | `false`      |            | Пулы прокси и ротация opencode для каждой учётной записи перестают повторно выдавать прокси, на котором только что произошёл сбой (отклонённая проверка TCP или ответ 429, полученный через него), на период в рамках процесса, который удваивается при каждом повторении вплоть до установленного предела. Статус прокси не записывается; если все кандидаты отложены, выбор остаётся неизменным. По умолчанию отключено.                                                                                                                                                                                       |
| `PROXY_POOL_EGRESS_OBSERVATION`                 | boolean | `false`      |            | Показывает на панели мониторинга под пулом прокси, сколько наблюдаемых исходящих IP-адресов обслуживали его участников за последние 24 ч и сколько соединений их использовали. Доступно только для чтения, вычисляется по журналу прокси и никогда не используется для маршрутизации. По умолчанию отключено.                                                                                                                                                                                                                                                                                                    |
| `OPENCODE_RESPONSES_STALL_ROTATION`             | boolean | `false`      |            | Для исполнителя OpenCode отслеживает первый байт тела потокового ответа Responses (окно: `RESPONSES_FIRST_BYTE_TIMEOUT_MS`, по умолчанию `15000`). Поток Responses с кодом 2xx, не передающий данные по истечении этого окна, считается зависшим: для учётной записи применяется период ожидания, а запрос один раз переключается на следующую учётную запись; при втором зависании происходит немедленный отказ. По умолчанию отключено: зависшие потоки продолжают ожидать в соответствии с текущим тайм-аутом готовности потока.                                                                              |
| `OPENCODE_USER_BLOCKED_ROTATION`                | boolean | `false`      |            | Исполнитель OpenCode: при ответе 403/451 с отказом `user_blocked` (не связанным с географическими ограничениями или отклонением цифрового отпечатка Cloudflare) временно исключить отклонённую учётную запись и переключиться на следующую не более одного раза за запрос; второй отказ возвращается без изменений, без отметки об успешном выполнении. По умолчанию отключено: обход блокировки пользователя на стороне вышестоящего сервиса может выглядеть как уклонение от ограничений и распространить флаг на весь пул.                                                                                    |
| `OPENCODE_TRANSIENT_FAILOVER_BACKOFF`           | boolean | `false`      |            | Ротация OpenCode: после двух последовательных временных сбоев вышестоящего сервиса (5xx или пустой ответ 400) приостановиться перед переходом к следующей учётной записи — 1,5 с с удвоением при каждом последующем сбое, максимум 6 с на паузу и 10 с на запрос; пауза пропускается при отключении клиента. Тело ответа со сбоем освобождается до ожидания. По умолчанию отключено: аварийное переключение выполняется немедленно.                                                                                                                                                                              |
| `OPENCODE_PARK_AND_RESUME`                      | boolean | `false`      |            | Ротация OpenCode: приостановить запрос после повторяющихся временных ответов 429 (или при наличии свежего маркера перегрузки пула), поддерживая соединение с помощью сигнала активности, а затем повторно выполнить один ограниченный этап, последовательно задействовав до 3 учётных записей, вместо параллельного обращения ко всему пулу. По умолчанию отключено: каждый ответ 429 приводит к переключению на следующую учётную запись, как и раньше.                                                                                                                                                         |
| `OPENCODE_RATE_LIMITED_429_EARLY_STOP`          | boolean | `false`      |            | Ротация OpenCode: остановить перебор учётных записей при первом ответе 429, классифицированном как фактическое ограничение частоты запросов (допускающий разбор `Retry-After` или тело ответа с упоминанием ограничения частоты/использования), и вернуть этот ответ 429 от вышестоящего сервиса без изменений. При неклассифицированных ответах 429 ротация продолжается. По умолчанию отключено: бесплатный уровень ограничен для каждого исходящего IP-адреса (#9611), поэтому каждый ответ 429 приводит к ротации, а при исчерпании всех вариантов возвращается последний ответ 429 от вышестоящего сервиса. |
| `MITM_DISABLE_TLS_VERIFY`                       | boolean | `false`      | ✓          | Отключить проверку TLS-сертификатов для MITM-прокси. **Опасно.**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `OMNIROUTE_ALLOW_PRIVATE_PROVIDER_URLS`         | boolean | `false`      |            | Разрешить URL-адреса провайдеров, указывающие на частные/внутренние сети.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `OMNIROUTE_ALLOW_LOCAL_PROVIDER_URLS`           | boolean | `true`       |            | Разрешить добавление/проверку провайдеров по локальным/частным адресам (127.0.0.1, localhost, LAN). Включено по умолчанию (приоритет локальной среды); отключите для строгой блокировки всех адресов, кроме общедоступных. Доступ к облачным метаданным остаётся заблокированным.                                                                                                                                                                                                                                                                                                                                |
| `ENABLE_CC_COMPATIBLE_PROVIDER`                 | boolean | `false`      | ✓          | Включить режим провайдера, совместимого с Claude Code.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |

### Политики (5)

| Ключ                            | Тип     | По умолчанию | Описание                                                                                                                                                                                                                                                             |
| ------------------------------- | ------- | ------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `TOOL_POLICY_MODE`              | enum    | `disabled`   | Режим применения политики использования инструментов. Значения: `disabled`, `warn`, `block`.                                                                                                                                                                         |
| `RATE_LIMIT_AUTO_ENABLE`        | boolean | `false`      | Автоматически включать ограничение частоты запросов на основе шаблонов использования.                                                                                                                                                                                |
| `DISABLE_CONTEXT_WINDOW_CHECKS` | boolean | `false`      | Пропустить локальную проверку OmniRoute размера контекстного окна / максимального количества входных токенов для прямых запросов к одной модели. Ограничения вышестоящего сервиса по-прежнему применяются.                                                           |
| `CAPABILITY_FILTER_ENABLED`     | boolean | `false`      | Отклонять запросы перед отправкой, если целевая модель не обладает необходимыми возможностями (зрение, инструменты, структурированный вывод, контекстное окно). Защищает прямые запросы к одному провайдеру, обходящие фильтр совместимости комбинированного уровня. |
| `RADAR_ENABLED`                 | boolean | `false`      | Включить модуль OmniRoute Radar (экраны ленты каталога и синхронизации). По умолчанию отключён; включение лишь разблокирует пользовательский интерфейс — синхронизация данных требует отдельного явного согласия.                                                    |

### Среда выполнения (33)

| Ключ                                        | Тип     | По умолчанию | Перезапуск | Описание                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| ------------------------------------------- | ------- | ------------ | ---------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `UNIVERSAL_CONTEXT_HANDOFF_ENABLED`         | boolean | `true`       |            | Создавать и добавлять сводки диалога при переключении моделей комбинированной маршрутизацией. Отключите, чтобы обрабатывать переключения моделей независимо и предотвратить фоновые запросы передачи контекста для всех существующих и будущих комбинаций.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `RESPONSES_PASSTHROUGH_DROP_COMMENTARY`     | boolean | `true`       |            | Удалять внутренние элементы вывода этапа комментариев из сквозных потоков Responses API перед их передачей клиентам. Отключите, чтобы получать необработанные комментарии от вышестоящего сервиса.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `OMNIROUTE_MCP_ENFORCE_SCOPES`              | boolean | `true`       |            | Применять ограничения областей доступа к инструментам MCP.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `OMNIROUTE_MCP_COMPRESS_DESCRIPTIONS`       | boolean | `false`      |            | Сжимать описания инструментов MCP для сокращения использования токенов.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `OMNIROUTE_ENABLE_RUNTIME_BACKGROUND_TASKS` | boolean | `false`      |            | Включить обработку фоновых задач во время выполнения.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `OMNIROUTE_DISABLE_BACKGROUND_SERVICES`     | boolean | `false`      | ✓          | Отключить все фоновые службы (обновление квот, синхронизацию и т. д.).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `OMNIROUTE_RTK_TRUST_PROJECT_FILTERS`       | boolean | `false`      |            | Доверять фильтрам RTK уровня проекта без проверки.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `OMNIROUTE_ENABLE_LIVE_WS`                  | boolean | `true`       | ✓          | Запускать при импорте WebSocket-сервер панели мониторинга в реальном времени (по умолчанию порт 20132).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `OMNIROUTE_CODEX_WS_ENABLED`                | boolean | `true`       |            | Разрешить Codex использовать транспорт Responses-over-WebSocket. Если отключено, Codex переключается на HTTP Responses.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `OMNIROUTE_CODEX_APP_SERVER_ENABLED`        | boolean | `true`       |            | Разрешить Codex использовать локальный транспорт app-server на основе WebSocket JSON-RPC (codexTransport=app-server). Если отключено, соединения, настроенные на app-server, переключаются на другие транспорты Codex.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `OMNIROUTE_EMERGENCY_FALLBACK`              | boolean | `true`       |            | Направлять запросы с исчерпанным бюджетом аварийному бесплатному резервному провайдеру/модели. (См. раздел [Аварийное переключение при исчерпании бюджета](#emergency-budget-fallback) ниже.)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `STREAM_RECOVERY_ENABLED`                   | boolean | `false`      |            | Включить прозрачную раннюю повторную попытку для усечённых входящих SSE-потоков до того, как клиенту будут переданы какие-либо байты ответа.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `STREAM_RECOVERY_MIDSTREAM_ENABLED`         | boolean | `false`      |            | Разрешить восстановление потока путём повторного запроса и объединения ответа после того, как байты уже были переданы клиенту.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `STREAM_RECOVERY_TOOLCALL_ORDER_FIX`        | boolean | `false`      |            | Сделать продолжение посреди потока безопасным для вызовов инструментов: никогда не возобновлять оборванный поток после отправки вызова инструмента (выполняющегося или уже завершённого с finish_reason tool_calls) и завершать работу после одного пустого продолжения вместо расходования всего бюджета. Если отключено: поведение релизной версии.                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `STREAM_EARLY_EOF_SIBLING_FAILOVER_ENABLED` | boolean | `false`      |            | Однократно переключаться на родственное соединение, если поток SSE закрывается до выдачи какого-либо полезного фрейма и ограниченное число повторных попыток в том же соединении исчерпано; если подходящего родственного соединения нет, возвращается исходная ошибка `STREAM_EARLY_EOF` 502. По умолчанию отключено: после повторной попытки в том же соединении ранний EOF остаётся терминальной ошибкой.                                                                                                                                                                                                                                                                                                                                                                                         |
| `MODEL_CATALOG_INCLUDE_NAMES`               | boolean | `true`       |            | Включать удобочитаемые поля имён в ответы `/v1/models`. Отключите для клиентов, которым нужны только идентификаторы моделей.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `MODELS_CATALOG_PREFIX_MODE`                | enum    | `dual`       |            | Управляет добавлением префиксов к идентификаторам моделей в /v1/models. Режим 'dual' (по умолчанию) выдаёт префиксы как псевдонима, так и канонического идентификатора провайдера для обратной совместимости. Режим 'alias' выдаёт только короткий префикс псевдонима (например, ds-web/model, а не deepseek-web/model). Режим 'canonical' выдаёт только полный префикс идентификатора провайдера. Значения: `dual`, `alias`, `canonical`.                                                                                                                                                                                                                                                                                                                                                           |
| `ARENA_ELO_SYNC_ENABLED`                    | boolean | `true`       |            | Включить периодическую синхронизацию ELO из рейтинговой таблицы Arena AI для ранжирования моделей по уровню интеллекта.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `EXPOSE_CC_DISCOVERY_ALIASES`               | boolean | `false`      |            | Публиковать зеркальные идентификаторы `claude/<provider>/<model>` в `/v1/models`, чтобы при обнаружении моделей шлюзом Claude Code в списке отображались модели, отличные от Claude. Глобальный уровень трёхуровневого механизма управления (переменная среды имеет приоритет над переопределением на панели управления). См. [Конфигурация Claude Code](../guides/CLAUDE-CODE-CONFIGURATION.md#discovery-aliases--surface-non-claude-models-in-the-model-picker).                                                                                                                                                                                                                                                                                                                                   |
| `NO_THINKING_ALIAS_ENABLED`                 | boolean | `true`       |            | Главный переключатель для псевдонимов шлюза no-think/<provider>/<model>. Включено (по умолчанию): /v1/models публикует вариант без рассуждений для каждой подходящей модели Claude с поддержкой рассуждений, а идентификатор no-think/, переданный в запросе, преобразуется обратно в реальную модель с отключёнными рассуждениями. Отключено: варианты не публикуются, а идентификатор no-think/ обрабатывается как любой другой неизвестный идентификатор модели. Индивидуальная настройка ModelSpec.noThinkingAlias для каждой модели продолжает действовать, пока этот параметр включён.                                                                                                                                                                                                         |
| `OMNIROUTE_DISABLE_THINKING_LEVEL_VARIANTS` | boolean | `false`      |            | Отключить генерацию вариантов уровней рассуждений (например, -low, -medium, -high) в каталоге /v1/models.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `OMNIROUTE_CHAT_VIRTUAL_LANES`              | boolean | `false`      | ✓          | Включить адаптивные виртуальные полосы допуска на уровне арендаторов для отправки запросов провайдеру (#9654): всплеск нагрузки одного арендатора больше не приводит к ошибкам 503 у другого. Переменная среды OMNIROUTE_CHAT_VIRTUAL_LANES имеет приоритет над этим переопределением на панели управления; изменения вступают в силу после перезапуска сервера.                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `EXPOSE_FUNCTIONAL_GATEWAY_MIRRORS`         | boolean | `false`      |            | Публиковать идентификаторы зеркал <gateway-alias>/<model> в /v1/models для моделей, у канонического владельца которых нет активных учетных данных, но которые маршрутизируются через сквозной шлюз с активными учетными данными. Предупреждение: при глобальном включении добавляет записи в каталог для всех клиентов.                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `NEWAPI_AGGREGATOR_BALANCE`                 | boolean | `false`      |            | Включить определение баланса для узлов, совместимых с агрегаторами New-API / One-API / Sub2API. Если параметр включен, совместимые узлы с установленным флагом агрегатора будут отображать свой баланс на панели управления и использовать его при предварительной проверке квоты во время маршрутизации.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `SERVER_OWNED_TOOL_LOOP_ENABLED`            | boolean | `false`      |            | Продолжать выполнение нестриминговых вызовов инструментов, управляемых сервером, пока модель не вернет ответ, пригодный для использования клиентом.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `SEARCH_STATS_HIDE_DELETED_CONNECTIONS`     | boolean | `false`      |            | В статистике поиска и недавних поисковых запросах учитывать только провайдеров, у которых все еще есть активное подключение (провайдеры без ключей, такие как duckduckgo-free, учитываются всегда). Если параметр выключен, сохраняются все строки поиска с идентификатором провайдера.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `FREE_BADGE_REQUIRES_PROVIDER_FREE_TIER`    | boolean | `false`      |            | Страницы провайдеров на панели управления: показывать значок Free только при наличии признаков, которые поддерживает провайдер, — не использовать эвристику на основе отображаемого имени, небулевы поля бесплатного доступа и суффиксы :free у зарегистрированных провайдеров без документированного бесплатного тарифа. Если параметр выключен, сохраняется прежнее правило отображения значка.                                                                                                                                                                                                                                                                                                                                                                                                    |
| `RETRY_AFTER_PROVENANCE_ENABLED`            | boolean | `false`      |            | В агрегированных ответах 429/503 о недоступности не включать `Retry-After`, если конкретное будущее время повторной попытки неизвестно (вместо синтетической задержки в 1 с), добавлять `error.retry_after_provenance` (`signal` \| `none`) и разрешить путям завершения комбинации считывать текстовые подсказки о повторной попытке из JSON и текстовых тел ответов вышестоящих сервисов. Поле появляется только в ответах, созданных функцией `unavailableResponse()`; остальные тела ответов 429/503 остаются без изменений.                                                                                                                                                                                                                                                                     |
| `PROTECTED_PRIORITY_INFRA_502_ENABLED`      | boolean | `false`      |            | Если цель комбинации `priority`, помеченная как резервная только при исчерпании квоты, останавливает комбинацию по причине, которая заведомо не связана с квотой (разомкнутый автоматический выключатель провайдера, пропуск из-за прогнозируемой задержки), возвращать ответ 502 вместо похожего на ошибку квоты ответа 503. При остановке из-за блокировки, периода ожидания, недоступности, исчерпания ресурсов или ограничения параллелизма по-прежнему возвращать 503.                                                                                                                                                                                                                                                                                                                          |
| `MISTRAL_AMBIGUOUS_401_SOFT_LOCKOUT`        | boolean | `false`      |            | Простой ответ Mistral 401 (`{"detail":"Unauthorized"}`, без явного сигнала об ошибке аутентификации) выглядит одинаково и для отозванного ключа, и для исчерпанной квоты. Если параметр включен, подключение переводится в период ожидания вместо пометки как `expired`, но не более 3 раз в час для каждого подключения; при следующем таком ответе оно помечается как неактивное, поэтому отозванный ключ в итоге все равно будет выявлен. По умолчанию параметр выключен: каждый простой ответ Mistral 401 помечает подключение как неактивное, как и прежде.                                                                                                                                                                                                                                     |
| `XAI_OAUTH_LIVE_MODEL_DISCOVERY`            | boolean | `false`      |            | Получать актуальный каталог моделей xAI для подключений `xai-oauth` с `https://api.x.ai/v1/models`, используя OAuth bearer-токен, вместо зафиксированного статического начального набора. По умолчанию отключено: `xai-oauth` продолжает предоставлять статический начальный набор без изменений. При любой ошибке разрешения обнаружение моделей возвращается к начальному набору (не проверено, принимает ли x.ai OAuth bearer-токен для этой конечной точки).                                                                                                                                                                                                                                                                                                                                     |
| `BATCH_AND_FILE_AUTO_CLEANUP_ENABLED`       | boolean | `false`      |            | Разрешить автоматической очистке удалять завершённые (completed/failed/cancelled/expired) задания Batch API старше `OMNIROUTE_BATCH_RETENTION_DAYS` вместе с их контрольными точками для каждой строки, а также очищать BLOB-содержимое загруженных файлов по истечении их собственного срока `expires_at`. По умолчанию отключено: во всех существующих установках эти данные сохраняются без изменений, пока оператор явно не включит эту функцию. Маршрут `DELETE /api/v1/batches/delete-completed`, запускаемый оператором, в любом случае не затрагивается — это отдельный безусловный контракт публичного API.                                                                                                                                                                                 |
| `ANTIGRAVITY_ACCOUNT_LEASE_ENABLED`         | boolean | `false`      |            | Резервировать выбранную учётную запись Antigravity на весь жизненный цикл потоковой передачи запроса, который её выбрал, чтобы параллельная повторная попытка или передача учётных данных не могли повторно выбрать учётную запись, уже назначенную активному потоку. Резервирование ограничено парой (подключение, вызываемая вышестоящая модель), поэтому одна учётная запись по-прежнему может одновременно обслуживать две разные модели. Если все подходящие учётные записи уже зарезервированы для этой модели, запрос возвращает структурированную ошибку 503 `antigravity_pool_busy` с ограниченным значением `Retry-After` вместо дополнительной нагрузки на занятую учётную запись. По умолчанию отключено: выбор учётной записи выполняется как прежде, и резервирование не производится. |

### CLI (5)

| Ключ                                  | Тип     | По умолчанию | Перезапуск | Описание                                                                                                                                                                                                                                                                  |
| ------------------------------------- | ------- | ------------ | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `CLI_COMPAT_ALL`                      | boolean | `false`      | ✓          | Включить режим совместимости для всех клиентов CLI.                                                                                                                                                                                                                       |
| `MODEL_ALIAS_COMPAT_ENABLED`          | boolean | `false`      |            | Включить слой совместимости псевдонимов моделей.                                                                                                                                                                                                                          |
| `PRICING_SYNC_ENABLED`                | boolean | `false`      |            | Включить автоматическую синхронизацию данных о ценах (также требуется переменная окружения `PRICING_SYNC_ENABLED`).                                                                                                                                                       |
| `OMNIROUTE_AUTO_SYNC_CODEX_PROFILES`  | boolean | `false`      |            | После синхронизации моделей провайдера автоматически (пере)записывать файлы профилей ~/.codex/*.config.toml из актуального каталога. Никогда не изменяет активную конфигурацию Codex или конфигурацию Codex по умолчанию. По умолчанию отключено.                         |
| `OMNIROUTE_AUTO_SYNC_CLAUDE_PROFILES` | boolean | `false`      |            | После синхронизации моделей провайдера автоматически (пере)записывать профили Claude Code ~/.claude/profiles/<name>/settings.json из актуального каталога. Никогда не изменяет активную конфигурацию Claude или конфигурацию Claude по умолчанию. По умолчанию отключено. |

### Работоспособность (5)

| Ключ                                      | Тип     | По умолчанию | Описание                                                                                                                                                                                                                                                                                                             |
| ----------------------------------------- | ------- | ------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `OMNIROUTE_DISABLE_LOCAL_HEALTHCHECK`     | boolean | `false`      | Отключает эндпоинт проверки работоспособности локального экземпляра.                                                                                                                                                                                                                                                 |
| `OMNIROUTE_DISABLE_TOKEN_HEALTHCHECK`     | boolean | `false`      | Отключает проверку работоспособности для валидации токена.                                                                                                                                                                                                                                                           |
| `SKILLS_SANDBOX_NETWORK_ENABLED`          | boolean | `false`      | Разрешает доступ к сети в изолированной среде навыков.                                                                                                                                                                                                                                                               |
| `PROXY_HEALTH_BLOCKED_RESETS_STREAK`      | boolean | `false`      | При проверке работоспособности прокси отказ целевого сервера (401/403/429) сбрасывает серию последовательных сбоев прокси. По умолчанию отключено: отказ остаётся нейтральным (#10654). Ответ 5xx в любом случае не позволяет сделать вывод; отказ никогда не удаляет, не отключает и не активирует повторно прокси. |
| `DB_HEALTHCHECK_STARTUP_DEFERRED_ENABLED` | boolean | `false`      | Выполняет проверку целостности и работоспособности БД при запуске после того, как сервер начинает принимать запросы (через `setImmediate`), вместо блокировки запуска до её завершения (#13717). По умолчанию отключено: запуск блокируется точно так же, как и до этого PR.                                         |

> [!NOTE]
> `INPUT_SANITIZER_BLOCK_THRESHOLD` и его устаревший псевдоним
> `INJECTION_GUARD_BLOCK_THRESHOLD` настраивают режим `block` для
> `INJECTION_GUARD_MODE`, но являются обычными переменными окружения, считываемыми
> в [`src/shared/utils/injectionSeverity.ts`](../../src/shared/utils/injectionSeverity.ts),
> а не флагами функций: их нельзя переопределить в БД, и для них нет переключателя на панели управления. См.
> [`ENVIRONMENT.md`](./ENVIRONMENT.md#4-security--authentication).

> [!NOTE]
> Столбец `Restart` отмечает флаги с `requiresRestart: true` — значение
> сохраняется мгновенно, но вступает в силу только после перезапуска процесса. Флаги
> перечислений отклоняют любые значения вне допустимого набора (валидация выполняется на стороне сервера
> как в `setFeatureFlagOverride()`, так и в REST-обработчике `PUT`).

---

## Переключение флагов

### Панель управления

Перейдите в раздел **Панель управления → Настройки → Флаги функций**
(`/dashboard/settings/feature-flags`). Таблица
(`src/app/(dashboard)/dashboard/settings/components/FeatureFlagsGrid.tsx`)
поддерживает:

- **Поиск** по ключу или описанию и **фильтрацию** по категории (а также синтетическое
  представление **Требуется перезапуск**).
- **Переключатель** для логических флагов и **раскрывающийся список** для флагов-перечислений
  (`src/app/(dashboard)/dashboard/settings/components/FeatureFlagCard.tsx`).
- **Значок источника** для каждого флага — `DB`, `ENV` или `DEF`, — показывающий, откуда
  было получено действующее значение.
- Кнопку **Сбросить** (отображается только для флагов с источником `DB`) для удаления переопределения
  и кнопку **Сбросить все переопределения** внизу.
- Баннер **Перезапустить сервер**, когда изменяется флаг `requiresRestart`.

### REST API

Все операции выполняются через единый маршрут:
[`src/app/api/settings/feature-flags/route.ts`](../../src/app/api/settings/feature-flags/route.ts).
Для каждого метода требуется аутентифицированный сеанс панели управления (в противном случае возвращается `401`).

#### `GET /api/settings/feature-flags`

Возвращает все флаги с их действующими значениями, источниками и сводной информацией.

```jsonc
{
  "flags": [
    {
      "key": "REQUIRE_API_KEY",
      "label": "Требовать ключ API",
      "description": "Требовать ключ API для всех входящих запросов",
      "category": "security",
      "type": "boolean",
      "enumValues": null,
      "defaultValue": "false",
      "effectiveValue": "false",
      "source": "default", // "db" | "env" | "default"
      "requiresRestart": false,
      "warningLevel": "caution",
    },
    // ... все 75 флагов
  ],
  "summary": {
    "total": 56,
    "active": 0,
    "inactive": 0,
    "overriddenByDb": 0,
    "overriddenByEnv": 0,
  },
}
```

#### `PUT /api/settings/feature-flags`

Устанавливает или удаляет одно переопределение. Тело: `{ key: string; value?: string }`.
Если `value` не указан, переопределение удаляется (восстанавливается значение из env / значение по умолчанию).

```bash
# Установить переопределение в БД
curl -X PUT http://localhost:20128/api/settings/feature-flags \
  -H "Content-Type: application/json" \
  -d '{"key":"REQUIRE_API_KEY","value":"true"}'

# Удалить переопределение (без "value")
curl -X PUT http://localhost:20128/api/settings/feature-flags \
  -H "Content-Type: application/json" \
  -d '{"key":"REQUIRE_API_KEY"}'
```

Ответ содержит новые значения `effectiveValue`/`source`, предыдущие значения `previousValue`/
`previousSource`, а также `requiresRestart`. Неизвестные ключи и значения перечисления
вне допустимого диапазона отклоняются с кодом `400`.

#### `DELETE /api/settings/feature-flags`

Удаляет **все** переопределения из БД за один раз, восстанавливая для каждого флага значение из env /
значение по умолчанию. Возвращает `{ cleared: <count>, message: "..." }`.

> [!NOTE]
> Флаги с `requiresRestart: true` вступают в силу только после перезапуска процесса.
> Процедура перезапуска из панели управления вызывает `POST /api/restart`, а затем опрашивает
> `GET /api/health/ping`, пока сервер снова не станет доступен.

---

## Аварийный резерв при исчерпании бюджета

`OMNIROUTE_EMERGENCY_FALLBACK` (категория `runtime`, значение по умолчанию `true`) управляет
аварийным путём бесплатного резервирования в
[`open-sse/services/emergencyFallback.ts`](../../open-sse/services/emergencyFallback.ts).
Когда он включён, запросы, исчерпавшие свой бюджет, направляются к бесплатному резервному
провайдеру/модели вместо немедленного завершения с ошибкой. Установите значение `false` (или `0`) — через
переключатель на панели управления, переопределение в БД или переменную окружения
`OMNIROUTE_EMERGENCY_FALLBACK` — чтобы отключить это поведение и позволить запросам с исчерпанным
бюджетом завершаться с ошибкой. (Добавлено как переключатель на панели управления в PR #3741 / #3752.)

---

## См. также

- [Справочник переменных окружения](./ENVIRONMENT.md) — для большинства флагов
  там задокументирована одноимённая переменная окружения (переопределение в БД
  имеет над ней приоритет).
- [`src/shared/constants/featureFlagDefinitions.ts`](../../src/shared/constants/featureFlagDefinitions.ts)
  — единый источник достоверной информации обо всех флагах.
- [`src/shared/utils/featureFlags.ts`](../../src/shared/utils/featureFlags.ts)
  — логика разрешения (`resolveFeatureFlag`, `isFeatureFlagEnabled`,
  `resolveAllFeatureFlags`).
- [`src/lib/db/featureFlags.ts`](../../src/lib/db/featureFlags.ts) — хранение
  переопределений БД в пространстве имён `feature_flags` таблицы `key_value`.
