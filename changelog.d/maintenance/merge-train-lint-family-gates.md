- **ci:** `scripts/release/merge-train.sh` now runs the cheap half of the `ci.yml:lint` family on the combined tree (migration-numbering, env-doc-sync, route-validation, db-rules, vitest-exclusions, tracked-artifacts, cycles, provider-consistency, error-helper, known-symbols) plus a FULL-only tier (agent-skills-sync, route-guard-membership, docs-counts, dashboard-typecheck) — the gates a 170-PR drain slipped past, leaving the release tip red on eleven checks no single PR could fail.
