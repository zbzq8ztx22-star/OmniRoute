- **fix(dashboard):** the **Free Stack ($0)** combo template inserted three Qoder model ids the registry no longer carries (`if/kimi-k2-thinking`, `if/qwen3-coder-plus`, `if/deepseek-v3.2`), so a combo created from it was born with dead targets and nothing failed until the user ran it — now `if/kimi-k2.7-code`, `if/qwen3.7-plus` and `if/deepseek-v4-pro`. Its description also still advertised Qwen, a provider in `RUNTIME_RETIRED_PROVIDER_IDS` that the preset never contained, and a Qoder count that was wrong; it now names what the preset actually routes across, in every locale. A new guard resolves every entry of both preset literals against the real registry (treating `passthroughModels` providers as always-resolving), which caught a second drift the report had only suspected: **Paid Premium** carried `cu/claude-4.6-opus-high`, and Cursor has no `claude-4.6-opus-*` at all — that generation is sonnet-only there — now `cu/claude-opus-5-high` ([#14077](https://github.com/diegosouzapw/OmniRoute/issues/14077))
