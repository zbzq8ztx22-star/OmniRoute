- **fix(call-logs):** chat-path `call_logs.id` is a fresh UUID (not the dashboard `traceId`), so two attempts in one request cannot collide or overwrite each other's artifact; an explicit-id UNIQUE collision still retries with a generated id ([#14451](https://github.com/diegosouzapw/OmniRoute/issues/14451)). Production `UNIQUE constraint failed: call_logs.id` reports from @VIPKaiser in [#14338](https://github.com/diegosouzapw/OmniRoute/issues/14338).
