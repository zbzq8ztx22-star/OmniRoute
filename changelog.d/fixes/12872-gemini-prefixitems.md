- **fix(gemini):** strip `additionalItems` (the draft-07 spelling of the tuple-typed array keyword also fixed for `prefixItems` in #12540) from tool parameter schemas — it triggered the same hard upstream 400 (`Unknown name "additionalItems"`), breaking tool-enabled Claude Code requests to Gemini models whose schemas still used the older spelling ([#12872](https://github.com/diegosouzapw/OmniRoute/pull/12872)) — thanks @sonirahul
