- **fix(build):** `OMNIROUTE_BUILD_PROFILE=minimal` builds again — the `ninerouter` and `mitm/cert/install` stubs now export every name real routes import (`getInstalledVersion`, `getLatestVersion`, `update`, `uninstall`, `installCertResult`), and the webpack stub replacement plugin resolves stub paths from the project root instead of the importing file's directory ([#12806](https://github.com/diegosouzapw/OmniRoute/issues/12806), [#12807](https://github.com/diegosouzapw/OmniRoute/issues/12807)).
