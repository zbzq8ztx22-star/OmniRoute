- **fix(providers):** Grok Build model discovery reads reasoning tiers when `/v1/models` returns objects (`{id, value, label, default}`) instead of bare strings, and keeps `xhigh` when the payload names it. grok-4.7 is in the built-in seed with the same four tiers as grok-4.6. A request that names no effort still leaves with the build default (`high`) on grok-4.7, not only on grok-4.5 and grok-4.6. A payload that omits the tier list still falls back to low/medium/high, so grok-4.5 does not gain an `xhigh` it does not advertise. ([#14525](https://github.com/diegosouzapw/OmniRoute/pull/14525)) — thanks @HouMinXi
