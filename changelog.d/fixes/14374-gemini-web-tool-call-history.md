- **fix(providers):** gemini-web carries the tool-call history into its next prompt. `buildGeminiToolPrompt()` sent only the system messages plus the last user turn, and because that adapter has no native message array the model never saw the call it had already answered or its result, so it re-issued the same tool call until the client's loop limit stopped it (#14368) — thanks @formilw
