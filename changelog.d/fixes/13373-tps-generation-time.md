- **fix(dashboard):** tokens-per-second now measures generation time (duration − TTFT) instead of wall-clock duration, and counting `tokens_reasoning` defensively when a provider excludes it from `completion_tokens` — thinking models (kimi k2.6/k2.7/k3) no longer look artificially slow in `/dashboard/logs`; TTFT is persisted per request and shown in the logs grid/detail and Playground TPS uses the same formula ([#13373](https://github.com/diegosouzapw/OmniRoute/pull/13373)) — thanks @ahmedhosnypro
