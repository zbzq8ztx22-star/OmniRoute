- **fix(mcp):** The "MCP Enforce Scopes" toggle on the Feature Flags page now takes effect without a restart; before, `OMNIROUTE_MCP_ENFORCE_SCOPES` was read from the environment only, so the page could show scope enforcement as on while every MCP tool call ran unchecked. The catalog default now says `false`, which is what the gate has always done ([#14459](https://github.com/diegosouzapw/OmniRoute/pull/14459))
