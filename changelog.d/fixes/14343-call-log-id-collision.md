- **fix(db):** call-log ids are now unique per call instead of per process. `generateLogId()` combined `Date.now()` with a module-level counter, so two instances of the writer - worker threads, separate route bundles - both starting at 0 produced identical ids in the same millisecond, and `call_logs.id` is `UNIQUE`, so the second row was dropped silently while the completion itself succeeded (#14338) — thanks @VIPKaiser
