- **Security — Obsidian integration (GHSA-474q-g63r-w4rr):** the operator-supplied `baseUrl` now goes through the outbound URL guard — cloud-metadata / link-local targets (`169.254.0.0/16`, IMDS hostnames, IPv4-mapped spellings) are rejected at `POST /api/settings/obsidian` before any fetch or persistence, every Obsidian REST call uses `safeOutboundFetch` with `block-metadata`, and redirects are never followed. Loopback, LAN and Tailscale bases keep working.
