- **fix(compression):** the compression worker pool no longer leaks request bodies when worker spawn fails synchronously (Turbopack standalone builds): every queued job fails open immediately and the broken pool short-circuits later runs, the fail-open is now logged (rate-limited), and the LLMLingua worker entry is passed as a URL object instead of a `file://` string (Node ≥ 21 rejects the latter) ([#12755](https://github.com/diegosouzapw/OmniRoute/pull/12755)).
