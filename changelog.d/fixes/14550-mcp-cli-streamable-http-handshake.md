- **fix(cli):** MCP-backed CLI commands (`compression`, `skills`, `resilience profile`, `oneproxy`, `combo suggest`, `mcp call`) no longer fail with `406 Not Acceptable` — both CLI MCP clients now advertise `application/json` and `text/event-stream` on every Streamable HTTP POST, and `mcpClient.mjs` forwards the issued `Mcp-Session-Id` to `tools/call` ([#14550](https://github.com/diegosouzapw/OmniRoute/pull/14550)) — thanks @Joaquinbrialva
