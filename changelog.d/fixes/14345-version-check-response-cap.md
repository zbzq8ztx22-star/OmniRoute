- **fix(dashboard):** the version lookups can now read the payloads the real sources send. `MAX_VERSION_RESPONSE_BYTES` was 16 KiB while the registry per-version document is 29,609 bytes and GitHub's latest-release payload is 154,002, so both HTTP fallbacks cancelled the body mid-read, swallowed the error and returned nothing - `/api/system/version` answered `"latest":"unavailable"` forever, and installs without an `npm` binary had no working source at all (#14339) — thanks @VIPKaiser
