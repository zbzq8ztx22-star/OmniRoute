- fix(sse): stop an intermittent empty Claude stream from emptying a whole combo — strip the client-only `safeguards` field Anthropic rejects outright, exempt OmniRoute's own synthesized empty-response 502 from the per-model lockout that was pulling healthy accounts out of rotation for 60s, and surface the real upstream streaming error instead of masking every failure as "Claude returned an empty response (no content block)"
