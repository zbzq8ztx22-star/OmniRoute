- **fix(build):** repair `better-sqlite3` native prebuild resolution for standalone packaging — detect musl (Alpine) and Android/Termux runtimes and try a 4-candidate resolution order (bundled app build, bundled app prebuild, root build, root prebuild) instead of a single platform/arch match before falling back to `npm rebuild` ([#12961](https://github.com/diegosouzapw/OmniRoute/pull/12961)) — thanks @raheemuddin786
