- **fix(runtime):** `omniroute repair`/`runtime check` now work on npm 11+, which hard-rejects the `--allow-scripts=<pkg>` CLI flag for project-scoped installs — the runtime's own `package.json` now declares `allowScripts` instead (retrofitted into existing installs automatically). Also fixed `isBetterSqliteBinaryValid()` only checking the `build/Release/` layout and reporting `valid: false` for a genuinely working binary installed under `prebuilds/<platform>-<arch>.node`; it now shares the same platform/arch detection `doctor` already used, so the two commands can no longer disagree about the same install. npm install failures during repair also now surface the actual npm error instead of being silently swallowed ([#14355](https://github.com/diegosouzapw/OmniRoute/issues/14355)) — thanks @ogunlowoaamos
