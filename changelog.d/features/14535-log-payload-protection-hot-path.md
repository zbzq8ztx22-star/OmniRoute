- **perf(logging):** stop walking whole request bodies several times per request when PII sanitization is off: feature flag overrides are cached instead of one SQLite `SELECT` per string leaf, `sanitizePayloadPII` returns the payload by reference when the flag is off, pending-request previews are truncated before they are protected, and the PII masker guardrail no longer JSON-clones every body. On 2 MB agentic bodies at 20 concurrent streams this removed ~12,800 flag lookups per request and cut TTFT p50 from 2.4 s to 1.7 s ([#14535](https://github.com/diegosouzapw/OmniRoute/pull/14535)) — thanks @dpozimski
