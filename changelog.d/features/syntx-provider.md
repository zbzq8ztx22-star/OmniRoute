- feat(providers): **Added SYNTX.ai (`syntx` / `stx`) as an unofficial/experimental JWT provider. Chat reuses `chat_uuid` across OpenAI multi-turn history, PUT `/api/v1/user/settings` for a 4k `system_prompt.default`, and keeps compact generate on `tools:[]`. Media covers image, video, speech, music, transcription, and upscale. Live `/api/v1/llm/models` discovery, Limits via subscription + rolling-window quotas, JWT validation, and a 10-minute stream-readiness window.**
