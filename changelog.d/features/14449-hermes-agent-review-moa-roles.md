- **feat(cli-tools):** assign models to the review and Mixture-of-Agents roles from the dashboard ([#14449](https://github.com/diegosouzapw/OmniRoute/pull/14449)) — the Hermes agent already routes work to a dedicated review model and to the MoA reference/aggregator pair, but the config generator had no slots for them, so picking a different model meant hand-editing a file the dashboard would overwrite on the next save. The roles are now first-class and translated across all locales.
