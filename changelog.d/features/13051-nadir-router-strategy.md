- **feat(auto-combo):** new `nadir` auto-router strategy (`config.routerStrategy: "nadir"`) asks [Nadir](https://getnadir.com)'s decision API which model in the candidate pool the prompt needs and routes to it, leaving the connection choice to `rules`; opt-in, fail-open with a 30 s cooldown after failures, only the last user turn and the model ids leave the box ([#13051](https://github.com/diegosouzapw/OmniRoute/pull/13051)) — thanks @doramirdor
